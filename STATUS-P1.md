# STATUS-P1 — Feature Development

**Developer:** Программист 1 (Feature development — any subsystem from roadmap)

---

## In progress

_(нет активных задач)_

---

## Next

> **Состояние очереди (ревизия 2026-06-21):** вся приоритетная очередь P1 из *ограниченных*
> задач закрыта. USABILITY-вертикаль (U-0…U-6) и слои 0–4 «полного плана» — готовы
> (BUG-167 FIXED; BUG-104/126/144/085 — основные дефекты закрыты, остатки в KNOWN_DEBTORS).
> «Направления без брифа» проверены по коду и в основном уже реализованы (Canvas `<img>`
> drawImage, DevTools RequestFailed, CMYK-JPEG). **Реально неготовое = только XL/опциональное:**
> U-4c **завершён полностью** (Stage 1 ✅ / Stage 2 ✅ / Stage 3 ✅ 2026-06-21 — adapter info +
> WGSL-валидация, буферы, compute-dispatch, render-пайплайны + render-в-текстуру + readback +
> **present в страничный `<canvas>`** — всё на живом GPU). Остаётся только Color management через
> ICC (полноценный CMM, Lab PCS + CMYK-профили). **Объём согласован с пользователем 2026-06-21:
> RGB + CMYK, разбит на 6 сессий ICC-1…ICC-6 — см. «Слой 6 — ICC Color Management» ниже.**
> ICC-1…ICC-6 ✅ 2026-06-21 (RGB matrix-shaper + CMYK A2B-LUT + кэш на этапе декода + graphic_test/Rust-тесты/доки;
> попутно BUG-229: PNG iCCP инфлейтился как raw deflate вместо zlib → ICC в PNG не работал, исправлено).
> **Слой 6 ICC завершён полностью.** USABILITY-вертикаль + ICC закрыты — остаётся **закрыть саму Фазу 2**:
> довести до паритета оставшиеся roadmap-фичи P2 (см. ниже «Закрытие Фазы 2»), затем bump 0.2.0→0.5.0.

### Закрытие Фазы 2 — оставшиеся roadmap-фичи (режим одного программиста, всё на P1)

Это полный список незакрытых задач Фазы 2 из `docs/roadmap.json` (статусы там теперь
авто-выводятся генератором, см. `scripts/gen_roadmap.py`). Все CSS-фичи в обычном режиме —
домен P4, но в режиме одного программиста ведёт P1. **Все четыре CSS-фичи уже есть как
заглушки/частичная реализация** — задача «довести до паритета ≤0.5% с Edge», не «с нуля».
Каждая — отдельная ветка/worktree `p1-<topic>`. Порядок — по возрастанию объёма (быстрые
победы сначала); закрытие бага фиксировать в `BUGS.md` (домен фиксов остаётся за процессом P3,
но в single-dev режиме баги фич ведёт P1 вместе с фичей).

| # | Задача (roadmap id) | Размер | Баги | Точка в коде | Что сделать |
|---|---------------------|--------|------|--------------|-------------|
| ~~**F2-1**~~ | ✅ 2026-06-22 — **Motion Path L1** (`P2-motionpath`). BUG-125 FIXED: боксы садились top-left-на-путь вместо `offset-anchor`-на-путь (off by пол-бокса). Добавлен `T(-anchor)` в `forward_box_transform` + `PropertyTrees::walk` (offset-anchor `auto`→transform-origin=центр), поворот вокруг anchor. TEST-76 3.18%→0.54% (боксы пиксель-в-пиксель). Остаток = `calc()` в color-stop диагонального трека схлопывает градиент → **BUG-230** (домен градиентов), KNOWN_DEBTOR. См. «Recent merges». | M ✅ | BUG-125 FIXED, +BUG-230 | `property_trees.rs` (`forward_box_transform`/`walk`/`motion_anchor_px`) | — |
| **F2-2** | **Scroll-Driven Animations L1** (`P2-scrolldriven`) | L | BUG-127 (TEST-78 12.02%) | `layout/src/scroll_timeline.rs` (стаб), `collect_named_{scroll,view}_timelines` | `scroll-timeline`/`view-timeline` → привязка прогресса скролла к анимациям; стаб собирает именованные таймлайны — нужен runtime-драйв прогресса и проводка в анимации (`lumen-js`/animation runtime). |
| **F2-3** | **CSS Masonry** (`P2-masonry`) | L | BUG-105 (TEST-63 26.13%), BUG-143 (TEST-75 16.97%) | `box_tree.rs:7853` (masonry dispatch) | Диспетч `masonry` уже есть (`GridTrackSize::Masonry`, column/row) — починить placement (`masonry-auto-flow`), распределение по дорожкам, выравнивание. Два бага = два дефекта раскладки. |
| **F2-4** | **View Transitions API** (`P2-viewtrans`) | XL | BUG-103 (TEST-61 99.53%) | `js/src/view_transitions.rs`, `lib.rs:290` (`ViewTransitionEvent`) | Событийный каркас `startViewTransition` есть, но переход не рендерится (99.53% = пусто). Нужен снимок старого/нового состояния + кросс-фейд/морф + проводка `::view-transition*` псевдо в paint. Самая объёмная. |
| ~~**F2-5**~~ | ✅ 2026-06-22 — **Knowledge layer ядро** (`P2-knowledge`). Ревизия по коду: ядро уже было готово (FTS5 history/notes, Read-later store+search, OpenTabsIndex, focus mode + Pomodoro `Ctrl+Shift+F`). Единственный незакрытый разрыв — omnibox-префиксы поиска: добавлены `@read-later <query>` (FTS5 по сохранённым → навигация на URL) и `@tabs <query>` (substring по заголовку+URL открытых вкладок → `switch-tab:<id>` → `switch_tab`). Стора `.search()` уже существовали; проводка в `query_omnibox_suggestions`/`handle_omnibox_commit` + 7 тестов. Russian Porter stemmer + vector/HNSW index — остаются ⬜ (опционально, вне «ядра»). См. «Recent merges». | M ✅ | — | `address_bar.rs`, `main.rs` | — |
| **F2-6** | **Кастомизация UI** (`P2-ui`) | L | — | `lumen-shell` (panels/, tabs/) | Drag&drop панелей + темы. Чисто-фичевая задача без багов; объём — UI-инфраструктура шелла. |
| **F2-7** | **Закрытие фазы: bump 0.2.0 → 0.5.0** | S | — | `Cargo.toml` `[workspace.package].version`; метка фазы в `README.md`, `docs/plan/phases.md`, `CLAUDE.md`, баннер шелла | Финальный шаг после F2-1…F2-6: поднять версию до 0.5.0 (Phase 2 target по политике версий), синхронизировать фазовую метку во всех источниках. Только когда все цели Фазы 2 достигнуты. |

**Зависимости:** F2-1…F2-6 независимы (можно в любом порядке/параллельно). F2-7 — строго последний.
**Минимум «Фаза 2 закрыта»:** F2-1…F2-6 доведены до паритета ≤0.5% (или запаркованы как KNOWN_DEBTORS с OPEN-багом), затем F2-7.
**Как мерить:** `python graphic_tests/run.py --build --only <NN>` для CSS-фич (TEST-76/78/63/75/61), `--screenshot` живых сайтов для knowledge/UI.

### Build/test инфраструктура

| # | Задача | Owner | Размер | Крейты / точки в коде |
|---|--------|-------|--------|----------------------|
| ~~**BT-1**~~ | ✅ 2026-06-21 — консолидированы 64 отдельных тест-бинаря `crates/driver` в один (`tests/all.rs` → `mod cases;`, 63 файла-теста переехали в `tests/cases/` как подмодули; неиспользуемый `common.rs` удалён). **Замер на этой машине (rust-lld линковщик): сборка тест-бинарей 1м50с → 7.6с (~14×)**; полная `cargo test --workspace` больше не упирается в 64 линковки полного движка. 113 default-тестов + feature-gated (`snapshot_cpu` под `cpu-render`, 6×`compare_backends` под `compare-backends`) — счёт тот же, что до консолидации. `#![cfg(feature=…)]` в двух файлах работает как gate подмодуля без правок. `include_bytes!` в `snapshot_vs_edge` углублён на один `../`. Найден предсуществующий красный тест → BUG-228 (P3). См. «Recent merges». | P1 | M ✅ | `crates/driver/tests/` |

### USABILITY-вертикаль — «зашёл на сайт и комфортно пользуешься» (ТОП-приоритет)

Контекст (анализ 2026-06-18): список фич в `CAPABILITIES.md` широкий (~90 Web-API,
сеть/layout работают на живых сайтах — github.com/HN грузятся через `--dump-layout`),
но **вертикаль повседневного использования разорвана**. Эти задачи закрывают разрыв; они
важнее экзотики из PH3. Owner проставлен — часть передаётся P2/P3/P4 (см. их STATUS).

| # | Задача | Owner | Размер | Крейты / точки в коде |
|---|--------|-------|--------|----------------------|
| **U-0** | ~~**`--screenshot <out.png> <url>` headless CPU-снимок**~~ ✅ завершена (2026-06-18) — инструмент наблюдения: честная картинка любого сайта без окна/Edge/ffmpeg. См. «Recent merges». | P1 | S | `lumen-shell` |
| **U-1** | **Неблокирующая загрузка страницы** — ~~этап 1~~ ✅ (2026-06-18): навигация (клик, адресная строка, back/forward, JS `location.href=`, reload) больше не мёрзнет — `reload()` идёт через тот же async streaming-пайплайн, что и первичная загрузка (`start_streaming_load`), окно рисует промежуточные кадры, тяжёлый `render_bytes` исполняется один раз в `LoadDone`. Добавлены load-generation guard (отброс устаревших событий гонки навигаций) и `pending_restore_scroll` (восстановление scroll для back/forward при асинхронном reload). ~~этап 2~~ ✅ 2026-06-19 (BUG-171): весь финальный pipeline (включая QuickJS) вынесен с UI-потока — `LoadEvent::LoadDone` спавнит worker с `render_bytes`, готовый результат применяется на UI через `RenderDone`/`apply_loaded_page`. Разблокировано B-1 (QuickJS — `Send`-хэндл). | P1 | XL ✅ | `lumen-shell` `main.rs` (`reload`/`start_streaming_load`/`apply_loaded_page`/`RenderDone`); `lumen-network` |
| **U-2** | **Шейпинг текста (GSUB/GPOS) + CFF-контуры.** ~~Этап 1: GPOS-кернинг + GSUB-лигатуры~~ ✅ 2026-06-18. ~~Этап 2: CFF-контуры~~ ✅ 2026-06-18 (`lumen-font::cff` — Type 2 charstrings, CID-keyed CFF, проводка через `Font::glyph_resolved` → CPU/wgpu/Canvas рисуют `.otf`-текст). См. «Recent merges». Опционально позже: CFF2 (variable PostScript), проводка шейпинга в femtovg live-окно и per-char measurement. | P1 | XL ✅ | `lumen-font` (`cff.rs`) |
| **U-3** | **Настоящая многовкладочность** — TAB-серия ниже. **Бо́льшая часть уже в коде** (ревизия 2026-06-18): TAB-1/2/3/6 реализованы (`PageSnapshot`+`bg_tabs`, `switch_tab`/`open_new_tab`/`close_tab`, tab-bar UI, hibernation, Ctrl+T/W/Tab). TAB-4 ✅ / TAB-5 ✅ (IPC, 2026-06-18). TAB-7 ✅ (run.py `--ipc`, 2026-06-18). | P1 | см. TAB | `lumen-shell` |
| **U-4** | ~~**WASM-исполнение + закрыть бросающие JS-заглушки**~~ ✅ 2026-06-18. Этап 1: pure-Rust интерпретатор WASM MVP (`lumen-js::wasm`) — `compile`/`validate`/`instantiate` исполняют реальный байткод, экспортируемые функции вызываемы, память/глобалы/таблицы/`call_indirect`/JS-импорты. Этап 2: WebCodecs `configure()` больше не бросает синхронно (white-screen) — async error-callback. См. «Recent merges». Опц. позже: SIMD/threads, live-aliasing памяти, ~~i64 BigInt-маршалинг~~ ✅ 2026-06-18, реальный WebGPU backend. | P1 | XL ✅ | `lumen-js` (`wasm/`, web_codecs) |
| **U-5** | ~~**Fullscreen пересчитывает вьюпорт**~~ ✅ 2026-06-19 (BUG-167) — `set_fullscreen` применяет размер асинхронно; `arm_fullscreen_resize`/`poll_fullscreen_resize` ждут смены `inner_size()` и прогоняют resize+relayout. См. «Recent merges». | P1 | S | `lumen-shell` `main.rs` |
| **U-6** | **Рендер-паритет high-deviation** — добить заметные отклонения: repeating-gradient (BUG-085), filter/backdrop (BUG-144), anchor-positioning (BUG-126 53%), scroll-snap (BUG-104 64%). | P1 | — | см. `BUGS.md` |

**Порядок для P1:** U-0 ✅ → U-1 этап 1 ✅ → U-3 (TAB-серия) ✅ → U-2 этап 1 (GSUB/GPOS шейпинг) ✅ → U-2 этап 2 (CFF-контуры) ✅ → U-4 (WASM MVP + graceful stubs) ✅ 2026-06-18 → U-4 опция i64/BigInt-маршалинг ✅ 2026-06-18 → **далее: оставшиеся U-4 опции (SIMD/threads, live-aliasing памяти, реальный WebGPU backend). Унаследованные от P2 задачи — все четыре уже в коде (ревизия 2026-06-18), см. ниже.** U-1 этап 2 (пайплайн вне UI) ✅ 2026-06-19 (BUG-171).

**Как мерить прогресс:** после каждого этапа снимай `--screenshot` 5–10 живых сайтов (github, новостной, блог, SPA) + `python graphic_tests/run.py --build --continue-on-fail` для CSS-паритета.

---

### USABILITY — полный план закрытия (режим одного программиста, всё на P1)

С 2026-06-19 переходим на путь одного программиста: блокеры, баги (бывшие домены P3/P4)
и опции — **все на P1**. Это исчерпывающий список, чтобы закрыть потребность
«зашёл на сайт и комфортно пользуешься». Брать строго сверху вниз по слоям; внутри слоя —
по убыванию заметности. Каждая задача — отдельная ветка/worktree (`p1-<topic>` / `p1-bug-NNN`).

**Слой 0 — Фундамент (разблокировка U-1 этап 2):**

| # | Задача | Размер | Точка | Зависит |
|---|--------|--------|-------|---------|
| ~~**B-1**~~ | ✅ 2026-06-19 (ADR-014) — `QuickJsRuntime` стал хэндлом: выделенный JS-поток владеет `Runtime`/`Context`, доступ через `run(f)` (командный канал + блокирующий ответ). Удалены несостоятельные `unsafe impl Send/Sync`; хэндл по-настоящему `Send + Sync` (`SyncSender`). 22 точки рантайма сконвертированы; `pointer_lock`/`file_input` thread_local → глобальный `Mutex`. См. «Recent merges». | XL ✅ | `lumen-js` | — |
| ~~**BUG-222**~~ | ✅ 2026-06-19 — закрыт вместе с B-1: `js_thread_main` зовёт `wasm::clear_registry()` в teardown JS-потока до дропа `Runtime`. | S ✅ | `lumen-js` | B-1 |

**Слой 1 — U-1 этап 2 (неблокирующий пайплайн, зависит от B-1):**

| # | Задача | Размер | Точка | Зависит |
|---|--------|--------|-------|---------|
| ~~**BUG-171**~~ | ✅ 2026-06-19 — весь финальный pipeline вне UI-потока. `LoadEvent::LoadDone` спавнит `std::thread` с `render_bytes` (fetch скриптов → QuickJS → fetch+декод картинок/CSS/шрифтов → layout); результат прилетает как `LoadEvent::RenderDone(Box<RenderOutcome>, gen)` и применяется на UI через `apply_loaded_page`. Окно отзывчиво всю CPU-фазу. Разблокировано B-1 (`PersistentJs: Send`, JS-хэндл создаётся на рендер-потоке). См. «Recent merges». | L ✅ | `main.rs:7322` | B-1 |
| ~~**BUG-172**~~ | ✅ 2026-06-19 — общий per-load кэш `image_cache::IMAGE_CACHE` (`DecodedImageCache`, generation-scoped + in-flight дедуп по образцу `prefetch`); streaming-загрузчик и финальный `fetch_and_decode_images` декодируют через единый `decode_image` → каждая `<img>` качается+декодируется один раз. См. «Recent merges». | S ✅ | `image_cache.rs` | — |

**Слой 2 — U-5 (Fullscreen, независимо):**

| # | Задача | Размер | Точка |
|---|--------|--------|-------|
| **BUG-167** | Вход в Fullscreen растягивает окно, но страница остаётся ~1024×720 — resize от `set_fullscreen` не доводится до relayout. | S | `main.rs:6400` |

**Слой 3 — U-6 (рендер-паритет high-deviation, независимо, по убыванию отклонения):**

| # | Задача | Откл. | Крейт |
|---|--------|-------|-------|
| ~~**BUG-104**~~ | ✅ 2026-06-19 — TEST-62 63.7%→2.32%: реальная причина не scroll snap, а column flex-grow (не распределял free space). См. «Recent merges». Остаток → KNOWN_DEBTORS (BUG-128 font-parity). | критич. ✅ | `layout` |
| ~~**BUG-126**~~ | ✅ 2026-06-19 — TEST-77 53.45%→12.94%: position-area definite-size элементы прижимаются к якорю вместо растягивания (anchor.rs `place_axis`/`align_in_band`). 3×3 сетка = Edge(position-area) пиксель-в-пиксель. Остаток → KNOWN_DEBTORS (тест использует устаревшее `inset-area`, Edge игнорирует; span-ряд Lumen спек-корректнее). См. «Recent merges». | критич. ✅ | `layout` |
| **BUG-144** | ✅ основной дефект 2026-06-20 — TEST-30 7.56%→4.36%: backdrop-filter colour-matrix/combo карты больше не тёмные (CPU backdrop-пайплайн в `apply_backdrop_filters`, мёртвый filter_image-readback убран). Все 6 backdrop-карт рендерят отфильтрованный контент. См. «Recent merges». Остаток → KNOWN_DEBTORS: box-blur≈Gaussian + edge-bleed на двух blur-картах. | средн. ✅ | `paint` |
| ~~**BUG-085**~~ | ✅ 2026-06-19 — TEST-39 12.05%→1.62%: femtovg игнорировал `repeating` и clamp-ил стопы, не дозаполняя хвост за последним стопом. `femtovg_stops`: non-repeating продлевает последний цвет до 1.0 (hard-stop), repeating замощает паттерн по [0,1]. Row 1 = Edge пиксель-в-пиксель. Остаток → KNOWN_DEBTORS (256-тексельная квантизация femtovg). См. «Recent merges». | средн. ✅ | `paint` |

**Слой 4 — Наблюдаемость (честное зеркало окна для «как мерить прогресс»):**

| # | Задача | Размер | Крейт |
|---|--------|--------|-------|
| ~~**BUG-221**~~ | ✅ 2026-06-20 — Паритет CPU-бэкенда снимка с femtovg по всем трём: TEST-36 60.60%→0.47% (клампинг радиусов FillRoundedRect), TEST-39 10.68%→1.40% (радиальный градиент-круг вместо эллипса; TEST-26 5.02%→0.00%), TEST-18 52.22%→2.15% (реальная отрисовка `<img>` + object-fit + area-averaged downscale). Разблокирует полную замену gdigrab на `run.py --ipc` (TAB-7). См. «Recent merges». | M ✅ | `paint` (`cpu_raster.rs`) |

**Слой 5 — U-4 опции (необязательны для «комфорта», брать последними):**

| # | Задача | Размер |
|---|--------|--------|
| U-4a | ~~WASM SIMD (`0xFD`)~~ ✅ 2026-06-20 + ~~threads/atomics (`0xFE`)~~ ✅ 2026-06-20 (весь набор atomic load/store/rmw/cmpxchg/wait/notify/fence в однопоточной семантике) + ~~relaxed-SIMD (`0xFD` sub `0x100..=0x113`)~~ ✅ 2026-06-20 (madd/nmadd/laneselect/min/max/trunc/swizzle/q15mulr/dot — детерминированная strict-семантика) + ~~JS-уровневые `SharedArrayBuffer`/`Atomics`~~ ✅ 2026-06-20 (нативны в QuickJS; добавлен шим `Atomics.waitAsync` ES2024 для одно-агентной модели, см. «Recent merges»). **U-4a закрыта полностью.** | L ✅ |
| U-4b | ~~Live-aliasing памяти WASM (emscripten `HEAP32`, сейчас снимок)~~ ✅ 2026-06-20 — экспортируемый `Memory.buffer` теперь **стабильный** JS `ArrayBuffer`, синхронизируемый с Rust-памятью на границах WASM-вызовов (JS→Rust до вызова, Rust→JS на месте после). Паттерн `HEAP32 = new Int32Array(memory.buffer)` когерентен в обе стороны, захваченный view живёт между вызовами; рост детачит/заменяет буфер. См. «Recent merges». | M ✅ |
| U-4c | Реальный WebGPU backend — ~~Stage 1: GPU-девайс + adapter info + WGSL-валидация~~ ✅ 2026-06-21 (`lumen_paint::webgpu_compute`, фича `lumen-js/webgpu`; `navigator.gpu` отдаёт реальные vendor/device/description и реальную компиляционную диагностику WGSL вместо Phase 0 stub). ~~Stage 2 под-этап 1: реальные `GPUBuffer` create/write/map-readback + `copyBufferToBuffer` submit через GPU-память~~ ✅ 2026-06-21 (реестр буферов в `webgpu_compute`, 5 нативов, шим `GPUBuffer` с graceful fallback). ~~Stage 2 под-этап 2: compute-пайплайны + bind-groups + `dispatchWorkgroups`~~ ✅ 2026-06-21 (реальный WGSL-compute исполняется на GPU; `shader_create`/`compute_pipeline_create`/`pipeline_bind_group_layout`/`bind_group_create` + `GpuOp::ComputePass`; проверено end-to-end удваивающим шейдером на живом GPU). ~~Stage 3 под-этап 1: render-пайплайны + offscreen `GPUTexture` render-таргеты + `beginRenderPass`/`draw` + `copyTextureToBuffer` readback~~ ✅ 2026-06-21 (реальный render на GPU; `texture_create`/`render_pipeline_create`/`render_pipeline_bind_group_layout` + `GpuOp::RenderPass`/`CopyTextureToBuffer` + `RenderCmd`; проверено end-to-end clear-цветом и полноэкранным треугольником, пиксели читаются обратно). ~~Stage 3 под-этап 2: present в canvas~~ ✅ 2026-06-21 (`canvas.getContext('webgpu')` → `GPUCanvasContext`; `configure`/`getCurrentTexture` дают реальный render-таргет; после render-`submit` кадр читается `texture_read_rgba` (readback+BGRA→RGBA) и пишется в CPU-буфер `<canvas>` → шелл показывает как `canvas:{nid}`). **U-4c WebGPU backend завершён.** См. «Recent merges». | XL ✅ |

**Слой 6 — ICC Color Management (XL, разбит на 6 сессий; согласован с пользователем 2026-06-21):**

Единственный незакрытый пункт очереди P1. Полноценный CMM: настоящий парсер ICC,
Lab PCS, matrix-shaper для RGB-профилей и LUT-путь для CMYK. Сейчас
`lumen_core::ColorSpace` (`crates/core/src/color.rs:4`) знает только Srgb/DisplayP3/Rec2020,
`detect_color_space_from_icc` (:28) — строковый сниффер, `'Lab '` помечен out-of-scope (:48),
CMYK-JPEG декодируется zune-jpeg в RGB в обход встроенного профиля. Объём: **RGB + CMYK
(все 6 подзадач)**. Каждая — отдельная ветка `p1-icc-N`/worktree, влезает в один контекст.

| # | Подзадача | Размер | Файлы / точки | «Готово» |
|---|-----------|--------|---------------|----------|
| ~~**ICC-1**~~ | ✅ 2026-06-21 — настоящий read-only парсер ICC (`crates/core/src/icc.rs`): валидация `'acsp'`, заголовок (`version`/`class`/`data_color_space`/`pcs`), tag-table, `rXYZ/gXYZ/bXYZ`+`wtpt` (`XyzNumber`, s15Fixed16), `rTRC/gTRC/bTRC` (`ToneCurve::{Identity,Gamma,Table,Parametric}`), сырые `A2B0/B2A0`. `IccProfile::color_space()` классифицирует RGB по колорант-примариям (sRGB/P3/Rec2020) без сниффинга строк; `detect_color_space_from_icc` — обёртка. Тесты: синтетические + реальный IEC sRGB (`tests/icc_real_profile.rs`). См. «Recent merges». | M ✅ | `crates/core/src/icc.rs`; `color.rs` | ✅ `IccProfile` с тегами; тест на реальном профиле; enum без сниффинга |
| ~~**ICC-2**~~ | ✅ 2026-06-21 — PCS-инфраструктура (`crates/core/src/pcs.rs`): типы `Xyz` (CIE XYZ) / `Lab` (CIE L*a*b*) + константы `Xyz::{D50,D65}`; `to_lab`/`to_xyz` — точные обратные (round-trip в пределах ε, кусочная CIE-нелинейность с линейным сегментом у чёрного); `adapt`/`d50_to_d65`/`d65_to_d50` — Bradford-адаптация (cone-response + диагональное масштабирование по белой точке); `From<XyzNumber>` интероп с `icc`. Добавлен `ColorSpace::Lab`. 7 тестов: round-trip, white→L*=100, эталон Lab, Bradford white→white и D65→D50-эталон. См. «Recent merges». | M ✅ | `crates/core/src/pcs.rs`, `color.rs` | ✅ round-trip XYZ→Lab→XYZ в пределах ε; адаптация D50↔D65 на эталонах |
| ~~**ICC-3**~~ | ✅ 2026-06-21 — matrix-shaper RGB→sRGB трансформ (`icc.rs`): `ToneCurve::eval` (identity/gamma/табличный/параметрический типы 0–4), `IccProfile::build_rgb_transform() -> Option<RgbTransform>` (колоранты Bradford D50→D65 × XYZ(D65)→sRGB в одну 3×3 + per-channel TRC-декод + sRGB OETF), `RgbTransform::apply`. Проводка в `lumen-image`: `to_rgba8`/`correct_rgba_pixels` через `apply_icc_rgb_transform`, femtovg-бэкенд (`image_to_rgba8_vec`) теперь тоже цветоуправляем. Любой RGB ICC-профиль (P3/Rec2020/Adobe RGB/ProPhoto) корректен в окне, CPU-снимке и PDF. Fallback на gamut-tone-mapping для LUT-only/не-RGB. См. «Recent merges». | L ✅ | `crates/core/src/icc.rs`; `lumen-image` lib.rs; femtovg_backend.rs | ✅ matrix-shaper трансформ; sRGB round-trip + P3 in-gamut тесты в icc.rs и lumen-image |
| ~~**ICC-4**~~ | ✅ 2026-06-21 — CMYK через ICC (LUT-путь): `IccProfile::build_cmyk_transform()` парсит `A2B0` (`lut8Type`/`lut16Type`/`lutAToBType`) в нормализованный конвейер (A-кривые → n-линейный CLUT → M-кривые → опц. матрица → B-кривые → PCS); `CmykTransform::apply(c,m,y,k)` гонит CMYK-краску через CLUT → PCS (XYZ u1Fixed15 / Lab v2-v4) → D50→D65 Bradford → sRGB. Проводка в JPEG-декодер (`try_decode_cmyk_icc`): CMYK/YCCK-JPEG с встроенным CMYK-профилем декодируется через профиль (учёт Adobe-инверсии APP14, YCCK→CMY через `ycbcr_to_rgb`) вместо наивного zune-RGB. Тесты: синтетический mft2-профиль (white/black/интерполяция), n-линейный CLUT, Adobe-маркер, YCbCr. См. «Recent merges». | L ✅ | `icc.rs`; JPEG-decode hook в `lumen-image` | ✅ CMYK-JPEG c CMYK-ICC рендерится через профиль |
| ~~**ICC-5**~~ | ✅ 2026-06-21 — проводка в pipeline + кэш трансформов. Процесс-глобальный кэш по байтам профиля (`lumen_core::icc::cached_rgb_transform`/`cached_cmyk_transform`, `LazyLock<Mutex<…>>`, `Arc`-шеринг, кэшируются и хиты, и промахи) — профиль парсится/компилируется максимум один раз. Колор-менеджмент перенесён на этап декода: `lumen_image::decode` зовёт `color_manage_in_place` (matrix-shaper или gamut-fallback in-place по компактному буферу Rgb8/Rgba8, gray-форматы отложены в `to_rgba8`), затем очищает `icc_profile` → каждая картинка трансформируется ровно один раз, register/resize/paint работают на уже-sRGB пикселях. `to_rgba8`/`correct_rgba_pixels`/JPEG-CMYK переведены на кэш. Graceful fallback на sRGB сохранён. Тесты: shared-Arc, miss-кэш, decode==paint паритет (RGBA+RGB8), gray сохраняет профиль. См. «Recent merges». | M ✅ | `lumen-image` decode; `lumen-core::icc` | ✅ каждая картинка трансформируется один раз; парс профиля максимум один раз |
| ~~**ICC-6**~~ | ✅ 2026-06-21 — graphic_test `128-icc-color-management.html` (Display-P3 PNG, **0.00% vs Edge** пиксель-в-пиксель) + Rust-интеграционный `crates/engine/image/tests/icc_color_management.rs` (P3 + CMYK через полный `decode`). Генератор `graphic_tests/gen_icc_images.py` (математика зеркалит `lumen_core::icc`). Попутно **найден и исправлен BUG-229**: PNG `iCCP` инфлейтился сырым `DeflateDecoder` вместо `ZlibDecoder` → ни один PNG с ICC не управлялся по цвету. `ColorSpace::Lab` в `name()`+тест. Доки: CAPABILITIES (🟡→✅), subsystems/image.md, COVERAGE. CMYK-ICC не в графтесте — Edge игнорирует встроенный CMYK-профиль (свой SWOP), покрыт Rust-тестом. См. «Recent merges». | S ✅ | `graphic_tests/`, `png/mod.rs`, `CAPABILITIES.md` | ✅ тест 0.00%, доки обновлены |

**Зависимости внутри слоя 6:** ICC-1 → (ICC-2, ICC-3); ICC-2 → ICC-4; ICC-3 ∥ ICC-4; обе → ICC-5 → ICC-6.
**Минимум полезного:** ICC-1+2+3 (корректный RGB для P3/Rec2020 фото) ✅ достигнут. CMYK (ICC-4) ✅ достигнут.
**Порядок:** ~~ICC-1~~ ✅ → ~~ICC-2~~ ✅ → ~~ICC-3~~ ✅ → ~~ICC-4~~ ✅ → ~~ICC-5~~ ✅ → ~~ICC-6~~ ✅ 2026-06-21. **Слой 6 ICC завершён полностью.**

---

**Критический путь и параллелизм:**

```
B-1 (XL) ──┬─► BUG-222 (S)
           └─► BUG-171 (L) ──► BUG-172 (S)        ← U-1 этап 2 закрыт
параллельно (без B-1):
  BUG-167 (S)                                      ← U-5
  BUG-104 ✅ → BUG-126 ✅ → BUG-085 ✅ → BUG-144    ← U-6
  BUG-221 (M)                                      ← наблюдаемость
потом (опционально): U-4a / U-4b / U-4c
```

**Минимум «потребность закрыта»:** слои 0–3. Слой 4 — для измерения, слой 5 — необязателен.
Эти баги остаются записаны в `BUGS.md` как OPEN (трекер не дублируем) — здесь только порядок
и владелец. Общий backlog P3 (BUG-181…217 и пр.) и CSS-очередь P4 в этот план НЕ входят —
это отдельные перечни; при полном переходе на одного программиста их мигрировать отдельно.

---

### TAB-series — Multi-tab support + screenshot IPC (приоритет)

Цель: один процесс Lumen держит несколько вкладок с изолированным состоянием; `run.py` открывает браузер один раз и получает скриншоты через IPC без gdigrab.

**Статус (ревизия 2026-06-18):** интерактивная многовкладочность в окне **уже реализована**
(вопреки прежнему описанию). В `Lumen` per-tab состояние вынесено в `PageSnapshot`
(`main.rs:3303`), фоновые вкладки в `bg_tabs: HashMap<usize, PageSnapshot>`; есть
`switch_tab`/`open_new_tab`/`close_tab`/`save_page_snapshot`/`restore_page_snapshot`,
tab-bar UI (`tabs::strip`), пятиуровневая lifecycle-модель (`tab_lifecycle/`, T0→T4,
hibernation в SQLite), биндинги Ctrl+T/Ctrl+W/Ctrl+Tab. Поэтому **TAB-1/2/3/6 закрыты кодом**
(модель — swap `PageSnapshot`, а не `Vec<TabState>`, но цель «per-tab изоляция» достигнута).

| # | Задача | Размер | Крейты | Статус |
|---|--------|--------|--------|--------|
| TAB-1 | **PageState extraction** — per-tab состояние. | M | `lumen-shell` | ✅ готово (`PageSnapshot`+`bg_tabs`) |
| TAB-2 | **Tab switching** — Ctrl+T/W/Tab, swap активной вкладки. | S | `lumen-shell` | ✅ готово (`switch_tab`) |
| TAB-3 | **Tab bar UI** — полоса вкладок, заголовок, крестик, `+`. | M | `lumen-shell` | ✅ готово (`tabs::strip`) |
| TAB-4 | **IPC tab routing** — `TabId` + `CreateTab`/`CloseTab`/`NavigateTab`. | S | `lumen-ipc`, `lumen-shell` | ✅ 2026-06-18 |
| TAB-5 | **Screenshot IPC** — `Screenshot(tab_id)` → PNG offscreen (CPU). | S | `lumen-ipc`, `lumen-shell` | ✅ 2026-06-18 |
| TAB-6 | **Подключить TabLifecycleManager** — idle T0→T2 по таймаутам. | S | `lumen-shell` | ✅ готово (`tab_lifecycle/`) |
| TAB-7 | **run.py IPC-режим** — `lumen.exe --ipc-server`, `NavigateTab + Screenshot` на тест, PNG vs Edge, убрать gdigrab. **Требует Python-реализацию bincode** для `lumen_ipc::{IpcRequest,IpcResponse}` (variant-tag u32 LE + u64 LE длины строк/Vec). | XS | `graphic_tests/run.py` | ✅ 2026-06-18 (опц. `--ipc`; полная замена gdigrab — после BUG-221) |

**Следующий шаг:** U-2 (шейпинг текста GSUB/GPOS + CFF-контуры) по основному порядку P1.
TAB-7 закрыт флагом `--ipc` (Python-клиент bincode к `--ipc-server` работает, протокол
верифицирован пиксельно на гео/цвет/текст-тестах). **Полностью убрать gdigrab нельзя до
паритета CPU-бэкенда снимка** (BUG-221: `render_to_image_cpu` не рисует border-radius/
gradients/images как femtovg), поэтому gdigrab пока дефолт, `--ipc` — опция.

---

### Унаследовано от P2 (P2 → резерв, 2026-06-18)

P2 выведен в резерв; все его незакрытые задачи теперь у P1. Приоритет ниже
USABILITY-вертикали и TAB-серии — брать после них или когда они заблокированы.

**Брифы task-файлов (`docs/tasks/`) — все четыре УЖЕ реализованы (ревизия 2026-06-18, брифы устарели):**

| # | Задача | Статус | Где в коде |
|---|--------|--------|-----------|
| P2→1 | **Tab tier tooltip при hover (10K.2)** | ✅ влита (`3c61a138`) | `shell/src/tabs/strip.rs:883` (`build`, «Вкладка спит…») |
| P2→2 | **Loading spinner при restore >200ms (10K.3)** | ✅ влита (`0256760c`) | `shell/src/main.rs:5645` `restore_spinner_start_ms` + `panels::restore_spinner` |
| P2→3 | **FemtovgBackend — PushFilter blur** | ✅ влита (`085d5b8d` PA-2, полный offscreen-blur, не Phase-1 заглушка) | `paint/.../femtovg_backend.rs:693` `box_blur_rgba` + `filter_layer_stack` |
| P2→4 | **FemtovgBackend — эллиптические border-radius** | ✅ влита (`537c8a3d`) | `paint/.../femtovg_backend.rs:957` `draw_fill_rounded_rect` (kappa-Безье) |

**Направления без брифа (бывшие «Опции» P2):**

- **Ad-block Phase 2/3** — ~~`$option` по типу ресурса (Phase 2)~~ ✅ 2026-06-20: EasyList `$`-опции (resource-type `$script`/`$image`/`$stylesheet`/`$font`/`$xmlhttprequest`/`$subdocument`/`$media`/`$other` + `~`-негация, `$third-party`/`$first-party`) парсятся и применяются через `RequestFilter::should_block_ctx(url, &RequestContext)`; `domain=` parsed-but-ignored. См. «Recent merges». Остаётся **UI подписок (Phase 3, handoff P3)**. Крейты: `lumen-network`, `lumen-shell`.
- **Canvas Phase extensions** — ~~paint-источники в JS-шиме~~ ✅ 2026-06-20: `<canvas>` 2D `createLinearGradient`/`createRadialGradient`/`createConicGradient`+`addColorStop`, `createPattern`, `fillStyle`/`strokeStyle` для gradient/pattern-объектов, `shadowColor`/`shadowBlur`/`shadowOffsetX`/`shadowOffsetY`, `drawImage` (canvas-источник), `putImageData` — проведены к существующему нативу (`canvas2d.rs`), раньше JS-шим возвращал фейк-заглушки. ~~source-crop 9-арг формы~~ ✅ 2026-06-20 (`Context2D::draw_image_cropped` + `_lumen_canvas2d_draw_image_crop`, 8 координат CSV-строкой; форма `(img,sx,sy,sw,sh,dx,dy,dw,dh)` режет под-прямоугольник источника). ~~`<img>`-источник в `drawImage`~~ ✅ 2026-06-21 (ревизия: уже в коде) — `_lumen_canvas2d_draw_image_from_img` / `_lumen_canvas2d_draw_image_crop_from_img` (`canvas2d.rs:836`) читают декодированный bitmap из `img_bitmap_store`; shell наполняет его через `register_img_bitmaps` (`main.rs:3824`), JS-шим `drawImage` различает canvas/img-источник по `__nid__`+tag (`dom.rs:3907`). Остаётся: Canvas 2D text-completeness (BUG-099, домен P3). Крейты: `lumen-canvas`, `lumen-js`.
- **Color management Phase 1+** — Lab/CMYK color spaces (H-2 Phase 1+), device-specific tone curves, advanced ICC. Крейты: `lumen-image`, `lumen-paint`. **Ревизия 2026-06-21:** CMYK/YCCK-JPEG уже декодируются корректно самим zune-jpeg 0.5.15 (`blinn_8x8`, Adobe-инверсия учтена → RGB8). Остаётся именно **ICC-управление цветом** (Lab PCS + CMYK ICC-профили): `lumen_core::ColorSpace` (`crates/core/src/color.rs:4`) знает только Srgb/DisplayP3/Rec2020, сигнатура `'Lab '` явно out-of-scope (:48). Это полноценный CMM = **XL**, не «M».
- ~~**DevTools network-panel: `Event::RequestFailed`**~~ ✅ 2026-06-21 (ревизия: уже в коде) — `network_panel.rs:288` (`Event::RequestFailed` → `record_failed`), отрисовка проваленных запросов + тесты (`network_panel.rs:908`). Крейт: `lumen-shell`.

> Баги высокого отклонения (BUG-085 gradient, BUG-088 transforms, BUG-090 line-clamp),
> которые в STATUS-P2 числились как «P3-вспомога», остаются у **P3** — это их домен
> по CLAUDE.md «Bug ownership: P3 only», в P1 не переносятся.

---

### Правило: фиксировать реализованное

После каждой задачи **в том же коммите** обновлять:
1. `STATUS-P1.md` — переместить из `Next` в `Recent merges` с кратким описанием
2. `CAPABILITIES.md` — изменить ⬜/🟡 → ✅ для реализованных возможностей
3. `subsystems/<crate>.md` — добавить bullet в раздел **Done**

Это предотвращает повторную реализацию уже готового другими сессиями.

---

### Streaming rendering — оставшиеся дыры (приоритет, до PH3)

PH1-2 закрыл только window-first + 60 Hz throttle + параллельную загрузку CSS. Реальная
потоковая отрисовка «по мере прихода из сети» ещё не работает. Три задачи по убыванию
заметности для пользователя:

| # | Задача | Размер | Крейты |
|---|--------|--------|--------|
| PH1-2a | ~~**TCP-level streaming HTTP body**~~ ✅ завершена (2026-06-16) — `HttpClient::fetch_page_streaming(url, on_chunk)` отдаёт декодированные порции тела по мере чтения сокета; shell `start_streaming_load` шлёт реальные сетевые чанки. | L | `lumen-network`, `lumen-shell` |
| PH1-2b | ~~**Инкрементальный (dirty-subtree) layout**~~ ✅ завершена (2026-06-16) — `layout_streaming_incremental` переиспользует геометрию неизменённого префикса из прошлого кадра, релейаутит лишь новые/изменённые поддеревья; `paint_partial_dom` гейтит через `stream_layout_seeded`. | L | `lumen-layout`, `lumen-shell` |
| PH1-2c | ~~**Прогрессивная подгрузка картинок во время streaming**~~ ✅ завершена (2026-06-16) — `paint_partial_dom` спавнит параллельные fetch+decode для `<img>` частичного DOM, картинки дорисовываются по приходу через `LoadEvent::ImageDecoded`. | M | `lumen-image`, `lumen-shell` |

### PH3 — Phase 3: v1.0 «Full Browser»

| # | Задача | Размер | Крейты |
|---|--------|--------|--------|
| PH3-1 | ~~**DevTools Elements styled-rules panel**~~ ✅ завершена | M | `lumen-shell` (devtools/) |
| PH3-3 | ~~**getUserMedia Phase 1**~~ ✅ завершена | L | `lumen-js`, `lumen-shell` |
| PH3-4 | ~~**Offscreen Canvas Phase 1**~~ ✅ завершена | M | `lumen-js`, `lumen-paint` |
| PH3-5 | ~~**Web Workers Phase 1**~~ ✅ завершена | L | `lumen-js` |
| PH3-9 | ~~**HTML5 Drag and Drop API**~~ ✅ завершена | M | `lumen-dom`, `lumen-js`, `lumen-shell` |
| PH3-11 | ~~**`<audio>` element Phase 1 — HTMLAudioElement real playback**~~ ✅ завершена | L | `lumen-core`, `lumen-js`, `lumen-shell` |
| PH3-12 | ~~**`<video>` element Phase 1 — HTMLVideoElement GIF playback**~~ ✅ завершена | M | `lumen-js`, `lumen-paint`, `lumen-shell` |
| PH3-13 | ~~**Screen Wake Lock API Phase 1**~~ ✅ завершена | M | `lumen-core`, `lumen-js`, `lumen-shell` |

---

## Recent merges

| Дата | Задача | Описание |
|------|--------|---------|
| 2026-06-22 | F2-5 — Knowledge layer ядро: omnibox-префиксы `@read-later` / `@tabs` | Ревизия по коду показала, что «ядро» knowledge-слоя уже было готово: FTS5-поиск history/notes, Read-later store с `.search()`, OpenTabsIndex, focus mode + Pomodoro (`Ctrl+Shift+F`). Единственный реальный разрыв из `CAPABILITIES.md` (lumen-knowledge ⬜) — два omnibox-префикса поиска отсутствовали. Добавлены: **`@read-later <query>`** → `read_later_store.search()` (FTS5, §12.3, до 7 результатов) → подсказки, выбор = `commit_value()` URL → обычная навигация на сохранённую страницу; **`@tabs <query>`** → case-insensitive substring-поиск по `tab_strip.tabs` (заголовок + URL; URL активной вкладки из `self.source.url_str()`, фоновых — из `bg_tabs[id].source`; пустой запрос = все вкладки, cap 8) → выбор = sentinel `switch-tab:<id>`, перехватывается в `handle_omnibox_commit` → `switch_tab(idx)` (id→index резолвится на месте, переживает reorder вкладок). Стора уже имели `.search()` — задача чисто проводочная. **Дизайн-нюанс:** `@read-later <url>` по-прежнему **сохраняет** на Enter через `omnibox::resolve` (`SaveReadLater`); новый префикс добавляет лишь поиск-во-время-ввода (выбор подсказки коммитит URL, не `@`-строку, поэтому resolve её не перехватывает). Изменения: `address_bar.rs` (`OmniboxPrefix::{ReadLater,Tabs}` + `parse_omnibox_prefix` + `OmniboxSuggestion::{ReadLater,Tab}` + 5 методов рендера), `main.rs` (`query_omnibox_suggestions` 2 ветки, `handle_omnibox_commit` `switch-tab:`). Russian Porter stemmer + vector/HNSW index — остаются ⬜ (опционально, вне «ядра», см. `lib.rs:6`). Тесты: +7 в `address_bar` (parse обоих префиксов + commit_value/label/sub_label). clippy `-p lumen-shell --all-targets` чисто. Доки: CAPABILITIES (knowledge + shell Navigation/Tabs), subsystems/shell.md. |
| 2026-06-22 | F2-1 — Motion Path L1 доведён до паритета (BUG-125, TEST-76 3.18%→0.54%) | CSS Motion Path L1 (`offset-path`/`offset-distance`/`offset-rotate`/`offset-anchor`) ставил на путевую точку **top-left** бокса, а не его `offset-anchor` — боксы съезжали на пол-бокса вправо-вниз от Edge. По CSS Motion Path L1 §3.3 на путь садится anchor (initial `auto` = `transform-origin` = центр `50% 50%`), поворот вокруг него; отсутствовал член `T(-anchor)`. **Фикс:** новый `motion_anchor_px()` (offset-anchor→px, `auto`→transform-origin). `forward_box_transform` (paint, путь display-list) строит motion-матрицу вокруг anchor: `T(rect+path)·R(θ)·m·T(-(rect+anchor))` (transform-origin = offset-anchor по спеку); `PropertyTrees::walk` (compositing/hit-test) — `T(path)·R(θ)·T(-anchor)`. Проверка: детерминированный CPU-снимок (`--screenshot`/`run.py --only 76 --ipc`) vs Edge-эталон — все боксы (горизонтальные/диагональные/cubic, auto/fixed rotate) пиксель-в-пиксель (маскирование диагональной полосы row 2 → 0.01%). Остаток 0.54% = **отдельный дефект**: диагональный трек-индикатор `.track-diag` рисуется `linear-gradient(... calc(50% ± 2px) ...)`, а `calc()` в позиции color-stop схлопывает градиент (`parse_length_q` парсит calc в `Length::Calc`, но позиция не резолвится против длины линии) → линия не рисуется → **BUG-230** (домен градиентов), TEST-76 запаркован как KNOWN_DEBTOR (BUG-230, baseline 0.54%). Регресс-тесты: `motion_path_centres_anchor_on_path_point` (lib.rs), `offset_path_at_zero_distance_centres_anchor_on_path_start` (property_trees.rs). clippy `-p lumen-layout --all-targets` чисто. Доки: CAPABILITIES (motion path ✅, offset-path убран из parse-only), BUGS (125→FIXED, +230). |
| 2026-06-21 | BT-1 — консолидация integration-тестов `crates/driver` (64 бинаря → 1) | Снят основной потолок `cargo test --workspace`. Раньше 64 файла `crates/driver/tests/*.rs` компилировались как 64 отдельных integration-бинаря, каждый — полная статическая линковка движка (wgpu/winit/rustls). Теперь единый бинарь: **`tests/all.rs`** = `mod cases;`, 63 тест-файла переехали в **`tests/cases/`** (подкаталог не авто-собирается cargo как отдельные бинари), перечислены в `tests/cases/mod.rs` как `mod <stem>;`. Неиспользуемый хелпер `common.rs` (ни один тест не ссылался) удалён. Два feature-gated файла (`snapshot_cpu` → `cpu-render`, `compare_backends` → `compare-backends`) сохранили inner-`#![cfg(feature=…)]` — как module-файлы он гейтит весь подмодуль, правок не потребовалось. Единственная правка тела теста — `include_bytes!` в `snapshot_vs_edge.rs` углублён на один `../` (файл переехал на уровень вниз). **Замер на этой машине** (`cargo test -p lumen-driver --no-run`, deps прогреты, rust-lld): 64 бинаря **1м50с** → 1 бинарь **7.6с** (~14×). Счёт тестов сохранён: 113 в default-сборке (= 120 `#[test]` − 7 feature-gated), `compare_backends` 6 под фичей, `snapshot_cpu` 1 под фичей — проверено `--list`. Прогон: 112 passed, 1 failed (`test_32_list_markers`) — failed и в изоляции, т.е. **предсуществующий**, уже заведён как **BUG-228** (P3, custom @counter-style/image-маркеры). clippy `-p lumen-driver --all-targets` чисто. Обновлён комментарий `scripts/scoped-test.sh` (консолидация выполнена). | |
| 2026-06-21 | ICC-6 — тесты + наблюдаемость + доки (слой 6, подзадача 6/6, **слой завершён**) | Завершён слой ICC Color Management. **graphic_test `graphic_tests/128-icc-color-management.html`** — Display-P3 PNG, свотчи закодированы так, что корректный P3→sRGB matrix-shaper восстанавливает известные in-gamut sRGB-цвета; Edge и Lumen (оба colour-managing) дают **пиксель-в-пиксель идентичный кадр (0.00%)**, подтверждено прямым прогоном Edge headless vs `lumen.exe --screenshot`. Зарегистрирован в `run.py` (TEST-128). **Rust-интеграционный тест `crates/engine/image/tests/icc_color_management.rs`** — декодирует committed `samples/images/icc_p3.png` (P3) и `icc_cmyk.jpg` (CMYK с A2B0-профилем) через полный `lumen_image::decode` и проверяет восстановленные sRGB-свотчи (детерминированно, без Edge). **Генератор `graphic_tests/gen_icc_images.py`** строит профили/картинки; математика зеркалит `lumen_core::icc`/`pcs` (те же Bradford/sRGB-матрицы, TRC-таблица из bundled sRGB.icc) → round-trip точный до 8-бит квантизации. **Попутно найден и исправлен BUG-229** (`png/mod.rs`): `iCCP` инфлейтился сырым `DeflateDecoder`, тогда как PNG хранит профиль zlib-датастримом (RFC 1950) → инфляция падала, профиль тихо терялся, **ни один PNG с ICC не управлялся по цвету**. Заменено на `ZlibDecoder` — matrix-shaper (ICC-3) и кэш (ICC-5) реально срабатывают на PNG. `ColorSpace::Lab` уже в `name()`→"lab"; добавлены ассерты в `color.rs`. **CMYK-ICC намеренно не в графтесте**: Edge не применяет встроенный CMYK ICC-профиль (использует свой SWOP — paper-white даёт 188, не 255), поэтому не может служить пиксельным эталоном; CMYK-путь Lumen спек-корректен и покрыт Rust-тестом. Доки: CAPABILITIES (🟡→✅), subsystems/image.md, COVERAGE.md. clippy `-p lumen-core -p lumen-image --all-targets` чисто; core color 6/6, image +2 интеграционных passed; графтест 0.00% vs Edge. |
| 2026-06-21 | ICC-5 — проводка в pipeline + кэш трансформов (слой 6, подзадача 5/6) | Колор-менеджмент перенесён на этап декода и закэширован, чтобы каждая картинка трансформировалась один раз, а профиль парсился максимум один раз. **`crates/core/src/icc.rs`:** процесс-глобальный `TRANSFORM_CACHE` (`LazyLock<Mutex<…>>`, recovery из poisoned-лока без паник) с двумя картами `HashMap<Vec<u8>, Option<Arc<…>>>` по сырым байтам профиля; `cached_rgb_transform(&[u8]) -> Option<Arc<RgbTransform>>` и `cached_cmyk_transform(&[u8]) -> Option<Arc<CmykTransform>>` строят трансформ при первом обращении и кэшируют результат — **в т.ч. промахи** (`None`), чтобы не-RGB / LUT-only / битый профиль не переразбирался на каждый вызов; `Arc` шерит один компилированный трансформ между всеми картинками с этим профилем. Ключ — полные байты (без риска коллизии хэша; профилей на странице единицы, каждый — килобайты). **`crates/engine/image/src/lib.rs`:** `decode` разбит на `decode_raw` (декод в нативный формат, профиль прикреплён) + `color_manage_in_place(&mut Image)` — применяет matrix-shaper (или gamut-fallback) **in-place по компактному буферу** (stride 3 для `Rgb8`, 4 для `Rgba8`, gray-форматы отложены в `to_rgba8`), затем очищает `icc_profile`. Эффект: register/resize/paint работают на уже-sRGB пикселях, тяжёлый per-pixel проход — один раз на картинку, а не на каждый кадр/перерегистрацию. `to_rgba8`, `correct_rgba_pixels` и JPEG-CMYK (`jpeg/mod.rs::try_decode_cmyk_icc`) переведены с прямого `parse().build_*()` на кэш. Хелперы `apply_rgb_transform_packed`/`convert_packed_to_srgb` обобщены по stride (старые RGBA8-обёртки сохранены). Graceful fallback на sRGB сохранён (нет трансформа + неизвестный gamut → no-op). Зависимостей не добавлено. Тесты: +3 в `icc.rs` (shared-`Arc` при повторном lookup, паритет cached==direct, кэш промахов без паник), +4 в `lumen-image` (decode-time RGBA/RGB8 == старый paint-path + профиль очищен + идемпотентность, gray сохраняет профиль, no-profile no-op). clippy `-p lumen-core -p lumen-image --all-targets` чисто; core 260+3, image 152+6+19+32 passed; `lumen-paint`/`lumen-shell` (потребители) компилируются. **Следующая подзадача:** ICC-6 (graphic_test + доки + `ColorSpace::Lab` в `name()`). |
| 2026-06-21 | ICC-4 — CMYK через ICC (LUT-путь) (слой 6, подзадача 4/6) | Реализован настоящий CMM CMYK→sRGB через `A2B0`-LUT профиля вместо наивного zune CMYK→RGB. **`crates/core/src/icc.rs`:** `IccProfile::build_cmyk_transform() -> Option<CmykTransform>` парсит `A2B0` трёх типов — `lut8Type` (`mft1`), `lut16Type` (`mft2`), `lutAToBType` (`mAB `) — в единый нормализованный `A2bLut` (input/A-кривые → CLUT → output/M-кривые → опц. 3×3+offset матрица → опц. B-кривые → PCS). CLUT интерполируется обобщённой n-линейной интерполяцией (`clut_interp`, dim-0-most-significant, per-dim grid-counts, поддержка 4D CMYK). Кривые хранятся как `ToneCurve` (mft-таблицы повышаются u8→u16); `mAB`-кривые `curv`/`para` парсятся с 4-байтовым выравниванием. `CmykTransform::apply(c,m,y,k)` гонит краску (0=нет краски) через LUT, декодирует PCS (XYZ u1Fixed15 v/32768; Lab с разделением v2 0xFF00 / v4 0xFFFF) в D50-XYZ, Bradford D50→D65, выдаёт gamma-encoded sRGB. `None` для не-CMYK / 4→3-несоответствия / без `A2B0`. **Проводка в `lumen-image` (`jpeg/mod.rs`):** `try_decode_cmyk_icc` — CMYK/YCCK-JPEG с встроенным CMYK-профилем декодируется сырыми 4 каналами (`out_colorspace == input`), восстанавливается покрытие краской (Adobe-APP14-инверсия через `jpeg_has_adobe_marker`; YCCK→stored-CMY через `ycbcr_to_rgb`), каждый пиксель прогоняется через профиль → sRGB `Rgb8` (без остаточного ICC). Fallback на zune CMYK→RGB когда профиля нет. Тесты: 5 в `icc.rs` (mft2 white/black/интерполяция, n-линейный CLUT-центр, reject RGB/без-A2B0) + 4 в `lumen-image` (Adobe-маркер ±, YCbCr→RGB). Зависимостей не добавлено. clippy `-p lumen-core -p lumen-image --all-targets` чисто; core 257, image 51 passed. **Следующая подзадача:** ICC-5 (кэш трансформов на этапе декода). |
| 2026-06-21 | ICC-3 — matrix-shaper RGB→sRGB трансформ (слой 6, подзадача 3/6) | Реализован настоящий ICC matrix-shaper: device RGB → sRGB по реальным данным профиля, а не по трём зашитым охватам. **`crates/core/src/icc.rs`:** `ToneCurve::eval(x) -> f64` вычисляет любую разобранную TRC в направлении device-encoded→linear (identity / gamma / интерполяция `u16`-таблицы / параметрические типы 0–4 по ICC.1:2010 §10.16; вход клампится, кривые с битыми параметрами → identity, без паник). `IccProfile::build_rgb_transform() -> Option<RgbTransform>` компилирует трансформ: колоранты Bradford-адаптируются D50→D65 (через `pcs`), складываются с матрицей XYZ(D65)→linear-sRGB в одну 3×3; три декод-кривые применяются по каналам, sRGB OETF — аналитически на выходе. `RgbTransform::apply(r,g,b)` маппит один gamma-encoded triple в gamma-encoded sRGB (клампинг). `None` для не-RGB / без колорантов (LUT-only → ICC-4). **Проводка в `lumen-image`:** `Image::to_rgba8` и `correct_rgba_pixels` предпочитают matrix-shaper (хелпер `apply_icc_rgb_transform`), откатываясь на старый gamut-tone-mapping когда трансформ не строится. Femtovg-бэкенд (`image_to_rgba8_vec`) переведён на `to_rgba8` — раньше **полностью игнорировал** ICC в живом окне. Эффект: фото с профилем P3/Rec2020/Adobe RGB/ProPhoto цветокорректны в окне, CPU-снимке и PDF-экспорте. Тесты: 6 в `icc.rs` (eval identity/gamma/table/параметрик-sRGB-type3; sRGB round-trip; P3 red в gamut + white→white; reject не-RGB) + 3 в `lumen-image` (sRGB round-trip через `to_rgba8`; P3 red-dominant + alpha; без профиля без изменений). Зависимостей не добавлено. clippy `-p lumen-core -p lumen-image -p lumen-paint --all-targets` чисто; core 252, image 144, paint 779 passed. **Следующая подзадача:** ICC-4 (CMYK через ICC LUT-путь). |
| 2026-06-21 | ICC-2 — PCS-инфраструктура: XYZ/Lab + Bradford (слой 6, подзадача 2/6) | **Новый модуль** `crates/core/src/pcs.rs` — Profile Connection Space maths поверх парсера ICC-1. Типы `Xyz` (CIE 1931 tristimulus, `y`-нормализован) и `Lab` (CIE 1976 L*a*b*), константы `Xyz::{D50, D65}` (D50 — ICC PCS white, D65 — рабочие RGB-пространства). `Xyz::to_lab(white)` / `Lab::to_xyz(white)` — точные взаимно-обратные (round-trip в пределах ε, кусочная CIE-нелинейность `f`/`f⁻¹` с линейным сегментом ниже `(6/29)³` для устойчивости у чёрного). `Xyz::adapt(src_white, dst_white)` — Bradford chromatic adaptation (sharpened cone-response `BRADFORD`/`BRADFORD_INV` + диагональное масштабирование по отношению белых точек); `d50_to_d65`/`d65_to_d50` ре-референсят между ICC PCS (D50) и D65 RGB working space, что нужно ICC-3. `From<XyzNumber>`/`Into<XyzNumber>` — интероп с парсером `icc`. Добавлен `ColorSpace::Lab` (PCS-кодировка, не пиксельное пространство дисплея; обработан в `name()` → `"lab"` и в `apply_tone_mapping` lumen-image как no-op). Всё `f64`, без аллокаций и паник. **Экспорт** в `lib.rs`: `Xyz`, `Lab`. Тесты: 7 unit в `pcs.rs` (round-trip XYZ→Lab→XYZ под D50 в пределах 1e-9; white→L*=100/a*=b*=0; известное Lab-значение mid-grey; Bradford white→white точно даёт целевой белый; D50↔D65 round-trip; D65→D50-эталон = D50 white; `XyzNumber` интероп). Зависимостей не добавлено. clippy `-p lumen-core -p lumen-image --all-targets` чисто; `lumen-core` 246+3 passed. **Следующая подзадача:** ICC-3 (matrix/TRC RGB-трансформ). |
| 2026-06-21 | ICC-1 — настоящий read-only парсер ICC (слой 6, подзадача 1/6) | Заменён строковый сниффер `detect_color_space_from_icc` настоящим парсером ICC.1:2010 (v2+v4). **Новый модуль** `crates/core/src/icc.rs`: `IccProfile::parse(&[u8]) -> Option<IccProfile>` — валидация `'acsp'`-сигнатуры, декод заголовка (`version (major,minor,bugfix)`, `class: ProfileClass`, `data_color_space`/`pcs: DataColorSpace`), обход tag-table, извлечение `rXYZ/gXYZ/bXYZ`+`wtpt` в `XyzNumber` (декод `s15Fixed16`), `rTRC/gTRC/bTRC` в `ToneCurve::{Identity,Gamma,Table,Parametric}` (типы `curv`+`para`), сырые байты `A2B0/B2A0` сохраняются для LUT-пути ICC-4. `IccProfile::color_space()` классифицирует RGB-профили по xy-хроматичности колорант-примариев (ближайший из sRGB/Display-P3/Rec.2020), а не по подстроке описания. `color::detect_color_space_from_icc` стал тонкой обёрткой. Парсер полностью bounds-checked, без паник; не-RGB / неразбираемое → `ColorSpace::Srgb`. **Экспорт** в `lib.rs`: `IccProfile`, `ProfileClass`, `DataColorSpace`, `ToneCurve`, `XyzNumber`. Тесты: 11 unit в `icc.rs` (header, теги, gamma-TRC, классификация sRGB/P3/Rec2020 по примариям, не-RGB fallback) + 2 в `color.rs` + **3 интеграционных на реальном IEC sRGB-профиле** (`crates/core/tests/icc_real_profile.rs`, fixture `tests/fixtures/sRGB.icc` — свободно распространяемый IEC sRGB; ловит ошибки смещений, как требует политика «реальные данные»). Зависимостей не добавлено. clippy `-p lumen-core --all-targets` чисто; `lumen-core` 239+3 passed; `lumen-image`/`lumen-paint` (потребители) компилируются. **Следующая подзадача:** ICC-2 (PCS Lab + Bradford). |
| 2026-06-21 | U-4c Stage 3 (под-этап 2): present WebGPU-текстуры в страничный `<canvas>` — **U-4c завершён** | Закрывает WebGPU backend: отрисованная на GPU текстура **появляется на странице**. **(1) lumen-paint** (`webgpu_compute.rs`): `texture_read_rgba(texture_id) -> Option<(w,h,Vec<u8>)>` — readback отрисованной текстуры в плотный top-left RGBA8 (copy-texture-to-buffer в временный `MAP_READ`-буфер с 256-выровненным `bytes_per_row`, map+poll(Wait), снятие padding строк, swap BGRA→RGBA для `bgra8unorm[-srgb]`; rgba8 копируется как есть; иной формат → `None`; sRGB/linear дают те же 0..255 байты, гамма не нужна). **(2) lumen-js canvas2d** (`canvas2d.rs`): `present_rgba(nid, w, h, &rgba)` пишет пиксели в `Context2D`-буфер узла (создаёт/ресайзит при нужде) + `mark_dirty`, чтобы пер-кадровый `flush_canvas_updates` залил их как `canvas:{nid}`. **(3) lumen-js webgpu** (`webgpu.rs`): натив `_lumen_webgpu_canvas_present(nid, textureHandle)` = `texture_read_rgba` → `present_rgba`. Шим: `GPUCanvasContext.configure` выделяет реальный render-таргет `GPUTexture` (по размеру canvas) и регистрирует контекст в `_gpuCanvasContexts`; `getCurrentTexture` отдаёт его; `unconfigure` дропает+дерегистрирует. `GPUQueue.submit` собирает id текстур, в которые рисовали render-pass'ы батча, и после успешного реального сабмита презентит те canvas-контексты, чья текущая текстура была render-target (unrelated compute/copy сабмиты не блитят устаревший кадр). **(4) lumen-js dom** (`dom.rs`): `getContext('webgpu')` → `GPUCanvasContext`, привязан к `__nid__`, кэш `_canvas_webgpu_ctxs`. **Graceful fallback:** нет фичи/GPU/хэндла → present no-op (canvas пуст, как раньше). Зависимостей не добавлено. Тесты: +3 в `webgpu_compute` (paint, GPU-gated — `texture_read_rgba_present_path_returns_cleared_frame` проверяет BGRA→RGBA + row-unpad на живом GPU; unknown-handle → None), +2 в `canvas2d` (js — present пишет пиксели+dirty; ресайз существующего canvas), +2 в `webgpu` (js — unconfigure дропает текстуру; present-натив отвергает неизвестный хэндл). На этой машине GPU доступен → реальные GPU-пути исполнились. clippy `-p lumen-paint --features backend-wgpu` / `-p lumen-js --features webgpu` `--all-targets` чисто; тесты `lumen-js --features webgpu` 2273/19/25/4 passed, `lumen-paint` webgpu_compute 20 passed. |
| 2026-06-21 | U-4c Stage 3 (под-этап 1): render-пайплайны + render-в-текстуру + readback | Продолжение Stage 1/2. JS render-путь `navigator.gpu` был Phase 0: `createRenderPipeline` — пустой stub, `GPURenderPassEncoder` — сплошные no-op, `GPUTexture` без backing. Теперь WGSL **реально рисует на GPU** в offscreen-текстуру, пиксели читаются обратно. **(1) lumen-paint** (`webgpu_compute.rs`, фича `backend-wgpu`): два новых реестра `TEXTURES` (offscreen `wgpu::Texture` render-таргеты) и `RENDER_PIPELINES`, общий счётчик `NEXT_COMPUTE_ID` (render-выведенные bind-group-layout кладутся в тот же `BIND_GROUP_LAYOUTS`, что и compute — единый счётчик исключает коллизию ключей). API: `texture_create`/`texture_destroy`, `render_pipeline_create` (auto-layout `wgpu::RenderPipeline` из vertex+fragment модулей, формата таргета, `GPUPrimitiveTopology`, разобранных `VertexBufferLayout`/`VertexAttr`), `render_pipeline_bind_group_layout`, `render_pipeline_destroy`. Новый `RenderCmd` (SetPipeline/SetBindGroup/SetVertexBuffer/SetIndexBuffer/Draw/DrawIndexed) + `GpuOp::RenderPass { color_texture, clear, commands }` и `GpuOp::CopyTextureToBuffer`; `submit` теперь лочит ещё текстуры/render-пайплайны и гоняет настоящий `begin_render_pass` (один color attachment, `LoadOp::Clear`/`Load`, `StoreOp::Store`) + `copy_texture_to_buffer` для readback. Хелперы `texture_format`/`vertex_format`/`primitive_topology`/`texture_usages` мапят W3C-строки в wgpu; неизвестный формат отвергает пайплайн. **(2) lumen-js** (`webgpu.rs`, фича `webgpu`): нативы `_lumen_webgpu_texture_create`/`_destroy`, `_lumen_webgpu_render_pipeline_create` (единый JSON-конфиг — обход лимита rquickjs 7 closure-арг) / `_bind_group_layout` / `_destroy`; `_lumen_webgpu_submit` расширен на `renderPass` + `copyTexToBuf`. Шим `GPUTexture`/`GPUTextureView` несут реальный хэндл текстуры, `GPURenderPipeline` строит конфиг и отдаёт реальный `getBindGroupLayout`, `GPURenderPassEncoder` пишет command-list в энкодер, `copyTextureToBuffer` пишет readback-op, `GPUQueue.submit` маршрутит render-pass на GPU (иначе graceful Phase-0 fallback). **Graceful fallback:** нет фичи/GPU/хэндла → in-memory путь, без регрессии. Зависимостей не добавлено. Тесты: +6 в `webgpu_compute` (paint, GPU-gated — `render_pass_clears_texture_to_color` clear-цветом, `render_pipeline_draws_triangle` полноэкранный треугольник с readback зелёного пикселя, отказ неизвестного формата/текстуры, format-мапперы), +4 в `webgpu` (js — `render_pipeline_api_shape`, `render_pass_records_op_on_encoder`, loadOp 'load' без clear, `real_backend_renders_triangle` end-to-end на живом GPU). На этой машине GPU доступен → реальные GPU-пути исполнились (не skip). clippy `-p lumen-paint --features backend-wgpu` / `-p lumen-js --features webgpu` `--all-targets` чисто. **Stage 3 остаток:** present в canvas (показ текстуры на странице `<canvas>`). |
| 2026-06-21 | U-4c Stage 2 (под-этап 2): реальные compute-пайплайны + `dispatchWorkgroups` | Продолжение Stage 2. JS compute-путь `navigator.gpu` был Phase 0 no-op (`createComputePipeline`/`getBindGroupLayout`/`createBindGroup`/`beginComputePass`/`dispatchWorkgroups` — заглушки); теперь WGSL **реально считает на GPU**. **(1) lumen-paint** (`webgpu_compute.rs`): четыре реестра (шейдеры / compute-пайплайны / bind-group-layout / bind-group, монотонные хэндлы) + функции `shader_create` (настоящий `wgpu::ShaderModule`), `compute_pipeline_create` (auto-layout `wgpu::ComputePipeline` + entry point), `pipeline_bind_group_layout` (`getBindGroupLayout(idx)`), `bind_group_create(layout, &[BufferBindEntry])` (буферы по `@binding`-индексу), `compute_pipeline_destroy`. `GpuOp` получил `ComputePass { commands: Vec<ComputeCmd> }` (`SetPipeline`/`SetBindGroup`/`Dispatch`); `submit` записывает их в настоящий `wgpu::ComputePass` и диспетчит. Создание пайплайна/bind-group/layout — под общим `GPU_LOCK` validation-error-scope (`guarded_create`): wgpu-ошибки валидации дают `None`, а не отравленный объект. **(2) lumen-js** (`webgpu.rs`): 5 нативов (`_lumen_webgpu_shader_create`/`_compute_pipeline_create`/`_pipeline_bind_group_layout`/`_bind_group_create`/`_compute_pipeline_destroy`); JSON entries парсятся в lumen-js (`serde_json` — там) в `BufferBindEntry`, чтобы lumen-paint не тянул JSON-зависимость. Шим `GPUShaderModule`/`GPUComputePipeline`/`GPUBindGroupLayout`/`GPUBindGroup` несут непрозрачные `_id`; `GPUComputePassEncoder` пишет command-list в родительский энкодер, флашится на `queue.submit`. **Graceful fallback:** нет фичи/GPU → compute no-op (WGSL на CPU не исполнить), без регрессии. Зависимостей не добавлено. Тесты: 12 в `webgpu_compute` (paint, GPU-gated — `compute_pipeline_doubles_buffer` реальный round-trip удвоения, отказ плохого шейдера, fail submit с неизвестным пайплайном), 22 в `webgpu` (js — `real_backend_runs_compute_shader` end-to-end удвоение на живом GPU, запись pass-op в энкодер, дефолты dispatch y/z). На этой машине GPU доступен → реальные GPU-пути исполнились (не skip). clippy `-p lumen-paint --features backend-wgpu` / `-p lumen-js --features webgpu` `--all-targets` чисто. **Stage 2 остаток:** render-пайплайны + present в canvas. |
| 2026-06-21 | U-4c Stage 2 (под-этап 1): реальные `GPUBuffer` (create/write/map-readback/copy) | Продолжение Stage 1. JS `GPUBuffer` `navigator.gpu` был чистым in-memory шимом; теперь подложен **настоящий** `wgpu::Buffer`. **(1) lumen-paint** (`webgpu_compute.rs`, фича `backend-wgpu`): реестр буферов `OnceLock<Mutex<HashMap<u64, BufferEntry>>>` + монотонные хэндлы; `buffer_create` (биты `GPUBufferUsage` → `wgpu::BufferUsages`, размер выравнивается ↑ до кратного 4), `buffer_write` (`queue.write_buffer` + пустой submit-флаш), `buffer_read` (`map_async(Read)` + `device.poll(Wait)` + `get_mapped_range` + `unmap`), `buffer_destroy`; новый `GpuOp` enum + `submit(&[GpuOp])` исполняет записанные command-encoder операции (пока `CopyBufferToBuffer`) в одном энкодере + `queue.submit` — как браузер батчит работу на `GPUQueue.submit`. `validate_wgsl` обёрнут в `Mutex` (error scope wgpu — единый per-device стек; параллельные валидации ловили чужие ошибки — латентная гонка Stage 1, обнажённая новыми GPU-тестами). **(2) lumen-js** (`webgpu.rs`, фича `webgpu`): 5 нативов `_lumen_webgpu_buffer_{create,write,read,destroy}` + `_submit` (`buffer_read` — свободная fn с единым `'js`, отдаёт `Uint8Array`/null; `submit` парсит JSON-операции). Шим `GPUBuffer` держит непрозрачный `_id`; `queue.writeBuffer`, `commandEncoder.copyBufferToBuffer` (5-арг и 3-арг сигнатуры) + `queue.submit`, `mapAsync`/`getMappedRange`/`unmap` ходят через реальную GPU-память. **Graceful fallback:** нет хэндла/адаптера/фичи → исходный Phase 0 in-memory путь (без регрессии); `mappedAtCreation` эмулируется in-memory (реальный буфер остаётся unmapped и пишется через queue). Зависимостей не добавлено. Тесты: 9 в `webgpu_compute` (paint, GPU-gated — round-trip write/read, copyB2B, usage-маппинг, out-of-bounds, неизвестный хэндл), 18 в `webgpu` (js — write→copy→map round-trip, 3-арг copy, mappedAtCreation; на реальном GPU прогоняются через настоящий путь). clippy `-p lumen-paint --features backend-wgpu` / `-p lumen-js --features webgpu` `--all-targets` чисто. **Stage 2 остаток:** compute-пайплайны + `dispatchWorkgroups`, render + present. |
| 2026-06-21 | U-4c Stage 1: реальный WebGPU backend (adapter info + WGSL-валидация) | `navigator.gpu` был чистым in-memory JS-шимом (Phase 0): фейковый stub-адаптер, `createShaderModule` ничего не проверял, `getCompilationInfo()` всегда пуст. Stage 1 даёт `lumen-js` доступ к **реальному** wgpu-устройству. **(1) lumen-paint** — новый модуль `webgpu_compute.rs` (за `backend-wgpu`): ленивый headless-девайс (тот же выбор бэкенда, что у рендерера — DX12/PRIMARY, без surface, `OnceLock<Option<…>>`, `None` если нет GPU), `adapter_info()` (реальные vendor/architecture/device/description из `wgpu::Adapter::get_info`, PCI vendor-id → `nvidia`/`amd`/`intel`/…), `validate_wgsl()` (реальная компиляционная диагностика через error scope: `push_error_scope(Validation)` + `create_shader_module` + `pop_error_scope`). **(2) lumen-js** — новая фича `webgpu` (→ `lumen-paint/backend-wgpu`, включается shell-фичей `quickjs`); нативы `_lumen_webgpu_adapter_info`/`_lumen_webgpu_validate_shader` регистрируются **до** eval шима; шим `GPUAdapterInfo`/`GPUShaderModule` берут реальные данные через `typeof … === 'function'`-пробу. **Graceful fallback:** нет фичи / нет GPU → исходный stub, без регрессии. **Stage 2 (открыто, см. Слой 5):** реальные `GPUBuffer` create/write/map, compute-пайплайны + `dispatchWorkgroups`, render + present. Зависимостей не добавлено: `wgpu` уже workspace-dep, ребро `lumen-js → lumen-paint` уже существовало. Тесты: 4 в `webgpu_compute` (paint, GPU-gated), 15 в `webgpu` (js). clippy `-p lumen-paint --features backend-wgpu` / `-p lumen-js --features webgpu` чисто. |
| 2026-06-21 | BUG-197: UA-дефолт `td,th { padding:1px }` (TEST-69) | TEST-69 (border-spacing) расходился с Edge на **3.61%**. Диагноз «asymmetric border-spacing» оказался ложным — геометрия border-spacing пиксельно корректна (`--dump-layout`). Настоящая причина: Lumen не применял UA-дефолт `td, th { padding: 1px }` (HTML Rendering §15.3.8). Edge рисует каждую ячейку на 2px шире/выше (60px→62px, 80px→82px, замерено по пикселям), разница накапливалась по вертикали (нижние таблицы съезжали ~10px). **Фикс** (`layout/src/style.rs`): новая UA-функция `apply_ua_table_cell_padding` в pre-cascade фазе — `<td>`/`<th>` получают `padding: 1px`, атрибут `cellpadding=N` на ancestor-`<table>` переопределяет (вкл. `cellpadding="0"` для legacy layout-таблиц), author `padding` выигрывает. Обновлены 3 table-теста в `box_tree.rs` (+2px/ячейка); +5 регрессионных тестов в `style.rs`. **TEST-69: 3.61% → 0.00% PASS**; TEST-80 (collapse) улучшился 9.91%→8.73% (debtor BUG-128). 3020 lumen-layout тестов зелёные. |
| 2026-06-20 | Canvas 2D drawImage: `<img>` как источник | Три формы `drawImage` (3/5/9-арг) теперь принимают `HTMLImageElement` как источник. Архитектура: shell после `fetch_and_decode_images` вызывает `QuickJsRuntime::register_img_bitmaps()` → `img_bitmap_store` (thread-local на JS-потоке). JS-шим определяет тип источника через `_lumen_get_tag_name()` = 'IMG' и диспетчирует к `_lumen_canvas2d_draw_image[_crop]_from_img`. +4 теста в `dom.rs`. `CAPABILITIES.md` / `subsystems/js.md` обновлены. |
| 2026-06-20 | Canvas 2D drawImage: source-crop 9-арг формы | До этого 9-арг форма `drawImage(img,sx,sy,sw,sh,dx,dy,dw,dh)` игнорировала кроп-прямоугольник источника — натив-блиттер всегда масштабировал **весь** источник в dest-rect (ограничение, записанное при проводке paint-источников). Реализован честный source-crop. **(1) lumen-canvas:** новый `Context2D::draw_image_cropped(src, src_w, src_h, sx, sy, sw, sh, dx, dy, dw, dh)` сэмплирует только под-прямоугольник `(sx,sy,sw,sh)` источника (per-pixel src-координаты клампятся к границам битмапа — без wrap/tiling); старый `draw_image` стал тонкой обёрткой с полным кропом (поведение 3/5-арг форм неизменно). **(2) lumen-js натив:** `_lumen_canvas2d_draw_image_crop(dst_nid, src_nid, "sx,sy,sw,sh,dx,dy,dw,dh")` — 8 координат передаются CSV-строкой, чтобы уложиться в лимит rquickjs 7 closure-параметров (тот же приём, что `path2d_add_path`). **(3) JS-шим:** 9-арг ветка `drawImage` теперь зовёт crop-натив вместо полного блита. Источник — по-прежнему `<canvas>`/OffscreenCanvas с `__nid__`. Регрессия: +4 unit-теста lumen-canvas (full-blit 1:1, crop под-прямоугольника, изоляция top-left квадранта, zero-extent no-op) + 1 e2e `dom.rs` (`canvas_draw_image_9arg_crops_source_subrect`: кропнутая синяя колонка рисуется, красная исключена). Проверка: clippy `-p lumen-canvas -p lumen-js --all-targets` чисто; 55 lumen-canvas + 84 canvas-тестов lumen-js зелёные. `subsystems/js.md` обновлён. Остаётся (Canvas extensions): `<img>`-источник в `drawImage` (нет натив-биндинга). |
| 2026-06-20 | Canvas 2D paint-источники: проводка JS-шима к нативу | Phase-3 натив paint-источников `<canvas>` 2D (`canvas2d.rs`: `_lumen_canvas2d_create_{linear,radial,conic}_gradient`, `_gradient_add_color_stop`, `_set_{fill,stroke}_style_gradient/_pattern`, `_set_shadow_*`, `_draw_image`, `_put_image_data`) **существовал**, но JS-шим `getContext('2d')` (`dom.rs::_lumen_make_canvas2d_ctx`) возвращал **фейк-заглушки** — `createLinearGradient`→`{addColorStop:noop}`, `createPattern`→`null`, `drawImage`/`putImageData`→пустые, shadow* в no-op `_stubProps`. Ничего из этого не доходило до страницы. **Проведено:** (1) `createLinearGradient`/`createRadialGradient`/`createConicGradient` строят `CanvasGradient` через `_lumen_make_canvas_gradient(gid)` с `__gid__`+рабочим `addColorStop`; сеттеры `fillStyle`/`strokeStyle` диспетчеризуют по значению (объект с `__gid__`→gradient-натив, с `__patid__`→pattern-натив, иначе CSS-строка — старый путь), геттеры возвращают сохранённый объект/строку. (2) `createPattern(image,rep)`→`_create_pattern(image.__nid__,rep)`, отдаёт `{__patid__}` (или `null` для источника без backing-битмапа). (3) `shadowColor`/`shadowBlur`/`shadowOffsetX`/`shadowOffsetY` сняты со stub-листа в wired-`defineProperty` (спек range-checks). (4) `drawImage` формы 3/5/9-арг, источник — `<canvas>`/OffscreenCanvas с `__nid__`. (5) `putImageData` hex-кодирует `imageData.data` и зовёт натив. `__patid__` отделён от Path2D `__pid__` (диспетч fillStyle не путает паттерн с путём). **Ограничения** (записаны в STATUS/js.md): `<img>`-источник `drawImage` не поддержан (нет натив-биндинга чтения декодированного `<img>` в canvas-слой), 9-арг форма без source-crop. Регрессия: +8 e2e-тестов в `dom.rs` (форма gradient-объекта, radial/conic разные id, gradient-fillStyle красит пиксели, shadow-props, createPattern id + null-для-невалидного, drawImage canvas→canvas, putImageData). Проверка: clippy `-p lumen-js --all-targets` чисто, 83 canvas-теста (вкл. 8 новых) зелёные. `subsystems/js.md` обновлён. |
| 2026-06-20 | Ad-block Phase 2: EasyList `$`-опции по типу ресурса + party | До этого EasyList-парсер **срезал** весь `$`-хвост (`strip_options`) — правило `||ads^$script` блокировало всё, тип ресурса игнорировался. Реализована проводка контекста запроса. **(1) core:** trait `RequestFilter` получил defaulted `should_block_ctx(url, &RequestContext)`; новый `RequestContext { resource_type: Option<ResourceType>, third_party: Option<bool> }` + enum `ResourceType` (8 типов). Дефолт делегирует в `should_block` — hosts/safe-browsing-фильтры не тронуты. **(2) easylist:** `strip_options` → `split_options` (тот же guard против `$` внутри URL) + `parse_options` строит `RuleOptions { types: Option<u16>, third_party: Option<bool> }`; type-keywords (`script`/`image`/`stylesheet`/`font`/`xmlhttprequest`/`subdocument`/`media`/`other` + алиасы `css`/`xhr`/`frame`/`object`) — позитивные дают allow-list, `~`-негация даёт block-all-except; `third-party`/`first-party` (+`3p`/`1p`) задают party. `domain=` и немоделируемые модификаторы (`important`/`match-case`/`csp=`/…) **parsed-but-ignored** — правило не сужается (нет over-allow). `FilterEntry::matches(url, ctx)` = URL-матч AND опции; неизвестные поля контекста удовлетворяют любому ограничению (консервативный блок) → `should_block` и старые call-path ведут себя как раньше (ноль регрессий). **(3) network call-site:** `fetch_with_redirect` строит `RequestContext` из `destination` (→ `destination_to_resource_type`) и `top_level_site` (тот же host-suffix party-тест, что cookie-путь), зовёт `should_block_ctx`. Регрессия: +12 unit-тестов (type allow/deny, негация, party, domain-ignored, unmodelled-ignored, typed-exception, алиасы, split-guard) + 2 старых «option_*_stripped» переписаны в `unknown_context_blocks_typed_rule_like_phase1`. Проверка: clippy `-p lumen-core -p lumen-network --all-targets` чисто; 231 core + 793 network-lib (вкл. 55 filter) зелёные; storage/shell компилируются. Остаётся Phase 3 (UI подписок, handoff P3). |
| 2026-06-20 | BUG-187 этап 2: value-текст form controls (U-6, TEST-34) | Продолжение BUG-187: статический `value` text-инпутов **не рисовался** — поля выглядели пустыми, а Edge показывает значения (главный остаток-должник прошлого этапа). Добавлены два хелпера в `display_list.rs`. **(1) `emit_input_value_text`:** рисует `value` text-инпутов (`text`/`email`/`password`/`tel`/`url`/`number`/`search`/`date`/`datetime-local`/`time`/`month`/`week`) как `DrawText`, **вертикально центрированный** в content-box (border-box − border − 2px inset) и **клиппленный** по нему (`PushClipRect`/`PopClip`) — длинные значения не вытекают за рамку. **Password маскируется** U+2022 BULLET по числу символов (plaintext не рисуется). **(2) Button-инпуты** (`submit`/`reset`/`button`) рисуют `value` как **горизонтально центрированный** лейбл (центрирование по аппроксимации ширины `font_size·0.5·chars` — тот же приём, что SVG text-anchor, реального TextMeasurer в крейте нет); `default_button_label` даёт UA-дефолты «Submit»/«Reset» при пустом `value`. Checkable-типы (checkbox/radio) проваливаются к индикатору ниже без изменений; `color`/`range`/`file`/`hidden`/`image` сюда как текст не доходят. Пиксельно: поля совпали с Edge (user@example.com / •••••• / 42 / query / Submit / disabled input), TEST-34 (ipc) **2.95%** — в пределах ±2% noise-band, `KNOWN_DEBTORS` baseline 3.02% сохранён. Остаток-должник BUG-187: placeholder пустых полей не рисуется (отдельная фича) + checkbox-галочка/radio-тик стилем расходятся с Edge + font-parity лейблов (BUG-128). Регрессия: +6 paint-тестов (`text_input_paints_value`, `empty_text_input_paints_no_value`, `password_input_masks_value`, `submit_input_default_label`, `submit_input_value_label`, `number_search_inputs_paint_value`). Проверка: clippy `-p lumen-paint --all-targets` чисто, 761 lumen-paint тест зелёный; TEST-34 ipc DEBTOR 2.95%. |
| 2026-06-20 | BUG-187: form controls static rendering (U-6, TEST-34) | Статическая отрисовка form controls расходилась с Edge на **4.78%** — главным образом потому, что контролы шли `display:block` (каждый на своей строке, разъезжаясь в столбик). **(1) inline-block-поток:** `default_display` (`style.rs`) теперь даёт `Display::InlineBlock` для `<input>`/`<button>`/`<select>`/`<selectlist>`/`<textarea>`/`<meter>`/`<progress>` (HTML rendering §15.5), а `is_inline_block` (`box_tree.rs`) перестал исключать form controls — их `BoxKind::FormControl` собирается в анонимный `InlineBlockRow` и течёт в строку рядом с текстом/соседними контролами, как в Edge (author `display:` поверх — выигрывает; размер берётся из явной `width` через `preferred_inline_block_width`). **(2) `<option>` не утекает:** UA-дефолт `<option>` → `Display::None` (§15.5.3) — текст опций больше не вытекает под/над закрытым `<select>` (ярлык читается из DOM `collect_select_label`); `<optgroup>` оставлен в потоке, чтобы стили вложенных опций считались для `:disabled`/`:checked`. **(3) radio-точка — круг:** индикатор checked-radio рисовался квадратным `FillRect`; теперь `FillRoundedRect` с радиусом в полстороны (чекбокс остаётся квадратом). **(4) color-swatch:** `<input type=color>` рисует свой value-цвет (дефолт `#000000`), игнорируя author `background`, как нативный виджет. Пиксельно: TEST-34 **4.78% → 3.02%** (`KNOWN_DEBTORS` `'34': ('BUG-187', 3.02)`). Остаток-должник: статический value-текст у text-инпутов (`text`/`email`/`password`/`number`/`search`/`submit`) **не рисуется** (отдельная фича: рендер value + маскирование password + клиппинг + вертикальное центрирование) + font-parity лейблов кнопок/опции (Inter vs Edge, BUG-128) + вертикальное центрирование текста контролов; BUG-187 остаётся OPEN-должником. Регрессия: +5 layout-тестов (form controls inline-block, author-override, option none) + 4 paint-теста (radio-круг/checkbox-квадрат, color-swatch value/default); `first_element_child` тест-хелпер спускается сквозь анонимный `InlineBlockRow`. Проверка: clippy `-p lumen-layout`/`-p lumen-paint` чисто, 2937 lumen-layout + 762 lumen-paint + 33 layout-integration + driver зелёные; полный graphic-прогон без регрессий (TEST-26/97 99% — артефакт пустого gdigrab-захвата, на ре-ране PASS/2.78%). |
| 2026-06-20 | U-4b: live-aliasing памяти WASM (emscripten `HEAP32`) | Экспортируемый `Memory.buffer` перестал быть снимком-копией, пересоздаваемой на каждое чтение, — паттерн `HEAP32 = new Int32Array(memory.buffer)` стал когерентным. Теперь это **один стабильный** JS `ArrayBuffer` (`makeExportMemory._buf`, строится один раз через новый натив `__lumen_wasm_mem_buffer(instId)` → `wasm::mem_read_all`, единичная bulk-копия всей линейной памяти), синхронизируемый с Rust-памятью **на границах WASM-вызовов**: каждый экспорт-врапер (`makeExportFn`, получает память через общий `memRef`) до `__lumen_wasm_call` зовёт `mem._syncIn()` (JS-буфер → Rust, `__lumen_wasm_mem_write`), после — `mem._syncOut()` (Rust → JS **на месте**, `new Uint8Array(_buf).set(...)`). Поэтому записи JS через HEAP-view видны WASM в следующем вызове, записи WASM видны JS через **тот же** буфер (идентичность сохраняется → захваченный `HEAP32` живёт между вызовами). Рост памяти (JS `Memory.grow` или инструкция `memory.grow`, детектится в `_syncOut` по смене числа страниц) выделяет свежий бо́льший буфер — спек-семантика detach-on-grow, потребитель пере-захватывает HEAP-view. Цена: полная in/out-копия памяти на вызов, когда память экспортируется (иначе sync пропускается) — задокументированный trade-off корректность/скорость, точный для одно-агентной модели (ADR-014). Остаточные границы (задокументированы): host-импорт не видит записи, сделанные раньше в том же in-flight вызове; *импортируемая* `Memory` не алиасит внутреннюю память инстанса (алиасинг только для экспортируемой). `mem.read`/`mem.write` оставлены MVP-лазейками в обход sync. +5 unit-тестов (WASM-запись видна через захваченный view, JS-запись видна WASM, идентичность буфера стабильна между вызовами, JS-grow увеличивает буфер, двунаправленный round-trip через HEAP). Проверка: clippy `-p lumen-js --all-targets` чисто, 61 wasm-тест (вкл. 5 новых) + 13 webassembly-тестов зелёные. CAPABILITIES/`subsystems/js.md` приведены в соответствие (снят тезис «снимок без live-aliasing»). **Слой 5: остаётся только U-4c (реальный WebGPU backend, XL).** |
| 2026-06-20 | U-4a (часть 4, финал): JS-уровневые `SharedArrayBuffer`/`Atomics` + шим `Atomics.waitAsync` | Замыкает U-4a JS-обвязкой. Проверка показала: QuickJS **уже** даёт нативные JS-уровневые `SharedArrayBuffer` (включая growable, `slice`) и `Atomics` (`load`/`store`/`add`/`sub`/`and`/`or`/`xor`/`exchange`/`compareExchange`/`notify`/`isLockFree`) — отдельные объекты заводить не нужно. Синхронный `Atomics.wait` спек-корректно бросает `TypeError: cannot block in this thread`, потому что Lumen исполняет весь JS на **единственном неблокирующем агенте** (один JS-поток, ADR-014) — ровно как главный поток браузера. Единственный пробел — `Atomics.waitAsync` (ES2024, Stage 4) был `undefined`. Реализован чистый JS-шим в `tc39_proposals.rs` (секция 11, ставится после DOM, нативных бинд не требует): валидирует представление (Int32Array/BigInt64Array поверх `SharedArrayBuffer`, иначе `TypeError`/`RangeError`); при несовпадении значения возвращает синхронно `{async:false,value:'not-equal'}`, при неположительном таймауте — `{async:false,value:'timed-out'}`; иначе паркует FIFO-ожидателя (ключ = блок данных SAB + байтовое смещение) и возвращает `{async:true,value:<Promise>}`. `Atomics.notify` обёрнут так, что помимо нативного пробуждения блокирующих агентов (на одном агенте их нет) резолвит совпадающих async-ожидателей в `'ok'`, добавляя их число к своему результату (как требует спек — RemoveWaiter применяется и к async-ожидателям); конечный таймаут резолвит `'timed-out'` через `setTimeout`. `Atomics.wait` оставлен бросающим — это спек-поведение неблокирующего агента; WASM-уровневые threads используют собственный неблокирующий интерпретатор (часть 2), независимый от JS Atomics. +8 unit-тестов (is-function, sync not-equal, zero-timeout, notify→ok + счётчик, notify по другому индексу не будит, non-shared `TypeError`, не-целочисленный массив `TypeError`, BigInt64-round-trip). Проверка: clippy `-p lumen-js --all-targets` чисто, 51 тест `tc39_proposals` (вкл. 8 новых) зелёные. CAPABILITIES/`subsystems/js.md` приведены в соответствие (JS-уровневые SAB/Atomics есть; снят устаревший тезис «нет JS-уровневых SharedArrayBuffer/Atomics» — он относился лишь к тому, что WASM-память не алиасит JS SAB). |
| 2026-06-20 | U-4a (часть 3): WASM relaxed-SIMD (`0xFD` sub `0x100..=0x113`) | WebAssembly-интерпретатор (`lumen-js::wasm`) перестал трапить на relaxed-SIMD — реализован **весь набор relaxed-операций** (предложение relaxed-SIMD, префикс `0xFD`, sub-опкоды `0x100..=0x113`). До этого парсер декодировал их в `Instr::Simd(sub)`, но `exec_simd` падал на catch-all `unsupported SIMD sub-opcode`. Новый `simd.rs::exec_simd_relaxed` (роутится из `exec_simd` по `0x100..=0x113`): relaxed-операции спецификацией допускают implementation-defined результат в краевых случаях (NaN, out-of-range swizzle-индексы, fused-vs-split multiply-add) — выбрана **детерминированная strict-семантика** (всегда корректный выбор). Где у relaxed-опа есть точный strict-двойник, делегируем в существующий код без дублирования lane-логики: `relaxed_swizzle`→swizzle (14), `relaxed_trunc_f32x4_s/u`+`_f64x2_*_zero`→trunc_sat (248/249/252/253), `relaxed_laneselect` (i8/16/32/64)→bitselect (82, побайтово идентичен для всех ширин), `relaxed_min/max f32x4/f64x2`→min/max (232/233/244/245), `relaxed_q15mulr_s`→q15mulr_sat (130). Реализованы прямо: `relaxed_madd`/`relaxed_nmadd` (f32x4/f64x2 FMA `a*b+c` / `-(a*b)+c`, 3 операнда), `relaxed_dot_i8x16_i7x16_s` (попарные произведения знаковых i8-лейнов → i16x8 с насыщением), `relaxed_dot_i8x16_i7x16_add_s` (тот же dot + аккумуляция в i32x4 операнд `c`). Парсер не менялся (relaxed-опы immediate-free, уже шли в `Instr::Simd`); поправлены устаревшие doc-комменты «rejected at decode». +12 unit-тестов (madd/nmadd f32x4+f64x2, laneselect, min/max, trunc, swizzle, q15mulr, оба dot, decode-accept). Проверка: clippy `-p lumen-js --all-targets` чисто, все wasm-тесты (вкл. 12 новых) зелёные. **Остаток U-4a (опц.)**: JS-уровневые `SharedArrayBuffer`/`Atomics`-объекты. |
| 2026-06-20 | U-4a (часть 2): WASM threads / atomics (`0xFE`) | WebAssembly-интерпретатор (`lumen-js::wasm`) перестал отклонять threads-предложение на декоде — реализован **весь набор atomic-операций** (префикс `0xFE`, sub 0x00..=0x02 и 0x10..=0x4E) в **однопоточной (non-blocking) семантике**: с единственным агентом каждый RMW тривиально атомарен. `parser.rs` декодирует `0xFE` в новые `Instr`-варианты `Atomic { sub, offset }` (memarg-несущие notify/wait/load/store/rmw/cmpxchg) и `AtomicFence` (`0xFE 0x03`, reserved-байт). `interp.rs::exec_atomic` исполняет: atomic load (`0x10..=0x16`, переиспользует plain `load`), atomic store (`0x17..=0x1D`, plain `store`), бинарные RMW add/sub/and/or/xor/xchg (`0x1E..=0x47`, 6 групп × 7 ширин — i32/i64/8u/16u/32u — возвращают старое значение), cmpxchg (`0x48..=0x4E`, сравнение с маской по ширине), `memory.atomic.notify` (всегда 0 разбуженных), `wait32`/`wait64` (никогда не блокируют: `1`=not-equal если ячейка изменилась, иначе `2`=timed-out), `atomic.fence` (no-op). Все доступы **трапят на невыровненный адрес** (natural alignment) по спеку; sub-ширинные RMW читают unsigned zero-extended и пишут младшие байты. Shared-memory флаг `0x03` (max+shared) уже парсился `limits()`. `validate`/`compile` теперь принимают threads-модули; relaxed-SIMD и неизвестные `0xFE` sub по-прежнему отклоняются на декоде. +18 unit-тестов (store/load roundtrip, rmw add/xor/xchg + запись суммы, byte-wide rmw8 с wrap, cmpxchg success/mismatch, i64 rmw, notify=0, wait not-equal/timed-out, fence-nop, unaligned-trap, decode-validate, relaxed-still-rejected). Проверка: clippy `-p lumen-js --all-targets` чисто, все 47 wasm-тестов (вкл. 18 новых) зелёные. |
| 2026-06-20 | U-4a (часть 1): WASM fixed-width SIMD (`v128` / `0xFD`) | WebAssembly-интерпретатор (`lumen-js::wasm`) перестал отклонять SIMD на декоде — реализован **полный набор fixed-width SIMD** (предложение `v128`, префикс `0xFD`, sub-opcodes 0..=255). Новый `value.rs`: `Value::V128([u8;16])` + `ValType::V128` (тег `0x7B`, blocktype `-5`). `parser.rs` декодирует `0xFD`-опкоды в выделенные `Instr`-варианты (`V128Const`/`V128Load`/`V128Store`/`V128LoadLane`/`V128StoreLane`/`Shuffle`/`SimdLane`/`Simd`) — корректно разбирает иммедиаты (16 байт const, 16 lane-индексов shuffle, memarg, lane-байт). Новый модуль `simd.rs` (≈700 строк) исполняет операции над сырыми 16 LE-байтами: const, все memarg-загрузки (`loadNxM_s/u` расширяющие, `loadN_splat`, `loadN_zero`) + lane load/store, splat, swizzle/shuffle, extract/replace lane, вся целочисленная и плавающая арифметика/сравнения/min-max/abs/neg/sqrt/округление, побитовые (`and`/`or`/`xor`/`not`/`andnot`/`bitselect`/`any_true`), сдвиги, `all_true`/`bitmask`, насыщающие add/sub, `avgr_u`, `q15mulr_sat`, narrow/extend(low/high)/extmul/dot/extadd_pairwise, и конверсии float↔int (`trunc_sat`/`convert`/`demote`/`promote`). NaN/`-0` семантика min/max и `trunc_sat` по спеку. v128 не имеет представления на JS-границе (схлопывается в 0, спек-валидный вызов туда не попадает). `validate`/`compile` теперь принимают SIMD-модули. Threads (атомики `0xFE`) и relaxed-SIMD (sub ≥256) по-прежнему отклоняются на декоде — **остаток U-4a**. +15 unit-тестов (i32x4/i8x16/f32x4 арифметика, extract/replace/splat, eq-mask, v128 store↔load, shuffle, bitselect, extend_low, dot, add_sat_s, trunc_sat, decode-not-rejected). Проверка: clippy `-p lumen-js --all-targets` чисто, 2199 тестов lumen-js зелёные. |
| 2026-06-20 | BUG-224: multi-column auto-height — НЕ регрессия, ре-базлайн юнит-теста | `test_33_multi_column` падал на чистом main (`.mc[4] should be 660x88, got 660x64`), баг приписывался влитию `p3-bug198-objectfit-svg`. Разбор: (1) diff `p3-bug198` по `box_tree.rs` — **только SVG-функции**, multicol не тронут; (2) реальное изменение высоты внёс **BUG-186** (`d08021b1`, фрагментация колонок, TEST-33 14.89%→0.12%) — корректное приближение к Edge; (3) после фрагментации Edge-высота `mc[4]` = **64px** (две 36px col-sm фрагментируются на 24px по 3 колонкам = 72/3, + span 16px, + ещё 24px = 24+16+24), **подтверждено напрямую** Edge headless `getBoundingClientRect().height → 64` + пиксельным паритетом TEST-33 ≈0.1%; (4) юнит-тест хранил устаревший ground-truth 88px (atomic one-box-per-column до BUG-186, который Edge не воспроизводит). Код движка корректен и **не менялся** — ре-базлайн `crates/driver/tests/test_33.rs` 88→64 + комментарий с выводом. BUGS.md: BUG-224 OPEN→FIXED. 68 тестов lumen-driver зелёные. |
| 2026-06-20 | BUG-221: паритет CPU-снимка с femtovg (border-radius / radial / images) — Слой 4 наблюдаемость | CPU-бэкенд снимка (`cpu_raster.rs`, на котором работают `--screenshot` и `--ipc-server`/`run.py --ipc`) расходился с femtovg-окном на трёх примитивах; доведён до паритета по `run.py --ipc`. **(1) border-radius** (TEST-36 **60.60%→0.47%**): `rasterize_fill_rounded_rect` не клампил радиусы к боксу — `border-radius:999px` пилюли уводили кубические control-точки далеко за прямоугольник, заливая гигантский диагональный клин (salmon `#fc8181`) поверх страницы. Теперь радиусы клампятся (`CornerRadii::clamped_to_box`) и контур строится общим kappa-Безье `push_rounded_rect_outline` (тот же, что rounded-border ring). **(2) gradients** (TEST-39 **10.68%→1.40%**): `rasterize_radial_gradient` рисовал анизотропный эллипс (`rx`/`ry`), femtovg-окно — изотропный **круг** радиусом `hypot(dx,dy)` (farthest-corner); CPU переписан на круг — кольца совпали с окном/Edge, попутно gradient-mask TEST-26 **5.02%→0.00%**, clip-path TEST-31 0.59%→0.06%. **(3) images** (TEST-18 **52.22%→2.15%**): `DrawImage`/`LazyImageSlot` рисовали серый placeholder; декодированные пиксели теперь прокидываются в `rasterize_cpu` (`render_source_to_png` передаёт `parsed.images`, бывший неиспользуемый `_unused_layers`-слот `render_to_image_cpu` перепрофилирован под `images`), рисуются с `object-fit`/`object-position` через общий `display_list::fit_image_rect`, и — как в femtovg (`resolve_image_for_rect`+`downscale_target`) — пред-уменьшаются area-averaged `lumen_image::resize_area_avg` до placement-размера (bilinear-сэмплинг полноразмерного фото иначе шумел против Edge). Все три сошлись к gdigrab-паритету (gdigrab: 1.11%/1.62%/2.11%) → **разблокирует полную замену gdigrab на `run.py --ipc`** (TAB-7, ранее опционален из-за этого бага). Затронут только CPU-снимок — femtovg-окно и gdigrab-гейт не менялись (правки изолированы в `cpu_raster.rs` + плумбинг сигнатуры). Регрессия: +4 unit-теста (`fill_rounded_rect_pill_radius_clamped_to_box`, `draw_image_paints_decoded_pixels`, `draw_image_missing_src_is_placeholder`, `radial_gradient_is_circular_not_elliptical`); 801 тест `lumen-paint` зелёный. NB: полный `run.py --ipc` показывает дельту против устаревшего CPU-baseline (другие merge с прошлого полного прогона) — затронутые мной тесты с 0 примитивов (16/100) подтверждают изоляцию. Проверка: clippy `-p lumen-paint --all-targets` чисто, TEST-36/39/18 сошлись. |
| 2026-06-20 | BUG-144: backdrop-filter colour-matrix/combo на CPU (U-6, TEST-30 Filter) | TEST-30 (CSS Filter Effects) расходился с Edge на **7.56%**, весь остаток сосредоточен в row 4 (`backdrop-filter`): карты `grayscale`/`brightness`/`invert` и комбо `blur(4px) brightness(0.6)` рисовались тёмно-синими (цвет `__f`-фона) вместо отфильтрованного радужного градиента под ними. Корень — `apply_backdrop_filters` (`crates/engine/paint/src/backends/femtovg_backend.rs`): для colour-matrix-фильтров код брал первый `screenshot()` текущего RT (opacity-FBO с градиентом), **загружал его в текстуру** `create_image`, переключал на неё render target и снимал `screenshot()` **повторно** — но FBO, подложенный `create_image`-загрузкой (равно как и `filter_image`-назначением для combo), читается через `glReadPixels` обратно **пустым**, поэтому `apply_filter_rgba` применялся к чёрным пикселям → тёмная карта. Работали только `bd-blur` (чистый GPU-blur, композитится напрямую без re-screenshot) и `bd-none` (контроль). **Фикс:** весь chain backdrop-фильтров переведён на CPU по первому скриншоту — blur через существующий `box_blur_rgba` (3-pass box ≈ Gaussian), colour-matrix через общий `apply_filter_rgba`, единственный upload результата; мёртвый GPU-round-trip (upload→switch RT→re-screenshot) удалён целиком (−69 строк нетто, `box_blur_rgba` снят с `#[allow(dead_code)]`). Теперь все 6 backdrop-карт рендерят отфильтрованный контент: gray/bright/invert совпадают с Edge, combo стал корректно-тёмно-бирюзовым. Пиксельно: TEST-30 **7.56% → 4.36%**, `KNOWN_DEBTORS` сбит (`'30': ('BUG-144', 4.36)`). Остаток-должник: `box_blur_rgba` — аппроксимация, не точный Gaussian Edge, + edge-bleed (blur у верхней кромки карты затягивает тёмный `__f`-фон над bd-scene) на двух blur-картах (cards 1,5); BUG-144 остаётся OPEN-должником. Регрессия: integration — TEST-30 (баг в GL-зависимом коде, юнит невозможен); 70 тестов `lumen-paint` зелёные (включая `box_blur_rgba_*`/`apply_filter_rgba_*`). Проверка: clippy `-p lumen-paint --all-targets` чисто, TEST-00 PASS 0.00% + TEST-30 DEBTOR 4.36%. |
| 2026-06-19 | BUG-085: repeating-градиенты + hard-stop tail-fill (U-6, TEST-39 Gradients) | TEST-39 (CSS gradients) расходился с Edge на **12.05%**. Корень — в femtovg-бэкенде (`crates/engine/paint/src/backends/femtovg_backend.rs`): обработчики `DrawLinearGradient`/`DrawRadialGradient` **игнорировали флаг `repeating`** (`{ .., }`) и просто clamp-или позиции стопов в [0,1]. femtovg синтезирует 256-тексельную текстуру градиента (`gradient_store.rs::linear_gradient_stops`): заполняет от 0 до первого стопа и между соседними стопами, но **область за последним стопом оставляет прозрачной** и игнорирует позиции вне [0,1]. Поэтому (1) `repeating-linear/-radial-gradient` (rows 2-3 теста) рисовали один clamp-период вместо повторения, (2) hard-stop без завершающего стопа (`… green 50%`) рисовал только первую половину. **Фикс:** новый `femtovg_stops(stops, line_len, repeating)` заменил `resolve_stops` на всех Draw-путях: для **non-repeating** продлевает последний цвет до 1.0 если последний стоп < 1.0 (femtovg сам хвост не дозаполняет); для **repeating** замощает паттерн (период = `last − first`) по всей линии [0,1] монотонными стопами (граница повтора = совпадающие позиции, femtovg даёт hard-переход). Попутно `linear_gradient_line_len(w,h,angle)` даёт корректную длину CSS-линии для непрямых углов (45°) — px-стопы делятся на неё, а не на `rect.width`. `resolve_stops` стал тонкой обёрткой `femtovg_stops(..., false)` (mask-градиенты тоже получили tail-fill). Проверено пиксельно: TEST-39 **12.05% → 1.62%**, row 1 (простые linear) совпадает с Edge пиксель-в-пиксель (вне diff-региона, регрессии нет); попутно TEST-30 (BUG-144) **10.48% → 7.56%** (row 2 hard-stop). Остаток TEST-39 → `KNOWN_DEBTORS` (`'39': ('BUG-085', 1.62)`): 256-тексельная квантизация градиент-текстуры femtovg на repeating-границах + radial AA vs Edge + gdigrab-шум; BUG-085 остаётся OPEN-должником. Регрессия: +4 unit-теста (`femtovg_stops_*`). Проверка: clippy `-p lumen-paint` чисто, тесты lumen-paint зелёные, TEST-00 PASS 0.00% + TEST-39 DEBTOR 1.62% + TEST-30 DEBTOR 7.56%. |
| 2026-06-19 | BUG-126: position-area placement (U-6, TEST-77 Anchor Positioning) | TEST-77 (CSS Anchor Positioning L1) расходился с Edge на **53.45%**. Корень: `resolve_inset_area` (`crates/engine/layout/src/anchor.rs`) всегда **растягивал** anchored-элемент на всю ячейку position-area grid, перетирая `width`/`height` размером band — для элементов с определённым размером (row 1 теста: 60×60 боксы) это давало 8 растянутых прямоугольников вместо аккуратной 3×3 сетки вокруг якоря. **Фикс:** введён `AxisSize { Auto, Fixed(f32) }` (used-size элемента по оси); `band_region` возвращает `[start,end]` band; `place_axis` для `Auto` растягивает на band, для `Fixed` — сохраняет размер и выравнивает к якорю через `align_in_band` (start-side прижимается к дальнему краю band, end-side — к ближнему, center/span — по центру; дефолтный position-area self-alignment §5.1). Оба call-site (`lay_out_abs_children`, `apply_anchor_positions_rec`) передают `AxisSize` из `style.width/height.is_some()` + учёт `anchor-size()`. Проверено `--dump-layout`: все 8 corner/edge боксов + 3 span-бара на спек-корректных позициях. Пиксельный diff vs Edge(`position-area`): **container 1 (3×3 сетка) совпадает пиксель-в-пиксель** (53.45% → 12.94%). Остаток-должник: (1) тест использует устаревшее `inset-area`, которое текущий Edge игнорирует (поддерживает только переименованное `position-area`); (2) span-ряд container 2 — Lumen по спеку растягивает auto-width элемент на band (видны бары), Edge не отрисовывает `span-*` вовсе. Lumen спек-корректнее Edge → `KNOWN_DEBTORS` (`'77': ('BUG-126', 12.94)`), BUG-126 остаётся OPEN-должником. Регрессия: +5 unit-тестов (`fixed_*`, `auto_width_fixed_height_stretches_only_width`). Проверка: clippy `-p lumen-layout` чисто, lumen-layout тесты зелёные, TEST-00 PASS 0.00% + TEST-77 DEBTOR 12.94%. |
| 2026-06-19 | BUG-104: column flex-grow распределяет free space (U-6, TEST-62 Scroll Snap) | TEST-62 (CSS Scroll Snap L1) расходился с Edge на **63.70%**. Тест — статический рендер: проверяет геометрию контейнеров/секций внутри скролл-контейнеров (scroll-snap-* парсятся и хранятся; runtime-снаппинг — отдельная задача). Реальная причина расхождения оказалась **не scroll snap**, а баг column flex-grow. `lay_out_flex` для column-направления хардкодил `container_main = 0.0` и `free_space = 0.0` (`box_tree.rs:7191/7330/7356`) → flex-grow никогда не распределял свободное место по главной (вертикальной) оси, даже когда у контейнера определённая высота. На странице правая колонка `.right-col` (`display:flex; flex-direction:column; flex:1`, без явной height, растянута row-родителем `.__f` до 702px) держала трёх детей `flex:1; height:220px` — они схлопывались в высоту ≈0 (видны только полоски-бордюры), их `height:100%`-потомки давали 0. **Фикс (3 части):** (1) `lay_out_flex` получил параметр `explicit_main: Option<f32>` — определённый content-box main-размер для column; `main_definite` теперь включает grow/shrink и justify-content remaining для column так же, как для row; (2) dispatch (`box_tree.rs:5097`) вычисляет `flex_explicit_main` из `s.height` (случай явной высоты column-контейнера); (3) случай растяжения родителем (нет явной height): column-flex ребёнок получает определённый main только **после** align-items:stretch, поэтому в ветке Stretch align-блока добавлен re-layout такого ребёнка с определённой высотой (force border-box + explicit height) — его `flex-grow` дети заполняют растянутую высоту. Геометрия стала пиксель-точной (diff: все цветные заливки контейнеров идентичны Edge). Остаток **2.32%** = font-parity меток секций (A/1/NW/Stop, Inter vs Edge — BUG-128) + border-radius edge-AA (BUG-176) → запись в `KNOWN_DEBTORS` (`run.py`: `'62': ('BUG-128', 2.32)`), TEST-62 теперь DEBTOR, не FAIL. Регрессия: 2 unit-теста (`flex_column_explicit_height_grows_items`, `flex_stretched_column_child_grows_its_items`). Проверка: clippy `-p lumen-layout` чисто, 2932 теста lumen-layout зелёные, TEST-00 PASS 0.00% + TEST-62 DEBTOR 2.32%. |
| 2026-06-19 | BUG-171 этап 2: финальный pipeline вне UI-потока (U-1 этап 2) | Тяжёлый финальный рендер страницы (`render_bytes`: fetch внешних скриптов → исполнение QuickJS ~1.9 с → fetch+декод картинок/CSS/`@font-face` → layout) исполнялся синхронно в обработчике `LoadEvent::LoadDone` прямо на потоке winit event loop — пока он крутился, окно не обрабатывало `RedrawRequested`/ввод и **замерзало** на всю CPU-фазу загрузки. Этап 1 (префетч-кэш) уже снял с UI-потока сеть подресурсов; этап 2 снимает оставшийся CPU. Теперь `LoadDone` читает UI-only входы (viewport из renderer, storage-хэндлы через `&mut self.ls_storage`), клонирует остальное (`Send`: `event_sink`/`cookie_jar`/`sw_worker_store`/`cache_store`/`hyp_provider`/`load_proxy`), `std::mem::take`-ает `preload_dispatched` и спавнит `std::thread`, который зовёт `render_bytes`; готовый результат улетает назад новым событием `LoadEvent::RenderDone(Box<RenderOutcome>, generation)`, а UI-поток применяет его через прежний `apply_loaded_page` (+ доставка Navigation Timing). Пока worker считает, event loop жив — окно перерисовывает последний streaming-кадр и реагирует на ввод. **Разблокировано B-1/ADR-014:** `QuickJsRuntime` теперь `Send`-хэндл (рантайм живёт на своём потоке), поэтому JS-контекст создаётся на worker-е и безопасно пересылается на UI внутри `RenderDone`. Опорные правки: `PersistentJs: Send` (чтобы `Box<dyn PersistentJs>` пересёк границу потоков), `hyp_provider: Arc<KnuthLiangHyphenation>` (делёж провайдера с worker-ом без потери прогретого кэша словарей), тип-алиас `RenderedPage`. U-1 generation-guard отбрасывает `RenderDone` устаревшей навигации (её страница + JS-хэндл дропаются, JS-поток джойнится); актуальный `RenderDone` восстанавливает забранный `preload_dispatched`. `render_bytes`/`parse_and_layout` уже были чистыми свободными функциями на `Send`/`Arc`-входах — поведение pipeline не изменилось, изменилось лишь *где* он исполняется. Регрессионная защита компайл-тайм: `render_pipeline_payload_is_send` ассертит `Send` у `RenderOutcome`/`LoadEvent`/`Box<dyn PersistentJs>`/`RawPage`. Проверка: clippy `-p lumen-shell` чисто, 1348 тестов lumen-shell зелёные. |
| 2026-06-19 | BUG-167: Fullscreen пересчитывает вьюпорт (U-5) | Вход/выход Fullscreen API (`requestFullscreen()`/`exitFullscreen()`/`Escape` → `window.set_fullscreen(..)`) растягивал окно ОС на весь десктоп, но вьюпорт страницы оставался в исходном ~1024×720: `vw`/`vh`, `innerWidth/Height` и `auto`-центрирование считались от старого размера, контент не растягивался. Корень: `set_fullscreen` применяет новый размер **асинхронно** — `inner_size()` сразу после вызова ещё возвращает старый, неоткуда взять финальные размеры для resize; обычный `WindowEvent::Resized` не гарантирован на всех платформах при toggle. Фикс (`crates/shell/src/main.rs`): поле `Lumen.fullscreen_resize_pending: Option<(u32,u32,u8)>` (физический pre-toggle inner-size + бюджет опросов); `arm_fullscreen_resize(prev)` вооружает reconciliation сразу после `set_fullscreen` (вход через `take_fullscreen_requests`, выход по `Escape`) и будит loop `request_redraw`; `poll_fullscreen_resize()` из `about_to_wait` каждую итерацию ждёт, пока `inner_size()` отличается от pre-toggle, затем прогоняет тот же путь, что `WindowEvent::Resized` (`renderer.resize` + `relayout` + доставка `ResizeObserver`). Бюджет 240 опросов (~4 с @60fps) защищает от спина при no-op toggle. Чистое решение вынесено в `decide_fullscreen_poll(prev, cur, attempts) -> FullscreenPoll{Apply/Wait/Done}` — 6 юнит-тестов без реального окна. Проверка: clippy `-p lumen-shell` чисто, 6 fullscreen-тестов + остальной шелл зелёные. |
| 2026-06-19 | BUG-172: дедуп двойной загрузки картинок (общий per-load кэш) | Картинки `<img>` на streaming-страницах качались и декодировались дважды: прогрессивный streaming-загрузчик (`spawn_stream_image_loads`, PH1-2c) и финальный `fetch_and_decode_images` в `parse_and_layout` независимо брали `collect_image_requests` и качали+декодили одни и те же `src`. Закрыто вариантом A из бага — общий per-load кэш декодированных картинок. Новый `crates/shell/src/image_cache.rs`: process-global `DecodedImageCache` (`IMAGE_CACHE`) по образцу `prefetch::PrefetchCache` — generation-scoped, in-flight дедуп через `Condvar` (первый вызыватель декодирует, конкурентные блокируются и делят результат), кэширует и успех, и неудачу (`Option<DecodedImage>`), стейл-поколение байпасит кэш. Значение `DecodedImage::{Static, Animated}` держится за `Arc`, поэтому cache-hit клонирует указатель, а не пиксели (полную копию делает только потребитель под owned `Image`). Единая fetch+decode-логика обоих путей вынесена в свободную `decode_image()` (один источник истины, прежнее логирование сохранено). Streaming-поток зовёт `get_or_decode(self.load_generation, …)` и шлёт `LoadEvent::ImageDecoded`; финальный проход — `get_or_decode_current(…)` в `parallel_map`, при попадании пропускает сеть+декод и лишь считает `wants_intrinsic`/`is_lazy` + клонирует пиксели. Сброс: `IMAGE_CACHE.reset(generation)` на старте навигации (рядом с `PREFETCH_CACHE.reset` в `start_streaming_load`) и `reset_new()` per-render в headless `render_source_to_png` (ограничивает память в `--ipc-server`, исключает кросс-страничное переиспользование). Итог: каждая не-lazy `<img>` качается+декодируется один раз за навигацию. Lazy-картинки (по скроллу) и `background-image` — отдельные пути, в дубль не входили, не тронуты. Проверка: 6 unit-тестов модуля + 1341 тест шелла зелёные, clippy `-p lumen-shell` чисто. |
| 2026-06-19 | BUG-171 этап 1: префетч-кэш подресурсов вне UI-потока | Тяжёлый `parse_and_layout` (fetch скриптов/CSS/картинок → JS → layout) исполнялся синхронно на UI-потоке в `LoadDone`, и сетевое ожидание подресурсов морозило окно на всю загрузку. Этап 1 снимает **сеть** с UI-потока для внешних скриптов и связанных стилей. Новый `crates/shell/src/prefetch.rs` — process-global `PREFETCH_CACHE`: streaming-поток (`feed_preload_and_emit`) прогревает `<script src>` и `<link rel=stylesheet>` по приходу HTML через preload-сканер, используя **ту же** проводку `http_client_for_subresource(...).fetch_subresource(url, dest)`, что и потребитель в `parse_and_layout`. Cache-hit отдаёт байт-в-байт те же данные → порядок исполнения скриптов (HTML LS §8.1.3.1) и каскад CSS не нарушены; cache-miss дофетчивает на месте (как раньше). Кэш generation-scoped (`reset(generation)` на старте навигации в `start_streaming_load`), с in-flight дедупом (первый вызыватель фетчит, конкурентные блокируются и делят один `Arc<Vec<u8>>`) и stale-generation bypass (поток устаревшей навигации не загрязняет текущую). CSS warm-up попутно парсит таблицу для прогрессивных кадров — заменяет прежний `load_css_for_streaming` (bare-client) и убирает лишний двойной fetch CSS. adblock-`reload()` уже асинхронный (U-1 этап 1). **Остаётся этап 2** (BUG-171 OPEN): JS (~1.9с) + layout всё ещё синхронны в `parse_and_layout` на UI → окно морозится на CPU-фазе; это ADR-006/BrowserSession (весь пайплайн, включая QuickJS, на per-tab поток), разблокирован B-1, но крупная отдельная задача. Картинки/шрифты в кэш не заведены (этап 2 / BUG-172). Проверка: clippy `-p lumen-shell` чисто, +5 prefetch-тестов, 1335 тестов lumen-shell зелёные. |
| 2026-06-19 | B-1: QuickJS-рантайм на выделенном потоке (ADR-014) | QuickJS (`rquickjs::Runtime`/`Context`) — C-FFI, `!Send`; жил на UI-потоке за двумя НЕсостоятельными `unsafe impl Send/Sync`. Теперь `QuickJsRuntime` — **хэндл**: выделенный поток `lumen-js` владеет `Inner { Runtime, Context }` всю жизнь (создаёт и дропает там); хэндл держит ограниченный `SyncSender<JsCommand>` (а не `Sender` — чтобы остаться `Sync`, чего требует `JsRuntime`) + прежние Arc-каналы вывода. Весь доступ — через приватный `run(f)`: упаковывает `f` в `JsCommand::Run(Box<dyn FnOnce(&Inner)+Send>)` и блокирующе ждёт ответ, поэтому `f` может заимствовать стек вызывающего (один задокументированный `unsafe` со стиранием lifetime, корректный благодаря блокировке). Два несостоятельных `unsafe impl` удалены — хэндл по-настоящему `Send + Sync`. `Drop` шлёт `Shutdown` + джойнит. Поведение не меняется (вызывающий блокируется как раньше) — B-1 лишь ПЕРЕСЕЛЯЕТ рантайм, неблокирующий пайплайн — BUG-171 (разблокирован). 22 точки рантайма (eval/install_dom/pumps/fire_*/gc/JsRuntime-методы) сконвертированы. `pointer_lock` + реестр токенов `file_input` сняты с `thread_local` → процесс-глобальный `Mutex` (синглтон/глобально-уникальные токены; шелл пишет с UI, JS читает с JS-потока), тесты pointer_lock сериализованы guard'ом; per-runtime JS-thread-only реестры (canvas2d/webgl/offscreen/wasm/subtle_crypto/capture) оставлены. Попутно закрыт **BUG-222**: `js_thread_main` зовёт `wasm::clear_registry()` в teardown JS-потока до дропа `Runtime`. Тест `canvas_css_resize_scales_pixels` поправлен на `rt.flush_canvas_updates()` (JS-поток). Проверка: clippy чисто, 2184 теста lumen-js + 1333 теста шелла зелёные, deadlock'ов нет. |
| 2026-06-18 | U-4 опция: WASM i64 ↔ JS BigInt маршалинг | Числовая граница JS↔WASM перестала быть `f64`-only: `i64` теперь пересекает её как JS `BigInt` (полная 64-битная точность, по W3C WebAssembly JS Interface), остальные типы — как `Number`. Покрыты экспортируемые функции (аргументы/результаты), функции-импорты (аргументы/результаты) и экспортируемые глобалы. Общие хелперы маршалинга `wasm_value_to_js`/`js_value_to_wasm` (`wasm/mod.rs`); биндинги `__lumen_wasm_call` и global get/set теперь несут `rquickjs::Value` вместо `f64`, коэрся каждый аргумент к объявленному типу через новый `func_signature`; `call_f64`/`global_get`/`global_set` заменены типизованными `call_typed`/`global_value`/`global_set_value`. JS-шим (`makeExportFn`, global-сеттер) пропускает значения нетронутыми — прежний `+arg` нумифицировал BigInt (throw/потеря точности). Попутно: `wasm::clear_registry()` освобождает `Persistent` импортов перед дропом `Runtime` (иначе QuickJS падает на `list_empty(&rt->gc_obj_list)`); вызывается из тест-харнеса, проводка в teardown шелла — **BUG-222** (handoff P3). Проверка: 3 новых JS-интеграционных теста (i64 export/global/import round-trip с 2^53+1 — exact BigInt), 2184 теста lumen-js зелёные, clippy чисто. Ревизия показала: 4 унаследованные от P2 задачи (tier-tooltip, restore-spinner, femtovg blur, эллиптический border-radius) уже в коде — STATUS поправлен. |
| 2026-06-18 | U-4: WASM MVP-исполнение + graceful WebCodecs | WebAssembly больше не «проверка magic-байтов» — добавлен **pure-Rust интерпретатор WASM 1.0 core** (`crates/js/src/wasm/`, без внешних рантаймов; принцип «lightweight custom engine»). `parser.rs` декодирует бинарь (все секции, LEB128, тела функций в плоский `Instr`-поток с пред-разрешёнными целями `block`/`loop`/`if`→`End`/`Else`). `interp.rs` — стек-интерпретатор: полный набор числовых/сравнения/конверсии/sign-ext опкодов, структурное управление (`block`/`loop`/`if`/`br`/`br_if`/`br_table`/`return`), `call`/`call_indirect` с проверкой сигнатур, linear memory load/store + `memory.size`/`grow`/`copy`/`fill`, глобалы, таблицы, ловушки (деление на 0, OOB, bad-cast), saturating-trunc (`0xFC`), `ref.null`. Мост (`mod.rs`): thread-local реестр модулей/инстансов; `webassembly.rs` регистрирует `__lumen_wasm_*` нативы, и JS-шим (`WebAssembly.Module`/`Instance`/`compile`/`validate`/`instantiate`) исполняет реальный байткод — `Instance.exports` теперь вызываемые функции, экспортируемая память/глобалы живут через copy-хелперы. Импорты-функции = JS-callable, хранятся как `Persistent<Function>`, зовутся из интерпретатора через трейт `HostImports`; аргументы/результаты маршалятся как `f64`. Re-entrant-вызов в занятый инстанс → ловушка (а не паника). Границы MVP (задокументированы): `f64`-граница (i64 > 2^53 теряет точность), `Memory.buffer` — снимок (нет live-aliasing emscripten `HEAP32`), импорты не трогают память инстанса в процессе вызова, SIMD (`0xFD`)/threads/`memory.init` отклоняются на декоде (graceful `CompileError`). Байты JS→Rust идут как `TypedArray<u8>` (движковый `Vec<u8>` `FromJs` требует настоящий `Array`). **Этап 2:** WebCodecs `VideoEncoder`/`VideoDecoder`/`AudioEncoder`/`AudioDecoder` `configure()` больше не бросают синхронно (это белило SPA, не оборачивающие вызов в try/catch) — переходят в `configured` и сообщают о неподдержке через спецификационный async error-callback; добавлен отсутствовавший класс `InvalidStateError`; `isConfigSupported()` по-прежнему резолвит `false`. Проверка: 20 wasm-тестов (14 движковых: add/факториал-цикл/if-else/память/импорт/деление-trap/f64/вложенный-branch + parse/validate; + bridge/JS-интеграция) + 8 web_codecs; clippy чисто, 2181 тест lumen-js зелёные. |
| 2026-06-18 | U-2 этап 2: CFF-контуры (PostScript-OpenType `.otf`) | `.otf`-шрифты с PostScript-контурами (`'OTTO'` sfnt, таблица `CFF ` вместо `glyf`/`loca`) не рисовали глифы ни на одном пути рендера. Новый модуль `lumen-font::cff`: `Cff::parse` разбирает структуру CFF (header → Name/TopDICT/String/GlobalSubr INDEX → CharStrings INDEX, Private DICT + local Subr INDEX), `Cff::glyph(gid)` интерпретирует Type 2 charstring в `Outline::Simple`. Кубические безье флэттятся в on-curve отрезки (`CUBIC_STEPS=10`), поэтому переиспользуется существующий квадратичный растеризатор без изменений; bbox вычисляется из флэттенных точек. Интерпретатор: все path-операторы (rmoveto/h/v, rlineto, h/vlineto, rrcurveto, rcurveline/rlinecurve, vv/hhcurveto, vh/hvcurveto), hint-операторы (hstem/vstem/-hm считаются, чтобы пропустить байты маски `hintmask`/`cntrmask`), `callsubr`/`callgsubr`/`return` со стандартным subr-bias (107/1131/32768, глубина ≤10), четыре flex-оператора, опциональный leading-width операнд, `endchar` с `seac`-композитами. **CID-keyed CFF:** `ROS`/`FDArray`/`FDSelect` (форматы 0+3) → per-FD local subrs. Проводка: `Font::cff()`/`has_cff()` + ранняя ветка в `Font::glyph_resolved`/`glyph_resolved_with_coords` — CPU-растеризатор, wgpu-renderer (`renderer.rs`) и Canvas 2D рисуют `.otf`-текст прозрачно, без изменений в потребителях. CFF v1 не имеет variation-дельт, поэтому `glyph_resolved_with_coords` для CFF игнорирует `coords` (CFF2 отложен). Проверка: 16 unit-тестов на рукотворной spec-valid CFF-таблице (INDEX/DICT/real/bias/квадрат/кривая/subr/hintmask/width/hv-lineto/FDSelect); dev-валидация против реальных системных OTF (AdobeClean — non-CID, MyriadCAD — CID-keyed) рендерит читаемые `R`/`g`/`A`. Реальный CFF-шрифт в репо не закоммичен (нет permissive `.otf` под рукой) — committed-тесты синтетические, но прогоняют полный парс+интерпрет на корректных байтах. clippy чисто, 354 font-теста. Вне этапа: CFF2 (variable PostScript), charstring arithmetic-операторы. |
| 2026-06-18 | U-2 этап 1: шейпинг текста (GSUB-лигатуры + GPOS-кернинг) | Текст на CPU-пути рисовался по одному глифу на символ, без OpenType Layout — ни кернинга, ни лигатур. Реализован движок шейпинга в `lumen-font` и подключён к CPU-растеризатору (путь `--screenshot` + snapshot-гейт), где `lumen-font` — авторитет рендера текста. Новые модули: `otlayout` (общий слой GSUB/GPOS — заголовок + навигация script→langsys→feature→lookup через `enabled_lookups`, политика DFLT→latn→cyrl; `Coverage` fmt 1/2, `ClassDef` fmt 1/2, `ValueRecord`, `resolve_extension`); `gsub` (Type 1 single fmt 1/2, Type 4 ligature fmt 1 — жадно, кластер лигатуры = min компонент, Type 7 extension; фичи `liga`/`clig`/`calt`/`rlig`/`ccmp` — `calt` включён, т.к. Inter не имеет `liga`, а стрелки/лигатуры лежат в `calt` type-4); `gpos` (Type 1 single fmt 1/2, Type 2 pair fmt 1 пары-глифов + fmt 2 пары-классов, Type 9 extension; фича `kern`); `shape` (`Shaper::shape(glyph_ids, hmtx) -> Vec<ShapedGlyph>` — GSUB → сид base advance из hmtx → GPOS; без таблиц деградирует к base advance, вывод идентичен прежнему). Проводка: `lumen-paint::cpu_raster::rasterize_text` шейпит по сегментам между табами, рисует глифы со сдвигами и шейпленными advance. **Намеренно не трогаются:** per-char measurement (layout) и live-окно femtovg (femtovg шейпит сам через `fill_text`) — это перекрывает headline-цель «текст в окне», но требует переписать femtovg-текст glyph-by-glyph (отдельная опция). Вне этапа 1: контекстные lookups (GSUB 5/6, GPOS 7/8), mark-позиционирование (GPOS 3–6), сложные скрипты, mark filtering, CFF-контуры (этап 2). Проверка: 14 unit + 7 интеграционных (`inter_shaping.rs`: GPOS кернит AV/To/Wa/Type, GSUB лигатит `->`→стрелка); сквозной `--screenshot` подтверждает `x->y`→`x→y` + кернинг; clippy чисто, 339 font + 792 paint тестов. Гейт `snapshot_cpu` (feature-gated) уже красный на main из-за дрейфа геометрии (BUG-221) — baseline не перегенерирован. |
| 2026-06-18 | TAB-7: run.py IPC-режим (`--ipc`) | `graphic_tests/run.py` получил флаг `--ipc` — захват Lumen через `lumen.exe --ipc-server` вместо окна+gdigrab. Чистая Python-реализация bincode-протокола `lumen_ipc` (без зависимостей): `LumenIpcClient` спавнит сервер, парсит `LUMEN_IPC_PORT=<port>` из stdout, дренирует остаток stdout фоновым потоком (иначе рендер-логи переполнят пайп → блок шелла, как в `crates/shell/tests/ipc_server.rs`), подключается по TCP loopback и шлёт length-prefixed bincode: variant-tag u32 LE, длины String/Vec u64 LE, u32-поля 4 байта LE. Команды `CreateTab`/`NavigateTab(abs_path)`/`Screenshot`→PNG/`Shutdown` (завершение через `atexit`). Одна вкладка переиспользуется на все тесты. CPU-снимок детерминирован и начинается с (0,0) — магента-калибровка и crop offset не нужны (`crop_offset=(0,0)`), Edge-эталон + ffmpeg-crop + diff-метрика прежние. Верифицировано: TEST-00/01/03/06/08/09/13/22 PASS 0.00–0.22% (пиксельный паритет геометрии/цвета/текста/transform, лучше gdigrab-шума). **Ограничение:** CPU-бэкенд снимка (`render_to_image_cpu`) пока не на паритете с femtovg по border-radius (TEST-36 квадрат), gradients (39), images (18) — заведён **BUG-221**; поэтому `--ipc` опционален, gdigrab остаётся дефолтом до закрытия BUG-221. Затрагивает только `graphic_tests/run.py` (test-infra, Rust не менялся). |
| 2026-06-18 | TAB-4 + TAB-5: IPC tab control + Screenshot | `lumen-ipc` расширен таб-командами (`TabId` + `IpcRequest::{CreateTab,CloseTab,NavigateTab,Screenshot}` / `IpcResponse::{TabCreated,TabClosed,Navigated,Screenshot,TabError}`). Новый headless-режим шелла `--ipc-server` (`CliMode::IpcServer`, `run_ipc_server`): шелл становится TCP-сервером таб-команд, печатает `LUMEN_IPC_PORT=<port>` в stdout, держит `HashMap<TabId, PageSource>`. «Вкладка» — headless-контекст рендера; `Screenshot` лениво гоняет тот же CPU-пайплайн, что `--screenshot` (рефактор `do_screenshot` → переиспользуемый `render_source_to_png`), без winit/wgpu — детерминированный PNG для CI/run.py. Состояние вкладок переживает переподключения; сервер выходит по `Shutdown`. Тесты: round-trip в `lumen-ipc` (5/5) + интеграционный `crates/shell/tests/ipc_server.rs` (спавнит реальный бинарь, гоняет create→navigate(file)→screenshot→close→shutdown, проверяет PNG-magic). Это TAB-4/TAB-5 минимального пути; TAB-7 (run.py-клиент с bincode на Python) — следующая. Ревизия показала: TAB-1/2/3/6 уже были в коде (`PageSnapshot`/`bg_tabs`/`switch_tab`/tab-bar/lifecycle), STATUS подправлен. |
| 2026-06-18 | U-1 (этап 1): неблокирующая навигация | `reload()` больше не гоняет весь fetch+parse+JS+layout синхронно на UI-потоке. Когда окно есть (любая навигация после первого кадра — клик по ссылке, адресная строка, back/forward, JS `location.href=`, reload палитры/tab-bar), сбрасываем streaming-состояние и делегируем в `start_streaming_load()` — тот же async-путь, что и первичная загрузка в `resumed()`: HTML стримится в фоновом потоке, окно рисует промежуточные кадры, тяжёлый финальный `render_bytes` исполняется один раз на UI-потоке в `LoadEvent::LoadDone` (`apply_loaded_page`). Синхронный fallback (прежний GpuSession/`source.load`) сохранён для пути без окна (headless/тесты). **Load-generation guard:** `Lumen.load_generation` инкрементится на каждую навигацию и метит все streaming-события (`EarlyPreloadHints`/`HtmlChunk`/`CssLoaded`/`LoadDone`/`LoadError`); `user_event` отбрасывает события устаревшего поколения — медленная вытесненная загрузка не подмешает DOM/CSS прошлой страницы и не нарисует её поверх новой (гонка, ставшая достижимой из-за того, что UI больше не блокируется). **Scroll-restore:** back/forward (и bfcache) стэшат offset в `Lumen.pending_restore_scroll` перед `reload()`, а `apply_loaded_page` применяет его после сброса scroll в 0 — прежний код ставил `scroll_x/y` сразу после (тогда синхронного) `reload()`, что при асинхронном `LoadDone` затёрлось бы. Остался этап 2 (ADR-006: финальный пайплайн, включая QuickJS, вне UI-потока — блокер: QuickJS не `Send`). Проверка: `cargo check`/clippy clean, 1333 теста шелла, graphic TEST-00/01/03/06/20 PASS, headless `--screenshot example.com` рендерит. |
| 2026-06-18 | U-0: `--screenshot <out.png> <url>` headless CPU-снимок | Новый CLI-режим шелла: `lumen --screenshot out.png <path-or-url>` гоняет полный headless-пайплайн (`source.load_bytes` → `parse_and_layout` с внешним CSS/картинками → `paint_ordered`) и растеризует display-list детерминированным CPU-бэкендом (`Renderer::render_to_image_cpu`, feature `cpu-render`, tiny-skia) → PNG. Без wgpu/winit/окна, без Edge и ffmpeg, пиксельно воспроизводимо на любой ОС. Высота снимка = высота layout-корня (`parsed.layout.rect.height`), зажата в `[720, 32768]` (полная страница, не только первый экран), ширина 1024. `extract_screenshot()` зеркалит `extract_print_to_pdf` (порядок `--screenshot <out> <url>`); новый `CliMode::Screenshot` + `run_screenshot`/`do_screenshot`. Шелл теперь включает `lumen-paint/cpu-render` в дефолте. Попутно: `#[allow(clippy::too_many_arguments)]` на `draw_border_side_h/v` в `cpu_raster.rs` (8 геом-параметров, открылись для строгого clippy при включении cpu-render). 4 unit-теста `extract_screenshot`. Инструмент наблюдаемости для USABILITY-вертикали (см. Next). |
| 2026-06-16 | PH3-20: Service Worker Fetch Interception Phase 1 | Активный SW исполняется в выделенном QuickJS-потоке (`lumen-js::sw_worker::spawn_sw_worker`): `ServiceWorkerGlobalScope` + `caches`/`Headers`/`Response` шим, реальные base64 `atob`/`btoa`, `install`/`activate`/`fetch` события. На фазе активации `_sw_run_lifecycle` фетчит скрипт SW и зовёт `_lumen_sw_activate_script(origin, scope, text)` → спавн потока, регистрация в `SwWorkerStore` (`(origin,scope)→SwWorkerHandle`). `ServiceWorkerInterceptor` (lumen-storage) маршрутизирует fetch через `sw_worker_store` со scope-prefix matching (longest-match), независимо от SQLite-регистраций (shell хранит их in-memory); диспатчит `FetchEvent`, ждёт `respondWith()` (5 c таймаут), отдаёт тело сети. Shell: общий in-memory `cache_store: Arc<CacheStorage>` передаётся в `install_dom` (страница + SW-поток делят кэш) и в глобальный `SW_FETCH_INTERCEPTOR`, подключаемый в `http_client_for_subresource`. Новые типы в `lumen-core::ext`: `SwFetchRequest`/`SwWorkerHandle`/`SwWorkerStore`. Ограничение Phase 1: внутри SW `fetch` — только cache-first (без сети), поэтому `cache.addAll()` прекэш из сети не работает; SW отдаёт лишь то, что закэшировала страница. 9 тестов sw_interceptor (5 SQLite + 4 worker-routing), 5 тестов sw_worker. Гочи: `rt.execute_pending_job()` нельзя вызывать внутри `ctx.with(...)` (реентрантность рантайма → паника rquickjs) — `flush_jobs` всегда вне `ctx.with`. |
| 2026-06-16 | PH3-19: font-display: swap — неблокирующая загрузка web-шрифтов | `load_font_faces` разделена на local()-sync + url()-async. `ParsedPage`/`LoadedPage` несут `pending_web_fonts: Vec<PendingWebFont>`; `apply_loaded_page` спавнит по фоновому потоку на каждый: fetch → WOFF-декод → sfnt-валидация → `LoadEvent::FontLoaded`. Обработчик на UI-потоке: `page_font_registry.register_from_bytes`, push в `web_fonts`, `relayout_page(..., &self.web_fonts)` строит `MultiFontMeasurer` из накопленных шрифтов (FOUT swap), `request_redraw`. BUG-170 закрыт. |
| 2026-06-16 | PH1-2c: прогрессивная подгрузка картинок во время streaming | `paint_partial_dom` после layout зовёт `spawn_stream_image_loads(doc, viewport)` — `collect_image_requests` по частичному DOM, для каждого не-lazy и ещё-не-запрошенного `src` спавнится поток `fetch_image_bytes`+`decode`/`decode_gif_animated`. По завершении поток шлёт `LoadEvent::ImageDecoded { src, image, animated }`; `user_event` регистрирует картинку в renderer-е (или `pending_images` до создания окна), кладёт в `image_cache`, анимированный GIF — в `animated_gifs` (тик в `RedrawRequested`), и просит redraw — картинки появляются по мере прихода, как CSS, а не разом в финальном `LoadDone`. Дедуп через новое поле `stream_images_requested: HashSet<String>` (сбрасывается на каждую навигацию; сохраняется/восстанавливается в `PageSnapshot`). `PageSource::resource_base()` — общий хелпер базы подресурсов. 3 новых shell-теста (итого 1326). |
| 2026-06-16 | PH1-2b: инкрементальный streaming-layout | `layout_streaming_incremental(doc, sheet, vp, m, hp, dark, prev)` строит свежее box-дерево из выросшего DOM и переиспользует геометрию из `prev` для поддеревьев с неизменными node-id/BoxKind-payload/ComputedStyle; релейаут только новых/изменённых поддеревьев через `lay_out_incremental`, неизменённый префикс репозиционируется за O(1). Layout: `incremental::mark_subtree_dirty` + `graft_geometry` (рекурсивное сопоставление по индексу, `kind_layout_eq`+`segments_eq` детектят дописываемый текст в InlineRun). Shell: `paint_partial_dom` гейтит graft через `stream_layout_seeded` (первый кадр навигации — полный layout-засев, чтобы не переиспользовать геометрию прошлой страницы). Тесты: геометрия инкремент-прохода совпадает с полным layout (append-блок + reflow абзаца) + unit graft. |
| 2026-06-16 | PH1-2a: TCP-level streaming HTTP body | `HttpClient::fetch_page_streaming(url, on_chunk)` отдаёт декодированные порции тела по мере чтения сокета — браузер начинает парсить/рисовать HTML до полного скачивания. Network: `read_response_streamed` + `BodyReader` (Content-Length / chunked / read-to-EOF Read-адаптер) + `TeeReader` + `detect_stream_decode`; streaming-decode для identity/br/gzip/deflate (br/gzip/deflate через streaming `Read`-декодеры, br — `brotli_decompressor::Decompressor`, остальные `flate2`), gated на финальный 2xx; sink проброшен через `do_request`/`fetch_single`/`fetch_with_redirect` (`ChunkSink` алиас). Возвращаемое тело — полное декодированное (как `fetch_page`), sink — best-effort preview. Shell: `PageSource::load_bytes_streaming` + `start_streaming_load` для URL-источников шлёт реальные сетевые чанки (File/Snapshot/Static — прежняя нарезка буфера); `feed_preload_and_emit` объединяет preload-scan обоих путей. 9 новых network-тестов. Попутно (BUG-168) — Linux-unblock pre-existing clippy/test-сбоев в platform-cfg коде (ctap2 ×3, screen_capture/file_dialog ×2). |
| 2026-06-16 | PH3-18: Pointer Lock Phase 1 | `pending_grab` флаг в `pointer_lock.rs` + `take_pending_grab()` для shell; `_ptr_lock_el` JS-переменная для `pointerLockElement` getter; `_lumen_dispatch_locked_mousemove()` — mousemove+pointermove с movementX/Y; `device_event()` в shell → `DeviceEvent::MouseMotion` → `_lumen_dispatch_locked_mousemove`; `about_to_wait` drain `CursorGrabMode::Locked`/`None`; Escape освобождает lock; `CursorMoved` при locked подавляется. 10 новых тестов. |
| 2026-06-16 | PH3-17: Screen Capture API Phase 1 | `ScreenCaptureProvider` трейт + `NullScreenCaptureProvider` в lumen-core::ext; `VideoFrame` struct; `__lumen_screen_capture_{list_sources,start,info,read_frame,stop}` нативные биндинги + `set_screen_capture_provider()` в lumen-js; `getDisplayMedia()` резолвится с живым `MediaStream` + video track + `readVideoFrame()`; `PlatformScreenCapture` (Win32 GDI `BitBlt`/`GetDIBits` + BGRA→RGBA) в shell/src/platform/screen_capture.rs. 14 новых тестов (3 lumen-core + 11 lumen-js). |
| 2026-06-16 | PH3-16: Idle Detection API Phase 1 | `__lumen_idle_get_idle_ms()` → Win32 `GetLastInputInfo+GetTickCount` на Windows, 0 на Linux/macOS; `IdleDetector.start()` запускает `setInterval(max(30s, threshold/2))`, диспатчит `'change'` при переходе `userState` active↔idle; `#[link(name = "user32")]`. 16 новых тестов. |
| 2026-06-16 | PH3-15: File System Access API Phase 1 | `showOpenFilePicker/showSaveFilePicker/showDirectoryPicker` → Promise; `FileSystemFileHandle`/`FileSystemDirectoryHandle`/`FileSystemWritableFileStream` JS-классы; `WriteRegistry` (append + flush-on-close); `DirRegistry`; OS диалоги WinForms/zenity/osascript; токен-безопасность через PH3-14 `register_file_token`. 33 новых теста lumen-js. |
| 2026-06-16 | PH3-14: File Input API Phase 1 | `register_file_token()` + thread-local `FILE_REGISTRY`; нативные биндинги `__lumen_file_read_text`/`__lumen_file_read_base64`; `File.prototype.text()`/`arrayBuffer()`/`stream()` читают реальные байты через токены; `entries_to_json_with_tokens()` в shell; JS не видит сырых путей файловой системы. 18 новых тестов lumen-js + 4 lumen-shell. |
| 2026-06-16 | PH3-13: Screen Wake Lock API Phase 1 | `WakeLockProvider` трейт + `NullWakeLockProvider` в lumen-core::ext; `set_wake_lock_provider()` + `__lumen_wake_lock_request`/`__lumen_wake_lock_release` биндинги + обновлённый JS-шим в lumen-js; `PlatformWakeLock` (`SetThreadExecutionState` на Windows, no-op на Linux/macOS) в shell/src/platform/wake_lock.rs. 23 новых теста. |
| 2026-06-16 | PH3-12: `<video>` element Phase 1 | `VideoGifStore` + `VideoPlaybackState` в lumen-js (без зависимости от lumen_image); 12 нативных биндингов `__lumen_video_*` + JS-шим; `BoxKind::Video` → `DrawImage { src: "video:{nid}" }` в display_list; `tick_video_gifs()` в shell — декодирует GIF, регистрирует кадры, продвигает анимацию. |
| 2026-06-16 | PH3-11: `<audio>` element Phase 1 | `AudioPlaybackProvider` трейт в lumen-core; 16 нативных биндингов `__lumen_audio_*` + JS-шим (play/pause/seek/timeupdate/ended/loop/canPlayType) в lumen-js; `PlatformAudioPlayer` на rodio 0.19 с per-handle audio thread + mpsc в lumen-shell. 39 новых тестов. |
| 2026-06-16 | PH3-10: Pointer Events API Level 3 | `pointer_captures` HashMap в lumen-dom; `pointer_capture.rs` Rust-биндинги + `pointer_capture_nid` Arc в lumen-js; `Element.setPointerCapture/releasePointerCapture/hasPointerCapture` + `gotpointercapture`/`lostpointercapture`; L3 свойства (altitudeAngle, getCoalescedEvents); shell routing + implicit release на pointerup. 10 новых тестов, итого 2091 lumen-js. |
| 2026-06-16 | PH3-9: HTML5 Drag and Drop API | `is_element_draggable()` в lumen-dom (HTML LS §9.3.3); `DndState` + `DND_THRESHOLD` + `js_drag_event()` в shell; полный lifecycle: dragstart→drag/dragover/dragenter/dragleave→drop/dragend. 231 тест lumen-dom, 2081 lumen-js. |
| 2026-06-16 | PH3-8: Web Animations API Level 1 (JS runtime) | `DocumentTimeline`, `KeyframeEffect`, `Animation` (play/pause/cancel/finish/reverse), `AnimationPlaybackEvent`; `element.animate()` + `element.getAnimations()`; `document.timeline` + `document.getAnimations()`; интерполяция (числа/цвета/transform), easing (linear/ease/cubic-bezier/steps), fill/direction/iterations. 21 тест. |
| 2026-06-16 | PH3-7: `contentEditable` + Input Events Level 2 + Selection routing | `node_is_contenteditable()`, `find_editing_host()` в lumen-dom; 5 Rust-биндингов + JS-свойства (`contentEditable`, `isContentEditable`) + `_lumen_handle_contenteditable_key()` в lumen-js; маршрутизация клавиш в shell через DOM (не eval_js). 17 новых тестов. |
| 2026-06-16 | PH3-6: `<dialog>` focus management + `<form method="dialog">` | `showModal()` фокусирует [autofocus]-потомок или сам диалог; `close()` восстанавливает предыдущий фокус; `<form method="dialog">` закрывает родительский `<dialog>`. `find_ancestor_dialog()` в lumen-dom. 8 новых тестов. |
| 2026-06-15 | PH3-5: Web Workers Phase 1 | `importScripts()` для data: и blob:lumen/ URL; `WorkerBlobStore` (Arc-shared); `atob`/`btoa` в worker globals; WORKER_SHIM оборачивает createObjectURL для auto-регистрации blob'ов. 20 новых тестов, итого 47 worker-тестов. |
| 2026-06-15 | PH3-4: Offscreen Canvas Phase 1 | `create_offscreen_from_pixels()` + `transferControlToOffscreen()` + `postMessage(data,[transfer])` с сериализацией OffscreenCanvas через сентинели. 8 новых тестов. |
| 2026-06-15 | PH3-3: getUserMedia Phase 1 | `AudioCaptureProvider` + `PlatformAudioCapture` (cpal/WASAPI/ALSA); `__lumen_start_audio_capture` + JS MediaStreamTrack. 247 тестов. |
| 2026-06-15 | PH3-2: `lumen-bidi-server` standalone крейт | WebDriver BiDi сервер вынесен из `shell/src/bidi/` в отдельный крейт. `lumen_bidi_server::spawn` — единственный публичный API. 89 тестов. |
| 2026-06-15 | PH3-1: DevTools Styles-таб | `ComplexSelector::to_css_str()`, `matched_rules_for_node()`, `InspectorTab::Styles` — CSS правила для выбранного узла в DevTools. 16 новых тестов. |
| 2026-06-15 | 9F.3: Tor circuit (`--tor` CLI) | `extract_tor_mode()` + `check_tor_connectivity()` + override `FingerprintProfile` → TorBrowser + `socks5://127.0.0.1:9050` + `no_persistent_state`. Завершает ADR-007 (все 6 слоёв). 6 тестов. |
| 2026-06-15 | PH2-7: Accessibility tree + platform bridges Phase 1 | `WinUiaBridge` Phase 1: `init_hwnd()` + `NotifyWinEvent` (EVENT_OBJECT_FOCUS/REORDER/STATECHANGE) + `handle_wm_get_object` + `ax_role_to_msaa()` (60 вариантов). 125 тестов в lumen-a11y. |
| 2026-06-15 | PH2-3: Профили + шифрование | `profile_vault` — AES-256-GCM key wrapping, PBKDF2-HMAC-SHA256 (100k iter). `ProfileRegistry`: `set_password`, `clear_password`, `unlock`, `is_encrypted`. 11 unit-тестов. |
| 2026-06-15 | PH2-2: Site isolation Phase 1 | `lumen-network::coop` — COOP/COEP/CORP парсинг; 27 тестов. `window.crossOriginIsolated` + pipeline wiring. |
| 2026-06-15 | PH1-8: Preload scanner | `PreloadScanner` struct поверх `PushTokenizer`; инкрементальный scan. 35 тестов. |
| 2026-06-15 | PH1-7: Compositor thread + Property Trees | `InProcessCompositor` + `ThreadedCompositor` + `PropertyTrees::build()` + `scroll_page_by`. 15 тестов. |
| 2026-06-15 | PH1-6: Stacking contexts + CSS Painting Order | `build_display_list_ordered` подключён к driver; 3 теста на CSS 2.1 Appendix E. |
| 2026-06-15 | PH1-5: CI/CD для Linux/macOS/Windows | `.github/workflows/ci.yml` + `release.yml`; 4 бинарных пакета. |
| 2026-06-15 | PH1-4: Network service в отдельном процессе | `lumen-ipc` крейт; `RemoteNetworkTransport`; `--network-service` флаг. |
| 2026-06-15 | PH1-15: T1 (paused) | `pause_event_loop()`/`unpause_event_loop()` в `PersistentJs`; 6 тестов. |
| 2026-06-15 | PH1-2: Progressive / streaming rendering pipeline | 60 Hz throttle; `LoadEvent::CssLoaded`; параллельная загрузка CSS; 3 теста. |
| 2026-06-15 | PH1-9: lumen-mcp-server крейт | 5 ресурсов + 7 инструментов; StdioTransport + TcpTransport; shell `--mcp` / `--mcp-port N`. 15 тестов. |
| 2026-06-14 | PH1-10..14: Auto-wait / Per-context isolation / A11y first-class / TabState / Image LRU | Все подтверждены в коде; STATUS обновлён. |
| 2026-06-14 | PH2-1/4/5/6/8/9/10/11/12/15/16: Phase 2 features | HTTP/2, anti-fingerprinting, meta viewport, Shadow DOM runtime, IME, mix-blend-mode stacking, BiDi, GPU LRU, Glyph LRU — все подтверждены. |
| 2026-06-14 | Y-series (Y-2..Y-5): Web Platform Phase 4 | unicode-range в lumen-font, scrollbar-width/color, color-scheme, scroll snap events — все реализованы. |
