# Политики как проверки: перевод правил CLAUDE.md в машинные линты

План перевода проектных правил из прозы в проверки, которые роняют сборку. Составлен 2026-08-17 по итогам разбора «нужен ли отдельный язык/форк компилятора для работы с ИИ» — вывод разбора в §8.

Статусы: ⬜ не внедрено · 🧪 внедрить и замерить · ✅ внедрено · 🚫 отвергнуто.

---

## 1. Задача

Значительная часть правил проекта записана в [CLAUDE.md](../CLAUDE.md) и [REVIEW.md](../REVIEW.md) текстом: «no `panic!`/`unwrap()` в проде», «каждый `unsafe` требует `// SAFETY:`», «`///` на всех публичных элементах — обязательно». Проверяет их только ревью — то есть человек или ассистент, по памяти, глазами. Это худший вид требования: невидимое в сигнатуре, не воспроизводимое и нарушаемое молча.

Цель — перевести каждое такое правило в одну из трёх форм, по убыванию предпочтения:

1. **готовое правило clippy**, включённое в конфиге (кода писать не надо);
2. **своё правило** через `dylint` — отдельный проход, не участвующий в сборке;
3. **скрипт проверки** там, где линт неуместен (например, содержимое `Cargo.toml`).

Правило, которое не удалось перевести ни в одну из форм, остаётся прозой — но это должно быть осознанным исключением, а не умолчанием.

---

## 2. Состояние

| Что | Было (замер 2026-08-17) | Стало (2026-08-18) |
|---|---|---|
| `[workspace.lints]` в корневом `Cargo.toml` | отсутствует | есть; `[lints] workspace = true` в 24 крейтах-членах (кроме `workspace-hack` — он генерируется `cargo-hakari` и кода не содержит) |
| `clippy.toml` | отсутствует | есть: `allow-{unwrap,expect,panic}-in-tests = true`, пустые `disallowed-methods`/`disallowed-types` |
| Включённых правил яруса 0 | 0 | 4: `undocumented_unsafe_blocks`, `panic`, `expect_used`, `unwrap_used` — все `deny` |
| Действующих `#[allow]` | — | реестр в §10 (пустой реестр = правило внедрено честно) |
| `#![deny(...)]` во всём workspace | один: `clippy::rc_buffer` в `crates/engine/dom/src/lib.rs:11` | там же (в секцию не переносился) |
| Гейт | `cargo clippy -p <crate> --all-targets -- -D warnings` | без изменений |

**Главное следствие:** гейт `-D warnings` поднимает до ошибки только те правила, что включены **по умолчанию**. Правила, закрывающие три политики выше, по умолчанию выключены (группы `restriction` и `pedantic`, а `missing_docs` — allow-by-default в rustc). То есть сборка зелёная, а политики не проверяются вообще.

Черновая перепись (`grep` по `crates`, исключены только каталоги `tests/`; внутрифайловые `mod tests` **включены**, поэтому числа завышены и годятся лишь для порядка величины):

| Узор | Вхождений |
|---|---|
| `.unwrap()` | ~9 600 в 400 файлах |
| `.expect(` | ~1 900 |
| `panic!(` | ~650 |
| `unsafe {` | 63 |
| `// SAFETY:` | 68 |

Из этого сразу видно две вещи. `unsafe` уже почти в порядке — комментариев больше, чем блоков, значит `undocumented_unsafe_blocks` включается дёшево и должен идти первым. А `unwrap` включить разом невозможно: даже с поправкой на тесты остаток будет большим, и это работа не на один заход.

Точные числа снимать не грепом, а самим clippy — см. протокол в §6. **Снято 2026-08-18, таблица
выше оставлена как иллюстрация промаха:** грепа `.unwrap()` дал ~9 600, clippy с исключением для
тестов — 865, то есть завышение в 11 раз. Настоящие числа — в таблице §3.

---

## 3. Ярус 0 — готовые правила clippy (кода писать не надо)

Механика: секция `[workspace.lints]` в корневом `Cargo.toml` (стабильно с Rust 1.74) плюс строка `lints.workspace = true` в каждом крейте-члене.

```toml
# Cargo.toml (корень)
[workspace.lints.clippy]
unwrap_used                = "warn"
expect_used                = "warn"
panic                      = "warn"
undocumented_unsafe_blocks = "warn"

[workspace.lints.rust]
missing_docs = "warn"
```

```toml
# crates/<любой>/Cargo.toml
[lints]
workspace = true
```

Соответствие политикам. Колонка «срабатываний» — перепись **самим clippy** (`--workspace
--all-targets`, `clippy.toml` уже на месте, дубли по `file:line:col` схлопнуты), 2026-08-18;
она заменяет грубые грепы §2, завышавшие `unwrap` в 11 раз.

| Политика (источник) | Правило | Срабатываний | Статус |
|---|---|---|---|
| «каждый `unsafe` требует `// SAFETY:`» (CLAUDE.md §Code conventions) | `clippy::undocumented_unsafe_blocks` | 17 → 0 | ✅ `deny` с 2026-08-18 |
| «no `panic!` в проде» | `clippy::panic` | 91 → 0 (25 под `#[allow]`) | ✅ `deny` с 2026-08-18 |
| «no `expect()` в проде» (следует из той же строки) | `clippy::expect_used` | 250 → 0 (128 под `#[allow]`) | ✅ `deny` с 2026-08-18 |
| «no `unwrap()` в проде» | `clippy::unwrap_used` | 865 → 0 (511 под `#[allow]`) | ✅ `deny` с 2026-08-18 |
| «`///` на всех публичных структурах, полях и функциях» | `missing_docs` (rustc) | 2006 | 🧪 — только по одному крейту за коммит |
| молчаливое проглатывание ошибок | `clippy::let_underscore_must_use`, `clippy::let_underscore_untyped` | не замерено | ⬜ |

Распределение по крейтам (вне каталогов `tests/`, топ-5) — оно и определяет, на кого ложится работа:

| Правило | Крейты |
|---|---|
| `panic` | js 31, layout 11, chrome/build.rs 7 |
| `expect_used` | paint 40, shell 35, js 13, storage 12, network 7 |
| `unwrap_used` | **js 621**, shell 106, network 43, layout 19, font 18 |
| `missing_docs` | layout 552, storage 315, core 201, paint 196, font 157 |

**Колонка «вне `tests/`» — не то же самое, что «продакшн».** Она отсекает только каталоги
`tests/`, но не `#[cfg(test)] mod` внутри `src/`. При заходе на `panic` из «49 вне `tests/`»
продакшн-кодом оказались 25: из 31 попадания в `lumen-js` 24 сидели во внутрифайловых тестовых
модулях. Для `expect_used`/`unwrap_used` числа выше завышены тем же образом — перед оценкой
работы разбивать по `#[cfg(test)] mod`, а не только по каталогу (скрипт разбора —
в журнале §9, узор `#\[cfg\([^)]*\btest\b` + `mod \w+ \{`).

Порядок внедрения — строго по возрастанию стоимости: `undocumented_unsafe_blocks` (сделано) →
`panic` (сделано) → `expect_used` (сделано) → `unwrap_used` (сделано) → `missing_docs` (по одному крейту). Порядок
относительно исходного плана изменён: перепись показала, что `missing_docs` (2006) дороже
`panic` (91) и `expect_used` (250), а не дешевле, как предполагалось на глаз.

Прогноз «`unwrap_used` отдельным заходом не берётся, 621 из 850 в `lumen-js`» **не подтвердился**:
после разбивки по `#[cfg(test)] mod` продакшн-мест оказалось 511, из них в `lumen-js` — 282, а не
621. Крейтовый `#![allow]` в `crates/js/src/lib.rs`, который предлагался здесь как «разумный
первый шаг», не понадобился и был бы вреден: он снял бы правило с самого крупного крейта целиком.

---

## 4. Ярус 1 — `clippy.toml`: настройка и свой чёрный список

Файл `clippy.toml` в корне репозитория. Две функции.

**Первая — исключения, без которых правила яруса 0 неприменимы.** Политика проекта разрешает `unwrap()` в тестах; clippy умеет это ровно так:

```toml
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests  = true
```

**Исключение уже, чем кажется — замерено 2026-08-18.** Clippy освобождает не «код в тестовом
таргете», а тело функции, помеченной `#[test]` (и содержимое `#[cfg(test)] mod`). Обычная
вспомогательная функция в интеграционном тесте под `tests/` — не тело `#[test]`, поэтому под
исключение не попадает: `crates/js/tests/cases/speech_api.rs` даёт 6 срабатываний `clippy::panic`
на трёх хелперах вида `fn bool_eval(..) -> bool { match .. { Err(e) => panic!(..) } }`. По
переписи ниже это 42 из 91 `panic`, 116 из 250 `expect`, 15 из 865 `unwrap`. Значит, включая
`panic`/`expect_used`, придётся либо ставить `#[allow]` на такие хелперы, либо (лучше)
переписать их на `#[track_caller]`-ассерты — решать при заходе на само правило.

**Вторая — собственные запреты без единой строки кода:**

```toml
disallowed-methods = [
  # { path = "std::env::set_var", reason = "гонки в многопоточном шелле" },
]
disallowed-types = [
  # { path = "std::sync::Mutex", reason = "в горячем пути paint использовать <...>" },
]
```

Это уже «свой линт»: перечисляете запрещённый вызов или тип и текст, который увидит нарушитель. Кандидатов надо набирать из реальных разборов багов, а не выдумывать заранее.

---

## 5. Ярус 2 — `dylint`: свои правила

`dylint` — механизм запуска собственных линтов: правило пишется как небольшая библиотека и вызывается отдельной командой (`cargo dylint --all`). **В сборку проекта не входит** — это второй проход, как clippy. Ломается или отстаёт от компилятора — сборка не страдает.

Сюда идёт то, чего в готовых наборах быть не может, потому что это про наш движок. Кандидаты (не проверены на реализуемость, список для обсуждения):

- вызов layout из фазы paint;
- изменение display list после сборки;
- аллокации в горячем пути растеризации;
- `#[touches(self.field, ...)]` — аннотация «функция трогает только эти поля», проверяемая по MIR. Языковой мощи не даёт (borrowck по-прежнему считает занятым весь `self`), но даёт заявленное намерение, проверенное машиной, и локальность в сигнатуре. Дешёвый заменитель отсутствующих в Rust view types.

Цена: около недели на обвязку (крейт линтов, пиннутый nightly **только для драйвера линтов**, вызов в CI) и 1–3 дня на правило. Заходить сюда стоит только после того, как ярус 0 внедрён и оказался недостаточным.

---

## 6. Протокол внедрения (одно правило — один заход)

Ровно как в [build-speed.md](build-speed.md) §2: не «на глаз», а числом.

```bash
export PATH="/c/Users/konstantin/.cargo/bin:$PATH"

# 1. Сколько срабатываний даст правило ДО включения (P5 — единственная роль,
#    которой можно --workspace; см. CLAUDE.md §Commands)
cargo clippy --workspace --all-targets --message-format=short -- -W clippy::<lint> \
  > .tmp/lint-<lint>.log 2>&1
grep -c "<текст сообщения правила>" .tmp/lint-<lint>.log
grep "<текст сообщения правила>" .tmp/lint-<lint>.log | sort -u   # адреса правок

# 2. Включить как "warn" в [workspace.lints], разгрести, снова посчитать (должно быть 0)
# 3. Перевести в "deny" тем же коммитом, что и последняя правка
```

**Считать по тексту сообщения, а не по имени правила.** `--message-format=short` печатает
только `path:line:col: warning: <текст>` — имени линта в строке нет, поэтому
`grep -c "clippy::<lint>"` (как было записано здесь до 2026-08-18) всегда даёт 0 и выглядит
как «правило уже внедрено». Для `undocumented_unsafe_blocks` искомый текст — `safety comment`.
Без `--message-format=short` имя правила печатается, но в отдельной строке `= note:`, и одно
срабатывание занимает 5–8 строк вывода.

Правила:
- одно правило за коммит; смешивать включение двух — терять возможность откатить одно;
- **верификационный прогон обязателен и является единственной проверкой корректности
  расстановки исключений.** Скрипт, определяющий «объемлющую функцию» или «тестовый ли это
  модуль», ошибается на реальном коде (три разных промаха за один заход `unwrap_used`, §9), и
  ошибку видно только как остаточные ошибки clippy. Правило считается внедрённым, когда
  `cargo clippy --workspace --all-targets` даёт ноль срабатываний, а не когда скрипт отчитался;
- `warn` → правки → `deny`; не включать `deny` до нуля срабатываний. Под нашим гейтом
  (`-D warnings`) разница уровней косметическая: `warn` из `[workspace.lints]` роняет сборку
  ровно так же, поэтому правило и его правки обязаны приехать одним коммитом;
- крейты, где остаток велик, гасить локальным `#![allow(...)]` **с комментарием и ссылкой на задачу**, а не откатывать правило целиком;
- правку писать по коду, а не по шаблону: комментарий `// SAFETY:`, повторяющий сигнатуру,
  бесполезен. Если инвариант на самом деле не выполняется — писать это прямо в комментарии
  (пример: `crates/network/src/ctap2.rs`, выравнивание `*const u16` внутри `Vec<u8>`);
- результат заносить в журнал §9.

---

## 7. Что линтом делать не надо

**Границы крейтов** («layout не тянет wgpu», «paint не тянет сеть») уже гарантирует Cargo: чего нет в `[dependencies]` крейта, того нельзя использовать физически. Проверять надо не код, а сами `Cargo.toml` — скриптом в CI, а не линтом. Ставить сюда `dylint` — это платить неделю за то, что уже работает.

**Правила про документацию и процесс** («обновить `CAPABILITIES.md` в том же коммите», «STATUS-PN.md — только строки-указатели») линтом не выражаются: они про содержание, а не про форму. Их место — [REVIEW.md](../REVIEW.md).

---

## 8. Почему не форк компилятора (разбор 2026-08-17)

Исходный вопрос был шире: не сделать ли форк rustc и не пилить ли в нём то, что нужно именно для работы с ИИ. Отвергнуто:

- **Цена.** rustc — порядка 1.5–2 млн строк; полная bootstrap-сборка идёт часами на каждой машине и в CI. Релиз апстрима каждые 6 недель, перебазирование — от часов (диагностика) до дней (typeck/borrowck). Фича уровня системы типов — работа на месяцы-годы: view types не приняты в апстриме годами именно поэтому.
- **Экосистема.** Наши зависимости (сотни крейтов, включая wgpu и rusty_v8) собираются стандартным компилятором. Новый синтаксис доступен только в нашем коде, rust-analyzer его не понимает, публикация отпадает.
- **Контрпродуктивность для самой цели.** Расширение, полезное ассистенту, должно быть в форме **атрибута или линта, а не грамматики**: атрибуты уже внутри корпуса, на котором обучена модель, и обобщаются с первого взгляда; новое ключевое слово — вне корпуса, и модель систематически про него забывает, скатываясь в обычный Rust. Форк «под ИИ», добавляющий синтаксис, делает ИИ хуже.

**Отложено, не отвергнуто** (ярус 3 в [build-speed.md](build-speed.md) §5, разобрано там же):

- `-Zthreads` (параллельный фронтенд) — плохо сочетается с sccache, а sccache у нас несущий;
- Cranelift для dev — на Windows только `panic = "abort"`, а `catch_unwind` используется в обвязке V8 (`crates/js/src/v8_compat.rs`) и обработчике крашей (`crates/core/src/crash.rs`).

Оба требуют перевода проекта на nightly ради выигрыша, который на нашей базе (инкремент S1 = 4.2 с) невелик. Настоящая цена сейчас — чистые сборки и V8, а их эти меры не трогают.

---

## 9. Журнал внедрения

| Дата | Правило | Срабатываний до | После | Статус | Примечание |
|---|---|---|---|---|---|
| 2026-08-18 | — (каркас) | — | — | ✅ | Заведены `[workspace.lints.clippy]` в корневом `Cargo.toml`, `clippy.toml` и `[lints] workspace = true` в 24 крейтах. Отдельным коммитом, семантически пустым: 25 правок манифестов не должны лежать в одном диффе с правками кода. `workspace-hack` пропущен (генерируется `cargo-hakari`). |
| 2026-08-18 | `clippy::undocumented_unsafe_blocks` | 17 в 7 крейтах | 0 | ✅ `deny` | Правки — только комментарии, ни строки логики. |
| 2026-08-18 | `clippy::panic` | 91 | 0 (25 под `#[allow]`) | ✅ `deny` | Из 91 срабатывания **66 — тестовый код**, до которого не достаёт `allow-panic-in-tests`; сняты семью `#![allow]` в корнях тест-бинарей и семнадцатью в `#[cfg(test)] mod`. Настоящих продакшн-паник 25, все под точечным `#[allow]` с обоснованием — реестр в §10. Ни одной правки поведения. |
| 2026-08-18 | `clippy::expect_used` | 250 | 0 (128 под `#[allow]`) | ✅ `deny` | 122 из 250 — тестовый код. Оставшиеся 128 унаследованы **на уровне функции** (71 функция) и файла (5 файлов бенчей/примеров), а не крейта: правило остаётся живым для любого нового кода. Правок поведения нет. |
| 2026-08-18 | `clippy::unwrap_used` | 865 | 0 (511 под `#[allow]`) | ✅ `deny` | Самое массовое правило яруса 0 и единственное, где область исключения местами пришлось взять грубее функции (184 из 511 — объемлющий `impl`). Правок поведения нет. |

**Что дало первое правило, кроме зелёного гейта** (2026-08-18):

- **Комментарий над двумя соседними `unsafe impl` документирует только первый.**
  `crates/engine/encoding/src/unicode_provider.rs` и `crates/network/src/ctap2.rs` — одинаковый
  узор: `// SAFETY:` над `unsafe impl Send`, следом голый `unsafe impl Sync`. Ревью такое
  пропускает годами (комментарий *визуально* покрывает обе строки), линт — нет.
- **Нашёлся реальный остаточный риск, а не только пропущенный комментарий.**
  `crates/network/src/ctap2.rs`: `buf.as_ptr().add(4).cast::<u16>()` на `Vec<u8>`, у которого
  гарантировано лишь 1-байтовое выравнивание — корректность держится на том, что системный
  аллокатор выдаёт блоки выравненными хотя бы на 2. Записано прямо в `// SAFETY:` вместе с
  лекарством (`read_unaligned`), а не заметено под формальный комментарий. Это и есть цена
  правила: заставляет либо доказать инвариант, либо признать допущение письменно.
- **`clippy::undocumented_unsafe_blocks` не знает про `allow-*-in-tests`.** Четыре блока в
  `#[cfg(test)]`-модуле `crates/shell/src/main.rs` (downcast `dyn EventSink` → конкретный тип
  через сырой указатель) и один в `crates/core/src/ext.rs` пришлось комментировать наравне с
  продакшн-кодом. Это правильно — «в тесте» не делает `unsafe` безопасным, — но при оценке
  стоимости следующего правила на это закладываться нельзя.
- **Ошибка в собственном протоколе §6** (`grep -c "clippy::<lint>"` при `--message-format=short`
  всегда даёт 0). Исправлена там же; замер по такой команде выглядел бы как «правило уже
  внедрено и срабатываний нет».

**Что дало второе правило** (`clippy::panic`, 2026-08-18):

- **Три четверти «нарушений политики» оказались тестовым кодом.** 66 из 91 срабатывания — в
  `#[cfg(test)] mod` и в интеграционных тестах, где `panic!` и есть механизм провала теста.
  Настоящих продакшн-паник 25. Любая оценка «сколько у нас нарушений», снятая грепом или даже
  clippy без этой разбивки, завышает работу в 3.6 раза.
- **Паники в продакшне сгруппированы, а не размазаны.** 11 из 25 — в `mathml.rs`/`ruby.rs`
  на пустом списке детей; 6 — один и тот же `unwrap_or_else(|_| panic!("hmac key"))`,
  скопированный шесть раз; 7 — build-скрипт, где паника легитимна. То есть «убрать паники из
  движка» — это три независимых решения, а не 25 правок.
- **Две паники прямо противоречат контракту своей функции.** `sign_data` документирована как
  «Returns signature bytes, or empty Vec on error», `verify_signature` — как «false otherwise»;
  обе вместо этого паникуют. Такое расхождение прозаическая политика не ловит в принципе:
  она про «есть ли `panic!`», а не про «совпадает ли он с докой».


**Что дало третье правило** (`clippy::expect_used`, 2026-08-18):
- **Исключение поставлено на функцию, а не на крейт — и это принципиально.** Крейтовый
  `#![allow]` в `lumen-paint` закрыл бы 40 срабатываний одной строкой и одновременно разрешил бы
  любой новый `expect()` в этом крейте навсегда. 71 функциональный `#[allow]` уродливее, но
  оставляет правило работающим для всего, что будет написано завтра. Правило, отключённое по
  крейту, — не правило, а запись в документации.
- **«Прод» пришлось разделить ещё раз: движок и инструменты.** `crates/bench` и `examples/` —
  не то, что видит пользователь; падение бенча равнозначно падению теста. Это дало пятый
  файловый `#![allow]` и убрало 10 ложных «нарушений политики» из списка долга.
- **Однородного лекарства нет.** У `panic` было три группы, у `expect` — как минимум четыре
  идиомы (отравление мьютекса, инициализация ресурса, индексация, прочее), и «убрать expect»
  означает четыре разных решения, а не одну замену. Отсюда и вывод для `unwrap_used`:
  865 срабатываний — это не одна задача.

**Что дало четвёртое правило** (`clippy::unwrap_used`, 2026-08-18):

- **Прогноз из §3 не подтвердился ровно в том месте, где его делали на глаз.** Ожидалось «621 из
  850 в `lumen-js`, отдельным заходом не берётся, нужен крейтовый `#![allow]`». После разбивки по
  `#[cfg(test)] mod` продакшн-мест оказалось 511, в `lumen-js` — 282. Крейтовое отключение не
  понадобилось.
- **Инструмент разбора пришлось чинить трижды, и каждый раз ловил его не глаз, а `cargo clippy`.**
  (1) «Первый `#[cfg(test)] mod` — и всё ниже тест» неверно: в `crates/core/src/ext.rs` тестовый
  модуль стоит ВЫШЕ `impl SseCancel`, и четыре продакшн-`unwrap` были молча зачислены в тесты.
  (2) «Ближайшая `fn` выше — объемлющая» неверно: в `crates/engine/paint/src/renderer.rs`
  вложенный `fn add_rect` объявлен на 1200 строк выше срабатывания, и исключение вставало не на
  ту функцию. (3) Подсчёт скобок вместо отступов не работает в том же `renderer.rs` — фигурные
  скобки в строках с WGSL-шейдерами. Вывод для протокола §6: **верификационный прогон — не
  формальность, а единственная проверка, что исключения встали куда надо**; «правило включено»
  без него ничего не значит.
- **Единственное место, где храповик слабее задуманного.** В 184 случаях из 511 (замыкания внутри
  макросов, `crates/js/src/v8_runtime.rs`) надёжно определить функцию не удалось, и `#[allow]`
  стоит на объемлющем `impl`. Новый `unwrap()`, дописанный в такой `impl`, линт пропустит.
  Это записано в §10 как долг, а не замолчано.

---

## 10. Реестр действующих исключений (`#[allow]`)

Каждое исключение — с причиной и владельцем. **Пустая строка здесь означает, что правило
внедрено честно;** длинная — что правило внедрено формально и работа не сделана. Реестр
обязателен: `#[allow]` без строки в этой таблице считается недосмотром, а не решением.

| Правило | Место | Тип | Причина / кому чинить |
|---|---|---|---|
| `clippy::panic` | `crates/{js,driver,engine/font,engine/image,engine/layout,engine/paint}/tests/*.rs` (7 корней тест-бинарей) | постоянное | `panic!` — механизм провала теста. `allow-panic-in-tests` из `clippy.toml` освобождает только тело `#[test]`-функции, а не хелперы тестового крейта; исключение компенсирует именно этот пробел. |
| `clippy::panic` | 17 модулей `#[cfg(test)] mod` в `crates/js/src/*.rs` | постоянное | То же самое, на уровне модуля. |
| `clippy::panic` | `crates/chrome/build.rs` | постоянное | Паника — штатный способ провалить сборку из build-скрипта; вернуть `Result` некуда. Мягкий отказ дал бы бинарь с невалидным ассетом хрома. |
| `clippy::panic` | `crates/engine/layout/src/mathml.rs` (10 мест), `ruby.rs::stack_boxes_horizontal` | **временное** | Паника на пустом списке детей. Сегодня недостижима (`lay_out_mathml`/`lay_out_ruby` не вызываются конвейером — «box-tree integration deferred», `style.rs`), но обе функции экспортируются из крейта: первая же страница с `<math></math>` уронит движок, как только интеграцию допишут. Правка — вернуть пустой `LayoutBox` — меняет поведение `lumen-layout`. **Владелец: P1/P4.** |
| `clippy::panic` | `crates/js/src/subtle_crypto.rs::{sign_data, verify_signature}` (6 мест) | **временное** | `unwrap_or_else(\|_\| panic!("hmac key"))` вместо документированных `Vec::new()`/`false`. `Hmac::new_from_slice` принимает ключ любой длины, поэтому ветка недостижима — но контракт функции нарушен. **Владелец: P1 (lumen-js).** |
| `clippy::panic` | `crates/js/src/v8_runtime.rs::run` | **временное** | JS-поток умер — ответа не будет; следующая строка всё равно паникует на `rx.recv()`. Убрать можно только сменой сигнатуры `run` на `Result`, что каскадом идёт по всем вызывающим. **Владелец: P1 (lumen-js).** |
| `clippy::expect_used` | 5 корней тест-бинарей (`driver`, `engine/{font,layout,paint}`, `shell/tests/ipc_server.rs`) + 4 тестовых модуля `lumen-js` | постоянное | Та же причина, что у `panic`: `allow-expect-in-tests` не достаёт до хелперов. |
| `clippy::expect_used` | `crates/bench/src/{main,ci_gate,bfcache_restore}.rs`, `crates/engine/font/examples/preview.rs`, `crates/engine/image/examples/gen_webp_sample.rs` | постоянное | Файлы-инструменты (бенч, примеры), а не движок: падение здесь ничего не роняет у пользователя. Файловый `#![allow]`. |
| `clippy::expect_used` | **71 функция в 35 файлах** (paint 40 срабатываний, shell 35, storage 12, js 7, network 7, core 3, layout 3, knowledge 3, driver 2, dom 2, html-parser 2, bidi-server 1, image 1) | **временное** | Унаследованный код. Исключение стоит **на функции**, а не на крейте — новый `expect()` в новой функции линт поймает. Идиомы разные и требуют разных решений: `Mutex::lock().expect(«poisoned»)` (12 мест — отравление мьютекса значит, что другой поток уже упал; кандидат на постоянное исключение), инициализация ресурса (41), индексация/`last()` (22), прочее (53). **Владельцы: по крейтам.** |
| `clippy::unwrap_used` | корень тест-бинаря `lumen-js` + 98 файлов с `#[cfg(test)] mod` | постоянное | Та же причина, что у `panic`/`expect_used`. 354 из 865 срабатываний. |
| `clippy::unwrap_used` | `crates/bench/src/bfcache_restore.rs` | постоянное | Файл-инструмент. |
| `clippy::unwrap_used` | **262 `#[allow]` в 123 файлах** (js 282 срабатывания, shell 106, network 43, font 21, layout 19, storage 16, paint 9, canvas 6, core 8, encoding 1) | **временное** | Унаследованные 511 мест. Область — функция, где её удалось определить надёжно; в 184 случаях (замыкания внутри макросов, например `crates/js/src/v8_runtime.rs`) пришлось взять более грубую — объемлющий `impl`/элемент верхнего уровня. Это единственное место, где храповик слабее задуманного: новый `unwrap()`, дописанный **в уже помеченный `impl`**, линт пропустит. **Владельцы: по крейтам.** |
