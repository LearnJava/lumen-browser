# WPT vendor notes — `connection-allowlist`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-connection-allowlist`, `docs/wpt-status.md`), included despite its 🚫 scope note (niche corporate API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`compute-pressure`. Same pinned commit, `git sparse-checkout` at the same commit hash, 183 files — the entire category lives under a single `tentative/` subdir, with no category-level `META.yml`/`WEB_FEATURES.yml`. Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/common/utils.js`, `/common/blank.html`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root connection-allowlist --recursive` completed fully (~11min, 73 selected ids): 46 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`), 10 ERROR (session/crash-restart artifact — `eval: live window closed before replying`/`WebSocket connection closed`, same class as `accelerometer`/`beacon`); 17 harness OK: 14 (6 `early_hints_*` + 8 `link_header_*`) each 0/1 subtest FAIL (`navigateToTestCase is not defined` — the category's own `resources/early-hints-helpers.sub.js` 500s from the same HTTPS-port-template gap as `clear-site-data`'s `support/test_utils.sub.js`, plus the un-vendored `/common/utils.js`); 3 `webrtc-*` — `webrtc-allow` 1/2 (Connection-Allowlist isn't implemented, so "allow" trivially passes), `webrtc-block(-default)` 0/2 each (`assert_equals: expected "NotAllowedError" but got "Error"` — WebRTC isn't gated at all, expected on a first pass), all three additionally FAIL a second subtest on a relative `fetch("/common/blank.html")`, same relative-URL gap as `browsing-topics`/`client-hints`. 1/20 subtests passed. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (коммит `35be3b44`, `tests/wpt/connection-allowlist/`, 183 файла, вся категория живёт под `tentative/`, без category-level `META.yml`/`WEB_FEATURES.yml`), включена несмотря на скоуп 🚫 (нишевый корпоративный API) по той же постоянной пользовательской договорённости, что `accelerometer`/`acid`/`ai`/…/`compute-pressure`. Внекатегорийные хелперы не довендорены: `/common/get-host-info.sub.js`, `/common/utils.js`, `/common/blank.html`, та же методология survey-gap, что `FileAPI`/`ai`. `run_report.py --all --root connection-allowlist --recursive` прошёл полностью (~11мин, 73 отобранных id): 46 TIMEOUT (`invalid url: invalid port: "None"`, тот же HTTPS-порт-гэп минимального исполнителя, что `WebCryptoAPI`/`ai`/`bluetooth`), 10 ERROR (session/crash-restart артефакт — `eval: live window closed before replying`/`WebSocket connection closed`, тот же класс, что `accelerometer`/`beacon`); 17 harness OK: 14 (6 `early_hints_*` + 8 `link_header_*`) — 0/1 сабтест FAIL каждый (`navigateToTestCase is not defined` — категорийный `resources/early-hints-helpers.sub.js` падает HTTP 500 из-за того же https-порт-шаблон-гэпа, что `support/test_utils.sub.js` в `clear-site-data`, плюс невендоренный `/common/utils.js`); 3 `webrtc-*` — `webrtc-allow` 1/2 (Connection-Allowlist не реализован, поэтому «allow» тривиально проходит), `webrtc-block(-default)` 0/2 каждый (`assert_equals: expected "NotAllowedError" but got "Error"` — WebRTC не блокируется вовсе, API не реализован, первый проход), у всех трёх второй сабтест FAIL на относительном `fetch("/common/blank.html")` — тот же гэп с относительным URL, что `browsing-topics`/`client-hints`. 1/20 сабтестов passed. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
