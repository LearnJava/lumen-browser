# WPT vendor notes — `preload`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/preload/`, 126 файлов: `META.yml`, `WEB_FEATURES.yml`, ~65 корневых `link-header-*`/`avoid-*`/`dynamic-adding-*`/`modulepreload-*`/`preload-*`/`prefetch-*`/`single-download-preload*`/`supported-as-values*` тестов, `resources/` (фикстуры + `preload_helper.js`); `LICENSE-WPT.md` скопирован из соседней `js`). Ни одного `testdriver.js`-хита, 13 `name="variant"` (раздувает `supported-as-values.html` до 13 id), 5 `.https.` файлов; внекатегорийные хелперы `/common/utils.js`+`/common/get-host-info.sub.js`+`/common/dispatcher/dispatcher.js` НЕ довендорены. `run_report.py --all --root preload --recursive` (~8 мин, 79 отобранных id): **36/79 harness OK, 10/203 сабтестов**. Доминирующая причина отказа — реконфирмация [BUG-485](../bugs/BUG-485-FIXED.md)/[BUG-565](../bugs/BUG-565-FIXED.md) (`document.head` всегда `undefined` — ~60 хитов `Cannot read properties of undefined (reading 'appendChild'/'append')` в `preload-error.sub.html` и `supported-as-values.html`, обе строят `<link>`/`<img>`/`<script>` через `document.head.appendChild`/`.append`). Остальной сигнал: [BUG-611](../bugs/BUG-611-OPEN.md) (`HTMLLinkElement.relList` отсутствует — `verifyPreloadAndRTSupport()` падает на `link.relList.supports`), [BUG-568](../bugs/BUG-568-OPEN.md) (`document.write` отсутствует), [BUG-380](../bugs/BUG-380-FIXED.md) (переиспользование browsing context — 4 ERROR `Got results from X, expected Y`), TLS-гэп `UnknownIssuer` на всех 5 `.https.` id, и `ReferenceError: get_host_info is not defined`/`token is not defined` на невендоренных `/common/*.js` (6 id). Новых багов не заведено
