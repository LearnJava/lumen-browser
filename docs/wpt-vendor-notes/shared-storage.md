# WPT vendor notes — `shared-storage`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-shared-storage`, `docs/wpt-status.md`), scope 🚫 (out of scope — ad-tech / Privacy Sandbox). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `shadow-dom` category, 100 files (`WEB_FEATURES.yml`, root tests, `resources/`). Nearly the whole category is `.https.`/`.tentative.` (88 of 90 selected ids), only 2 files pull `testdriver.js`, no `variant` hits. `run_report.py --all --root shared-storage --recursive --processes=4` completed in ~9:41 (90 selected ids): **2/90 harness OK, 0/2 subtests**. 86 ids TIMEOUT and 2 ERROR on the already-documented WPT-RUN-2 TLS `UnknownIssuer` gap / stuck-browsing-context artifact (same class as BUG-380 — `AssertionError: Got results from insecure-context.tentative.http.html, expected interest-groups.tentative.https.sub.html`). The two non-`.https.` tests that actually ran both FAIL, and both reconfirm the same already-open root cause — [BUG-399](../../bugs/BUG-399-OPEN.md): `window.isSecureContext` is hardcoded `true` unconditionally, so no `[SecureContext]` WebIDL gate is ever enforced anywhere in the engine. `sharedStorage` (`crates/js/src/shared_storage.rs`) is installed on `window` with no context check at all, so `insecure-context.tentative.http.html` (asserts `window.sharedStorage === undefined` on an insecure origin) sees a live object instead, and `shared-storage-writable-insecure-context.tentative.http.sub.html` (asserts `sharedStorageWritable` rejects on an insecure origin) sees it silently succeed. No new bug numbers filed. See `docs/wpt-status.md` for the full breakdown.
