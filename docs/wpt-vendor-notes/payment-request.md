# WPT vendor notes — `payment-request`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/payment-request/`, 76 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `payment-method-id`, `PaymentAddress/`, `PaymentMethodChangeEvent/`, `PaymentRequestUpdateEvent/`, `PaymentValidationErrors/`, `payment-response/`, `resources/`, 26 корневых тестовых файлов). Ни variant-ов; 33 из 36 отобранных тестов `.https.`, 11 файлов тянут `testdriver.js`. `run_report.py --all --root payment-request --recursive --processes=4` (~3 мин 25 с, 32 отобранных id): **2/32 harness OK, 0/2 сабтестов**. 30/32 TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer`. Два исполнившихся не-`.https.` теста дали две реконфирмации известных дефектов, а не новые баги: `payment-request-insecure.http.html` (`assert_false(isSecureContext); assert_false("PaymentRequest" in window)`) падает на первом `assert_false` — реконфирмация [BUG-399](../bugs/BUG-399-OPEN.md) (`window.isSecureContext` захардкожен `true`); `PaymentRequestUpdateEvent/constructor.http.html` (`assert_false("PaymentRequestUpdateEvent" in Window)`) падает `ReferenceError: Window is not defined` — реконфирмация [BUG-637](../bugs/BUG-637-OPEN.md) (глобальный интерфейсный объект `Window` не установлен вовсе). Новых багов не заведено
