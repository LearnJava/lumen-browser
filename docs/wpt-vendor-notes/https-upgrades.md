# WPT vendor notes — `https-upgrades`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-04 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-https-upgrades`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 8 files (`README`, `resources/pass.html`+`pass-with-referrer.html`, 6 `tentative/*.sub.html` tests, `LICENSE-WPT.md` copied from the sibling `hsts` category — this category has no `META.yml`/`WEB_FEATURES.yml`). One out-of-category helper (`/common/get-host-info.sub.js`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root https-upgrades --recursive` — ~2:38, 6 selected ids: **0/6 harness OK**, all TIMEOUT. The category tests the Fetch spec's automatic port-preserving http→https upgrade (Lumen only implements the HSTS-header variant, `crates/network/src/lib.rs` `hsts::maybe_upgrade_url_to_https`) via a `window.open(url)` + `postMessage`-back-to-`opener` pattern. All 6 ids die on that mechanism, not on the upgrade behavior itself: `window.open` (`crates/js/src/dom.rs:8741`) returns a stub object (`opener: null`, `postMessage` a no-op) — the already-filed "second barrier" of [BUG-359](../bugs/BUG-359-FIXED.md) ("Real popup support… is what the `-late` tests actually need"). No new bug filed — reconfirmation of BUG-359.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-04 (коммит `35be3b44`, `tests/wpt/https-upgrades/`, 8 файлов: `README`, `resources/pass.html`+`pass-with-referrer.html`, 6 `tentative/*.sub.html` тестов, `LICENSE-WPT.md`). `run_report.py --all --root https-upgrades --recursive` — ~2:38, 6 selected ids: **0/6 harness OK**, все TIMEOUT. Категория проверяет автоматический http→https апгрейд (Fetch spec, порт-сохраняющий, отличный от HSTS — в Lumen реализован только HSTS-вариант, `crates/network/src/lib.rs` `hsts::maybe_upgrade_url_to_https`) через паттерн `window.open(url)` + `postMessage` обратно в `opener`. Реальная причина отказа не в отсутствии HTTPS-апгрейда: `window.open` (`crates/js/src/dom.rs:8741`) возвращает stub-объект (`opener: null`, `postMessage: function() {}` — no-op), тот же «второй барьер», что уже задокументирован в [BUG-359](../bugs/BUG-359-FIXED.md) («Real popup support… is what the `-late` tests actually need»). Все 6 тестов зависят от этого барьера — новых багов не заведено, переподтверждение существующего
