# WPT vendor notes — `beacon`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-beacon`, `docs/wpt-status.md`), in-scope (⬜, Beacon API — `navigator.sendBeacon`). Same pinned commit, `git sparse-checkout` at the same commit hash, 22 files (`META.yml`, `WEB_FEATURES.yml`, 5 root `.https.` test files, `beacon-common.sub.js`, `idlharness.any.js`, `headers/` — 9 test files + `header-referrer.js`, `resources/` — 4 Python handlers). Out-of-category helpers not vendored: `/common/utils.js`, `/common/get-host-info.sub.js`, `/common/redirect.py`, `/common/blank.html`, `/common/text-plain.txt`, and `idlharness.any.js`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`/`audio-session`. First category in this backlog that is not entirely `.https.` (9 of 16 selected ids are plain HTTP), which for the first time surfaced a real effect of the un-vendored `/common/*` helpers on execution rather than just the HTTPS-port TIMEOUT: `headers/header-content-type-and-body.html` harness OK but 0/6 subtests FAIL (`self.token is not a function`, `/common/utils.js` 404); three more non-https tests TIMEOUT (same `self.token`/`get_host_info is not defined` errors thrown before test registration, hanging the harness instead of reporting); `idlharness.any.html` TIMEOUT (`idl_test is not defined`, same pattern as `audio-session`/`animation-worklet`). The 9 `.https.` tests hit the usual HTTPS-port gap (`invalid url: invalid port: "None"`) — 7 TIMEOUT, 2 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`ambient-light`/`animation-worklet`). `run_report.py --all --root beacon --recursive` completed in ~6:16. 1/16 harness OK, 0/6 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (коммит `35be3b44`, `tests/wpt/beacon/`, 22 файла: `META.yml`, `WEB_FEATURES.yml`, 5 корневых `.https.` тестовых файлов, `beacon-common.sub.js`, `idlharness.any.js`, `headers/` — 9 тестовых файлов + `header-referrer.js`, `resources/` — 4 Python-хендлера). Внекатегорийные хелперы не довендорены: `/common/utils.js`, `/common/get-host-info.sub.js`, `/common/redirect.py`, `/common/blank.html`, `/common/text-plain.txt`, а `idlharness.any.js` ссылается на `/resources/WebIDLParser.js`+`/resources/idlharness.js` — та же методология, что `FileAPI`/`ai`/`audio-session`. Первая категория этого бэклога, не целиком `.https.` (9 из 16 отобранных id — обычный HTTP), что впервые показало реальный эффект невендоренных `/common/*`-хелперов на исполнение, а не только TIMEOUT по HTTPS-порт-гэпу: `headers/header-content-type-and-body.html` — харнесс OK, но 0/6 сабтестов FAIL (`self.token is not a function`, `/common/utils.js` даёт 404); `headers/header-origin-same-origin.html`/`header-referrer-no-referrer.html`/`header-referrer-same-origin.html` — TIMEOUT (тот же `self.token is not a function`, но брошенный до регистрации теста — харнесс зависает вместо репорта); `headers/header-referrer-origin-when-cross-origin.html`/`header-referrer-origin.html` — TIMEOUT (`get_host_info is not defined`, `/common/get-host-info.sub.js` 404); `idlharness.any.html` — TIMEOUT (`idl_test is not defined`, невендоренные `/resources/WebIDLParser.js`+`idlharness.js`, тот же паттерн, что `audio-session`/`animation-worklet`). Оставшиеся 7 `.https.`-тестов (`beacon-basic`/`beacon-cors`/`beacon-navigate`/`beacon-redirect`/`beacon-url-encoding-euc-kr`/`header-referrer-strict-origin`/`header-referrer-unsafe-url`) — TIMEOUT с тем же HTTPS-порт-гэпом (`invalid url: invalid port: "None"`, минимальный исполнитель не поднимает HTTPS-порт), что `WebCryptoAPI`/`ai`/`ambient-light`; ещё 2 `.https.`-теста (`header-referrer-no-referrer-when-downgrade`, `header-referrer-strict-origin-when-cross-origin`) — ERROR (session/URL-reuse артефакт, тот же класс, что `accelerometer`/`ambient-light`/`animation-worklet`). `run_report.py --all --root beacon --recursive` добежал целиком за ~6:16 (16 отобранных id). 1/16 harness OK, 0/6 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет; невендоренные `/common/*` 404 — задокументированный методологический гэп, не движковый баг
