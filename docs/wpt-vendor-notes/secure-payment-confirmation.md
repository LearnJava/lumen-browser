# WPT vendor notes — `secure-payment-confirmation`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/secure-payment-confirmation/`, 28 файлов: `META.yml`, `WEB_FEATURES.yml`, 20 `.https.html` тестов (`authentication-*`, `constructor*`, `enrollment*`), `resources/`, `utils(-bbk).sub.js`, `troy.png`, `sync-network-logo.png`, `LICENSE-WPT.md`), скоуп 🚫 (Payment Request API). Прогон `run_report.py --all --root secure-payment-confirmation --recursive` (~8:50, 20 отобранных id): **0/20 harness OK, 0/0 сабтестов** — все 20 id TIMEOUT на `network error: TLS handshake: invalid peer certificate: UnknownIssuer`, тот же уже задокументированный HTTPS-порт/TLS-гэп, что `screen-details`/`screen-capture`/`digital-credentials`; ноль тестов дошли до навигации. Живая проба (`--mcp-live-port`, локальный HTTP-сервер вне вендоренного `wptserve`) подтвердила: `window.PaymentRequest`/`window.PublicKeyCredential` присутствуют (обе `typeof === 'function'`), `new PaymentRequest([{supportedMethods: 'secure-payment-confirmation', data: {...}}], details)` конструируется успешно с корректной цепочкой прототипов, но без единой проверки полей SPC-специфичного `data` (`credentialIds`, `challenge`, `rpId`, `payeeOrigin`, `instrument`) — тот же класс дефекта, что уже заведён [BUG-646](../../bugs/BUG-646-OPEN.md) (`crates/js/src/payment_request.rs`: конструктор проверяет только `typeof === 'object'`, спек-валидация полей отсутствует целиком); SPC не добавляет собственной логики поверх общего Phase-0 `PaymentRequest`-шима — `secure-payment-confirmation` как имя метода не отличается от `basic-card`. Новых номеров багов не заведено (реконфирмация BUG-646)
