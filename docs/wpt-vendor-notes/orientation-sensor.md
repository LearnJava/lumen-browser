# WPT vendor notes — `orientation-sensor`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/orientation-sensor/`, 22 файла: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `orientation-event`, `AbsoluteOrientationSensor*`/`RelativeOrientationSensor*`/`OrientationSensor_insecure_context.html`, `idlharness.https.window.js`, `resources/`). Ни variant-ов; 15 из 16 отобранных id `.https.`, 4/17 файлов тянут `testdriver.js`. `run_report.py --all --root orientation-sensor --recursive` (~7 мин 20 с, 16 отобранных id): **1/16 harness OK, 0/2 сабтестов**. 15/16 TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer`. Единственный не-`.https.` тест (`OrientationSensor_insecure_context.html`) реально исполнился и провалил оба сабтеста: `AbsoluteOrientationSensor`/`RelativeOrientationSensor` не должны быть видны на небезопасном origin, но видны — прямая переподтверждение уже заведённого [BUG-399](../bugs/BUG-399-FIXED.md) (`window.isSecureContext` захардкожен `true` в `crates/js/src/dom.rs:11531`, вся Generic Sensor family не гейтится по спеке). API реализован как Phase 0 заглушка (`crates/js/src/generic_sensor.rs`, нет реального железа). Новый баг не заведён
