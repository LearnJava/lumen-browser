# WPT vendor notes — `scroll-to-text-fragment`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/scroll-to-text-fragment/`, 62 файла). Прогон `run_report.py --all --root scroll-to-text-fragment --recursive` (~1:39, 39 отобранных id, 25 фактически запущенных — 14 «target»/помощничьих страниц не тесты): 18/25 harness OK, 0/142 сабтестов. Весь сигнал сводится к уже задокументированным дефектам, новых не найдено: большинство FAIL — `ReferenceError: token is not defined` от невендоренного `/common/utils.js` (та же методология, что `FileAPI`/`animation-worklet`); кластер `TypeError: elementDocument.contains is not a function` (`resources/testdriver.js:48`, `element.ownerDocument.contains` не определён) — реконфирмация [BUG-462](../bugs/BUG-462-OPEN.md)/[BUG-574](../bugs/BUG-574-OPEN.md)/[BUG-655](../bugs/BUG-655-OPEN.md); `document.fragmentDirective is defined expected "object" but got "undefined"` — API Scroll To Text Fragment не реализован в движке вовсе (`grep -rli fragmentDirective crates/` — ноль совпадений), согласуется с `scroll-performance-timing`; 6/6 TIMEOUT с 0/0 сабтестов (`same-document-tests*.html`, `percent-encoding.html`, `iframes.sub.html`) — все используют `<body onload="runTests()">`, реконфирмация [BUG-360](../bugs/BUG-360-FIXED.md) (инлайн-обработчики событий мертвы, `runTests()` никогда не вызывается); `iframe-scroll.sub.html` `Cannot set properties of undefined (setting 'location')` на `frames[0].location = ...` — реконфирмация класса [BUG-480](../bugs/BUG-480-OPEN.md) (у `<iframe>` нет отдельного browsing context). Новых багов не заведено
