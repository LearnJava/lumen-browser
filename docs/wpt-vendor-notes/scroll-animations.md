# WPT vendor notes — `scroll-animations`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/scroll-animations/`, 283 файла: `animation-trigger/`, `css/`, `crashtests/`, `scroll-timelines/`, `view-timelines/`). Внекатегорийные хелперы `/web-animations/testcommon.js` (используется почти всеми тестами — `createDiv`/`target`/`scroller`/... не определены) и `/dom/events/scrolling/scroll_support.js` НЕ довендорены, та же методология, что `FileAPI`/`animation-worklet`. Прогон `run_report.py --all --root scroll-animations --recursive` (~2:46, 205 отобранных id): 160/205 harness OK, 355/1853 сабтестов passed. Замеченные кластеры провалов: большинство — `ReferenceError` от невендоренного `testcommon.js` (документированный гэп, не движковый баг); 40× `CSS.percent is not a function` + 6× `CSS.px is not a function` — реконфирмация уже открытого [BUG-554](../../bugs/BUG-554-OPEN.md) (CSS Typed OM numeric factories отсутствуют); 2× `animation.effect.getComputedTiming is not a function` на тестах, исполнившихся независимо от `testcommon.js` — новая находка, живой пробой (`--mcp-live-port`) подтверждён [BUG-670](../../bugs/BUG-670-OPEN.md): `KeyframeEffect.prototype.getComputedTiming` отсутствует целиком
