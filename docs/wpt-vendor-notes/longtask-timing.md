# WPT vendor notes — `longtask-timing`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-longtask-timing`, `docs/wpt-status.md`), scope ⬜ (candidate, Long Tasks API / `PerformanceLongTaskTiming`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 30 files (`META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `long-animation-frame` category, 21 top-level test files incl. `shared-renderer/longtask-in-new-window.html`, `resources/` — 5 helpers). One out-of-category helper not vendored: `/common/get-host-info.sub.js` (used by the two cross-origin iframe tests), same documented survey gap as `FileAPI`/`ai`. No `variant` hits, no `.https.` files, no `testdriver.js` hits — self-contained and cheap. `run_report.py --all --root longtask-timing --recursive` completed in ~1:04 (21 selected ids): **15/21 harness OK, 1/23 subtests passed**. The Long Tasks API is entirely absent — a `crates/js/src/dom.rs` grep found no `PerformanceLongTaskTiming` global constructor, so most FAIL/ERROR are `assert_implements: Longtasks are not supported` (`window.PerformanceLongTaskTiming` is undefined). The one passing subtest — `supported-longtask-types.window.html`'s first assertion (`PerformanceObserver.supportedEntryTypes` contains `'longtask'`) — re-confirms [BUG-354](../../bugs/BUG-354-FIXED.md) (the hardcoded `supportedEntryTypes` list advertises unimplemented types, `longtask` among them); the file's second assertion (an actual `PerformanceObserver` delivery) TIMEOUT. 2 further TIMEOUT from the un-vendored `get-host-info.sub.js` in the cross-origin iframe tests; 1 TIMEOUT — `idlharness.window.html`, un-vendored `/resources/WebIDLParser.js`+`idlharness.js`, same pattern as `audio-session`/`element-timing`. No bug filed for the Long Tasks gap itself: first pass documenting an absent candidate feature. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/longtask-timing/`, 30 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `long-animation-frame`, 21 тестовый файл верхнего уровня (включая `shared-renderer/longtask-in-new-window.html`), `resources/` — 5 хелперов), скоуп ⬜ (кандидат, Long Tasks API / `PerformanceLongTaskTiming`). Ни variant-ов, ни `.https.`, ни `testdriver.js` — самодостаточная и дешёвая категория. `run_report.py --all --root longtask-timing --recursive` — ~1:04, 21 отобранный id: **15/21 harness OK, 1/23 сабтестов**. Long Tasks API целиком отсутствует — живой грep `crates/js/src/dom.rs` подтвердил: нет `PerformanceLongTaskTiming` (глобальный конструктор), большинство FAIL/ERROR — `assert_implements: Longtasks are not supported` (`window.PerformanceLongTaskTiming` undefined). Единственный прошедший сабтест — `supported-longtask-types.window.html` первая проверка (`supportedEntryTypes` содержит `'longtask'`) — то же переподтверждение [BUG-354](../../bugs/BUG-354-FIXED.md) (захардкоженный список `supportedEntryTypes` рекламирует нереализованные типы, включая `longtask`), вторая проверка того же файла (реальная доставка записи через `PerformanceObserver`) TIMEOUT. 2 TIMEOUT дополнительно — невендоренный `/common/get-host-info.sub.js` в кросс-ориджин iframe-тестах (тот же документированный разрыв, что `FileAPI`/`ai`), 1 TIMEOUT — `idlharness.window.html` (невендоренные `/resources/WebIDLParser.js`+`idlharness.js`, тот же паттерн, что `audio-session`/`element-timing`). Новых багов не заведено — первый проход по целиком отсутствующему кандидатному API, как `Profiler`/LCP/LoAF.
