# WPT vendor notes — `picture-in-picture`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/picture-in-picture/`, 24 файла: `META.yml`, `WEB_FEATURES.yml`, 17 тестовых файлов, `resources/` — 4 хелпера, `LICENSE-WPT.md` скопирован из соседней `permissions-revoke`), скоуп 🚫 (PiP — нет медиа-конвейера видео), включена несмотря на скоуп по постоянному решению пользователя, как остальные WPT-VENDOR-категории. `run_report.py --all --root picture-in-picture --recursive` (~51 с, 17 отобранных id): **14/17 harness OK, 0/36 сабтестов**. Доминирующая причина отказа — `ReferenceError: getVideoURI is not defined` (`/common/media.js` не вендорен, тот же документированный гэп, что `FileAPI`/`ai`); `permissions-policy.https.sub.html` ERROR — реконфирмация BUG-380 (переиспользование browsing context); `unload-document-exits-picture-in-picture.html` TIMEOUT. Один FAIL не связан с медиа-гэпом: `disable-picture-in-picture.html` — `assert_true: expected true got false` на `video.setAttribute('disablepictureinpicture', 'foo'); assert_true(video.disablePictureInPicture)`. Живая проба (`--mcp-live-port`) подтвердила механизм напрямую: `hasAttribute(...) === true`, но `video.disablePictureInPicture === false` — уже реализованный PiP-шим (`crates/js/src/video_pip.rs`) кеширует состояние атрибута один раз при патчинге элемента вместо живого чтения. Заведён [BUG-653](../../bugs/BUG-653-OPEN.md)
