# WPT vendor notes — `clipboard-apis`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-clipboard-apis`, `docs/wpt-status.md`), in-scope (⬜, Clipboard API and events — W3C `clipboard-apis` spec). Same pinned commit, `git sparse-checkout` at the same commit hash, 76 files (`META.yml`, root-level tests, `detached-iframe/`+`events/`+`permissions/`+`permissions-policy/` subcategories each with its own `WEB_FEATURES.yml`). Out-of-category helpers not vendored: `/common/subset-tests.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/testdriver.js`+`testdriver-vendor.js`+`testdriver-action.js`+`testdriver-actions.js`, same documented survey gap as `FileAPI`/`ai`. Manifest selects 58 ids: 53 `.https.`-only + 5 non-https — `run_report.py --all --root clipboard-apis --recursive` completed fully (~34s): 51 SKIP (`Executor does not support testdriver.js` — 50 from the `.https.` subset + 1 non-https `events/copy-event.html`, synthetic copy/cut/paste and permission tests need testdriver-driven emulation, same gap as `accname`/`attribution-reporting`/`clear-site-data`); 3 `.https.` ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`captured-mouse-events`/`client-hints`); of the remaining 4 non-https: `drag-multiple-urls.html` TIMEOUT (harness hangs on drag-and-drop `text/uri-list` parsing, unrelated to the HTTPS-port gap); `clipboard-events-synthetic.html` harness OK but 0/9 subtests FAIL (synthetic copy/cut/paste events don't set `composed`); `data-transfer-file-list-change-reference-updates.html` harness OK but 0/1 subtest FAIL (`DataTransfer.files` doesn't reflect changes); `dataTransfer-clearData.html` harness OK but 0/1 subtest FAIL (`DataTransfer.clearData()` doesn't work). 3/58 harness OK, 0/12 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (коммит `35be3b44`, `tests/wpt/clipboard-apis/`, 76 файлов: `META.yml`, корневые тесты, `detached-iframe/`+`events/`+`permissions/`+`permissions-policy/` подкатегории, каждая со своим `WEB_FEATURES.yml`). Внекатегорийные хелперы не довендорены: `/common/subset-tests.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/testdriver.js`+`testdriver-vendor.js`+`testdriver-action.js`+`testdriver-actions.js`, та же методология, что `FileAPI`/`ai`. `run_report.py --all --root clipboard-apis --recursive` добежал полностью (58 отобранных id: 53 `.https.`-only + 5 не-https, ~34с): 51 SKIP (`Executor does not support testdriver.js` — 50 из `.https.` подмножества + 1 не-https `events/copy-event.html`, синтетические copy/cut/paste и permissions-тесты требуют testdriver-эмуляции, тот же гэп, что `accname`/`attribution-reporting`/`clear-site-data`); 3 `.https.` ERROR (session/URL-reuse артефакт, тот же класс, что `accelerometer`/`beacon`/`captured-mouse-events`/`client-hints`); из оставшихся 4 не-https: `drag-multiple-urls.html` — TIMEOUT (харнесс зависает на drag-and-drop тексте `text/uri-list`, не связан с HTTPS-портом); `clipboard-events-synthetic.html` — harness OK, 0/9 сабтестов FAIL (синтетические copy/cut/paste события не проставляют `composed`); `data-transfer-file-list-change-reference-updates.html` — harness OK, 0/1 сабтест FAIL (`DataTransfer.files` не отражает изменения); `dataTransfer-clearData.html` — harness OK, 0/1 сабтест FAIL (`DataTransfer.clearData()` не работает). 3/58 harness OK, 0/12 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
