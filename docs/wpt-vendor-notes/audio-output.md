# WPT vendor notes — `audio-output`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-audio-output`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`. Same pinned commit, `git sparse-checkout` at the same commit hash, 12 files (`META.yml`, `idlharness.https.window.js`, 9 `*.https(.sub).html`, `setSinkId-manual.https.html`). Three out-of-category helpers (`/common/get-host-info.sub.js`, `/common/gc.js`, `/permissions-policy/resources/permissions-policy.js`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root audio-output --recursive` completed in ~1 min (10 selected ids, `setSinkId-manual.https.html` excluded by `run_report.py`'s own `-manual` filter): 5 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); 3 `.https.`-only TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`); 2 ERROR — `secure-context.html` (the sole non-`.https.` test, actually served over plain HTTP) fails its own setup assumption `assert_false(window.isSecureContext)`: the spec treats `http://127.0.0.1` as a potentially-trustworthy origin ("localhost"), so `isSecureContext` correctly returns `true`, matching real browsers — the test assumes an environment without that exception, not a Lumen engine bug; `selectAudioOutput-sans-user-activation.https.html` is the same session-reuse artifact as `accelerometer`/`ambient-light`/`animation-worklet` (its result came back tagged with the previous test's URL, `secure-context.html`). 0/10 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (`tests/wpt/audio-output/`, 12 файлов: `META.yml`, `idlharness.https.window.js`, 9 `*.https(.sub).html`, `setSinkId-manual.https.html`), включена несмотря на скоуп 🚫 (медиа-конвейер) по тому же постоянному решению пользователя, что и `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`. Внекатегорийные хелперы `/common/get-host-info.sub.js`, `/common/gc.js`, `/permissions-policy/resources/permissions-policy.js` не довендорены, та же методология, что `FileAPI`/`ai`. `run_report.py --all --root audio-output --recursive` добежал за ~1 мин (10 отобранных id, `setSinkId-manual.https.html` исключён фильтром `-manual`): 5 SKIP (`Executor does not support testdriver.js`, тот же гэп, что `accname`/`attribution-reporting`); 3 `.https.`-only TIMEOUT (`invalid url: invalid port: "None"`, тот же HTTPS-порт-гэп, что `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`); 2 ERROR — `secure-context.html` (единственный не-`.https.` тест, реально прогнан по HTTP) упал на собственном допущении `assert_false(window.isSecureContext)`: спека считает `http://127.0.0.1` потенциально доверенным происхождением («localhost»), поэтому `isSecureContext` корректно возвращает `true`, как и в настоящих браузерах — не движковый баг; `selectAudioOutput-sans-user-activation.https.html` — тот же артефакт переиспользования сессии, что `accelerometer`/`ambient-light`/`animation-worklet` (результат пришёл с URL предыдущего теста `secure-context.html`). 0/10 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
