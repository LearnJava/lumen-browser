# WPT vendor notes — `mediacapture-region`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-mediacapture-region`, `docs/wpt-status.md`), included despite its 🚫 scope note (media capture — no pipeline) by the same standing user decision as `mediacapture-record`/`mediacapture-insertable-streams`/`mediacapture-image`/`mediacapture-handle`/`mediacapture-fromelement`. Same pinned commit, `git sparse-checkout add` at the same commit hash, 3 files (`WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `mediacapture-image`/`mediacapture-fromelement` category, the sole test `CropTarget-fromElement.https.html`) — the smallest category vendored in this backlog after `mediacapture-handle`. No `variant` hits, no out-of-category deps, no `testdriver.js` reference; the one file is `.https.`. `run_report.py --all --root mediacapture-region --recursive` completed in ~36s (1 selected id): **0/1 harness OK, 0/0 subtests**. TIMEOUT on the standard test-cert TLS gap (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, same class as `mediacapture-handle`/`mediacapture-insertable-streams`) — navigation to the `.https.` URL never completes. A `crates/js/src/*.rs` grep for `CropTarget`/`RestrictionTarget`/`cropTo`/`restrictTo` found zero matches — the whole Region Capture API is entirely absent, same class as `mediacapture-handle`/`mediacapture-insertable-streams`. No bug filed — first pass of an absent 🚫-scope API. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/mediacapture-region/`, 3 файла: `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `mediacapture-image`, единственный тест `CropTarget-fromElement.https.html`), включена несмотря на скоуп 🚫 по тому же постоянному решению пользователя, что `mediacapture-record`/`mediacapture-insertable-streams`/`mediacapture-handle`. Самая маленькая категория после `mediacapture-handle`: ни variant-ов, ни вне-категорийных зависимостей, ни `testdriver.js`; единственный файл `.https.`. Прогон `run_report.py --all --root mediacapture-region --recursive` (~36с): 1 отобранный id, **0/1 harness OK, 0/0 сабтестов**. TIMEOUT на стандартном TLS-гэпе тестового сертификата (`UnknownIssuer`, тот же класс, что `mediacapture-handle`/`mediacapture-insertable-streams`) — навигация на `.https.` URL не проходит. Живой грep `crates/js/src/*.rs` на `CropTarget`/`RestrictionTarget`/`cropTo`/`restrictTo` не нашёл ни одного совпадения — весь API Region Capture не реализован. Не заведён отдельный баг — первый проход отсутствующего 🚫-API
