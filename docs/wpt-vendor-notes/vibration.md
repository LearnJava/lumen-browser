# WPT vendor notes — `vibration`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (`tests/wpt/vibration/`, 16 файлов, 11 `-manual`), включена несмотря на скоуп 🚫 (аппаратный API вибро). Прогон `run_report.py --all --root vibration --recursive`: 4 отобранных id, **2/4 harness OK, 0/9 сабтестов**. `navigator.vibrate` отсутствует целиком (ожидаемо, не баг). `idlharness.window.html` — TIMEOUT (известный класс: `/resources/idlharness.js`+`WebIDLParser.js` не вендорены). `silent-ignore.html` — TIMEOUT объяснён живой пробой, не движковый баг: тест регистрирует `test()` только если `navigator.vibrate` определён, а `testharness.js`'s `Tests.prototype.all_done()` требует хотя бы один зарегистрированный тест, чтобы объявить страницу завершённой — та же семантика в любом браузере на устройстве без вибро; `window` `load`-событие и `setTimeout(…,0)` внутри его хендлера отработали штатно (проверено отдельно). `document.permissionsPolicy.features()` пуст — переподтверждение уже открытого [BUG-361](../../bugs/BUG-361-FIXED.md), новый номер не заводился
