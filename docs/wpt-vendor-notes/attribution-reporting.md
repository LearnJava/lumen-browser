# WPT vendor notes — `attribution-reporting`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-attribution-reporting`, `docs/wpt-status.md`), included despite its 🚫 scope note (ad-tech / Privacy Sandbox) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`. Same pinned commit, `git sparse-checkout` at the same commit hash, 13 files (`WEB_FEATURES.yml`, `resources/` — `helpers.js` + `wptserve` Python handlers, `aggregatable-debug/` — 2 test files). No out-of-category helpers. `run_report.py --all --root attribution-reporting --recursive` completed in ~5 min (24 selected ids, all from 7 `.sub.https.html` files — `referrer-policy.sub.https.html` and `request-format.sub.https.html` each expand into several query-string variants): 19 SKIP (`referrer-policy`/`request-format` variants — `Executor does not support testdriver.js`, same gap as `accname`); the remaining 5 (`aggregatable-debug/simple-source-…`, `aggregatable-debug/simple-trigger-…`, `header-parsing-error-debug-report`, `simple-verbose-debug-report`, `aggregatable-report-no-contributions`) are `.https.`-only and hit the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet` — 4 TIMEOUT (`invalid url: invalid port: "None"`), 1 ERROR (`eval: automation channel closed — no live window running`, the same manifestation of the missing HTTPS port as the `ambient-light`/`animation-worklet` session-reuse-looking errors). 0/24 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (`tests/wpt/attribution-reporting/`, 13 файлов: `WEB_FEATURES.yml`, `resources/` — `helpers.js` + Python-хендлеры `wptserve`, `aggregatable-debug/` — 2 тестовых файла, плюс `referrer-policy.sub.https.html`/`request-format.sub.https.html`/`aggregatable-report-no-contributions.sub.https.html`/`header-parsing-error-debug-report.sub.https.html`/`simple-verbose-debug-report.sub.https.html`), включена несмотря на скоуп 🚫 (ad-tech/Privacy Sandbox) по тому же постоянному решению пользователя, что и `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`. Внекатегорийных хелперов не обнаружено. `run_report.py --all --root attribution-reporting --recursive` добежал за ~5 мин (24 отобранных id, все из 7 `.sub.https.html`-файлов — `referrer-policy`/`request-format` раскрываются в несколько query-string вариантов): 19 SKIP (варианты `referrer-policy`/`request-format` — `Executor does not support testdriver.js`, тот же гэп, что `accname`); остальные 5 (`aggregatable-debug/simple-source-…`, `aggregatable-debug/simple-trigger-…`, `header-parsing-error-debug-report`, `simple-verbose-debug-report`, `aggregatable-report-no-contributions`) целиком `.https.`-only и упираются в тот же HTTPS-порт-гэп, что `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet` — 4 TIMEOUT (`invalid url: invalid port: "None"`), 1 ERROR (`eval: automation channel closed — no live window running`, то же проявление отсутствующего HTTPS-порта, что «session-reuse»-похожие ошибки в `ambient-light`/`animation-worklet`). 0/24 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
