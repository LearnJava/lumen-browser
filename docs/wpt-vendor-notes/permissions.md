# WPT vendor notes — `permissions`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/permissions/`, 18 файлов: `META.yml`, `WEB_FEATURES.yml`, `all-permissions.html`, `crashtests/` — 2 файла, `edge-cases.https.html`, `event-model.https.html`, `idlharness.any.js`, `midi-permission.html`, `non-fully-active.https.html`, `permissions-cg.https.html`, `permissions-garbage-collect.https.html`, `permissions-policy-permissions-query.html`, `permissions-query-permissions-policy-attribute.https.sub.html`, `permissionsstatus-name.html`, `revocation.https.html`, `worker.https.html`, `resources/`; `LICENSE-WPT.md` скопирован из соседней `performance-timeline`). `run_report.py --all --root permissions --recursive` (~1 мин 39 с, 14 отобранных id): **5/14 harness OK, 40/40 сабтестов**. `crashtests/permissions-query-worker.window.html` формально прошёл (1/1, crashtest проверяет только «не упало»), но лог вскрыл живой дефект: воркер бросает `ReferenceError: navigator is not defined` на первой же строке с `navigator.permissions.query(...)` — `worker_global_shim` (`crates/js/src/worker.rs:227-352`) не определяет `navigator` вовсе, любой `navigator.*` изнутри Worker недоступен в принципе, не только Permissions API. Заведён [BUG-649](../../bugs/BUG-649-OPEN.md). Остальные 9 id: 5 TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` (`event-model`/`permissions-cg`/`permissions-garbage-collect`/`permissions-query-permissions-policy-attribute`/`worker.https.html`), 3 ERROR — реконфирмация BUG-380 (browsing-context reuse после соседнего TIMEOUT возвращает результаты предыдущего теста: `edge-cases`/`non-fully-active`/`revocation.https.html`), 1 TIMEOUT на невендоренных общих ресурсах (`idlharness.any.html` — 404 на `/resources/idlharness.js`+`WebIDLParser.js`)
