# WPT vendor notes — `touch-events`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (пин `35be3b44`, `tests/wpt/touch-events/`, 17 файлов: `META.yml`/`WEB_FEATURES.yml`, `historical.html`, `idlharness.window.js`, `multi-touch-*`, `pinch-zoom-change.html`, `single-tap-*`, `single-touch*`, `touch-*`, `support/`). Прогон `run_report.py --all --root touch-events --recursive`, ~53 с, 15 отобранных id: **8/14 harness OK, 19/44 сабтестов**. Заведён [BUG-688](../bugs/BUG-688-OPEN.md): `Touch`/`TouchList`/`TouchEvent` глобальные конструкторы отсутствуют вовсе (`ReferenceError`), `GlobalEventHandlers` без `ontouchstart`/`ontouchmove`/`ontouchend`/`ontouchcancel` — прямая причина `historical.html` (2/8), `touch-touchevent-constructor.html` (0/5), половины `touch-globaleventhandler-interface.html` (1/3), `pinch-zoom-change.html` (0/1, `event.scale` не определён); ни один нативный код не диспатчит реальный `TouchEvent`, поэтому `single-touch*.html` TIMEOUT. Второстепенный сигнал — реконфирмация: 5 файлов `Browsing context for element was detached` ⇒ [BUG-622](../bugs/BUG-622-OPEN.md) (`document.defaultView` отсутствует); `multi-touch-touchmove.html` ⇒ документированный гэп `action_sequence` в `executorlumen.py`; `idlharness.window.html` ⇒ невендоренный `/resources/idlharness.js`+`WebIDLParser.js`. Один новый номер бага
