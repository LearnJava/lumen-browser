# WPT vendor notes — `measure-memory`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-measure-memory`, `docs/wpt-status.md`), scope ⬜ (candidate, `performance.measureMemory()` / Cross-Origin-Isolated Memory Measurement API). Same pinned commit, `git sparse-checkout add` at the same commit hash, 32 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `mathml` category, 16 top-level test files (13 `.https.`, including `service-worker.https.any.js`/`shared-worker.https.any.js`), `resources/common.js`). No `variant` hits, no `testdriver.js` hits. `run_report.py --all --root measure-memory --recursive --processes=4` completed in ~7:12 (14 selected ids — the worker-only globals `.any.worker`/`.any.sharedworker` aren't a testharness-compatible type for this runner): **0/14 harness OK, 0/0 subtests**. 13 of 14 (all `.https.`) TIMEOUT on the standard self-signed-cert TLS gap (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, same class as `keyboard-lock`/`keyboard-map`/`loading`/`magnetometer`/`managed`); the one non-`.https.` id (`idlharness.window.html`) never reported its own result — ERROR `Got results from /measure-memory/idlharness.window.html, expected /measure-memory/iframe.cross-site.https.window.html`, a re-confirmation of already-open [BUG-380](../bugs/BUG-380-OPEN.md) (reused browsing context returns the previous test's result). A `crates/js/src/dom.rs` grep for `measureMemory`/`MeasureMemory` found nothing — `performance.measureMemory()` is entirely unimplemented (the idlharness test itself only checks that one signature, `// Nothing to add`). No bug filed: same pattern as `keyboard-lock`/`keyboard-map`/`managed`, a first pass documenting an absent candidate feature. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/measure-memory/`, 32 файла: `META.yml`, `README.md`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `mathml`, 16 тестовых файлов верхнего уровня (13 `.https.`, включая `service-worker.https.any.js`/`shared-worker.https.any.js`), `resources/` — 1 хелпер `common.js`), скоуп ⬜ (кандидат, `performance.measureMemory()` / Cross-Origin-Isolated Memory Measurement API). Ни variant-ов, ни `testdriver.js`. `run_report.py --all --root measure-memory --recursive --processes=4` — ~7:12, 14 отобранных id (worker-глобали `.any.worker`/`.any.sharedworker` не отобраны — не testharness-совместимый тип для этого раннера): **0/14 harness OK, 0/0 сабтестов**. 13 из 14 (все `.https.`) TIMEOUT на стандартном TLS-гэпе тестового сертификата (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, тот же класс, что `keyboard-lock`/`keyboard-map`/`loading`/`magnetometer`/`managed`); единственный не-`.https.` тест (`idlharness.window.html`) не дошёл до собственного результата — ERROR `Got results from /measure-memory/idlharness.window.html, expected /measure-memory/iframe.cross-site.https.window.html`, переподтверждение уже открытого [BUG-380](../bugs/BUG-380-OPEN.md) (переиспользуемый browsing context отдаёт результаты предыдущего теста). Живой грep `crates/js/src/dom.rs` на `measureMemory`/`MeasureMemory` ничего не нашёл — `performance.measureMemory()` не реализован вовсе (сам `idlharness.window.html` целиком проверяет только эту сигнатуру, `// Nothing to add`). Новых багов не заведено — первый проход по отсутствующему кандидатному API, как `keyboard-lock`/`keyboard-map`/`managed`.
