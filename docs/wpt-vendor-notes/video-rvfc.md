# WPT vendor notes — `video-rvfc`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (`tests/wpt/video-rvfc/`, 8 файлов), включена несмотря на скоуп 🚫 (медиа-конвейер). Прогон `run_report.py --all --root video-rvfc --recursive`: 8 отобранных id, **2/8 harness OK, 0/4 сабтестов**. `HTMLVideoElement.prototype.requestVideoFrameCallback` отсутствует целиком (ожидаемо, не баг): `-parallel.html`/`-repeating.html` дают чистый `Test OK` harness + `FAIL` на каждом сабтесте с `TypeError: video.requestVideoFrameCallback is not a function`. `idlharness.window.html` — TIMEOUT (известный класс: `/resources/idlharness.js`+`WebIDLParser.js` не вендорены). `-dom.html`/`request-video-frame-callback.html` — TIMEOUT: оба вызывают `getVideoURI()` из невендоренного `/common/media.js` на верхнем уровне скрипта (не внутри `promise_test`), `ReferenceError` до регистрации теста → 0 тестов → `testharness.js`'s `Tests.prototype.all_done()` не завершается — тот же класс, что `silent-ignore.html` в `vibration`, не движковый баг. Три `.https.` файла — `ERROR` с `AssertionError: Got results from …` — переподтверждение уже открытого [BUG-380](../bugs/BUG-380-FIXED.md) (стейл-атрибуция результатов при провалившейся навигации на переиспользуемом browsing context). Новый номер бага не заводился
