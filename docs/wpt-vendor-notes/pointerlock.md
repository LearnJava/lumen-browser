# WPT vendor notes — `pointerlock`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/pointerlock/`, 30 файлов: `META.yml`, `WEB_FEATURES.yml`, 27 тестовых файлов (7 `-manual.html` не запускаются `run_report.py`-фильтром), `test-helper.js`; `LICENSE-WPT.md` скопирован из соседней `pointerevents`; один внекатегорийный хелпер `../shadow-dom/resources/shadow-dom.js` (использует `pointerlock_shadow.html`) НЕ вендорен). `run_report.py --all --root pointerlock --recursive` (~2 мин, 20 отобранных id): **10/20 harness OK, 2/22 сабтестов**. Заведён [BUG-655](../../bugs/BUG-655-OPEN.md): `requestPointerLock()` не проверяет transient user activation, блокировка выдаётся синхронно и безусловно — подтверждено обоими тестами (`pointerlock-requires-gesture.html`, `pointerlock_without_gesture.html`) и живой пробой `--mcp-live-port` (тот же класс пробела, что [BUG-365](../../bugs/BUG-365-FIXED.md)/[BUG-390](../../bugs/BUG-390-FIXED.md)). Остальной сигнал — реконфирмации [BUG-622](../../bugs/BUG-622-OPEN.md) (`document.defaultView` отсутствует, доминирует в половине FAIL как «Browsing context for element was detached»), BUG-462/574 (`Node.prototype.contains`), [BUG-596](../../bugs/BUG-596-OPEN.md) (`Event.prototype.initEvent` отсутствует, реконфирмировано живой пробой); 9 id TIMEOUT на `test_driver.Actions().pointerMove`/аналогичных действиях, которые `executors/executorlumen.py::_handle_action` не реализует (только `click`)
