# WPT vendor notes — `keyboard-map`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-keyboard-map`, `docs/wpt-status.md`), scope ⬜ (candidate, `navigator.keyboard.getLayoutMap()`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 13 files (`META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `keyboard-lock` category, `idlharness.https.window.js`, 7 `navigator-keyboard-map*.https.html`, `resources/iframe-keyboard-map-helper.html`+`iframe-keyboard-map-allow-helper.html`). No `variant` hits, no `testdriver.js`, all 8 selected ids `.https.`. `run_report.py --all --root keyboard-map --recursive` completed in ~2:22 (8 selected ids): **0/8 harness OK, 0/0 subtests**. All 8 TIMEOUT on the standard self-signed-cert TLS gap (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, same documented residual as `keyboard-lock`/`jpegxl`/`js-self-profiling`, `tests/wpt/certs/README.md`) — no `.sub.html`-style HTTP-500 variant, no BUG-380 contamination (single-shot TIMEOUTs, no cross-test bleed observed). A `crates/js/src/dom.rs` grep for `keyboard.getLayoutMap`/`KeyboardLayoutMap`/`navigator.keyboard` found nothing beyond the existing `KeyboardEvent` — the Keyboard Map API is entirely unimplemented. No bug filed: same pattern as `keyboard-lock`/`navigator.scheduling`/`Profiler`, a first pass documenting an absent candidate feature, not a defect in an existing one. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/keyboard-map/`, 13 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `keyboard-lock`, `idlharness.https.window.js`, 7 `navigator-keyboard-map*.https.html`, `resources/iframe-keyboard-map-helper.html`+`iframe-keyboard-map-allow-helper.html`), скоуп ⬜ (кандидат, `navigator.keyboard.getLayoutMap()`). `run_report.py --all --root keyboard-map --recursive` — ~2:22, 8 отобранных id (все `.https.`): **0/8 harness OK, 0/0 сабтестов**. Все 8 TIMEOUT на стандартном TLS-гэпе тестового сертификата (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, тот же документированный остаток, что `keyboard-lock`/`jpegxl`/`js-self-profiling`, `tests/wpt/certs/README.md`); без `.sub.html`, без наблюдаемой контаминации BUG-380. Грep `crates/js/src/dom.rs` на `keyboard.getLayoutMap`/`KeyboardLayoutMap`/`navigator.keyboard` ничего не нашёл кроме существующего `KeyboardEvent` — Keyboard Map API не реализован вовсе. Новых багов не заведено — как `keyboard-lock`/`navigator.scheduling`/`Profiler`, первый проход по отсутствующему кандидатному API.
