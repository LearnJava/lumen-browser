# WPT vendor notes — `secure-contexts`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/secure-contexts/`, 21 файл: `basic-dedicated-worker(.https).html`, `basic-popup-and-iframe-tests(.https).html(.js)`, `basic-shared-worker(.https).html`, `shared-worker-{insecure,secure}-first.https.html`, `postMessage-helper(.https).html`, `server-locations.sub.js`, `support/`). Прогон `run_report.py --all --root secure-contexts --recursive` (~1:49, 8 отобранных id): 0/8 harness OK, 0/12 сабтестов. Три не-`.https.` файла TIMEOUT (воркер никогда не отвечает); их `.https.` пары ERROR `Got results from <insecure>, expected <https>` — реконфирмация [BUG-380](../bugs/BUG-380-FIXED.md); оба `shared-worker-*-first.https.html` TIMEOUT напрямую. Каждый тест создаёт `Worker`/`SharedWorker` по внешнему `support/*.js` URL — блокируется [BUG-364](../bugs/BUG-364-FIXED.md) (внешний URL не фетчится, воркер немой). Живая проба (`--mcp-live-port`) на `data:` URL воркере (не подпадает под BUG-364) обогатила [BUG-591](../bugs/BUG-591-FIXED.md) новым конкретным механизмом: `Worker`/`SharedWorker` не имеет `onerror`-аксессора на родительской стороне вовсе (`crates/js/src/worker.rs:502-521` — только `onmessage`), `addEventListener('error', …)` — молчаливый no-op (фильтр по `type === 'message'`), а `run_worker_thread_v8` (`worker.rs:699-702`) при ошибке top-level скрипта воркера только `eprintln!`-ит в stderr хоста, не сообщая родителю — воспроизведено живьём `throw`/голым `ReferenceError` (`postMessage(isSecureContext)`, буквальный `w7`-сабтест из `basic-dedicated-worker.html`). Новых номеров багов не заведено
