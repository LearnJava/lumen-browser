# WPT vendor notes — `annotation-vocab`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-vocab`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 10 files (6 `NN-*-manual.html`, `META.yml`, `tools/anno.jsonld`, `tools/README.md`, `tools/vocab_tester.py`). No out-of-category helpers — all 6 HTML reference only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. Entirely manual by spec design like `annotation-model`/`annotation-protocol` — all 6 test files are `-manual.html`, excluded by `run_report.py`'s own filter, and no `.any.js`/`.window.js` exist either (`tools/vocab_tester.py` is a Python reference implementation, not a test); `run_report.py --all --root annotation-vocab --recursive` selected 0 tests (`no tests selected`), the same silent gap as `annotation-model` rather than the manifest crash seen in `annotation-protocol`. Third vendored category with zero automatically runnable tests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-23 (`tests/wpt/annotation-vocab/`, 10 файлов: 6 `NN-*-manual.html`, `META.yml`, `tools/anno.jsonld`, `tools/README.md`, `tools/vocab_tester.py`). Все 6 HTML ссылаются только на уже вендоренные `/resources/testharness.js`+`testharnessreport.js` — внекатегорийных хелперов нет. Категория целиком ручная по дизайну спеки, как `annotation-model`/`annotation-protocol` — все 6 тестовых файлов носят суффикс `-manual.html`, исключаются фильтром `run_report.py`, `.any.js`/`.window.js` нет вовсе, `tools/vocab_tester.py` не тест. `run_report.py --all --root annotation-vocab --recursive` → `no tests selected`, 0/0, тот же тихий гэп, что `annotation-model` (не краш манифеста, как у `annotation-protocol`). Третья вендоренная категория без единого автоматически прогоняемого теста. Не заводился отдельный BUG-NNN — не движковый баг, а изначально задуманный тип тестирования спеки
