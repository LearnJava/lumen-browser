# WPT vendor notes — `digital-credentials`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-digital-credentials`, `docs/wpt-status.md`), included despite its 🚫 scope note (Digital Credentials API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`device-posture`. Same pinned commit, `git sparse-checkout` at the same commit hash, 32 files (`META.yml`, `dc-types.ts`, `tsconfig.json`, 21 `.https(.sub)?.html`/`.http.html` test files, `historical.any.js`, `permissions-policy/` — 2 iframe fixture pages loaded by other tests, not standalone tests, `support/` — `helper.js`+`iframe.html`, `webdriver/` — 3 test files). Two out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root digital-credentials --recursive` selected 23 ids (`permissions-policy/*.html` and `support/*` correctly excluded by the manifest — not tests) and completed fully (~1:08): 19 `.https.` tests SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`delegated-ink`/`device-posture`); 2 TIMEOUT on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`, same class as `WebCryptoAPI`/`ai`/`device-memory`); `historical.any.html` harness OK (1/1 subtest passed); `non-secure-contexts.http.html` harness OK but 0/1 subtest FAIL — the test expects `window.isSecureContext === false` over plain HTTP, but the spec treats `http://127.0.0.1` as a potentially-trustworthy origin (the "localhost" exception), so Lumen correctly returns `true`, same pattern as `audio-output`'s `secure-context.html`. 2/23 harness OK, 1/2 subtests passed. Not filed as a separate BUG-NNN — both known gaps plus one test-assumption mismatch, no new engine defect. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/digital-credentials/`, 32 файла: `META.yml`, `dc-types.ts`, `tsconfig.json`, 21 `.https(.sub)?.html`/`.http.html` тестов, `historical.any.js`, `permissions-policy/` — 2 iframe-хелпера (не тесты), `support/` — `helper.js`+`iframe.html`, `webdriver/` — 3 теста), скоуп 🚫 (Digital Credentials API), включена несмотря на скоуп по постоянному решению пользователя, как `accelerometer`/`acid`/`ai`/…/`device-posture`. Два внекатегорийных хелпера не довендорены: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, тот же гэп, что `FileAPI`/`audio-session`. `run_report.py --all --root digital-credentials --recursive` отобрал 23 id (`permissions-policy/*.html` и `support/*` манифестом не выбраны — не тесты) и прошёл полностью (~1:08): 19 `.https.`-тестов SKIP (`Executor does not support testdriver.js`, тот же гэп, что `accname`/`delegated-ink`/`device-posture`); 2 TIMEOUT на уже известный HTTPS-порт-гэп (`invalid url: invalid port: "None"`, тот же класс, что `WebCryptoAPI`/`ai`/`device-memory`); `historical.any.html` harness OK (1/1 сабтест passed); `non-secure-contexts.http.html` harness OK, но 0/1 сабтест FAIL — тест ожидает `window.isSecureContext === false` по обычному HTTP, а спека трактует `http://127.0.0.1` как потенциально доверенное происхождение («localhost»-исключение), так что Lumen корректно возвращает `true`, тот же паттерн, что `secure-context.html` у `audio-output`. 2/23 harness OK, 1/2 сабтестов passed. Отдельный BUG-NNN не заводился — оба гэпа уже известны, третий результат — расхождение с ожиданием самого теста, не движковый дефект
