# WPT vendor notes — `mediacapture-handle`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-mediacapture-handle`, `docs/wpt-status.md`), included despite its 🚫 scope note (media capture — no pipeline) by the same standing user decision as `mediacapture-fromelement`/`mediacapture-extensions`/`media-capabilities`/`media-playback-quality`/`media-source`. Same pinned commit, `git sparse-checkout add` at the same commit hash, 2 files (`LICENSE-WPT.md` copied from the sibling `mediacapture-fromelement` category, `identity/MediaDevices-setCaptureHandleConfig.https.window.js`) — the smallest category vendored in this backlog so far. No `variant` hits, no out-of-category deps (the sole test only references the already-vendored `testharness.js`+`testharnessreport.js`); the one file is `.https.`. `run_report.py --all --root mediacapture-handle --recursive` completed in ~33s (1 selected id): **0/1 harness OK, 0/0 subtests**. TIMEOUT on the standard test-cert TLS gap (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, same class as `mediacapture-extensions`/`keyboard-lock`/`magnetometer`/`managed`) — navigation to the `.https.` URL never completes. A `crates/js/src/media_devices.rs` grep confirmed `navigator.mediaDevices.setCaptureHandleConfig` is entirely absent — the only near-name hit was an unrelated internal field (`_captureHandleId`, native audio-capture bookkeeping, no relation to the Capture Handle Identity API). No bug filed — first pass of an absent 🚫-scope API, same class as `mediacapture-fromelement`/`media-source`. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/mediacapture-handle/`, 2 файла: `LICENSE-WPT.md` скопирован из соседней `mediacapture-fromelement`, `identity/MediaDevices-setCaptureHandleConfig.https.window.js`), включена несмотря на скоуп 🚫 по тому же постоянному решению пользователя, что `mediacapture-fromelement`/`mediacapture-extensions`/`media-source`. Самая маленькая категория в этом бэклоге: ни variant-ов, ни вне-категорийных зависимостей, единственный файл `.https.`. Прогон `run_report.py --all --root mediacapture-handle --recursive` (~33с): 1 отобранный id, **0/1 harness OK, 0/0 сабтестов**. TIMEOUT на стандартном TLS-гэпе тестового сертификата (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, тот же класс, что `mediacapture-extensions`/`keyboard-lock`/`magnetometer`/`managed`) — навигация на `.https.` URL не проходит. Живой грep `crates/js/src/media_devices.rs` подтвердил `navigator.mediaDevices.setCaptureHandleConfig` целиком отсутствует — единственное похожее по имени совпадение (`_captureHandleId`) не связано, это внутреннее поле нативного аудио-захвата. Не заведён отдельный баг — первый проход отсутствующего 🚫-API, тот же класс, что `mediacapture-fromelement`/`media-source`
