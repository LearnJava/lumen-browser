# WPT vendor notes — `autoplay-policy-detection`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-autoplay-policy-detection`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`. Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `autoplaypolicy.html`, `autoplaypolicy_media_element.html`, `idlharness.window.js`). Two out-of-category dependencies not vendored: `autoplaypolicy_media_element.html` references `/common/media.js`, `idlharness.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root autoplay-policy-detection --recursive` completed in ~13s (3 selected ids, none `.https.` — the first category in this backlog run with no HTTPS-port gap): `autoplaypolicy.html` ran (harness OK) but all 3 subtests FAIL — `window.navigator.getAutoplayPolicy is not a function` (the Autoplay Policy Detection API itself isn't implemented in Lumen, expected on a 🚫-scope category's first pass); `autoplaypolicy_media_element.html` ran (harness OK) but both subtests FAIL (`getVideoURI is not defined`, 404 on the un-vendored `/common/media.js`); `idlharness.window.js` actually ran over plain HTTP but 404'd on its two out-of-category dependencies (`idl_test is not defined`), so the harness never reported — TIMEOUT. 2/3 harness OK, 0/5 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (`tests/wpt/autoplay-policy-detection/`, 4 файла: `META.yml`, `autoplaypolicy.html`, `autoplaypolicy_media_element.html`, `idlharness.window.js`), включена несмотря на скоуп 🚫 (медиа-конвейер) по тому же постоянному решению пользователя, что и `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`. Два внекатегорийных хелпера не довендорены: `autoplaypolicy_media_element.html` ссылается на `/common/media.js`, `idlharness.window.js` — на `/resources/WebIDLParser.js`+`/resources/idlharness.js`, та же методология, что `FileAPI`/`audio-session`. `run_report.py --all --root autoplay-policy-detection --recursive` добежал за ~13с (3 отобранных id, все без `.https.` — первая категория из этой серии без HTTPS-порт-гэпа): `autoplaypolicy.html` — харнесс OK, но 0/3 сабтестов FAIL (`window.navigator.getAutoplayPolicy is not a function` — сама Autoplay Policy Detection API не реализована в Lumen, ожидаемо для 🚫-категории при первом проходе); `autoplaypolicy_media_element.html` — харнесс OK, 0/2 сабтестов FAIL (`getVideoURI is not defined` из-за 404 на невендоренный `/common/media.js`); `idlharness.window.js` реально прогнан по HTTP, но `idl_test is not defined` из-за 404 на невендоренные хелперы — харнесс не отрапортовал, TIMEOUT. 2/3 harness OK, 0/5 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет, `getAutoplayPolicy` вне скоупа
