# WPT vendor notes — `images`

## Vendoring (`tests/wpt/VENDOR.md`)

Shared fixture images (`WPT-RUN-3` slice 22, `ROADMAP.md`) — 84 files, 400K, referenced via `../../images/*.{gif,png,jpg}` by out-of-category tests across `css/` (found via `css/css-image-animation`'s `image-animation-pseudo-animated-image-dynamic.html`, but the same 404 gap is documented un-vendored in `apng`/`avif` rows above — vendoring once here removes it for every future category that shares the fixture instead of re-discovering it per category). No further out-of-category dependencies of its own. Does **not** fix the reference in `image-animation-pseudo-animated-image-dynamic.html` itself — that path is `../../images/anim-gr.gif` (two dot-segments), which still 404s under [BUG-346](../../bugs/BUG-346-OPEN.md) (`Url::resolve()` doesn't collapse `..`); confirmed the vendored file *is* correctly served for single-dot-segment references. `WPT-VENDOR-images` (`ROADMAP.md`) formally closed 2026-08-04: re-checked against the pinned upstream clone (`35be3b44`) and confirmed the file set is complete and unchanged — the category has zero of its own test files (`.html`/`.js`), `run_report.py --all --root images --recursive` returns `no tests selected` instantly.

## Прогон и находки (`docs/wpt-status.md`)

Уже вендорена целиком раньше — как разделяемая фикстур-директория для `WPT-RUN-3` slice 22 (`tests/wpt/images/`, 84 файла + `LICENSE-WPT.md`), задача `WPT-VENDOR-images` из бэклога переоткрыта 2026-08-04 только формально закрыта: сверка `diff` upstream-клона (пин `35be3b44`) против локальной директории — файловые наборы совпадают побайтово по именам, ничего не довендорено. Категория состоит из 90 растровых/векторных фикстур (`.png`/`.svg`/`.gif`/`.jpg`/`.avif`) + одного `wptserve`-хендлера (`blue-png-cachable.py`) + `README.md`/`META.yml` — **ни одного собственного тестового файла** (`.html`/`.js`), `grep -c '"images/' tests/wpt/metadata/MANIFEST.json` = 0, `run_report.py --all --root images --recursive` завершается мгновенно `no tests selected` ещё до печати числа id-шников (более жёсткий случай, чем `annotation-model`, где хотя бы 0 из manual-суффиксных HTML отбирались). Новых багов не заведено — не движковый дефект, категория по дизайну не содержит исполняемых тестов, это shared-фикстура для других категорий (`css/css-image-animation`, `apng`, `avif`, …).
