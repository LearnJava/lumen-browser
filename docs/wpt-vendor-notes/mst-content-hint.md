# WPT vendor notes — `mst-content-hint`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-mst-content-hint`, `docs/wpt-status.md`), included despite its 🚫 scope note (WebRTC — no pipeline) by the same standing user decision as the rest of the backlog. Same pinned commit, `git sparse-checkout add` at the same commit hash, 5 files (`META.yml`, `LICENSE-WPT.md` copied from the sibling `mixed-content` category, 3 root test files: `MediaStreamTrack-contentHint.html`, `RTCRtpSendParameters-degradationEffect.html`, `RTCRtpSendParameters-degradationPreference.html`, `idlharness.window.js`). No `variant` hits, no `.https.` files, no `testdriver.js` references — the cheapest predictor profile possible; 4 selectable ids. `run_report.py --all --root mst-content-hint --recursive` completed in ~35s: **3/4 harness OK, 0/14 subtests passed**. `MediaStreamTrack-contentHint.html` — all 7 subtests FAIL before reaching the `contentHint` assertion itself: `AudioContext.createMediaStreamDestination().stream` is a stub object with no `getAudioTracks`/`getVideoTracks` (only `getTracks()`, always `[]`), and `canvas.captureStream` is entirely absent (same class as `mediacapture-fromelement`/`mediacapture-image`/`mediacapture-record`) — both are already documented Phase 0 gaps (Web Audio "graph only, no DSP" in `CAPABILITIES.md`; captureStream is the known `mediacapture-*` backlog gap), no new finding. `RTCRtpSendParameters-degradationEffect.html` — `ReferenceError: getNoiseStream is not defined`: pulls the out-of-category `/webrtc/RTCPeerConnection-helper.js`, not vendored. `RTCRtpSendParameters-degradationPreference.html` — all 6 subtests FAIL: `pc.addTransceiver(...)` returns `null` instead of an `RTCRtpTransceiver` — consistent with the 🚫 scope (WebRTC unimplemented, `CAPABILITIES.md`: "⬜ WebRTC (mDNS-only stub, no IP leak)"). `idlharness.window.html` TIMEOUT — the same `getNoiseStream` helper inside `idl_test`. No bug filed — every finding reconfirms an already-documented Phase 0/🚫 gap (Web Audio, captureStream, WebRTC), not a new one. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/mst-content-hint/`, 5 файлов: `META.yml`, `LICENSE-WPT.md` скопирован из соседней `mixed-content`, 3 корневых тестовых файла: `MediaStreamTrack-contentHint.html`, `RTCRtpSendParameters-degradationEffect.html`, `RTCRtpSendParameters-degradationPreference.html`, `idlharness.window.js`), включена несмотря на скоуп 🚫 (WebRTC, нет конвейера). Ни variant-ов, ни `.https.`-файлов, ни `testdriver.js` — самый дешёвый профиль; 4 отобранных id. Прогон `run_report.py --all --root mst-content-hint --recursive` (~35с): **3/4 harness OK, 0/14 сабтестов passed**. `MediaStreamTrack-contentHint.html` — все 7 сабтестов FAIL до проверки самого `contentHint`: `AudioContext.createMediaStreamDestination().stream` — заглушечный объект без `getAudioTracks`/`getVideoTracks` (только `getTracks()`, всегда `[]`), а `canvas.captureStream` целиком отсутствует (тот же класс, что `mediacapture-fromelement`/`mediacapture-image`/`mediacapture-record`) — оба уже задокументированы как Phase 0 (Web Audio «graph only, no DSP» в `CAPABILITIES.md`; captureStream — известный пробел бэклога `mediacapture-*`), новых находок нет. `RTCRtpSendParameters-degradationEffect.html` — `ReferenceError: getNoiseStream is not defined`: тянет вне-категорийный `/webrtc/RTCPeerConnection-helper.js`, не вендорен. `RTCRtpSendParameters-degradationPreference.html` — все 6 сабтестов FAIL: `pc.addTransceiver(...)` возвращает `null` вместо `RTCRtpTransceiver` — согласуется со скоупом 🚫 (WebRTC не реализован, `CAPABILITIES.md`: «⬜ WebRTC (mDNS-only stub, no IP leak)»). `idlharness.window.html` TIMEOUT — тот же хелпер `getNoiseStream` внутри `idl_test`. Не заведён отдельный баг — все находки переподтверждают уже задокументированные Phase 0/🚫-гэпы (Web Audio, captureStream, WebRTC), не новая находка
