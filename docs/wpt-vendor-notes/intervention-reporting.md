# WPT vendor notes — `intervention-reporting`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-intervention-reporting`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 2 files (`META.yml`, one `idlharness.any.js`; `LICENSE-WPT.md` copied from the sibling `idle-detection` category). Not `.https.`, no `variant` hits, no `testdriver.js`. `run_report.py --all --root intervention-reporting --recursive` completed in ~32 s (1 selected id): **0/1 harness OK**. `idlharness.any.html` TIMEOUT — same un-vendored `/resources/WebIDLParser.js`+`/resources/idlharness.js` gap as `input-device-capabilities`/`installedapp`; the upstream test itself is also an empty stub (`idl_array.add_objects({ // TODO: objects })`), so even a hypothetical vendored idlharness would assert nothing concrete. Per the WPT-VENDOR "probe one level up" convention, a `--mcp-live-port` live probe targeted the Reporting API the category exercises (`crates/js/src/reporting_api.rs`, shared with the in-scope `reporting` category) and found a new bug: [BUG-629](../../bugs/BUG-629-OPEN.md) (`ReportingObserver`/`Report` are plain ES5 constructor functions with no `new.target` guard — calling either without `new` silently succeeds and leaks the instance's fields onto `globalThis` as own properties, e.g. `Report("t","u",null)` sets `globalThis.type`/`globalThis.url`; `Report` is additionally constructible **with** `new` at all, though the spec defines no constructor for it, letting page script forge fake `Report` objects indistinguishable via `instanceof`; `ReportingObserver.prototype`'s methods, including the underscore-prefixed internal `_accepts`, are enumerable where WebIDL requires non-enumerable). See `docs/wpt-status.md` for details.
