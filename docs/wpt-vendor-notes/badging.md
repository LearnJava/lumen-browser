# WPT vendor notes — `badging`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-badging`, `docs/wpt-status.md`), included despite its 🚫 scope note (PWA / OS integration) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`. Same pinned commit, `git sparse-checkout` at the same commit hash, 9 files (`META.yml`, `WEB_FEATURES.yml`, `badge-error.https.any.js`, `badge-success.https.any.js`, `idlharness.https.any.js`, `non-fully-active.https.html`, `setAppBadge_cross_origin.sub.https.html`, `resources/badge_iframe.html`, `resources/message-handler.js`). The whole category is `.https.`-only (all 5 selected test ids), same class as `background-sync`/`background-fetch`/`WebCryptoAPI`/`ai` — `run_report.py --all --root badging --recursive` completed in ~2:36: all 5 ids TIMEOUT with the same HTTPS-port gap (`invalid url: invalid port: "None"`, the minimal executor serves no HTTPS port). 0/5 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (коммит `35be3b44`, `tests/wpt/badging/`, 9 файлов: `META.yml`, `WEB_FEATURES.yml`, `badge-error.https.any.js`, `badge-success.https.any.js`, `idlharness.https.any.js`, `non-fully-active.https.html`, `setAppBadge_cross_origin.sub.https.html`, `resources/badge_iframe.html`, `resources/message-handler.js`), включена несмотря на скоуп 🚫 (PWA/ОС-интеграция) по тому же постоянному решению пользователя, что и `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`. Вся категория `.https.`-only (все 5 отобранных id: `badge-error.https.any.html`, `badge-success.https.any.html`, `idlharness.https.any.html`, `non-fully-active.https.html`, `setAppBadge_cross_origin.sub.https.html`), тот же класс, что `background-sync`/`background-fetch`/`WebCryptoAPI`/`ai`. `run_report.py --all --root badging --recursive` добежал за ~2:36: все 5 — TIMEOUT (`invalid url: invalid port: "None"`, тот же HTTPS-порт-гэп минимального исполнителя). 0/5 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
