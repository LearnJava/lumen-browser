# WPT vendor notes — `client-hints`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-client-hints`, `docs/wpt-status.md`), in-scope (⬜, Client Hints Infrastructure — WICG spec). Same pinned commit, `git sparse-checkout` at the same commit hash, 276 files (`META.yml`, `accept-ch`/`accept-ch-stickiness`/`clear-site-data`/`critical-ch`/`inner-delegation`/`permissions-policy`/`sandbox`/`service-workers`/`viewport-size` subcategories + root-level tests + `resources/`). Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/common/utils.js`, same documented survey gap as `clear-site-data`/`FileAPI`. Manifest selects 133 ids: 127 `.https.`-only + 6 non-https — a large category, comparable in order of magnitude to `WebCryptoAPI` (125) — `run_report.py --all --root client-hints --recursive` did not complete: a bounded trial run (~11 min, stopped manually) processed 17 of the 133 ids: 15 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`), 1 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`captured-mouse-events`/`clear-site-data`); the one non-https test that ran, `accept-ch/non-secure.http.html`, had harness OK but 0/1 subtest FAIL (`fetch error: invalid url: missing scheme` for a relative URL, `resources/echo-client-hints-received.py` — the same relative-`fetch()`-resolution engine gap seen in `browsing-topics`' `fetch-topics-insecure-context` test). 1/17 harness OK, 0/1 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (коммит `35be3b44`, `tests/wpt/client-hints/`, 276 файлов: `META.yml`, подкатегории `accept-ch`/`accept-ch-stickiness`/`clear-site-data`/`critical-ch`/`inner-delegation`/`permissions-policy`/`sandbox`/`service-workers`/`viewport-size` + корневые тесты + `resources/`). Внекатегорийные хелперы не довендорены: `/common/get-host-info.sub.js`, `/common/utils.js`, та же методология, что `clear-site-data`/`FileAPI`. `run_report.py --all --root client-hints --recursive` не добежал полностью (133 отобранных id: 127 `.https.`-only + 6 не-https, крупная категория сопоставимого порядка с `WebCryptoAPI` 125) — пробный прогон (~11 мин, остановлен вручную) обработал 17 id: 15 TIMEOUT (тот же HTTPS-порт-гэп, что `WebCryptoAPI`/`ai`/`bluetooth`), 1 ERROR (session/URL-reuse артефакт, тот же класс, что `accelerometer`/`beacon`/`captured-mouse-events`/`clear-site-data`); единственный прогнанный не-https тест `accept-ch/non-secure.http.html` — harness OK, 0/1 сабтест FAIL (`fetch error: invalid url: missing scheme` на относительный URL `resources/echo-client-hints-received.py`, тот же движковый гэп с относительным `fetch()`, что и `fetch-topics-insecure-context` в `browsing-topics`). 1/17 harness OK, 0/1 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
