# WPT vendor notes — `private-aggregation`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/private-aggregation/`, 17 файлов: 3 `private-aggregation-permissions-policy-*.https.sub.html` + 2 `.headers`, 6 `shared-storage-surface-*.https.html`, `resources/` — 5 файлов (`util.js`, `reports.py`, `protected_audience_event_level_report_handler.py`, `shared-storage-helper-module.js`+`.headers`); `LICENSE-WPT.md` скопирован из соседней `print`), скоуп 🚫 (ad-tech, Privacy Sandbox), включена несмотря на скоуп по постоянному решению пользователя, как остальные WPT-VENDOR-категории. Внекатегорийные хелперы `/common/utils.js` и `/shared-storage/resources/util.js` (категория `shared-storage` сама не вендорена) НЕ довендорены — документированный пробел, не баг; `/fenced-frame/resources/utils.js` уже вендорен (категория `fenced-frame`, 2026-07-28). `run_report.py --all --root private-aggregation --recursive` (~2 мин 38 с, 9 отобранных id, все `.https.`): **0/9 harness OK**. Все 9 id TIMEOUT на стандартном TLS-гэпе `UnknownIssuer` до JS — тот же класс, что `presentation-api`/`pointerlock`/`preload`. Все тесты категории вызывают `VerifyContributeToHistogram()` (`resources/util.js`), которая опирается на `privateAggregation.contributeToHistogram()` внутри Shared Storage worklet-операции — `grep -rli "privateAggregation\|contributeToHistogram" crates/` не даёт ни одного совпадения: сам Private Aggregation API не реализован вовсе. `window.sharedStorage` (хост-страничная часть, НЕ worklet) реализован Phase 0-заглушкой (`crates/js/src/shared_storage.rs`) — `run()`/`worklet.addModule()` уже задокументированы в исходнике как no-op, ничего нового не найдено при чтении. Раз API целиком отсутствует по дизайну категории (скоуп 🚫), бага не заведено
