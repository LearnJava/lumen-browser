# WPT vendor notes — `png`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/png/`, 11 файлов: `META.yml`, `WEB_FEATURES.yml`, `cICP-wins.html`/`cICP-wins-ref.html`, `cicp-chunk.html`, `exif-chunk.html`, `trns-chunk.html`, `apng/` (manual+resources+support), `errors/` (support), `support/`; `LICENSE-WPT.md` скопирован из соседней `picture-in-picture`). `run_report.py --all --root png --recursive` (~56 с, 35 отобранных id — 32 reftest не поддерживаются `run_report.py`, "Unsupported test type reftest", + 3 testharness): **0/3 harness OK, 0/3 сабтестов**. Все три исполнившихся теста (`cicp-chunk`/`exif-chunk`/`trns-chunk`, каждый ждёт `img.onload` перед отложенным canvas-тестом на пиксели) TIMEOUT по общей причине, не связанной с PNG-чанками: живая проба (`--mcp-live-port`) с контрольным PNG без экзотических чанков (и обычным, и добавленным в DOM через `appendChild`) показала, что `onload`/`onerror` не срабатывают вовсе ни для одного `new Image()` — переподтверждение уже заведённого [BUG-630](../bugs/BUG-630-OPEN.md) (`<img>` не диспатчит `load`/`error` в принципе, для любого формата). Новых багов не заведено
