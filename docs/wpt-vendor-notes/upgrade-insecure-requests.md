# WPT vendor notes — `upgrade-insecure-requests`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/upgrade-insecure-requests/`, 254 файла: `META.yml`/`README.md`/`WEB_FEATURES.yml`, `gen/` (196 сгенерированных `.https.html`, зависят от невендоренного `/common/security-features/resources/common.sub.js`), `link-upgrade/`, `link-upgrade.sub.https.html`, `generic/test-case.sub.js`, `support/redirect-cors.py`). Прогон `run_report.py --all --root upgrade-insecure-requests --recursive --processes=4`, ~64 мин, 197 отобранных id: **0/197 harness OK**, все TIMEOUT — каждый файл категории сам `.https.html`, TLS-хендшейк рвётся на документированном гэпе `UnknownIssuer` (`docs/wpt-status.md:26`) до загрузки страницы, весь сигнал прогона — 100% реконфирмация, не новая находка (невендоренный `/common/` в данном прогоне ни на что не повлиял — TLS рвётся раньше). Заведён [BUG-692](../bugs/BUG-692-OPEN.md) прямой пробой источника (grep контейнера `CspPolicy` по всему `crates/`, не прогоном): директива `upgrade-insecure-requests` парсится в `CspPolicy::upgrade_insecure_requests`, но поле нигде не читается — ни один `http://`-подресурс никогда не переписывается в `https://`; отдельный mixed-content классификатор (`lumen-network::mixed_content`) только блокирует, апгрейда не делает. Один новый номер бага
