# WPT vendor notes — `hr-time`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-hr-time`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 19 files (`META.yml`, `WEB_FEATURES.yml`, 13 top-level test files, `resources/` — 8 files, `LICENSE-WPT.md`). Fully self-contained: zero `variant`/`testdriver.js` hits, only out-of-category `src=` refs are the already-vendored `/resources/testharness(report).js`. `run_report.py --all --root hr-time --recursive` completed in 1 min 37 s: 4/13 harness OK, 8/16 subtests passed (2 ERROR — session-reuse artifact of the class BUG-380; 1 TIMEOUT — un-vendored `/resources/idlharness.js`, known infra gap; 1 `.https.` TIMEOUT — ordinary HTTPS-port gap) — most ids actually ran, well above this backlog's recent average. Two new bugs from real FAIL/TIMEOUT subtests: BUG-400 (`performance` doesn't extend `EventTarget`, no `toJSON()`) and BUG-401 (`performance` entirely missing from Worker global scope). Two remaining TIMEOUTs are not a new finding — both hit the already-documented `<iframe>` without a browsing context limit. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-28 (коммит `35be3b44`, `tests/wpt/hr-time/`, 19 файлов: `META.yml`, `WEB_FEATURES.yml`, 13 тестовых файлов верхнего уровня (3 `.https.` + 2 `.https.*.headers`), `resources/` из 8 файлов, `LICENSE-WPT.md`), скоуп ⬜ (кандидат). Самодостаточная категория: ни `variant`, ни `testdriver.js` не встречаются вовсе (0 файлов), единственный внекатегорийный `src=` — вендоренные `/resources/testharness(report).js`. `run_report.py --all --root hr-time --recursive` — **1 мин 37 с, 4/13 harness OK, 8/16 сабтестов** (2 ERROR — session-reuse артефакт класса [BUG-380](../bugs/BUG-380-FIXED.md) на двух `.https.` файлах сразу за исполнившимся тестом; 1 TIMEOUT — `idlharness.any.html` на невендоренном `/resources/idlharness.js`, известный инфраструктурный пробел; 1 `.https.` TIMEOUT — обычный HTTPS-порт-гэп). Заметно выше сигнала большинства недавних категорий backlog-а — почти все 13 id реально исполнились. Два новых бага из трёх реально FAIL/TIMEOUT-подтестов: [BUG-400](../bugs/BUG-400-FIXED.md) — `performance` (`dom.rs:10987`, плоский объектный литерал) не наследует `EventTarget` (`basic.any.js`: «Performance interface extends EventTarget» — `addEventListener is not a function`) и не имеет `toJSON()` (`performance-tojson.html`), оба явно требуются W3C HR Time L3 §4. [BUG-401](../bugs/BUG-401-OPEN.md) — `performance` полностью отсутствует в Worker global scope (`worker.rs::worker_global_shim` не заводит его вовсе) — 3 файла (`clamped-time-origin.html`, `timeOrigin.html`, `window-worker-timeOrigin.window.html`) падают `ReferenceError`/TIMEOUT ровно на этом, хотя спека помечает интерфейс `[Exposed=(Window,Worker)]`. Два оставшихся TIMEOUT (`test_cross_frame_start.html`, `navigation-start-post-before-unload.html`) не дают новой находки — оба используют `iframe.contentWindow`/ждут `iframe`'s `load`-событие и упираются в уже задокументированный (без отдельного номера в `BUGS.md`, но зафиксированный в `CAPABILITIES.md` строке DOM) Phase-0 предел «`<iframe>` без browsing context», тот же класс, что `focus`/`gamepad`/`fullscreen`.
