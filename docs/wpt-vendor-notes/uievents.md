# WPT vendor notes — `uievents`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/uievents/`, 146 файлов: `META.yml`/`WEB_FEATURES.yml`, `click/`, `constructors/`, `hierarchy/`, `interface/`, `keyboard/`, `legacy-domevents-tests/`, `order-of-events/`, `textInput/`, `resources/`, `historical.html`, `idlharness.window.js`, `ui_event_pseudo_target.html`). Прогон `run_report.py --all --root uievents --recursive`, ~6:41, 70 отобранных id: **33/70 harness OK, 17/127 сабтестов**. Заведён [BUG-691](../bugs/BUG-691-OPEN.md): `window.TextEvent` (легаси DOM3 Events, всё ещё в UI Events §4.4) отсутствует вовсе (`ReferenceError` вместо ожидаемого `TypeError`), `UIEvent.prototype.pseudoTarget` (UI Events §idl-uievent) не определён ни у `UIEvent`, ни у `MouseEvent` — по одному файлу на дефект, 8 сабтестов суммарно. Второстепенный сигнал — реконфирмация: 34 сабтеста ⇒ уже открытый [BUG-574](../bugs/BUG-574-OPEN.md) (`Node.contains` отсутствует, ломает `test_driver.click()`/`send_keys()`), 52 ⇒ [BUG-622](../bugs/BUG-622-OPEN.md) (`document.defaultView` отсутствует, iframe-тесты detached), 18 ⇒ [BUG-590](../bugs/BUG-590-OPEN.md) (`document.createEvent` отсутствует целиком), 2 ⇒ [BUG-384](../bugs/BUG-384-OPEN.md) (именованный доступ `window.<id>` не реализован). Один новый номер бага
