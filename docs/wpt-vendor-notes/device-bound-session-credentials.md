# WPT vendor notes — `device-bound-session-credentials`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-device-bound-session-credentials`, `docs/wpt-status.md`), included despite its 🚫 scope note (niche Device Bound Session Credentials auth API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`delegated-ink`. Same pinned commit, `git sparse-checkout` at the same commit hash, 49 files (27 `.https.html`/`.html` test files, `helper.js`, `url_fetcher.html` — a fixture page, not a test, ~19 Python `wptserve` handler scripts, `excludeInScopeSpecification/`+`includeInScopeSpecification/` subdirs). One out-of-category helper not vendored: `/common/get-host-info.sub.js` (used by 3 tests), same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root device-bound-session-credentials --recursive` selected 27 ids (`url_fetcher.html` correctly excluded by the manifest — not a test) and completed fully (~7:29): 25 of the 26 `.https.` tests TIMEOUT on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`, same class as `WebCryptoAPI`/`ai`/`ambient-light`/…); `refresh-does-not-send-challenge.https.html` ERROR — a session-reuse artifact (result tagged with the previous test's URL), same pattern as `accelerometer`/`ambient-light`; the sole non-`.https.` test, `not-secure-connection.html`, also TIMEOUT but for a NEW reason — root-caused and filed as [BUG-350](../../bugs/BUG-350-FIXED.md): `<script type="module">` has no real module semantics on the default V8 build at all (`V8JsRuntime` never overrides `eval_module`, so the trait default silently runs module bodies through classic `eval()`), failing on the first `export`/`import` token (`Unexpected token 'export'`) — broad-impact, not WPT-specific, 80 vendored files use `type="module"`. 0/27 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/device-bound-session-credentials/`, 49 файлов: 27 `.https.html`/`.html` тестов, `helper.js`, `url_fetcher.html` (fixture-страница, не тест), ~19 Python wptserve-хелперов, `excludeInScopeSpecification/`+`includeInScopeSpecification/` подкаталоги), скоуп 🚫 (нишевый Device Bound Session Credentials auth API), включена несмотря на скоуп по постоянному решению пользователя, как `accelerometer`/`acid`/`ai`/…/`delegated-ink`. Один внекатегорийный хелпер не довендорен: `/common/get-host-info.sub.js` (используется тремя тестами), тот же гэп, что `FileAPI`/`ai`. `run_report.py --all --root device-bound-session-credentials --recursive` отобрал 27 id (`url_fetcher.html` манифестом не выбран — не тест), прошёл полностью (~7:29): 25 из 26 `.https.`-тестов TIMEOUT на уже известный HTTPS-порт-гэп (`invalid url: invalid port: "None"`, тот же класс, что `WebCryptoAPI`/`ai`/`ambient-light`/…); `refresh-does-not-send-challenge.https.html` ERROR — session-reuse-артефакт (результат пришёл с URL предыдущего теста), тот же паттерн, что `accelerometer`/`ambient-light`; единственный не-`.https.` тест, `not-secure-connection.html`, тоже TIMEOUT, но по НОВОЙ причине — root-caused и заведён [BUG-350](../../bugs/BUG-350-FIXED.md): `<script type="module">` на дефолтном V8-билде не имеет реальной семантики модуля вовсе (`V8JsRuntime` не переопределяет `eval_module`, трейтовый дефолт молча прогоняет модульный код через classic eval), падает на первом же `export`/`import` (`Unexpected token 'export'`) — широкий охват, не WPT-специфично, 80 вендоренных файлов используют `type="module"`. 0/27 harness OK, 0/0 сабтестов
