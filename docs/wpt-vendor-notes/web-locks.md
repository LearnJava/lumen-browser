# WPT vendor notes — `web-locks`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-09 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-web-locks`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 47 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, `LICENSE-WPT.md`, `bfcache/`, `crashtests/`, `resources/`, 22 of 47 files `.https.`). No `testdriver.js`/`variant` hits. Out-of-category helpers not vendored: `/common/utils.js`+`/common/dispatcher/dispatcher.js` (6 references each), same documented survey gap as `web-bundle`/sibling categories. `run_report.py --all --root web-locks --recursive` selected 40 ids: **1/40 harness OK, 0/1 subtests**. 34 TIMEOUT + 5 ERROR — standard TLS `UnknownIssuer` gap ([BUG-657](../../bugs/BUG-657-OPEN.md)) on the `.https.` files. The one non-`.https.` test that ran (`non-secure-context.any.html`) reached harness OK but its single subtest FAILed: `assert_false: API presence in non-secure contexts expected false got true`. Live probe (`--mcp-live-port`) confirmed the API itself works (`navigator.locks.request`/`.query` acquire and release cleanly) and confirmed the FAIL's cause: `window.isSecureContext` is unconditionally `true`, even for `file:` — reconfirmation of already-open [BUG-399](../../bugs/BUG-399-OPEN.md) (`isSecureContext` hardcoded instead of derived from protocol/host). No new bug filed this pass. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/web-locks/`, 47 файлов: `bfcache/`, `crashtests/`, `resources/`, `LICENSE-WPT.md`, `META.yml`, `README.md`, `WEB_FEATURES.yml`). Ноль `testdriver.js`-файлов, ноль `variant`-хитов, 22 из 47 файлов `.https.`. Внекатегорийные хелперы не довендорены: `/common/utils.js`+`/common/dispatcher/dispatcher.js` (по 6 ссылок), тот же документированный survey-gap, что у соседних категорий. `run_report.py --all --root web-locks --recursive` отобрал 40 id: **1/40 harness OK, 0/1 сабтестов**. 34 TIMEOUT + 5 ERROR — стандартный TLS-гэп `UnknownIssuer` ([BUG-657](../bugs/BUG-657-OPEN.md)) на `.https.`-файлах. Единственный исполнившийся не-`.https.` тест (`non-secure-context.any.html`) формально OK, но его единственный сабтест FAIL: `assert_false: API presence in non-secure contexts expected false got true` — `navigator.locks` доступен в небезопасном контексте. Живая проба (`--mcp-live-port`) подтвердила API функционально работает (`request`/`query`/lock acquire-release проходят чисто), но также подтвердила причину FAIL: `window.isSecureContext === true` безусловно (даже для `file:`) — переподтверждение уже открытого [BUG-399](../bugs/BUG-399-OPEN.md) (`isSecureContext` захардкожен `true`, не вычисляется из протокола/хоста). Новый номер бага не заводился
