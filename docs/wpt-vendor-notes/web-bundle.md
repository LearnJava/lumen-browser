# WPT vendor notes — `web-bundle`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-09 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-web-bundle`, `docs/wpt-status.md`), scope ⬜ (candidate) but zero implementation in `crates/` (`grep webbundle\|wbn` — no hits at all). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `web-based-payment-handler` category, 116 files (`META.yml`, `README.md`, `resources/` — 47 files including `.har`/`.wbn` fixtures and Python helper servers, `subresource-loading/` — 27 `.https.tentative.html` test files + 3 support files, all top-level tests `.https.tentative.`). No `testdriver.js`/`variant` hits. `run_report.py --all --root web-bundle --recursive` completed in ~7:28 (28 selected ids): **0/28 harness OK**, all 28 TIMEOUT on the standard TLS `UnknownIssuer` gap. Live probe (`--mcp-live-port`) of the category's own container — `<script type="webbundle" resources=… scopes=… credentials=…>` — found the element itself inert and spec-correct (`tagName === "SCRIPT"`, `type` attribute reflects, unknown script type simply not executed). One level up, the same probe found a new bug: [BUG-705](../../bugs/BUG-705-OPEN.md) — `document.scripts`/`.images`/`.forms`/`.links`/`.anchors`/`.embeds`/`.plugins` are all `undefined` (`document.scripts.length` throws `TypeError`), a family of live non-obsolete HTML Standard `Document` collections entirely missing from `crates/js/src/dom.rs`, distinct from the already-open [BUG-606](../../bugs/BUG-606-OPEN.md) (`document.applets`, spec-mandated always-empty legacy collection). One new bug number. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/web-bundle/`, 116 файлов, `LICENSE-WPT.md` скопирован из `web-based-payment-handler`; в крейтах нет вообще никакой реализации — `grep webbundle\|wbn` по `crates/` пуст). `run_report.py --all --root web-bundle --recursive` — ~7:28, 28 отобранных id (все `subresource-loading/*.https.tentative.html`): **0/28 harness OK**. Все 28 TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer`. Живая проба (`--mcp-live-port`) несущего элемента категории — `<script type="webbundle" resources=… scopes=… credentials=…>` — сам элемент безопасно инертен (`tagName===SCRIPT`, `type` рефлексирует корректно, неисполнение неизвестного типа скрипта соответствует спеке), но проба на уровень выше вскрыла новый баг: [BUG-705](../../bugs/BUG-705-OPEN.md) — `document.scripts`/`.images`/`.forms`/`.links`/`.anchors`/`.embeds`/`.plugins` все `undefined` целиком (живые нестарые `HTMLCollection` на `Document`, отдельно от уже открытого [BUG-606](../../bugs/BUG-606-OPEN.md) `document.applets`, спеково всегда-пустой legacy-коллекции).
