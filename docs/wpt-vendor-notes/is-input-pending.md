# WPT vendor notes — `is-input-pending`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-is-input-pending`, `docs/wpt-status.md`), scope ⬜ (candidate, WICG draft `navigator.scheduling.isInputPending()`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 12 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `input-events` category, `idlharness.window.js`, 9 `security/cross-origin-subframe*.sub.html`, `tentative/same-origin-subframe.sub.html`+`toplevel.html`, `resources/blank.html`+`input-onmessage.js`+`pending-input-utils.js`). No `.https.` files, no `variant` hits, all 11 non-idlharness files pull `testdriver.js`. `run_report.py --all --root is-input-pending --recursive` completed in ~2:18 (12 selected ids): **0/12 harness OK**, all TIMEOUT. `idlharness.window.html` — same un-vendored `/resources/WebIDLParser.js`+`/resources/idlharness.js` gap as `input-device-capabilities`/`intervention-reporting`. The other 11 (every `.sub.html` file, all living one directory below the category root in `security/` or `tentative/`) TIMEOUT on already-open [BUG-346](../../bugs/BUG-346-FIXED.md): each pulls its category helpers via a `..`-relative path (`security/../resources/pending-input-utils.js`, `tentative/../resources/input-onmessage.js`), `Url::resolve()` never collapses the dot-segment, wptserve 404s the literal path, and the helper-less test script never reaches a point where it can report a result. Also 404 (known infra-gap class, not new): un-vendored `/resources/testdriver-vendor.js`+`testdriver-actions.js`. A live probe (`--mcp-live-port`) confirmed `navigator.scheduling` is entirely absent (`typeof navigator.scheduling === 'undefined'`; a `crates/js/src/` grep for `"scheduling"`/`isInputPending` also comes up empty) — the whole API is unimplemented, no new bug filed, same pattern as `input-device-capabilities`. Worth a re-run once BUG-346 is fixed — 11 of 12 ids would then at least reach their own test logic. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/is-input-pending/`, 12 файлов: `META.yml`, `README.md`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `input-events`, `idlharness.window.js`, 9 `security/cross-origin-subframe*.sub.html`, `tentative/same-origin-subframe.sub.html`+`toplevel.html`, `resources/blank.html`+`input-onmessage.js`+`pending-input-utils.js`), скоуп ⬜ (кандидат, WICG-черновик `navigator.scheduling.isInputPending()`). `run_report.py --all --root is-input-pending --recursive` — ~2:18, 12 отобранных id: **0/12 harness OK**, все TIMEOUT. `idlharness.window.html` — невендоренные `/resources/WebIDLParser.js`+`/resources/idlharness.js`, тот же класс гэпа, что `input-device-capabilities`/`intervention-reporting`. Остальные 11 (все `.sub.html`) TIMEOUT по уже открытому [BUG-346](../../bugs/BUG-346-FIXED.md): каждый файл подключает категорийные хелперы относительным путём на уровень вверх (`security/../resources/pending-input-utils.js`, `tentative/../resources/input-onmessage.js`) — `Url::resolve()` не схлопывает `..`, wptserve честно 404-ит буквальный путь, хелперы не грузятся, тест зависает без сигнала о завершении. Отдельно 404-ят невендоренные `/resources/testdriver-vendor.js`+`testdriver-actions.js` — тот же класс, что в других категориях. Живая проба подтвердила: `navigator.scheduling` целиком отсутствует (`typeof navigator.scheduling === 'undefined'`, грепом по `crates/js/src/` тоже не установлен) — целиком не реализованное API, не заведён отдельный BUG-NNN, тот же паттерн, что `input-device-capabilities`/`custom-elements/registries`. После фикса BUG-346 категорию стоит перепрогнать — 11 из 12 id должны хотя бы дойти до собственной логики теста.
