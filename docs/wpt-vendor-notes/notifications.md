# WPT vendor notes — `notifications`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/notifications/`, 46 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md`, 41 корневой тестовый/скриптовый файл, `resources/`). `run_report.py --all --root notifications --recursive` — **~5 мин 20 с, 2/24 harness OK, 5/7 сабтестов** (24 отобранных id из 46 файлов — 11 `-manual` файлов раннер исключает). Notification API реально реализован (`crates/js/src/notifications_bindings.rs`, Phase 0: конструктор, `requestPermission()`, `Notification.permission`, события через `onclick`/`onclose`/`onerror`/`onshow`, `ServiceWorkerRegistration.showNotification`/`getNotifications`). Два `harness OK` теста не касаются конструктора: `historical.any.html` (2/2) и `permission.html` (1/1, только читает `Notification.permission`). Единственный собственный сигнал категории — `constructor-non-secure.html` (не-`.https.`, реально исполняется, не упирается в TLS-гэп): `new Notification()` при `_permission !== 'granted'` (дефолт движка `denied`) не диспатчит `error`, как требует W3C Notifications API §6 «create a notification» — конструктор просто ничего не делает после создания объекта. Заведён [BUG-642](../../bugs/BUG-642-OPEN.md). `permissions-non-secure.html` (Test TIMEOUT, 2/3 сабтеста, 1 unexpected) — третий сабтест грузит `resources/permission-worker.js` через `new Worker(url, {type:'module'})` с обычным внешним URL — реконфирмация уже заведённого [BUG-364](../../bugs/BUG-364-FIXED.md) (внешний URL воркера не фетчится, тело пустое), не новая находка. Остальные 20/24 id — все `.https.`, TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` (`docs/wpt-status.md:25-28`, `tests/wpt/certs/README.md`)
