# WPT vendor notes — `timing-entrytypes-registry`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (пин `35be3b44`, `tests/wpt/timing-entrytypes-registry/`, 4 файла: `META.yml`, `registry.any.js`, `registry.window.js`, `resources/utils.js`). Прогон `run_report.py --all --root timing-entrytypes-registry --recursive`, ~45 с, 2 отобранных id: **0/2 harness OK, 2/8 сабтестов**. Заведён [BUG-687](../../bugs/BUG-687-OPEN.md): `performance.mark()`/`.measure()` создают записи как обычные объектные литералы, не инстансы `PerformanceMark`/`PerformanceMeasure` (`[object Object]` вместо ожидаемого класса) — тот же класс, что [BUG-645](../../bugs/BUG-645-OPEN.md)/[BUG-673](../../bugs/BUG-673-OPEN.md), теперь подтверждён для типов записей, создаваемых целиком на JS-стороне (User Timing L3), а не нативным хуком. Второстепенный сигнал — реконфирмация: `resource`-сабтест NOTRUN ([BUG-520](../../bugs/BUG-520-OPEN.md), Resource Timing хук не вызывается для реальных загрузок); `paint`/`longtask` NOTRUN (paint доставляется один раз за загрузку страницы, см. BUG-645; `longtask` числится в `supportedEntryTypes`, но никогда не генерируется, [BUG-354](../../bugs/BUG-354-FIXED.md)). Один новый номер бага
