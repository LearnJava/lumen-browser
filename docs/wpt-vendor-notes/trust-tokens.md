# WPT vendor notes — `trust-tokens`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (пин `35be3b44`, `tests/wpt/trust-tokens/`, 5 файлов: `META.yml`, `trust-token-parameter-validation(-xhr).tentative.https.html`, `end-to-end/has-trust-token-with-no-top-frame.tentative.https.html` + `README.txt`), включена несмотря на скоуп 🚫 (ad-tech/Privacy Sandbox) по тому же постоянному решению пользователя, что и `accelerometer`/`ai`/`attribution-reporting`/… Все 3 отобранных id `.https.`-only и самодостаточны (только `testharness.js`/`testharnessreport.js`, нет testdriver, нет variant-фанаута). Прогон `run_report.py --all --root trust-tokens --recursive`, ~1:07: **0/3 harness OK, 0/0 сабтестов** — все 3 TIMEOUT на документированном HTTPS-порт-гэпе (`invalid url: invalid port: "None"`, тот же класс, что `WebCryptoAPI`/`ai`/`ambient-light`). Trust Tokens API не реализован в движке вовсе (`grep -rli trust.?token crates/` — ноль совпадений), пробовать нечего впрямую; по правилу «пробуй контейнер уровнем выше» проверен `Request()` с полем `privateToken` — конструктор молча игнорирует любые неизвестные/невалидные поля `init` (`crates/js/src/dom.rs:6251`), что является переподтверждением уже открытого [BUG-370](../../bugs/BUG-370-FIXED.md) («ни один конструктор Request/Response не валидирует свои аргументы»), а не новой находкой. Новых номеров бага не заведено
