# WPT vendor notes — `cookiestore`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cookiestore`, `docs/wpt-status.md`), in-scope (⬜, CookieStore API — async `cookieStore.get`/`set`/`delete`/`getAll` + `cookiechange` events, not implemented in Lumen at all). Same pinned commit, `git sparse-checkout` at the same commit hash, 65 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `resources/` — 8 helpers, 54 test files). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js`, `/service-workers/service-worker/resources/test-helpers.sub.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`. The entire category is `.https.`-only — `run_report.py --all --root cookiestore --recursive` completed fully (~12:35, 46 selected ids): all 46 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`/`compute-pressure`) — cleanest first-pass outcome so far, no SKIP/ERROR/FAIL, every test blocked by the single already-documented infra gap rather than a mix of causes. 0/46 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/cookiestore/`, 65 файлов: `META.yml`, `README.md`, `WEB_FEATURES.yml`, `resources/` — 8 хелперов, 54 тестовых файла). Внекатегорийные хелперы не довендорены: `/resources/testdriver.js`+`testdriver-vendor.js`, `/service-workers/service-worker/resources/test-helpers.sub.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js`, та же методология survey-gap, что `FileAPI`/`ai`. Вся категория `.https.`-only (46 отобранных id — `run_report.py` насчитал 54 all-vendored файла, но манифест реально отбирает только 46 автопрогоняемых) — `run_report.py --all --root cookiestore --recursive` прошёл полностью (~12:35): все 46 TIMEOUT (`invalid url: invalid port: "None"` — минимальный исполнитель не поднимает HTTPS-порт, тот же класс ограничения, что `WebCryptoAPI`/`ai`/`bluetooth`/`compute-pressure`). Чище прочих first-pass записей: ни одного SKIP/ERROR/FAIL — единственная причина провала для всей категории один и тот же уже задокументированный инфра-гэп, `CookieStore` API сам по себе не был вовсе прощупан ни одним тестом. 0/46 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — первый проход, единый root cause (инфра-гэп, не движковый дефект)
