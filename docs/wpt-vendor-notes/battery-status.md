# WPT vendor notes — `battery-status`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-battery-status`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`/`badging`. Same pinned commit, `git sparse-checkout` at the same commit hash, 30 files (`META.yml`, `WEB_FEATURES.yml`, 22 test/header files, `resources/` — `battery-status-helpers.js`+`support-iframe.html`+`support-window-open.html`+`window-postmessage-open-close.html`). One out-of-category dependency pair not vendored — `idlharness.https.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`/`background-sync`/`badging`. The manifest selects 17 ids (5 `-manual.https.html` files are excluded as non-automatic, matching upstream convention): `page-visibility.https.html` SKIP (`Executor does not support testdriver.js`, same known executor gap as elsewhere in this backlog); the other 16 (all `.https.`-only) hit the same HTTPS-port gap as `background-sync`/`background-fetch`/`badging`/`WebCryptoAPI`/`ai` — `run_report.py --all --root battery-status --recursive` completed in ~5:00: 16 TIMEOUT (`invalid url: invalid port: "None"`), 1 SKIP. 0/17 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (коммит `35be3b44`, `tests/wpt/battery-status/`, 30 файлов: `META.yml`, `WEB_FEATURES.yml`, 22 тестовых/header-файла, `resources/` — `battery-status-helpers.js`+`support-iframe.html`+`support-window-open.html`+`window-postmessage-open-close.html`), включена несмотря на скоуп 🚫 (датчик устройства) по тому же постоянному решению пользователя, что и `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`/`badging`. Один внекатегорийный хелпер не довендорен: `idlharness.https.window.js` ссылается на `/resources/WebIDLParser.js`+`/resources/idlharness.js`, та же методология, что `FileAPI`/`audio-session`/`background-sync`/`badging`. Манифест отбирает 17 id (5 файлов `-manual.https.html` исключены как неавтоматические, по upstream-соглашению): `page-visibility.https.html` — SKIP (`Executor does not support testdriver.js`, тот же известный гэп исполнителя, что и в остальном бэклоге); остальные 16 (все `.https.`-only) упираются в тот же HTTPS-порт-гэп, что `background-sync`/`background-fetch`/`badging`/`WebCryptoAPI`/`ai` — `run_report.py --all --root battery-status --recursive` добежал за ~5:00: 16 — TIMEOUT (`invalid url: invalid port: "None"`), 1 — SKIP. 0/17 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
