# WPT vendor notes — `nav-tracking-mitigations`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-nav-tracking-mitigations`, `docs/wpt-status.md`), scope 🚫 (Chrome-experimental Bounce Tracking Mitigations privacy feature — `test_driver.run_bounce_tracking_mitigations()` — Lumen has no such pipeline and `tools/wptrunner`'s own `executorlumen.py::_handle_action` implements only `action == "click"`). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `element-timing` category, 9 files (`META.yml`, `resources/` — 5 files, 2 root test files: `stateful-client-bounce.sub.https.html`, `stateless-client-bounce.sub.https.html`). No `variant` hits; both selectable ids are `.https.` and both pull `testdriver.js`/`testdriver-vendor.js`; both also depend on the out-of-category `/storage-access-api/helpers.js`, which this repo does not vendor (`tests/wpt/storage-access-api/` doesn't exist) — currently unobservable, see below. `run_report.py --all --root nav-tracking-mitigations --recursive` completed in ~52s: **0/2 harness OK, 100% TIMEOUT**, both hitting the already-documented WPT-RUN-2 TLS `UnknownIssuer` gap (`docs/wpt-status.md` "Охват", `tests/wpt/certs/README.md`) before any script (including the missing `storage-access-api/helpers.js`) could even load. No new `BUG-NNN` filed — the TLS gap is already tracked as WPT-RUN-2's documented residual, and the category's own feature (bounce tracking mitigations) is out of scope regardless of the TLS gap. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/nav-tracking-mitigations/`, 9 файлов: `META.yml`, `LICENSE-WPT.md` скопирован из соседней `element-timing`, `resources/` — 5 файлов, 2 корневых тестовых файла: `stateful-client-bounce.sub.https.html`, `stateless-client-bounce.sub.https.html`). Скоуп 🚫: Chrome-экспериментальная фича Bounce Tracking Mitigations (`test_driver.run_bounce_tracking_mitigations()`), в Lumen конвейера нет, `executorlumen.py::_handle_action` реализует только `action == "click"`. Ни variant-ов; оба id — `.https.` и оба тянут `testdriver.js`/`testdriver-vendor.js`; оба также зависят от вне-категорийного `/storage-access-api/helpers.js`, не вендоренного в этом репозитории (`tests/wpt/storage-access-api/` не существует) — сейчас ненаблюдаемо, см. ниже. Прогон `run_report.py --all --root nav-tracking-mitigations --recursive` (~52с): **0/2 harness OK, 100% TIMEOUT** — оба на уже задокументированном TLS-гэпе WPT-RUN-2 `UnknownIssuer` (`tests/wpt/certs/README.md`), до загрузки любого скрипта (включая недостающий `storage-access-api/helpers.js`). Новый баг не заведён — гэп уже учтён как остаток WPT-RUN-2, а сама фича категории вне скоупа независимо от TLS
