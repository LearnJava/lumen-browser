# WPT vendor notes — `orientation-event`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/orientation-event/`, 41 файл: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `notifications`, `device-orientation-events-*.html`, `idlharness.https.window.js`, `motion/`, `orientation/`, `resources/`). Ни variant-ов; все не-`-manual` тестовые файлы `.https.`, почти все тянут `testdriver.js`/`testdriver-vendor.js` (`test_driver.set_permission`/сенсорный мок, не реализованный экзекьютором — только `action == "click"`). `run_report.py --all --root orientation-event --recursive` (~7 мин, 27 отобранных id из 33 файлов — 10 `-manual` раннер исключает): **0/27 harness OK, 0/0 сабтестов**. 26/27 TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` (все id `.https.`); 1/27 (`idlharness.https.window.html`) — реконфирмация [BUG-380](../bugs/BUG-380-FIXED.md) (browsing context переиспользуется, отдаёт результаты предыдущего теста, `device-orientation-events-unavailable-on-insecure-origins.html`). `DeviceOrientationEvent`/`DeviceMotionEvent` реально реализованы как Phase 0 заглушка (`crates/js/src/device_sensors.rs`) — сам прогон сигнала не дал (весь код API недостижим раньше TLS-гэпа), но живая проба через `--mcp-live-port` нашла реальный дефект шима: синтетическое событие `deviceorientation`/`devicemotion` диспатчится ровно один раз, только самому первому за всю жизнь страницы зарегистрированному слушателю данного типа (`window.addEventListener`-monkey-patch гейтится page-global флагом `firedOrientationEvent`/`firedMotionEvent`) — любой второй, третий или более поздний слушатель молча никогда не получает событие (`l1_fired=1, l2_fired=0, l3_fired=0`, проверено живой пробой). Заведён [BUG-643](../bugs/BUG-643-OPEN.md)
