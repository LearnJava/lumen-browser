# WPT vendor notes — `avif`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-avif`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `WEB_FEATURES.yml`, reftest pair `animated-avif-timeout.html`+`animated-avif-timeout-ref.html`). Two out-of-category image resources (`/images/animated-avif.avif`, `/images/green.avif`) not vendored, same documented survey gap as `apng`/`FileAPI`. The category's sole test is the reftest `animated-avif-timeout.html` (`<link rel="match">`) — WPT-manifest type `reftest`, unsupported by the minimal executor (same class of gap as `acid`/`apng`/`animation-worklet`), while its partner `animated-avif-timeout-ref.html` is a *reference* file, not a test — so `run_report.py --all --root avif --recursive` completed in ~5s having selected 0 runnable tests (`Ran 0 checks` / `No tests ran` CRITICAL, plus a harmless empty-queue `AttributeError` in wptrunner's cleanup thread), 0/0. First vendored category whose *only* test is a reftest — the same net "zero automatically runnable tests" outcome as `annotation-model`/`annotation-vocab` (manual-only) but reached through the reftest gap instead; unlike `apng` (which had a `testharness` test alongside its reftest), there is nothing here for the executor to run at all. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-24 (коммит `35be3b44`, `tests/wpt/avif/`, 4 файла: `META.yml`, `WEB_FEATURES.yml`, reftest-пара `animated-avif-timeout.html`+`animated-avif-timeout-ref.html`). Два внекатегорийных ресурса-картинки (`/images/animated-avif.avif`, `/images/green.avif`) не довендорены, та же методология, что `apng`/`FileAPI`. Единственный тест категории — reftest `animated-avif-timeout.html` (`<link rel="match">`), тип WPT-манифеста `reftest`, минимальный исполнитель его не поддерживает (тот же класс гэпа, что `acid`/`apng`/`animation-worklet`), а его пара `animated-avif-timeout-ref.html` — reference-файл, не тест; поэтому `run_report.py --all --root avif --recursive` добежал за ~5с, отобрав 0 исполнимых тестов (`Ran 0 checks` / `No tests ran`), 0/0. Первая вендоренная категория, чей единственный тест — reftest: тот же итог «ноль автоматически исполнимых тестов», что у `annotation-model`/`annotation-vocab` (там причина — только `-manual.html`), но достигнутый через reftest-гэп; в отличие от `apng` (у которого рядом с reftest был ещё и `testharness`-тест), здесь исполнителю нечего запускать вовсе. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
