# WPT vendor notes — `wai-aria`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (`tests/wpt/wai-aria/`, 274 файла: `role/` — 20 файлов ролей, `checked`/`pressed` — 2 файла состояния, `aria-actions/` — 4 `.tentative.html` (черновая ARIA Actions relationship, ещё не в спеке), `subtree/` — 1, `manual/` — 234 файла (ARIA/AAM verification suite, тип `manual`, вне выборки `run_report.py`), `accessibility_properties_basic.tentative.html`, `idlharness.window.js`). `run_report.py --all --root wai-aria --recursive` отобрал 32 id (все `manual/` исключены собственным фильтром) — **31/32 harness OK, 6/437 сабтестов**. Доминирующий отказ (337/355 FAIL) — `TypeError: element.getRootNode is not a function`, реконфирмация уже открытого [BUG-599](../../bugs/BUG-599-OPEN.md) (`Node.prototype.getRootNode()` отсутствует целиком, ломает `get_selector_array` в вендоренном testdriver-шиме). 4 FAIL `Error: Browsing context for element was detached` (`accessibility_properties_basic.tentative.html`) — реконфирмация [BUG-622](../../bugs/BUG-622-OPEN.md) (`document.defaultView` отсутствует целиком). 1 FAIL `shadowRoot.append is not a function` (`aria-actions/aria-actions-host-accname-multiple-targets.tentative.html`) — реконфирмация [BUG-676](../../bugs/BUG-676-OPEN.md) (`ShadowRoot` — plain-объект без `Node`-методов). Остальные 8 FAIL в `aria-actions/*` — `Element.ariaActionsElements`/`ElementInternals.ariaActionsElements` (IDL-рефлексия для черновой ARIA Actions relationship) отсутствуют целиком — ожидаемо для `.tentative.` черновика, не баг. `idlharness.window.html` — TIMEOUT, известный класс «idlharness.js/WebIDLParser.js не вендорены». Новый номер бага не заводился
