# WPT vendor notes — `quirks`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/quirks/`, 72 файла: `META.yml`, `hashless-hex-color/`, `unitless-length/`, `crashtests/`, `historical/`, `support/`, ~20 корневых тестов; `LICENSE-WPT.md` скопирован из соседней `print`). Ни одного `.https.`/`testdriver.js`/`variant` хита. `run_report.py --all --root quirks --recursive` (~2 мин, 17 отобранных id): **6/17 harness OK, 2/75 сабтестов**. 7 TIMEOUT (`hashless-hex-color/*`, `table-cell-width-calculation*`, `line-height-calculation`, `percentage-height-calculation`, `blocks-ignore-line-height`) — все сравнивают quirks/limited-quirks/no-quirks режимы через `iframe.contentWindow`+`document.open/write/close()`, реконфирмация [BUG-480](../../bugs/BUG-480-OPEN.md). `classname-query-after-sibling-adoption.html` (`templateDocument.compatMode` — `undefined`) реконфирмирует [BUG-358](../../bugs/BUG-358-FIXED.md)/[BUG-482](../../bugs/BUG-482-OPEN.md)/[BUG-552](../../bugs/BUG-552-OPEN.md); `supports.html` (`@supports`/`CSS.supports()` принимают невалидные quirky-значения "00ff00"/"1 1") реконфирмирует [BUG-501](../../bugs/BUG-501-OPEN.md) (value полностью игнорируется). Новый [BUG-658](../../bugs/BUG-658-OPEN.md): `table-cell-width-calculation-applies-to.html` даёт `width expected 80 but got 0` под wptrunner, но `--dump-layout` на том же файле пост-settle показывает верную геометрию (`w=80.00`) — тот же гэп синхронизации публикации layout-снапшота, что [BUG-555](../../bugs/BUG-555-OPEN.md) документировал для `getComputedStyle()`, теперь подтверждён и для `offsetWidth`/`getBoundingClientRect()` (`_lumen_get_bounding_rect`)
