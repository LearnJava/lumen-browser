# WPT vendor notes — `soft-navigation-heuristics`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-soft-navigation-heuristics`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `shadow-dom` category, 104 files (`detection/`, `dom/`, `history/`, `icp/`, `lcp/`, `resources/`, `smoke/`). 93 of 103 test files reference `testdriver` textually, but 0 of them are `SKIP`ed by the executor because the category relies only on `test_driver.click()` — the one action `executorlumen.py::_handle_action` actually implements. `run_report.py --all --root soft-navigation-heuristics --recursive --processes=4` completed in 1:38 (94 selected ids): **73/94 harness OK, 2/101 subtests**. Dominant signal (54 FAIL, `Error: Timed out waiting for LCP entries`) is a new, previously unfiled defect — [BUG-678](../../bugs/BUG-678-OPEN.md): `_lumen_deliver_soft_nav` (`crates/js/src/soft_navigation.rs`, its own doc comment calls it a "shell hook") has no caller anywhere in the engine, unlike sibling hooks `_lumen_deliver_lcp_entry`/`_lumen_deliver_paint_entry` which are wired from `crates/shell/`/`crates/driver/` — soft-navigation detection is simply never triggered, distinct from the already-filed [BUG-354](../../bugs/BUG-354-OPEN.md) (`supportedEntryTypes` false positive). Secondary signal in the same bug (14 FAIL) — `window.LargestContentfulPaint` (the WebIDL entry constructor) is missing entirely even though LCP entries are delivered as plain object literals, same defect class as [BUG-673](../../bugs/BUG-673-OPEN.md), first confirmed for LCP. Remaining signal reconfirms already-open bugs: 34 FAIL [BUG-574](../../bugs/BUG-574-OPEN.md) (`Node.contains` missing) and 28 FAIL/ERROR [BUG-346](../../bugs/BUG-346-OPEN.md) (`..`-segments in `Url::resolve()` — 404 on `../../resources/soft-navigation-test-helper.js` from the `dom/tentative/`/`detection/tentative/` subdirs; the absolute-path `/soft-navigation-heuristics/resources/...` form resolves fine). See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/soft-navigation-heuristics/`, 104 файла: `detection/`, `dom/`, `history/`, `icp/`, `lcp/`, `resources/`, `smoke/`). `run_report.py --all --root soft-navigation-heuristics --recursive --processes=4` — 1:38, 94 отобранных id: **73/94 harness OK, 2/101 сабтестов**. Необычно высокий harness-OK для testdriver-насыщенной категории (93/103 файлов упоминают testdriver, SKIP — ноль): вся категория опирается только на `test_driver.click()`, единственное реально реализованное действие исполнителя. Доминирующий сигнал — 54 FAIL «Timed out waiting for LCP entries»: новая находка — [BUG-678](../bugs/BUG-678-OPEN.md), `_lumen_deliver_soft_nav` (`crates/js/src/soft_navigation.rs`, «shell hook» по своему doc-комментарию) не вызывается вообще нигде в движке, в отличие от соседних `_lumen_deliver_lcp_entry`/`_lumen_deliver_paint_entry` — детекция мягкой навигации не реализована, отличается от уже известного [BUG-354](../bugs/BUG-354-OPEN.md) (тот про ложь `supportedEntryTypes`). Второстепенный сигнал того же бага — 14 FAIL `assert_implements: LargestContentfulPaint is not implemented` (WebIDL-конструктор LCP-записи отсутствует, тот же класс, что [BUG-673](../bugs/BUG-673-OPEN.md), впервые для LCP). Остальной сигнал — реконфирмация: 34 FAIL [BUG-574](../bugs/BUG-574-OPEN.md) (`Node.contains` отсутствует), 28 FAIL/ERROR [BUG-346](../bugs/BUG-346-OPEN.md) (`..`-сегменты в `Url::resolve()` — 404 на `../../resources/soft-navigation-test-helper.js` из `dom/tentative/`/`detection/tentative/`)
