# WPT vendor notes — `apng`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-apng`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, reftest pair `animated-png-timeout.html`+`animated-png-timeout-ref.html`, `supported-in-source-type.html`). Four out-of-category image resources (`/images/apng.png`, `/images/green.png`, `/images/anim-gr.png`, `/images/anim-gr.gif`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root apng --recursive` completed fully in ~9s (2 selected ids): the reftest pair is WPT-manifest type `reftest`, unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`accessibility`/`animation-worklet`) — both "not run"; `supported-in-source-type.html` (testharness) actually ran — 1/1 subtests passed despite a 404 on the out-of-category `/images/anim-gr.gif` (the test only checks `<picture><source>` selection via `currentSrc`, not that the image actually loaded). See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-23 (`tests/wpt/apng/`, 4 файла: `META.yml`, reftest-пара `animated-png-timeout.html`+`animated-png-timeout-ref.html`, `supported-in-source-type.html`). Внекатегорийные ресурсы `/images/apng.png`, `/images/green.png`, `/images/anim-gr.png`, `/images/anim-gr.gif` не довендорены, та же методология, что `FileAPI`/`ai`. `run_report.py --all --root apng --recursive` добежал целиком за ~9с (2 отобранных id): reftest-пара — WPT-манифест тип `reftest`, исполнитель не поддерживает (`Unsupported test type reftest`, тот же класс гэпа, что `acid`/`accessibility`/`animation-worklet`), оба «not run»; `supported-in-source-type.html` (testharness) реально прогнан — 1/1 сабтестов passed, несмотря на 404 внекатегорийного `/images/anim-gr.gif` (тест сверяет только выбор `<picture><source>` по `currentSrc`, не факт загрузки картинки). Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
