# WPT vendor notes — `shared-storage-selecturl-limit`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-shared-storage-selecturl-limit`, `docs/wpt-status.md`), scope 🚫 (out of scope — ad-tech / Privacy Sandbox). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `shared-storage` category, 10 files (7 root `.tentative.https.sub.html`, `resources/`). Entire category is `.https.sub.`, no `variant`/`testdriver` hits. `run_report.py --all --root shared-storage-selecturl-limit --recursive` completed in 2:10 (7 selected ids): **0/7 harness OK**. All 7 TIMEOUT on the already-documented TLS `UnknownIssuer` gap — none reached navigation. `sharedStorage.selectURL`/`remainingBudget` (`crates/js/src/shared_storage.rs`) are Phase 0 stubs whose behavior matches their own doc-comment (`selectURL` always returns the first URL with no limit tracking; `remainingBudget()` always returns 12) — not a bug by itself. A live probe (`--mcp-live-port`, 30 consecutive `selectURL({resolveToConfig:true})` calls) confirmed no call-limit enforcement exists at all (all 30 succeed identically) and `resolveToConfig:true` is ignored (returns a plain string, not a `FencedFrameConfig`) — both gaps are covered by the same documented Phase 0 scope, no new bug numbers filed

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/shared-storage-selecturl-limit/`, 10 файлов: 7 корневых `.tentative.https.sub.html`, `resources/`). `run_report.py --all --root shared-storage-selecturl-limit --recursive` — 2:10, 7 отобранных id, все `.https.sub.`: **0/7 harness OK**, все TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` — ни один тест не дошёл до навигации. `sharedStorage.selectURL`/`remainingBudget` — Phase 0 заглушки (`crates/js/src/shared_storage.rs`), поведение точно соответствует doc-комментарию (`selectURL` всегда возвращает первый URL без учёта лимита, `remainingBudget()` всегда 12) — не баг. Живая проба (`--mcp-live-port`, 30 последовательных вызовов `selectURL({resolveToConfig:true})`) подтвердила: лимит вызовов не отслеживается вовсе (все 30 успешны и идентичны), `resolveToConfig:true` игнорируется (возвращается строка, а не `FencedFrameConfig`) — оба несоответствия покрыты тем же документированным Phase 0 гэпом, новых номеров багов не заведено
