# WPT vendor notes — `css`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-css`, `docs/wpt-status.md`), in-scope (⬜, the umbrella top-level directory for every CSS spec test suite — `CSS1`/`CSS2`, `WOFF2`, `compositing`, and every `css-*` module: `css-align`, `css-anchor-position`, `css-animations`, `css-backgrounds`, `css-box`, `css-break`, `css-cascade`, `css-color`, `css-conditional`, `css-contain`, `css-display`, `css-flexbox`, `css-fonts`, `css-grid`, `css-transforms`, `css-transitions`, … — already Lumen's largest single implementation surface, `css-parser`+`layout`+`paint`). Same pinned commit, `git sparse-checkout` at the same commit hash. By far the largest category vendored so far: 54350 files, ~209 MB (vs. the previous largest, `content-security-policy`'s 1356 files) — a `git sparse-checkout` of the single upstream `css/` root directory, copied verbatim, no filtering. Out-of-category helpers are not individually enumerated here (impractical at this scale, unlike smaller categories) — the bounded trial run below observed 404s on `/resources/check-layout-th.js` (56 hits) and `/fonts/ahem.css` (3 hits), same un-vendored-helper survey gap as `compat`'s `webkit-box-item-shrink-001/002`; the full un-vendored-dependency surface across all 34607 selected ids is unknown. The manifest selects **34607 ids** — also by far the largest (next: `content-security-policy`'s 865) — so `run_report.py --all --root css --recursive` did **not** complete, same limitation class as `bluetooth`/`client-hints`/`content-security-policy` but far more extreme (at the observed pace, a full run would take on the order of a day, not hours). A bounded ~11:30 trial run (stopped manually) processed 382 of 34607 ids (~1.1%, covering `CSS1`/`CSS2` box-model/floats/normal-flow tests plus the start of `compositing`): 267 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`clear-site-data`), 62 bare TIMEOUT (harness never reported), 53 harness OK with 1294/2594 subtests passed. Failure clusters in the trial: `background-blend-mode` not recognized in computed style (38 occurrences — the property isn't implemented); `document.elementFromPoint is not a function` (32 occurrences — missing DOM API); several `min-height:stretch`/`calc()` margin-collapse-through cases FAIL (`assert_equals: margins should collapse through expected 50 but got 0`); the `webkit-box-item-shrink`-style layout tests 404 on the un-vendored `/resources/check-layout-th.js`+`/fonts/ahem.css`, same gap as `compat`. Not filed as a separate BUG-NNN — first pass, bounded trial only, failures are a mix of already-known missing-feature classes rather than a single fresh regression. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/css/`, 54350 файлов, ~209 МБ — весь верхнеуровневый каталог `css/` апстрима: `CSS1`/`CSS2`, `WOFF2`, `compositing`, все модули `css-*` (`css-align`, `css-anchor-position`, `css-animations`, `css-backgrounds`, `css-box`, `css-break`, `css-cascade`, `css-color`, `css-conditional`, `css-contain`, `css-display`, `css-flexbox`, `css-fonts`, `css-grid`, `css-transforms`, `css-transitions`, …)), скоуп ⬜ (уже крупнейшая поверхность реализации в Lumen — `css-parser`+`layout`+`paint`). Самая большая вендоренная категория из всех (в 40 раз больше предыдущего рекорда `content-security-policy`, 1356 файлов). Внекатегорийные хелперы поштучно не переписаны (нереалистично при таком объёме) — в пробном прогоне замечены 404 на `/resources/check-layout-th.js` (56 попаданий) и `/fonts/ahem.css` (3), тот же гэп, что у `compat`'s `webkit-box-item-shrink-001/002`; полный список невендоренных зависимостей по всем 34607 отобранным id неизвестен. Манифест отбирает **34607 id** — тоже рекорд (следующий по размеру — `content-security-policy`, 865) — `run_report.py --all --root css --recursive` **не завершился**, тот же класс ограничения, что `bluetooth`/`client-hints`/`content-security-policy`, но значительно острее (при наблюдаемом темпе полный прогон занял бы порядка суток, не часов). Ограниченный пробный прогон ~11:30 (остановлен вручную) обработал 382 из 34607 id (~1.1%, покрыл `CSS1`/`CSS2` box-model/floats/normal-flow плюс начало `compositing`): 267 SKIP (`Executor does not support testdriver.js`, тот же гэп, что `accname`/`clear-site-data`), 62 голых TIMEOUT (гарнес не отчитался), 53 harness OK, 1294/2594 сабтестов passed. Кластеры провалов в пробе: `background-blend-mode` не распознаётся в computed style (38 упоминаний — свойство не реализовано); `document.elementFromPoint is not a function` (32 упоминания — отсутствующий DOM API); несколько кейсов `min-height:stretch`/`calc()` margin-collapse-through проваливаются (`assert_equals: margins should collapse through expected 50 but got 0`); `webkit-box-item-shrink`-подобные layout-тесты 404 на невендоренных `/resources/check-layout-th.js`+`/fonts/ahem.css`, тот же гэп, что `compat`. Не заводился отдельный BUG-NNN — первый проход, только ограниченная проба, провалы — смесь уже известных классов "функция не реализована", а не свежая регрессия. **`WPT-RUN-1` закрыта 2026-08-02**: `/resources/check-layout-th.js` довендорен (`tests/wpt/VENDOR.md`) — это была прикидка по 1.1%-пробе, полный статический скан всего `css/` (`tests/wpt/find_missing_resources.py --root css`, читает `src=`/`href=`/`url()` во всех 48594 файлах без единого прогона) даёт куда бо́льшую цифру, **1119 файлов**, что делает его безоговорочно самым референсируемым внекатегорийным хелпером во всём корпусе. `/fonts/ahem.css` из старой пробы больше не в списке пропавших — довендорен позже как побочный эффект вендоринга целой категории `fonts` (2026-07-28, см. её строку ниже); сам файл `Ahem.ttf` там же, так что довендоривать по этой строке было больше нечего. Точечная проверка (`run_smoke.py` на 4 файлах `CSS2/floats/zero-space-between-floats-00{1..4}.html`, которые раньше падали на отсутствующем хелпере) подтверждает DoD: хелпер теперь выполняется и даёт содержательные фейлы с числами (`offsetLeft expected 100 but got 0`, `offsetTop expected 100/200 but got 0`) — похоже на реальный дефект позиционирования float с нулевым зазором, не заводился отдельным BUG-NNN (не входит в DoD этой задачи, группировка находок — `WPT-RUN-3`). Полный список из скана — 75 остальных внекатегорийных путей (`/common/reftest-wait.js` 1118 — но упирается в отсутствие reftest-executor'а, `TEST-4`; `/resources/testdriver-vendor.js`+`testdriver.js` 280 каждый, `/resources/testdriver-actions.js` 215 — `WPT-RUN-2`; `/common/worklet-reftest.js` 250 — тот же reftest-гэп; остальное — длинный хвост картинок/шрифтов/медиа под 100 хитов) — не вендорился этим срезом: узкий скоуп задачи ("начиная с `check-layout-th.js`", размер S) и нулевая отдача до соответствующих зависимостей (`TEST-4` reftest-executor, `WPT-RUN-2` testdriver.js) — задел для будущих срезов, зафиксирован тут, не потерян. **`WPT-RUN-3` срез 1 (2026-08-02, только шардинг, не массовый прогон)**: `run_report.py` получил `--processes`/`--offset`/`--limit` (детали — `docs/tasks/p2-wpt-runner-throughput.md`); на срезе `--offset 0 --limit 600` (353 отобранных id) `--processes=6` дал ~14× ускорение (~7.7 id/с против ~0.55 id/с последовательной пробы), что пересматривает оценку «сутки» до порядка ~75 минут чистого времени `wptrunner` на весь корпус — не гарантия, ещё не измерено на полном `css/`. **`WPT-RUN-3` срез 2 (2026-08-02, массовый прогон + `.ini` + группировка на `css/CSS2`)**: `run_report.py --all --root css/CSS2 --recursive --processes=6` отобрал по файловой системе 805 id, но манифест `wptrunner` признаёт `testharness`-тестами лишь **63** — подавляющее большинство `css/CSS2` (742 id) это reftest/crashtest-пары, для которых у `browsers/lumen.py` нет исполнителя (`TEST-4`, тот же класс гэпа, что `cssom`/`compat`), не единый недосчёт, а структурный состав категории (историческая CSS WG практика — визуальные reftest'ы, не testharness-ассерты). Из 63: 53 harness OK, 10 TIMEOUT, 1206/2454 сабтестов passed. Полная поимённая триаж (не только по числам, как в пробе WPT-RUN-1) даёт 8 новых кластеров + 2 уже известных: **[BUG-463](../bugs/BUG-463-OPEN.md)** WAAPI `animate`/`getAnimations` не на `Element.prototype` → ложноотрицательный feature-detect (428 сабтестов/7 файлов); **[BUG-464](../bugs/BUG-464-OPEN.md)** `document.elementFromPoint`/`elementsFromPoint` отсутствуют вовсе (10 файлов, 2 из них TIMEOUT); **[BUG-465](../bugs/BUG-465-OPEN.md)** цвет не сериализуется в канонический `rgb()` (904/1192 сабтестов одного файла — крупнейший кластер среза); **[BUG-466](../bugs/BUG-466-OPEN.md)** margin collapsing "через" пустой блок не реализован (72/72, формализует пробу WPT-RUN-1); **[BUG-467](../bugs/BUG-467-OPEN.md)** `document.fonts.ready` отсутствует → TIMEOUT (2 файла); **[BUG-468](../bugs/BUG-468-OPEN.md)** процентный margin/padding не пересчитывается после JS-мутации `container.style.width` (8 файлов); **[BUG-469](../bugs/BUG-469-OPEN.md)** нулевой-ширины блок между float'ами не попадает в нулевой-ширины щель (4 файла, формализует пробу WPT-RUN-1); **[BUG-470](../bugs/BUG-470-OPEN.md)** `getComputedStyle()` не отражает `float`/`clear` (4 сабтеста). Ещё 13 файлов объяснены уже открытыми находками, не задвоены: **[BUG-384](../bugs/BUG-384-OPEN.md)** named access on Window (10 файлов, 3 из них TIMEOUT — падение случается в верхнеуровневом `<script>` до регистрации теста) и **[BUG-360](../bugs/BUG-360-FIXED.md)** мёртвый `onload=`-атрибут (2 файла TIMEOUT). Committed `.ini` под `tests/wpt/metadata/css/CSS2/` для всех 58 классифицированных файлов (`expected: FAIL`/`TIMEOUT`, шапка с ссылкой на `BUG-NNN`) — `run_suite.py`-подобный прогон подтверждён 0 unexpected на них. 5 файлов остались без `.ini` (не кластеризованы намеренно, не молчаливый пропуск): `normal-flow/unresolvable-{min,max}-height.html` (процентный `min/max-height` против неопределённой высоты), `normal-flow/auto-margins-root-element.html` (похоже на `window.innerWidth` = NaN в этой среде, не обязательно layout-баг), `positioning/line-break-after-leading-float.html` (текст после leading float не сдвигается на `text-indent`), `linebox/inline-negative-margin-001.html` (гонка `checkLayout()` с асинхронной загрузкой `Ahem` — тот же класс, что в строке `fonts` ниже, не новый гэп). Остаток DoD — полный прогон остальных ~34500 id `css/` (не только `CSS2`), продолжение триажа непокрытых `testharness`-тестов — задел для среза 3. **`WPT-RUN-3` срез 3 (2026-08-02, массовый прогон + `.ini` + группировка на `css/cssom`, 226 файловых id → 188 `testharness`)**: первый срез внутри задела «остальные ~34500 id `css/`» — не вся оставшаяся масса, одна поддиректория (`cssom`, CSSOM Object Model), тем же паттерном, что срез 2 у `CSS2`. `run_report.py --all --root css/cssom --recursive --processes=6`: 151/188 harness OK, 727/1448 сабтестов passed, 178 файлов с хотя бы одним непройденным сабтестом. Первый прогон обнаружил и исправил дефект самого триажного инструментария: свободнотекстовый разбор вывода `wptrunner`'s `mach`-форматтера (`" - "` как разделитель имени сабтеста и причины) обрезает имена, содержащие собственный `" - "` (например `"CSSOM - CSSRuleList interface"` → ошибочно `"CSSOM"`), и не экранирует буквальные `\n`/`\t` внутри имён (`css/cssom/getComputedStyle-pseudo-with-argument.html`, сабтесты с whitespace-вариантами `::highlight(...)`) — оба класса ломают `wptmanifest`-парсер `.ini` или дают ложные unexpected. Источник переключён на структурированный NDJSON (`wptrunner --log-raw=...`, `action: test_status`/`test_end` с точными полями `test`/`subtest`/`status`), а экранирование — на переиспользованную `tools/wptrunner/wptrunner/wptmanifest/serializer.py::escape()` вместо самодельного (та же категория гочи, что уже задокументирована для `[`/`]` в срезе 2, но шире). Полная поимённая триаж 178 файлов (не оценка по частоте строк): **[BUG-471](../bugs/BUG-471-OPEN.md)** — CSSOM-модель таблиц стилей/правил (`document.styleSheets`, `.sheet`, `CSSStyleSheet`, `CSSRule`-иерархия, `insertRule`/`deleteRule`, `MediaList`) не подключена к шиму вовсе, крупнейший кластер, 97 файлов (подтверждено статическим сканом исходников тестов на использование этих символов, не оценкой по частоте строк в логе); **[BUG-472](../bugs/BUG-472-OPEN.md)** — `getComputedStyle()` резолвит по фиксированной ~64-свойственной карте (`computed_style_to_map`), заполняемой один раз за проход layout, вне списка — тихий `""`, 37 файлов (used-value insets, `min-*:auto`→`0px`, псевдоэлементы, пользовательские свойства, числовой `line-height`); **[BUG-473](../bugs/BUG-473-OPEN.md)** — `CSSStyleDeclaration.cssText` не схлопывает лонгхенды в шорткод и не ставит завершающий `;`, 32 файла; **[BUG-474](../bugs/BUG-474-OPEN.md)** — `document.caretRangeFromPoint` отсутствует вовсе (не путать с `caretPositionFromPoint`, который есть, но Phase-0 заглушкой), 2 файла. Ещё 3 файла объяснены уже открытыми находками: **[BUG-384](../bugs/BUG-384-OPEN.md)** named access on Window (2 файла) и **[BUG-462](../bugs/BUG-462-OPEN.md)** `Node.prototype.contains` отсутствует (1 файл). Committed `.ini` под `tests/wpt/metadata/css/cssom/` для всех 171 классифицированного файла — `run_suite.py`-подобный прогон подтверждён **0 unexpected** на 1476 проверках (171 тест / 1305 сабтестов). 7 файлов остались без `.ini` (не кластеризованы намеренно): `cssstyledeclaration-csstext-setter.window.html` (`.window.js`-двойной-глобал репортит буквально дублирующиеся имена сабтестов — `wptrunner` сам валит гарнес `ERROR` на этой дубликации до сравнения с ожиданиями, не наша находка), `caretRangeFromPoint-replace-document.tentative.html` (`document.replaceChild is not a function` — единичное наблюдение, живой `document` в принципе не несёт Node-методов, но обобщать по одному файлу не решились), `caretPositionFromPoint-with-transformation.html` (TIMEOUT, сценарий с `<iframe>` — вероятно упирается в multi-window ограничение минимального исполнителя, не проверено отдельно), `base-uri.html` (падения на `KeyframeEffect`/Web Animations, не про base URI — неясная причина), `CSS-namespace-object-class-string.html` (`@@toStringTag` на `CSS`-неймспейс-объекте), `MutationObserver-style.html` (двойной вызов callback), `style-attr-update-across-documents.html` (TIMEOUT, перенос узла между двумя документами). Остаток DoD — та же процедура на оставшихся ~34300 id `css/` (все `css-*`-модули, `cssom-view`, `selectors`, и т.д.) — задел для среза 4. **Срез 4 (2026-08-02) готов на `css/cssom-view`**: 235 файлов, 208 testharness id, 169/208 harness OK, 469/1694 сабтестов. Поимённая триаж всех 178 непройденных файлов → 8 новых `BUG-NNN` (BUG-475 `scrollWidth`/`scrollHeight`/`scrollTop`/`scrollLeft` = 0 вне `overflow:scroll/auto`, крупнейший кластер ~600 сабтестов; BUG-476 `offsetLeft`/`offsetTop` — viewport-абсолютные координаты вместо offsetParent-relative; BUG-477 `elementFromPoint`/`elementsFromPoint` отсутствуют — на JS-стороне нет hit-test'а «точка → узел» вовсе; BUG-478 `getClientRects`/`getBoxQuads` отсутствуют на `Element`; BUG-479 скролл-API — нет алиаса `scroll()`, `scrollIntoView` игнорирует опции, ни один метод не возвращает `Promise`; BUG-480 `<iframe>` без отдельного browsing context — `contentWindow`/`contentDocument` отсутствуют вовсе; BUG-481 `window.visualViewport` отсутствует; BUG-482 `offsetParent`/`document.scrollingElement` отсутствуют + `document.compatMode` — константа) + 6 файлов объяснены уже открытыми BUG-384 (named access on Window, крупный вторичный кластер — 25 файлов) /BUG-462/BUG-368/BUG-472. Committed `.ini` на 113/178 файлов под `tests/wpt/metadata/css/cssom-view/`, прогон подтверждает **0 unexpected passes** (66 unexpected results остаются на непокрытых файлах — ожидаемо, `.ini` для них не заведён). 9 файлов пропущены как невендоренная зависимость (`/dom/events/scrolling/scroll_support.js` — `waitForCompositorReady`/`waitForCompositorCommit`, тот же класс гэпа, что `check-layout-th.js`/`ahem.css`). **56 файлов намеренно оставлены без `.ini`** — заметно больше, чем в срезах 2/3 (5-7 файлов): `cssom-view` несёт непропорционально много мелких, взаимно-независимых геометрических гэпов (getBoundingClientRect под zoom/scroll/SVG, screen.*/screenLeft/Top, resizeTo, inline-элементы в offsetTopLeft/offsetParent с точным нулём вместо неверного числа — вероятно отдельный, не подтверждённый в этом срезе гэп на стыке с BUG-476, `idlharness.html`) — не форсировались в один из 8 багов, чтобы не давать недостоверную атрибуцию; задел на будущий срез cssom-view-2 или отдельные точечные баги. Остаток DoD — та же процедура на `css-*`-модулях/`selectors`/прочем `css/` — задел для среза 5 **Срез 5 (2026-08-02) начат на `css/css-box`**: 176 файлов, 83 testharness id, 42/83 harness OK, 671/957 сабтестов. Поимённая триаж всех 65 непройденных файлов → 2 новых `BUG-NNN` + 3 уже открытых. **[BUG-483](../bugs/BUG-483-OPEN.md)** — `window.getComputedStyle()` возвращает `Proxy` без `has`-ловушки, поэтому `property in getComputedStyle(el)` — первая проверка в хелпере `computed-testcommon.js` — даёт `false` для ЛЮБОГО свойства независимо от реальной поддержки (подтверждено `--mcp-live-port`-пробой: `getPropertyValue` отдаёт верное значение, `in`-проверка всё равно `false`); универсальная находка — тем же хелпером пользуются 456 файлов только в `css/`, не только `css-box`. 7 файлов/79 сабтестов этого среза падают исключительно на этом assert (`clear-computed.html`, `float-computed.html`, `margin-computed.html`, `padding-computed.html`, `margin-trim-computed.html`, `visibility-computed.html`, `inheritance.html`). **[BUG-484](../bugs/BUG-484-OPEN.md)** — инлайновый `element.style`-сеттер (`_lumen_make_style`, `dom.rs:4264`) никогда не пропускает значение через реальный CSS-парсер: невалидные значения принимаются вместо отклонения (10 файлов `*-invalid.html`, 77 сабтестов), шорткоды `margin`/`padding` не разворачиваются в лонгхенды (`margin-shorthand.html`/`padding-shorthand.html`, 32 сабтеста), многотокенные/`calc()`-значения не канонизируются при сериализации (`margin-trim.html`/`padding-valid.html`, 24 сабтеста) — 14 файлов/133 сабтеста суммарно. 41 файл (весь `margin-trim/`, TIMEOUT) объяснён уже открытым **[BUG-360](../bugs/BUG-360-FIXED.md)** (мёртвый `onload=` — все используют `<body onload="checkLayout(...)">`, `checkLayout` никогда не вызывается, `test()` не регистрируется, харнесс висит до глобального таймаута с нулём сабтестов). 2 файла `animation/margin-interpolation.html`/`padding-interpolation.html` (72 сабтеста) объяснены уже открытым **[BUG-463](../bugs/BUG-463-OPEN.md)** (WAAPI `animate` не на `Element.prototype`, тот же хелпер `interpolation-testcommon.js`, тот же текст ошибки). Committed `.ini` под `tests/wpt/metadata/css/css-box/` для 62 из 65 непройденных файлов, повторный прогон подтверждает ровно **3 unexpected, 0 unexpected passes** (65 − 62 = 3, совпадает с ожиданием). 1 файл остался без `.ini` намеренно: `margin-trim/computed-margin-values/flexbox-column-style-change-triggers-layout-block-start.html` (2 сабтеста, `offsetTop` ожидается 18/78, получено 0 — премис теста завязан на `margin-trim`, но нулевые (не просто неверные) офсеты у обоих item'ов не сводятся уверенно ни к BUG-483, ни к BUG-484, ни к отсутствию `margin-trim`; нужен отдельный разбор после того как margin-trim получит хоть какую-то реализацию). Остаток DoD — продолжение той же процедуры на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 6. **Срез 6 (2026-08-02) на `css/css-cascade`**: 96 testharness id, 59/96 harness OK, 11/469 сабтестов. Поимённая триаж всех 95 непройденных файлов → 3 новых `BUG-NNN` + расширены 4 уже открытых. **[BUG-485](../bugs/BUG-485-OPEN.md)** — `document.head` отсутствует вовсе: ни геттера на живом `document`, ни нативного `_lumen_get_head` в обоих движках (только `_lumen_get_body` есть) — `document.head.appendChild`/`.append(...)`, самый частый паттерн внедрения `<style>` в WPT, бросает `TypeError` на `undefined`. 15 файлов этого среза (7 TIMEOUT — throw случается до регистрации первого `test()`, 8 FAIL/rejected-promise — throw внутри `promise_test()`); `grep -rl 'document\.head\.' tests/wpt/css` → минимум 84 файла во всём `css/`. **[BUG-486](../bugs/BUG-486-OPEN.md)** — `document.currentScript` отсутствует вовсе (`grep` — ноль совпадений), резолвится в `undefined`; общий хелпер `test_scope(document.currentScript, ...)` в `scope-evaluation.html`/`scope-invalidation.html`/`scope-proximity.html` (3 файла/55 сабтестов) читает `script_element.previousElementSibling`, бросая `TypeError` на `undefined`. **[BUG-487](../bugs/BUG-487-OPEN.md)** — CSS-wide keyword `revert-rule` (CSS Cascade L5) не реализован вовсе в `css-parser` (сосед `revert-layer` работает) — декларация не откатывается к предыдущему правилу каскада. 4 файла чисто + 2 смешанных с BUG-384 (`revert-rule-important.html`/`revert-rule-shadow.html`), суммарно 6 файлов/18 сабтестов этого бага. Расширения: **[BUG-384](../bugs/BUG-384-OPEN.md)** named access on Window — крупнейший кластер среза, 36 файлов (включая `scope-deep.html`, поймано отдельно — бросает на верхнеуровневом `main.append(...)` до регистрации первого `test()`, TIMEOUT); **[BUG-471](../bugs/BUG-471-OPEN.md)** CSSOM-модель — +4 файла (`layer-stylesheet-multi-adoption.html`/`layer-replaceSync-clears-stale.html` — `new CSSStyleSheet()` отсутствует; `layer-rules-cssom.html` — `CSSLayerBlockRule`/`CSSLayerStatementRule` отсутствуют; `unset-value-storage.html` — `document.styleSheets[0]` на `undefined`); **[BUG-483](../bugs/BUG-483-OPEN.md)** — второй симптом того же стаба: Proxy `getComputedStyle()` не имеет `Symbol.iterator`/реального `length`/`item()`, `for (let p of style)` бросает синхронно до регистрации теста — `all-prop-revert-layer.html`+`all-prop-revert-layer-noop.html`(×8 вариантов `?include=N`)+`all-prop-revert-noop.html`(×8) = 17 файлов/id, все TIMEOUT; **[BUG-484](../bugs/BUG-484-OPEN.md)** — +1 файл (`parsing/all-invalid.html`, 7 сабтестов, инлайновый `style`-сеттер не отклоняет невалидные значения шорткода `all`). Committed `.ini` под `tests/wpt/metadata/css/css-cascade/` на 70/95 непройденных файлов, повторный прогон подтверждает **0 unexpected passes** (наблюдался разброс числа unexpected между запусками — 13–21 — из-за флейкующих таймингов на непокрытых файлах, не из-за неверной атрибуции покрытых). **12 файлов оставлены без атрибуции**: `idlharness.html` (невендоренный `/interfaces/*.idl` — инфраструктурный гэп уровня WPT-RUN-1, не движковый баг, `/interfaces/` пуст — 0 файлов при полном вендоринге `css/`); `scope-implicit-external.html` (TIMEOUT, `<link>`/`@import` внешний stylesheet не долетает до события `load` — не подтверждено уверенно, кандидат на будущий баг); 5 файлов с одинаковым паттерном «`@scope` внутри `@container`/`@supports`/`@layer`/`@media`/`@starting-style` не скоупит» (`scope-container.html`/`scope-supports.html`/`scope-layer.html`/`scope-media.html`/`scope-starting-style.html`) — живая `--mcp-live-port`-проба того же CSS дала другой результат (`auto`, а не `""`), чем прогон wptrunner, воспроизвести расхождение уверенно не удалось за отведённое время, оставлено на будущий срез; `inherit-initial.html` (4 сабтеста — `z-index`/`position`/`overflow`/`background-color`:`inherit` на `document.documentElement` даёт `""` вместо computed-значения, хотя все четыре свойства есть в `computed_style_to_map` — причина не установлена уверенно); `presentational-hints-rollback.html` (8 сабтестов, взаимодействие presentational hints/`revert`/`revert-layer`/`@layer`, многофакторный тест); `revert-val-003.html`/`revert-layer-008.html` (по 1 сабтесту, `revert`/`revert-layer` + CSS transitions). Остаток DoD — продолжение той же процедуры на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 7.  **Срез 7 (2026-08-02) на `css/css-display`**: 33 testharness id, 28/33 harness OK, 111/428 сабтестов. Поимённая триаж всех 28 непройденных файлов → 3 новых `BUG-NNN` + расширены 5 уже открытых. **[BUG-488](../bugs/BUG-488-OPEN.md)** — самая широкая по охвату находка WPT-RUN-3 на сегодня: `getComputedStyle()`/`getBoundingClientRect()` пусты/нулевые для ЛЮБОГО inline-элемента (`<span>`, `<a>`, `<em>`, …) независимо от содержимого — inline-контент группируется в `InlineRun`/`InlineBlockRow`-боксы по `NodeId` содержащего блока, собственный `NodeId` inline-элемента нигде не сохраняется; изолировано минимальной живой пробой (`--mcp-port`) независимо от `display:contents` — первая гипотеза (гэп специфичен для `display:contents`-потомков) была опровергнута прямым экспериментом. **[BUG-489](../bugs/BUG-489-OPEN.md)** — `getComputedStyle()` пуст для самого элемента `display:contents` (не его потомков — это BUG-488): `flatten_contents` убирает его бокс из дерева целиком без замены, `collect_computed_styles`/`collect_layout_rects` — чистые обходчики box-дерева. **[BUG-490](../bugs/BUG-490-OPEN.md)** — `getComputedStyle(el, pseudoElt)` полностью игнорирует второй аргумент (задокументировано в коде шима как «not yet supported»), запрос `::before`/`::after`/`::first-line`/`::first-letter` отдаёт стиль самого `el`; расходится по конкретному наблюдаемому значению между живой пробой (`"block"`) и прогоном wptrunner (`""`) — не разобрано до конца. Расширения: **[BUG-483](../bugs/BUG-483-OPEN.md)** (Proxy без `has`-ловушки) — 5 файлов/135 сабтестов (`inheritance.html`, `reading-flow/reading-order`-computed, `parsing/display-computed.html` ×2); **[BUG-484](../bugs/BUG-484-OPEN.md)** (инлайновый `style`-сеттер не валидирует/не канонизирует) — 5 файлов/146 сабтестов (`parsing/display-{valid,invalid}.html` ×3, `reading-flow/reading-order-invalid.html` ×2); **[BUG-384](../bugs/BUG-384-OPEN.md)** (named access on Window) — 2 файла; **[BUG-470](../bugs/BUG-470-OPEN.md)** (`float`/`clear` отсутствуют в computed style) — 2 файла; **[BUG-462](../bugs/BUG-462-OPEN.md)**, **[BUG-463](../bugs/BUG-463-OPEN.md)**, **[BUG-467](../bugs/BUG-467-OPEN.md)** — по 1-2 файла. Committed `.ini` под `tests/wpt/metadata/css/css-display/` на 25/28 непройденных файлов, повторный прогон подтверждает **0 unexpected passes** (4 unexpected остаются на 3 намеренно некластеризованных файлах). **3 файла оставлены без атрибуции**: `textarea-display.html` (живая проба через `--mcp-port` дала верное значение `"inline-block"`, прогон wptrunner — пустую строку; то же расхождение живая-проба-vs-wptrunner, что у BUG-490, не разобрано до конца — вероятная гипотеза: разница между локальной файловой навигацией пробы и HTTP-навигацией через `--bidi-port` у wptrunner); `display-contents-blockify-dynamic.html` (3 сабтеста смешивают минимум два механизма — первый провал — на `<span>`-grid-item ДО применения `display:contents` вообще, значит BUG-488, но следующие два — на уже блочных `<div>`-контейнерах, не объяснённых BUG-488; плюс тест использует HTML с незакрытым `<span>`, полагающийся на HTML5 error-recovery парсера, что дополнительно осложняет анализ); `accessibility/display-contents-role-and-label.html` (TIMEOUT, невендоренный `/wai-aria/scripts/aria-utils.js` — инфраструктурный гэп уровня WPT-RUN-1, не движковый — директории `wai-aria/` в вендоренном дереве нет вовсе). Остаток DoD — продолжение той же процедуры на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 8. **Срез 8 (2026-08-02) готов на `css/css-borders`**: 234 файла, 79 testharness id (остальное reftest/crashtest-пары без исполнителя, тот же структурный класс, что `CSS2`/`cssom-view`), 79/79 harness OK, 234/1553 сабтестов passed. Технический гоча этого среза: наивный текстовый разбор `wptrunner`'s интерлив-стдаута под `--processes=6` **вводит в заблуждение** — `TEST_START`/`TEST_END`/`FAIL`-строки от параллельных worker-процессов перемежаются построчно, так что "первая пара TEST_END + следующие FAIL-строки" может принадлежать РАЗНЫМ тестам (пойман на `box-shadow-offset-invalid.html`: сырой лог показывал `110/241 passed`, реальное значение — `0/4`, число `110/241` принадлежало `corner-shape-valid.html`); источник переключён на структурированный `--log-wptreport` JSON, который `run_report.py` уже строит внутри себя (`render_report`, JSON `results[].subtests[]`, не текстовый стдаут) — читать оттуда, не из `.log`. Поимённая триаж всех 66 непройденных файлов → 4 новых `BUG-NNN` + расширены 5 уже открытых. **[BUG-491](../bugs/BUG-491-OPEN.md)** — CSS Borders and Box Decorations Level 4 (черновая, ещё не CR, спека) целиком не реализована: `corner-shape` и вся его семья лонгхендов/шортхендов, шортхенд `border-shape`, отдельные лонгхенды `box-shadow-{offset,color,blur,spread,position}` (не путать с уже ✅ шортхендом `box-shadow`), парные радиус-шортхенды `border-*-radius`, `border-clip`, ключевое слово `hairline` — 44 файла/~1500 сабтестов, крупнейшая находка среза, подтверждено и `grep`-ом по исходникам (ноль совпадений в `crates/`), и тройным WPT-сигналом (`getComputedStyle()` пуст, `CSS.supports()` лжёт `false`, геометрия `shape-outside`-интеграции не меняется). **[BUG-492](../bugs/BUG-492-OPEN.md)** — `border-image` (CSS Backgrounds and Borders Level **3**, зрелая спека, не путать с BUG-491's L4) не реализован вовсе — отдельная находка, 1 файл (`border-image-width-interpolation-math-functions.html`). **[BUG-493](../bugs/BUG-493-OPEN.md)** — `getComputedStyle()` читает закешированный снапшот (`update_computed_styles`), который не форсируется синхронно перед чтением: узел, созданный+стилизованный+добавленный в течение ОДНОГО скрипт-тика, ещё не имеет записи в снапшоте вовсе (не просто устаревшей — отсутствующей), подтверждено живой `--mcp-port`-пробой (два `eval()`-вызова через границу релейаута DEVX-9 дают верный результат, один — пустой); 1 файл этого среза (частично, `outline-width`-половина `border-width-rounding.tentative.html`). **[BUG-494](../bugs/BUG-494-OPEN.md)** — `element.style = "css text"` (присвоение строки целиком) — молчаливый no-op: у `.style` в JS-шиме есть только геттер, нет сеттера/`[PutForwards=cssText]`-переадресации на `.style.cssText`; подтверждено живой пробой (`getAttribute('style')` остаётся `null` после присвоения); не даёт нового покрытия `.ini` этого среза (тот же файл уже красный из-за BUG-493), заведён отдельно как самостоятельный, ранее не задокументированный гэп. Расширения: **[BUG-484](../bugs/BUG-484-OPEN.md)** (инлайновый `style`-сеттер никогда не парсит/не валидирует/не канонизирует — тот же стаб, что нашёл BUG-483's сосед в срезе 5) — крупнейшее расширение среза, 25 файлов/~460 сабтестов (все `*-invalid.html` этого среза плюс частично `*-valid.html`, где WPT случайно совпал raw-echo с каноническим значением на 13 файлах, давшим ложный полный PASS без реального парсинга); **[BUG-477](../bugs/BUG-477-OPEN.md)** (`elementFromPoint`/`elementsFromPoint` отсутствуют) — 4 файла; **[BUG-463](../bugs/BUG-463-OPEN.md)** (WAAPI `animate` не на прототипе) — 3 файла, тот же хелпер `interpolation-testcommon.js`, что срезы 2/5; **[BUG-472](../bugs/BUG-472-OPEN.md)** (`getComputedStyle()` — фиксированная карта, шортхенды/`outline-offset` вне списка) — 2 файла. Committed `.ini` на 60 из 66 непройденных файлов (1309 сабтестов), повторный прогон **из самого worktree** (не из корневого чекаута — критично: `run_report.py`, запущенный из корня, резолвит `--metadata` в корневой `tests/wpt/metadata/`, где этих `.ini` ещё нет, и тогда `wptrunner`'s "Got N unexpected results" считает файлы, а не сабтесты, давая обманчивое число 66/79 вместо истинных 234/1553 — тот же класс "два чекаута разошлись", что `reference_wpt_run_report_invocation_recipe` уже предупреждает для бинарника, здесь — для `.ini`) подтверждает **7 unexpected, 0 unexpected passes**: 6 файлов оставлены полностью без атрибуции (`border-shape-circle-hit-test*.html` ×4, `border-shape-hit-test-overflow.html`, `border-shape-replaced-hit-test.html` — все падают на `test_driver.Actions is not a constructor`, невендоренный `/resources/testdriver-actions.js`, уже занесённый в задел `WPT-RUN-2` в `docs/tasks/p2-wpt-runner-throughput.md`, инфраструктурный гэп, не движковый) плюс 2 сабтеста внутри `border-shape-stacking-context-hit-test.html` (тот же `Actions`-гэп — 4 из 6 сабтестов этого файла покрыты `.ini` под BUG-477, 2 оставлены). 13 файлов не нуждались в `.ini` — прошли полностью (WPT-автор случайно написал уже канонический литерал как входное значение, раскрыт эффект BUG-484 без реального парсинга). Остаток DoD — продолжение той же процедуры на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 9. **Срез 9 (2026-08-02) готов на `css/css-backgrounds`**: 121 `testharness`-тестов, 119/121 harness OK, 2654/6105 сабтестов; поимённая триаж 100 непройденных файлов + 2 harness TIMEOUT → 3 новых BUG-NNN и расширение 6 уже открытых, крупнейший срез трека по числу переклассифицированных сабтестов. **[BUG-495](../bugs/BUG-495-OPEN.md)** — `background-position-x`/`background-position-y` (отдельные лонгхенды CSS Backgrounds/Borders L4) не реализованы вовсе: `CSS.supports()` тоже лжёт `false` (не просто гэп карты computed style, как у BUG-472) — 5 файлов/214 сабтестов. **[BUG-496](../bugs/BUG-496-OPEN.md)** — `HTMLElement.dataset` (DOMStringMap) отсутствует вовсе, `grep` ноль совпадений; скрипт падает на первом же `element.dataset.x` до регистрации любого `test()`, харнес зависает TIMEOUT — 1 файл (`border-width-cssom.html`), потенциально широкий по охвату гэп вне этого среза. **[BUG-497](../bugs/BUG-497-OPEN.md)** — `CSSStyleDeclaration.cssText` не добавляет завершающую `;` после (единственной) декларации — 1 сабтест. Расширения: **[BUG-492](../bugs/BUG-492-OPEN.md)** (border-image) — оказалось, что настоящий upstream-дом этого свойства это `css/css-backgrounds`, а не `css/css-borders` среза 8 (там был один случайный interpolation-файл): 25 файлов/1646 сабтестов, включая самый большой падающий файл всего трека на сегодня (`border-image-width-interpolation.html`, 558 сабтестов). **[BUG-463](../bugs/BUG-463-OPEN.md)** (WAAPI `animate` не на прототипе) — 16 файлов/952 сабтеста, весь `animations/`-поддиректорий категории. **[BUG-484](../bugs/BUG-484-OPEN.md)** (инлайновый `style`-сеттер не парсит/не валидирует/не канонизирует) — 34 файла/~300 сабтестов, крупнейшее расширение среза по числу файлов; новая грань того же дефекта — тот же Proxy лишён `Symbol.iterator`/`length` (`webkit-border-radius-valid.html`, `"style is not iterable"`), тот же класс missing-trap, что BUG-483 уже документирует на соседнем (computed-style) Proxy. **[BUG-472](../bugs/BUG-472-OPEN.md)** (`getComputedStyle()` — фиксированная карта) — 25 файлов/~324 сабтеста; новое наблюдение — гэп задевает даже физические лонгхенды (`border-top-color`/`-width`/`-style`/`-left-radius`), не только шорткоды, как предполагалось на срезе 3. **[BUG-384](../bugs/BUG-384-OPEN.md)** (named access на Window) — ещё один harness TIMEOUT (`inheritance.sub.html`, голый идентификатор `container` до первого `test()`). **[BUG-494](../bugs/BUG-494-OPEN.md)** (`element.style = "текст"` no-op) — первый файл, где это единственная и полная причина всех сабтестов, а не сопутствующий фактор (`background-shorthand-serialization.html`, 11/11). Гоча этого среза, не движковая: `BUGS.md` не содержал строк таблицы для BUG-491…494 (файлы заведены в срезе 8, таблица — забыта); исправлено задним числом отдельным коммитом в начале среза 9. Committed `.ini` на 99 из 102 непройденных файлов (100 с провалившимися сабтестами + 2 TIMEOUT), повторный прогон **из самого worktree** подтверждает на полном 121-id срезе (не только на `.ini`-покрытых файлах) **3 unexpected, 0 unexpected passes** (6223/6226 expected) — заметно чище предыдущих срезов. 3 файла оставлены полностью без атрибуции: `background-color-transition-colormix.html` (`ReferenceError: waitForNextFrame is not defined` — невендоренный `/web-animations/testcommon.js`, тот же класс инфраструктурного гэпа, что `WPT-RUN-1`/`WPT-RUN-2`, не движковый), `border-width-transition-with-style-change.html` (`Should have 4 transitions (one per side) expected 4 but got 0` — причина не разобрана до конца), `background-image-cors-no-reload.html` (CORS/`var()`-повторная загрузка фонового изображения — причина не разобрана). **WPT-RUN-3 срез 10 (`css/css-variables`, 2026-08-02)**: 49 файлов с непройденными сабтестами из 59 harness-OK файлов (273 файла в манифесте, 264 test id, 100/516 сабтестов passed). Доминирующий кластер — **[BUG-493](../bugs/BUG-493-OPEN.md)** (расширен, decisively confirmed живой A/B-пробой `--mcp-port`: `getComputedStyle()` в синхронном top-level `<script>`, идущем сразу за статичной разметкой, всегда читает пустой кэш — кэш `computed_styles` заполняется только ПОСЛЕ `navigate()`, а не в ответ на сам вызов; `document.documentElement.offsetHeight` эту гочу не форсирует) — затрагивает ~20 файлов. **[BUG-499](../bugs/BUG-499-OPEN.md)** (новый) — `getComputedStyle().getPropertyValue('--custom-prop')` всегда возвращает `""`: `computed_style_to_map` вообще не сериализует `ComputedStyle::custom_props`, независимо от свежести кэша — 10+ файлов. **[BUG-472](../bugs/BUG-472-OPEN.md)** (расширен) — новые недостающие ключи карты: `border-spacing`, `box-shadow`, `text-shadow`, `perspective-origin`, `transition-duration`, весь `background-attachment/-clip/-image/-origin/-position/-repeat/-size` (кроме `-color`), ~40 SVG presentation-свойств, шорткоды `margin`/`padding`/`border`/`border-radius`. **[BUG-484](../bugs/BUG-484-OPEN.md)** (расширен) — четвёртая форма того же гэпа: `cssText`-геттер — сырой passthrough HTML-атрибута `style=`, без дедупликации по свойству, без вырезания комментариев, без раскрытия шорткод↔лонгхенд; плюс невалидный `var()`-синтаксис принимается инлайн-сеттером без проверки. **[BUG-471](../bugs/BUG-471-OPEN.md)** (расширен) — 2 новых сабтеста (`document.styleSheets`/`.sheet` не реализованы). **[BUG-384](../bugs/BUG-384-OPEN.md)** (расширен) — 5 новых файлов named-access-масок. **[BUG-360](../bugs/BUG-360-FIXED.md)** (расширен) — ещё один `onload=` мёртвый обработчик, TIMEOUT с нулём сабтестов. Новые баги: **[BUG-500](../bugs/BUG-500-OPEN.md)** (`ident()` CSS Values L5 не реализован, замаскирован BUG-384), **[BUG-501](../bugs/BUG-501-OPEN.md)** (`CSS.supports()` — однoаргументная форма без спекового фоллбека «обернуть в скобки», кастомные свойства никогда не «поддержаны»), **[BUG-502](../bugs/BUG-502-OPEN.md)** (`sign()`/`round()`/`mod()`/`rem()`/`abs()` в `calc()` не реализованы вовсе), **[BUG-503](../bugs/BUG-503-OPEN.md)** (`animationend`/предположительно `transitionend` никогда не срабатывают для реальной анимации — 9 файлов TIMEOUT/NOTRUN). 2 файла оставлены без атрибуции (`variable-animation-guaranteed-invalid.html` — checkLayout даёт width 0 повсеместно, механизм не разобран; `variable-reference-refresh.html` — TIMEOUT, iframe `load`-событие предположительно не срабатывает). Повторный прогон **из самого worktree** подтверждает **0 unexpected passes**. Остаток DoD — продолжение той же процедуры на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 11. **Срез 11 (2026-08-02) готов на `css/css-overflow`**: 197 testharness id, 157/197 harness OK, 194/958 сабтестов; поимённая триаж 40 непройденных файлов (harness TIMEOUT/ERROR) + 143 файлов с частичными провалами → 2 новых [BUG-504](../bugs/BUG-504-OPEN.md) (scrollable-overflow `content_width`/`content_height` игнорирует CSS `transform` — подтверждено чтением кода и живой пробой, 20 файлов) и [BUG-505](../bugs/BUG-505-OPEN.md) (CSS Overflow L3-L5 свойства/псевдоэлементы `text-overflow`/`line-clamp`/`block-ellipsis`/`overflow-clip-margin`/`scroll-axis-lock`/`scrollbar-gutter`/`-webkit-box`/`::scroll-marker`/`::scroll-button()`/`scroll-target-group` практически не реализованы, 60 файлов) + расширены [BUG-360](../bugs/BUG-360-FIXED.md) (крупнейшее расширение — весь `check-layout-th.js`-протокол `<body onload=…>` мёртв на этой категории, 25 файлов; диагностировано живой `--mcp-live-port`-пробой), [BUG-384](../bugs/BUG-384-OPEN.md) (+18 файлов), [BUG-477](../bugs/BUG-477-OPEN.md) (+5 файлов). Committed `.ini` на 128/183 непройденных файлов, повторный прогон подтверждает 0 unexpected passes. 55 файлов намеренно без атрибуции: 38 на известном гэпе исполнителя `test_driver.Actions` (WPT-RUN-2, «click» — единственное реализованное действие), 12 на невендоренном `/dom/events/scrolling/scroll_support.js` (тот же класс, что WPT-RUN-1), 5 индивидуальных без ясной причины (`hit-test-stacking-context-parent-border-radius.html`, `keyboard-scroll.html` — `elementDocument.contains is not a function`, `overflow-clip-margin-intersection-observer.html`, `scroll-overflow-padding-block-001.html` — harness ERROR при 6/6 пройденных сабтестах, `scrollable-overflow-fixed-positioned-initial-containing-block.html`). Детали — этот файл, строка `css`. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 12. **Срез 12 (2026-08-02) готов на `css/css-logical`**: 98 файлов, 86 testharness id, 73/86 harness OK, 242/808 сабтестов; поимённая триаж всех 80 непройденных файлов (67 с провалом сабтестов + 13 TIMEOUT) → 1 новый [BUG-506](bugs/BUG-506-OPEN.md) (внешний `<script src="../css-animations/support/testcommon.js">`, путь через границу каталога, не выполняется до зависимого инлайн-кода под реальным wptrunner-конвейером — воспроизводимо повторным прогоном, но ручная `--mcp-live-port`-проба той же страницы отдельным `eval()` показывает хелпер корректно определённым; механизм не изолирован до конца, 5 файлов/57 сабтестов) + расширены 7 уже открытых: [BUG-472](bugs/BUG-472-OPEN.md) (крупнейшее расширение — прямым чтением `selector_query.rs` подтверждено, что вся семья CSS Logical Properties отсутствует в `computed_style_to_map` целиком, ни одного совпадения на логические имена при том, что физические properties, в которые они резолвятся, в карте есть и корректно вычисляются; 19 файлов/~180 сабтестов), [BUG-484](bugs/BUG-484-OPEN.md) (крупнейшее расширение — все три формы гэпа инлайнового `style`-сеттера повторяются на логических свойствах; 32 файла/~280 сабтестов, весь `parsing/`-поддиректорий), [BUG-446](bugs/BUG-446-OPEN.md) (первое массовое подтверждение предсказанного охвата — граф модулей `<script type="module"> import` не подгружается; 11 файлов, весь `logical-box-*`/`logical-values-*` кластер, TIMEOUT), [BUG-493](bugs/BUG-493-OPEN.md) (расширен на `offsetWidth`/`clientWidth` — тот же архитектурный гэп «нет синхронного флаша перед чтением», подтверждено чтением кода `_lumen_get_bounding_rect`/`layout_rects`, не только `getComputedStyle`; 5 файлов/38 сабтестов), [BUG-384](bugs/BUG-384-OPEN.md) (+1 файл, harness-wide TIMEOUT), [BUG-471](bugs/BUG-471-OPEN.md) (+1 файл, `document.styleSheets[0]` — `undefined`), [BUG-463](bugs/BUG-463-OPEN.md) (+6 файлов/38 сабтестов, включая `caption-side-no-interpolation.html` — изначально TIMEOUT в первом прогоне, на повторном и на верификационном прогоне честно завершается с этим же симптомом; TIMEOUT отмечен в `.ini` как вероятный флейк планировщика под `--processes`, не отдельный дефект). Committed `.ini` на 79/80 непройденных файлов, повторный прогон **из самого worktree** подтверждает **0 unexpected / 0 unexpected passes** на полном 86-id срезе (936/936 expected). 1 файл вошёл в атрибуцию BUG-463 уже после первичной триажи (см. выше) — итог 0 файлов оставлено без атрибуции. Детали — docs/wpt-status.md, строка `css`. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 13. **Срез 13 (2026-08-02) готов на `css/css-exclusions`**: 8 файлов, 8 testharness id, 1/8 harness OK, 0/4 сабтестов; поимённая триаж всех 8 непройденных файлов → 1 новый [BUG-507](bugs/BUG-507-OPEN.md) — CSS Exclusions L1 (`wrap-flow`/`wrap-through`) не реализованы вовсе (`grep -rn "wrap-flow\|wrap-through" crates/` — ноль совпадений); 7 файлов падают на `assert_implements` как ERROR-предусловие (0 сабтестов зарегистрировано), `inheritance.html` доходит до harness OK, но все 4 сабтеста FAIL (`getComputedStyle` не отдаёт значение свойства вовсе). Заведено по политике трека, а не как регрессия — Editor's Draft, никогда не шёл ни в одном evergreen-браузере (только экспериментальная реализация в IE10/11). Committed `.ini` на все 8 файлов, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (12/12 expected). 0 файлов оставлено без атрибуции — самая маленькая и полностью закрытая категория трека на сегодня. Детали — docs/wpt-status.md, строка `css`. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 14. **Срез 14 (2026-08-02) готов на `css/css-color-hdr`**: 5 файлов, 4 testharness id, 4/4 harness OK, 13/116 сабтестов; поимённая триаж всех непройденных сабтестов → 1 новый [BUG-508](bugs/BUG-508-OPEN.md) — CSS Color HDR `dynamic-range-limit` не реализовано вовсе, ни парсинга, ни computed style, ни интерполяции/Web Animations (`computed.html` 21/21, `inheritance.html` 2/2, `interpolation.html` 64/64 FAIL) + расширен [BUG-484](bugs/BUG-484-OPEN.md) (`parsing.html`, 16/29 — инлайновый `style`-сеттер принимает невалидные значения `dynamic-range-limit`/`dynamic-range-limit-mix()` без отклонения, тот же паттерн что во всех предыдущих срезах). Committed `.ini` на все 4 непройденных файла, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (116/116 expected). 0 файлов без атрибуции — вторая по счёту полностью закрытая категория трека. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 15. **Срез 15 (2026-08-02) готов на `css/css-easing`**: 12 файлов, 12 testharness id, 12/12 harness OK, 36/156 сабтестов; поимённая триаж всех 120 непройденных сабтестов → 0 новых BUG-NNN, все 120 расходятся по 3 уже открытым багам того же механизма (не только текста сообщения — проверено чтением кода и живой `--mcp-port`-пробой): [BUG-472](bugs/BUG-472-OPEN.md) (расширен — весь `animation-timing-function`/`transition-*` семейство отсутствует в `computed_style_to_map`, `grep` ноль совпадений; 34 сабтеста/2 файла: `timing-functions-syntax-computed.html` 21/21, `linear-timing-functions-syntax.html` 13), [BUG-484](bugs/BUG-484-OPEN.md) (расширен — инлайновый `style`-сеттер не валидирует/не канонизирует `<easing-function>`, плюс новая 4-я форма находки — keyword-алиасы `step-start`/`step-end`/`steps(N, end)` не сворачиваются в канонический `steps(1, start)`/`steps(1)`/`steps(N)`; 40 сабтестов/4 файла), [BUG-493](bugs/BUG-493-OPEN.md) (расширен — самое крупное расширение среза: `offsetWidth`, читаемый синхронно в `<script>` сразу после статичной разметки без предшествующего релейаута, всегда `0`, подтверждено минимальной анимационно-нейтральной живой пробой (`--mcp-port`: `123` после `navigate()`+отдельный `eval()`, но `0` внутри самого синхронного скрипта страницы); плюс новый вариант через WAAPI `Animation.currentTime` + немедленный `getComputedStyle()` в одном тике — в 2 файлах эта же пустая строка дополнительно роняет тест JS-исключением (`testcommon.js::pxToNum` вызывает `"".match(...)[1]` → `Cannot read properties of null`); 46 сабтестов/6 файлов). Committed `.ini` на все 12 непройденных файлов, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (168/168 expected). 0 файлов без атрибуции — третья по счёту полностью закрытая категория трека, и первая, где ни один найденный симптом не потребовал нового BUG-NNN. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 16. **Срез 16 (2026-08-02) готов на `css/css-forms`**: 36 файлов (в основном reftest-пары, вне скоупа `testharness`), 7 testharness id, 7/7 harness OK, 0/65 сабтестов; поимённая триаж всех 65 непройденных сабтестов → 0 новых BUG-NNN, все расходятся по 4 уже открытым багам (вторая категория трека без единого нового бага): [BUG-493](bugs/BUG-493-OPEN.md) (расширен — новая маскирующая грань: `appearance-base-basic.html` сравнивает пять унаследованных свойств между собой, оба через пустой same-tick-кэш, поэтому они совпадают и ложно проходят, а вскрывает баг только шестое сравнение с литералом `"border-box"`; живой `--mcp-live-port` A/B-пробой подтверждено, что post-navigate чтение даёт `"content-box"`, а не пустую строку — расхождение с логом wptrunner полностью объясняется этим багом; 10 сабтестов/1 файл), [BUG-384](bugs/BUG-384-OPEN.md) (расширен — 3 файла: `checkbox-`/`radio-checkmark-animation.html` читают голые `checkbox`/`radio`; `input-text-base-appearance-computed-style.html` — отдельная грань, апстримная опечатка `const intial = …` оставляет корректно написанный `initial` резолвящимся только через named access), [BUG-485](bugs/BUG-485-OPEN.md) (расширен — новая точка входа: общий хелпер `css/support/parsing-testcommon.js::test_valid_selector` делает `document.head.append(style)`, а не только инлайновый код категорий; 21 сабтест/3 файла `parsing/*`), [BUG-391](bugs/BUG-391-OPEN.md) (расширен — новая грань механизма: `querySelector`/`insertRule` не валидируют структуру/аргументы псевдоэлементов вовсе — комбинатор или второй псевдоэлемент после `::checkmark`/`::picker-icon`/`::picker()`, произвольный аргумент `::picker(foo)` вместо только `select` — не бросают `SyntaxError`; подтверждено чтением `crates/engine/css-parser/src/parser.rs:4539-4549` и живой пробой на `::before *`/`::before::before`, не специфично для css-forms; 31 сабтест/3 файла). Committed `.ini` на все 7 непройденных файлов, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (65/65 expected). 0 файлов без атрибуции — четвёртая полностью закрытая категория трека. Остаток DoD — продолжение на прочих `css-*`-модулях/`selectors`/`cssom`-соседях — задел для среза 17. **Срез 17 (2026-08-02) готов на `css/css-font-loading`**: 36 файлов (в основном reftest-пары, вне скоупа testharness), 21 testharness id, 17/21 harness OK, 0/79 сабтестов; поимённая триаж всех 79 непройденных сабтестов → 0 новых BUG-NNN, все расходятся по [BUG-467](bugs/BUG-467-OPEN.md) (расширен из узкого «`document.fonts.ready` не реализован» в «CSS Font Loading API практически не реализована целиком»: глобальные `FontFace`/`FontFaceSet`/`FontFaceSetLoadEvent` отсутствуют вовсе — `new FontFace(...)` бросает `ReferenceError`, `new FontFaceSet([])` тоже бросает `ReferenceError` вместо спекового `TypeError`; `document.fonts` — урезанный read-only снимок только с `length`/`item`/`entries`/`forEach`/`Symbol.iterator`, без `size` (спека требует `size`, не `length`)/`add`/`delete`/`clear`/`has`/`keys`/`load`/`ready`/`addEventListener`; подтверждено чтением `crates/js/src/dom.rs:6771` (`_lumen_get_fonts()`) и грепом `"FontFace"`/`"FontFaceSet"` — ноль совпадений вне комментариев); третья категория трека без единого нового бага. `idlharness.https.html` — отдельно, `TEST_END: ERROR` того же класса, что [BUG-380](bugs/BUG-380-OPEN.md) (browsing-context reuse не проверяет исход навигации). Committed `.ini` на все 21 непройденных файла (2 файла — file-level `expected: TIMEOUT` с посабтестной раскладкой FAIL/TIMEOUT/NOTRUN, `document.fonts.load` бросает синхронно, но промис-цепочка не резолвится вовремя и харнес не успевает добежать до остальных сабтестов), повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (79/79 expected). 0 файлов без атрибуции — пятая полностью закрытая категория трека. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 18. **Срез 18 (2026-08-03) готов на `css/css-syntax`**: 47 файлов, 43 testharness id, 43/43 harness OK, 38/432 сабтестов; поимённая триаж 37 непройденных файлов (394 сабтеста) → 2 новых BUG-NNN (BUG-509 — внешний CSS-фетч (`fetch_stylesheet_text`) всегда декодирует байты как UTF-8 lossy, алгоритм CSS Syntax "determine the fallback encoding" (BOM/`@charset`/HTTP `Content-Type` charset/`<link charset>`/кодировка ссылающегося документа) не реализован вовсе — весь `css/css-syntax/charset/`, 14 файлов/14 сабтестов; BUG-510 — токенизатор пропускает пробелы по Unicode `White_Space` вместо спекового набора из 5 ASCII-символов, U+000B/U+0085 ошибочно работают как разделитель-комбинатор в селекторах, `whitespace.html`, 2 сабтеста) + расширены 6 уже открытых: [BUG-471](bugs/BUG-471-OPEN.md) (крупнейшее расширение — CSSOM-модель, `document.styleSheets[N].cssRules` напрямую, а не через bare-id `.sheet`, 244 сабтеста/11 файлов, включая `urange-parsing.html` 95 и `inclusive-ranges.html` 38), [BUG-499](bugs/BUG-499-OPEN.md) (81 сабтест/2 файла — `getComputedStyle().getPropertyValue('--x')` пуст для кастомных свойств вне зависимости от тайминга), [BUG-384](bugs/BUG-384-OPEN.md) (26 сабтестов/5 файлов — bare-id доступ к `<style id=foo>` как переменной `foo` бросает `ReferenceError` раньше, чем код доходит до BUG-471's `.sheet`), [BUG-493](bugs/BUG-493-OPEN.md) (9 сабтестов/2 файла — синхронное чтение `getComputedStyle()` в топ-левел `<script>` сразу после статичной разметки), [BUG-485](bugs/BUG-485-OPEN.md) (15 сабтестов/1 файл — `document.head` отсутствует вовсе), [BUG-484](bugs/BUG-484-OPEN.md) (2 сабтеста/1 файл — новая грань: незакрытый `url(...` не канонизируется до `url(...)` при сериализации), [BUG-391](bugs/BUG-391-OPEN.md) (1 сабтест — числовой hash-токен `#123` как ID-селектор не бросает `SyntaxError`, тот же файл частично объяснён BUG-471). Committed `.ini` на все 37 непройденных файлов, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (475/475 expected). 0 файлов без атрибуции — шестая полностью закрытая категория трека. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 19. **Срез 19 (2026-08-03) готов на `css/css-nesting`**: 22 testharness id (из 53 файлов — 17 reftest-пар вне скоупа testharness, остальное support/help), 20/22 harness OK, 0/85 сабтестов; базовый парсинг CSS Nesting L1 (`&`, implicit descendant/relative-combinator nesting) реализован в парсере (`crates/engine/css-parser/src/parser.rs::parse_declaration_block_with_nesting`), поэтому все 85 непройденных сабтестов — не пробел самой фичи, а зависимость от уже известных смежных гэпов; поимённая триаж всех 22 файлов → 0 новых BUG-NNN, все расходятся по 5 уже открытым (седьмая категория трека без единого нового бага): [BUG-471](bugs/BUG-471-OPEN.md) (крупнейшее расширение — CSSOM-модель, вся собственная CSSOM-поверхность CSS Nesting — `CSSNestedDeclarations`, под-`cssRules` вложенных правил, `insertRule`/`deleteRule`/`selectorText` — недостижима; `parsing.html` даёт file-level `TIMEOUT` — `let [ss] = document.styleSheets` на верхнем уровне скрипта бросает раньше регистрации любого `test()`; 45 сабтестов + 1 TIMEOUT/6 файлов), [BUG-493](bugs/BUG-493-OPEN.md) (синхронное чтение `getComputedStyle()` в топ-левел `<script>` сразу после статичной разметки; 12 сабтестов/9 файлов), [BUG-384](bugs/BUG-384-OPEN.md) (крупнейший кластер файлов среза — bare-id доступ к `<div id=target>`/`<style id=style>` как переменной; `set-selector-text.html` — единственный файл среза со статусом `ERROR`, а не `FAIL`: `t.add_cleanup` тоже ссылается на тот же bare-id и падает в cleanup, остаток параметризованного цикла из 6 `test()` не запускается вовсе (`NOTRUN`); 13 сабтестов/6 файлов), [BUG-499](bugs/BUG-499-OPEN.md) (весь `nested-declarations-matching.html` — WPT-идиома кодирует ожидаемый исход строкой `'PASS'`/`'FAIL'` в значении кастомного свойства, что делает пустой результат этого бага неотличимым от настоящего `FAIL`; 11 сабтестов/1 файл), [BUG-487](bugs/BUG-487-OPEN.md) (`revert-rule` не реализован — та же чистая сигнатура `assert_true: expected true got false`, что и в срезе 6, теперь и для CSS Nesting-специфичных случаев `CSSNestedDeclarationsRule`; 4 сабтеста/1 файл). Committed `.ini` на все 22 файла, повторный прогон из самого worktree подтверждает 0 unexpected / 0 unexpected passes (107/107 expected: 85 сабтестов + 22 теста). 0 файлов без атрибуции — седьмая полностью закрытая категория трека. **Срез 20 (2026-08-03) готов на четырёх мелких модулях `css/css-motion-path`+`css/css-zoom`+`css/css-namespaces`+`css/css-parser-api`**: первые три — 0 testharness id вовсе (`css-motion-path`/`css-zoom` — только `crashtests/`, `css-namespaces` — 16 reftest `.xml`+`reference/`, тот же класс TEST-4-вне-скоупа, что уже видели у reftest-пар css-nesting/css-forms/css-font-loading); `css-parser-api` — 1 testharness id (`idlharness.html`), TIMEOUT на невендоренных `/resources/WebIDLParser.js`+`/resources/idlharness.js` (404) — тот же инфраструктурный гэп WPT-RUN-1-класса, что уже задокументирован в BUG-367/BUG-374/BUG-392/BUG-397 на других категориях, не движковый дефект этой категории. 0 новых BUG-NNN, 0 файлов получили `.ini` (инфра-гэп не форсируется в атрибуцию, тот же прецедент что `testdriver-actions.js` в срезе 8) — все 4 категории полностью закрыты, остатка нет. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 21. **Срез 21 (2026-08-03) готов на шести мелких модулях `css/css-link-params`+`css/css-forced-color-adjust`+`css/css-size-adjust`+`css/css-device-adapt`+`css/css-env`+`css/css-overscroll-behavior`**: 44 testharness id, все harness OK; поимённая триаж → 6 новых BUG-NNN (BUG-511 `link-parameters` — CSS Linked Parameters L1, очень ранний Editor's Draft, заведено по политике трека; BUG-512 `forced-color-adjust` — реальная отгруженная спека, не реализовано вовсе; BUG-513 `text-size-adjust` — отгружена в мобильных браузерах, не реализовано вовсе, крупнейшая находка среза, 196 сабтестов через Web Animations interpolation-харнесс; BUG-514 функция `env()` — CSS Environment Variables L1, safe-area-inset-*, не распознаётся парсером вовсе; BUG-515 `window.devicePixelRatio` не определён на одиночном окне по умолчанию; BUG-516 логические `overscroll-behavior-block`/`-inline` не распознаются парсером, в отличие от физических `-x`/`-y`) + расширены 5 уже открытых: BUG-472 (+12), BUG-481 (+5 файлов, `visualViewport` целиком отсутствует), BUG-484 (+9 файлов/52 сабтеста), BUG-384 (+3 файла), BUG-487 (+1 файл, `revert-rule` в `env()`-фоллбэке). Committed `.ini` на 34 файла, повторный прогон из самого worktree подтверждает 0 unexpected passes (`css-overscroll-behavior` даёт 26 unexpected сабтестов на 6 намеренно неатрибутированных файлах — известный инфра-гэп `waitForCompositorReady`/`waitForCompositorCommit`, `testdriver-vendor.js` не довендорен). `css-speech` (11 файлов, целиком ручная спека) и `css-style-attr` (58 файлов, 16 `.xht` reftest-пар) — оба TEST-4-вне-скоупа, 0 testharness id, без `.ini`. **Срез 22 (2026-08-03) готов на шести модулях `css/css-round-display`+`css/css-gcpm`+`css/css-rhythm`+`css/css-image-animation`+`css/css-mixins`+`css/css-color-adjust`**: первые два — 0 testharness id вовсе (35 файлов, чисто визуальные, тот же класс "100% manual by spec design"), без `.ini`. Попутно довендорена общая директория фикстур `tests/wpt/images/` (84 файла) — референсится многими категориями `css/`. `css-rhythm` (15 testharness id, 15/17 harness OK): 155 сабтестов → новый [BUG-517](bugs/BUG-517-OPEN.md) (CSS Rhythm `block-step*` не реализованы вовсе). `css-mixins` (47 testharness id, 35/47 harness OK): 311 сабтестов → новые [BUG-518](bugs/BUG-518-OPEN.md) (`@mixin`/`@apply`/`@contents` не реализованы вовсе) и [BUG-519](bugs/BUG-519-OPEN.md) (некоторые `@function`-тела зависают TIMEOUT вместо FAIL) + крупнейшее расширение трека [BUG-471](bugs/BUG-471-OPEN.md) (135 сабтестов/6 файлов). `css-color-adjust` (14 testharness id, 13/14 harness OK): 169 сабтестов → расширены [BUG-472](bugs/BUG-472-OPEN.md)/[BUG-463](bugs/BUG-463-OPEN.md)/[BUG-384](bugs/BUG-384-OPEN.md). Committed `.ini` на 71 файл совокупно, повторный прогон подтверждает 0 unexpected passes на всех шести категориях; 1 файл (`color-scheme-root-background.html`) намеренно без атрибуции. Детали — `ROADMAP.md`, строка `WPT-RUN-3`. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 23. **Срез 23 (2026-08-03) готов на пяти модулях `css/CSS1`+`css/fetching`+`css/fill-stroke`+`css/geometry`+`css/printing`**: `CSS1` и `printing` — 0 testharness id (reftest/crashtest-пары), без `.ini`; `printing` попутно вскрыл гочу самого `wptrunner` — пустая выборка тестов роняет `testrunner.py::_cleanup_run_loop` (`AttributeError`), не Lumen-баг. `fetching` (1 testharness id) → новый [BUG-520](bugs/BUG-520-OPEN.md) — Resource Timing (`performance.getEntriesByType('resource')`) никогда не наполняется для реальных загрузок: нативный хук `_lumen_record_resource_timing` существует, но сетевой слой его не вызывает; 4 сабтеста, файл TIMEOUT. `fill-stroke` (4 testharness id, 4/4 harness OK): 371 сабтест → новый [BUG-521](bugs/BUG-521-OPEN.md) (`text-decoration-fill`/`text-decoration-stroke`/`-webkit-text-stroke` не реализованы вовсе, 2 файла/7 сабтестов) + расширен [BUG-472](bugs/BUG-472-OPEN.md) (крупнейшее одно-файловое расширение трека — `stroke-color-interpolation.html` 172/172 сабтестов; `fill`/`stroke`-семья распарсена и хранится, но не преподана `computed_style_to_map`). `geometry` (27 testharness id, 23/27 harness OK) → новый [BUG-522](bugs/BUG-522-OPEN.md), крупнейшая по числу сабтестов находка трека на сегодня: весь модуль Geometry Interfaces (`DOMMatrix`/`DOMPoint`/`DOMRect`/`DOMQuad`/`WebKitCSSMatrix`) отсутствует целиком как глобалы; сопутствующие находки — `Element.prototype.getClientRects` тоже отсутствует, `CanvasRenderingContext2D.setTransform()` не принимает словарную форму и не валидирует NaN/Infinity, `getTransform()` не существует вовсе. ~720/784 сабтестов, 23 файла. `idlharness.any.html` — стандартный невендоренный `idlharness.js`-гэп, без атрибуции. Committed `.ini` на 31 файл совокупно (1 `css/fetching` + 4 `css/fill-stroke` + 26 `css/geometry`), повторный прогон из самого worktree подтверждает 0 unexpected passes на всех пяти категориях. Детали — `ROADMAP.md`, строка `WPT-RUN-3`. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 25. **Срез 26 (2026-08-03) готов на `css/css-paint-api`+`css/css-layout-api`+`css/css-counter-styles`+`css/css-highlight-api`+`css/css-ruby`+`css/css-page`**: `css-paint-api`/`css-layout-api` — все testharness-файлы инфра-гэпы (HTTPS UnknownIssuer / idlharness.js), без .ini; новые [BUG-533](bugs/BUG-533-OPEN.md) (StaticRange отсутствует вовсе), [BUG-534](bugs/BUG-534-OPEN.md) (CSS Custom Highlight API — Phase-0-шим не реализует Setlike/Maplike), [BUG-535](bugs/BUG-535-OPEN.md) (`ruby-position: alternate` без эффекта) + расширены [BUG-368](bugs/BUG-368-OPEN.md), [BUG-384](bugs/BUG-384-OPEN.md), [BUG-467](bugs/BUG-467-OPEN.md), [BUG-471](bugs/BUG-471-OPEN.md), [BUG-472](bugs/BUG-472-OPEN.md), [BUG-480](bugs/BUG-480-OPEN.md), [BUG-484](bugs/BUG-484-OPEN.md), [BUG-485](bugs/BUG-485-OPEN.md), [BUG-491](bugs/BUG-491-OPEN.md), [BUG-493](bugs/BUG-493-OPEN.md). Committed `.ini` на 60 файлов совокупно; повторный прогон подтверждает ожидаемый unexpected-остаток на всех шести категориях (только известные инфра-гэпы: idlharness.js/HTTPS-сертификат). Детали — `ROADMAP.md`, строка `WPT-RUN-3`. **Срез 27 (2026-08-03) готов на `css/css-transitions`**: 120 testharness id, 99/120 harness OK, 650/3211 сабтестов; новый [BUG-536](bugs/BUG-536-OPEN.md) (CSS Transitions не производят никакого JS-наблюдаемого эффекта — getAnimations() всегда пуст, transitionrun/transitionstart/transitionend/transitioncancel никогда не диспатчатся, getComputedStyle() не отражает живое интерполированное значение мидтранзишен) объясняет большинство провалов слайса; расширены [BUG-483](bugs/BUG-483-OPEN.md) (has-ловушка getComputedStyle, 49 сабтестов), [BUG-484](bugs/BUG-484-OPEN.md) (невалидация/сериализация transition-*, включая алиас-фолдинг step-start/step-end), [BUG-463](bugs/BUG-463-OPEN.md) (Web Animations feature-detect, 3 файла интерполяции), [BUG-384](bugs/BUG-384-OPEN.md) (bare-id доступ, 10 файлов), [BUG-471](bugs/BUG-471-OPEN.md) (CSSStyleSheet не реализован, 2 файла). Committed `.ini` на 112 файлов; повторный прогон подтверждает 0 unexpected. Детали — `ROADMAP.md`, строка `WPT-RUN-3`. Остаток DoD — продолжение на прочих css-*-модулях/selectors/cssom-соседях — задел для среза 28. Срезы 28–43 (детали по каждому — `ROADMAP.md`, строка `WPT-RUN-3`, эта ячейка здесь дальше не дублировалась) прошли по всем остальным `css/`-директориям: модуль за модулем инструментированы `.ini`+`BUG-NNN` (80 из 94 директорий `css/`), включая крупные `css-grid`/`css-sizing`/`css-position`/`css-anchor-position`; оставшиеся 14 (`CSS1`/`WOFF2`/`css-namespaces`/`css-gcpm`/`css-motion-path`/`css-round-display`/`css-speech`/`css-style-attr`/`css-zoom`/`printing`/`css-parser-api` + служебные `README.md`/`reference`/`support`) дают 0 `testharness`-id (только `manual`/`visual`/`reftest`/`crashtest`), закрываются отдельным S8-исполнителем (`docs/tasks/p2-wpt-integration.md`), не этим треком. **Срез 44 (2026-08-04) — закрытие трека WPT-RUN-3.** Регрессионная сверка на пяти модулях (`css-position`/`css-multicol`/`css-masking`/`css-transitions`/`css-anchor-position`, ~630 `testharness`-id, ~24000 сабтестов) дала 0 unexpected против закоммиченных `.ini` — недавние движковые правки (снос `rquickjs`, финал V8-миграции) не задели трек. Монолитный прогон всех 34607 файловых id `css/` подряд ни разу не совершался (не помещается в один интерактивный вызов, см. срез 1) — покрытие подтверждено суммой отдельных прогонов по срезам, не одним сквозным прогоном; окончательная сводная цифра по всей категории `css` остаётся открытым TODO вне скоупа закрытого трека.
