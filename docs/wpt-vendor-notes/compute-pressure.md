# WPT vendor notes — `compute-pressure`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-compute-pressure`, `docs/wpt-status.md`), included despite its 🚫 scope note (Compute Pressure API — device CPU/GPU load sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`. Same pinned commit, `git sparse-checkout` at the same commit hash, 37 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, 19 root `.https.` tests, `permissions-policy/` subdir — 9 files + 2 `.headers`, `resources/` — 4 JS helpers). Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`. The whole category is `.https.`-only bar one file (39 selected ids — 19 files × `globalScope=window`/`dedicated_worker` variants). `run_report.py --all --root compute-pressure --recursive` completed fully (~19min): 38 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`); the one non-`.https.` file, `permissions-policy/compute-pressure-supported-by-permissions-policy.html`, ran to completion — harness OK, 0/1 subtest FAIL (`document.permissionsPolicy.features` doesn't list `"compute-pressure"`, same pattern as `accelerometer`). 1/39 harness OK, 0/1 subtests. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (`tests/wpt/compute-pressure/`, 37 файлов: `META.yml`, `README.md`, `WEB_FEATURES.yml`, 19 корневых `.https.` тестов, `permissions-policy/` подкаталог (9 файлов + 2 `.headers`), `resources/` — 4 JS-хелпера), включена несмотря на скоуп 🚫 (Compute Pressure API — датчик нагрузки CPU/GPU устройства) по прямому запросу пользователя 2026-07-21, тому же постоянному решению, что `accelerometer`/`acid`/`ai`. Внекатегорийные хелперы не довендорены: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js` — та же документированная методология survey-gap, что `FileAPI`/`ai`. Вся категория `.https.`-only (39 отобранных id — 19 файлов × `globalScope=window`/`dedicated_worker` варианты, кроме одного не-https теста). `run_report.py --all --root compute-pressure --recursive` прошёл полностью (~19мин): 38 TIMEOUT (`invalid url: invalid port: "None"` — минимальный исполнитель не поднимает HTTPS-порт, тот же класс ограничения, что `WebCryptoAPI`/`ai`/`ambient-light`); единственный не-https `permissions-policy/compute-pressure-supported-by-permissions-policy.html` — harness OK, 0/1 сабтест FAIL (`document.permissionsPolicy.features` не содержит `"compute-pressure"` — Permissions Policy не объявляет эту фичу, тот же паттерн, что `accelerometer`). 1/39 harness OK, 0/1 сабтестов. Не заводился отдельный BUG-NNN — первый проход, см. методологию выше
