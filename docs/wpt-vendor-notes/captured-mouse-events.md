# WPT vendor notes — `captured-mouse-events`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-captured-mouse-events`, `docs/wpt-status.md`), in-scope (⬜, Captured Mouse Events — `CapturedMouseEvent`/`CaptureController` extensions for Screen Capture). Same pinned commit, `git sparse-checkout` at the same commit hash, 6 files (`META.yml`, `capture-controller-oncapturedmousechange.https.html`, `captured-mouse-event-constructor.html`, `captured-mouse-event-constructor-inherited.html`, `capturing-mouse-coordinates-manual.tentative.https.html`, `idlharness.https.window.js`). One out-of-category dependency pair not vendored: `idlharness.https.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root captured-mouse-events --recursive` completed fully (4 selected ids, `capturing-mouse-coordinates-manual.tentative.https.html` excluded by the `-manual` filter): the 2 non-https constructor tests ran (harness OK) but all 6 subtests FAIL (`CapturedMouseEvent is not defined` — the API itself isn't implemented in Lumen, expected on a first pass); `capture-controller-oncapturedmousechange.https.html` TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`/`browsing-topics`); `idlharness.https.window.html` ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`browsing-topics`). 2/4 harness OK, 0/6 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (коммит `35be3b44`, `tests/wpt/captured-mouse-events/`, 6 файлов: `META.yml`, `capture-controller-oncapturedmousechange.https.html`, `captured-mouse-event-constructor.html`, `captured-mouse-event-constructor-inherited.html`, `capturing-mouse-coordinates-manual.tentative.https.html`, `idlharness.https.window.js`). Внекатегорийный хелпер не довендорен: `idlharness.https.window.js` ссылается на `/resources/WebIDLParser.js`+`/resources/idlharness.js`, та же методология, что `FileAPI`/`audio-session`. Манифест отбирает 4 id (`capturing-mouse-coordinates-manual.tentative.https.html` исключён как `-manual`): 2 не-https конструкторных теста (`captured-mouse-event-constructor(.-inherited).html`) — harness OK, но 0/6 сабтестов FAIL (`CapturedMouseEvent is not defined` — Captured Mouse Events API не реализован, первый проход категории); `capture-controller-oncapturedmousechange.https.html` — TIMEOUT (`invalid url: invalid port: "None"`, тот же HTTPS-порт-гэп, что `WebCryptoAPI`/`ai`/`bluetooth`/`browsing-topics`); `idlharness.https.window.html` — ERROR (session/URL-reuse артефакт, тот же класс, что `accelerometer`/`beacon`/`browsing-topics`). `run_report.py --all --root captured-mouse-events --recursive` добежал полностью. 2/4 harness OK, 0/6 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
