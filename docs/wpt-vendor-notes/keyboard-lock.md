# WPT vendor notes — `keyboard-lock`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-keyboard-lock`, `docs/wpt-status.md`), scope ⬜ (candidate, `navigator.keyboard.lock()`/`unlock()`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 7 files (`META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `is-input-pending` category, `idlharness.https.window.js`, 5 `navigator-keyboard-lock*.https.html`+`navigator-keyboard-unlock.https.html`, `resources/iframe-lock-helper.html`). No `variant` hits, no `testdriver.js`, all 7 selected ids `.https.`. `run_report.py --all --root keyboard-lock --recursive` completed in ~2:54 (7 selected ids): **0/7 harness OK, 0/0 subtests**. All 7 TIMEOUT on the standard self-signed-cert TLS gap (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, same documented residual as `jpegxl`/`js-self-profiling`, `tests/wpt/certs/README.md`) — no `.sub.html`-style HTTP-500 variant, no BUG-380 contamination (single-shot TIMEOUTs, no cross-test bleed observed). A `crates/js/src/dom.rs` grep for `keyboard.lock`/`KeyboardLayoutMap`/`navigator.keyboard` found nothing beyond the existing `KeyboardEvent` — the Keyboard Lock API is entirely unimplemented. No bug filed: same pattern as `navigator.scheduling` in `is-input-pending` and `Profiler` in `js-self-profiling`, a first pass documenting an absent candidate feature, not a defect in an existing one. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/keyboard-lock/`, 7 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `is-input-pending`, `idlharness.https.window.js`, 5 `navigator-keyboard-lock*.https.html`+`navigator-keyboard-unlock.https.html`, `resources/iframe-lock-helper.html`), скоуп ⬜ (кандидат, `navigator.keyboard.lock()`/`unlock()`). `run_report.py --all --root keyboard-lock --recursive` — ~2:54, 7 отобранных id (все `.https.`): **0/7 harness OK, 0/0 сабтестов**. Все 7 TIMEOUT на стандартном TLS-гэпе тестового сертификата (`network error: TLS handshake: invalid peer certificate: UnknownIssuer`, тот же документированный остаток, что `jpegxl`/`js-self-profiling`, `tests/wpt/certs/README.md`); без `.sub.html`, без наблюдаемой контаминации BUG-380. Грep `crates/js/src/dom.rs` на `keyboard.lock`/`KeyboardLayoutMap`/`navigator.keyboard` ничего не нашёл кроме существующего `KeyboardEvent` — Keyboard Lock API не реализован вовсе. Новых багов не заведено — как `navigator.scheduling` в `is-input-pending` и `Profiler` в `js-self-profiling`, первый проход по отсутствующему кандидатному API.
