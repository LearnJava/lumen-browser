# WPT vendor notes — `inert`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-04 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-inert`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 31 files (`WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `images` category, 28 test files, `resources/`). No `.https.` files, no `variant` hits. `run_report.py --all --root inert --recursive` completed in ~1:55 (29 selected ids): **17/29 harness OK, 19/82 subtests passed**. Dominant maskers were [BUG-384](../../bugs/BUG-384-FIXED.md) (named access on `window`, e.g. `dialog`/`wrapper`/`child` id-as-global) and [BUG-462](../../bugs/BUG-462-OPEN.md)/[BUG-574](../../bugs/BUG-574-OPEN.md) (`Node.prototype.contains` missing, breaks vendored `testdriver.js`'s click helper); 6 files additionally fail because `/resources/testdriver-actions.js` (upstream's separate `test_driver.Actions` builder file) isn't vendored under `tests/wpt/resources/` — same class of documented survey gap as the un-vendored out-of-category helpers noted on `FileAPI`/`animation-worklet` above. `inert-focus-in-frames.html`/`inert-iframe-hittest.html`/`inert-iframe-tabbing.html` reconfirm [BUG-480](../../bugs/BUG-480-OPEN.md) (`<iframe>` has no browsing context, `contentWindow`/`contentDocument` absent). Six new bugs from findings that survived past the maskers, all confirmed live via `--mcp-live-port`: [BUG-618](../../bugs/BUG-618-OPEN.md) (`.inert` IDL getter shadowed by a stale Phase-0 stub, always reads `false` on markup-declared `inert`), [BUG-619](../../bugs/BUG-619-OPEN.md) (inert doesn't propagate from a shadow host to non-slotted shadow-tree children — ancestor walk never crosses the shadow-root→host boundary), [BUG-620](../../bugs/BUG-620-OPEN.md) (`Selection`/`Range.prototype.toString()` return `""` whenever the shared start/end container is an Element rather than Text, e.g. after `selectAllChildren()` — root cause traced to `range_text()`'s same-container fast path in `crates/engine/dom/src/lib.rs`), [BUG-621](../../bugs/BUG-621-OPEN.md) (`<label>.focus()` doesn't delegate to `label.control`), [BUG-622](../../bugs/BUG-622-OPEN.md) (`document.defaultView` missing entirely), [BUG-623](../../bugs/BUG-623-OPEN.md) (legacy `window.find()` missing, lower priority). See `docs/wpt-status.md` for details.
