# WPT vendor notes — `payment-method-basic-card`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/payment-method-basic-card/`, 7 файлов: `META.yml`, `apply_the_modifiers.html`, `billing-address-is-null-manual.https.html`, `empty-data-manual.https.html`, `historical.https.html`, `payment-request-canmakepayment-method.https.html`, `steps_for_selecting_the_payment_handler.html`; `LICENSE-WPT.md` скопирован из соседней `parakeet`). `run_report.py --all --root payment-method-basic-card --recursive` (~1 мин 21 с, 4 отобранных id из 6 — 2 `-manual` в имени раннер исключает): **0/4 harness OK**. Все 4 TIMEOUT: `historical.https.html`/`payment-request-canmakepayment-method.https.html` — уже задокументированный TLS-гэп `UnknownIssuer`; `apply_the_modifiers.html`/`steps_for_selecting_the_payment_handler.html` — без `-manual` в имени файла, но фактически ручные тесты (`promise_test` вызовы навешаны только на `onclick`-кнопки, ничего не запускается автоматически). `window.PaymentRequest` реально реализован (Phase 0 stub, `crates/js/src/payment_request.rs`) — первая вендоренная категория семейства Payment Request API, живая проба конструктора через `--mcp-live-port` нашла: нет входной валидации вовсе (пустой `methodData`, отрицательная/нечисловая сумма, некорректный ISO 4217 код валюты — все обязаны бросать по спеке §3.1/§4.9, ни один не бросает). Заведён [BUG-646](../bugs/BUG-646-OPEN.md)
