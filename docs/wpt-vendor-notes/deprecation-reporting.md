# WPT vendor notes — `deprecation-reporting`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-deprecation-reporting`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`META.yml`, `__dir__.ini`, `idlharness.any.js`). Upstream's own `__dir__.ini` marks the whole category `implementation-status: not-implementing` — the WICG Deprecation Reporting proposal has no shipping implementation anywhere, Lumen included. One out-of-category helper pair not vendored: `idlharness.any.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`/`autoplay-policy-detection`. Smallest category vendored so far by file count (3, vs. `cpu-performance`'s 2 and `audio-session`'s 3). `run_report.py --all --root deprecation-reporting --recursive` selected 1 id (`idlharness.any.html`) and completed in ~15s: the test ran over plain HTTP but 404'd on both out-of-category dependencies (`idl_test is not defined`), so the harness never registers a result — TIMEOUT, the same "un-vendored-helper-style" hang pattern as `audio-session`/`autoplay-policy-detection`'s own `idlharness.window.js`. 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known survey gap, not an engine defect. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/deprecation-reporting/`, 3 файла: `META.yml`, `__dir__.ini`, `idlharness.any.js`). Апстримный `__dir__.ini` сам помечает категорию `implementation-status: not-implementing` — предложение WICG Deprecation Reporting нигде не реализовано, включая Lumen. Один внекатегорийный хелпер не довендорен: `idlharness.any.js` ссылается на `/resources/WebIDLParser.js`+`/resources/idlharness.js`, тот же гэп, что `FileAPI`/`audio-session`/`autoplay-policy-detection`. Самая маленькая вендоренная категория по числу файлов (3, против 2 у `cpu-performance`). `run_report.py --all --root deprecation-reporting --recursive` отобрал 1 id (`idlharness.any.html`), прошёл за ~15с: тест выполнился по обычному HTTP, но словил 404 на оба внекатегорийных хелпера (`idl_test is not defined`), гарнес так и не отчитался — TIMEOUT, тот же паттерн зависания, что `idlharness.window.js` у `audio-session`/`autoplay-policy-detection`. 0/1 harness OK, 0/0 сабтестов. Не заводился отдельный BUG-NNN — известный survey-гэп, не движковый дефект
