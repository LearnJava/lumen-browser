# WPT vendor notes — `web-animations`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-09 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-web-animations`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `element-timing` category, 236 files (`animation-model/`, `animation-trigger/`, `crashtests/`, `interfaces/`, `resources/`, `responsive/`, `timing-model/`, `idlharness.window.js`, `testcommon.js`). No `variant`/`.https.` hits, only 3 `testdriver.js` files, self-contained aside from a handful of out-of-category deps (`/common/reftest-wait.js` for the reftest-typed `crashtests/`, one file each pulling `../css-animations/support/testcommon.js`/`../../../scroll-animations/scroll-timelines/testcommon.js`, neither vendored yet). `run_report.py --all --root web-animations --recursive` completed in ~4 min (139 selected ids): **121/139 harness OK, 930/4147 subtests** — an unusually high ratio for this backlog (`fetch`/`svg`-class, a substantially-implemented category). **Binary-staleness trap hit and resolved this session**: the *first* run, against the shared root worktree's warm `target/dev-release/lumen.exe`, showed 128/139 harness OK but only 723/3625 subtests, dominated by 3966× `ReferenceError: createElement is not defined` + 1208× `createDiv is not defined` — looked exactly like [BUG-346](../../bugs/BUG-346-FIXED.md) (`Url::resolve()` not collapsing `..`) regressing, since `<script src="../../testcommon.js">` was going out over the wire un-collapsed and 404ing. A temporary unit test proved the *source* already resolves this correctly; rebuilding `lumen-shell` in-slot dropped the 404 to 2 (both a genuinely unvendored out-of-category helper) and produced the 121/139, 930/4147 numbers reported above — BUG-346 is not regressed, see its FIXED file for the full writeup. Remaining signal: reconfirms already-open [BUG-670](../../bugs/BUG-670-OPEN.md) (`AnimationEffect.getComputedTiming()` missing, ~556 raw occurrences, the run's largest single driver); new finding — [BUG-704](../../bugs/BUG-704-OPEN.md): `Animation.prototype.commitStyles`/`.persist` are missing entirely (`grep` — zero hits in `crates/js/src/dom.rs`), second-largest cluster (62 sites). One new bug number. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/web-animations/`, 236 файлов: `animation-model/`, `animation-trigger/`, `crashtests/`, `interfaces/`, `resources/`, `responsive/`, `timing-model/`, `idlharness.window.js`, `testcommon.js`). Ноль `.https.`/`variant`-хитов, 3 файла тянут `testdriver.js`. `run_report.py --all --root web-animations --recursive` — ~4 мин, 139 отобранных id: **121/139 harness OK, 930/4147 сабтестов** — необычно высокая доля для этого бэклога. Первый прогон (на устаревшем shared-бинарнике root-worktree) дал 128/139, 723/3625, с тысячами `ReferenceError: createElement/createDiv is not defined` — выглядело как регресс [BUG-346](../../bugs/BUG-346-FIXED.md) (`../../testcommon.js` не резолвился). Юнит-тест на текущих исходниках подтвердил, что `Url::resolve()` уже корректно схлопывает `..`; пересборка бинарника в слоте убрала артефакт (см. `bugs/BUG-346-FIXED.md`, раздел "Live-run verification"). Реконфирмация уже открытого [BUG-670](../../bugs/BUG-670-OPEN.md) (`AnimationEffect.getComputedTiming()` отсутствует, крупнейший драйвер — ~556 вхождений текста ошибки). Новый номер — [BUG-704](../../bugs/BUG-704-OPEN.md): `Animation.prototype.commitStyles`/`.persist` отсутствуют целиком (62 сайта вызова).
