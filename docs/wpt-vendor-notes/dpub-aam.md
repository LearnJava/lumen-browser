# WPT vendor notes — `dpub-aam`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-dpub-aam`, `docs/wpt-status.md`), in-scope (⬜, Digital Publishing Accessibility API Mappings — `doc-*` ARIA role/computedrole mapping for EPUB/digital-publishing semantics; not separately implemented in Lumen, no accessibility-tree role-mapping for these roles). Same pinned commit, `git sparse-checkout` at the same commit hash, 43 files (`META.yml`, `role/roles.html`, `manual/` — 41 `doc-*-manual.html` fixtures + `META.yml` + `README.md`). One out-of-category helper not vendored: `role/roles.html` references `/wai-aria/scripts/aria-utils.js` (shared ARIA role-verification harness), same documented survey gap as `FileAPI`/`ai`; moot here since the test never reaches it. Entirely manual by spec design except one file — all 41 `manual/doc-*-manual.html` are `-manual.html`, excluded by `run_report.py`'s own filter; `run_report.py --all --root dpub-aam --recursive` selected exactly 1 id, `role/roles.html`, and completed in a few seconds: SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`/`document-picture-in-picture` — the test drives role assignment via `test_driver.set_permission`/similar through `aria-utils.js`). 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known executor gap, not a new engine defect. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-26 (коммит `35be3b44`, `tests/wpt/dpub-aam/`, 43 файла: `META.yml`, `role/roles.html`, `manual/` — 41 `doc-*-manual.html` фикстура + `META.yml` + `README.md`), в скоупе (⬜, Digital Publishing Accessibility API Mappings — маппинг `doc-*` ARIA-ролей для EPUB/digital-publishing семантики, отдельно не реализовано в Lumen). Один внекатегорийный хелпер не вендорен — `role/roles.html` ссылается на `/wai-aria/scripts/aria-utils.js`, тот же документированный гэп, что `FileAPI`/`ai`, но не влияет на результат. Целиком manual по замыслу спецификации кроме одного файла — все 41 `manual/doc-*-manual.html` суть `-manual.html`, исключены фильтром `run_report.py`. `run_report.py --all --root dpub-aam --recursive` отобрал ровно 1 id, `role/roles.html`, прошёл за несколько секунд: SKIP (`Executor does not support testdriver.js`, тот же гэп, что `accname`/`attribution-reporting`/`document-picture-in-picture`). 0/1 harness OK, 0/0 сабтестов. Отдельный BUG-NNN не заводился — известный гэп executor'а, не новый движковый дефект
