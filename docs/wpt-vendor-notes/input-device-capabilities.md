# WPT vendor notes — `input-device-capabilities`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-input-device-capabilities`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 3 files (`META.yml`, `LICENSE-WPT.md` copied from the sibling `idle-detection` category, `idlharness.window.js` — the category's only test). `idlharness.window.js` references un-vendored `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `audio-session`/`animation-worklet`. `run_report.py --all --root input-device-capabilities --recursive` completed in ~44 s (1 selected id): **0/1 harness OK**, TIMEOUT — dies on the 404s above before `idl_test` is ever defined, no signal from the run itself. A `--dump-layout` + `console.log` live probe confirmed the whole API is unimplemented: `typeof window.InputDeviceCapabilities === "undefined"`, `'sourceCapabilities' in UIEvent.prototype`/`MouseEvent.prototype`/`KeyboardEvent.prototype` all `false`, and a live `MouseEvent` instance's `.sourceCapabilities` is `undefined`. Not filed as its own BUG-NNN — a wholly missing feature on a first pass, same pattern as `custom-elements/registries`/`container-timing`/`compute-pressure`/Element Timing itself. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/input-device-capabilities/`, 3 файла: `META.yml`, `LICENSE-WPT.md` скопирован из соседней `idle-detection`, единственный тест `idlharness.window.js`), скоуп ⬜ (кандидат). `run_report.py --all --root input-device-capabilities --recursive` — ~44 с, 1 отобранный id: **0/1 harness OK**, TIMEOUT — тест 404-ит на невендоренные `/resources/WebIDLParser.js`+`/resources/idlharness.js` (тот же класс гэпа, что `audio-session`/`animation-worklet`), сигнала от прогона нет. Живая проба (`--dump-layout` + `console.log`) подтвердила: `InputDeviceCapabilities` — целиком не реализованное API (`window.InputDeviceCapabilities === undefined`, `'sourceCapabilities' in UIEvent.prototype`/`MouseEvent.prototype`/`KeyboardEvent.prototype` все `false`, `event.sourceCapabilities === undefined` на живом `MouseEvent`). Не заводился отдельный BUG-NNN — целиком отсутствующая фича при первом проходе, тот же паттерн, что `custom-elements/registries`/`container-timing`/`compute-pressure`/Element Timing самого себя.
