# WPT vendor notes — `serial`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-serial`, `docs/wpt-status.md`), scope 🚫 (out of scope — hardware API). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `selection` category, 18 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `getPorts/`, `requestPort/`, `resources/`, root `.sub.html`/`idlharness.https.any.js`, 5 `serialPort_*-manual.https.html`). No `variant` hits, 2/18 files pull `testdriver.js`. `run_report.py --all --root serial --recursive` completed in ~3:51 (10 selected ids, 5 `-manual.https.html` excluded by the runner's own filter): **0/10 harness OK**. All 10 are `.https.`, all TIMEOUT on the already-documented WPT-RUN-2 TLS `UnknownIssuer` gap, none reached navigation. The Serial API is actually implemented as a Phase 0 stub (`crates/js/src/serial.rs`) and behaves exactly as its own doc comment describes (`requestPort()` → `NotSupportedError`, `getPorts()` → `[]`) — not a bug. A live probe (`--mcp-live-port`, independent of the WPT runner) found one new, previously unfiled defect — [BUG-672](../../bugs/BUG-672-OPEN.md): `Serial`/`SerialPort` are plain `class X extends EventTarget` with no constructor guard, so `new SerialPort()`/`new Serial()` both succeed even though the WICG Serial spec defines no constructor operation for either interface — same defect class as [BUG-629](../../bugs/BUG-629-OPEN.md) (`Report`)/[BUG-374](../../bugs/BUG-374-OPEN.md) (`FileSystemFileHandle`), a new surface. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

аппаратный API (Serial). Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/serial/`, 18 файлов). `run_report.py --all --root serial --recursive` — ~3:51, 10 selected ids (5 `-manual.https.html` исключены фильтром раннера), все `.https.`: **0/10 harness OK**, все TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` — не новая находка. Serial API реально реализован как Phase 0 заглушка (`crates/js/src/serial.rs`) и ведёт себя ровно так, как описывает собственный doc-комментарий (`requestPort()`→`NotSupportedError`, `getPorts()`→`[]`) — не баг. Живая проба через `--mcp-live-port` нашла [BUG-672](../bugs/BUG-672-OPEN.md): `Serial`/`SerialPort` — обычные `class X extends EventTarget` без guard на конструктор, `new SerialPort()`/`new Serial()` конструируются успешно, хотя спека не определяет конструктор ни для одного из них (тот же класс, что [BUG-629](../bugs/BUG-629-OPEN.md)/[BUG-374](../bugs/BUG-374-OPEN.md), новая поверхность)
