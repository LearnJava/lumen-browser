# WPT vendor notes — `url`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/url/`, 50 файлов + внекатегорийный `/common/subset-tests-by-key.js`, довендорен в `tests/wpt/common/`). Прогон `run_report.py --all --root url --recursive`, ~2:19, 44 отобранных id: **39/44 harness OK, 1742/7451 сабтестов** — крупнейший провальный кластер бэклога на сегодня, дешёвые предикторы (0 `.https.`, 0 `testdriver.js`) не отражают объём сигнала. Заведено два новых номера: [BUG-693](../bugs/BUG-693-OPEN.md) — `_lumen_parse_url`/`_url_resolve` (`dom.rs`, обслуживает `URL`/`location`/весь `HTMLHyperlinkElementUtils`-микс на `<a>`/`<area>`) — рукописный строковый сплиттер, не алгоритм WHATWG URL Standard: нет IDNA/punycode-обработки хоста, не режется таб/перевод строки, userinfo отбрасывается при парсинге, `_url_resolve` ошибочно считает любую `scheme:`-строку уже абсолютной (не резолвит `http:foo.com` относительно базы для совпадающей special-схемы), сеттеры не валидируют вход. Отдельная, RFC-3986-приближенная `Url` в `crates/network` (уже чинилась под [BUG-346](../bugs/BUG-346-OPEN.md)) этим кодом не переиспользуется. `url-constructor.any.html` 223/899, `a-element*` 5/1313, `url-setters*` 116/1102, `url-origin.any.html` 156/413, `toascii.window.html` 193/784, `IdnaTestV2.any.html` 987/2671, `url-statics-canparse.any.html` 4/8. [BUG-694](../bugs/BUG-694-OPEN.md) — `URLSearchParams` не итерируем (`Symbol.iterator` отсутствует, `entries()`/`keys()`/`values()` отдают `Array` вместо итератора, тот же класс дефекта, что `Headers`/[BUG-369](../bugs/BUG-369-FIXED.md)); копирующий конструктор `new URLSearchParams(existingParams)` протекает через перечислимое внутреннее поле `_p`, отдавая один параметр `_p=a%2Cb` вместо копии пар; двухаргументная форма `delete(name, value)`/`has(name, value)` не поддержана. `urlencoded-parser.any.html` 0/105 (сквозь итерацию завязан весь набор), `urlsearchparams-sort.any.html` 0/17. Два новых номера бага
