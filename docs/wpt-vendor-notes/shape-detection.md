# WPT vendor notes — `shape-detection`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-shape-detection`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `serial` category, 26 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, 22 root test files, `resources/` — image/video fixtures plus `shapedetection-helpers.js`/`single-detection-helpers.js`). All test files are `.https.` (one `.sub.https.`), 0 files pull `testdriver.js`, no `variant` hits, no `manual/` dir. `run_report.py --all --root shape-detection --recursive --processes=4` completed in 2:16 (23 selected ids): **0/23 harness OK**, all TIMEOUT on the already-documented WPT-RUN-2 TLS `UnknownIssuer` gap (port 18443), none reached navigation. The Shape Detection API is actually implemented as a Phase 0 stub (`crates/js/src/shape_detection.rs`) and behaves exactly as its own doc comment describes (`FaceDetector`/`BarcodeDetector`/`TextDetector.detect()` always return `[]`) — not a bug. A live probe (`--mcp-live-port`, independent of the WPT runner; `about:blank` and a `data:` URL both failed to establish a JS context — the latter with the same "treated as a file path" `os error 123` class as [BUG-651](../../bugs/BUG-651-OPEN.md), worked around with a local `http.server`) found one new, previously unfiled defect — [BUG-677](../../bugs/BUG-677-OPEN.md): all three classes deviate from their WebIDL interface shape — constructor `.length` is 1 instead of the spec's 0 (no `= {}` default on the `options` parameter), no `Symbol.toStringTag` (`Object.prototype.toString.call(new FaceDetector())` → `"[object Object]"`), and internal state (`options`/`maxDetectedFaces`/`formats`) leaks as own enumerable instance properties instead of living in private slots — same defect class as [BUG-365](../../bugs/BUG-365-FIXED.md) (`EyeDropper`). See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/shape-detection/`, 26 файлов). `run_report.py --all --root shape-detection --recursive --processes=4` — 2:16, 23 отобранных id, все `.https.`/`.sub.https.`: **0/23 harness OK**, все TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` — не новая находка. Shape Detection API реально реализован как Phase 0 заглушка (`crates/js/src/shape_detection.rs`, doc-комментарий точно описывает поведение: `FaceDetector`/`BarcodeDetector`/`TextDetector` всегда возвращают пустой массив) — не баг сам по себе. Живая проба через `--mcp-live-port` нашла [BUG-677](../../bugs/BUG-677-OPEN.md): все три класса не совпадают с WebIDL-формой — `constructor.length === 1` вместо 0 (нет `= {}` default), нет `Symbol.toStringTag` (`Object.prototype.toString.call()` → `[object Object]`), внутреннее состояние (`options`/`maxDetectedFaces`/`formats`) утекает как собственные перечисляемые свойства инстанса вместо приватных слотов — тот же класс, что [BUG-365](../../bugs/BUG-365-FIXED.md) (`EyeDropper`)
