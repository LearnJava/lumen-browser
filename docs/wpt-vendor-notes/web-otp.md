# WPT vendor notes — `web-otp`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-09 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-web-otp`, `docs/wpt-status.md`), scope 🚫 (out of scope — WebOTP/SMS). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `web-locks` category, 3 files (`META.yml`, `WEB_FEATURES.yml`, `idlharness.https.window.js` — the category's only test file). `run_report.py --all --root web-otp --recursive` selected 1 id: **0/1 harness OK**. The lone `.https.window.html` id TIMEOUT on the standard TLS `UnknownIssuer` gap ([BUG-657](../../bugs/BUG-657-OPEN.md)). Live probe (`--mcp-live-port`) of the category's own API confirmed `OTPCredential`'s constructor throws `TypeError: Illegal constructor` (spec-correct) and `navigator.credentials.get({otp:{transport:['sms']}})` rejects `NotSupportedError` — expected for a 🚫-scope category, not a bug. A first probe pass (on a not-yet-rebuilt binary) reproduced the pre-fix [BUG-366](../../bugs/BUG-366-FIXED.md) symptom on the shared `navigator.credentials` object (methods as own instance properties, `_get_original` leaking) — a stale-binary artifact, not a regression: BUG-366 had merged to `main` at 11:39 the same day but the root worktree's `target/dev-release/lumen.exe` predated that merge (built 10:27); a rebuild reproduced the fixed shape (`hasOwnProperty` false for all four methods, `_get_original` absent, correct `Symbol.toStringTag`). No new bug filed this pass. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

WebOTP (SMS). Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/web-otp/`, `META.yml`+`WEB_FEATURES.yml`+`idlharness.https.window.js`, `LICENSE-WPT.md` скопирован из `web-locks`), включена несмотря на скоуп 🚫 (единственный тест-файл). `run_report.py --all --root web-otp --recursive` отобрал 1 id: **0/1 harness OK**. Единственный `.https.window.html` TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer` ([BUG-657](../bugs/BUG-657-OPEN.md)). Живая проба (`--mcp-live-port`, свежепересобранный бинарник — первый прогон случайно попал на бинарник до мержа [BUG-366](../bugs/BUG-366-FIXED.md) в 11:39, пересборка обязательна) несущего объекта категории — `navigator.credentials`/`OTPCredential` — показала: `OTPCredential` реализован как корректный 🚫-скоуп заглушечный конструктор (`new OTPCredential()` бросает `TypeError: Illegal constructor`, спека это и требует), `navigator.credentials.get({otp:{transport:['sms']}})` корректно отклоняется `NotSupportedError` — не баг, тот же паттерн, что `fedcm`. Проба несущего `navigator.credentials` подтвердила, что фикс [BUG-366](../bugs/BUG-366-FIXED.md) держится: `create`/`get`/`preventSilentAccess`/`store` — на прототипе (`hasOwnProperty` всех четырёх — `false`), `_get_original` не течёт, `Symbol.toStringTag` корректен. Новый номер бага не заводился
