# WPT vendor notes — `user-timing`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/user-timing/`, 44 файла + внекатегорийный `/common/performance-timeline-utils.js`, довендорен в `tests/wpt/common/`). Самопитающаяся категория — 0 `.https.`, 0 `testdriver.js`, 0 variant-хитов. Прогон `run_report.py --all --root user-timing --recursive`, ~1:42, 36 отобранных id: **27/36 harness OK, 66/176 сабтестов**. Заведён [BUG-696](../bugs/BUG-696-OPEN.md): `performance.mark()`/`.measure()` (`dom.rs:8366-8392`) не валидируют аргументы вовсе — ни `SyntaxError` на зарезервированных именах вроде `navigationStart` (`invoke_with_timing_attributes.html`), ни `TypeError` на невалидных mark-options (`123`/`NaN`/строка/отрицательный `startTime`, `mark-errors.any.html`), ни `SyntaxError`/`TypeError` на несуществующих метках или конфликтующих полях в `measure()` (`measure-exceptions.html`, `measure_exception.html`, `measure_exceptions_navigation_timing.html`, `measure_syntax_err.any.html`), `detail` не клонируется вовсе (`structured-serialize-detail.any.html`). Второстепенный сигнал — реконфирмация [BUG-687](../bugs/BUG-687-OPEN.md) (`PerformanceMark`/`PerformanceMeasure` — plain-объекты, нет `toJSON()`) и [BUG-360](../bugs/BUG-360-FIXED.md) (`<body onload="...">` мёртв на момент прогона — 7 TIMEOUT-файлов: `clearMarks.html`/`clearMeasures.html`/`mark.html`/`measure.html`/`measures.html`/`measure_associated_with_navigation_timing.html`/`measure_navigation_timing.html`; фикс влит в main параллельно тем же днём, повторный прогон не переснят), плюс известный невендоренный `idlharness.js` (`idlharness.any.html` TIMEOUT). Один новый номер бага
