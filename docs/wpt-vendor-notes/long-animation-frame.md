# WPT vendor notes — `long-animation-frame`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-long-animation-frame`, `docs/wpt-status.md`), scope ⬜ (candidate, Long Animation Frame Timing API / `PerformanceLongAnimationFrameTiming`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 57 files (`META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `loading` category, 40 top-level test files incl. one `.worker.js`, `resources/` — 13 helpers). Out-of-category helpers not vendored: `/common/dispatcher/dispatcher.js`, `/common/utils.js`, `/resources/testdriver-actions.js`+`testdriver-vendor.js`, same documented survey gap as `FileAPI`/`ai`. No `variant` hits, no `.https.` files, 6 `testdriver.js` hits. `run_report.py --all --root long-animation-frame --recursive` completed in ~4:03 (40 selected ids): **18/40 harness OK, 2/93 subtests passed**. The Long Animation Frame API is entirely absent — a `crates/js/src/dom.rs` grep found no `PerformanceLongAnimationFrameTiming` and `'long-animation-frame'` is not in `PerformanceObserver.supportedEntryTypes` — so most FAIL/TIMEOUT are a `PerformanceObserver` callback that never fires (same pattern as `layout-instability`/`largest-contentful-paint`); the two passing subtests are incidental setup assertions, not LoAF behavior. One re-confirmed defect, not a new one: `elementDocument.contains is not a function` on 4 tests — [BUG-462](../bugs/BUG-462-OPEN.md)/[BUG-574](../bugs/BUG-574-OPEN.md) (`Node.prototype.contains` missing entirely). No bug filed for the LoAF gap itself: first pass documenting an absent candidate feature. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/long-animation-frame/`, 57 файлов: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `loading`, 40 тестовых файлов верхнего уровня включая один `.worker.js`, `resources/` — 13 хелперов), скоуп ⬜ (кандидат, Long Animation Frame Timing API / `PerformanceLongAnimationFrameTiming`). `run_report.py --all --root long-animation-frame --recursive` — ~4:03, 40 отобранных id (без `.https.`): **18/40 harness OK, 2/93 сабтестов**. API целиком отсутствует — грep `crates/js/src/dom.rs` не нашёл `PerformanceLongAnimationFrameTiming`, `'long-animation-frame'` не входит в `PerformanceObserver.supportedEntryTypes` — большинство FAIL/TIMEOUT это `PerformanceObserver`, вечно ждущий недостижимую запись (тот же паттерн, что `layout-instability`/`largest-contentful-paint`); 2 прошедших сабтеста — попутные setup-проверки, не поведение LoAF. Одно переподтверждение, не новая находка: `elementDocument.contains is not a function` на 4 тестах — [BUG-462](../bugs/BUG-462-OPEN.md)/[BUG-574](../bugs/BUG-574-OPEN.md) (`Node.prototype.contains` отсутствует целиком). Новых багов не заведено — первый проход отсутствующего кандидатного API.
