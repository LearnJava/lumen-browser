# WPT vendor notes — `annotation-model`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-model`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 290 files (259 JSON fixtures under `definitions/`/`examples/`/`tools/samples/`/`tools/tests/`, `scripts/` third-party libs `ajv.min.js`/`JSONtest.js`/`showdown.min.js`, 7 `*-manual.html`). No out-of-category helpers. Entirely manual by spec design (README: no general automation mechanism, only a textarea-driven `JSONtest.js` harness for a human to paste client JSON output into) — all 7 HTML files are `-manual.html`, and `run_report.py --all --root annotation-model --recursive` explicitly excludes `-manual.html` and found no `.any.js`/`.window.js` either, so it selected 0 tests (`no tests selected`) — the first vendored category with zero automatically runnable tests, a harder gap than the `testdriver.js`/`crashtest` gaps in `accname`/`accessibility`. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-23 (`tests/wpt/annotation-model/`, 290 файлов: `definitions/`+`examples/`+`tools/samples/`+`tools/tests/` — 259 JSON-фикстур, `scripts/` — `ajv.min.js`/`JSONtest.js`/`showdown.min.js`, 7 корневых/вложенных `*-manual.html`). Внекатегорийных хелперов не обнаружено. Категория целиком состоит из ручных тестов по дизайну спеки (README: «no general automation mechanism … implementors could take advantage of the plumbing … to push their data into the tests», один `JSONtest.js`-харнесс на textarea с JSON-вводом) — все 7 HTML-файлов носят суффикс `-manual`, а `.any.js`/`.window.js`-файлов в категории нет вовсе, поэтому `run_report.py --all --root annotation-model --recursive` (который явно исключает `-manual.html`, см. `run_report.py`) отобрал 0 тестов (`no tests selected`) — первая вендоренная категория без единого автоматически прогоняемого теста, более жёсткий гэп, чем `testdriver.js` в `accname`/`accelerometer` или `crashtest` в `accessibility`. Не заводился отдельный BUG-NNN — не движковый баг, а изначально задуманный тип тестирования спеки
