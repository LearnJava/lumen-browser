# WPT vendor notes — `subapps`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-06 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-subapps`, `docs/wpt-status.md`), included despite its 🚫 scope note (PWA installation) by the same standing user decision as `accelerometer`/`acid`/`ai`/… (see `appmanifest` entry above for the full precedent chain). Same pinned commit, `git sparse-checkout add` at the same commit hash, `LICENSE-WPT.md` copied from the sibling `streams` category, 9 files (`resources/subapps-helpers.js`, 7 root `.tentative.` test files, 1 `idlharness.tentative.https.window.js`). No `variant` hits; 2 files (`add-error`/`add-success`) reference `testdriver.js`, but neither actually calls `test_driver.*` before hitting the TLS gap below; one out-of-category dep pair unvendored (`idlharness.tentative.https.window.js` → `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented gap as `beacon`/`animation-worklet`). `run_report.py --all --root subapps --recursive` completed in 2:00 (8 selected ids): **1/8 harness OK, 1/1 subtest passed**. 7 of 8 ids are `.https.` and TIMEOUT on the standard TLS `UnknownIssuer` gap (see `WPT-VENDOR-storage-access-api`'s entry); one of those (`list-error.tentative.https.html`) first surfaced as an `AssertionError: Got results from insecure-context-error…, expected list-error…` — reconfirmation of the already-open BUG-380 class (reused browsing context returns the previous test's stale result after a failed navigation), same as `beacon`/`browsing-topics`. The sole non-`.https.` test, `insecure-context-error.tentative.html`, ran and passed (`window.subApps`/`window.SubApps` both `undefined`). A `crates/js/src/*.rs` grep for `subapps`/`SubApps` found zero matches — the Sub Apps API is entirely unimplemented, expected for a 🚫-scope category; separately, the upstream mock itself (`resources/subapps-helpers.js`'s `isChromiumBased` guard, throws `'Unsupported browser'` otherwise) is Chromium-only, so no engine — conformant or not — could pass the 7 mocked tests even without the TLS gap. No bug filed — first pass only vendors + runs, all signal reconfirms already-open gaps. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/subapps/`, 9 файлов: `resources/subapps-helpers.js`, 7 корневых `.tentative.` тестов, `idlharness.tentative.https.window.js`). `run_report.py --all --root subapps --recursive` — 2:00, 8 отобранных id: **1/8 harness OK, 1/1 сабтест**. 7 `.https.` id — TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer`; один (`list-error.tentative.https.html`) сначала дал `AssertionError: Got results from insecure-context-error…, expected list-error…` — реконфирмация [BUG-380](../../bugs/BUG-380-FIXED.md). Единственный не-`.https.` тест (`insecure-context-error.tentative.html`) прошёл — `window.subApps`/`window.SubApps` действительно `undefined`. `window.subApps`/`SubApps` не реализован вовсе (грep по `crates/js/src/*.rs` — ноль совпадений), ожидаемо для 🚫-scope; апстримный мок сам Chromium-only (`isChromiumBased`-гвард бросает `'Unsupported browser'`) — содержательный прогон невозможен в принципе, не только из-за TLS-гэпа. Новых номеров багов не заведено
