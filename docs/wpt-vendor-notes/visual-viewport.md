# WPT vendor notes — `visual-viewport`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/visual-viewport/`, 32 файла: `META.yml`/`WEB_FEATURES.yml`, 10 `-manual` — исключены отбором, `viewport_support.js`, 19 тестовых файлов). Самопитающаяся категория — 0 `.https.`, 0 variant-хитов, 4 файла тянут `testdriver.js`. Прогон `run_report.py --all --root visual-viewport --recursive`, ~1:40, 19 отобранных id: **11/19 harness OK, 0/22 сабтестов**. `window.visualViewport` отсутствует целиком — реконфирмация уже открытого [BUG-481](../../bugs/BUG-481-OPEN.md), не новый номер. Форма отказа зависит от того, где по коду файла лежит первое обращение к `visualViewport`: внутри `test()`/`async_test().step()` — чистый `FAIL` (`TypeError: Cannot read properties of undefined`, 11 файлов); на верхнем уровне скрипта или внутри незавёрнутого коллбэка `addEventListener` — необработанное исключение до вызова `done()` → `TIMEOUT` (`viewport-no-resize-event-on-overflow-recalc.html`, `viewport-resize-event-on-load-overflowing-page.html`) или `ERROR: done() was called without first defining any tests` (`viewport-scrollbars-cause-resize.html`, тест падает до первого `test()`). Три файла с iframe (`page-and-offset-in-iframe.html`, `viewport-resize-event-on-iframe-show.html`, `viewport-resize-event-on-iframe-size-change.html`, `viewport-scrollbars-cause-resize-in-iframe.html`, `viewport-unscaled-scroll-iframe.html`) — TIMEOUT, реконфирмация уже открытого [BUG-480](../../bugs/BUG-480-OPEN.md) (`<iframe>` без отдельного browsing context, `load` не долетает). Новый номер бага не заводился
