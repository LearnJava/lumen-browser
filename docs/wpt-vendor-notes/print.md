# WPT vendor notes — `print`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/print/`, 2 файла: `WEB_FEATURES.yml` + `crashtests/reload-crash.html`; `LICENSE-WPT.md` скопирован из соседней `preload`). Категория upstream почти пуста — единственный файл манифестом классифицирован как тип `crashtest`, для которого у минимального исполнителя нет реализации (тот же класс ограничения, что `accessibility`/`acid`). `run_report.py --all --root print --recursive` завершился за ~1 с: **0/0 harness OK** (id не попал в очередь прогона вовсе). Живая проба (`--mcp-live-port`) на самом файле — `<body onload="PrintAndRefreshPage()">`, где обработчик должен вызвать `window.print()` и затем через `setTimeout` переоткрыть страницу с `?isFirstLoad=false` — показала, что `onload` не срабатывает вовсе (`document.body.onload === undefined`, URL не меняется): реконфирмация уже заведённого [BUG-360](../bugs/BUG-360-FIXED.md) (inline-обработчики событий мертвы), не специфично для печати. Вызванный вручную `window.print()` отработал штатно (лог `[window.print()] Opening print preview dialog` + реальный экспорт PDF) — сам API реализован и работает. Новых багов не заведено
