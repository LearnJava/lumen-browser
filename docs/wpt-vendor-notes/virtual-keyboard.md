# WPT vendor notes — `virtual-keyboard`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (`tests/wpt/virtual-keyboard/`, 6 файлов, 2 `-manual`), включена несмотря на скоуп 🚫 (мобильная ОС-интеграция). Прогон `run_report.py --all --root virtual-keyboard --recursive`: 3 отобранных id, **1/3 harness OK, 2/6 сабтестов**. `idlharness.https.window.html` — TIMEOUT (известный класс: `/resources/idlharness.js`+`WebIDLParser.js` не вендорены). `virtual-keyboard-policy.html` — harness OK, 2/6 сабтестов: базовые `getAttribute`/`setAttribute("virtualKeyboardPolicy", …)` работают корректно, 4 FAIL на прямом чтении IDL-свойства `element.virtualKeyboardPolicy` (рефлексия HTML-атрибута) — API не реализовано целиком, ожидаемо для 🚫-скоупа, не баг. `virtual-keyboard-type.https.html` — `ERROR: Got results from …/virtual-keyboard-policy.html` — переподтверждение уже открытого [BUG-380](../../bugs/BUG-380-FIXED.md) (стейл-атрибуция результатов при провалившейся `.https.`-навигации на переиспользуемом browsing context). Новый номер бага не заводился
