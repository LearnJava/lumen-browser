# WPT vendor notes — `top-level-storage-access-api`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (пин `35be3b44`, `tests/wpt/top-level-storage-access-api/`, 3 файла: `META.yml`, `requestStorageAccessFor-insecure.sub.window.js`, `requestStorageAccessFor.sub.https.window.js`, `top-level-storage-access-permission.sub.https.window.js`, `resources/`). Прогон `run_report.py --all --root top-level-storage-access-api --recursive`, ~35 с, 3 отобранных id: **0/3 harness OK, 1/7 сабтестов**. Весь сигнал — реконфирмация уже открытых багов, новых номеров не заведено: `document.requestStorageAccessFor` на документе из `DOMParser.parseFromString` — реконфирмация [BUG-682](../bugs/BUG-682-OPEN.md) (Storage Access API методы есть только у живого `document`, `_lumen_build_detached_document` их не зеркалит); `requestStorageAccessFor()` резолвится без проверки user gesture (Phase 0 шим «always granted») — тот же корень, что [BUG-399](../bugs/BUG-399-OPEN.md); `CreateDetachedFrame().requestStorageAccessFor` падает на `null` (`frame.contentDocument` пуст) — реконфирмация [BUG-480](../bugs/BUG-480-OPEN.md); оба `.https.` id дают `AssertionError: Got results from <previous test>` — реконфирмация [BUG-380](../bugs/BUG-380-FIXED.md); третий `.https.` id TIMEOUT на документированном TLS-гэпе `UnknownIssuer`
