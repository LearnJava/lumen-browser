# WPT vendor notes — `permissions-policy`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/permissions-policy/`, 79 файлов: `META.yml`, `README.md`, `WEB_FEATURES.yml`, ~55 корневых `*-allowed-by-permissions-policy*`/`*-disabled-by-permissions-policy*`/`*-default-permissions-policy*`/`permissions-policy-header-*`/`permissions-policy-frame-policy-*` тестов, `experimental-features/` (14 файлов), `reporting/` (34 файла — `ReportingObserver`-сценарии по фичам), `resources/` (36 хелперов, включая `permissions-policy.js`); `LICENSE-WPT.md` скопирован из соседней `permissions`). `run_report.py --all --root permissions-policy --recursive --processes=4` (~8 мин 53 с, 114 отобранных id): **26/114 harness OK, 1/48 сабтестов**. Доминирующая причина отказа — уже задокументированный TLS-гэп `UnknownIssuer` на `.https.`-тестах (~76 id) и невендоренные общие ресурсы `/common/dispatcher/dispatcher.js`+`/common/utils.js` (404 → `ReferenceError: token is not defined`, 17 `unload`-тестов в `experimental-features/`). Исполнившиеся тесты полностью реконфирмируют уже заведённые баги, новых не найдено: [BUG-361](../bugs/BUG-361-FIXED.md) (`document.permissionsPolicy.features()` всегда `[]` — 4 сабтеста `private-state-token-redemption`/`private-state-token-issuance`/`picture-in-picture`/`payment` не проходят `assert_in_array`), [BUG-358](../bugs/BUG-358-OPEN.md) (`document.location` не определён — `TypeError: Cannot read properties of undefined (reading 'href')` в `sync-xhr-reporting.html`/`sync-xhr-report-only.html`/`fullscreen-reporting.html`), [BUG-390](../bugs/BUG-390-FIXED.md) (`requestFullscreen()` не проверяет transient user activation, промис резолвится вместо ожидаемого reject — `fullscreen-reporting.html`), [BUG-462](../bugs/BUG-462-OPEN.md)/[BUG-574](../bugs/BUG-574-OPEN.md) (`Node.prototype.contains` отсутствует — `elementDocument.contains is not a function` в `fullscreen-report-only.html`), [BUG-380](../bugs/BUG-380-FIXED.md) (переиспользование browsing context — 16 ERROR `Got results from X, expected Y` вперемешку с соседними TIMEOUT). Отдельный BUG-NNN не заводился
