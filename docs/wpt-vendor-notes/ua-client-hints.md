# WPT vendor notes — `ua-client-hints`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-09 (пин `35be3b44`, `tests/wpt/ua-client-hints/`, 4 файла: `META.yml`, `WEB_FEATURES.yml`, `idlharness.https.any.js`, `useragentdata.https.any.js`). Прогон `run_report.py --all --root ua-client-hints --recursive`, ~1 мин, 2 отобранных id: **0/2 harness OK** — оба `.https.` TIMEOUT на документированном TLS-гэпе `UnknownIssuer`. `navigator.userAgentData` (Phase 0, статический профиль Chrome 114/Windows 10) функционально корректен — все ассерты `useragentdata.https.any.js` прошли бы, будь TLS-гэпа. Заведён [BUG-690](../bugs/BUG-690-OPEN.md) живой пробой (`--mcp-live-port`): `NavigatorUAData` — обычная `function` без `new.target`-guard (`new NavigatorUAData()` успешно конструируется со страницы, хотя спека не определяет конструктор) и без `[Symbol.toStringTag]` (`Object.prototype.toString.call(...)` даёт `[object Object]`) — тот же класс, что [BUG-672](../bugs/BUG-672-OPEN.md)/[BUG-677](../bugs/BUG-677-OPEN.md). Один новый номер бага
