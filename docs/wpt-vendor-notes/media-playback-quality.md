# WPT vendor notes — `media-playback-quality`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-08-05 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-media-playback-quality`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `media-capabilities`/`audio-output`/`audio-session`. Same pinned commit, `git sparse-checkout add` at the same commit hash, 4 files (`META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` copied from the sibling `media` category, `idlharness.window.js`). No `variant` hits, no `.https.` files, no `testdriver.js` hits — a single self-contained idlharness id. `run_report.py --all --root media-playback-quality --recursive` completed in ~13 s (1 selected id): **0/1 harness OK, 0/0 subtests**. The one test TIMEOUT: `idl_test is not defined`, because `idlharness.window.js` itself 404'd on its un-vendored helpers (`/resources/WebIDLParser.js`+`/resources/idlharness.js`), same documented survey gap as `media-capabilities`/`FileAPI`/`audio-session` — the test never got far enough to exercise `HTMLVideoElement.getVideoPlaybackQuality()`. A `crates/js/src/` grep for `VideoPlaybackQuality`/`getVideoPlaybackQuality` found nothing — the API is entirely unimplemented, same pattern as other absent-candidate categories. No bug filed. See `docs/wpt-status.md` for the full breakdown.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/media-playback-quality/`, 4 файла: `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md` скопирован из соседней `media`, `idlharness.window.js`), включена несмотря на скоуп 🚫 по тому же постоянному решению пользователя, что `media-capabilities`/`audio-output`/`audio-session`. Ни variant-ов, ни `.https.`-файлов, ни `testdriver.js` — единственный самодостаточный idlharness-id. Прогон `run_report.py --all --root media-playback-quality --recursive` (~13с): 1 отобранный id, **0/1 harness OK, 0/0 сабтестов**. TIMEOUT: `idl_test is not defined` — `idlharness.window.js` сам 404 на невендоренные `/resources/WebIDLParser.js`+`idlharness.js`, тот же документированный гэп, что у `media-capabilities`/`FileAPI`/`audio-session`; до вызова `HTMLVideoElement.getVideoPlaybackQuality()` тест не дошёл. Живой грep `crates/js/src/` на `VideoPlaybackQuality`/`getVideoPlaybackQuality` ничего не нашёл — API целиком не реализован. Новых багов не заведено — первый проход по отсутствующему кандидатному API
