# WPT vendor notes — `clear-site-data`

## Vendoring (`tests/wpt/VENDOR.md`)

Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-clear-site-data`, `docs/wpt-status.md`), in-scope (⬜, Clear-Site-Data API — the `Clear-Site-Data` HTTP response header). Same pinned commit, `git sparse-checkout` at the same commit hash, 28 files (`META.yml`, `WEB_FEATURES.yml`, 13 test files, `support/` — 5 Python handlers + `clear-cache-helper.sub.js`+`test_utils.sub.js`+`iframe_executionContexts.html`+`page_using_service_worker.html`+`page_with_resource.sub.html`+`send_report.html`+`service_worker.js`). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js`, `/common/dispatcher/dispatcher.js`, `/common/utils.js`, `/common/get-host-info.sub.js`, `/html/browsers/browsing-the-web/back-forward-cache/resources/helper.sub.js`, `../service-workers/service-worker/resources/test-helpers.sub.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root clear-site-data --recursive` completed fully (13 selected ids): 4 `clear-cache*` SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); `executionContexts.sub.html` harness OK but 0/2 subtests FAIL (`get_host_info is not defined`, 404 on the un-vendored `/common/get-host-info.sub.js`); `navigation.https.html`/`set-cookie-after-clear-all.https.html`/`set-cookie-after-clear-cookies.https.html`/`set-cookie-before-clear-cookies.https.html` ERROR (the latter two `WebSocket connection closed`); `set-cookie-before-clear-all.https.html`/`storage.https.html` TIMEOUT — all 6 the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`. A new manifestation of that same gap: `navigation-insecure.html` (non-https) and `resource.html` both load the category's own vendored `support/test_utils.sub.js`, which itself embeds a `{{ports[https][0]}}` placeholder — wptserve 500s on the substitution (`AssertionError: deque([])`, empty `ports["https"]`), the first time this surfaces inside a category's own vendored file rather than only in un-vendored out-of-category helpers; `resource.html` harness OK but 0/4 subtests FAIL (`TestUtils is not defined`), `navigation-insecure.html` TIMEOUT (harness hangs instead of reporting). 2/13 harness OK, 0/6 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details.

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-07-25 (коммит `35be3b44`, `tests/wpt/clear-site-data/`, 28 файлов: `META.yml`, `WEB_FEATURES.yml`, 13 тестовых файлов, `support/` — 5 Python-хендлеров + `clear-cache-helper.sub.js`+`test_utils.sub.js`+`iframe_executionContexts.html`+`page_using_service_worker.html`+`page_with_resource.sub.html`+`send_report.html`+`service_worker.js`). Внекатегорийные хелперы не довендорены: `/resources/testdriver.js`+`testdriver-vendor.js`, `/common/dispatcher/dispatcher.js`, `/common/utils.js`, `/common/get-host-info.sub.js`, `/html/browsers/browsing-the-web/back-forward-cache/resources/helper.sub.js`, `../service-workers/service-worker/resources/test-helpers.sub.js`, та же методология, что `FileAPI`/`ai`. `run_report.py --all --root clear-site-data --recursive` добежал полностью (13 отобранных id): 4 `clear-cache*` — SKIP (`Executor does not support testdriver.js`, тот же гэп, что `accname`/`attribution-reporting`); `executionContexts.sub.html` — harness OK, 0/2 сабтестов FAIL (`get_host_info is not defined`, 404 на невендоренный `/common/get-host-info.sub.js`); `navigation.https.html`/`set-cookie-after-clear-all.https.html`/`set-cookie-after-clear-cookies.https.html`/`set-cookie-before-clear-cookies.https.html` — ERROR (два последних — `WebSocket connection closed`); `set-cookie-before-clear-all.https.html`/`storage.https.html` — TIMEOUT — все 6 тот же HTTPS-порт-гэп, что `WebCryptoAPI`/`ai`/`bluetooth`. Новое проявление того же гэпа: `navigation-insecure.html` (не-https) и `resource.html` оба грузят вендоренный `support/test_utils.sub.js`, который сам содержит `{{ports[https][0]}}`-плейсхолдер — wptserve падает HTTP 500 при подстановке (`AssertionError: deque([])`, пустой `ports["https"]`), впервые внутри собственного вендоренного файла категории, а не только во внекатегорийных хелперах; `resource.html` — harness OK, 0/4 сабтестов FAIL (`TestUtils is not defined`), `navigation-insecure.html` — TIMEOUT (харнесс зависает вместо репорта). 2/13 harness OK, 0/6 сабтестов. Не заводился отдельный BUG-NNN — первый проход категории только вендорит+прогоняет
