# WPT vendor notes — `private-click-measurement`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-05 (коммит `35be3b44`, `tests/wpt/private-click-measurement/`, 2 файла: `WEB_FEATURES.yml` + `idlharness.window.js`; `LICENSE-WPT.md` скопирован из соседней `private-aggregation`), скоуп 🚫 (ad-tech, Privacy Sandbox/WebKit PCM), включена несмотря на скоуп по постоянному решению пользователя. Категория upstream почти пуста — единственный тест (`idlharness.window.html`, не `.https.`) IDL-проверяет `partial interface HTMLAnchorElement { [CEReactions] attribute unsigned long attributionSourceId; }` (сверено с апстримным `interfaces/private-click-measurement.idl`). `run_report.py --all --root private-click-measurement --recursive` (~15 с, 1 отобранный id): **0/1 harness OK**, TIMEOUT — не HTTPS-порт-гэп (файл не `.https.`), а `idl_test is not defined`: невендоренные `/resources/WebIDLParser.js`+`/resources/idlharness.js` дают 404, та же документированная методология survey-gap, что `FileAPI`/`audio-session`/`animation-worklet`/`beacon`/`cookiestore` (в `tests/wpt/resources/` лежат только `testharness.js`+`testharnessreport.js`). `grep -rli "attributionSourceId" crates/` — ноль совпадений: PCM-специфичный `HTMLAnchorElement.attributionSourceId` не реализован (отличная от уже частично реализованного WICG Attribution Reporting API `attributionsrc`, `crates/js/src/attribution_reporting.rs` — разные, несовместимые спеки под похожим названием). Раз собственный API категории целиком отсутствует по дизайну (скоуп 🚫) и харнесс не добежал по инфра-гэпу, а не по движковому дефекту, бага не заведено
