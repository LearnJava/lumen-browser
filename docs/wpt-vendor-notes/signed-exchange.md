# WPT vendor notes — `signed-exchange`

## Прогон и находки (`docs/wpt-status.md`)

Вендорена целиком 2026-08-06 (коммит `35be3b44`, `tests/wpt/signed-exchange/`, 129 файлов: `reporting/`, `resources/`, `service-workers/`, `subresource/`). `run_report.py --all --root signed-exchange --recursive` — ~8:00, 60 отобранных id: **24/60 harness OK, 0/24 сабтестов**. SXG (`application/signed-exchange`) не реализован в движке вовсе — единственное упоминание в коде (`crates/network/src/http/mod.rs`) это строка `Accept`-заголовка, парсинга/верификации подписи нет. Весь сигнал прогона без находок движка: 24 «harness OK» упали на внекатегорийной `/common/get-host-info.sub.js` (`ReferenceError: get_host_info is not defined`, документированный пробел обзора, не вендорится по политике репозитория); оставшиеся `.https.` id — TIMEOUT на уже задокументированном TLS-гэпе `UnknownIssuer`; один `ERROR` (`sxg-prefetch`) — тот же класс, что [BUG-380](../bugs/BUG-380-OPEN.md) (переиспользуемый browsing context отдаёт результаты предыдущего теста после проваленной навигации). Живая проба не проводилась — у SXG нет JS/DOM-поверхности, пробовать нечего. Новых номеров багов не заведено
