# Automation & introspection surfaces — what to use when

Reference for every built-in automation/diagnostic capability of `lumen.exe` and the workspace crates. Read this **before** writing a new debugging script, adding a `time.sleep`, or reaching for pixel-diffing — the browser probably already has a cheaper surface for the job. Audit date: 2026-07-16 (full sweep of shell/mcp/bidi-server/ipc/driver/devtools + scripts/).

Related docs: [`docs/commands.md`](commands.md) (day-to-day commands), [`docs/graphic-tests.md`](graphic-tests.md) (visual pipeline). Improvement tasks for the gaps below: `DEVX-*` rows in [`ROADMAP.md`](../ROADMAP.md) — `DEVX-1…6` (done, tooling, owner P2) and `DEVX-7…16` (planned, engine-side, owner P1, brief [`docs/tasks/p1-introspection-track.md`](tasks/p1-introspection-track.md)).

**Before adding a new surface, read [ADR-024](decisions/ADR-024-introspection-api-levels.md).** It assigns every introspection capability a level — L0 internal (Rust only, no guarantees), L1 experimental (wire-exposed under an `x-` prefix, unstable), L2 stable (versioned, removal needs an ADR) — and the level is chosen in the same commit as the capability. A new MCP/BiDi tool without an `x-` prefix and without `"experimental": true` is a review error. Identity semantics for anything geometry- or paint-flavoured come from [ADR-025](decisions/ADR-025-identity-propagation.md): `NodeId` alone is **not** a valid key (anonymous boxes reuse the parent id, `NodeId::from_index(0)` aliases the document root, pseudo-elements have no id).

---

## Quick chooser: task → tool

| I need to… | Use | Caveat |
|---|---|---|
| See the decoded HTML the parser received | `lumen --dump-source <src>` | headless, stdout |
| Check box geometry without pixel-diffing | `lumen --dump-layout <src>` | headless, stdout; text-diffable |
| Check paint order / colors / z-index | `lumen --dump-display-list <src>` | headless, stdout; text-diffable |
| Catch layout/paint-order regressions without GPU/Edge | `python graphic_tests/dump_golden.py` (`--update` to promote, DEVX-3) | fixed page set, structural diff vs `graphic_tests/dump-golden/` (DEVX-13); complements the pixel pipeline, doesn't replace it |
| Deterministic screenshot without a window | `lumen --screenshot out.png <src>` | CPU path, not at parity with windowed render (BUG-221). Page scripts **do** run (one synchronous pass through `parse_and_layout`, Canvas 2D bitmaps composited since BUG-428) — what is missing is the live loop's per-frame pumps, so timers/rAF/async work after that pass never fire |
| See the load waterfall of one navigation (find the next bottleneck) | `lumen --trace-nav out.json <src>` (PERF-1) | headless CPU path (same as `--screenshot`); Chrome Trace Event Format → open in Perfetto / chrome://tracing / edge://tracing. Phase spans (`fetch-document`→`parse-html`→`fetch-*`/`layout`→`paint`→`first-paint`) + per-resource fetch spans with byte `size`. No DNS/TLS/TTFB sub-split yet |
| User-centric metrics (FCP/LCP/TTI/TBT proxies) over the corpus + regression compare | `python scripts/perf_metrics.py` (PERF-2) over `docs/perf/corpus.txt` | derives metrics from `--trace-nav` spans (no engine code); `--repeat N` (median), `--compare <prev.json>`, `--selftest` (extraction check, no network). Journal `docs/perf/metrics.md`, runs `docs/perf/metrics-runs/*.json`. Headless single paint → fcp/lcp/tti collapse to paint-complete (ttfb/script/tbt/layout/paint are the distinct numbers) |
| A built `main` binary to A/B a perf change against | `bash scripts/perf-baseline.sh [--tests] [ref]` | prints the binary path (last line); cached by SHA under `.claude/perf-baseline/<sha>/`, so a repeat request costs nothing. First request per SHA builds in the persistent `perf-base` slot (warm `target/` → incremental). `--tests` gives the `lumen-shell` test binary (perf gates like `CC12_HOVER`/`CC12_KEY`) instead of `lumen.exe`. Do **not** hand-pick a binary from another tree: "binary ↔ SHA" is unverifiable outside cargo (fingerprints are path-bound, mtimes are rewritten by any checkout), and a wrong baseline yields a silently wrong verdict. Measure A/B **interleaved** — machine noise exceeds most effects |
| Input latency + frame smoothness as a distribution (p95/p99 + dropped frames) | `python scripts/input_perf.py` (PERF-3) in a live window | drives MCP `scroll`/`click`/`type` with `LUMEN_FRAME_LOG=1` (no engine code); parses the engine-reported frame ms (not line-arrival intervals). Three families: `frame_*` (p50/p95/p99/max + dropped over `--budget-ms`), `ack_*` (dispatch round-trip, dense), `latency_*`+`latency_n` (input→repaint, dense for scroll). Fixture `scripts/perf-fixtures/input.html`; `--selftest` (pure stats, no browser), `--compare`, `--json`. Journal `docs/perf/input.md`, runs `docs/perf/input-runs/*.json`. Discrete click/type coalesce repaints (few frames) → their reliable latency is `ack_*` |
| Full visual regression vs Edge | `python graphic_tests/run.py` (one live window per run by default, SDC-4; `--per-test-process` opts back into kill+relaunch per test) | see docs/graphic-tests.md |
| Localize which paint optimization causes an artifact | `python graphic_tests/run.py --paint-bisect NN` (DEVX-4) | runs baseline + each `LUMEN_NO_*` flag (see table below) once, prints a diff% table |
| Kill flake from `Date.now()` / `Math.random()` / rAF timestamps | `--deterministic` (+ `--viewport WxH` to keep a non-default window size, DEVX-1; `--rng-seed`/`--monotonic-clock` to override the seed / advance the clock, DEVX-16) | see CLI flags below; crates/shell/src/deterministic.rs |
| Drive the real visible window from a script | `--mcp-live-port <N>` (MCP JSON-RPC over TCP) or `--bidi-port <N>` (WebDriver BiDi over WS) | both wired to the live window (SDC-2) |
| Drive a page without a window (CI, no GPU) | `--mcp [url]` / `--mcp-port N` (MCP JSON-RPC, DEVX-5) | `InProcessSession`: CPU screenshot, persistent-V8 `eval`, DOM-level click/type/scroll — re-layouts after each mutation (DEVX-9) |
| Headless tab control (no GPU, CI) | `--ipc-server` (prints `LUMEN_IPC_PORT=<port>`) | CPU screenshots, BUG-221 parity |
| Assert on geometry/cascade/a11y in Rust tests | `lumen-driver::BrowserSession`: `layout_box_by_selector`, `computed_style_snapshot`, `query_a11y` | InProcessSession = full headless pipeline |
| Read console/network/a11y/layout of a live page | MCP resources `resource://console` / `network` / `a11y_tree` / `layout` / `screenshot` | live window only |
| Per-frame paint timings | `LUMEN_FRAME_LOG=1`; `=2` adds the wgpu phase split (`faces`/`collect`/`prep`/`encode`/`submit` + the render plan's per-pass counts, and the top-8 DisplayCommands on the femtovg path); `=2` also carries `prep-top` (the `prep` phase split: atlas / uniform slots / vertex buffers — resource creation and vertex write counted separately — / bind groups / offscreen, plus the frame's buffer count and bytes) and `state` (state commands elided, draws merged); `=3` adds `collect-top` — the wgpu `collect` phase split per DisplayCommand variant, with the culled count per variant | used by scripts/scroll_perf.py. Take `=3` only when you need to know *which command* the phase is spent on: it stamps `Instant::now()` per command, which is itself a visible share of that phase, so measurements to be compared must all be taken at the same level. It is what named BUG-405 slice 9 (`DrawSvgStroke` = 69% of `collect`); `prep-top`/`draw-sub` are what named slice 10 (`drop(pass)` = 35.2 of the 35.7 ms `encode`, while vertex-buffer *creation* — the obvious candidate — is only 2.0 of 10.3 ms) |
| Layout/cascade phase timings as a call tree | `LUMEN_PROFILE_TREE=1` | stderr; same-named sibling scopes merge into one line with a `×N` call count |
| Per-node breakdown inside the cascade (`compute_style` / pseudo-element phases) | `LUMEN_PROFILE_TREE=1 LUMEN_PROFILE_DETAIL=1` | read *shares within* a stage only — the per-node timers inflate the stage they sit in, so absolute numbers are not comparable with a stage-only run |
| GUI timeline profiler | `cargo run --features tracy` | Tracy client needed |
| Scroll performance benchmark | `scripts/scroll_perf.py`, `scripts/mt_stall_bench.py` | drives `--mcp-live-port` |
| Real-site load perf: live GUI run (tab per site), stats, journal, bug filing | `python scripts/perf_audit.py` over `docs/perf/corpus.txt` (default: one visible window, `new_tab` per site, cumulative RAM); `--phases` = headless per-phase decomposition; full protocol = skill `/lumen-perf-audit` | dev-release build; screenshots via CPU path (BUG-221) |
| Cache/memory growth diagnosis | `LUMEN_MEM_REPORT=1` (~10 s cadence dump) | TEMP instrumentation from BUG-272. Includes the render backend's own report: per-category GPU bytes on femtovg (slice 16), offscreen-layer pool state on the default wgpu backend (slice 21 — free textures / size classes / MiB vs budget / evictions / pool hits+misses) |
| Print/pagination check | `lumen --print-to-pdf out.pdf <src>` | A4 |
| Reproduce input-order bugs | `--activity-log` / `--click-log` → `activity.log` | |
| Session-health journal (panics/console.error/load fails/white-screen) → prioritize P3 fixes by frequency | `--health-log` (or `--activity-log` / `LUMEN_HEALTH_LOG=1`) → `health.log` (JSONL); report via `python scripts/health_report.py` (PERF-6) | console/render signals fire in live window only; panic in any mode |
| Reproduce a user session | `--import-session <file.lsession>` | URL + scroll restored |
| Network isolation / proxy / Tor testing | `--network-service`, `--proxy <url>`, `--tor [--tor-port N]` | |

---

## CLI flags (crates/shell/src/main.rs, `print_usage()`)

Headless one-shot: `--dump-source` · `--dump-layout` · `--dump-display-list` · `--screenshot` · `--trace-nav <out.json>` (PERF-1, load waterfall as Chrome-trace JSON) · `--print-to-pdf`.
Servers: `--ipc-server [--ipc-port N]` · `--mcp [url]` · `--mcp-port N` · `--mcp-live-port N <src>` · `--bidi-port N` · `--devtools-port N` (CDP, stub — see below). **All four TCP automation ports require a token** (ADR-024 §Access model, DEVX-15) — see below.

### Access model: mandatory per-run token (ADR-024, DEVX-15)

`--mcp-port`/`--mcp-live-port`/`--bidi-port`/`--ipc-server` bind `127.0.0.1` only, but loopback-only is not private — any local process can connect. Each of the four prints a fresh random token to stderr/stdout right next to its port line, and every connection must authenticate with it before running any other command. **No escape hatch** — there is no `--mcp-allow-anonymous` and none will be added without a new ADR (user decision 2026-07-30, ADR-024 §Decided questions Q1).

| Port | Token line | How to send it |
|---|---|---|
| `--mcp-port` / `--mcp-live-port` | stderr: `MCP token: <token>` / `[mcp] token: <token>` | `initialize` request params: `{"token": "<token>"}`. Rejected with JSON-RPC error `-32001` until a matching `initialize` succeeds; every other method is rejected the same way in the meantime. |
| `--bidi-port` | stderr: `[bidi] token: <token>` | `session.new` params: `capabilities.alwaysMatch.token`. No session (and so no browsing context) is created on mismatch — `session not created` error. |
| `--ipc-server` | stdout: `LUMEN_IPC_TOKEN=<token>` (next to `LUMEN_IPC_PORT=<port>`) | First message on the connection must be `IpcRequest::Auth{token}` → `IpcResponse::AuthOk`/`AuthErr`. Any other request before a successful `Auth` gets `AuthErr`. |

stdio-mode MCP (no `--port`) does **not** require a token — only the parent process that spawned it can reach its stdin/stdout, so a token adds nothing there. Same reasoning for the network-service IPC channel (PH1-4, child spawned by the shell itself) — out of ADR-024's scope, unauthenticated.

Token generation/comparison: `lumen_core::auth` (`generate_token`, `tokens_match` — constant-time). All in-repo consumers of these ports were updated in the same commit: `graphic_tests/run.py` (`LumenIpcClient`, `LiveWindowClient`), `scripts/bench_scroll.py`+`scripts/miss_probe.py` (`mcp_rpc_factory`), `scripts/input_perf.py`/`scripts/mem_perf.py`/`scripts/mt_stall_bench.py`/`scripts/scroll_perf.py`/`scripts/perf_audit.py`/`scripts/scroll_blit_accept.py` (each script's own inline MCP client class), `tests/wpt/verify_s3_bidi_session.py`/`verify_devx6_bidi_scenarios.py`/`verify_s6_await_promise.py`, and `tools/wptrunner/wptrunner/browsers/lumen.py`+`executors/executorlumen.py` (`_TokenCapturingOutputHandler` taps the existing per-line output callback; `LumenBidiProtocol.connect()` merges the live token into `capabilities.alwaysMatch`).
Determinism: `--deterministic` · `--rng-seed N` · `--monotonic-clock` (DEVX-16, fixed 2026-08-02: `DetConfig` now reaches the V8 runtime in full through `V8JsRuntime::set_deterministic_mode(on, rng_seed, monotonic_clock)` — `--rng-seed N` overrides the URL-hash-derived `Math.random` seed, `--monotonic-clock` makes `Date.now()`/`performance.now()` advance by 1 ms per call, off one shared counter, instead of staying frozen at 0. QuickJS's rollback path is unchanged — no new functionality there per policy) · `--viewport WxH` (DEVX-1: pins the window's CSS content viewport, overriding `--deterministic`'s 1280×800 default — used by `graphic_tests/run.py`'s default live window to combine determinism with the pipeline's calibrated 1024×720).
Misc: `--maximized` (window opens full-screen — live perf audit) · `--no-scrollbar` (cleaner screenshot crops) · `--activity-log` / `--click-log` · `--health-log` (PERF-6 session-health journal → `health.log`; also on with `--activity-log`) · `--import-session` · `--network-service` · `--proxy` · `--tor`.

## MCP (`crates/mcp`) — the richest scripting surface

`--mcp-live-port N <src>` runs MCP JSON-RPC over TCP against the **live window**. All 8 legacy tools wired: `navigate`, `new_tab` (opens a tab, makes it active, navigates — used by the live perf audit to give every site its own tab), `wait` (conditions: `document_ready` / `visible` / `stable` / `network_idle` / `js_idle`), `click`, `type`, `scroll`, `eval` (JS), `query` (CSS selector → DOM nodes). All 6 resources wired: `resource://screenshot` (PNG, CPU path), `resource://a11y_tree`, `resource://layout` (box model JSON), `resource://console`, `resource://network`, `resource://x-display-list` (DEVX-14, see below).

Plus one **ADR-024 L1** tool, `x-explain-element` (DEVX-10): `{selector}` → causal chain `in_dom` → `style_applied` → `in_layout` → `size` → `creates_stacking_context` → `commands_emitted`/`clip_depth`/`layer` (the last three from `ProvenanceIndex`, DEVX-7) → `heuristic` (labelled guess, not fact). `tools/list` marks it `"experimental": true` — the `x-` prefix and the flag mean it may change shape or vanish between commits (ADR-024); the 8 legacy tools above predate the ADR and are intentionally not marked, not promoted, and not renamed (ADR-024 §Decided questions Q2). Wired against `InProcessSession`/`WinitSession`; live window returns the all-default `ExplainElement` (`in_dom: false`) — `explain_element` needs per-selector layout/provenance data, which the live channel still doesn't carry even after DEVX-14 wired the whole-page `layout_snapshot`/`network_log`.

Plus a page-level `x-explain-page` (DEVX-11, also L1): no arguments, returns DEVX-8a/8b invariant-firing counts by category plus page telemetry (box/anonymous-box/overflow counts, command count, max clip depth, `relayout_count`, phase timings) — for comparing one page against a previous run, not standalone reading.

Plus 6 **ADR-024 L1** `x-scope-*` tools (DEVX-12): `x-scope-layout`/`x-scope-query`/`x-scope-screenshot`/`x-scope-display-list` (read-only, `{selector}` scoped to that element's subtree instead of the whole page — `x-scope-query` additionally takes `root_selector`) and `x-scope-relayout`/`x-scope-eval` (mutating: recompute layout for `{selector}`'s subtree only, reusing `mark_dirty`/`lay_out_incremental` from `lumen_layout::incremental` directly instead of a full-page relayout). `x-scope-relayout` recomputes `ComputedStyle` for the selector's own node only (not its descendants) before laying out — a direct `el.style.x = ...` mutation is picked up, but a change that should cascade to children (e.g. `font-size`) is not; that needs the full relayout DEVX-9 already wires after `click`/`type`/`eval`. Wired against `InProcessSession` fully; `WinitSession` has the 4 reads but not the 2 mutations (its `eval` already runs against a one-shot DOM snapshot with nothing to relay out); live window still has this gap (DEVX-14 closed the whole-page reads, not the selector-scoped ones).

Plus **ADR-024 L1** `resource://x-display-list` and tool `x-computed-style` (DEVX-14): the resource is the whole-page display-list text dump (`lumen --dump-display-list`'s output, mechanically exposed over the wire) — complements `x-scope-display-list`'s single-subtree dump. The tool is `{selector}` → computed CSS properties (property name → resolved value), `null` if nothing matches — `BrowserSession::computed_style` existed in-process already, just wasn't reachable from an MCP client before. Both wired against `InProcessSession`/`WinitSession`; live window has neither — `AutomationCommand` carries no display-list or computed-style variant yet (same category of gap `x-explain-element` has).

Headless variants `--mcp` / `--mcp-port` (default build, `v8`+`cpu-render` features): `screenshot`/`eval`/`click`/`type`/`scroll` are wired against `InProcessSession` (DEVX-5, complete). Slice 1 gave `screenshot` (CPU tiny-skia path, `cpu-render` — a **default** feature of `lumen-driver`, no GPU adapter needed) and `scroll` (wired to the existing off-main-thread `scroll_page_by`). Slice 2 gave `eval`/`click`/`type` a persistent V8 runtime (`v8`, now also default) installed on the DOM at navigation time (`Arc<Mutex<Document>>`, re-installed on every `navigate()`) — JS-side mutations persist across `eval()` calls, unlike the live window's per-navigation-only runtime — plus synthetic mouse/keyboard event dispatch (`click`/`type` — `mousedown`→`mouseup`→`click` via `_lumen_dispatch_mouse_event`, `lumen_paint::hit_test` for point-based `click`; per-char `keydown`/`input`/`keyup` for `type`). **BUG-338 (2026-08-06):** `scroll`'s `target` (a CSS selector/`NodeId`/point) is no longer ignored — `InProcessSession::scroll`/`WinitSession::scroll` resolve it to a `NodeId` and route the delta to its nearest scrolling `overflow:auto`/`scroll` ancestor (`lumen_layout::find_scroll_container_for_node`), falling back to the old whole-page `scroll_page_by` only when there's no target, no DOM match, or no scrolling ancestor (e.g. `body`). The **live-window** path (`--mcp-live-port` → `AutomationCommand::Scroll`) still doesn't carry `target` on the wire — that gap is unfixed, tracked as a BUG-338 follow-up.

`graphic_tests/run.py`'s default live window (DEVX-1, SDC-4) also spawns with `--deterministic --viewport 1024x720` (kills Date.now/Math.random/rAF flake in JS tests like TEST-57/129-138 while keeping the pipeline's calibrated viewport) and reads `resource://console` after every test — a `console.error` FAILs the test and its text lands in the HTML report, independent of the pixel diff. `resource://console` for `LiveWindowSession` round-trips through a new `AutomationCommand::ConsoleLog` to the shell's DevTools console buffer (cleared on every `navigate()`); `resource://layout`/`resource://network` do the same since DEVX-14, through new `AutomationCommand::LayoutSnapshot`/`NetworkLog` variants — the latter reads the same shared `NetworkLog` the DevTools network panel renders from (`size_bytes` always 0, unlike `InProcessSession`'s network log — the panel's own `NetworkEntry` doesn't track response size).

## Golden dump gate (`graphic_tests/dump_golden.py`, DEVX-3)

Runs `lumen --dump-layout`/`--dump-display-list` over a fixed 6-page set (`samples/page.html`, `samples/test-06-layout.html`, and three `graphic_tests/*.html` pages covering table/grid/flex/transform-stacking) and diffs the stdout against committed golden files in `graphic_tests/dump-golden/<page>.{layout,display-list}.txt`. `--update` promotes the current output to the new golden; a mismatch exits 1. No GPU, no Edge, no ffmpeg — catches geometry/paint-order/z-index regressions cheaply and cross-platform, complementing (not replacing) the pixel pipeline.

**Mismatch diff is structural, not textual (DEVX-13, `graphic_tests/structural_diff.py`).** Both dump formats are parsed into a tree — the layout dump is already indentation-nested; the flat display-list dump is re-nested by matching `Push*`/`Pop*` pairs (clip/transform/opacity/scroll-layer = stacking context). The diff walks both trees positionally and reports, per differing node, a path (box/command kind + sibling index at each level) and the changed attribute — e.g. `Block[0]/Block[1]/Block[2]: p: (0.00, 0.00, 0.00, 0.00) -> (10.00, 0.00, 0.00, 0.00)` — instead of a line-numbered unified diff. Layout-dump attributes carry real CSS-ish names (`p=`, `bg=`, `w=`, ...); most paint commands are positional (`FillRect (rect) color`, no `key=`), so their changed argument is named `#1`/`#2`/... by position rather than by name (see the module docstring for why a full per-command name table isn't worth the duplication risk). If the structural walk finds no differing node despite the raw texts differing (e.g. a stray trailing newline), it falls back to a normal unified diff rather than staying silent.

## WebDriver BiDi (`crates/bidi-server`, `--bidi-port N`)

Live-window MVP (SDC-2): `browsingContext.navigate` (blocks on real `document.readyState`) / `captureScreenshot` / `setViewport`, `script.evaluate` / `callFunction` / preload scripts, `input.performActions` (pointer+key subset), `session.*`, `network.setOfflineStatus` / `addIntercept`+`continueRequest`/`failRequest` (`beforeRequestSent` phase only — genuinely pauses a matching request against `lumen_network`'s process-global registry and resumes/fails it by id, `crates/network/src/intercept.rs`), `browser.setTimezoneOverride`, `emulation.setUserAgentOverride` — all verified live-effect by DEVX-6's `tests/wpt/verify_devx6_bidi_scenarios.py` ([BUG-295](../bugs/BUG-295-FIXED.md), closed 2026-08-06). Still **accepted-and-stored only, no live-window effect**: `network.continueResponse`/`continueWithAuth` and the `responseStarted`/`authRequired` intercept phases (documented residual of BUG-295). `storage.getCookies|setCookie|deleteCookies` likewise unverified against a live window (not covered by DEVX-6). Without a window the server falls back to an in-memory stub state. Consumer today: `tools/wptrunner` plugin (session negotiation only).

## IPC (`crates/ipc`, `--ipc-server`)

Length-prefixed bincode over TCP loopback: `CreateTab` / `NavigateTab` / `Screenshot` (CPU PNG) / `CloseTab` (+ `Fetch`/`Ping`/`Shutdown` for the network service). Consumer: `graphic_tests/run.py --ipc`.

## lumen-driver (`crates/driver`) — for Rust-side tests

`BrowserSession` trait; `InProcessSession` runs the full headless pipeline. Read-only resources ideal for **non-pixel regression tests** (DEVX-2): `layout_snapshot`, `layout_box_by_selector`, `all_layout_boxes_by_selector`, `computed_style` / `computed_style_snapshot` (typed), `query_a11y` / `query_a11y_all` (`AxQuery::Role{role,name}` — Playwright-style getByRole), `a11y_tree`, `network_log`, `console_log`, `screenshot`. Setters: `set_fingerprint_profile`, `set_user_agent`, `set_clock`, `set_rng_seed`, `freeze_fingerprint`.

Interaction (DEVX-5, cargo features `v8-backend`/`cpu-render`, both on by default in `lumen-shell`): `click`/`type_text` mutate the DOM directly (link nav, checkbox/radio toggle, `value` overwrite — no synthetic event dispatch), `scroll` drives the existing off-main-thread compositor scroll, `eval` runs against a **persistent** V8 runtime installed on the document at navigation time — unlike `WinitSession::eval`'s one-shot runtime, JS-side DOM mutations are visible to later `eval()`/`click`/`type_text` calls within the same navigation (shared `Arc<Mutex<Document>>`). **DEVX-9 (2026-08-02):** `click`/`type_text`/a successful `eval` now feed back into `layout_root`/`flat_tree` — `InProcessSession::relayout()` re-runs style+layout+display-list-build+compositor-commit, so `layout_snapshot`/`layout_box_by_selector`/`screenshot` reflect the mutated DOM, not just the state as of `navigate()`. `WinitSession` keeps the old no-relayout behavior (out of DEVX-9's scope).

Snapshot-test env vars: `SNAPSHOT_VS_EDGE_STRICT=1` (hard-gate `crates/driver/tests/cases/snapshot_vs_edge.rs`), `SAVE_CPU_SNAPSHOTS`, `SAVE_SNAPSHOTS`, `UPDATE_SNAPSHOTS` (layout/paint golden tests).

## Env toggles (rendering / engine)

| Var | Effect |
|---|---|
| `LUMEN_BACKEND` | Renderer: empty = probe (wgpu first), `femtovg`, `wgpu` |
| `WGPU_BACKEND` / `LUMEN_NO_BACKEND_PROBE` | Force / skip GPU backend probe |
| `LUMEN_NO_ENGINE_THREAD=1` | **Rollback** for the off-thread layout engine thread, which is **on by default since ADR-023** (was opt-in `LUMEN_ENGINE_THREAD=1`, ADR-016 M2.2). `LUMEN_ENGINE_THREAD=0` also disables it; a leftover `=1` still works and now just agrees with the default |
| `LUMEN_RENDER_THREAD` | Render thread on/off |
| `LUMEN_PRESENT` | Present mode override |
| `LUMEN_TEXTURE_POOL_MB=<N>` | Byte budget of the wgpu offscreen-layer pool's free list (default 64; `0` removes the cap = pre-BUG-272-slice-21 behaviour). A/B memory measurements on one binary |
| `LUMEN_NO_FRAME_SKIP` / `LUMEN_NO_SCROLL_COMPOSITOR` / `LUMEN_NO_ANIM_SPLIT` / `LUMEN_NO_BBOX_SCISSOR` / `LUMEN_NO_BBOX_BACKDROP` / `LUMEN_NO_IMAGE_MIPS` / `LUMEN_NO_BAND_BIAS` / `LUMEN_NO_SVG_AA` / `LUMEN_NO_ROT_AA` / `LUMEN_NO_ROT_CLIP` | Disable one paint optimization each — **the paint-regression bisection kit** (crates/engine/paint/src/renderer.rs), driven automatically by `run.py --paint-bisect NN` (DEVX-4) |
| `LUMEN_EAGER_PIPELINES=1` | **Rollback + A/B lever** for lazy wgpu pipeline compilation (BUG-406): compiles all 18 pipelines (5 eager + 13 lazy, of which mask-layer is two pipelines in one cell) at construction instead of the 5 hot ones. Run it once after touching any `build_*_pipeline` — a lazy pipeline that is never built is never validated by wgpu. Since BUG-405 it walks the same single list as the warm-up (`PipelineDeps::build_all_lazy`); the old separate list had drifted and no longer forced `rrect_clip`/`path_clip` |
| `LUMEN_NO_PIPELINE_WARMUP=1` | **Rollback + A/B lever** for the background warm-up of the lazy pipelines (BUG-405): each one is compiled again on the frame that first needs it — i.e. mid-scroll, which is the whole defect. Windowed path only; headless never spawns the thread |
| `LUMEN_NO_SPLIT_SUBMIT=1` | **Rollback + A/B lever** for chunked frame submission (BUG-405 slice 2): the frame is encoded into one command list again instead of being submitted every 8 render-plan items. Only affects frames with many passes (a band re-paint during scroll); short frames never reach the chunk size either way |
| `LUMEN_NO_SHADER_RRECT_CLIP=1` | **Rollback + A/B lever** for the shader rounded clip (BUG-405 slice 4): every `PushClipRoundedRect` opens an offscreen level again and `PopClip` composites it through the contour (three passes per clip) instead of carrying the contour in a group-0 uniform slot. The pixel gate of that slice is exactly this arm — run any graphic test both ways on one binary |
| `LUMEN_NO_CULL_MERGE=1` | **Rollback + A/B lever** for merging the parent pass back around a culled offscreen level (BUG-405 slice 5): a level dropped by viewport-cull (empty bbox or entirely off-surface) leaves the parent's pass split in two again, as it did before the slice — 316 such levels per `lenta.ru` scroll run cost 244 extra `draw` passes. The same arm is available per instance via `Renderer::set_cull_merge_enabled(false)`, which is how the pixel-identity gate runs both arms in one process |
| `LUMEN_NO_BLUR_MERGE=1` | **Rollback + A/B lever** for merging the vertical blur pass with the composite (BUG-405 slice 6): a filter group with a blur costs three passes again (blur H into scratch, blur V back into the level, composite into the parent) instead of two. Every `box-shadow` with a blur radius goes through this path — `emit_box_shadows` paints the shadow as `PushFilter [blur]` + one rounded rect. The same arm is available per instance via `Renderer::set_blur_merge_enabled(false)`, which is how the pixel gate runs both arms in one process |
| `LUMEN_NO_NESTED_SHADER_CLIP=1` | **Rollback + A/B lever** for the *second* shader clip contour (BUG-405 slice 8): a `PushClipRoundedRect` nested inside an already-active rounded clip opens an offscreen level again (three passes) instead of riding in the same group-0 uniform slot as a second contour. Separate from `LUMEN_NO_SHADER_RRECT_CLIP` on purpose — that one disables the shader clip wholesale and so cannot separate slice 8 from slice 4. On `lenta.ru` this arm is the difference between 52 and 178 passes per scroll run. The same arm is available per instance via `Renderer::set_nested_shader_clip_enabled(false)`, which is how the pixel gate runs both arms in one process |
| `LUMEN_NO_COVERAGE_CACHE=1` | **Rollback + A/B lever** for the coverage-quad memo cache (BUG-405 slice 9): the edge coverage of an SVG fill/stroke soup (and of a rotated `FillRect`) is re-rasterised on the CPU on every frame again instead of being looked up by the soup's exact bits. On `lenta.ru` this arm is the difference between 67.6 ms and 29.5 ms of `collect` per scroll run — the header icons are 16 distinct soups re-rasterised 704 times. Unlike the other levers here the two arms are **byte-identical** (memoisation of a pure function), which is what the pixel gate asserts. The same arm is available per instance via `Renderer::set_coverage_cache_enabled(false)` |
| `LUMEN_NO_SVG_SHAPE_CACHE=1` | **Rollback + A/B lever** for memoising a whole SVG shape (BUG-405 slice 12): a `DrawSvgFill`/`DrawSvgStroke` command re-tessellates its contours, lays out the soup and hashes it on every frame again, instead of being looked up by the command's *input* bits. With slice 9's coverage cache already hitting 100%, that key-building work was the entire remaining cost of the command — 17.5 µs against 1.9 µs for `DrawText`; the arm is the difference between 12.29 ms and 4.29 ms of `DrawSvgStroke` per `lenta.ru` scroll run. Both arms are **byte-identical** (memoisation of a pure chain, colour multiplied in afterwards), which is what the pixel gate asserts. The same arm is available per instance via `Renderer::set_svg_shape_cache_enabled(false)` |
| `LUMEN_NO_STATE_ELISION=1` | **Rollback + A/B lever** for eliding repeated render-pass state commands and coalescing adjacent draws (BUG-405 slice 10): every draw op sends its pipeline, both bind groups and its vertex buffer again even when the pass already has exactly those, and every op gets its own `draw` call. Pass commands are paid for in `drop(pass)`, where `wgpu-core` replays them into the command list — 35.2 of the 35.7 ms `encode` phase of a `lenta.ru` scroll run, growing with the op count (85 ops → 0.14 ms, 310 → 2.14 ms). A typical scroll frame elides 125 commands and merges 20 draws. Both arms are **byte-identical** (`draw(a..c)` submits the same primitives in the same order as `draw(a..b)`+`draw(b..c)` when nothing changed in between), which is what the pixel gate asserts. The same arm is available per instance via `Renderer::set_state_elision_enabled(false)` |
| `LUMEN_SCROLL_BLIT` / `LUMEN_NO_FAST_SCROLL_DEGRADE` | Scroll-blit opt / fast-scroll quality degrade |
| `LUMEN_NO_STDERR_SINK=1` | **Rollback + A/B lever** for the non-blocking stderr sink ([BUG-770](../bugs/BUG-770-FIXED.md), `crates/shell/src/diag_stderr.rs`): `eprintln!` writes straight to the inherited stderr handle again, so a parent that captures stderr as a pipe and stops reading it blocks the next writing thread — with the UI thread that freezes the window mid-run. The sink is installed only when stderr *is* a pipe, so this flag changes nothing for a console or a file. Both arms of the fix's gate run off this one binary |
| `LUMEN_FRAME_LOG=1\|2` · `LUMEN_PROFILE_TREE=1` (+ `LUMEN_PROFILE_DETAIL=1`) · `LUMEN_MEM_REPORT=1` · `LUMEN_BENCH` / `LUMEN_BENCH_ITERS` | Diagnostics (see chooser table) |

## Known stubs — do NOT rely on these

- **CDP `--devtools-port`**: only `Browser.getVersion` is real; `DOM.getDocument` returns an empty document, `*.enable` are ACK stubs, everything else → `-32601`. Use BiDi or MCP instead; no DEVX task (BiDi/MCP cover the need).
- **CPU screenshot path** (`--screenshot`, `--ipc`, `resource://screenshot`, and now headless MCP's `screenshot` tool — DEVX-5): rendering not at parity with the windowed backend (BUG-221) — fine for coarse checks, not for the Edge-diff gate. `InProcessSession`'s `screenshot`/`screenshot_cpu_rgba` re-layout after a `click`/`type_text`/`eval` mutation since DEVX-9 (2026-08-02); the other CPU-screenshot entry points (`--screenshot`, `--ipc-server`) run a single synchronous script pass with no interactive mutation in between, so this never applied to them. The **live window**'s `resource://screenshot` used to hand the rasterizer no images at all, so every picture came out as the grey placeholder and the snapshot read as "this browser draws no images" ([BUG-729](../bugs/BUG-729-FIXED.md), fixed 2026-08-09) — it now passes the shell's decoded-image cache; Canvas 2D bitmaps are still absent there (they reach paint only through the per-frame drain into the GPU renderer).
- **`InProcessSession` renders (driver `screenshot_cpu_rgba`, headless MCP `screenshot`, the `cases::snapshot_cpu` gate)**: the session now runs the page's **inline classic** `<script>` elements before layout and hands the resulting Canvas 2D bitmaps to the rasterizer (BUG-429 fixed 2026-07-29), so scripted rendering is covered. Still missing on this path: external `<script src>` (deliberately not fetched — a deterministic offline gate must not depend on the network; the count of skipped ones goes to stderr), a module's *imports* — inline `<script type="module">` itself now runs ([BUG-350](../bugs/BUG-350-FIXED.md) fixed 2026-07-29), but nothing fetches the imported module graph ([BUG-446](../bugs/BUG-446-OPEN.md)) — and decoded `<img>` pixels — every image box still paints the grey placeholder, so `18-images`/`19-object-fit` gate geometry, not pixels ([BUG-430](../bugs/BUG-430-OPEN.md)). Not the same path as `--screenshot`/`--ipc-server`, which fetch subresources and run scripts through the shell (BUG-428).
- **A successful `navigate` does not mean the page loaded** ([BUG-438](../bugs/BUG-438-OPEN.md)). Every navigation surface — BiDi `browsingContext.navigate` (even with `wait="complete"`), MCP/driver `navigate` + `wait document_ready` — answers success for an unreachable URL and silently leaves the previous document live, `location.href` and all. `current_url` lies too (`LiveWindowSession::navigate` records the requested URL before attempting the load). Not `data:`-specific: a closed port, a missing file and a malformed URL all behave this way (probed 2026-08-10 under [BUG-380](../bugs/BUG-380-FIXED.md)). Verify by **document identity**, not by the navigate result and not by comparing URLs (a redirect breaks that): set a global on the outgoing document and check it is gone afterwards — `executorlumen.py`'s `RESET_EXPRESSION`/`STALE_GLOBAL` is the worked example.
- In-app DevTools panels (console/inspector/network in the shell UI) are interactive-only — not scriptable.
