# Невлитые ветки в `origin` — закрыто 2026-08-31

Записка оставлена ради одной практической вещи — **SHA удалённых веток**, по которым любую из
них можно вернуть. Разбор каждой лежит в истории коммита `b7d73bf9e`.

`origin` держал 31 ветку; влитые удалены механически, четыре оставшиеся разобраны по
содержимому и удалены по решению пользователя. **Дополнение 2026-08-31 (вечер): пятой удалена
и `p1-exp-wgpu-only`** — полигон, который до того сохраняли. Осталась одна ссылка: `main`.

## Что удалено и как вернуть

```bash
git push origin <sha>:refs/heads/<имя>
```

| Ветка | SHA на момент удаления | Почему удалена |
|---|---|---|
| `p1-p3-bfcache` | `decda3173d318e9d1b59e467fb7787fdbcc9854e` | Содержимое целиком в `main`: `BfCachePayload`, `FrozenPage`, `has_frozen()` и тестовые хелперы пришли другим путём |
| `merge-bug085-temp` | `6721d00c0364758afa7cf6aed47fadef39ca61e0` | [BUG-085](../../BUGS-FIXED.md) закрыт в `main` на три недели позже и иначе. Ветка держала в `origin` бинарник `utils/lumen.exe` 26.9 МБ и огрызки `last_session.db-*` |
| `merge-p2-bug076` | `7d29255525d34c3b3c0b8db5cf962ba16ef19bcf` | Единственный содержательный кандидат: добавляла `bounds: Option<Rect>` в `PushFilter`, чтобы offscreen-слой тени был размером с тень, а не с вьюпорт. **Проверено построчно перед удалением — это уже в `main`** (`crates/engine/paint/src/display_list.rs:722`, с той же сериализацией `bounds=(…)`) |
| `p1-wgpu-cross-platform` | `e69da6066247aa53bcf8f7068e04de12763afcda` | 10 коммитов авторства пользователя: платформо-зависимые graphics-фичи и стенд замеров скролла. Стенд уже в `main` и ушёл там дальше; остальное списано решением пользователя |
| `p1-exp-wgpu-only` | `origin` — `03b8599d733061c02c7ffa031448800b6d9b0e5c`, локальный tip — `c7bd2f1d1b77d3e86310b72a2af8b4019eb96ed2` | Удалена 2026-08-31 вечером по решению пользователя («уже не нужна»). Полигон производительности, в `main` не вливался по замыслу; всё содержательное перенесено дорожкой P1-wgpu-default ([ADR-017](../decisions/ADR-017-wgpu-default-backend.md), бриф [ph-wgpu-default.md](ph-wgpu-default.md)) |

### Отдельно про `p1-exp-wgpu-only`

Это единственная из удалённых веток, **чей журнал сохранён в репозитории**: её `EXPERIMENT.md`
(26 срезов с числами ДО/ПОСЛЕ, «Карта заимствований», «Грабли») снят дословно и лежит как
[`docs/perf/experiment-wgpu-only.md`](../perf/experiment-wgpu-only.md). Так сделано потому, что
около тридцати комментариев в `crates/engine/paint` и `crates/shell` объясняют работающий в
`main` код ссылкой «EXPERIMENT.md §N» — без архива они повисли бы в пустоту.

Особенность, которой нет у остальных четырёх: **локальная ветка опережала `origin` на два
коммита**, никогда не пушенных (`debug_mem_report()` — атрибуция GPU-памяти — и
`mem_report_probe.ps1`, сессия 2026-08-30). Их результат описан в п.26 архива, но сам код
восстановим только из reflog локального клона и только до GC; в `origin` его никогда не было,
поэтому `git push origin <sha>:...` вернёт ветку без этих двух коммитов.

## Два правила для следующей чистки

**Старую ветку нельзя вливать `git merge`.** Все четыре предшествовали перестройкам
репозитория: слияние `p1-wgpu-cross-platform` удалило бы `.claude-manager/` и воскресило
`.claude/memory/*` — раскладку, которой больше нет. Если из восстановленной ветки что-то
нужно, брать точечно:

```bash
git diff main...<ветка> -- <путь> | git apply --3way
```

**Проверять содержимое, а не название.** «В ветке есть коммиты, которых нет в `main`» ничего
не говорит о том, есть ли в `main` их содержимое. `merge-tmp-bug591-sw` выглядела застрявшей
чужой работой, а оказалась merge-коммитом, оба родителя которого уже в `main`. И наоборот:
`merge-p2-bug076` по названию читалась как доводка закрытого бага, а несла отдельную
оптимизацию offscreen-слоя — которая тоже успела прийти в `main` своим путём, но узнать это
можно было только прочитав диф.
