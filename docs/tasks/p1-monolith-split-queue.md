# Очередь SPLIT — разрезание монолитных файлов (P1)

**Developer:** P1 (батчи `crates/shell` может брать P2 — код механический)
**Крейты:** `lumen-shell`, `lumen-layout`, `lumen-paint`, `lumen-network`, `lumen-css-parser`, `lumen-dom`, `lumen-js` (добавлен 2026-08-26, см. пересчёт в §1)
**Родитель:** `SPLIT` в [`ROADMAP.md`](../../ROADMAP.md)
**Ветки:** `p1-split-<id>` (например `p1-split-sh1`)
**Заведена:** 2026-08-23, по итогам аудита здоровья кода
**Назначена P1:** 2026-08-26 — все 19 оставшихся батчей заведены строками в
`ROADMAP.md` (id `SPLIT-SH2`…`SPLIT-DM1`) и указателями в `STATUS-P1.md`; порядок
там сверху вниз и есть порядок работы. Группа SH идёт с хвоста файла к голове,
чтобы вырезка не сдвигала анкеры ещё не взятых батчей.

Один батч = одна строка в `STATUS-P1.md` = одна сессия. Каждый батч — чисто
механический перенос кода между модулями: ноль изменений поведения, ноль правок
сигнатур. Это НЕ рефакторинг API — публичная поверхность каждого крейта не меняется.

---

## 1. Контекст

Аудит 2026-08-23: workspace ≈ 545 тыс. строк, при этом 15 файлов >2000 строк,
шесть из них >19 000. Монолиты тормозят ревью, мержи и работу агентных сессий
(контекст не помещается). Цель дорожки: **ни одного файла >8000 строк**; гиганты
свести к набору файлов ≤2000 строк. Поведенческие фиксы и новые фичи в эти файлы
в этот период НЕ заводить — иначе разрез гонится за движущейся целью.

### Перепись на 2026-08-23 (метод воспроизводим)

Крупнейшие непрерывные регионы файла (границы по top-level item'ам):

```bash
python scripts/split_census.py <file>                  # карта top-level item'ов
python scripts/split_census.py <file> --inner LO HI    # карта тем внутри модуля
```

Скрипт заведён ST-0 (2026-08-27) и **заменяет однострочник `rg "^pub struct |…"`,
которым эта перепись пользовалась раньше** — тот врёт в трёх местах, и каждое
стоило дорожке отдельной осечки. Он не отличает код от строкового литерала (в
`crates/js/src/dom.rs` 45 531 строка почти целиком лежит внутри `const
WEB_API_SHIM: &str = r#"…"#`, и JS внутри начинается теми же словами); он не знает
границ item'а, а без них нельзя измерить регион — числа переписи 2026-08-23
поэтому и промахивались на 600–1 000 строк; и он не поднимает начало item'а над
его атрибутами и `///`, из-за чего вырезка по такому числу отрывает
доккомментарий от функции (осечка SH-4b, повторённая на SH-5 и трижды на SH-6).
Скрипт считает глубину скобок вне строк, raw-строк, символьных литералов и
вложенных `/* */`, проверяет себя итоговой глубиной (обязана быть 0) и печатает
счётчик item'ов без `///` над ними — машинный признак отклеившегося
доккомментария из SH-3c. Проверен на четырёх монолитах очереди (`style.rs`,
`box_tree.rs`, `js/dom.rs`, `paint/renderer.rs`): глубина сходится на всех.

| Файл | Строк (физ.) | Статус |
|---|---:|---|
| `crates/engine/layout/src/style.rs` | ~38 000 | очередь |
| `crates/shell/src/main.rs` | 29 823 | очередь |
| `crates/engine/layout/src/box_tree.rs` | 22 551 | очередь |
| `crates/engine/paint/src/renderer.rs` | 20 919 | очередь |
| `crates/engine/paint/src/display_list.rs` | 19 698 | очередь |
| `crates/engine/layout/src/lib.rs` | 19 115 | очередь |
| `crates/network/src/lib.rs` | 9 799 | очередь |
| `crates/engine/css-parser/src/parser.rs` | 9 192 | очередь |
| `crates/engine/paint/src/backends/femtovg_backend.rs` | 7 955 | очередь |
| `crates/engine/dom/src/lib.rs` | 6 934 | очередь |

НЕ резать (табличные/когерентные, монолитность безвредна): `html-parser/entities.rs`,
`layout/counters.rs`, `layout/incremental.rs`, `animation.rs`, `paint/svg_path.rs`.

**Перепись 2026-08-23 пропустила крупнейший файл workspace.** `crates/js/src/dom.rs`
— **45 541 строка**, больше `style.rs`, который значится здесь первым; рядом
`crates/js/src/v8_runtime.rs` (9 415). Крейта `lumen-js` не было ни в таблице, ни
в списке крейтов дорожки. Вскрыто 2026-08-26 гейтом `scripts/check_file_sizes.py`,
который меряет **все** `.rs` подряд вместо списка, составленного человеком, —
26 файлов длиннее 2000 строк против 10 в этой таблице. Мораль для любой будущей
переписи: список строится машиной по всему дереву, а сверяется глазами, не наоборот.

**Пересчёт на 2026-08-26** (при назначении дорожки P1): style.rs 38 031,
shell/main.rs 29 298 (после SH-1), box_tree.rs **23 470** (+919), renderer.rs 20 919,
display_list.rs 19 698, layout/lib.rs 19 155, network/lib.rs **10 217** (+418),
css-parser/parser.rs 9 192, femtovg_backend.rs 7 955, dom/lib.rs 6 998.
Два файла выросли, то есть правило «фичи в эти файлы в этот период не заводить»
не соблюдается — цель движется, и оценки батчей BT-0 и NW-0 снимать заново.

---

## 2. Железные правила каждого батча

1. **Только перенос.** Запрещено менять поведение, сигнатуры `pub`/`pub(crate)`
   функций, имена типов и полей. Меняются только пути модулей; для внешних
   потребителей крейта — `pub use` реэкспорт со старого пути.
2. **Тесты едут вместе с кодом.** Счётчик `#[test]` крейта до и после батча —
   в теле коммита. Тест-модуль разрешено перенести в файл рядом с кодом либо в
   `tests/<name>.rs`; удалять тесты запрещено.
3. **serde/bincode (ADR-008 hibernation-snapshots):** структуры с
   `derive(Serialize, Deserialize)` переносить целиком, поля и атрибуты serde не
   трогать. Переименование типа = запрет даже внутри крейта (snapshot-совместимость
   и grep-аемость истории).
4. **#[allow]-реестр.** Файловый `#![allow(missing_docs)]` при дроблении НЕ
   копировать в новые файлы автоматически: если долг реально остаётся за
   переносимым кодом — оставить allow в новом файле И обновить счётчик в
   `docs/lint-policy.md §10` тем же коммитом. Точечные `#[allow]` переносятся
   вместе с кодом как есть.
5. **Пиксели не двигаются,** но snapshot/CPU-тесты (`graphic_tests/run.py`,
   snapshot-наборы layout/paint) прогнать перед мержем — страховка от случайной
   семантической правки при вырезке.
6. **Размер батча:** один регион одного файла за сессию. Если регион >4000
   перемещённых строк — резать на подбатчи по внутренним секциям, а не тащить
   одним коммитом.
7. Правило CLAUDE.md сохраняется: clippy `-D warnings` чист по каждому крейту
   каждой ветки перед мержем.

---

## 3. Процедура батча (шаблон)

```bash
export PATH="/c/Users/konstantin/.cargo/bin:$PATH"
git worktree add .claude/worktrees/split-<id> -b p1-split-<id>
cd .claude/worktrees/split-<id>
```

1. Замер ДО: `(Get-Content <file> | Measure-Object -Line).Lines`,
   `rg -c "#\[test\]" <файлы>` — записать в черновик коммита.
2. Создать целевой модуль (`src/<name>.rs` или каталог с `mod.rs`), добавить
   `mod <name>;` туда, где жил код.
3. Вырезать регион по анкерам из таблицы ниже (границы — top-level item'ы),
   вставить как есть; поправить `use`-блоки обоих файлов. Внутрикрейтовые
   вызыватели переводить на новый путь напрямую; `pub use` со старого пути —
   только если путь используется другим крейтом.
4. Прогнать: `cargo check -p <crate>` → `cargo test -p <crate>` →
   `cargo clippy -p <crate> --all-targets -- -D warnings`. Для paint/layout —
   плюс snapshot-прогон (правило §2.5).
5. Коммит: `P1/SPLIT-<id>: <что перенесено> (<N> строк, тестов было X — стало X)`.
   Merge `--no-ff` в main, удалить указатель батча из `STATUS-P1.md`, отметить
   DONE в таблице этого файла тем же коммитом.

---

## 4. Очередь

### Группа SH — `crates/shell/src/main.rs` (10 334 строки после SH-3d)

Приоритет сверху вниз: сначала самодостаточные регионы, ядро (`impl Lumen`) — последним.

**Анкеры в этой таблице дрейфуют — пересчитывать в начале батча, не верить числу.**
Числа переписи 2026-08-23 были сняты с ещё более старого снимка (файл на дату аудита
уже был 32 088 строк, а не 29 823), из-за чего каждый анкер группы промахивался на
~600–1 000 строк. Пересчёт — одной командой:
`rg -n "^(pub )?(struct|enum|impl|fn|mod|trait|const|static|type) " crates/shell/src/main.rs`.

Вторая ловушка переписи: регион в ней **назван по первому item'у**, а не по содержимому.
SH-1 значился «view-transition слой, самодостаточный, не пересекается с ядром `impl Lumen`»
только потому, что регион открывался `struct ViewTransitionState`; на деле три типа занимали
29 строк, а остальные 3 845 были одним блоком `impl Lumen` с релейаутом, chrome и загрузкой
страницы. Всё, что относится к view transitions, — 25 упоминаний, разбросанных по циклу
событий, отдельным файлом стать не может. Перед батчем читать карту методов региона, а не
его первую строку.

| ID | Анкер (строки main.rs) | Регион | Цель |
|---|---|---:|---|
| SH-1 ✅ DONE 2026-08-26 | `impl Lumen` (9878) … до `struct PipOsWindow` (13723) | 3 633 | `src/relayout.rs` (889), `src/chrome_ui.rs` (1 353), `src/page_load.rs` (1 391). Регион оказался не view-transition слоем, а блоком `impl Lumen`; имена модулей даны по содержимому. В `main.rs` остались 5 методов (141 строка), не относящихся ни к одной из трёх тем: `find_resize_grip_node`, `open_file_picker`, `arm_fullscreen_resize`, `poll_fullscreen_resize`, `begin_zoom_preview` |
| SH-2 ✅ DONE 2026-08-27 | `struct PipOsWindow` (10160) … до `impl Lumen` (15331) | 5 170 | Каталог `src/app/` из девяти файлов: `mod.rs` (315), `resumed.rs` (157), `user_event.rs` (261), `about_to_wait.rs` (1 383), `window_event/{mod.rs (15), cursor_moved.rs (352), mouse_input.rs (976), mouse_wheel.rs (296), redraw_requested.rs (1 509)}`; плюс два PiP-типа в уже существующие `panels/{pip_os_window,doc_pip_os_window}.rs`. **Ни цель «`src/pip_window.rs`», ни имя региона не годились** — ловушка из шапки этого раздела в третий раз: на PiP приходятся 37 строк из 5 170, остальные 5 138 — один блок `impl ApplicationHandler<LoadEvent> for Lumen` (winit-цикл, шесть методов). Главное ограничение батча найдено здесь же: **реализацию трейта нельзя разложить по нескольким блокам**, а её тело втрое больше потолка `check_file_sizes.py`, — поэтому метод трейта стал переходником в одну строку, а тело переехало как `pub(crate) fn on_<метод>` в обычный `impl Lumen`, который дробить можно. Это не косметика, а единственная форма, в которой регион вообще помещается в правило размера файла. Четыре среза: PiP-типы → три метода (`resumed`/`user_event`/`about_to_wait`) → четыре крупные ветки `match event` в `window_event` → сам похудевший trait-impl. **Приём, снимающий риск вырезки на срезах 1–2 и 4: дедента нет вовсе** — `fn` в `impl Lumen` стоит на той же глубине, что и в `impl ApplicationHandler`, поэтому тела перенесены байт в байт (сверка с `HEAD` дала 0 расхождений на 295 строках trait-impl и ровно строку сигнатуры на каждом из трёх методов). На срезе 3 дедент неизбежен (ветка живёт на 16 пробелах, метод — на 8), и там сработала осечка SH-4b: три многострочных `eprintln!`, у которых строка-продолжение начинается пробелами ВНУТРИ литерала. Считать «внутри литерала» пришлось сканером по всему региону (экранирование, `r#"…"#`, символьные литералы, `//`), и он назвал ровно те три строки, что видны глазами. Ранний `return` внутри ветки остался эквивалентен, потому что `match` — последний оператор `window_event`. Атрибуты едут за своим кодом: `#[allow(clippy::{expect,unwrap}_used)]` стояли на `fn window_event`, а покрывали тела `MouseInput`/`RedrawRequested` — теперь стоят там; с похудевшего `window_event` сняты, `unwrap()`/`expect()` в нём не осталось. main.rs 22 813 → 17 638, тестов было 1624 — стало 1624, `dump_golden.py` 12/12 без дельт |
| SH-3 | фрагменты 1363–9996: `run_dump_mode` (1363), `V8PersistentJs` (3381), `impl PageSource` (3780), `fetch_vtt_text` (6009) | ~5 400 | `src/dump_mode.rs`, `src/page_source.rs`, `src/vtt_fetch.rs` (по одному мини-батчу на файл: SH-3a/b/c) |
| SH-3a ✅ DONE 2026-08-27 | `run_dump_mode` (1374) … до `impl PageSource` (3791) | 2 284 | Шесть адресов, а не один: `src/dump_mode.rs` (609), `src/automation_server.rs` (216), `src/cli_args.rs` (456), `src/layout_metrics.rs` (34), `src/persistent_js.rs` (994), плюс `chrome_ui.rs` (+19). **Цель «один `src/dump_mode.rs`» описывает первую тему из четырёх** — ловушка из шапки раздела в четвёртый раз, но, как на SH-5, замеченная ДО вырезки: между двумя анкерами батча лежат одноразовые headless-рендеры (`--dump-*`/`--screenshot`/`--print-to-pdf`/`--trace-nav`), headless-серверы автоматизации (`run_ipc_server`, `run_mcp_mode` — блокируют процесс на сокете вместо winit-цикла), разбор командной строки (`print_usage` + 18 `extract_*`) и мост персистентного JS (`PersistentJs`/`V8PersistentJs`). Три среза, по коммиту на тему. **Навигационные типы (`enum PageSource`, `NavEntry`, `JsNavigateRequest`, `DomTouchedSummary`, 133 строки) сознательно оставлены в `main.rs` для SH-3B**: объявление `enum PageSource` обязано ехать вместе с `impl PageSource`, иначе тип и его реализация окажутся в разных файлах. Две функции стояли ВНУТРИ блока `extract_*`, не относясь к нему, — `count_layout_boxes`/`count_rendered_units` (счётчики box-дерева, PERF-6, зовёт `page_load.rs`) уехали в собственный `layout_metrics.rs`, а не в соседний `display_list_metrics.rs`: тот меряет дисплей-лист, вход другой. Приём тот же, что на SH-1/SH-5, и он подтвердился на всех шести адресах: тела переносятся байт в байт (сверка с `HEAD` после снятия `pub(crate) ` — 0 расхождений на 2 284 строках), единственная правка — видимость, а приватные `use` в корне крейта работают как реэкспорт для `use crate::*`/`use super::*`, поэтому ни один вызов не тронут. **Новое здесь — cfg у реэкспорта:** он обязан повторять cfg исходного item'а, иначе сборка без фичи или без тестов ловит неиспользованный импорт; три строки уехали под `#[cfg(test)]` (`encode_images_as_pdf`, `chrome_node_changes`) и `#[cfg(all(test, feature = "v8"))]` (`popstate_eval_source`), а `V8PersistentJs` — под `#[cfg(feature = "v8")]`. Поля структуры тоже: `PrintOptions` и `V8PersistentJs.rt` конструируются из `main.rs`. main.rs 17 638 → 15 381, тестов крейта 1624 → 1624, `dump_golden.py` 12/12 без дельт |
| SH-3b ✅ DONE 2026-08-27 | `enum PageSource` (1400) … `cache_control_no_store` (1810), плюс `page_source_for_automation_url`/`resolve_js_navigation` (659) | 479 | Пять адресов: новый `src/page_source.rs` (371), новый `src/nav_history.rs` (102), плюс `persistent_js.rs` (+19), `session_persist.rs` (+12) и две строки `mod`/реэкспорта в `main.rs`. **Цель «один `src/page_source.rs`» описывала одну тему из трёх** — ловушка из шапки раздела в пятый раз, снова замеченная ДО вырезки. Анкер `impl PageSource` дрейфанул с ~2511 на 1534 (977 строк, SH-3a вырезал выше). Разрез по содержимому: «откуда берутся байты документа» (`enum`/`impl PageSource`, возвращаемый `RawPage`, вытащенный из `load_bytes` предикат `cache_control_no_store` и два конструктора `PageSource` из URL-строки, стоявшие на 700 строк выше — их доккомментарии прямо ссылаются на `PageSource::from_arg`) → `page_source.rs`; «история навигации» (`NavEntry` + `impl NavEntry`, `JsNavigateRequest`) → `nav_history.rs`. **Два залётных типа из региона к навигации отношения не имеют:** `DomTouchedSummary` числился навигационным только по адресу — его единственный пользователь во всём крейте это `PersistentJs::take_dom_touched`, то есть файл, созданный SH-3a (в `main.rs` оставалось одно вхождение, само объявление); `should_restore_session` — предикат сессии на диске, дописан в `session_persist.rs` рядом с `source_url_string`/`dom_blob_of`, которые SH-5 отправил туда по той же теме. Два среза, по коммиту на тему. Тела перенесены байт в байт (сверка с `HEAD` после снятия `pub(crate) ` — 0 расхождений на 483 строках), правок 33 и все они — видимость: поля `RawPage` читает `app/user_event.rs`, поля `NavEntry` — `navigate_by.rs` и `app/about_to_wait.rs`. **Новое здесь:** правило SH-3a про cfg реэкспорта сработало в форме «пользуются только тесты» — `cache_control_no_store` ушёл под `#[cfg(test)]`, потому что 17 совпадений грепа по `main.rs` оказались одноимённым ПОЛЕМ `RawPage`, а не вызовами функции; без cfg обычная сборка ловит unused import. Осечка SH-5 (`items after a test module`) обойдена заранее: в `session_persist.rs` вставка сделана ПЕРЕД `#[cfg(test)] mod tests`, а не в конец файла. main.rs 15 381 → 14 902, тестов крейта 1624 → 1624, `dump_golden.py` 12/12 без дельт |
| SH-3c ✅ DONE 2026-08-27 | `fetch_vtt_text` (3284 после SH-3b) … до `struct Lumen` (5969) | 2 639 | **ЧЕТЫРНАДЦАТЬ адресов, а не один `src/vtt_fetch.rs`.** Цель переписи называет первую функцию региона из пятнадцати тем — ловушка из шапки раздела в ШЕСТОЙ раз, снова пойманная ДО вырезки. Шесть срезов, по коммиту на тему: **(1)** подресурсы, которые конвейер тянет по `ResourceBase` после разбора документа (`.vtt`, `background-image`, `@font-face`) → новый `subresources.rs` (207) — **не** в `tracks.rs`, чей доккомментарий объявляет модуль сетенезависимым (фетч там за замыканием ради юнит-тестируемости), перенос сломал бы заявленный инвариант; **(2)** read-only обходы построенного дерева → новый `layout_walk.rs` (54), обратимая обрезка `#contentArea` → `chrome_ui.rs` (+172), `paint_ordered`/`next_dl_epoch` → `display_list_metrics.rs` (+15), геометрия из размера поверхности → новый `window_metrics.rs` (61); **(3)** конкретные для shell типы движкового потока и три маршрутизатора UI→JS → новый `engine_bridge.rs` (144) — **не** дописка в `engine_thread.rs`, который обобщён по `C`/`S` ровно затем, чтобы не зависеть от layout- и JS-типов (его же доккомментарий это декларирует и обновлён на новый адрес), счётчики пофазного лога → новый `frame_log.rs` (50), свободные входы в layout и диф `content-visibility` → `relayout.rs` (+276), партиции хранилищ по origin → новый `storage_stores.rs` (123); **(4)** `render_bytes`+`dispatch_preload_hints` → новый `page_pipeline.rs` (137, отдельный файл, а не `page_load.rs`: туда SH-3d повезёт `parse_and_layout` и 1 417+137+412 вышло бы за потолок), чтение фактов из документа → новый `doc_extract.rs` (121), состояние DnD → новый `input/dnd.rs` (23), `PendingWait` → `app/about_to_wait.rs` (+10, единственный потребитель); **(5)** всё про `<script>` двумя кусками региона в один новый `scripts.rs` (737); **(6)** вложенные контексты просмотра → новый `frames.rs` (542). Тела перенесены байт в байт на всех четырнадцати адресах (0 расхождений на 2 639 строках после снятия `pub(crate) `). Ловушка SH-3a про **поля** сработала пять раз подряд, теперь её стоит считать правилом, а не осечкой: `pub(crate)` типу почти всегда тянет `pub(crate)` полям (и дважды — методам `impl`), потому что читатель обычно `src/tests/`. **НОВОЕ ДЛЯ ДОРОЖКИ: два отклеившихся доккомментария найдены в ГОТОВОМ коде** — осечка SH-5 с другой стороны. Доккомментарий `route_task_js` был разорван вставленным между его строк `frame_log_nanos` (первые четыре строки стояли над счётчиком, собственный блок функции начинался со строчной буквы), доккомментарий `ls_store_for_base` приклеен к `lumen_idb_dir`, а сама функция осталась без документации. Оба воссоединены при переносе — единственная правка текста в батче. **Признак, по которому их видно машинно:** top-level item без `///` над ним (скрипт из восьми строк по всему региону дал восемь кандидатов, из них два — настоящие). `struct Lumen` и три соседних view-transition типа оставлены в `main.rs` для SH-6: объявление обязано ехать вместе с `impl Lumen`, как `enum PageSource` ехал с `impl PageSource`. Головной фрагмент выше анкера выделен в SH-3d. main.rs 14 902 → 12 263, тестов 1624 → 1624, `dump_golden.py` 12/12 без дельт |
| SH-3d | `enum CliMode` (1335 после SH-3c) … до `fn render_bytes` | ~1 950 | Выделен из SH-3 2026-08-27 при разметке SH-3c: регион SH-3 вдвое больше правила §2.6, поэтому нижняя половина закрыта SH-3c, а голова осталась отдельной сессией. Карта тем снята и переписи не требует: режимы командной строки (`CliMode`/`McpMode`/`DumpKind`/`parse_cli`, ~113) → готовый `cli_args.rs`; `LoadedPage`+`impl` (~76); клавиатура (`TypeableField`, `KeyCommand`, `keybinding_for`, ~274) → `input/keybindings.rs`; `ResourceBase`+`ResolvedResource`+перехватчик SW+`feed_preload_and_emit`+`parallel_map` (~220); таблицы стилей и media-контексты (~300); картинки (~250) → в созданный SH-3c `subresources.rs`; `js_string_literal` (~19) → готовый `js_escape.rs`; типы конвейера (`FrameHandle`, `ParsedPage`, `LayoutSource`, `ParkedPage`, `PageSnapshot`, ~250) и `parse_and_layout` (412) → в созданный SH-3c `page_pipeline.rs`. Осторожно: `ParsedPage`/`PageSnapshot` участвуют в bfcache-снапшотах (§2.3) **ЗАКРЫТ 2026-08-27** (ветка `p1-split-sh3d`): 1 929 строк уехали по ВОСЬМИ адресам, семь срезов по коммиту на тему. Карта тем, снятая при разметке SH-3c, подтвердилась в шести пунктах из восьми и разошлась в двух — оба раза потому, что регион назван по адресу, а не по смыслу. **(1)** режимы командной строки (`CliMode`/`McpMode`/`DumpKind`/`parse_cli`, 112) → готовый `cli_args.rs`: `parse_cli` читает ровно то, что осталось после 18 функций `extract_*`. **(2)** `TypeableField` + `KeyCommand` + `keybinding_for` (277) → новый `input/keybindings.rs` — один файл на два типа, потому что это две половины одного пути ввода: чего клавиша стоит и куда `edit_focused_field` запишет результат, если `keybinding_for` её не забрал (BUG-436). **(3)** «разрешение внешних ресурсов» распалось на ТРИ адреса: `ResourceBase`+`impl`+`ResolvedResource`+перехватчик SW (112) → новый `resource_base.rs` (его читают 12 файлов, включая `storage_stores.rs` и `hibernate.rs`, где никакой выборки нет — им нужен origin, поэтому дописка в `subresources.rs` была бы неверной по его же доккомментарию); `MAX_PARALLEL_FETCHES`+`parallel_map` (62) → новый `parallel_fetch.rs` (общий приём, четыре вызывателя); `feed_preload_and_emit` (87) → `page_load.rs`, где его единственный вызыватель. **(4)** таблицы стилей и оба media-контекста (283) → новый `stylesheets.rs`: билдеры контекста отвечают на вопрос про ЭЛЕМЕНТ `<link>` (гейт BUG-268 роняет лист до всякой выборки, print-конвейер подменяет контекст — BUG-270), а не про правило каскада. **(5)** картинки (233) → `subresources.rs`, а `js_string_literal` (18), стоявший между ними по адресу, — в готовый `js_escape.rs`. **(6)** «типы конвейера» из карты распались надвое: `FrameHandle`+`MAX_FRAME_DEPTH` (29) → `frames.rs` по теме, а `ParkedPage`+`PARKED_PAGES_MAX`+`PageSnapshot` (159) → новый `page_state.rs` — это не конвейер, а отложенное состояние страницы. **(7)** `LoadedPage`+`impl`, `ParsedPage`, `LayoutSource`, `parse_and_layout` (555) → `page_pipeline.rs`, который тем самым собран целиком: `render_bytes` там уже лежал и является обёрткой над `parse_and_layout`. `LoadedPage` стоял на 500 строк выше остальных и по адресу читался частью головы файла. Тела перенесены байт в байт на всех восьми адресах (0 расхождений на 1 929 строках после снятия `pub(crate) `); правок 151 и все они — видимость, из них 111 — поля (ловушка SH-3a про поля сработала шестой раз подряд: у `PageSnapshot` их 72). Правило SH-3a про cfg реэкспорта сработало дважды: `#[cfg(test)]` для `collect_link_hrefs`/`contains_ignore_ascii_case` и `#[cfg(feature = "v8")]` для `js_string_literal`; в обоих случаях `cargo check` молчит, ловит только clippy. Три item'а реэкспорта не получили вовсе (`MAX_FRAME_DEPTH`, `ParsedPage`, `feed_preload_and_emit`) — их имена в `main.rs` больше не встречаются. Две линейки-разделителя опустевших секций сняты, их смысл перешёл в доккомментарии новых модулей. main.rs 12 263 → 10 334, тестов крейта 1624 → 1624, `dump_golden.py` 12/12 без дельт |
| SH-4 ✅ DONE 2026-08-26 | `mod navigate_by_tests` (29325) … конец файла (29469) | 145 | `src/navigate_by.rs` (143), `#[cfg(test)] mod navigate_by;`. Анкер переписи промахнулся на 170 строк — файл успел вырасти до 29 469. Модуль переименован в `navigate_by` под имя файла: ссылок на `navigate_by_tests` в репозитории не было, поменялись только id тестов. `use super::*;` перенесён как есть — `super` подмодуля корня крейта это тот же корень, что и у inline-модуля |
| SH-4b ✅ DONE 2026-08-27 | `mod tests` (23108) … до `#[cfg(test)] mod navigate_by;` (29325) | 6 199 | Каталог `src/tests/` из восьми файлов: `mod.rs` (307), `chrome_incremental.rs` (1 217), `bug341_census.rs` (1 795), `bfcache_salvage.rs` (256), `page_pipeline.rs` (855), `page_resources.rs` (634), `cli.rs` (654), `scripts_and_frames.rs` (541). **Не один `src/tests.rs`, как значилось в цели**: `scripts/check_file_sizes.py` запрещает заводить файл длиннее 2 000 строк, а §2.6 — тащить регион >4 000 строк одним коммитом; поэтому 4 среза и разрез по темам. Приём, который делает такой батч пошаговым: инлайновый `mod tests` в корне крейта ищет потомков в `src/tests/`, так что подмодули выносятся по одному прямо по конечному пути, а обёртка снимается последним срезом — файлы не переезжают. Потомку хватает `use super::*;`: он видит приватные элементы предка, а через них и глоб корня крейта. Общие нескольким подмодулям хелперы (стенд CC-12, `census_count_boxes`, `args`) оставлены в `mod.rs` — видимость не менялась нигде. Отступ снимается ровно 4 пробела и **только вне строковых литералов**: в регионе 170 строк начинаются внутри многострочного `r#"<html>…"#` или строки с переносом `\`, слепой дедент правил бы их содержимое. Осечка, которую стоит помнить: граница второго куска среза 2 отрезала `///`-блок от его теста — компиляция молчит, доккомментарий переклеивается на соседний элемент; проверять, что строка перед началом куска и последняя строка куска не `///`, не `#[...]` и не баннер секции |
| SH-5 ✅ DONE 2026-08-27 | `fn winit_modifiers_state` (22794, с доккомментарием) … до `#[cfg(test)] mod tests;` (23108) | 299 | Шесть адресов, а не один: `src/input/winit_events.rs` (82 — `winit_modifiers_state`, `cursor_icon_for_hover`, `css_cursor_to_winit`), `src/scroll/metrics.rs` (24 — `LINE_STEP_CSS_PX`, `page_step`, `clamp_scroll`), `src/display_list_metrics.rs` (168 — `content_height_of`, `content_width_of`, `build_split_placeholder`), `src/js_escape.rs` (36 — `escape_js_string{,_char}`), `src/session_persist.rs` (+22 — `source_url_string`, `dom_blob_of`), `src/tabs/containers.rs` (+8 — `origin_of_url`). **Цель «существующий `src/input/`» не годилась**: в регион входит 15 свободных функций и констант шести разных тем, к winit относятся три из них — целиком в `src/input/` уехали бы измерение display list и escape JS-строк. Это ровно ловушка переписи из шапки этого раздела (регион назван по первому item'у), только замеченная не в конце батча, а до вырезки. `IDLE_BUDGET_MS` осознанно оставлен в `main.rs`: единственный его читатель — `about_to_wait` там же, ни к одной из шести тем он не относится (SH-1 так же оставил 5 методов). Тестов в регионе не было, счётчик крейта 1624 → 1624. Две осечки, обе уже описаны в SH-4b как общий приём: приписанный в конец файла item попал ПОСЛЕ `#[cfg(test)] mod tests` (clippy `items after a test module` — вставлять надо перед тест-модулем), а при переносе выше по файлу доккомментарий отклеился от своей функции и остался сиротой в хвосте. `use crate::*` в `relayout.rs`/`page_load.rs` и `use super::*` в `tests/` менять не пришлось: приватный `use` в корне крейта виден потомкам, поэтому шесть строк импорта в `main.rs` работают и как реэкспорт — ни один вызов не тронут. Ушли и два импорта, чьи единственные пользователи уехали (`lumen_layout::Cursor as CssCursor`, `winit::event::Modifiers`) |
| SH-6 ✅ DONE 2026-08-27 | `struct Lumen` (1401 после SH-3d) … до конца ядра | 10 045 | **ЦЕЛЬ ДОРОЖКИ ДОСТИГНУТА: main.rs 10 334 → 289 строк** (потолок батча 300), за всю дорожку 22 813 → 289 — 98.7 % файла. Собственных item'ов в `main.rs` не осталось, кроме `fn main` (16 строк); всё прочее — `mod`-объявления, приватные `use` (реэкспорт для `use crate::*` потомков) и два `#[cfg(test)] mod`. ТРИНАДЦАТЬ срезов, по коммиту на тему: **(1)** команды вкладок → новый `lumen/tabs_cmd.rs` (476) и ответы автоматизации → `lumen/automation.rs`, 704; **(2)** сессия (175) / гибернация T1–T3 (381) / снапшот страницы (213) тремя файлами, а не одним: пара `save`/`restore_page_snapshot` лежит в основании и переключения вкладок, и яруса T2, 684; **(3)** скроллинг (431) + его потребители `content-visibility` (134) отдельным файлом («как двигать» ≠ «что пересчитать, когда сдвинулось») + меню правописания (278), 763; **(4)** геометрия вьюпорта (96) / доки панелей (253) / данные панелей (183) / палитра команд (158) / reader view и view-source (96) / AI-ответ (72), 728; **(5)** строка поиска (128) / клавиши панелей (301) / печать (124) / hint-режим (167) / курсор (70), 704; **(6)** `commit_nav_state` и уведомления Navigation API (117) / bfcache двумя механизмами (224) / сама навигация (650), 973; **(7)** указатель ОС→JS (176) / отправка форм (182) / `handle_click_at` целиком (598), 950; **(8)** `handle_key` целиком (672 одной функцией) / инжект текста и IME (246), 897; **(9)** пикт-в-пикт во всех трёх формах (343) / отложенные изменения вьюпорта (71) / файловый диалог (45) / ручка `resize` (27) / a11y-медиазапросы (22) / жесты (17), 525; **(10)** адресная строка (401) / `about:newtab` (71) / раскладка ленты вкладок, 493; **(11)** объявление `struct Lumen` целиком → `lumen/state.rs` (1 283, 240 полей) + `view_transition.rs` (34) + `PendingIntercepted` → `nav_history.rs` + `IDLE_BUDGET_MS` → `app/about_to_wait.rs`, 1 311; **(12)** осадок вокруг `fn main` — пятнадцать item'ов по ВОСЬМИ адресам (`page_load.rs`, `page_pipeline.rs`, `subresources.rs`, новые `frame_pacing.rs`/`event_sink.rs`/`assets.rs`, `lumen/automation.rs`, `engine_bridge.rs`), 333; **(13)** `run_cli` → `cli_args.rs` (+252, модуль уже написан вокруг неё) и `run_window_mode` → новый `window_mode.rs` (562) — **не** в `app/`, чей доккомментарий держит тела методов ПОСЛЕ `run_app`, а здесь сборка цикла ДО первого кадра, 801. Тела перенесены байт в байт на всех срезах (0 расхождений после снятия `pub(crate) `), правится только видимость. **Ловушка SH-3a про поля дала максимум за дорожку:** на срезе 11 из 247 правок 243 — поля (240 у `struct Lumen`), потому что до переезда они лежали в корне крейта и были видны каждому потомку без единого `pub`. Ловушка «регион назван по первому item'у» подтвердилась ещё дважды (срез 7 — цель «мышь/указатель → JS» накрывала треть; срез 9 — «один адрес» оказался четырьмя) и один раз НЕ сработала (срез 11 — между `struct Lumen` и её `}` нет посторонних item'ов, проверено грепом по колонке 0), то есть проверять надо всегда, а не только когда подозрительно. **Осечка с доккомментариями набрала три новые формы к двум прежним:** сдвоенный блок (срез 6 — над `navigate_to` три строки стояли ДВАЖДЫ побайтово; удалять после сравнения, а не на глаз), чужой блок над соседом (срез 9 — доки `toggle_pip` над `deliver_a11y_media_changes`) и **разъезжающийся по двум адресам** (срез 10 — доки `handle_omnibox_commit` над `build_newtab_source`, функции уезжали в разные файлы, «перенести как есть» задокументировало бы `newtab_page.rs` рассказом про омнибокс). **НОВОЕ ДЛЯ ДОРОЖКИ, четыре приёма.** Вырезка ведётся **побайтово** (`split(b"\n")`, срез по 1-индексным номерам): `main.rs` — смесь UTF-8 и двойной кодировки, декодирование портит комментарии, до которых не дотрагиваемся. **cfg-пара одноимённых item'ов** (`ai_answer_for` под `#[cfg(feature = "ai")]`/`#[cfg(not(…))]`) обязана ехать вместе — скрипт вырезки пришлось научить одноимённым спанам, иначе словарь оставляет последний и половина пары молча остаётся в `main.rs`. Список «кого сделать `pub(crate)`» грепать по ДВУМ словам rustc — «method» и «associated function» (`offset_overlay_x` не попал в первый список именно так). Конец региона отмерять пересчётом, а не по `sed`-выводу (срез 7: последняя `}` осталась в `main.rs` — у `handle_click_at` четыре закрывающие скобки подряд сливаются глазами; компилятор ловит сразу, но дешевле пересчитать). `restore_t2_tab` показал, почему гейт — именно `clippy --all-targets`: `cargo check` его пропускает, единственный внешний вызыватель метода живёт в `src/tests/`. `main.rs` выпал из `scripts/file-size-baseline.tsv` целиком на срезе 11 — впервые за дорожку под потолком 2 000 строк. Тестов 1624 → 1624 на всех тринадцати срезах, `SYMBOLS.md` не двинулся ни разу (наружу не вышло ни одного `pub` item'а), `dump_golden.py` 12/12 без дельт. Ловушка для следующих батчей: clippy на профиле `dev-release` красен на `lumen-layout` (`invariants.rs`, три функции под `debug_assertions`) — не наш крейт, гейт гонять на профиле по умолчанию |

**Приём, обкатанный на SH-1** (`impl Lumen` в подмодуле): блок `impl Lumen { … }` в
`src/<name>.rs` рядом с `use crate::*;` компилируется без единой правки тела — приватные
элементы корня крейта видны потомкам, поэтому импортировать поимённо ничего не нужно.
Единственное неизбежное изменение — видимость: метод, который зовут из `main.rs`,
обязан стать `pub(crate) fn` (родительский модуль не видит приватные элементы потомка).
Механическая проверка переноса — сверка построчной подпоследовательности нового файла
с вырезанным регионом исходника (скрипт батча делал именно это, 3 590 непустых строк).

### Группа ST — `crates/engine/layout/src/style.rs` (38 031 строка)

Перепись ST-0 закрыта 2026-08-27 (`python scripts/split_census.py
crates/engine/layout/src/style.rs`): **723 top-level item'а**, лексер сходится
(итоговая глубина 0). Файл делится по содержимому на 23 региона; ниже они
собраны в 18 батчей и идут, как и группа SH, **с хвоста файла к голове** — так
вырезка не сдвигает анкеры ещё не взятых батчей.

**Числа переписи, которые меняют постановку двух уже заведённых батчей.**
Прежняя строка ST-1 указывала анкер `mod rule_index_regression` (~36541) и
объём «до конца файла»; на самом деле тестов в файле **13 813 строк, 36 % всего
файла и 1 195 `#[test]`**, а главный из них — `mod tests` на 24304…36581,
**12 278 строк**, то есть в двенадцати тысячах строк ВЫШЕ указанного анкера.
Целью значился каталог `tests/style/` — это каталог интеграционных тестов
крейта, куда этот модуль переехать не может вовсе: он читает приватные
элементы `style` (`use super::*`), а интеграционный тест видит только `pub`.
Верный адрес — `src/style/tests/`, приём SH-4b: инлайновый `#[cfg(test)] mod
tests` в `style.rs` ищет потомков ровно там, поэтому подмодули выносятся по
одному сразу по конечному пути, а обёртка снимается последним срезом.
Прежняя ST-2 («логические свойства, анкер 17906») верна по адресу, но по объёму
это 161 строка на два item'а — самостоятельным батчем не тянет и слита с ST-7.

**Четыре правила, которыми группа ST отличается от SH, и все четыре — следствие
того, что `style.rs` это модуль БИБЛИОТЕЧНОГО крейта, а не корень бинарника.**

1. **Реэкспорт `pub use`, а не приватный `use`.** В `lib.rs` стоит `pub mod
   style;` плюс два блока `pub use style::{…}`, а по репозиторию 113 обращений
   вида `lumen_layout::style::<Имя>` из шести крейтов. Приём SH («приватный
   `use` в корне крейта работает реэкспортом для потомков») здесь закрывает
   только внутрикрейтовых читателей; каждый переехавший **`pub`** item обязан
   получить `pub use` из `style.rs`, иначе ломается публичная поверхность.
2. **`SYMBOLS.md` двинется на КАЖДОМ батче.** За всю группу SH он не двинулся
   ни разу (наружу не вышло ни одного `pub` item'а); здесь `pub`-item'ов 208 из
   723, и они переезжают. `python scripts/gen_symbols.py` тем же коммитом —
   CI-задание `doc-drift` блокирующее.
3. **`#![allow(missing_docs)]` едет с кодом, и §10 `docs/lint-policy.md`
   обновляется тем же коммитом.** §2.4 запрещает копировать файловый allow
   автоматически — но здесь долг действительно едет: без `///` в файле **13
   top-level item'ов, 86 `pub`-полей и 326 вариантов `pub enum`** (≈ 425 из 552,
   которые §10 числит за крейтом layout). Долг сидит в типах значений (варианты)
   и в `struct ComputedStyle` (поля), поэтому файл, которому достались только
   документированные item'ы, allow получать НЕ должен, а счётчик файлов в §10
   («layout 552 (7 файлов)») растёт с каждым батчем.
4. **Пиксельный гейт обязателен (§2.5).** `style.rs` кормит layout, то есть
   геометрию: `scripts/scoped-test.sh` + `python graphic_tests/dump_golden.py`
   с пустым дифом. Гейт висит на `lumen-network` ([BUG-805](../../bugs/BUG-805-OPEN.md)) —
   тесты гонять адресно (`cargo test -p lumen-layout`, 3 620 `#[test]` по крейту)
   и писать в тело коммита, почему гейт не зелёный.

| ID | Анкер (строки style.rs) | Регион | Цель |
|---|---|---:|---|
| ST-1 | `mod tests` (24304) … до `mod rule_index_regression` (36581) | 12 278 | `src/style/tests/` — 8 файлов. Модуль ПЛОСКИЙ: ни одного вложенного `mod`, 117 авторских секций-баннеров (`python scripts/split_census.py … --inner 24304 36581`). Шапка (24304…24808, 505 строк, 36 хелперов вроде `rgba`) остаётся в `tests/mod.rs` — её видят все потомки. Резать по баннерам, группируя секции под будущие производственные модули ST-3…ST-18, а не подряд: темы в модуле чередуются (цвет лежит в четырёх местах, шрифты в трёх). Секция — атом, границей служит строка баннера. Механическая проверка: сумма перенесённых строк = 12 278, счётчик `#[test]` крейта 3 620 → 3 620 |
| ST-2 | `mod rule_index_regression` (36582) … конец файла | 1 451 | Семь уже именованных `#[cfg(test)] mod` → `src/style/tests/<имя>.rs` каждый (`rule_index_regression` 104, `shadow_dom_selectors` 147, `gap_rule_tests` 67, `masonry_auto_flow_tests` 67, `anchor_positioning_tests` 560, `state_fanout_tests` 302, `node_fanout_tests` 204). Дешевле ST-1 и делается ПОСЛЕ него, а не до: имя `tests` до ST-1 занято инлайновым модулем с телом, и объявить `mod tests;` для каталога рядом с ним нельзя. Модули становятся потомками `tests`, то есть путь `crate::style::rule_index_regression` → `crate::style::tests::rule_index_regression`; ссылок на эти имена в репозитории нет, меняются только id тестов (прецедент SH-4) |
| ST-3 | `fn parse_color` (23036) … до `mod tests` (24303) | 1 268 | Разбор цвета: legacy/`color()`/hex/`color-mix()`/`color-contrast()`, относительные цвета и их микро-лексер (`ColorTok`), конверсии oklab/lab/oklch → `style/parse/color.rs`. Таблица `NAMED_COLORS` (23283…23437, 155) — отдельным `style/values/named_colors.rs`: она табличная, и §1 плана именно такие файлы выводит из-под правила размера |
| ST-4 | `fn media_context_from_viewport` (22116) … до `fn parse_color` (23035) | 920 | Две темы, не одна: `font-size`/`line-height`/`font`-шортхенд с его токенайзером (22116…22514) → `style/parse/font_size.rs`; сеттеры сторон бокса, `margin`/`padding`/`border`/`outline`-шортхенды, scroll-snap, `anchor()`/`anchor-size()` (22515…23035) → `style/parse/box_sides.rs` |
| ST-5 | `fn parse_counter_list` (19769) … до `fn media_context_from_viewport` (22115) | 2 347 | Парсеры значений, четыре файла: счётчики/кавычки/углы/`clip-path` (19769…20058) и `transform`/`filter` (20059…20240) → `style/parse/transform.rs`; `content` (20241…20352) → `style/parse/content.rs`; таймлайны, `animation`/`transition`-шортхенды (20353…20818) → `style/parse/timeline.rs`; изображения — `image-set`/`cross-fade`, слои `background`/`mask`, градиенты (20819…22115, 1 297) → `style/parse/image.rs`. Последний придётся делить: 1 297 строк это один файл под потолком, но вместе с `parse_grid_template_areas` (21479) уже нет |
| ST-6 | `fn apply_css_wide_keyword` (18696) … до `fn parse_counter_list` (19768) | 1 073 | Одна функция → `style/apply/css_wide.rs`. Помещается в потолок целиком, правок не требует |
| ST-7 | `struct ParsedTextDecorationShorthand` (17914) … до `fn apply_css_wide_keyword` (18695) | 782 | Логические свойства (`resolve_logical_property` + `mod logical_properties_tests`, 17914…18074) → `style/logical.rs` — это бывшая ST-2; развёртка шортхендов text-decoration/text-emphasis/contain-intrinsic/text-wrap/flex/grid-area (18075…18695) → `style/shorthand.rs`. `resolve_logical_properties` (8639…8714, пост-каскадная, другая функция того же семейства) дописывается в `style/logical.rs` батчем ST-13 — тянуть её сюда значит сдвинуть анкеры всех батчей ниже |
| ST-8 | `fn apply_declaration` (14869) … до `struct ParsedTextDecorationShorthand` (17913) | 3 045 | **Самый рискованный батч группы, единственный, который не решается чистым переносом.** Разбор — ниже отдельным разделом |
| ST-9 | `enum LengthOrAuto` (13303) … до `fn apply_declaration` (14868) | 1 566 | Три файла: `Length`/`LengthOrAuto`/`parse_length_q` (13303…13418 + 13700…13951) → `style/values/length.rs`; `CalcNode`/`MathFn`/токенайзер/`resolve_math_func` (13419…13699 + 13952…14358) → `style/calc.rs`; подстановки `var()`/`env()`/`attr()`/пользовательских функций (14359…14868) → `style/substitute.rs`. Осторожно: темы чередуются по адресу — `impl CalcNode` стоит между `enum Length` и `impl Length` |
| ST-10 | `fn set_cq_context` (12413) … до `enum LengthOrAuto` (13302) | 890 | Фан-аут рестайла (`StateRestyleIndex`, `NodeRestyleIndex`, `restyle_root_set_for_*`, 12552…13139) → `style/restyle.rs` — это ядро BUG-341, у него уже есть свои тест-модули `state_fanout_tests`/`node_fanout_tests` (переезжают ST-2); thread-local контексты, `StyleEnvSnapshot`, флаги forced-colors/print и `node_in_scope` (12413…12551 + 13140…13302) → `style/env.rs` |
| ST-11 | `fn default_display` (10828) … до `fn set_cq_context` (12412) | 1 585 | Четыре файла: дефолты UA-таблицы и правки под элементы (`apply_ua_*`, `ua_*`) → `style/ua.rs`; презентационные HTML-атрибуты (`apply_*_presentational_hint`, `parse_legacy_color_html_attr`) → `style/presentational.rs`; квирки (`apply_quirks_*`) → `style/quirks.rs`; шрифтовые парсеры (12147…12412: `parse_font_family`, `parse_font_variation_settings`, `parse_font_feature_settings`, `parse_font_palette`) → `style/parse/font.rs` |
| ST-12 | `fn matches_complex` (9432) … до `fn default_display` (10827) | 1 396 | Матчинг селекторов. Два файла, потому что это два разных вопроса: обход комбинаторов и compound/attribute/`::slotted` (9432…9902) → `style/matching.rs`; предикаты состояния формы и позиции в дереве (`matches_disabled`/`matches_checked`/`matches_indeterminate`/`matches_default`/`element_index*`, 9903…10827) → `style/matching/forms.rs` |
| ST-13 | `fn element_can_have_scrollbar` (8361) … до `fn matches_complex` (9431) | 1 071 | Пост-каскадные правки (`apply_webkit_scrollbar_pseudos`, `resolve_system_colors_in_style`, `apply_forced_colors_mode`, `coerce_overflow_axes`) → `style/adjust.rs`; псевдоэлементы (`compute_pseudo_element_style*`, `pseudo_inherited_style`, `merge_pseudo_inherited`, `compute_selection_style`) → `style/pseudo.rs`; валидация `@property`-синтаксиса (9294…9431) → `style/property_syntax.rs`. Плюс `resolve_logical_properties` (8639…8714) дописывается в созданный ST-7 `style/logical.rs` |
| ST-14 | `static COMPUTE_STYLE_CALLS` (7070) … до `fn element_can_have_scrollbar` (8360) | 1 291 | `compute_style` (7201…8360, 1 160 одной функцией) плюс `zoom` (7070…7200) → `style/cascade.rs`. Функция помещается в потолок, но файл на 1 291 строку — почти под ним, поэтому `zoom` при первом же росте уедет в `style/zoom.rs` |
| ST-15 | `struct ComputedStyle` (3287…4141) + `impl ComputedStyle` (6726…7069) | 1 199 | `style/computed.rs`. **Единственный батч с разрывным регионом, и разрыв обязателен:** объявление и реализация обязаны ехать вместе (прецедент `enum PageSource`/`impl PageSource`, SH-3a/SH-3b), а между ними лежат 2 584 строки чужих типов значений. Тянет за собой правило 3 в максимуме: 375 `pub`-полей, из них 86 без `///` |
| ST-16 | `enum Display` (673) … до `struct ComputedStyle` (3286) | 2 614 | Типы значений, голова. Четыре файла по группам свойств: типографика и текст (673…1639) → `style/values/typography.rs`; цвет и цветовые пространства (1640…2124) → `style/values/color.rs`; бокс, границы, таблицы, SVG-краска, позиционирование (2125…2577) → `style/values/box_model.rs`; тайминг-функции, кривые Безье и перечисления анимации (2578…3286) → `style/values/timing.rs` |
| ST-17 | `enum Content` (4142) … до `impl ComputedStyle` (6725) | 2 584 | Типы значений, хвост. Пять файлов: содержимое/списки/перенос/интерактивность (4142…4625) → `style/values/misc.rs`; контейнерные запросы вместе с `evaluate_container_condition` и `apply_container_rules` (4626…5058) → `style/container.rs` (это не тип значения, а вычисление); режим письма, user-select, scroll-snap (5059…5223) → `style/values/scroll.rs`; градиенты, фон, маски, object-fit (5224…5804) → `style/values/background.rs`; flex, grid-треки, выравнивание, clip-path, трансформы, фильтры (5805…6725) → `style/values/flexgrid.rs` + `style/values/transform.rs` |
| ST-18 | `struct CascadeIndex` (43) … до `enum Display` (672) | 630 | Кэш индекса каскада и его диагностика (`CascadeIndexStats`, `PseudoCascadeStats`, счётчики BUG-341, теневые таблицы стилей) → `style/cascade_index.rs`. Имя **не** `rule_index.rs`: такой модуль в крейте уже есть и решает другую задачу — раскладывает правила по корзинам правого простого селектора, а здесь кэш построенного индекса и его счётчики |

**ST-8 (`apply_declaration`, 3 045 строк) — почему он не решается чистым
переносом и что с ним делать.** Это одна функция: преамбула на 70 строк
(пропуск `--*`, подстановка `var()`) и один `match prop` (14940…17911) из **343
веток** с `_ => {}` в конце. Потолок нового файла — 2 000 строк (`CAP` в
`scripts/check_file_sizes.py`, правило №1: «новый монолит завести нельзя
вообще»), и он жёсткий, в отличие от храповика для уже существующих файлов.
Значит функция целиком в новый файл не помещается, а поделить её — это правка
кода, которую §2.1 у батча отбирает.

Прецедент, разрешающий такой батч, на дорожке уже есть: SH-2 превратил методы
трейта в однострочные переходники, потому что это была «единственная форма, в
которой регион вообще помещается в правило размера файла». Здесь измерены три
вещи, которые делают аналогичный приём механическим, и одна, которая делает его
опасным.

* Ветки переносятся **побайтово**: внутри `match` нет ни одного `return`
  (проверено), а три вхождения `?` — все в комментариях. Форма помощника,
  сохраняющая тела веток байт в байт, такая: `fn apply_decl_<тема>(…) -> bool {
  match prop { <ветки как есть>, _ => return false, } true }`, а
  `apply_declaration` после преамбулы выстраивает их цепочкой
  `if apply_decl_layout(…) { return; }`. Новыми в файле оказываются только
  заголовок функции, строка `_ => return false,` и завершающее `true` — тела
  веток не редактируются вовсе. Приём проходит по типам, потому что каждая ветка
  вычисляется в `()`.
* Ветки можно **перегруппировать по смыслу**, а не резать подряд: все 351 метка
  (343 ветки, часть многометочных) уникальны, дублей нет, так что порядок веток
  в `match` по строковым литералам семантики не несёт.
* **И ровно это последнее — ловушка батча.** Компилятор поймает дубль метки
  (недостижимый паттерн), но НЕ поймает потерянную ветку: она молча проваливается
  в `_ => {}`, то есть свойство перестаёт применяться, и ни один тип, ни один
  тест сборки этого не заметит. Поэтому механическая проверка батча — не сверка
  тел (её тоже делать), а **сверка мультимножества меток до и после: 351 метка,
  тот же состав**. Скрипт на пять строк, и без него батч брать нельзя.
* Швы измерены по именам веток, шесть отрезков почти равного веса:
  14941…15430 (490) `display`…`max-block-size` — раскладка, flex, grid, размеры;
  15431…15935 (505) `font-style`…`offset-anchor` — шрифт, текст, тени, обводка,
  счётчики, трансформы, фильтры; 15936…16425 (490) `row-gap`…`place-self` —
  колонки, разрывы страниц, выравнивание; 16426…16966 (541)
  `will-change`…`container` — фон-слои, позиционирование, inset, contain,
  контейнеры; 16967…17537 (571) `writing-mode`…`mask-*` — режим письма, скролл,
  переносы, transitions/animations/таймлайны, маски; 17538…17909 (372)
  `line-height`…`box-sizing` — SVG-краска, отступы, text-decoration, границы.
  Собираются в четыре файла `style/apply/{layout,text,paint,motion}.rs` плюс
  `mod.rs` с преамбулой и цепочкой вызовов; конкретная развесовка — за батчем,
  потолок 2 000 строк выдерживают любые две соседние пары.

**Запасной вариант, если приём выше сочтут выходом за устав батча:**
`apply_declaration` остаётся в самом `style.rs`. Цель дорожки («ни одного файла
>8000 строк», §1) при этом достигается — после ST-1…ST-18 в файле останутся
`use`-блок, преамбула, эта функция и реэкспорты, ≈ 3 200 строк, а `style.rs`
числится в базовой линии, где действует храповик, а не потолок. Цена: критерий
§5 «целевой файл ≤2000 строк» на этом батче не выполняется, и `style.rs`
остаётся крупнейшим файлом крейта.

### Группы JS/BT/PR/LB/NW/CP/DM — второй эшелон

| ID | Файл | Первый шаг |
|---|---|---|
| JS-0 | `js/dom.rs` (45 541) + `js/v8_runtime.rs` (9 415) | перепись → нарезка. Крупнейший файл workspace, в переписи 2026-08-23 отсутствовал вовсе. `WEB_API_SHIM` внутри — строковый литерал JS, резать его надо по границам шимов (`WEB_API_SHIM_MID` и соседи), а не по строкам Rust; половина «известных готч» CLAUDE.md живёт в этом файле, так что регионы именовать по содержимому особенно строго |
| BT-0 | `layout/box_tree.rs` (23 470) | перепись → конструкторы по режимам `{block, inline, flex, grid, table, abspos, iframe}`; `build_iframe_document` (зависимость layout→html-parser) — кандидат на отдельный модуль |
| LB-0 | `layout/lib.rs` (19 155) | перепись → нарезка. Группы под этот файл в исходном плане не было — пробел, замеченный при назначении дорожки P1 2026-08-26, хотя файл шестой по величине в переписи |
| PR-0 | `paint/renderer.rs` (20 919) | перепись → `{pipelines_wgsl, glyph_atlas, texture_pool, compute}`; WGSL-шейдеры в `.wgsl`-файлы с `include_str!` |
| PR-1 | `paint/display_list.rs` (19 698) | перепись → `{commands, builder, serialize, hit_test, cache}` |
| PR-2 | `paint/backends/femtovg_backend.rs` (7 955) | по командам: path/text/image/blend |
| NW-0 | `network/lib.rs` (9 799) | HTTP/1.1-ядро → `http1/{request,response,chunked}.rs`; в lib.rs оставить сборку transport |
| CP-1 | `css-parser/parser.rs` (9 192) | тестовый модуль (с ~5098) → `tests/`; затем `parser/{selectors,at_rules,declarations}` |
| DM-1 | `dom/lib.rs` (6 934) | формы → `forms.rs`, selection/range → `selection.rs`, IME → `ime.rs`, FontFaceSet → `font_faces.rs`, PerformanceTimeline → `performance.rs` (образец — уже вынесенные `vtt.rs`, `contenteditable.rs`) |

---

## 5. Критерии готовности батча

- [ ] `cargo test -p <crate>` зелёный, счётчик тестов не уменьшился
- [ ] `cargo clippy -p <crate> --all-targets -- -D warnings` чист
- [ ] Для layout/paint: графический/snapshot-прогон без пиксельных дельт
- [ ] Ни один тип/поле с `Serialize/Deserialize` не переименован
- [ ] `docs/lint-policy.md §10` обновлён, если дробился file-level allow
- [ ] Указатель батча удалён из `STATUS-P1.md`, таблица §4 помечена DONE
- [ ] Целевой файл(ы) ≤2000 строк; исходный уменьшился ровно на размер региона

## 6. Критерии закрытия дорожки SPLIT

Все батчи DONE; ни один .rs-файл workspace (кроме явно исключённых в §1) не
превышает 8000 строк; строка `SPLIT` в ROADMAP.md → `done`, task-файл удалить
после мержа последнего батча.
