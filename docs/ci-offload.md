# Перенос гейта в CI (GitHub Actions)

План выноса части сборочно-тестового гейта с рабочей машины на GitHub Actions: что уезжает, что остаётся, как настроить и как ассистент забирает результат. Составлен 2026-08-18 после проверки лимитов трёх бесплатных CI (GitHub / GitLab / Bitbucket) и аудита существующих workflow.

**Статус на 2026-08-19: §4–§8 и §11.5 внедрены,** остаток — только §11.6 (self-hosted, опционально). Пять джобов в `ci.yml`, из них блокируют мерж `test`, `doc-drift` и `lint`; `snapshot-cpu` остаётся разведкой до закрытия [BUG-784](../bugs/BUG-784-OPEN.md). Оценки времени в §4 **заменены замерами** (CI-1б, 2026-08-19) — они были завышены в 4–8 раз. Ход внедрения по шагам — журнал §12.

Смежные документы: [build-speed.md](build-speed.md) (локальная сборка и её замеры), [graphic-tests.md](graphic-tests.md) (почему пиксельный гейт локальный), [perf-method.md](perf-method.md) (почему перф нельзя гейтить по wall-clock), [git-workflow.md](git-workflow.md) (7-шаговый чеклист, который §8 меняет).

---

## 1. Исходная посылка

Репозиторий `LearnJava/lumen-browser` **публичный**, поэтому стандартные раннеры GitHub Actions бесплатны и без лимита минут на всех трёх ОС. Минуты перестают быть ограничением, и вопрос сводится к тому, что физически имеет смысл выполнять на чужой машине.

Ключевое разделение, из которого растёт весь документ:

- **Цикл разработки** (правка → `cargo check -p` = 4–6 с, `dev-release` инкремент = 3.8 с) в CI не переносится ни при каких условиях: раунд-трип до сервера на три порядка дороже. Локальный `target/` и процессорное время на `check` остаются.
- **Гейт задачи** (`clippy --workspace`, тесты крейтов, кросс-платформенная сборка) — один прогон на задачу, десятки минут CPU. Вот он и уезжает.
- **Прогон браузера** (живое окно, MCP-пробы, перф, graphic tests) привязан к локальному GPU, фокусу окна и сравнению с Edge — не уезжает.

Ожидаемый выигрыш формулируется честно: «минус один долгий гейт на задачу», а не разгрузка машины вообще. Дисковые 38 ГиБ корневого `target/` и слоты пула CI не освобождает.

---

## 2. Лимиты, которые продолжают действовать (проверено 2026-08-18)

| Лимит | Значение | Влияние на нас |
|---|---|---|
| Минуты, публичный репо, стандартные раннеры | безлимитно, $0 | нет |
| Larger runners | тарифицируются всегда, даже для публичных | **не использовать** |
| Кэш | 10 ГБ на репозиторий, вытеснение после 7 дней без обращения | **главный ограничитель, см. §5** |
| Скоуп кэша | прогон читает кэш своей ветки и базовой/дефолтной; соседние ветки — нет | §5: единый кэш на `main` |
| Время джоба | 6 ч (self-hosted — 5 дней) | влезаем с запасом |
| Время всего workflow | 35 дней | нет |
| Конкурентные джобы (Free) | 20 | 5 ролей × 3 ОС = 15, впритык — см. §5 про `concurrency` |
| Артефакты (Free) | 500 МБ, ретенция по умолчанию 90 дней | `lumen.exe` = 77 МБ, см. §6 |
| `GITHUB_TOKEN` | 1000 запросов/час на репозиторий | комментарии в PR — не задевает |
| Размер файла | предупреждение 50 MiB, жёсткий блок 100 MiB | проверять при вендоринге корпусов |
| Размер репозитория | рекомендация <1 ГБ, «настоятельно» <5 ГБ | `.git` = 495 МБ, 108 534 файла — см. §5 про checkout |

GitLab Free (400 мин/мес на namespace, обязательная верификация картой для shared runners, не более трёх top-level групп для аккаунтов после 27.01.2026) и Bitbucket Free (50 мин на весь workspace) при публичном репозитории на GitHub не дают ничего — рассматривались и отклонены.

---

## 3. Что уже есть и что с этим не так

| Workflow | Триггер | Состояние |
|---|---|---|
| [ci.yml](../.github/workflows/ci.yml) | push в `main` и в ветки `p[1-5]-*`, любой PR | Матрица ubuntu/macos/windows: `cargo check -p lumen-shell` + `cargo test` по 16 крейтам. **Был красным с 2026-06-17 по 2026-08-18** — 1187 прогонов на `main`, ноль успешных; починен, см. §3.1. Остаётся дефект кэша: `target` тремя ключами по числу ОС в общий бюджет 10 ГБ (§5). Не гоняет `clippy --workspace` — главный локальный гейт не продублирован. |
| ~~bench-gate.yml~~ | — | **Удалён 2026-08-19** (§11.5). Гейтил по 5 % отклонения wall-clock-медиан на shared runner против эталона трёхмесячной давности; 0 прогонов за всё время. Разбор — §11.5. Преемник — PERF-7 поверх `crates/bench/src/ci_gate.rs` |
| [red-lines.yml](../.github/workflows/red-lines.yml) | PR, push в main | ubuntu, две python-проверки с самотестом. Работает как задумано, трогать не нужно. |
| [release.yml](../.github/workflows/release.yml) | тег `v*.*.*`, `workflow_dispatch` | Четыре таргета (linux, macos aarch64/x86_64, windows). **0 прогонов за всё время** — тегов `v*.*.*` не было, поэтому обе поломки из §3.1 в нём не проявились; починены превентивно. Лучшее применение CI: локально эти артефакты не собрать. |

### 3.1. Почему CI был мёртв два месяца (найдено и починено 2026-08-18)

Аудит выше писался по содержимому workflow, без чтения истории прогонов, и поэтому описывал
`ci.yml` как работающий с дефектом кэша. На деле он не проходил **ни разу с 2026-06-17**: 1187
прогонов на `main`, из 800 просмотренных подряд успешных ноль. Причин было две, вложенные.

**Первая — sccache.** Закоммиченный `.cargo/config.toml` задаёт `build.rustc-wrapper = "sccache"`
(влит `2ffbee511`). На раннерах sccache нет, поэтому любая команда cargo умирала за две секунды,
до компиляции: `error: could not execute process 'sccache …/rustc -vV' (never executed)`.
Починка — `RUSTC_WRAPPER: ""` в `env:` у `ci.yml`/`release.yml`/`bench-gate.yml` (последний удалён 2026-08-19, §11.5).

**Вторая — ALSA, только Linux.** Сразу после снятия первой windows и macos стали зелёными, а
ubuntu упал на `failed to run custom build command for alsa-sys v0.3.1`: `crates/shell` тянет
`cpal`/`rodio`, на Linux они мапятся на ALSA, а шаг установки зависимостей ставил только
X11/Wayland/EGL. Добавлен `libasound2-dev` в `ci.yml` и `release.yml`.

Два вывода для этого документа:

- **`gh` не нужен для диагностики.** Репозиторий публичный, поэтому статусы прогонов, джобы, шаги
  и тайминги читаются анонимным `curl` к `api.github.com`; авторизация нужна **только** для
  логов (`/logs` → 403 «Must have admin rights»). Предусловие §11.1 снимается для всего, кроме
  чтения логов. Анонимный лимит — 60 запросов/час, на постраничном обходе ловится как `curl: (56)`.
- **Аудит workflow по их тексту недостаточен.** Оба дефекта не видны в YAML: первый живёт в
  `.cargo/config.toml`, второй — в списке apt-пакетов против дерева зависимостей. Проверять
  состояние CI надо по истории прогонов, а не по конфигу.

---

## 4. Целевая раскладка джобов

**Замеры сняты 2026-08-19 (CI-1б), оценки заменены фактами.** Колонка «холодный» — прогон
`32188492988` (кэш пуст, джоб его наполняет), «тёплый» — `32250819715` (кэш восстановлен).
Оценки были завышены в 4–8 раз: закладывались 25–45 минут на джоб, по факту тёплый прогон
целиком укладывается в 8,5 минут wall-clock.

| Джоб | ОС | Триггер | Холодный | Тёплый | Блокирует мерж |
|---|---|---|---|---|---|
| `lint` — `clippy --workspace --all-targets -- -D warnings` | ubuntu + macos | PR, push main | 6,5 / 6,1 мин | ещё не снят¹ | да (с 2026-08-19) |
| `test` — тесты 16 крейтов (как сейчас в `ci.yml`) | ubuntu | PR, push main | 7,8 мин | **3,8 мин** | да |
| `snapshot-cpu` — `cargo test -p lumen-driver --features cpu-render` | ubuntu | PR, push main | 8,2 мин | ещё не снят¹ | нет (разведка, BUG-784) |
| `cross` — `cargo check -p lumen-shell` + юнит-тесты | macos, windows | push main, nightly | 12,9 / 15,4 мин | **6,1 / 8,4 мин** | нет |
| `doc-drift` — `gen_roadmap.py`, `gen_symbols.py` | ubuntu | PR, push main | 0,2 мин | 0,3 мин | да |
| `red-lines` — как есть | ubuntu | PR, push main | <1 мин | <1 мин | да |
| `release` — как есть | 4 таргета | тег | не замерялся | — | — |

¹ Джоб, завершившийся ошибкой, **кэш не сохраняет** (`cache-on-failure` у `Swatinem/rust-cache`
по умолчанию `false`), поэтому пока `lint` и `snapshot-cpu` были красными разведчиками, они
платили холодную сборку каждый прогон. `lint` позеленел 2026-08-19 и записал кэш впервые —
его тёплое время станет известно со следующего прогона; `snapshot-cpu` красный до фикса
BUG-784 и остаётся холодным. Подробности и числа — §12.

Обоснование трёх неочевидных пунктов:

**`snapshot-cpu` — главное приобретение.** [snapshot_cpu.rs](../crates/driver/tests/cases/snapshot_cpu.rs) явно спроектирован кросс-платформенным: растеризация идёт через tiny-skia (чистый Rust, программный растр), шрифт бандленный (Inter Regular через `lumen_font::Rasterizer`), подгрузки сабресурсов в headless-пути нет. Комментарий в шапке файла прямо утверждает идентичность вывода на Windows/macOS/Linux. 81 эталонный PNG в `graphic_tests/snapshots/cpu/`. Это единственная часть пиксельного гейта, не требующая GPU и фокуса окна, и именно её протухание рецидивирует (BUG-118 / 149 / 297 / 316) — сейчас его ловит посторонний человек своим красным гейтом, а не автор правки. **Перед внедрением проверить утверждение о кросс-платформенности эмпирически**: первый прогон на ubuntu против закоммиченных эталонов, снятых на Windows; расхождение хотя бы в одном пикселе означает, что джоб надо ставить на `windows-latest` (тоже бесплатно, просто медленнее по очереди).

**`lint` дублирует локальный `clippy --workspace`.** Это самый дорогой шаг `/lumen-task-finish` (компилирует весь workspace, 32 члена). При бесплатных минутах его дублирование в CI бесплатно, а локально он после этого становится необязательным (§8).

**`cross` вынесен с PR на push/nightly.** Кросс-платформенная поломка ловится сейчас только редким `release.yml`, но чинить её в момент мержа некому и незачем — это не блокер задачи. Плюс это снимает 10 из 15 конкурентных джобов при пяти параллельных ролях.

---

## 5. Кэш, checkout и конкурентность

Три настройки, без которых прогоны будут стабильно холодными и по 45 минут.

**Кэш `target` кэшировать целиком нельзя.** Локальный корневой `target/` = 38 ГиБ при бюджете 10 ГБ на весь репозиторий, а текущий `ci.yml` заводит по отдельному ключу на каждую из трёх ОС. Итог — постоянное взаимное вытеснение и нулевой hit rate. Правильная схема:

- кэшировать не `target` целиком, а только артефакты зависимостей — этим занимается `Swatinem/rust-cache` (вычищает артефакты крейтов workspace, оставляет внешние). Стороннее действие, поэтому пинить по SHA, а не по тегу;
- дополнительно кэшировать `~/.cargo/registry`, `~/.cargo/git` и `$CARGO_HOME/.rusty_v8/` — последнее убирает повторную загрузку ~220 МБ статической библиотеки V8 (механизм описан в [build-speed.md §3.7](build-speed.md));
- писать кэш **только на `main`**, а в PR-прогонах восстанавливать из базовой ветки (правило скоупа это разрешает). Иначе пять ролевых веток разделят 10 ГБ на пять бесполезных долей;
- `sccache` в CI не заводить: он решает ту же задачу, что и кэш зависимостей, но требует внешнего хранилища и на холодной машине выигрыша не даёт.

Даже при идеальном кэше первая сборка после смены `Cargo.lock` останется холодной. Это приемлемо: при бесплатных минутах холодная сборка — только задержка, не стоимость.

**Что показали замеры (2026-08-19, CI-1б).** Три уточнения к схеме выше, ни одно не было очевидно на бумаге:

- **Ключ кэша — на джоб, а не на репозиторий.** `Swatinem/rust-cache` подмешивает в ключ имя джоба: `v0-rust-<job>-<os>-<arch>-<hash>-<hash>`. То есть `test`, `lint`, `cross` держат отдельные записи и наполняют их независимо; переименование джоба (`check` → `test`/`cross`) осиротило старые записи, а не переиспользовало их.
- **Упавший джоб кэш не сохраняет.** `cache-on-failure` по умолчанию `false`, и на каждом красном джобе шаг `Post Cargo dependency cache` стоит `skipped`. Для джоба-разведчика с `continue-on-error` это значит: пока он красный, он ещё и вечно холодный. Симптом читается как «кэш не работает», хотя работает — просто нечего восстанавливать.
- **Правка `rust-toolchain.toml` инвалидирует все записи**, даже когда версия компилятора не меняется (пин `stable` → `1.97.0` при том же 1.97.0): файл тулчейна входит в хэш ключа. Прогон сразу после такого коммита — холодный по всем джобам.

**Бюджет 10 ГБ не является узким местом.** На 2026-08-19 занято 6,38 ГБ в 15 записях, но живое поколение — лишь ~2,2 ГБ (`test` 511 МБ, `cross` 525+491 МБ, `lint` 357+336 МБ). Остальные ~4,2 ГБ — мусор трёх прошлых схем (шесть записей `<OS>-cargo-build|registry-…` от старого трёхключевого кэша, две `v0-rust-check-…` от переименованного джоба, три записи пред-пинового поколения). GitHub вытесняет их по LRU при достижении лимита, вручную чистить не требуется — но при подсчёте «сколько занимает кэш» надо смотреть на живое поколение, а не на суммарную цифру.

**Checkout.** 108 534 файла в индексе, из них 105 702 — вендоренный WPT под `tests/`, `.git` = 495 МБ. Джобам сборки WPT не нужен, поэтому:

```yaml
- uses: actions/checkout@v4
  with:
    fetch-depth: 1
    sparse-checkout: |
      /*
      !/tests/wpt
    sparse-checkout-cone-mode: false
```

Джобу `doc-drift` (он проверяет `wpt_expectations`) — обычный полный checkout.

**Конкурентность.** Лимит 20 джобов на Free, а суперсёданные прогоны при активном push-цикле съедают очередь. В каждый workflow:

```yaml
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
```

---

## 6. Артефакт: как забрать экзешник

`target/dev-release/lumen.exe` = **77 МБ**, в zip-артефакте примерно вдвое меньше; лимит артефактов Free-плана 500 МБ, ретенция по умолчанию 90 дней. Влезает.

Но применение у этого узкое, и это надо держать в голове: **скачивание артефакта не заменяет локальную сборку**. Инкрементальная пересборка shell в `dev-release` после правки — 3.8 с, каскад layout→shell — 8.6 с ([build-speed.md §7](build-speed.md)); CI-прогон это 25–45 мин плюс выгрузка и загрузка 77 МБ. Артефакт оправдан ровно в трёх случаях:

1. бинарь под Linux/macOS — локально не собрать вообще;
2. проверка «собирается на чистой машине» перед релизом;
3. если рабочая машина занята длинным прогоном и нужен бинарь параллельно.

Для повседневной итерации — собирать локально.

Джоб выгрузки (только на `push` в main, чтобы не плодить артефакты на каждый PR):

```yaml
- name: Build dev-release shell
  run: cargo build -p lumen-shell --profile dev-release
- uses: actions/upload-artifact@v4
  with:
    name: lumen-${{ runner.os }}-dev-release
    path: target/dev-release/lumen*
    retention-days: 14
```

---

## 7. Как ассистент пользуется результатами

**Предусловие, требует действия пользователя:** на машине нет `gh` (`bash: gh: command not found`). Без него ассистент не может ни прочитать статус прогона, ни забрать логи, ни скачать артефакт — установка лежит вне рабочего каталога, поэтому её выполняет пользователь:

```bash
winget install --id GitHub.cli
```

После установки — однократная авторизация `gh auth login` (тоже пользователем, ассистент не вводит учётные данные).

Дальше протокол ассистента. Все команды read-only, кроме явно оговорённых.

```bash
# статус последних прогонов
gh run list -L 10

# прогоны конкретной ветки
gh run list --branch p3-bug-405 -L 5

# что именно упало: логи только упавших шагов
gh run view <run-id> --log-failed

# дождаться завершения (для скрипта/фонового ожидания)
gh run watch <run-id>

# забрать артефакт
gh run download <run-id> -n lumen-Linux-dev-release -D .tmp/artifacts/
```

Правила потребления результата:

- **Один прогон — один лог-файл**, как и для локального cargo: `gh run view <id> --log-failed > .tmp/ci-<id>.log 2>&1`, дальше грепать файл. Не дёргать `gh` повторно ради другого фильтра.
- **Ожидание прогона — фоновая задача** (`run_in_background: true`), не `sleep` в цикле. 30–45 минут блокирующего ожидания — то же самое выгорание времени, что описано в CLAUDE.md про 76 минут из 188.
- **Красный CI не диагностировать по названию джоба.** Сначала `--log-failed`, потом вывод. Отдельно держать в голове, что `snapshot-cpu` краснеет от протухших эталонов чаще, чем от реальной регрессии.
- **`gh run rerun` — только по просьбе пользователя.** Это не read-only и не бесплатно по времени очереди.
- **Push ветки для запуска CI требует явной просьбы пользователя** — правило CLAUDE.md «`git push` без явного запроса запрещён» никак не отменяется появлением CI.

---

## 8. Что меняется в протоколе задачи

Сейчас гейт локальный и синхронный: `/lumen-task-finish` прогоняет `clippy --workspace` + `scoped-test.sh`, затем сразу `merge --no-ff` и push. С переносом гейта в CI появляется разрыв в 30–45 минут между «код готов» и «можно мержить».

Предлагаемая раскладка (внедрять только после §11.3, когда CI подтверждён зелёным на нескольких задачах):

| Шаг | Было | Стало |
|---|---|---|
| Перед коммитом | `clippy -p <crate> -- -D warnings` + `cargo test -p <crate>` | без изменений (быстрый локальный отсев) |
| Гейт | `clippy --workspace` + `scoped-test.sh` локально, 10–40 мин | push ветки → джобы `lint`/`test`/`snapshot-cpu`/`doc-drift` |
| Пиксельный гейт | `graphic_tests/run.py` при правках, двигающих пиксели | без изменений, локально (§9) |
| Мерж | сразу после локального гейта | после зелёного CI на ветке |

**Решено 2026-08-18: вариант 1** — ветка `p<N>-…` пушится в `origin`, ждём зелёного CI, мержим
локально. Реализовано: `ci.yml` и `red-lines.yml` триггерятся на push веток `p[1-5]-*` (без этого
push ветки не запускал ничего — триггер был только на `main`), в оба добавлен `concurrency` с
отменой предыдущего прогона на неглавных ветках. Протокол — `CLAUDE.md` §Git workflow, шаг 1б;
удаление удалённой ветки — шаг 7. Полноценные PR с merge queue (вариант 2) не берём. (Прежняя формулировка этого абзаца ставила
условием перевод `bench-gate.yml` на счётчики — workflow удалён 2026-08-19, §11.5, так что
условие отпало вместе с ним; решение против merge queue держится на своих основаниях.)

**Локальный гейт при этом не снимается.** Таблица выше писалась в расчёте на джобы `lint` и
`snapshot-cpu` из §4, которых ещё нет: сегодня CI гоняет `cargo check -p lumen-shell` и юнит-тесты
16 крейтов, то есть **слабее** локального гейта — не проверяет ни `clippy --workspace`, ни
снапшоты, ни scoped-тесты. Поэтому шаг 1б добавлен *поверх* `/lumen-task-finish`, а не вместо
него: CI ловит Linux и macOS, локальный гейт — всё остальное. Снимать локальный гейт можно будет
только после §11.3.

| Шаг | Было | Стало |
|---|---|---|
| Перед коммитом | `clippy -p <crate>` + `cargo test -p <crate>` | без изменений |
| Гейт | `clippy --workspace` + `scoped-test.sh` локально | без изменений (CI пока слабее) |
| Пиксельный гейт | `graphic_tests/run.py` при правках, двигающих пиксели | без изменений, локально (§9) |
| Перед мержем | — | push ветки → зелёный CI (Linux/macOS/Windows) |
| Мерж | сразу после локального гейта | после зелёного CI |
| После мержа | — | `git push origin --delete p<N>-…` |

---

## 9. Что не переносить

| Что | Почему |
|---|---|
| `graphic_tests/run.py` (~20 мин, 70+ страниц) | `gdigrab` снимает весь рабочий стол и требует фокуса окна; сравнение с Edge; порог 0.5 %. На shared runner нет GPU — wgpu уедет на программный бэкенд. Подробности — [graphic-tests.md](graphic-tests.md) |
| Любой перф-замер по wall-clock (по этой причине удалён `bench-gate.yml`, §11.5) | Шумные соседи на shared runner. Бэкенд решает всё: 116 мс DX12 против 53 мс Vulkan на одной машине (BUG-405, срез 14). Прямое подтверждение на нашем же CI (CI-1б): один и тот же шаг на одном коммите — 27 с против 447 с, плюс 205 минут в очереди раннеров. Гейтить можно только счётчиком, идентичностью или абсолютным потолком с большим запасом — [perf-method.md](perf-method.md) |
| Цикл правка→`check` | 4–6 с локально против минут раунд-трипа |
| Прогоны живого окна, MCP-пробы, отладка реальных сайтов | Требуют GPU, окна и интерактивности |
| Всё, что требует larger runners | Тарифицируется даже для публичных репозиториев (§2) |

---

## 10. Self-hosted runner (этап 2, опционально)

Если цель — именно разгрузить рабочую машину, а не автоматизировать проверки, то потолок GitHub-hosted раннеров здесь исчерпан: с них нельзя снять GPU-зависимую часть, а она и есть самая долгая (graphic tests ~20 мин, перф-прогоны).

Self-hosted runner на **втором** железе снимает и это: минуты не тарифицируются на любом плане, лимит джоба 5 дней вместо 6 часов, доступен реальный GPU и рабочий стол. Условия применимости:

- нужна вторая машина с Windows, дискретным или интегрированным GPU и залогиненной десктоп-сессией (иначе `gdigrab` бесполезен);
- на ней действует та же готча: любая смена фокуса рабочего стола во время прогона молча портит все скриншоты — значит машина должна быть выделенной, не рабочей;
- **на публичном репозитории self-hosted runner небезопасен по умолчанию**: PR от постороннего исполнит произвольный код на вашей машине. Обязательно ограничить триггеры (`push` в main и `workflow_dispatch`, без `pull_request` от форков) либо требовать ручного одобрения запусков.

Установка runner на ту же машину, где идёт разработка, смысла не имеет — ресурсы не освобождаются, добавляется только автоматизация запуска.

---

## 11. План внедрения

Порядок важен: каждый шаг проверяем прежде, чем строить на нём следующий.

**11.1. ~~Поставить `gh` и снять базовые замеры~~ — СДЕЛАНО 2026-08-18.** `gh` поставлен; выяснилось, что для статусов он не нужен (§3.1). История прочитана — `ci.yml` был мёртв два месяца, `release.yml` и `bench-gate.yml` не запускались ни разу; обе поломки починены, CI зелёный на трёх ОС. Оценки §4 всё ещё не заменены фактами: замеры времени снимать после §11.2.

**11.2. Починить кэш и checkout в существующем `ci.yml`** (§5) — убрать кэш `target` по трём ключам, добавить `rust-cache` + `sparse-checkout` + `concurrency`. Это отдельная небольшая задача с веткой; проверка — падение времени второго прогона против первого.

**11.3. Добавить `lint`, `snapshot-cpu`, `doc-drift`; `cross` перевести на push/nightly** (§4). Перед включением `snapshot-cpu` в блокирующие — эмпирически подтвердить кросс-платформенность эталонов (§4). **`lint` вводить в два приёма**, как сами правила (`docs/lint-policy.md` §6): ярус 0 внедрялся и проверялся только на Windows/MSVC, а 12 файлов содержат код под `#[cfg(target_os = "linux"/"macos")]`/`unix`, который ни разу не линтовался. Сначала `continue-on-error: true` — собрать список нарушений на Linux и macOS, закрыть отдельным коммитом, и только потом делать джоб блокирующим.

**Приём 1 сделан 2026-08-18.** Все пять джобов в `ci.yml`; `lint` (ubuntu + macos) и `snapshot-cpu` (ubuntu) стоят с `continue-on-error: true` — это разведка, а не готовый гейт. `doc-drift` включён сразу блокирующим (сборка не нужна, платформенного поведения нет). Отклонения от §4, сознательные:

- **`cross` гоняет не только `cargo check`, но и юнит-тесты.** По §4 предполагался только `check`, но тогда тесты на macOS/Windows исчезли бы совсем — а именно macOS-прогон вскрыл флак [BUG-783](../bugs/BUG-783-OPEN.md). Полная матрица уходит с горячего пути ветки на push-в-main и nightly (`cron 0 3 * * *`), так что стоимость та же, что была, а очередь ветки освобождается.
- **`run_report.py --check` в `doc-drift` не входит**, хотя §4 его туда записывал: это часы, живой бинарник браузера и работающий `wss`-сервер (CLAUDE.md §Known gotchas). Остаётся ручным гейтом дорожки TEST у P2. Джоб проверяет только `gen_symbols.py` и `gen_roadmap.py` — обоим хватает Python и 2 минут.

**Результат разведки** (прогон `32183754261`, ветка `p5-ci-jobs`, 2026-08-18). Блокирующие джобы зелёные, оба разведчика красные — ровно то, ради чего они и заводились:

| Джоб | Находка |
|---|---|
| `lint` / macos | `crates/core/src/memory_pressure.rs:209` — `unsafe` без `// SAFETY:`. Ровно предсказанная дыра: блок под macOS-`cfg`, clippy его никогда не видел, в реестре §10 `docs/lint-policy.md` его нет |
| `lint` / ubuntu | `crates/engine/layout/src/style.rs:22032` — `clippy::question_mark`. **Платформа ни при чём:** раннер берёт `dtolnay/rust-toolchain@stable` = 1.97.0, а на машине разработчика 1.96.1, и в 1.97 этот линт стал срабатывать шире. То есть CI и локальный гейт гоняют РАЗНЫЙ clippy |
| `snapshot-cpu` | Прогон `…754261`: 80 из 81 эталона совпали побайтно, разошёлся `49-background-blend-mode` — 108 байт из 2 949 120 (0,004 %). Прогон `…116431`, **тот же код** (коммит правит только доки), тот же `ubuntu-latest`: **все 81 совпали**. Прогон `…481322`: снова красный. Итого 2 из 3 — это не платформенная разница, а **недетерминизм самого теста**, при том что вся его посылка в детерминированности (tiny-skia, бандленный шрифт, без сети). [BUG-784](../bugs/BUG-784-OPEN.md) |
| `doc-drift` | Зелёный на `SYMBOLS.md` (после регенерации), **красный на `docs/roadmap-*.html`** — и это дефект самой проверки, а не дрейф: `gen_roadmap.py` вшивает в JSON поле `"generated": "<сегодня>"`, поэтому побайтное сравнение обречено в любой день после коммита, а раннер живёт в UTC и расходится с автором даже в тот же день. Лечится `git diff -I'"generated":'` |

Три следствия, каждое требует решения перед снятием флагов:

1. **Расхождение тулчейнов надо закрыть явно** — либо пин `dtolnay/rust-toolchain@1.96.1` в CI (тогда CI перестаёт ловить новые линты, но гейты совпадают), либо `rustup update` на машине и правка «Rust 1.95+» в CLAUDE.md. Пока расхождение живо, «зелёный локально» и «зелёный в CI» — разные утверждения. Решение за пользователем.
2. **Список нарушений `lint` заведомо неполон.** `-D warnings` роняет сборку крейта на первой ошибке, поэтому виден один дефект на ОС, а не все: после правки `memory_pressure.rs` и `style.rs` прогон покажет следующий слой. Приём 2 — это цикл «починил → перегнал», а не один коммит.
3. **`49-background-blend-mode` плавает** — красный и зелёный на одном и том же коде и одной и той же ОС, с разницей в один прогон. Это не «протухший эталон» и не платформенная разница: регенерация ничего не чинит, а джоб, включённый блокирующим прямо сейчас, будет ронять чужие ветки случайным образом (и, что хуже, приучит их перезапускать). Заведён [BUG-784](../bugs/BUG-784-OPEN.md). Пока баг открыт, `snapshot-cpu` остаётся разведкой — это единственная причина, по которой он ещё не блокирующий: с эталонами всё в порядке, 80 из 81 совпали побайтно в обоих прогонах.

Отдельный вывод про сам метод: **два прогона одного коммита стоили дешевле, чем разбор одного.** Первый прогон выглядел как «нашли платформенную разницу растеризации» и уехал бы в задачу P3 на полдня; второй показал, что находка другого класса. Для любого нового CI-джоба, чей предмет — детерминизм, первый красный результат нельзя брать за факт до повторного прогона.

**Приём 2 сделан 2026-08-19.** Оба следствия №1 и №2 закрыты, `continue-on-error` снят с `lint` (обе ОС в матрице); `snapshot-cpu` флагом остался — следствие №3 (BUG-784) ещё открыто.

- Следствие №1 (расхождение тулчейнов) решил пользователь в тот же день: `rust-toolchain.toml` запинён на `1.97.0` вместо плавающего `"stable"` (коммит `38ba92d7f`), CI и машина разработчика теперь гоняют один и тот же clippy. Побочная цена — `sccache` 0.15.0 несовместим с 1.97.0 и отключён (`STATUS_STACK_BUFFER_OVERRUN` на любом вызове компилятора), подробности в `CLAUDE.md` §Known gotchas.
- Обе находки приёма 1 исправлены: `memory_pressure.rs` получил второй `// SAFETY:` для соседнего `unsafe`-блока (первый комментарий покрывал только `mem::zeroed`, не `host_statistics64` — ровно тот паттерн, о котором предупреждает CLAUDE.md), `style.rs::apply_font_size` переписан на `?` вместо `match ... None => return None`.
- Следствие №2 (список неполон, `-D warnings` роняет на первой ошибке) закрыто не итерациями «чинил → перегонял», а статическим сканом всех `#[cfg(target_os = "linux"/"macos")]`/`unix`-гейтов сразу — дешевле, чем гонять CI по одной находке за прогон. По пути нашлись три `Mutex::lock().unwrap()` в `ctap2.rs::linux_hid` (linux-only production-код): не переписаны на `Ctap2Error` (сменило бы поведение), а заведены в реестр §10 `docs/lint-policy.md` с функциональным `#[allow]`, как остальные 511 мест. `missing_docs` отдельного скана не потребовал — cfg-гейтов на объявлениях модулей в воркспейсе нет.
- Локальная проверка перед снятием флага: `cargo clippy --workspace --all-targets -- -D warnings` под запиненным 1.97.0 — чисто (лог `.tmp/p5_clippy_workspace.log` на ветке `p5-ci-lint-fixes`).
- `snapshot-cpu` остаётся `continue-on-error` — BUG-784 не закрыт, решение то же: снимать флаг только после фикса недетерминизма, не раньше.

**11.4. ~~Решить вопрос PR-based workflow~~ — СДЕЛАНО 2026-08-18:** вариант 1, ветки гейтятся push-ом без PR (§8).

**11.5. ~~Разобраться с `bench-gate.yml`~~ — СДЕЛАНО 2026-08-19 (CI-3): workflow удалён.**

Выбор был «перевести на счётчики либо отключить». Переводить оказалось нечего: под именем
перф-гейта в проекте жили **два независимых механизма, и ни один никуда не подключён**.

| | `bench/compare.py` (звал `bench-gate.yml`) | `crates/bench/src/ci_gate.rs` (`lumen-bench --ci`) |
|---|---|---|
| Критерий | отклонение медианы и p95 от эталона > 5 % | абсолютные потолки: mean < 200 мс, peak RSS < 512 МБ, bfcache P50 < 50 мс |
| Эталон | `bench/baseline.json`, снят **2026-05-28**, `"lumen_version": "Phase 0 prototype"` | потолков не требует |
| Кто вызывает | `bench-gate.yml` | **никто** — ни workflow, ни `scoped-test.sh` |
| Прогонов | **0 за всё время** | — |

Удалён первый. Шесть причин, любой хватило бы:

1. **Гейт по дельте wall-clock на shared runner запрещён §9** и [perf-method.md](perf-method.md). Порог 5 % применялся к медианам в микросекундах: у фазы `decode` медиана эталона 1,5 мкс, то есть гейт срабатывал на 0,075 мкс. На виртуалке с шумными соседями это чистый шум — см. числа CI-1б в §12.
2. **Эталон протух на три месяца**: снят 28 мая на «Phase 0 prototype», проект с тех пор дошёл до Phase 2 v0.5.0. Сравнивать с ним нечего.
3. **Оба финальных шага комментируют PR** (`github.rest.issues.createComment` по `context.issue.number`), а проект решением §11.4 (вариант 1) PR не открывает — гейтит push ветки. В этом контексте шаги нерабочие.
4. **Кэш — та самая устаревшая схема**, которую CI-1 выпилил из `ci.yml`: `actions/cache@v4` на весь `target` ключом `${{ runner.os }}-cargo-build-target-…`. Именно её осиротевшие записи нашлись при замерах CI-1б (§12).
5. **`compare.py` зашивает путь конкретной машины** — `env["PATH"] = "/c/Users/konstantin/.cargo/bin:…"`.
6. **Ни одного прогона** — триггер `pull_request` при отсутствии PR.

**Что осталось и почему.** `bench/compare.py` и `bench/baseline.json` не удалены: как локальный
инструмент по [bench/UPDATE.md](../bench/UPDATE.md) они осмысленны — на одной машине сравнение с
эталоном корректно, там нет шумных соседей. Но эталон требует обновления перед первым же
осмысленным запуском.

**Преемник — [PERF-7](../ROADMAP.md)** (`planned`, владелец P1/P3): ночной прогон корпуса с
порогами в `crates/bench/src/ci_gate.rs`, «аналог храповика KNOWN_DEBTORS». Механизм там уже
правильнее — абсолютные потолки с большим запасом переносят дрожание раннера так, как никогда
не перенесёт дельта в 5 %. Заводить отдельную задачу не потребовалось: PERF-7 покрывает её
целиком. **P5 в `crates/bench` не лезет** — это чужой крейт, правка меняет поведение.

**11.6. Опционально — self-hosted runner** (§10), только при наличии второго железа.

Не входит в план и делается отдельно: в `.claude/worktrees/` лежат 63 каталога при 10 зарегистрированных в git — 53 осиротевших worktree с копиями дерева на 108 тысяч файлов. По освобождению ресурсов машины эта чистка (`/lumen-health-check branches`, роль P5) почти наверняка даст больше, чем весь перенос сборок в CI, и не требует менять протокол.

---

## 12. Журнал

| Дата | Шаг | Результат |
|---|---|---|
| 2026-08-18 | Документ составлен | Лимиты трёх CI проверены по документации; аудит четырёх существующих workflow; ничего не внедрено |
| 2026-08-18 | Прочитана история прогонов | `ci.yml` красный с 2026-06-17 (1187 прогонов, 0 успешных); `release.yml` и `bench-gate.yml` — 0 прогонов. Причины: sccache-обёртка из `.cargo/config.toml` и отсутствие `libasound2-dev`. См. §3.1 |
| 2026-08-18 | Обе причины починены | Прогон `32139440248` — success на ubuntu, macos, windows. Первый зелёный CI за два месяца |
| 2026-08-18 | §11.2 сделан: кэш и checkout | Кэш всего `target/` тремя ключами по числу ОС убран, заменён на `Swatinem/rust-cache` (пин по SHA `6323deb1`, v2.9.2) с `save-if` только на `main` и дополнительным `~/.cargo/.rusty_v8`. Добавлен `sparse-checkout` без `tests/wpt` (105 702 из 108 534 файлов; вне `tests/wpt` в `tests/` нет ничего). **Числа пока не сняты:** прогон на ветке кэш не пишет (`save-if`), поэтому выигрыш виден только на втором прогоне `main` после мержа — сравнивать N и N+1. *(Сняты 2026-08-19 — строка CI-1б ниже и разбор под таблицей.)* |
| 2026-08-18 | §11.4 решён: вариант 1 | `ci.yml`/`red-lines.yml` триггерятся на `p[1-5]-*`, добавлен `concurrency`; протокол задачи вырос с 7 до 8 шагов |
| 2026-08-18 | §11.3, приём 1: джобы заведены | Было 1 джоб (`check`, матрица 3 ОС), стало 5: `test` (ubuntu, блокирует), `doc-drift` (ubuntu, блокирует), `lint` (ubuntu+macos, **разведка**), `snapshot-cpu` (ubuntu, **разведка**), `cross` (macos+windows, только push-в-main и nightly). `doc-drift` потребовал регенерации `SYMBOLS.md` — индекс отстал от кода (2295 строк дрейфа, сдвиги `file:line` + новые `///`), то есть проверка оправдала себя ещё до первого прогона. `docs/roadmap-*.html` дрейфа не имели |
| 2026-08-19 | §11.3, приём 2: `lint` стал блокирующим | Тулчейн запинён на 1.97.0 пользователем (снимает расхождение CI/машина), обе находки приёма 1 исправлены (`memory_pressure.rs` SAFETY, `style.rs` `?`), слепое пятно не-Windows `cfg`-гейтов закрыто статическим сканом (не итерациями CI) — три `unwrap()` в `ctap2.rs::linux_hid` ушли в реестр §10. `continue-on-error` снят с `lint` (обе ОС). `snapshot-cpu` остаётся разведкой — BUG-784 (флак эталона) не закрыт. Проверено на прогоне `32250819715`: `lint` зелёный на ubuntu и macos с первого блокирующего захода |
| 2026-08-19 | **CI-1б: числа по кэшу сняты** | Оценки §4 заменены замерами (были завышены в 4–8 раз), в §5 добавлены три неочевидных факта о ключе кэша. Полный разбор — ниже под таблицей |
| 2026-08-19 | **CI-3 (§11.5): `bench-gate.yml` удалён** | Переводить на счётчики было нечего: перф-гейтов в проекте оказалось два (`bench/compare.py` и `crates/bench/src/ci_gate.rs`), и ни один не вызывался ниоткуда. Удалён тот, что гейтил по 5 % дельты wall-clock-медиан (у `decode` это 0,075 мкс) против эталона «Phase 0 prototype» от 28 мая, комментировал несуществующие PR и держал устаревшую схему кэша. `bench/compare.py` оставлен как локальный инструмент. Преемник — PERF-7 поверх `ci_gate.rs` (абсолютные потолки вместо дельт) |

### CI-1б: замер выигрыша от кэша (2026-08-19)

Задача требовала сравнить первый прогон `main` после мержа CI-1 (пишет кэш) со следующим
(восстанавливает). Удачно вышло лучше: **два прогона одного и того же коммита** `30d530316` —
`32188492988` (кэш пуст) и `32212888423` (кэш наполнен). Различие только в состоянии кэша,
поэтому это честный A/B, а не сравнение разных деревьев.

**Сначала о том, чем мерить нельзя.** Wall-clock прогона целиком для этого не годится:
`32188492988` числится как 220 минут, из которых **205 — очередь раннеров** (все семь джобов
стартовали через 205m02s–205m06s после `run_started_at`). Ровно та ловушка, о которой
предупреждает [perf-method.md](perf-method.md): мерить надо шаг, который меняешь, а не общее
время. Ниже — время джобов и шагов.

| Шаг | Холодный (`…492988`) | Тёплый (`…888423`) | Δ |
|---|---|---|---|
| `test` / Compile check (lumen-shell) | 168 с | **31 с** | −137 с (5,4×) |
| `test` / юнит-тесты (три шага суммой) | 251 с | 196 с | −55 с |
| `test` / джоб целиком | 466 с | 342 с | −124 с (−27 %) |
| `cross` windows / Compile check | 354 с | **71 с** | −283 с (5,0×) |
| `cross` windows / джоб целиком | 923 с | 488 с | −435 с (−47 %) |
| `cross` macos / Compile check | 254 с | **29 с** | −225 с (8,8×) |
| восстановление кэша (шаг `Cargo dependency cache`) | 1–4 с (промах) | 11–25 с | +10–20 с |
| сохранение кэша (шаг `Post …`) | 7–117 с | ~0–1 с | — |

Выигрыш сосредоточен в компиляции зависимостей: `cargo check -p lumen-shell` ускоряется в
5–9 раз. Юнит-тесты выигрывают заметно меньше — их время это в основном исполнение, а не сборка.
Плата за восстановление (10–20 с) и за сохранение (до 117 с на macOS) окупается на порядок.

Установившийся тёплый режим (`32250819715`, `0f1e14481`): `test` 3,8 мин, `cross` macos 6,1 мин,
`cross` windows 8,4 мин, `doc-drift` 16 с; весь прогон — **8 мин 30 с** wall-clock против
20 мин 41 с у холодного прогона `32243483490`. Для сравнения, §4 закладывала 25–45 минут на
**каждый** джоб.

**Три находки, каждая меняет то, как читать результат:**

1. **Красный джоб не сохраняет кэш.** У `Swatinem/rust-cache` `cache-on-failure` по умолчанию
   `false`, и на всех упавших джобах шаг `Post Cargo dependency cache` стоит `skipped`. Поэтому
   `lint` и `snapshot-cpu`, стоявшие разведчиками с `continue-on-error`, оставались холодными
   бесконечно: шаг clippy занимал 150 → 149 → 238 → 192 с по четырём прогонам подряд без следа
   прогрева. Как только `lint` позеленел (`32250819715`), `Post` отработал за 28 и 9 с и в
   реестре кэша впервые появились записи `v0-rust-lint-*`. У `snapshot-cpu` картина не изменится,
   пока открыт BUG-784: 411 → 405 → 394 → 394 → 389 с. **Следствие для любого будущего
   джоба-разведчика:** «разведка» стоит дороже, чем кажется, — не только не гейтит, но и не
   греется, и её время нельзя сравнивать с временем блокирующего джоба.
2. **Ключ кэша включает имя джоба и файл тулчейна.** Записи выглядят как
   `v0-rust-<job>-<os>-<arch>-<hash>-<hash>`, у `test`/`cross`/`lint` они независимы.
   Переименование джоба осиротило старые записи; правка `rust-toolchain.toml` (пин `stable` →
   `1.97.0` **при той же версии компилятора**) сменила хэш и обнулила все — прогон сразу после
   пина (`32243483490`) отработал холодным по всем джобам (Compile check 216 с), а следующий
   (`32246145795`) — тёплым (16 с).
3. **Бюджет 10 ГБ не жмёт.** Занято 6,38 ГБ в 15 записях, но живое поколение — ~2,2 ГБ;
   остальное мусор трёх прошлых схем, который GitHub вытесняет по LRU. Опасение §5 «пять ролевых
   веток разделят 10 ГБ» не подтвердилось и по другой причине: `save-if` пишет только с `main`,
   ветки лишь читают.

Отдельно стоит держать в голове **шум раннера**: шаг `Install Linux system dependencies`
(`apt-get`) на одном и том же коммите занимал 27 с в одном прогоне и 447 с в другом. Он не
кэшируется и к кэшу отношения не имеет, но легко съедает весь выигрыш в отчёте по джобу —
сравнивать надо шаги сборки, а не джоб целиком.
