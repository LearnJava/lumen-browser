# ROADMAP.md — Дерево задач Lumen Browser

Плоский, grep-friendly источник структуры для roadmap-деревьев (`docs/roadmap-*.html`).
Одна запись = одна строка. Размер файла нерелевантен: `grep "| U-6 " ROADMAP.md` достаёт ровно свою задачу.

Заменяет прежний вложенный `docs/roadmap.json`. После любой правки запусти:

```bash
python scripts/gen_roadmap.py
```

**Что здесь хранится:** структура фаз/задач (id, иерархия `parent`), курируемый статус и связи
баг→задача (колонка `bugs`). **Статусы и заголовки самих багов НЕ здесь** — они подтягиваются из
`BUGS.md` автоматически генератором.

**Статус задачи — производный.** `gen_roadmap.py` пересчитывает его из живых багов (все
FIXED/DEBTOR → `done`, IN PROGRESS → `active`) и из подзадач (все `done` → `done`). Колонка
`status` ниже — лишь запасной вариант для фич БЕЗ багов и БЕЗ подзадач (planned-фичи); у задач с
багами её править бесполезно — перезапишется при генерации.

**Иерархия:** `parent` пуст → задача висит прямо под фазой; `parent = <id>` → вкладывается в эту
задачу (рекурсивно).

**Статусы задач:** `done` · `active` · `blocker` · `wait` · `ready` · `queued` · `planned` · `opt`.
**Статусы багов** (из `BUGS.md`, здесь не задаются): `open` · `inprogress` · `fixed` · `wontfix`.

---

## Фазы

| id | status | date | title |
|---|---|---|---|
| P0 | done | 2026-05-26 | Фаза 0 — Прототип |
| P1 | done | 2026-06 | Фаза 1 — v0.1 «Reader» |
| P2 | done | app v0.5.0 | Фаза 2 — v0.5 «Interactive» (завершена) |
| P3 | planned | 36–48 мес | Фаза 3 — v1.0 «Full Browser» |
| P4 | planned | — | Фаза 4 — После 1.0 |

## Задачи

| id | phase | parent | status | size | bugs | note | title |
|---|---|---|---|---|---|---|---|
| P0-ws | P0 | | done | | | | Workspace + базовые крейты |
| P0-dom | P0 | | done | | | | DOM (арена NodeId, без Rc<RefCell>) |
| P0-layout | P0 | | done | | | | Block-flow layout + word-wrapping |
| P0-paint | P0 | | done | | | | Paint: FillRect через wgpu |
| P0-driver | P0 | | done | | | | Automation foundation: lumen-driver + off-screen рендер + tiny-skia |
| P0-tabinv | P0 | | done | | | | Tab lifecycle инварианты (DOM arena / JS suspend / pure layout) |
| P1-font | P1 | | done | | | | Font fallback / matcher (системный загрузчик) |
| P1-hidpi | P1 | | done | | | | HiDPI / DPR-awareness |
| P1-scroll | P1 | | done | | | | Scroll + базовый input в shell |
| P1-stream | P1 | | done | | | | Progressive / streaming rendering pipeline |
| PH1-2 | P1 | P1-stream | done | | | | PH1-2: window-first + 60Hz throttle + параллельный CSS |
| PH1-2a | P1 | P1-stream | done | | | | PH1-2a: TCP-level streaming HTTP body |
| PH1-2b | P1 | P1-stream | done | | | | PH1-2b: инкрементальный (dirty-subtree) layout |
| PH1-2c | P1 | P1-stream | done | | | | PH1-2c: прогрессивная подгрузка картинок |
| P1-url | P1 | | done | | | | Url как структурированный тип |
| P1-net | P1 | | done | | | | Network: EventSink + RequestFilter hook + bench baseline |
| P1-css21 | P1 | | done | | | | CSS 2.1 + flexbox |
| P1-img | P1 | | done | | | | Картинки (decode/paint) |
| P1-tabs | P1 | | done | | | | Вкладки, история, закладки |
| PH1-4 | P1 | | done | | | | PH1-4: Network service в отдельном процессе (lumen-ipc) |
| P1-storage | P1 | | done | | | | Storage service + базовый adblock + DoH |
| PH1-5 | P1 | | done | | | | PH1-5: пакеты Linux/macOS/Windows (CI/CD) |
| PH1-6 | P1 | | done | | | | PH1-6: Stacking contexts + CSS Painting Order |
| PH1-7 | P1 | | done | | | | PH1-7: Compositor thread + Property Trees |
| PH1-8 | P1 | | done | | | | PH1-8: Preload scanner |
| PH1-9 | P1 | | done | | | | PH1-9: lumen-mcp-server (Automation Phase 1) |
| P1-tablife1 | P1 | | done | | | | Tab lifecycle Phase 1 (TabState T0–T4, memory pressure, image LRU, T1 paused) |
| P2-usability | P2 | | ready | | | Все подзадачи done, остался U-6 (ready) — остаточные рендер-баги через BUGS.md | USABILITY-вертикаль — «зашёл на сайт и комфортно пользуешься» |
| U-0 | P2 | P2-usability | done | | | | U-0: --screenshot headless CPU-снимок |
| U-1.1 | P2 | P2-usability | done | | | | U-1 этап 1: неблокирующая навигация |
| U-2 | P2 | P2-usability | done | | | | U-2: шейпинг текста GSUB/GPOS + CFF-контуры |
| U-3 | P2 | P2-usability | done | | | | U-3: многовкладочность TAB-1…7 + IPC |
| U-4 | P2 | P2-usability | done | | | | U-4: WASM MVP-интерпретатор + graceful WebCodecs + i64↔BigInt |
| B-1 | P2 | P2-usability | done | XL | BUG-222 | | B-1: QuickJS вне UI-потока (разблокировка) |
| U-1.2 | P2 | P2-usability | done | L | BUG-171,BUG-172 | | U-1 этап 2: весь пайплайн вне UI-потока |
| U-5 | P2 | P2-usability | done | S | BUG-167 | | U-5: Fullscreen пересчитывает вьюпорт |
| U-6 | P2 | P2-usability | ready | | BUG-104,BUG-126,BUG-144,BUG-085 | | U-6: рендер-паритет high-deviation |
| OBS | P2 | P2-usability | done | M | BUG-221 | | Наблюдаемость: CPU-снимок = окно (полная замена gdigrab) |
| U-4opt | P2 | P2-usability | done | | | U-4a SIMD/threads/atomics + U-4b live-aliasing + U-4c WebGPU backend все ✅ 2026-06-20…21 | U-4 опции: WASM SIMD/threads, live-aliasing, WebGPU backend |
| P2-js | P2 | | done | | | | QuickJS интеграция + Tier 1 Web APIs |
| P2-forms | P2 | | done | | | | Формы, базовая интерактивность (Forms runtime) |
| P2-h2 | P2 | | done | | | | HTTP/2 |
| P2-gpu | P2 | | done | | | | GPU compositor (wgpu) |
| P2-grid | P2 | | done | | | | CSS Grid |
| PH2-2 | P2 | | done | | | | PH2-2: Site isolation Phase 1 (COOP/COEP/CORP) |
| PH2-3 | P2 | | done | | | | PH2-3: Профили + шифрование (AES-256-GCM vault) |
| P2-fp | P2 | | done | | | | Anti-fingerprinting |
| P2-knowledge | P2 | | done | | | F2-5 ✅ 2026-06-22 | Knowledge layer ядро (FTS, аннотации, read-later, focus mode) |
| P2-viewport | P2 | | done | | | | <meta viewport> parsing + page zoom |
| P2-ui | P2 | | done | | | F2-6 ✅ (light/dark/system themes + 6 accent presets; docked sidebars drag-resize + cross-dock move + persist); остаток F2-6 — инфраструктурный SurfaceManager (ADR-009), без новой UX-ценности | Кастомизация UI (drag&drop панелей, темы) |
| P2-shadow | P2 | | done | | | | Shadow DOM + custom elements + <template>/<slot> |
| PH2-7 | P2 | | done | | | | PH2-7: Accessibility tree + platform bridges Phase 1 |
| P2-picture | P2 | | done | | | | <picture>/srcset/sizes + loading=lazy |
| P2-ime | P2 | | done | | | | IME composition events |
| P2-find | P2 | | done | | | | Find in page (Ctrl+F) |
| P2-devtools | P2 | | done | | | | DevTools / Inspector через CDP (DOM + computed + network) |
| P2-blend | P2 | | done | | BUG-144 | | mix-blend-mode / backdrop-filter / isolation |
| PH3-2 | P2 | | done | | | | lumen-bidi-server (WebDriver BiDi, Automation Phase 2) |
| P2-tablife2 | P2 | | done | | | | Tab lifecycle Phase 2 (T2 heap snapshot, T3 hibernation, GPU/glyph LRU, UI affordance) |
| P2-canvas | P2 | | done | | BUG-099 | | Canvas 2D (Phases 1–5, Path2D) |
| P2-viewtrans | P2 | | done | | BUG-103 | F2-4 ✅ 2026-06-22 (root cross-fade); полный L1 — опц. остаток | View Transitions API |
| P2-masonry | P2 | | done | | BUG-105,BUG-143 | F2-3 ✅ 2026-06-22 (паритет с Edge-fallback; Edge не поддерживает masonry) | CSS Masonry layout |
| P2-scrolldriven | P2 | | planned | | BUG-127 | PARTIAL (ревизия P3 2026-07-04): парсинг + resolve прогресса от scroll/view готовы (scroll_timeline.rs), композит animated background-color готов (BUG-231 FIXED); TEST-78 остаток = font-parity (BUG-127 → KNOWN_DEBTOR 4.64) | CSS Scroll-Driven Animations L1 |
| P2-motionpath | P2 | | done | | BUG-125 | BUG-125 FIXED 2026-06-22 | CSS Motion Path L1 (offset-path) |
| P3-v8 | P3 | | planned | XL | | Разблокирован (v0.5.0 вышел). Единственное лекарство для тяжёлых SPA: аудит 2026-07-02 — github.com не дорендерился за 280 с (ресурсы загружены к 6.6 с, затык в QuickJS-интерпретаторе без JIT). Рекомендация: делать ПОСЛЕ дешёвых RP-5…RP-9 (они закрывают бо́льшую часть «не как в Edge» и не зависят от JS-движка), затем V8 как флагман Phase 3. Опц. промежуточный митигейт — watchdog/бюджет исполнения JS, чтобы страницы падали graceful, а не висли. Бриф docs/tasks/ph3-v8-migration.md | Переход на V8 (rusty_v8) + Tier 2 Web APIs |
| P3-idb | P3 | | done | | | P3-idb ✅ 2026-06-25 (NativeIdbStore wired into shell; structured schema mirror via _lumen_idb_schema_op; snapshot-blob restore; JS integration test) | IndexedDB |
| P3-h3 | P3 | | active | | | В работе (P1): срез 1 ✅ 2026-07-04 — QUIC varint (RFC 9000 §16) + HTTP/3 frame codec (RFC 9114 §7.2); срез 2 ✅ 2026-07-04 — QPACK field-section codec (RFC 9204, static table only = поведение при SETTINGS_QPACK_MAX_TABLE_CAPACITY=0; переиспользует HPACK-примитивы); срез 3 ✅ 2026-07-04 — Alt-Svc parser + AltSvcCache (RFC 7838, h3-only, TTL-expiry, детерминируемый по Instant), pure parse без IO; срез 4 ✅ 2026-07-04 — QUIC transport frame codec (RFC 9000 §19, все типы фреймов PADDING…HANDSHAKE_DONE, pure parse/serialize поверх varint, +22 теста); срез 5 ✅ 2026-07-04 — QUIC packet header codec (RFC 9000 §17, все формы пакетов: Initial/0-RTT/Handshake/Retry/Version Negotiation/short 1-RTT, header-protected биты и AEAD-payload сохраняются как opaque, pure parse/serialize, +19 тестов); срез 6 ✅ 2026-07-04 — QPACK dynamic table + encoder/decoder instruction streams (RFC 9204 §3.2/§4.3/§4.4: byte-budget capacity, oldest-first eviction, absolute/relative indexing, Set Capacity/Insert Name Ref/Insert Literal Name/Duplicate, Section Ack/Stream Cancellation/Insert Count Increment; apply() воспроизводит таблицу пира; переиспользует qpack string/static-table pub(crate), +25 тестов); срез 7 ✅ 2026-07-04 — QUIC RTT-эстиматор + NewReno congestion controller (RFC 9002 §5/§7: smoothed_rtt/rttvar/min_rtt EWMA 7-8·3-4, PTO §6.2.1; cwnd через slow start/congestion avoidance/recovery, halving §7.3.2, collapse под persistent congestion §7.6; чистые state-machine, драйвятся acked/lost пакетами, без sent-packet registry и IO, +24 теста); срез 8 ✅ 2026-07-04 — QUIC sent-packet registry + loss detection (RFC 9002 §6: per-space SentPacketRegistry на BTreeMap, on_ack_received удаляет newly-acked + RTT-сэмпл §5.1, detect_and_remove_lost_packets по packet-threshold §6.1.1 и time-threshold §6.1.2, loss_time для таймера, establishes_persistent_congestion §7.6.2; чистая state-machine, now передаётся снаружи, драйвит recovery; переиспользует recovery::K_GRANULARITY + quic_frame::AckRange, +19 тестов); срез 9 ✅ 2026-07-04 — QUIC loss-detection timer + PTO (RFC 9002 §6.2, Appendix A: LossDetection связывает 3 per-space registry + RttEstimator + pto_count; set_loss_detection_timer = earliest loss_time, иначе PTO с exp-backoff 2^pto_count, иначе disarm; get_pto_time_and_space с anti-deadlock от now (§6.2.2.1) и пропуском App Data до handshake_confirmed; on_timeout = declare time-threshold losses ЛИБО SendProbe 1/2 + bump backoff; App Data PTO добавляет max_ack_delay; discard_space §A.4; чистая state-machine, now снаружи, saturating arithmetic, +14 тестов); срез 10 ✅ 2026-07-04 — QUIC stream data model (RFC 9000 §2/§3/§4: RecvStream = out-of-order/overlapping reassembly с предпочтением уже принятых байт §2.2, receive flow-control §4.1, final-size инварианты §4.5, recv state-machine §3.2; SendStream = буфер + emit STREAM-фреймов bounded peer max_stream_data §4.1, on_ack→DataRecvd, reset, send state-machine §3.1; stream-id helpers §2.1; чистая state-machine, без connection-level flow control/ретрансмиссии/IO, +29 тестов); срез 11 ✅ 2026-07-04 — QUIC connection-level flow control + stream-count limits (RFC 9000 §4.1/§4.6: SendConnFlow/RecvConnFlow = connection-wide MAX_DATA budget поверх per-stream MAX_STREAM_DATA — send-side available/allowed/blocked_at (DATA_BLOCKED §19.12), recv-side record_received enforcement + record_read/window_update re-advertise; SendStreamLimit/RecvStreamLimit = MAX_STREAMS budget по направлению — send-side open() выдаёт client-initiated stream-id n*4+dir_bit / blocked_at (STREAMS_BLOCKED §19.14), recv-side record_open enforcement + record_closed/window_update; StreamDir/stream_count helpers §2.1; ConnError→FLOW_CONTROL_ERROR/STREAM_LIMIT_ERROR; чистая state-machine без IO, +22 теста); срез 12 ✅ 2026-07-04 — HTTP/3 stream layer (RFC 9114 §6.2/§7.1/§4.1: UniStreamType демультиплексирует однонаправленные стримы по type-префиксу — control/push+PushID/QPACK enc/QPACK dec/reserved; UniStreamRegistry = «ровно один» control/QPACK-encoder/QPACK-decoder → H3_STREAM_CREATION_ERROR на дубль, H3_CLOSED_CRITICAL_STREAM на закрытие критического; ControlStream = первый фрейм SETTINGS иначе H3_MISSING_SETTINGS, SETTINGS максимум раз, без request-фреймов; RequestStream = грамматика HEADERS+→DATA*→опц. trailer HEADERS с интерливом PUSH_PROMISE, всё прочее H3_FRAME_UNEXPECTED; переиспользует frame type-коды + H3_FRAME_UNEXPECTED; чистая state-machine без IO, +27 тестов); срез 13 ✅ 2026-07-04 — QUIC key schedule (RFC 9001 §5.1/§5.2: TLS 1.3 HKDF over SHA-256 — HMAC-SHA256 поверх имеющегося sha2 без новой зависимости, HKDF-Extract/Expand/Expand-Label с префиксом «tls13 »; INITIAL_SALT_V1 + Initial-secret chain из client DCID → client_initial_secret/server_initial_secret; PacketProtectionKeys{key/iv/hp/secret} = quic key/quic iv/quic hp, Debug редактирует ключевой материал; InitialKeys::derive обе стороны; next_generation_secret = quic ku key update §6.1; сверено с RFC 9001 Appendix A.1 векторами + RFC 4231/5869; +14 тестов); срез 14 ✅ 2026-07-04 — QUIC packet protection (RFC 9001 §5.3/§5.4, h3::packet_protect): AEAD payload protection AES-128-GCM (aes_128_gcm_seal/open — nonce = iv XOR packet_number правым выравниванием, unprotected header как AAD, ciphertext‖16-байт tag) + header protection (aes_128_hp_mask = AES-ECB(hp, 16-байт sample) → 5-байт маска, apply/remove_header_protection маскируют младшие биты первого байта [4 бита long / 5 бит short] и октеты packet number, sample всегда на pn_offset+4); AES-suite only (ChaCha20 §5.4.4 отложен); новые dep aes-gcm/aes (уже в воркспейсе у lumen-js/lumen-storage); чистые функции над буферами, сверено с RFC 9001 Appendix A.2/A.3 + McGrew–Viega GCM KAT; +16 тестов); срез 15 ✅ 2026-07-05 — TLS 1.3 key schedule (RFC 8446 §7.1, h3::tls_schedule): Transcript-Hash/Derive-Secret over SHA-256; no-PSK Early Secret HKDF-Extract(0,0); Handshake Secret = HKDF-Extract(Derive-Secret(Early,«derived»), (EC)DHE) → HandshakeTrafficSecrets{client «c hs traffic»/server «s hs traffic» по CH..SH hash}; Master Secret = HKDF-Extract(Derive-Secret(HS,«derived»),0) → ApplicationTrafficSecrets{«c/s ap traffic» по CH..SF hash} + exporter/resumption; каждый traffic secret → QUIC-ключи через PacketProtectionKeys::aes_128_gcm_from_secret (DirectionalKeys), key update через next_generation_secret; Debug редактирует секреты; переиспользует key_schedule HKDF (hkdf_extract стал pub(crate)), без новой зависимости; сверено с RFC 8448 §3 (early/derived/handshake secret/handshake traffic/master); 1-RTT/exporter векторы завязаны на CH..SF транскрипт-хеш (создаётся кодеком handshake-сообщений позже) — проверены структурно + по меткам; +18 тестов); срез 16 ✅ 2026-07-05 — TLS 1.3 handshake message codec (RFC 8446 §4, h3::tls_message): чистый parse/serialize сообщений TLS 1.3, которые QUIC несёт в CRYPTO-фреймах (RFC 9001 §4) — источник байт для транскрипт-хеша среза 15 и (EC)DHE-публичного значения; Handshake-обёртка (msg_type·uint24 length·body), parse→Ok(None) при недоборе (как frame::Frame); ClientHello/ServerHello(+HelloRetryRequest по magic random)/EncryptedExtensions/CertificateRequest/Certificate/CertificateVerify/Finished/NewSessionTicket/KeyUpdate/EndOfEarlyData + Unknown (сохранение неизвестных типов для транскрипта); расширения обобщённо Extension{type,data} (байт-точно), legacy version/compression валидируются к фиксированным значениям; типизированные тела моста QUIC/TLS — KeyShareEntry (X25519-публичное значение для tls_schedule::handshake_secret, разные кодеки CH-список/SH-один) + supported_versions (CH 1-байт-префикс / SH голый selected_version); TlsError{Malformed,Overflow}, без паник; без новой зависимости; байт-точный ре-энкод RFC 8448 §3 ServerHello + композиция CH‖SH в HandshakeTrafficSecrets::derive; +12 тестов); срез 17 ✅ 2026-07-05 — X25519 key agreement (RFC 7748, RFC 8446 §4.2.8, h3::key_agreement): Curve25519 ECDH через x25519-dalek — x25519_public_key (наш ephemeral для key_share), x25519_shared_secret/x25519_ecdhe_from_key_share (скалярное умножение над KeyShareEntry пира → (EC)DHE, отбраковка small-order/non-contributory ключа RFC 7748 §6.1 = zero secret), x25519_key_share (наш KeyShareEntry), generate_x25519_private_key (OS CSPRNG); детерминированное ядро сверено с RFC 7748 §5.2 (2 вектора) + §6.1 (Alice/Bob DH); e2e с срезом 15 (ecdhe → tls_schedule::handshake_secret обеими сторонами); новая dep x25519-dalek (Exception #3: crypto, своя реализация Curve25519 — антипаттерн); +10 тестов); срез 18 ✅ 2026-07-05 — TLS 1.3 Finished MAC (RFC 8446 §4.4.4, h3::tls_finished): handshake key-confirmation, которое QUIC несёт в CRYPTO-фреймах (RFC 9001 §4) — finished_key(BaseKey)=HKDF-Expand-Label(BaseKey,«finished»,«»,32) от handshake traffic secret отправителя (tls_schedule::HandshakeTrafficSecrets::{server,client}), finished_verify_data=HMAC(finished_key,Transcript-Hash(…)) для своего Finished, verify_finished — приёмная проверка с константновременным ct_eq (mismatch = фатальный decrypt_error); переиспользует HMAC среза 13 (key_schedule::hmac_sha256 стал pub(crate)) + HKDF-Expand-Label, без новой зависимости, без IO; сквозные векторы RFC 8448 §3 (server/client finished_key + server verify_data по реконструированному транскрипт-хешу CH…CertificateVerify); +13 тестов); срез 19 ✅ 2026-07-05 — TLS 1.3 CertificateVerify signature (RFC 8446 §4.4.3, h3::tls_cert_verify): peer-аутентификация хендшейка (в отличие от Finished-MAC среза 18, доказывает владение приватным ключом end-entity-сертификата), которую QUIC несёт в CRYPTO-фреймах (RFC 9001 §4) — certificate_verify_content строит подписываемый контент (64×0x20 ‖ role-context «TLS 1.3, server/client CertificateVerify» ‖ 0x00 ‖ transcript-hash, чтобы подпись TLS 1.3 нельзя было спутать по версии/роли), verify_certificate_verify проверяет DER-подпись под публичным ключом пира для названной SignatureScheme; провязан ecdsa_secp256r1_sha256 (0x0403, MTI по RFC 8446 §9.1) через уже имеющийся p256 (WebAuthn ES256) — без новой зависимости; ecdsa_p256_sha256_verify сверен с вектором RFC 6979 Appendix A.2.5 (P-256/SHA-256), e2e — детерминированная RustCrypto-подпись над реальным транскрипт-хешем + отбраковка чужой роли/ключа/изменённого транскрипта; остальные схемы (rsa_pss_rsae_sha256/ed25519/…) → UnsupportedScheme, извлечение SPKI из сертификата — на вызывающем (SEC1-точка на входе); чистые функции, без IO, +14 тестов); срез 20 ✅ 2026-07-05 — ed25519 CertificateVerify (RFC 8446 §4.2.3, RFC 8032, h3::tls_cert_verify::ed25519_verify): EdDSA над Curve25519 — подписывает signed content напрямую без prehash и несёт подпись сырой без DER (в отличие от ECDSA-схем); verify_certificate_verify теперь диспатчит ecdsa_secp256r1_sha256 + ed25519, публичный ключ = сырая 32-байтовая точка (RFC 8410 §4); верификатор из ed25519-dalek (Exception #3: crypto, переиспользует curve25519-dalek уже в дереве транзитивно через x25519-dalek, constant-time, verify-only); сверено с вектором RFC 8032 §7.1 TEST 2 (подпись детерминированно восстановлена из секретного ключа RFC + pin публичного ключа), +7 тестов); срез 21 ✅ 2026-07-05 — rsa_pss_rsae_sha256 CertificateVerify (RFC 8446 §4.2.3, RFC 8017 §8.1, h3::tls_cert_verify::rsa_pss_sha256_verify): RSASSA-PSS с MGF1-SHA-256 маской и SHA-256 хешем сообщения — подпись, которую несёт подавляющее большинство реальных серверных сертификатов; verify_certificate_verify теперь диспатчит ecdsa_secp256r1_sha256 + ed25519 + rsa_pss_rsae_sha256, публичный ключ = PKCS#1 DER RSAPublicKey (subjectPublicKey из rsaEncryption SPKI), подпись = сырое big-endian целое одной длины модуля; верификатор из rsa (Exception #3: crypto, pure-Rust RustCrypto, переиспользует sha2 в дереве, verify-only), salt длина восстанавливается при EMSA-PSS-VERIFY → RFC 8446 salt=digest-length подписи проверяются интероперабельно; e2e — подпись CertificateVerify-контента сгенерированным RSA-ключом + отбраковка роли/транскрипта/подписи/ключа; +3 теста); срез 22 ✅ 2026-07-05 — rsa_pss_rsae_sha384 + rsa_pss_rsae_sha512 CertificateVerify (RFC 8446 §4.2.3, RFC 8017 §8.1, h3::tls_cert_verify::rsa_pss_sha384_verify/rsa_pss_sha512_verify): SHA-384/512-сиблинги среза 21 — идентичны, отличаются лишь MGF1-маской и хешем сообщения (обычно подписывают 3072/4096-битные сертификаты); все три RSA-PSS-варианта сведены в один generic-верификатор rsa_pss_verify<D: Digest + FixedOutputReset>, переиспользует rsa + sha2 из дерева — без новой зависимости; verify_certificate_verify теперь диспатчит ecdsa_secp256r1_sha256 + ed25519 + rsa_pss_rsae_sha256/384/512, кодировка ключа/подписи та же, что у sha256-варианта; e2e — подпись CertificateVerify-контента сгенерированными RSA-ключами каждого digest + отбраковка cross-digest подписи (SHA-384 не проходит под SHA-512-схемой); +2 теста); срез 23 ✅ 2026-07-05 — ecdsa_secp384r1_sha384 + ecdsa_secp521r1_sha512 CertificateVerify (RFC 8446 §4.2.3, h3::tls_cert_verify::ecdsa_p384_sha384_verify/ecdsa_p521_sha512_verify): P-384/P-521-сиблинги ecdsa_secp256r1_sha256 среза 19 — та же форма (SEC1-точка + DER (r,s) + curve-default digest SHA-384/512, применяемый верификатором внутри), отличаются лишь кривой; verify_certificate_verify теперь диспатчит все три NIST-ECDSA + ed25519 + rsa_pss_rsae_sha256/384/512; верификаторы из p384/p521 (Exception #3: crypto, тот же RustCrypto ecdsa/signature v2, что и p256, pure-Rust, verify-only); e2e — RustCrypto-подпись CertificateVerify-контента каждой кривой + отбраковка роли/транскрипта/ключа (p256/p384 детерминированы; p521 подписывает через OsRng, публичный ключ берётся через VerifyingKey::from(&sk), т.к. inherent verifying_key закрыт незаданной feature); +5 тестов); срез 24 ✅ 2026-07-05 — QUIC datagram coalescing (RFC 9000 §12.2/§14.1, h3::datagram): чистый слой над packet — parse_datagram разбивает один UDP-датаграм на упорядоченную последовательность коалесцированных пакетов, encode_datagram собирает обратно; только length-delimited long-header (Initial/0-RTT/Handshake) может нести следующий пакет, поэтому short/Retry/Version Negotiation допустим лишь последним — на encode это enforce (DatagramError::UnterminatedCoalescing), на parse автоматически (эти формы потребляют хвост датаграма); Packet::is_length_delimited() как компаньон is_long_header(); initial_padding_shortfall сообщает недобор до MIN_INITIAL_DATAGRAM_LEN=1200 (клиент добивает PADDING-фреймами внутри Initial до шифрования); чистые функции без IO, +10 тестов); срез 25 ✅ 2026-07-05 — QUIC transport parameters codec (RFC 9000 §18/§7.4, h3::transport_params): чистый parse/serialize тела TLS-расширения quic_transport_parameters (RFC 9001 §8.2) — последовательность (id, length, value) поверх varint — в типизированный TransportParameters; именно эти значения конфигурируют state-machine ранних срезов (initial_max_data→conn_flow::SendConnFlow, initial_max_stream_data_*→stream::SendStream, initial_max_streams_*→conn_flow stream-limits, max_udp_payload_size→recovery datagram-size, ack_delay_exponent/max_ack_delay→loss/pto); валидация §18.2 (единичность id→DuplicateParameter, integer-varint должен заполнить Length→MalformedValue, диапазоны max_udp_payload_size≥1200/ack_delay_exponent≤20/max_ack_delay<2^14/active_connection_id_limit≥2→InvalidValue, фикс-длины stateless_reset_token/disable_active_migration/preferred_address cid); accessor-методы применяют дефолты §18.2 (65527/3/25/2); PreferredAddress типизирован; unknown/reserved (GREASE §18.1) сохраняются байт-точно для round-trip; TransportParameterError→TRANSPORT_PARAMETER_ERROR(0x08); без новой зависимости, без IO, +17 тестов); срез 26 ✅ 2026-07-05 — QUIC packet number encoding/decoding (RFC 9000 §17.1, Appendix A, h3::packet_number): truncation-кодек между packet-хедером (несёт packet number в opaque protected + 2-битную Packet Number Length в header-protected первом байте) и loss/recovery (оперируют полными 62-битными номерами); на отправку packet_number_length выбирает минимум b∈1..=4 c 2^(8b−1)≥num_unacked (A.2, largest_acked=None до первого ack = полный диапазон 0..=full_pn) + encode_packet_number пишет b младших big-endian байт + encode_pn_length_bits для поля длины; на приём decode_packet_number восстанавливает полный номер ближайший к largest_pn+1 (A.3) с guard'ами переполнения (записано candidate+hwin<=expected во избежание underflow), read_truncated_packet_number/pn_length_from_first_byte достают то, что хедер оставил opaque; MAX_PACKET_NUMBER=2^62−1, PacketNumberError::Unrepresentable при gap>2^31; чистые функции без IO, сверено с примерами RFC 9000 §17.1/A.2/A.3, +14 тестов); срез 27 ✅ 2026-07-05 — QUIC Path MTU Discovery (DPLPMTUD, RFC 9000 §14.2–14.4, RFC 8899, h3::path_mtu): чистая state-machine поиска максимального размера датаграммы, который бьёт границу congestion window в recovery и размер собираемых пакетов; PathMtuDiscovery подтверждает базовый QUIC_MIN_PLPMTU=1200 (§5.2; завершённый handshake это уже доказывает — with_confirmed_base стартует сразу в поиск), затем бинарный поиск вверх: next_probe предлагает размер PMTU-пробы, ack поднимает подтверждённый размер + пол поиска, потеря MAX_PROBES=3 раз подряд опускает search_high ниже размера (§5.3); on_black_hole сбрасывает к базе и рестартует поиск при пропадании обычных датаграмм текущего размера (§5.4); одна проба в полёте, PmtuState Base/Searching/SearchComplete/Error; чистая state-machine без IO и без сборки probe-пакета, потерянную пробу вызывающий не скармливает congestion controller (§14.4); +16 тестов); срез 28 ✅ 2026-07-05 — QUIC ACK generation (RFC 9000 §13.2/§19.3, h3::ack): receiver-side зеркало loss.rs — AckGenerator (по одному на PacketNumberSpace) фиксирует принятые packet numbers как непересекающиеся inclusive-диапазоны (слияние соседних, дедуп дубликатов), решает когда ACK нужен (immediate при reordering/gap-fill §13.2.1, при достижении ack-eliciting threshold §13.2.2 = 2, при ECN-CE, либо всегда в Initial/Handshake без задержки; иначе delayed до peer max_ack_delay — ack_urgency() отдаёт дедлайн под таймер) и строит Frame::Ack сверху вниз (largest_acked/first_ack_range + Gap/Length диапазоны §19.3.1, ACK Delay scaled по ack_delay_exponent, ECN-счётчики §19.3.2); on_ack_of_ack() ограничивает набор диапазонов после подтверждения нашего ACK (§13.2.4), сохраняя верхний диапазон; чистая state-machine без IO/таймеров/сборки пакетов; round-trip через quic_frame::Frame; +21 тест); срез 29 ✅ 2026-07-05 — QUIC packet protection pipeline (RFC 9001 §5.3/§5.4, h3::packet_crypt): единственное место, связывающее header-кодек packet + truncation-кодек packet_number + AEAD/header-protection packet_protect + ключи key_schedule в две сквозные операции connection-слоя над каждым 1-RTT-несущим пакетом; encrypt_packet собирает хедер через packet::encode (Length/in-clear поля точны), кладёт clear packet number, AEAD-запечатывает payload с unprotected header как AAD и применяет header protection → байты для датаграммы; decrypt_packet — инверсия: parse хедера, remove_header_protection на рабочей копии (buf не мутируется), decode_packet_number ближайший к largest_pn+1 (A.3), AEAD-open, DecryptedPacket{header,packet_number,payload,consumed} с consumed для обхода коалесцированной датаграммы; ProtectedHeader{Initial+token/ZeroRtt/Handshake/Short+spin+key_phase}, encrypt_packet_with_pn_length для фиксированной ширины; Retry/Version Negotiation → NotProtected; AES-suite only; чистые функции без IO, round-trip + байт-точный in-clear header сверены с RFC 9001 Appendix A.2 client Initial (Length=0x449e), +11 тестов); срез 30 ✅ 2026-07-05 — QPACK field-section dynamic-table wiring (RFC 9204 §4.5.1–§4.5.5, h3::qpack): расширил field-section-кодек (был static-only) второй половиной — ссылками в разделяемую dynamic table, которую принимает как &qpack_stream::DynamicTable (её строит из encoder-стрима пира DynamicTable::apply, срез 6); encode_field_section_dynamic выбирает на поле дешёвейшую форму (dynamic/static Indexed §4.5.2, dynamic/static Literal-with-name-ref §4.5.4, Literal-with-literal-name §4.5.6), ставит Required Insert Count = (макс. absolute index ссылки)+1 и Base = ему же (все ссылки pre-Base, Delta Base 0), сохраняя sensitive/never-index-поведение; decode_field_section_dynamic парсит RIC/Base-префикс (§4.5.1.1 wrap-реконструкция от insert_count()/max_entries(), §4.5.1.2 знаковый Base) и разрешает все 5 форм, включая Post-Base Indexed §4.5.3 и Post-Base Name Ref §4.5.5 (Base−Index−1 / Base+Index); +DynamicTable::{max_entries, find_absolute, find_name_absolute}; +QpackError::{InvalidRequiredInsertCount, InvalidDynamicReference} → QPACK_DECOMPRESSION_FAILED; заблокированный/вне-диапазона RIC и ссылка на вытесненную запись отбраковываются; чистые функции без IO, без новой зависимости, +10 тестов); срез 31 ✅ 2026-07-05 — QUIC CRYPTO stream (RFC 9000 §7.5/§19.6, h3::crypto_stream): per-encryption-level реассемблинг TLS-handshake байтового потока из CRYPTO-фреймов (quic_frame::Frame::Crypto, RFC 9001 §4) — мост между декодированными транспортными фреймами и tls_message; в отличие от stream нет stream-id, flow-control и FIN; CryptoRecvStream сливает out-of-order/перекрывающиеся/дублирующие CRYPTO-фреймы в непрерывный handshake-префикс (предпочтение уже принятых байт §7.5), enforce реассембли-границы read_offset+buffer_limit → CryptoStreamError::BufferExceeded (CRYPTO_BUFFER_EXCEEDED=0x0d §7.5/§20.1, дефолт 64 KiB); CryptoSendStream буферизует TLS-вывод, отдаёт CRYPTO-фреймы bounded size-cap'ом, трекает acked-диапазоны (is_fully_acked); recv_frame/into_frame провязка с quic_frame; чистая state-machine без ретрансмиссии/IO, +15 тестов); срез 32 ✅ 2026-07-05 — QUIC connection-ID management (RFC 9000 §5.1, h3::conn_id): две разделённые state-machine поверх NEW_CONNECTION_ID (§19.15) / RETIRE_CONNECTION_ID (§19.16) фреймов, которые quic_frame уже кодирует. RemoteConnIds = ID пира, которыми мы штампуем исходящие пакеты: seed'ится handshake-SCID пира (sequence 0), record_new_connection_id вбирает каждый NEW_CONNECTION_ID — отбраковка Retire Prior To > sequence или CID вне 1..=20 (ConnIdError::Malformed → FRAME_ENCODING_ERROR), переиспользование sequence под другой ID/token (SequenceConflict → PROTOCOL_VIOLATION, ловится даже после ретайра через seen-карту), переполнение active-набора сверх нашего active_connection_id_limit (LimitExceeded → CONNECTION_ID_LIMIT_ERROR); honor Retire Prior To (ретайрит все ниже + сам ID, если его sequence < порога), возвращает sequence-номера под RETIRE_CONNECTION_ID, мигрирует current на низший active при ретайре; is_stateless_reset (§10.3.1) + switch_to для добровольной миграции. LocalConnIds = ID, которые мы выдаём пиру: монотонные sequence, issue отказывает сверх peer_limit, retire дропает ID (ретайр невыданного = PROTOCOL_VIOLATION), accepts для роутинга входящего DCID; чистые state-machine без IO и без packet-level Destination-CID контекста, +23 теста); срез 33 ✅ 2026-07-05 — QUIC Retry packet integrity + token handling (RFC 9000 §17.2.5/§8.1, RFC 9001 §5.8, h3::retry): чистая крипта + состояние клиента на принятый Retry-пакет (packet::Packet::Retry). retry_integrity_tag/verify_retry_integrity считают и проверяют Retry Integrity Tag — AEAD_AES_128_GCM над пустым plaintext с version-fixed RETRY_KEY_V1/RETRY_NONCE_V1 (переиспользует packet_protect::aes_128_gcm_seal с pn=0 ⇒ nonce=iv напрямую), аутентифицируя Retry Pseudo-Packet: ODCID первого клиентского Initial (length-prefixed одним октетом) + весь Retry-пакет до тега; ODCID никогда не на проводе ⇒ off-path атакующий не подделает Retry; RetryHandler — клиентская state-machine: верифицирует тег, enforce at-most-one-Retry-per-connection (§17.2.5, DuplicateRetry), возвращает RetryOutcome (new_dcid = Retry SCID + token для эха); ct_eq для сравнения тега; сверено с вектором RFC 9001 Appendix A.4; без новой зависимости, без IO, +14 тестов); срез 34 ✅ 2026-07-05 — QUIC packet payload assembly (RFC 9000 §12.4/§13.2.1/§14.1, RFC 9002 §2, h3::packet_payload): слой между quic_frame и packet_crypt, решающий какие фреймы кладутся в пакет и упаковывающий их под байтовый бюджет. PacketType (Initial/0-RTT/Handshake/1-RTT) отвечает на таблицу разрешений §12.4 через permits() — тоньше трёх loss::PacketNumberSpace, т.к. 0-RTT и 1-RTT делят Application Data space, но допускают разные фреймы (ACK/CRYPTO не в 0-RTT; NEW_TOKEN/PATH_RESPONSE/HANDSHAKE_DONE только 1-RTT; stream/flow-control/conn-mgmt только 0-RTT/1-RTT; transport CONNECTION_CLOSE 0x1c везде, application 0x1d не в Initial/Handshake); number_space() мэппит тип в пространство. PayloadBuilder аккумулирует разрешённые фреймы до limit — try_push отбраковывает не-помещающийся фрейм без мутации payload (Ok(false)) и запрещённый в типе как PayloadError::FrameNotPermitted, трекает ack-eliciting (§13.2.1) и in-flight (RFC 9002 §2 = ack-eliciting ИЛИ PADDING), pad_to добивает PADDING до целевого размера (1200-байт client Initial §14.1 / PMTU-проба path_mtu) не превышая limit; чистое состояние над буферами, без назначения packet number/хедера/шифрования, +17 тестов); срез 35 ✅ 2026-07-05 — QUIC connection lifecycle (RFC 9000 §10, h3::lifecycle): чистая state-machine переходов Active/Closing/Draining/Closed. ConnectionLifecycle трекает idle-timeout дедлайн (§10.1 — effective_idle_timeout = min ненулевых max_idle_timeout обоих пиров либо единственное ненулевое, иначе disabled; рестарт таймера при получении пакета либо ПЕРВОМ ack-eliciting отправленном с момента приёма §10.1; пол 3·PTO), immediate close с отправкой CONNECTION_CLOSE и входом в Closing (§10.2.1, close() выдаёт Frame один раз, on_packet_while_closing ресендит с экспоненциальным rate-limit — порог удваивается), Draining при приёме CONNECTION_CLOSE пира (§10.2.2, may_send=false, ничего не шлёт), период Closing/Draining 3·PTO (§10.2) после которого discard→Closed; now и PTO снаружи (recovery::RttEstimator::pto), без timer-IO и удержания ключей, +19 тестов); срез 36 ✅ 2026-07-05 — QUIC path validation + anti-amplification limit (RFC 9000 §8.1/§8.2, h3::path_validation): две чистые state-machine, охраняющие непроверенный адрес пира. AntiAmplificationLimit (§8.1) ограничивает отправку тремя объёмами принятого (send_allowance = 3·received − sent, saturating; can_send/is_blocked), снимая лимит по mark_validated (успешная валидация пути / обработанный Handshake-пакет / токен Retry/NEW_TOKEN). PathValidator — sender-side: send_challenge кладёт 8 непредсказуемых байт (подаются извне, как clock — модуль не генерит рандом) в PATH_CHALLENGE и (пере)арминг дедлайна now+3·PTO (§8.2.4), on_path_response валидирует путь при совпадении с ЛЮБЫМ outstanding-челленджем на любом пути (§8.2.3), on_timeout бракует по дедлайну (Failed); respond_to_challenge — receiver-side эхо PATH_RESPONSE (§8.2.2); MIN_PROBE_DATAGRAM_LEN=1200 (§8.2.1/§14.1). Чистые state-machine, now/PTO/challenge-байты снаружи, без рандома/timer-IO/сборки датаграмм; миграция соединения §9 отложена, +21 тест); срез 37 ✅ 2026-07-05 — QUIC connection migration (RFC 9000 §9, h3::path_migration): оркестрация переживания смены пути, связывающая примитивы среза 36 (path_validation) с conn_id и recovery. is_probing_frame/is_probing_packet классифицируют probing vs non-probing пакеты §9.1 (probing = только PATH_CHALLENGE/PATH_RESPONSE/NEW_CONNECTION_ID/PADDING; пустой набор = non-probing). ConnectionMigration гейтит миграцию на confirmed handshake §9 (MigrationError::HandshakeNotConfirmed) и «одна за раз» (AlreadyMigrating); initiate стартует PathValidator на новом пути + запоминает свежий peer-CID sequence для штамповки §9.5 + заводит AntiAmplificationLimit на новый путь лишь если его peer-адрес непроверен §9.3; send_challenge ретрансмитит свежий челлендж; on_received/on_sent/send_allowance/can_send/is_blocked проксируют лимит нового пути; on_path_response завершает миграцию по совпавшему PATH_RESPONSE, возвращая MigrationOutcome{active_cid_seq, reset_congestion_and_rtt} (§9.4 — reset кроме port-only NAT-rebind) и откатывая в Stable на новом пути; on_timeout бракует протухшую валидацию обратно на старый путь §8.2.4; NewPath-дескриптор {peer_address_validated, port_only} с local_migration()/peer_rebinding(). Ретайр CID (conn_id::switch_to) и reset congestion/RTT — на вызывающем; одновременный multi-path вне scope. Чистая state-machine, now/PTO/challenge-байты снаружи, без рандома/IO/таймеров, +23 теста); срез 38 ✅ 2026-07-05 — QUIC client-side version negotiation (RFC 9000 §6.2, h3::version_nego): чистая клиентская state-machine на принятый Version Negotiation-пакет (packet::Packet::VersionNegotiation), зеркаля retry (срез 33). VersionNegotiator хранит упорядоченные по предпочтению версии клиента + версию первого Initial; process валидирует эхо-CID (§6.1: DCID пакета = наш SCID, SCID пакета = наш DCID → ConnectionIdMismatch) и enforce §6.2: обрабатывается не более одного и только до любого другого пакета (AlreadyProcessed на повтор), пакет со списком, содержащим уже испробованную версию, отбраковывается как поддельный/ошибочный (DowngradeDetected), выбор самой предпочтительной взаимно поддерживаемой версии (предпочтение клиента важнее серверного) с отказом при пустом пересечении (NoCompatibleVersion → abandon); возвращает VersionNegotiationOutcome{selected_version} для рестарта хендшейка; QUIC_VERSION_1/VERSION_NEGOTIATION_VERSION константы; чистая state-machine без IO, downgrade-защита §6.3 (эхо в transport params) отдельно, +12 тестов); срез 39 ✅ 2026-07-05 — клиентский TLS 1.3 handshake-flow (RFC 8446 §4, RFC 9001 §4, h3::tls_handshake): чистая state-machine, секвенирующая ранние TLS-примитивы (tls_message/key_agreement/tls_schedule/tls_finished, реассембл crypto_stream) в упорядоченный клиентский хендшейк. ClientHandshake сидится клиентским эфемерным X25519-приватником + байтами ClientHello (сид транскрипта), затем принимает серверный flight по одному сообщению (handle_message): ServerHello → EncryptedExtensions → опц. CertificateRequest → Certificate → CertificateVerify → Finished. Форсит порядок RFC 8446 §4, на ServerHello гоняет (EC)DHE и отдаёт Handshake-level packet-ключи (HandshakeKeysReady), на server Finished верифицирует MAC над CH…CertificateVerify (§4.4.4), затем деривит 1-RTT-ключи + master/exporter/resumption секреты (CH…SF / CH…CF) и эмитит клиентский Finished. Фикс TLS_AES_128_GCM_SHA256 + X25519; валидация цепочки сертификата — на вызывающем (сырые Certificate/CertificateVerify отдаются в HandshakeComplete для tls_cert_verify); HelloRetryRequest/PSK/client-auth/post-handshake вне scope; секреты редактируются в Debug; чистая state-machine без IO, +12 тестов) (crates/network/src/h3/, 614 тестов); срез 40 ✅ 2026-07-05 — HTTP/3 request/response message translation (RFC 9114 §4.1–§4.3, h3::h3_request): семантический мост между HTTP-сообщением и QPACK field-section в HEADERS-фрейме — request-путь-компаньон qpack (field-list→байты) и frame (opaque field-block→фрейм), зеркалит h2::conn. build_request_fields/encode_request строят упорядоченный request field-list — четыре псевдозаголовка (:method/:scheme/:authority/:path §4.3.1) в fingerprint-порядке импертонируемого браузера (H3Profile Chrome/Firefox/Safari) + обычные поля, валидируя каждый §4.2 (lower-case, непустой, не connection-specific, te только trailers) — и QPACK-кодируют список в HEADERS-фрейм; validate_response_fields/decode_response разбирают response field-block в H3ResponseHead{status:u16,headers} с форсом malformed-правил §4.1.2/§4.2/§4.3.2 (ровно один :status 100–599 первым, никаких request/unknown псевдо в ответе, никаких псевдо после обычного поля, lower-case имена, нет connection-specific); MessageError→H3_MESSAGE_ERROR на транспорте; чистые функции над static-only QPACK, без IO/новых dep, +17 тестов, h3/ → 631 тест); срез 41 ✅ 2026-07-05 — QPACK encoder driver (RFC 9204 §2.1, h3::qpack_encoder): request-path policy-слой над QPACK-кодеками — QpackEncoder держит зеркало динамической таблицы декодера, вставляет выгодные поля (эмитит encoder-stream инструкции qpack_stream — «врезка instruction stream в request-путь») и кодирует field-section через новую qpack::encode_field_section_dynamic_bounded, НЕ ссылаясь на неподтверждённую запись если SETTINGS_QPACK_BLOCKED_STREAMS не разрешает blocked-stream (§2.1.2); учёт outstanding-секций защищает референсимые записи от вытеснения до ack (§2.1.3, eviction_safe), Known Received Count двигается по Section Acknowledgment/Insert Count Increment, ссылки освобождаются по Stream Cancellation (§2.1.4, §4.4); +2 error-варианта QpackStreamError (UnexpectedSectionAck/InsertCountIncrementOverflow → QPACK_DECODER_STREAM_ERROR); чистый state-machine без IO, +13 тестов, h3/ → 644 теста); срез 42 ✅ 2026-07-05 — QUIC outgoing datagram assembly (RFC 9000 §12.2/§14.1, h3::datagram_build): send-side зеркало datagram — DatagramBuilder коалесцирует зашифрованные байтовые строки пакетов из packet_crypt::encrypt_packet в один исходящий UDP-датаграм под бюджетом confirmed path-MTU (path_mtu), enforce §12.2 на send-пути (следовать может только length-delimited long-header пакет; short-header пакет запечатывает датаграм → is_sealed, дубль-push = DatagramBuildError::Sealed) — mirror UnterminatedCoalescing приёмного encode_datagram; переполняющий пакет отвергается без мутации (Ok(false) → вызывающий флашит и ретраит тем же packet number в свежем датаграме), initial_padding_shortfall сообщает недобор до MIN_INITIAL_DATAGRAM_LEN (§14.1), который вызывающий добивает PADDING-фреймами (0x00, §19.1) внутри payload ДО шифрования 1:1 (у реального client Initial Length уже 2-байтный varint); это и есть «сборка probe-датаграмм» плана — PTO-проба = датаграм отсюда; чистый state-machine без IO, +14 тестов, h3/ → 658 тестов); срез 43 ✅ 2026-07-05 — QUIC connection timer scheduler (RFC 9000 §8.2.4/§10.1/§13.2.1, RFC 9002 §6.2, h3::timer): чистый мультиплексор, сводящий индивидуальные дедлайны всех ранних срезов (loss/PTO среза 9 pto::LossTimer, per-space delayed-ACK среза 28 ack::AckUrgency, idle-timeout + closing/draining среза 35 lifecycle, path-validation среза 36 path_validation::deadline) в единственный OS-таймер QUIC event loop. ConnectionTimers::next → самый ранний дедлайн + владелец TimerKind (значение под OS-таймер, тай-брейк по приоритету LossDetection<per-space ACK<PathValidation<IdleTimeout<DrainingClose), fired(now) → все истёкшие таймеры earliest-first для диспатча владельцам; set_ack_delay арминг только на Delayed (Immediate = «слать сейчас», не таймер); дедлайн ровно в now = истёк; без cross-timer политики (соединение гасит неактуальный дедлайн через None), чистый state-machine, now снаружи, без IO/таймер-объектов, +15 тестов, h3/ → 673 теста); срез 44 ✅ 2026-07-05 — QUIC 1-RTT key update state-machine (RFC 9001 §6, h3::crypto_state): OneRttKeyState владеет ротацией 1-RTT-ключей за время жизни соединения (в отличие от Initial/Handshake-уровней, чьи ключи живут только на хендшейк) — периодически двигает traffic secret каждого направления на следующее поколение (key_schedule::next_generation_secret, «quic ku») чтобы ограничить число пакетов под одним ключом (§6.6), единственный Key Phase бит (RFC 9000 §17.3.1) сигналит текущее поколение; держит наши send-ключи + пировские recv-ключи текущего поколения, предвычисляет следующее recv-поколение для trial-decrypt пакета с флипнутой фазой (RecvKeyDecision::Next), удерживает предыдущее recv-поколение на окно для пакетов, переупорядоченных через апдейт по границе packet number (§6.3, RecvKeyDecision::Previous); §6.1 гейты инициации (handshake_confirmed; не второй апдейт пока первый не подтверждён ack — KeyUpdateError) + §6.2 логика ответчика (детект пир-инициированного апдейта по различию Key Phase → двигаем свои send-ключи в ответ, если не инициировали сами); hp-ключ НЕ ротируется апдейтом (§6.1); AEAD confidentiality limit 2^23 (§6.6); чистая state-machine без clock/IO — дедлайн ретайра у timer, discard Initial/Handshake-ключей (§4.9) у драйвера, без новой зависимости, +20 тестов, h3/ → 693 теста); срез 45 ✅ 2026-07-05 — HTTP/3 connection SETTINGS (RFC 9114 §7.2.4, RFC 9204 §5, RFC 9220 §3, h3::settings): типизированный мост между сырыми (id, value)-парами frame-кодека SETTINGS и слоями-потребителями — qpack_encoder читает объявленную пиром QPACK-ёмкость динамической таблицы + blocked-streams бюджет, h3_request чтит max_field_section_size. H3Settings::for_profile строит локальные SETTINGS для control stream в fingerprint-порядке импертонируемого браузера (зеркалит http::h2_settings; Chrome/Edge 64 KiB таблица + 100 blocked, Firefox 64 KiB + 20, Safari консервативно 4 KiB); to_pairs опускает дефолтные значения (реальный браузер не шлёт настройки по умолчанию); from_pairs парсит SETTINGS пира в типизированные значения, применяя RFC-дефолт к каждому отсутствующему id, ПЕРЕ-энфорся reserved-HTTP/2-id (0x00/0x02–0x05) и duplicate-id правила §7.2.4.1 (типослой самодостаточен вне frame-кодека), валидируя диапазон SETTINGS_ENABLE_CONNECT_PROTOCOL (0/1, RFC 9220 §3) и игнорируя unknown/greased id §7.2.4.2; max_field_section_size=Option (None=без лимита, отличимо от объявленного 0); H3SettingsError→H3_SETTINGS_ERROR; чистый codec без IO, +18 тестов, h3/ → 711 тестов). срез 46 ✅ 2026-07-05 — QUIC datagram transport (RFC 9000 §5/§12.2, h3::udp): первый срез, касающийся OS-сокета. DatagramTransport — message-oriented send/recv-шов, над которым дженерик connection-слой (QUIC-аналог `Read + Write`-байтстрима h2::conn::H2Conn): реальный UdpDatagram (connected std::net::UdpSocket, send отвергает short-write, set_read_timeout маппит Duration::ZERO в non-blocking) в проде и MockDatagramTransport (scripted inbound + captured outbound, пустой recv → WouldBlock) в тестах; read-timeout позволяет одному блокирующему recv служить timer-ожиданием event loop — recv_timeout конвертирует следующий Instant-дедлайн ConnectionTimers в длительность блокировки, recv_timed_out распознаёт портируемый сигнал WouldBlock/TimedOut «дедлайн истёк, пали таймеры»; MAX_DATAGRAM_SIZE=65527; реассембли/ретрансмиссия/порядок остаются в connection-слое, транспорт лишь двигает opaque-датаграммы; +17 тестов, h3/ → 728 тестов. срез 47 ✅ 2026-07-05 — QUIC event-loop wait (RFC 9000 §10.1/§13.2.1, RFC 9002 §6.2, h3::event_loop): склейка timer::ConnectionTimers (срез 43) с udp::DatagramTransport read-timeout (срез 46) — единственное место, превращающее «следующий QUIC-дедлайн» в «блокируй сокет ровно на столько, потом скажи почему проснулся»; срез итерации wait, не всего цикла. DatagramEventLoop<T> владеет транспортом + переиспользуемый MAX_DATAGRAM_SIZE recv-буфер (нет аллокации на такт); wait(&ConnectionTimers, now) арминг read-timeout из next_read_timeout (ConnectionTimers::next + udp::recv_timeout) → recv, отдаёт Wakeup::Datagram(n) при приёме либо Wakeup::TimerExpired на портируемом read-timeout сигнале (recv_timed_out), прочие io-ошибки пробрасывает; datagram(n) отдаёт первые n байт буфера, next_read_timeout — свободный helper (нет таймера→None=блок навсегда, будущий ближайший дедлайн→остаток, уже наступивший→Duration::ZERO=poll без блокировки); финальный ConnectionTimers::fired остаётся у вызывающего (модуль без часов, детерминируется MockDatagramTransport); +14 тестов, h3/ → 742 теста. срез 48 ✅ 2026-07-05 — QUIC connection-level receive dispatch (RFC 9000 §12.4/§13/§19, h3::connection): первый composition-срез движка — единственное место, владеющее connection-wide state-machine и роутящее каждый расшифрованный фрейм в владеющую машину. QuicConnection держит per-space ack-генераторы (ack) + crypto-реассембли (crypto_stream), send-side conn_flow-лимиты, conn_id-наборы (RemoteConnIds/LocalConnIds), path_validation-валидатор + anti-amplification, lifecycle — драйвит их по одному расшифрованному пакету. process_packet фиксирует packet number для ACK нужного пространства, затем диспатчит каждый фрейм: MAX_DATA/MAX_STREAMS поднимают наши send-лимиты, NEW_CONNECTION_ID/RETIRE_CONNECTION_ID двигают ID-наборы (возвращая seq на ретайр), PATH_CHALLENGE эхо-ится + PATH_RESPONSE валидирует путь (снимая anti-amplification), CRYPTO реассемблится per-space, CONNECTION_CLOSE/HANDSHAKE_DONE двигают lifecycle; ACK (loss detection), per-stream фреймы (stream manager) и NEW_TOKEN уходят в PacketEffects::deferred для следующего среза; refresh_timers сворачивает дедлайны машин в ConnectionTimers для event_loop; ProcessError оборачивает conn_flow/conn_id/crypto ошибки с общим code(); чистый state-machine, now снаружи, дешифровка/send-путь у вызывающего, +18 тестов, h3/ → 760 тестов; срез 49 ✅ 2026-07-05 — QUIC stream manager (RFC 9000 §2/§3/§4/§19, h3::stream_manager): receive-side диспетчер per-stream фреймов, отложенных срезом 48 в PacketEffects::deferred. StreamManager владеет обеими половинами каждого живого потока (stream::RecvStream/SendStream), connection-wide receive-flow-бюджетом (conn_flow::RecvConnFlow) и двумя receive-лимитами числа потоков (conn_flow::RecvStreamLimit) и роутит: STREAM/RESET_STREAM в приёмную половину (лениво создавая RecvStream с receive-окном по типу потока — initial_max_stream_data_bidi_local/_remote/_uni §18.2 — с enforce receive-stream-count лимита для peer-initiated потока §4.6 и общего receive-бюджета по всем потокам §4.1, без двойного учёта ретрансмита через highwater-карту recv_counted), STOP_SENDING в передающую половину (reset + отдача RESET_STREAM с final_size = фактически отправленный офсет §3.5/§4.5), MAX_STREAM_DATA в per-stream send-лимит §19.10, STREAM_DATA_BLOCKED как принятый no-op §19.13; направленность потока (§2.1) гейтит какую половину трогает фрейм — иначе StreamManagerError::StreamState → STREAM_STATE_ERROR (0x05); read() кормит record_read для re-advertise connection-окна; open_send_stream регистрирует send-половину под будущий send-путь; чистый state-machine без IO, +19 тестов, h3/ → 779 тестов. срез 50 ✅ 2026-07-05 — QUIC send-side frame scheduler (RFC 9000 §12.4/§13.2.1, RFC 9002 §2, h3::send): первый composition-срез send-пути, зеркало connection (срез 48). Где process_packet роутит принятый фрейм в владеющую машину, SendScheduler ставит в очередь фреймы, которые соединение ДОЛЖНО отправить (ACK от ack-генератора, PATH_RESPONSE/RETIRE_CONNECTION_ID из PacketEffects, CRYPTO/STREAM из send-стримов) и упаковывает их по убыванию SendPriority (Close→Ack→Crypto→Control→Stream→Probe) под байтовый бюджет в последовательные PayloadBuilder-пейлоады. Валидирует каждый фрейм по таблице разрешений типа пакета (§12.4) при enqueue, FIFO внутри приоритета (пропуская фрейм, не влезший в остаток бюджета, чтобы меньший следующий того же класса всё же упаковался), SendError::FrameTooLarge когда фрейм не влезает даже в пустой пакет. Чистое состояние без IO/крипто. +16 тестов, h3/ → 795 тестов. срез 51 ✅ 2026-07-05 — QUIC send engine (RFC 9000 §12.2/§14.1/§17.1, RFC 9002 §2, h3::send_engine): чистый композиционный срез send-пути — превращает фреймы из SendScheduler в зашифрованные, склеенные UDP-датаграммы и записи loss::SentPacket для loss recovery, зеркало приёмного connection. SpaceSender владеет монотонным счётчиком packet number одного пространства (§12.3): build_packet упаковывает фреймы в один payload, назначает номер, шифрует через packet_crypt::encrypt_packet; fill_datagram сливает всё пространство в DatagramBuilder, склеивая столько пакетов, сколько влезает (long-header Initial/Handshake коалесцируются, short-header 1-RTT запечатывает датаграмму §12.2 → не более одного пакета на датаграмму), остаток фреймов остаётся в очереди для следующей датаграммы; build_padded_initial — первый Initial клиента, добит паддингом до порога MIN_INITIAL_DATAGRAM_LEN §14.1. max_packet_overhead — верхняя граница накладных байт пакета, по которой fill_datagram бюджетирует payload, чтобы коалесценция не переполнялась. Чистое состояние, now снаружи, без сокета. +16 тестов, h3/ → 811 тестов. срез 52 ✅ 2026-07-06 — QUIC send-path datagram flush (RFC 9000 §12.2/§12.4/§14.1, RFC 9002 §2/§6, h3::send_path): композиционный срез send-пути, зеркало приёмного connection. flush() сворачивает pending-фреймы каждого пространства через SpaceSender::fill_datagram в один DatagramBuilder (long-header Initial/Handshake коалесцируются, первый short-header 1-RTT запечатывает датаграм §12.2), пишет коалесцированные байты в udp::DatagramTransport и записывает каждый loss::SentPacket в SentPacketRegistry (loss detection/PTO) + CongestionController::on_packet_sent (bytes_in_flight §7) для in-flight пакетов; цикл по датаграмам пока в каком-либо пространстве есть pending (пространство больше одной датаграмы дренится по нескольким). SpaceFlush<'a> — заимствованный per-space бандл; порядок пространств (Initial, Handshake, Application-Data) задаёт вызывающий. send_padded_initial() — first-flight клиентский Initial через build_padded_initial, добитый до MIN_INITIAL_DATAGRAM_LEN §14.1 и отправленный один. FlushError::DatagramTooSmall вместо бесконечного цикла/тихого дропа при бюджете меньше overhead; чистое состояние кроме мокабельной transport-записи, +15 тестов, h3/ → 826 тестов. срез 53 ✅ 2026-07-06 — QUIC receive-path datagram ingest (RFC 9000 §8.1/§10.1/§12.2/§12.4, RFC 9001 §5.5, h3::recv_path): приёмное зеркало send_path::flush. ingest_datagram() кредитует байты датаграма в anti-amplification + рестартит idle-таймер (on_datagram_received), затем идёт по коалесцированным пакетам спереди назад (§12.2): пикает хедер (Packet::parse) → выбирает SpaceKeys из RecvKeyRing по пространству пакета, decrypt_packet на рабочей копии, parse_all payload → фреймы, permits-проверка по типу пакета (§12.4) и диспатч через QuicConnection::process_packet, сливая PacketEffects всех пакетов в один IngestReport. Только контент аутентифицированного пакета поднимает IngestError (malformed frame → FRAME_ENCODING_ERROR / barred frame → PROTOCOL_VIOLATION / conn-level violation → ProcessError::code); пакет без установленных ключей = undecryptable (вызывающий буферизует и повторит), AEAD-фейл или битый коалесц-хедер = тихий дроп (RFC 9001 §5.5.2), оба лишь считаются в IngestReport; largest_pn пространства двигается по успешной расшифровке для реконструкции truncated-номера. Чистый state кроме caller-supplied now, +12 тестов, h3/ → 838 тестов. срез 54 ✅ 2026-07-06 — QUIC connection driver (RFC 9000 §8.2.4/§10.1/§10.2/§13.2.1, RFC 9002 §6.2, h3::driver): тело цикла, решающее КОГДА принимать датаграм и КОГДА действовать по таймеру — связывает recv_path с timer-планировщиком поверх event_loop wait. ConnectionDriver владеет event loop + QuicConnection + LossDetection (её per-space registries + PTO-таймер) + унифицированным ConnectionTimers + RecvKeyRing. wait() рефрешит ОБА источника таймеров — собственные дедлайны соединения (refresh_timers: idle/closing-draining/path/per-space ACK) плюс loss/PTO-таймер, который соединение НЕ трогает (LossDetection::set_loss_detection_timer) — арминг read-timeout сокета на ближайший, блок; на Datagram-wake ingest() гонит датаграм через recv_path, на TimerExpired dispatch_timers() гонит каждый истёкший таймер в свою машину (PTO probe / declared loss, owed ACK, path abandon, idle close, draining discard) и репортит send-side обязательства как DriverAction. Добавлены driving-методы QuicConnection: is_idle_expired/on_path_validation_timeout/on_close_timeout. Clock-free кроме caller-supplied now, детерминируемо под MockDatagramTransport. +13 тестов, h3/ → 851 тест. Остаётся: send_path флаш-интеграция (сборка/запись исходящих датаграм по DriverAction, заимствует per-space send-состояние) и h3_do_request dispatch. срез 55 ✅ 2026-07-06 — QUIC send-side connection state (RFC 9000 §12.3/§14.1, RFC 9002 §7, h3::send_state): владелец send-стороны, которую driver (срез 54) оставил caller'у. ConnectionSendState держит per-space send-состояние (SpaceSender-счётчик packet number, SendScheduler-очередь фреймов, send-PacketProtectionKeys, per-space CongestionController) + общие поля заголовка (version, Destination/Source Connection ID, Initial-токен). Пространство появляется по install (деривация ключей уровня шифрования) и исчезает по discard (RFC 9001 §4.9). flush() заимствует per-space registries у LossDetection (registries_mut) и сворачивает установленные пространства через send_path::flush в порядке Initial→Handshake→AppData (long-header коалесцируются, 1-RTT запечатывает датаграм §12.2); send_padded_initial — first-flight клиента с паддингом до MIN_INITIAL_DATAGRAM_LEN §14.1. Congestion пока per-space (совпадает со швом SpaceFlush); унификация в один per-path контроллер §B.2 — отложена. Чистый кроме мокабельной записи в транспорт. +16 тестов, h3/ → 867 тестов. срез 56 ✅ 2026-07-06 — QUIC connection turn integrator (RFC 9000 §12.2/§13.2.1, RFC 9002 §6.2.4/§7, h3::conn_turn): шов, соединяющий приёмный driver (срез 54) с send-side состоянием (срез 55) — две половины, которые нижние срезы намеренно оставили порознь. ConnectionTurn владеет обоими и переводит каждый DriverAction в ровно одну send-операцию: SendProbe → enqueue count PING-фреймов в пространство (§6.2.4, ack-eliciting probe против дедлока loss-таймера), PacketsLost → CongestionController::on_packets_lost по байтам потерянных пакетов (§7.6; ретрансмит содержимого фреймов — отдельный слой, sent-packet registry хранит метаданные пакета, не фреймы), SendAck → QuicConnection::generate_ack → enqueue (или AckNotOwed, если ACK уже уехал), PathAbandoned/IdleTimeout/Drained → терминальные TurnEffect (driver уже применил их к receiver-состоянию). dispatch_and_apply() гонит истёкшие таймеры и применяет полученные действия одним вызовом; flush()/send_padded_initial() делегируют в send_state, беря loss-registries и transport у driver одним заимствованием (новый ConnectionDriver::send_flush_parts_mut). TurnError { SpaceNotInstalled, Enqueue } когда probe/ACK целит в пространство без установленных ключей. Чистый кроме мокабельной записи в транспорт, now снаружи. +13 тестов, h3/ → 880 тестов. срез 57 ✅ 2026-07-06 — QUIC handshake driving-loop (RFC 9000 §7/§10.1/§10.2/§13.2.1, h3::conn_handshake): HandshakeDriver владеет ConnectionTurn (срез 56) и гоняет цикл wait→(ingest+acknowledge | dispatch_and_apply)→flush до confirmed — «decide when to ingest and flush» event loop, который driver/turn намеренно оставили caller'у. poll() = один оборот: на Wakeup::Datagram → ingest датаграма + enqueue owed ACK по каждому installed-пространству (Initial/Handshake ACK немедленный, не таймерный — переиспользует SendAck-путь conn_turn::apply_action), затем flush; на Wakeup::TimerExpired → dispatch_and_apply гонит истёкшие таймеры (PTO/loss/delayed-ACK), flush пропускается при терминальном TurnEffect (§10, соединение закрыто — ничего не шлём). run(clock, max_turns) читает часы раз в оборот и останавливается на handshake_confirmed (Confirmed, проверяется до каждого оборота — уже подтверждённое соединение не драйвится) / терминальном TurnEffect IdleTimeout|Drained (Terminated) / исчерпании бюджета (Incomplete, защита от пира, который никогда не завершает хендшейк). HandshakeError оборачивает шаг wait/ingest/turn/flush. Clock-free кроме caller-supplied now; TLS-стейт-машина (feed CRYPTO→derive keys→install на обе половины→enqueue ответный CRYPTO) достаётся через turn_mut() между poll'ами — отдельный срез. +10 тестов, h3/ → 890 тестов. срез 58 ✅ 2026-07-06 — QUIC TLS-to-connection wiring (RFC 9001 §4/§4.9, RFC 8446 §4.4.4, h3::conn_tls): мост, который handshake-loop (срез 57) намеренно оставил caller'у — «feed CRYPTO → derive keys → install на обе половины → enqueue ответный CRYPTO». TlsConnState владеет ClientHandshake и сшивает три нижних чистых среза: connection реассемблит per-level CRYPTO-поток (read_crypto), но не толкует; ClientHandshake секвенирует хендшейк и деривит ключи, но без IO; ConnectionTurn ставит ключи и фреймы, но не знает какие. send_client_hello enqueue'ит ClientHello в Initial-CRYPTO (идемпотентно); advance() дренит свежий CRYPTO на уровнях Initial+Handshake, фреймит в TLS-сообщения (Handshake::parse, буферизуя сообщение, разбитое по датаграмам, в per-level residual), кормит стейт-машину и на каждый HandshakeEvent ставит выведенные packet-keys на обе половины (client→send ConnectionSendState, server→recv RecvKeyRing, §5.1): Handshake-ключи на HandshakeKeysReady, 1-RTT-ключи + client Finished в Handshake-CRYPTO на Complete (§4.4.4). TlsAdvance{messages_processed, handshake_keys_installed, completed} — completed несёт серверный Certificate/CertificateVerify для аутентификации caller'ом; TlsConnError{Handshake, Framing, Enqueue}. Чистое состояние, без часов и IO — только через заимствованный ConnectionTurn; per-level offset исходящего CRYPTO. Отложено: аутентификация цепочки сертификата (tls_cert_verify), сборка ClientHello (ALPN/transport params/SNI), HANDSHAKE_DONE/key-discard. +10 тестов, h3/ → 900 тестов; срез 59 ✅ 2026-07-06 — QUIC connect-цикл (RFC 9000 §7, RFC 9001 §4.1/§4.9, RFC 8446 §4.4.4): conn_connect::ConnectDriver сшивает HandshakeDriver (control flow) и TlsConnState (TLS-машину) в единый connect(): шлёт padded ClientHello первым ходом, после каждого Ingested-poll драйвит tls.advance по свежему CRYPTO, ставит Handshake/1-RTT ключи, флашит client Finished, возвращает Confirmed по HANDSHAKE_DONE; completed() отдаёт серверный сертификат для аутентификации caller'ом; полный handshake гоняется через реально зашифрованные server-flight'ы (+12 тестов). срез 60 ✅ 2026-07-06 — X.509 end-entity SubjectPublicKeyInfo extractor (RFC 5280 §4.1, RFC 5480, RFC 8410, h3::x509_spki): недостающее звено, которое tls_cert_verify явно делегирует caller'у («extracting the public key from the end-entity certificate's SubjectPublicKeyInfo is the caller's job»). Чистый DER-парсер: extract_end_entity_public_key() спускается по Certificate→tbsCertificate (пропуская optional [0] version + пять полей до SPKI, не толкуя их) до subjectPublicKeyInfo, читает алгоритм-OID (и named-curve OID для NIST-кривых) и возвращает subjectPublicKey как ServerPublicKey{algorithm, key_material} в точной форме под каждый верификатор tls_cert_verify — SEC1-точка для ECDSA P-256/384/521, сырые 32 байта для Ed25519, PKCS#1 DER RSAPublicKey для RSA. ServerPublicKey::accepts_scheme привязывает тип ключа к легальным SignatureScheme (RFC 8446 §4.2.3), отбивая key/scheme-mismatch до verify_certificate_verify. Читает только путь до SPKI (не validity/extensions/цепочку к trust anchor). Compose-тесты: извлечённый ключ проверяет реально подписанный CertificateVerify (p256/ed25519). Чистый парсинг, без часов/IO, +13 тестов, h3/ → 925 тестов. Остаётся: проводка cert-аутентификации в connect-цикл (транскрипт-хэш до Certificate в HandshakeComplete + verify_certificate_verify) и h3_do_request dispatch; срез 61 ✅ 2026-07-06 — QUIC server-certificate authentication wiring (RFC 8446 §4.4.2/§4.4.3, h3::conn_cert_auth): звено, которое connect-цикл и все нижние TLS-срезы явно делегировали caller'у. ClientHandshake снимает Transcript-Hash(ClientHello…Certificate) в момент попадания Certificate в транскрипт и отдаёт его в HandshakeComplete.certificate_transcript_hash — точный вход подписи CertificateVerify. authenticate_server_certificate() сшивает два нижних чистых среза: extract_end_entity_public_key (срез 60) достаёт SPKI end-entity-сертификата, accepts_scheme отбивает key/scheme-mismatch до верификатора, verify_certificate_verify проверяет подпись над снятым хэшем — доказательство владения приватным ключом предъявленного сертификата. ConnectDriver::poll аутентифицирует сертификат ПЕРЕД флашем client Finished: провал (CertAuthError→ConnectError::CertAuth) бросает соединение, не сигналя успешное рукопожатие неаутентифицированному пиру. Чистая аутентификация владения, без часов/IO; compose-тесты на реальных Ed25519/P-256 сертификатах + реальных подписях (чужой транскрипт/клиентский контекст/scheme-mismatch/битый DER отбиваются; connect-цикл роняет неаутентифицированный flight). trust-anchor-цепочка (промежуточные + validity) — deferred. +8 тестов, h3/ → 933 теста; срез 62 ✅ 2026-07-06 — X.509 hostname (SNI) verification (RFC 6125 §6, RFC 9110 §4.3.4, RFC 5280 §4.2.1.6): чистый verify_certificate_hostname парсит subjectAltName dNSName и матчит reference-хост (case-insensitive, left-most wildcard *.example.com = ровно один лейбл, legacy CN игнорируется); deferred = iPAddress SAN + проводка SNI из connect-цикла; +21 тест, h3/ → 954 теста; срез 63 ✅ 2026-07-06 — hostname (SNI) verification wired into connect loop (RFC 6125 §6, RFC 9110 §4.3.4): ConnectDriver получил reference_host, poll после authenticate_server_certificate (possession) зовёт verify_certificate_hostname (identity) на end-entity cert; несовпадение SAN = ConnectError::Hostname, бросает соединение до client Finished; +2 теста, h3/ → 956. срез 64 ✅ 2026-07-06 — X.509 certificate validity-period verification (RFC 5280 §4.1.2.5, h3::x509_validity): чистый verify_certificate_validity парсит validity SEQUENCE из TBSCertificate (по порядку полей до validity), декодирует notBefore/notAfter (UTCTime «YYMMDDHHMMSSZ» с окном годов §4.1.2.5.1 = 50→19xx/00→20xx + GeneralizedTime «YYYYMMDDHHMMSSZ», mandatory-seconds+Z-профиль) в Unix-секунды через days_from_civil (алгоритм Хиннанта, без chrono/новых зависимостей), проверяет notBefore ≤ now ≤ notAfter против переданного вызывающим now; NotYetValid/Expired/MalformedTime; отбивает out-of-range поля и невозможные даты (2021-02-29 в невисокосном), допускает leap-second (SS=60) и leap-day; certificate_validity() отдаёт сырые границы. Чистая функция, clock-free (now снаружи). Отложено = проводка реального wall-clock из connect-цикла (как срез 63 для hostname). +22 теста, h3/ → 978. срез 65 ✅ 2026-07-06 — certificate validity-period verification wired into connect loop (RFC 5280 §4.1.2.5, h3::conn_connect): ConnectDriver получил now_unix (wall-clock Unix-секунды; таймеры цикла монотонны и о календарной дате молчат), poll после possession (authenticate) и identity (hostname) зовёт verify_certificate_validity (срез 64) на end-entity cert — notBefore ≤ now_unix ≤ notAfter. Not-yet-valid/expired/битый validity = ConnectError::Validity, бросает соединение до client Finished (как срез 63 для hostname). Fixture-сертификат тестов получил реальное окно 2020–2030. +2 теста, h3/ → 980; срез 66 ✅ 2026-07-06 — X.509 cert-to-cert signature verification (RFC 5280 §4.1.1.3/§4.1.2.3, h3::x509_chain): чистый verify_certificate_signature(cert_der, issuer_public_key) режет Certificate на tbsCertificate/signatureAlgorithm/signatureValue, держит СЫРОЙ DER TBS (ровно подписанные байты, §4.1.1.3, через read_tlv_raw), сверяет внешний signatureAlgorithm с внутренним tbsCertificate.signature (§4.1.1.2, признак подмены), маппит OID и проверяет подпись над сырым TBS ключом издателя (ServerPublicKey из x509_spki, срез 60). Алгоритмы: ecdsa-with-SHA256/384/512 + id-Ed25519, переиспользуют digest-связанные верификаторы tls_cert_verify, каждый ECDSA-OID спарен с канонической кривой (fail-closed на неканонических парах и несовпадении типа ключа). ChainError{Malformed/AlgorithmMismatch/UnsupportedSignatureAlgorithm/BadSignature}. Отложено: RSA-подписи (PKCS#1 v1.5 + PSS — нет верификатора), name-chaining/basicConstraints/trust-anchor termination (§6), обход всей цепочки. +12 тестов, h3/ → 992. срез 67 ✅ 2026-07-06 — X.509 chain-signature walk (RFC 5280 §4.1.1.3, RFC 8446 §4.4.2, h3::x509_chain): verify_chain_signatures(chain: &[&[u8]]) обходит server certificate_list, извлекает SPKI каждого следующего сертификата (extract_end_entity_public_key, срез 60) и проверяет подпись каждого сертификата ключом вышестоящего (verify_certificate_signature, срез 66); последний сертификат в списке не проверяется (его издатель — trust anchor вне списка). ChainWalkError{EmptyChain / IssuerKey{issuer_index} / Link{subject_index}} указывает позицию сломанного звена; пустая цепочка → EmptyChain, одиночный сертификат → вакуумный успех. Чистый обход, без часов/IO. +7 тестов (2/3-звенные цепочки, разрыв в середине с позицией, лист не подписан издателем, неразбираемый SPKI издателя, пустая, одиночная), h3/ → 999. срез 68 ✅ 2026-07-06 — chain-signature verification wired into connect loop (RFC 5280 §4.1.1.3, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession (authenticate) / identity (hostname) / time (validity) собирает весь certificate_list в &[&[u8]] и зовёт verify_chain_signatures (срез 67); несогласованная цепочка = ConnectError::Chain(ChainWalkError), бросает соединение до client Finished. Одиночный сертификат проходит вакуумно (существующие фикстуры зелёные); фикстура двухзвенной цепочки — end-entity реально подписан Ed25519-ключом промежуточного, разрыв звена = Link{subject_index:0}. +2 теста, h3/ → 1001. срез 69 ✅ 2026-07-06 — X.509 name-chaining verification (RFC 5280 §4.1.2.4/§4.1.2.6/§6.1, h3::x509_name_chain): name-комплемент подписи (x509_chain, срезы 66–68 её откладывали). Чистый certificate_names достаёт сырой DER issuer/subject Name из TBSCertificate (read_tlv_raw); verify_name_chain обходит certificate_list и для каждой пары сверяет issuer[i] == subject[i+1] байт-в-байт (RFC 5280 §6.1) — консервативный поднабор нормализованного сравнения §7.1 (StringPrep case/whitespace, отложено), строже него: не примет несовпадение, которое §7.1 отверг бы. NameChainError{Malformed}, NameChainWalkError{EmptyChain, Certificate{index}, NameMismatch{subject_index}}; пустой список = EmptyChain, один сертификат вакуумно проходит. Чистый DER, без часов/IO. Отложено: полное §7.1-сравнение, проводка walk в connect-цикл, trust-anchor termination §6.1, basicConstraints/keyUsage, name constraints §4.2.1.10, RSA. +14 тестов, h3/ → 1015. срез 70 ✅ 2026-07-06 — name-chaining verification wired into connect loop (RFC 5280 §4.1.2.4/§4.1.2.6/§6.1, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity/time/chain-signature (срез 68) зовёт verify_name_chain (срез 69) на том же certificate_list — issuer[i] байт-в-байт == subject[i+1] (RFC 5280 §6.1). Несогласованное имя = ConnectError::NameChain(NameChainWalkError), бросает соединение до client Finished (как срез 68 для подписи); одиночный сертификат вакуумно проходит. Фикстуры ed25519_tbs_named+name_rdn строят двухзвенную цепочку с верной подписью (лист реально подписан промежуточным), но опционально расходящимися именами. +2 теста, h3/ → 1017. срез 71 ✅ 2026-07-06 — X.509 basicConstraints verification (RFC 5280 §4.2.1.9/§6.1.4, h3::x509_basic_constraints): чистый certificate_basic_constraints навигирует TBSCertificate до extensions [3], находит basicConstraints по OID 2.5.29.19 и декодирует cA BOOLEAN + опц. pathLenConstraint INTEGER (отсутствие расширения = cA FALSE §4.2.1.9); verify_ca_constraints обходит certificate_list — каждый ВЫШЕ листа сертификат ИЗДАЁТ нижний и обязан нести cA=TRUE (NotaCa), а pathLenConstraint должен вмещать число промежуточных под ним (index-1, PathLenExceeded §6.1.4(m)); лист (index 0) CA быть не обязан, одиночная цепочка вакуумна, пустая = EmptyChain. Комплемент подписи (срез 67) и имени (срез 69): цепочка должна пройти все три. Чистый DER, без часов/IO. Отложено: keyUsage §4.2.1.3, проводка walk в connect-цикл, trust-anchor termination §6.1, name constraints §4.2.1.10, RSA. +23 теста, h3/ → 1040. срез 72 ✅ 2026-07-06 — basicConstraints verification wired into connect loop (RFC 5280 §4.2.1.9/§6.1.4, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity/time/chain-signature (срез 68)/name (срез 70) зовёт verify_ca_constraints (срез 71) на том же certificate_list — каждый издающий сертификат (chain[1..]) обязан нести basicConstraints cA=TRUE и укладываться в pathLenConstraint (§6.1.4(m)). Не-CA издатель = ConnectError::CaConstraints(NotaCa), бросает соединение до client Finished (как срез 70 для имён). Фикстуры-цепочки получили cA=TRUE на промежуточных (ed25519_tbs_ca/ca_extensions); +2 теста проводки (permitted-CA completes / non-CA issuer rejected), h3/ → 1042. срез 73 ✅ 2026-07-06 — X.509 keyUsage verification (RFC 5280 §4.2.1.3/§6.1.4, h3::x509_key_usage): keyCertSign-комплемент basicConstraints (срез 71). Чистый certificate_key_usage навигирует TBSCertificate до extensions [3], находит keyUsage по OID 2.5.29.15 и декодирует BIT STRING именованных бит (bit 0 = MSB первого октета; keyCertSign = bit 5); отсутствие расширения = неограниченный ключ §4.2.1.3; verify_cert_sign_usage обходит certificate_list — каждый ВЫШЕ листа сертификат, ЕСЛИ несёт keyUsage, обязан утверждать keyCertSign (NotCertSign), издатель без keyUsage не ограничивается (cA=TRUE уже потребован срезом 71); лист (index 0) не проверяется, одиночная цепочка вакуумна, пустая = EmptyChain. KeyUsage{allows(KeyUsageBit)/key_cert_sign()}, KeyUsageError{Malformed}, KeyUsageWalkError{EmptyChain, Certificate{index}, NotCertSign{index}}. Чистый DER, без часов/IO. Отложено: остальные биты keyUsage + extendedKeyUsage §4.2.1.12, проводка walk в connect-цикл, trust-anchor termination §6.1, RSA. +21 тест, h3/ → 1063. срез 74 ✅ 2026-07-06 — keyUsage verification wired into connect loop (RFC 5280 §4.2.1.3/§6.1.4, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity/time/chain-signature (срез 68)/name (срез 70)/basicConstraints (срез 72) зовёт verify_cert_sign_usage (срез 73) на том же certificate_list — каждый издающий сертификат, несущий keyUsage, обязан утверждать keyCertSign (§4.2.1.3). Издатель с keyUsage без keyCertSign = ConnectError::KeyUsage(NotCertSign), бросает соединение до client Finished (как срез 72 для basicConstraints); издатель без keyUsage не ограничивается (cA=TRUE уже потребован срезом 72). Фикстура получила keyUsage на промежуточном (key_usage_entry/ed25519_tbs_named_ext); +2 теста проводки (permitted keyCertSign completes / missing keyCertSign rejected), h3/ → 1065. срез 75 ✅ 2026-07-06 — X.509 trust-anchor termination verification (RFC 5280 §6.1/§4.1.1.3, RFC 8446 §4.4.2, h3::x509_trust_anchor): термин-комплемент подписи (срезы 66-68), имени (69-70), basicConstraints (71-72) и keyUsage (73-74) — все четыре останавливались на звене короче trust anchor. TrustAnchor{subject, subject_public_key_info} приходит от вызывающего параметром (как RootCertStore в tls::build_client_config), не хардкодится — реальный Mozilla root store (webpki_roots) подключит вызывающий в срезе проводки, как и h3_do_request. find_trust_anchor ищет anchor.subject == issuer байт-в-байт; verify_trust_anchor берёт topmost = chain[len-1] (для одиночной цепочки — сам end-entity), извлекает его issuer (переиспользует x509_name_chain::certificate_names), матчит якорь, парсит его SPKI новым x509_spki::parse_subject_public_key_info (общий код с extract_end_entity_public_key вынесен в parse_spki_contents) и проверяет подпись topmost под ключом якоря (переиспользует x509_chain::verify_certificate_signature). TrustAnchorError{Names, UnknownIssuer, AnchorKey, Signature}, TrustAnchorWalkError{EmptyChain, Untrusted{index}}. Чистый DER + подпись, без часов/IO. Отложено: проводка walk в connect-цикл (как срез 72/74 для basicConstraints/keyUsage), реальный Mozilla root store как поставщик anchors, RSA cert signatures, h3_do_request dispatch. +11 тестов, h3/ → 1076. срез 76 ✅ 2026-07-06 — X.509 trust-anchor termination wired into connect loop (RFC 5280 §6.1/§4.1.1.3, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity/time/chain-signature (срез 68)/name (срез 70)/basicConstraints (срез 72)/keyUsage (срез 74) зовёт verify_trust_anchor (срез 75) на том же certificate_list — термин-комплемент всех четырёх смежных обходов, каждый из которых останавливается на одно звено короче якоря. ConnectDriver получил поле trust_anchors: Vec<OwnedTrustAnchor> (owned пара subject/subject_public_key_info, чтобы не тащить заимствованный lifetime TrustAnchor) и пятый параметр ConnectDriver::new; реальный Mozilla root store подключит вызывающий в срезе проводки HttpClient, как и h3_do_request. Цепочка без доверенного якоря (пустой trust store) или с якорем под чужим ключом бросает соединение до client Finished (ConnectError::TrustAnchor). Фикстуры-цепочки (одиночный leaf и intermediate) теперь подписаны реальным корневым ключом вместо плейсхолдера; +2 теста проводки, h3/ → 1078. срез 77 ✅ 2026-07-06 — X.509 RSASSA-PKCS1-v1_5 certificate-signature verification (RFC 5280 §4.1.1.2/§4.1.1.3, RFC 8017 §8.2/Appendix A.2.4, h3::x509_chain): RSA-комплемент срезов 66-68 (ECDSA/Ed25519) — sha256/384/512WithRSAEncryption (1.2.840.113549.1.1.11/12/13) под rsaEncryption issuer-ключом через новый rsa_pkcs1v15_verify<D> (RSASSA-PKCS1-v1_5, не RSASSA-PSS tls_cert_verify для TLS CertificateVerify — X.509-сертификаты, включая большинство Mozilla root/intermediate CA, по-прежнему подписаны PKCS#1 v1.5, схемой, которую TLS 1.3 запрещает только внутри CertificateVerify §4.2.3, но не для сертификатов); classify_signature_algorithm/verify получили три новых варианта, спаренных с SpkiAlgorithm::Rsa так же, как ECDSA-варианты спарены со своей кривой (несовпадение = AlgorithmMismatch); sha2 получил feature="oid" (не новая зависимость — фича уже используемого крейта) для AssociatedOid, нужного pkcs1v15::VerifyingKey<D>::new под DigestInfo-префикс §9.2; RSASSA-PSS certificate signature (RFC 4055, отдельный параметризованный OID) и RSA с SHA-1 остаются UnsupportedSignatureAlgorithm. Чистая крипта без часов/IO, +6 тестов (net), h3/ → 1083. срез 78 ✅ 2026-07-06 — X.509 chain-wide validity walk (RFC 5280 §6.1.3(a)(2)): verify_validity_chain в h3::x509_validity применяет verify_certificate_validity к КАЖДОМУ сертификату certificate_list, а не только к end-entity — сейчас connect-цикл проверяет notBefore/notAfter лишь у листа, а просроченный intermediate так же фатален (не может более считаться выдавшим нижестоящий сертификат). Валидатор-компаньон обходов signature/name/basicConstraints/keyUsage, но, в отличие от issuer-only обходов, покрывает и лист (index 0). ValidityWalkError{EmptyChain, Certificate{index,error}}. Чистый DER без часов сверх переданного now, +6 тестов (net), h3/ → 1089. срез 79 ✅ 2026-07-06 — chain-wide validity walk wired into connect loop (RFC 5280 §4.1.2.5/§6.1.3(a)(2), RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity собирает весь certificate_list в &[&[u8]] и зовёт verify_validity_chain (срез 78) вместо verify_certificate_validity лишь по листу — каждый сертификат обязан быть в своём окне [notBefore, notAfter] против now_unix, просроченный intermediate так же фатален, как просроченный лист (издатель вне даты не мог выдать нижестоящий). ConnectError::Validity теперь несёт ValidityWalkError (индекс сломанного звена + вложенная ValidityError), сиблинг Chain/NameChain/CaConstraints/KeyUsage-ошибок; цепочка собирается один раз и переиспользуется всеми обходами ниже. Несогласованность по времени бросает соединение до client Finished. Фикстура-цепочка с просроченным intermediate (окно 2000–2010 при now=2025) отбивается на index 1, лист остаётся в дате. +1 тест (net), h3/ → 1090. Остаётся: реальный Mozilla root store + h3_do_request dispatch. срез 80 ✅ 2026-07-06 — X.509 extendedKeyUsage serverAuth walk (RFC 5280 §4.2.1.12): чистый h3::x509_ext_key_usage — verify_server_auth_eku проверяет лист (chain[0]); при наличии extendedKeyUsage требует id-kp-serverAuth (1.3.6.1.5.5.7.3.1) или anyExtendedKeyUsage (2.5.29.37.0); отсутствие расширения = назначение не ограничено; issuers не проверяются (RFC 5280 не мандатит EKU-чейнинг). +20 тестов, h3/ → 1110. срез 81 ✅ 2026-07-06 — X.509 serverAuth-EKU wired into connect loop (RFC 5280 §4.2.1.12, RFC 8446 §4.4.2, h3::conn_connect): ConnectDriver::poll после possession/identity/time/chain-signature/name/basicConstraints/keyUsage/trust-anchor зовёт verify_server_auth_eku (срез 80) на том же certificate_list — девятая и последняя проверка сертификата перед flush клиентского Finished; лист с extendedKeyUsage, не называющим serverAuth/anyExtendedKeyUsage (напр. только clientAuth), фатален так же, как непроверенная подпись или неукоренённая цепочка. ConnectError::ExtKeyUsage(ExtKeyUsageWalkError) — сиблинг Chain/NameChain/CaConstraints/KeyUsage/TrustAnchor-ошибок, +2 теста (лист serverAuth завершает handshake, лист clientAuth-only → NotServerAuth{index:0}). Остаётся: h3_do_request dispatch. срез 82 ✅ 2026-07-04 — X.509 unrecognized-critical-extension rejection (RFC 5280 §4.2, §6.1.4(n)). срез 83 ✅ 2026-07-06 — реальный Mozilla root store (RFC 5280 §6.1, RFC 8446 §4.4.2, h3::mozilla_roots): чистый mozilla_trust_anchors() превращает compiled-in webpki_roots::TLS_SERVER_ROOTS (тот же список Mozilla, что HTTP/1.1/2-путь грузит в rustls::RootCertStore) в Vec<OwnedTrustAnchor> для ConnectDriver::new — единый набор якорей на оба транспорта. Ключевая тонкость: rustls_pki_types::TrustAnchor хранит И subject, И subject_public_key_info как голые значения БЕЗ внешнего SEQUENCE-заголовка (subject начинается с RDN SET 0x31, SPKI — с внутреннего AlgorithmIdentifier 0x30), поэтому оба поля пере-оборачиваются в SEQUENCE (wrap_in_sequence) — иначе subject не совпал бы ни с одним issuer, а ключ не декодировался бы (x509_spki принял бы внутренний AlgorithmIdentifier за внешний SPKI): хранилище, молча не доверяющее ничему. Адверсариальный тест every_public_key_is_a_wrapped_parsable_spki поймал ранний черновик, оборачивавший лишь subject. name_constraints отбрасываются (OwnedTrustAnchor их не моделирует). Чисто, детерминированно, +6 тестов. срез 84 ✅ 2026-07-06 — HTTP/3 client response assembler (RFC 9114 §4.1, §7.1, h3::h3_exchange): чистый ResponseAssembler композит frame-кодека + RequestStream-грамматики (h3_stream) + decode_response/decode_trailers (h3_request) — push_bytes инкрементально парсит фреймы из растущего буфера (partial-frame переносится между чанками, RFC 9114 §7.1), гоняет каждый через грамматику, классифицирует HEADERS по фазе (header-section 1xx/final до тела vs trailer-section после), копит DATA в тело; finish() на stream-FIN отдаёт H3Response{status,headers,body,informational,trailers}. Добавлен h3_request::decode_trailers + validate_trailer_fields (RFC 9114 §4.1: в трейлерах запрещены псевдо-заголовки, MessageError::PseudoInTrailer). +17 тестов. Срез 85 ✅ 2026-07-06 — client request exchange (h3::request_exchange): ClientExchange композирует encode_request (send) и ResponseAssembler (recv) в жизненный цикл запроса; start() рендерит HEADERS+DATA-байты, on_recv(data,fin) собирает H3Response; чистый автомат, +13 тестов. Срез 86 ✅ 2026-07-07 — client request multiplexer (RFC 9000 §2.1, RFC 9114 §6.1, h3::request_mux): чистый RequestMux аллоцирует клиентские bidi-stream-id (0,4,8,… = 4·n, две младшие бита 0b00) и маршрутизирует несколько параллельных ClientExchange по stream-id — open() отдаёт OpenedRequest{stream_id, bytes-с-FIN} и продвигает номер потока только при успехе (плохой запрос не жжёт id), on_recv(stream_id,data,fin) кормит нужный exchange и на завершении (H3Response) либо ошибке ретайрит его; MuxError::UnknownStream для байтов вне known-потока, OpenError::StreamsExhausted на исчерпании 2^60 id; +9 тестов. Срез 87 ✅ 2026-07-07 — client request dispatch over QUIC streams (RFC 9000 §2.1/§3, RFC 9114 §4.1/§6.1, h3::request_dispatch): чистый RequestDispatch связывает HTTP/3-башню (request_mux → request_exchange → кодеки) с QUIC-башней потоков (stream_manager → stream) — send_request аллоцирует клиентский bidi-поток через mux, открывает SendStream, пишет отрендеренное сообщение и закрывает send-половину STREAM FIN (запрос = одно сообщение; id тратится только на успехе, плохой запрос не жжёт ни id, ни send-state); on_stream_frame маршрутизирует входящий STREAM-фрейм в реассемблинг manager'а, дренирует новый непрерывный префикс, детектит recv STREAM FIN по переходу RecvStream в DataRead (и на чтении последнего байта, и на голом FIN после дренажа тела) и кормит байты+FIN нужному exchange через mux — отдаёт H3Response на несущем FIN фрейме и ретайрит поток; байты для потока без in-flight запроса отбиваются заранее (DispatchError::Mux(UnknownStream)) без фантомного recv-state, нарушение flow-control/final-size = DispatchError::Stream, кривой ответ = DispatchError::Mux(Exchange). +14 тестов, h3/ → 1187. срез 88 ✅ 2026-07-07 — HTTP/3 request pump (RequestPump над RequestDispatch, RFC 9000 §19.4/§19.5/§19.8/§19.10/§19.13, RFC 9114 §4.1/§6.1): poll_transmit дренирует send-половины запросов в Frame::Stream (сегментация по бюджету кадра, стоп на flow-control окне, FIN на последнем чанке); on_frame маршрутизирует входящие per-stream кадры — STREAM→on_stream_frame (Response/Progress), RESET_STREAM→recv_reset+abort запроса (final_size в conn-бюджет §4.5, Aborted), STOP_SENDING→reset send-половины + ответный RESET_STREAM (StopSending), MAX_STREAM_DATA/STREAM_DATA_BLOCKED→сдвиг лимита (Progress); кадр для неизвестного/завершённого потока = Ignored без фантомного recv-state; +StreamManager::send_stream_ids, RequestMux/RequestDispatch::abort, +20 тестов. Срез 89 ✅ 2026-07-07 — RequestTurn: проводка RequestPump к живому ConnectionTurn (h3::request_turn, RFC 9000 §12.4/§19.8, RFC 9114 §4.1/§6.1): RequestTurn владеет ConnectionTurn+RequestPump и связывает два фрейм-порта пампа с портами соединения. Отправка — stage_requests дренирует pump.poll_transmit (сегментация по max_request_frame_len, дефолт = MTU−STREAM_FRAME_MTU_OVERHEAD) и enqueue'ит каждый STREAM-фрейм в Application-Data очередь (STREAM 1-RTT-only §12.5); flush = passthrough ConnectionTurn::flush; transmit = stage+flush. Приём — route_deferred маршрутизирует пять per-stream фреймов (STREAM/RESET_STREAM/STOP_SENDING/MAX_STREAM_DATA/STREAM_DATA_BLOCKED) через pump.on_frame и собирает PumpEvent'ы, а не-запросные (ACK/NEW_TOKEN) кладёт в residual — ничего не глотается; ingest = driver.ingest + route_deferred, возвращает IngestReport (счётчики + PATH_RESPONSE/RETIRE_CID) рядом с RequestIngest; RequestIngest::into_responses. RequestTurnError{Enqueue/Flush/Ingest/Route}. Чистый автомат, IO только транспорт. +22 теста, h3/ → 1229. срез 89 ✅ 2026-07-07 — RequestTurn: RequestPump, сшитый с живым ConnectionTurn (stage/flush исходящих STREAM-кадров, ingest+route входящих per-stream кадров; RFC 9000 §12.4/§19.8, RFC 9114 §4.1/§6.1). срез 90 ✅ 2026-07-07 — RequestDriver: гоняет RequestTurn по event-loop до завершения запросов (poll = wait → ingest+ack | dispatch_and_apply → flush; run стейджит send-half перед каждым ходом, копит H3Response в порядке завершения, шлёт RESET_STREAM в ответ на STOP_SENDING; терминальный тайм-аут/бюджет ходов останавливают цикл; зеркало HandshakeDriver для request-фазы; RFC 9000 §10.1/§13.2.1, RFC 9002 §6.2, RFC 9114 §4.1/§6.1; +12 тестов). срез 91 ✅ 2026-07-07 — ConnectDriver::into_request_driver: склейка ConnectDriver→RequestDriver (RFC 9114 §3.3/§4.1, RFC 9000 §19.20): на подтверждённом рукопожатии (is_confirmed) отдаёт RequestDriver, сшитый с ConnectionTurn через RequestTurn::with_default_frame_len; до подтверждения — RequestSpliceError::NotConfirmed; сертификат уже проверен на этапе connect, здесь отбрасывается; +4 теста. срез 92 ✅ 2026-07-07 — client_bootstrap::connect_client: единая точка сборки готового к гонке ConnectDriver из тройки (transport, server_name, trust store) (RFC 9000 §7.2/§7.3, RFC 9001 §5.2, RFC 9114 §3.2): генерит случайные Initial DCID/SCID, эфемерную пару X25519 и реальный TLS 1.3 ClientHello (SNI, ALPN h3, один X25519 key_share, обязательные расширения, quic_transport_parameters с initial_source_connection_id=SCID), выводит Initial-ключи из DCID (send=client-секрет, recv=server-секрет — в отличие от фикстур, что сами шифруют входящее), сшивает recv/loss/TLS в ConnectDriver; ClientConnectConfig с браузероразумными дефолтами; +5 тестов. срез 93 ✅ 2026-07-07 — client_request: оркестратор h3_do_request над готовым ConnectDriver (RFC 9114 §3.3/§4.1/§6.1): fetch() кладёт один запрос на подтверждённый RequestDriver и гоняет его до единственного H3Response (готовый ответ приоритетнее причины останова — терминальный таймер не теряет уже собранный ответ); connect_and_fetch() сшивает три фазы — ConnectDriver::connect до Confirmed (иначе NotConfirmed(outcome)), into_request_driver splice, fetch — деля один clock между рукопожатием и запросом; ClientConnectConfig::request_pump()/stream_manager_config() строят RequestPump из объявленных лимитов (per-stream send-seed = peer initial_max_stream_data_bidi_remote, провизорно до сервер-параметров). FetchError/ConnectFetchError. Transport-generic, IO только транспорт. +6 тестов. срез 94 ✅ 2026-07-07 — client_transport: реальный UDP-транспорт h3_do_request (RFC 9114 §3.3/§4.1) — резолвит authority через инъектированный DnsResolver, открывает настоящий udp::UdpDatagram к первому адресу (локальный bind по семейству пира), грузит mozilla_trust_anchors, читает Instant::now + Unix-epoch, и гонит connect_client→connect_and_fetch до одного H3Response; transport-generic ядро h3_exchange тестируется поверх MockDatagramTransport (молчащий транспорт → NotConfirmed) без реального сервера; authority опускает дефолтный 443. Возвращает H3Response как есть (маппинг — на границе диспетчеризации в lib.rs). +5 тестов; срез 95 ✅ 2026-07-07 — H3Response→крейтовый Response на границе диспетчеризации (impl From<H3Response> for Response в lib.rs): заголовки декодируются из непрозрачных октетов (RFC 9114 §4.2) через UTF-8 lossy, informational (1xx) и трейлеры отбрасываются (в Response нет слота, паритет с H2). +3 теста; срез 96 ✅ 2026-07-07 — Alt-Svc dispatch-решение try_h3_dispatch в lib.rs (RFC 7838): маршрутизирует origin на h3_do_request только при свежей записи в AltSvcCache (ключ origin_key host:port, цель через AltSvcEntry::connect_target), мапит H3Response→Response, при провале QUIC-плеча выселяет «broken»-альтернативу (RFC 7838 §2.4) и возвращает None (фолбэк на H2/H1.1); ещё не вызывается из fetch_single (как срез 58 отложил вызов From). +6 тестов. срез 97 ✅ 2026-07-07 — HttpClient получил кэш Alt-Svc: флаг http3_enabled (opt-in with_http3(), off by default) + shared alt_svc_cache (Arc<Mutex<AltSvcCache>>, RFC 7838) + record_alt_svc() — скан Alt-Svc-заголовков ответа (мультизначения по RFC 7230 §3.2.2), парс h3-альтернатив, вставка под origin_key(host,port) с TTL из ma; no-op при выключенном h3/без h3-рекламы; ещё не вызывается из fetch_single (проводка скана+try_h3_dispatch = срез 98). +3 теста. Остаётся (срез 98+): вызов record_alt_svc на H2/H1.1-ответах + try_h3_dispatch перед connect в fetch_single, конверсия header-блока в (name,value)-байты | HTTP/3 |
| PH3-20 | P3 | | done | | | | Service Workers (PH3-20 fetch interception Phase 1) |
| P3-woff2 | P3 | | done | | | | WebFonts WOFF2 |
| P3-ext | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): расширения Phase 0 (MV3-subset: manifest.json loader + content_scripts + chrome.runtime стаб) — shell/extensions | Расширения (минимальный формат) |
| P3-wpt | P3 | | planned | | | | WPT pass rate ≥ 60% |
| P3-ai | P3 | | planned | | | | Опциональный AI-модуль (lumen-ai, семантический поиск/RAG) |
| P3-ws | P3 | | done | | | P3-ws ✅ 2026-06-25 (in-flight fetch abort + SSE non-blocking reconnect + WS sub-protocol/wasClean/state-machine + e2e suite; deep zero-poll WS push = optional shell refinement) | WebSockets + SSE + Fetch API + AbortController |
| P3-auth | P3 | | done | | | | HTTP auth (Basic + Digest) |
| P3-safebrowse | P3 | | done | | | | Safe Browsing equivalent |
| P3-bfcache | P3 | | active | | | Незавершённая работа в ветке `p1-ph3-bfcache` (не на main, см. HEALTH-LOG 2026-07-01); lifecycle-срез (freeze/thaw) уже влит в main отдельно | Back/forward cache (bfcache) |
| P3-navapi | P3 | | active | | | Незавершённая работа в ветке `p1-ph3-navapi` (не на main, см. HEALTH-LOG 2026-07-01); фазы 1a/1b — см. бриф docs/tasks/ph3-navigation-history-api.md | Navigation API + History API runtime |
| PH3-8 | P3 | | done | | | | Web Animations API runtime (PH3-8) |
| PH3-7 | P3 | | done | | | | contentEditable + Input Events L2 + Selection/Range (PH3-7) |
| P3-spell | P3 | | done | | | PARTIAL 2026-07-04 (срезы 1-3): lumen_core::spell — pure-Rust движок Hunspell + срез 2: shell::spellcheck (load_dictionaries из data/spell, MultiDictionary, extract_words/misspelled_ranges, build_spell_overlay) + обвязка main.rs (squiggly для фокусного input/textarea) + срез 3 (P1, 2026-07-04): squiggly в contenteditable (find_editing_host), контекст-меню page_context_menu (right-click → ≤5 подсказок + «Добавить в словарь»/user_words.txt + «Пропустить»/сессия) для однострочного input, misspelled_ranges_with(allow-set), 25 тестов; срез 4 (P1, 2026-07-06): apply в textarea/contenteditable — try_open_spell_menu снимает ограничение single-line, собирает рендер-строки поля по порядку и переиспользует `spellcheck::locate_line_word_in_full_text` (прямой substring-поиск с курсором вперёд) для пересчёта byte-смещения слова из visual-line в глобальное внутри полного значения (`value` для input, `lumen_dom::node_text_content` для textarea/contenteditable); apply для input/textarea перестраивает полное значение (`SpellTarget::apply` + `forms::set_textarea_text` — текстовые узлы, не value-атрибут); для contenteditable правит только текстовый узел со словом через новый `lumen_dom::locate_text_offset_range` + `delete_range`/`insert_text_at`, не трогая остальной rich-text; +13 тестов (4 lumen-dom, 4 spellcheck, 2 forms); дошить — OS-провайдер ISpellChecker (опц.) | Spell check (Hunspell, русский словарь) |
| P3-varfonts | P3 | | planned | | BUG-109 | PARTIAL (сверка с кодом 2026-07-02): fvar/gvar/avar/HVAR/MVAR + apply_variations в CPU/wgpu (variation.rs); дошить — проводка в живое окно femtovg (BUG-109, сейчас дефолт-instance) | Variable fonts axes runtime (font-variation-settings) |
| P3-color | P3 | | active | | | | Color management + Display P3 / Rec2020 / ICC |
| P3-print | P3 | | done | | | | Print pipeline runtime (pagination + PDF) |
| P3-media | P3 | | done | | | | Медиа Phase 1: getUserMedia / <audio> / <video> / Screen Capture / Pointer Lock / Idle / Wake Lock / File System Access |
| P3-dnd | P3 | | done | | | | HTML5 Drag and Drop API (PH3-9) |
| P3-subgrid | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): алгоритм subgrid.rs наследует треки родителя; дошить — CSS-проводка keyword subgrid в каскад (CSS-SPECS §42) | CSS Subgrid (grid-template-rows/columns: subgrid) |
| P3-anchorpos | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): анкер-алгоритм anchor.rs (resolve_anchor/inset_area) готов; дошить — CSS-проводка anchor-name/position-anchor/inset-area/anchor-size() в каскад (BUG-126) | CSS Anchor Positioning L1 (anchor()/anchor-size()/position-area) |
| P3-nesting | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): полный CSS Nesting L1 — parser.rs expand_nesting (& явный/неявный + вложенные @media/@supports/@layer/@container), 20 тестов | CSS Nesting (полный спек: & и вложенные правила) |
| P3-scope | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): @scope root-matching готов (node_is_in_scope); дошить — donut/limit (inner-scope), CSS-SPECS §53 Phase 2 | CSS @scope (scoped styling + donut scope) |
| P3-stylequery | P3 | | done | | | Phase 0 ✅ 2026-07-02 (смешанное программирование, nemotron-3-ultra): style(--prop[: value]) в @container — evaluate_container_condition + ContainerContext.custom_props; одна декларация, литеральное сравнение; state() ⬜ | CSS Container Style Queries (style()/state()) |
| P3-textwrap | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): text-wrap-mode/style парсятся в ComputedStyle; дошить — алгоритм balance/pretty в line-break (сейчас поля не влияют на перенос) | CSS text-wrap: balance / pretty |
| P3-has | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): :has() relational matching в каскаде — style.rs matches_relative (Selectors L4 §17.2) | Селектор :has() (полная поддержка в каскаде) |
| P3-colormix | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): color-mix() + relative color syntax — color_mix.rs (все color-spaces srgb/hsl/hwb/lab/lch/oklab/oklch/xyz) | CSS color-mix() + relative color syntax |
| P3-regprop | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): @property registration + typed enforcement — style.rs PropertyRule/registry (syntax/inherits/initial-value) | CSS @property (registered custom props + Typed OM) |
| P3-counterstyle | P3 | | done | | | Статус был протухшим: сверка с кодом 2026-07-02 (вечер) — @counter-style реализован ПОЛНОСТЬЮ (parse_counter_style_rule + CounterStyleRegistry + ListStyleType::Custom + build_list_marker_text, тесты в parser.rs и counters.rs) | CSS @counter-style (кастомные маркеры списков) |
| P3-multicol | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): column-count/width/gap/rule + балансировка высоты — box_tree.rs multi_column_layout + column-rule paint | CSS Multi-column layout (column-count/width/gap/rule) |
| P3-contentvis | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): content-visibility hidden/auto работают (skip + ratchet); дошить — применение contain-intrinsic-size как размер-хинта в layout | CSS content-visibility + contain-intrinsic-size |
| P3-fragmentation | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): break-before/after/inside + orphans/widows парсятся, применяются в pagination.rs (print); дошить — применение вне печати (multicol/regions) | CSS Fragmentation (break-inside / widows / orphans) |
| P3-initialletter | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): Phase 0 drop-cap float (size×line-height, sink); дошить — точное cap-height/baseline выравнивание, raised-cap, RTL | CSS initial-letter (буквица drop-cap) |
| P3-vertical | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): block axis-swap vertical-rl/lr готов (vertical.rs); дошить — вертикальный inline-поток (Phase 2, lay_out_vertical_inline_run) | CSS writing-mode: вертикальный текст (полный layout) |
| P3-resizeobs | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): ResizeObserver observe/unobserve/disconnect + delivery после relayout — dom.rs | ResizeObserver |
| P3-intersectobs2 | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): IntersectionObserver + threshold/isIntersecting/rootMargin — dom.rs (используется lazy-load) | IntersectionObserver v2 (visibility tracking) |
| P3-streams | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): ReadableStream/WritableStream/TransformStream (getReader/tee/pipeTo/pipeThrough) — dom.rs (Phase 0, без pull-backpressure) | Streams API (Readable/Writable/Transform) |
| P3-webcrypto | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): ECDSA-P256 + HMAC-SHA* + AES-GCM (subtle_crypto.rs); дошить — RSA/Ed25519/PBKDF2/HKDF/AES-CBC-CTR/standalone digest (~20 алгоритмов спека) | Web Crypto SubtleCrypto (полный набор алгоритмов) |
| P3-weblocks | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): navigator.locks request/query (exclusive/shared, ifAvailable, steal, signal) — web_locks.rs (Phase 0, без cross-tab) | Web Locks API |
| P3-broadcast | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): BroadcastChannel cross-runtime (process-global hub + pump в event loop) — broadcast_channel.rs | BroadcastChannel |
| P3-structclone | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): structuredClone (примитивы/Map/Set/массивы/объекты) — dom.rs; дошить — Transferable (ArrayBuffer/ImageBitmap/OffscreenCanvas transfer) | structuredClone + Transferable objects |
| P3-ricallback | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): requestIdleCallback/IdleDeadline (timeout honored) — dom.rs (idle эмулируется таймером) | requestIdleCallback / IdleDeadline |
| P3-clipboard | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): navigator.clipboard readText/writeText через platform provider — clipboard.rs (Phase 0, текст; image — стаб) | Async Clipboard API (read/write text+image) |
| P3-trustedtypes | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): Trusted Types L2 createPolicy/TrustedHTML|Script|ScriptURL — trusted_types.rs (Phase 0, без sink-enforcement) | Trusted Types (защита от DOM XSS) |
| P3-sanitizer | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): Sanitizer/setHTML — sanitizer.rs (Phase 0, regex: срез <script> + on*-атрибуты) | HTML Sanitizer API |
| P3-customstate | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): ElementInternals + CustomStateSet готовы (element_internals.rs); дошить — CSS-селектор :state() в каскаде (P4-handoff) | ElementInternals + custom element states (:state()) |
| P3-pointerfull | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): base PointerEvent L2 работает; дошить — L3 getCoalescedEvents/getPredictedEvents (сейчас возвращают []) | Pointer Events L3 (coalesced / predicted events) |
| P3-compressionstream | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): классы CompressionStream/DecompressionStream есть, но throw для всех форматов; дошить — реальный gzip/deflate/brotli (переиспользовать декодеры lumen-network flate/brotli) | Compression Streams (gzip/deflate/brotli) |
| P3-cookiestore | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): cookieStore get/getAll/set/delete + CookieChangeEvent — cookie_store.rs (Phase 0, in-memory) | Cookie Store API |
| P3-cacheapi | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): caches.open/match/delete/keys/has + backend memory|SQLite, wired в SW fetch — dom.rs + cache bindings | Cache API + offline-first навигация |
| P3-webtransport | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): стаб webtransport.rs всегда reject (нет QUIC); дошить — зависит от QUIC/H3 (P3-h3) | WebTransport (поверх HTTP/3) |
| P3-reporting | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): ReportingObserver observe/disconnect/takeRecords — reporting_api.rs | Reporting API + Network Error Logging |
| P3-earlyhints | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): FetchPriority enum (High/Med/Low) есть — core/event.rs; дошить — обработка 103 Early Hints ответа | fetch Priority Hints + 103 Early Hints |
| P3-storagebuckets | P3 | | done | | | P1 2026-06-26 (navigator.storageBuckets open/keys/delete + StorageBucket persist/estimate/durability/expires/getDirectory, Phase 0 in-memory) | Storage Buckets API + quota management |
| P3-permissions | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): navigator.permissions.query -> PermissionStatus — dom.rs (desktop-политика) | Permissions API (query / onchange) |
| P3-notifications | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): Notification + requestPermission + show/close/click, OS-доставка через шелл — notifications_bindings.rs | Notifications API + системные уведомления |
| P3-pushapi | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): PushManager subscribe/getSubscription стаб (push_api.rs), подписки in-memory; дошить — реальная доставка push-endpoint | Push API (через Service Worker) |
| P3-offscreencanvas | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): OffscreenCanvas getContext/transferToImageBitmap + transferControlToOffscreen, работает в Worker — offscreen_canvas.rs | OffscreenCanvas + рендеринг в Worker |
| P3-webgl2 | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): WebGL2-контекст + GLSL ES 3.0 интерпретатор (webgl_bindings.rs); дошить — present фреймбуфера в страничный <canvas> (как P4-webgl, сейчас только readPixels) | WebGL2 подмножество |
| P3-avif | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): AVIF-декодер за feature-флагом avif (libavif+cmake+nasm), в дефолт-сборке не декодит; дошить — AVIF в дефолте + JPEG XL (сейчас sniff-only Err) | AVIF / JPEG XL декодирование |
| P3-webvtt | P3 | | done | | | DONE 2026-07-04 (P1, срез 4): lumen_dom::vtt (парсер cue + collect_video_tracks + active_cues/strip_cue_markup/resolve_cue_box) + shell::tracks (load_video_tracks, build_cue_overlay, collect_video_rects) + TextTrack JS API (video.textTracks/TextTrack/TextTrackCue/activeCues/cuechange через TextTrackStore-мост) + playback-клок из VideoGifStore (фолбэк nav_start для не-GIF). Остаток вне WebVTT: addTextTrack() и playback-клок для не-GIF <video> (зависит от HTML5-video decode) | WebVTT субтитры для <video> (<track>) |
| P3-mediasession | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): MediaSession metadata/playbackState/setActionHandler/setPositionState — media_session.rs (Phase 0, JS-сторона) | Media Session API |
| P3-pip | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): Document PiP requestWindow готов (document_pip.rs), Element.requestPictureInPicture частично; дошить — element-PiP + полноценное OS-окно | Picture-in-Picture API |
| P3-imagebitmap | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): createImageBitmap(ImageData) + transferToImageBitmap (offscreen_canvas.rs); дошить — ImageBitmapRenderingContext (bitmaprenderer) | createImageBitmap + ImageBitmapRenderingContext |
| P3-dialog | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): <dialog> show/showModal/close + returnValue + inert + возврат фокуса — dom.rs + layout | <dialog> модальные окна + атрибут inert |
| P3-popover | P3 | | done | | | реализовано (сверка с кодом 2026-07-02): Popover API showPopover/hidePopover/togglePopover + :popover-open — dom.rs + parser | Popover API (атрибут popover + ::backdrop) |
| P3-selectmenu | P3 | | planned | | | OPEN (сверка с кодом 2026-07-02, отдан P4): кастомный <select> appearance:base-select — CSS есть, HTMLSelectMenu нет | Кастомизируемый <select> (appearance: base-select) |
| P3-lazyembed | P3 | | done | | | Phase 0 ✅ 2026-07-02 (смешанное программирование, nano-omni worker + ревьюер): IframeInfo.loading_lazy/fetch_priority (dom) + ImageRequest.fetch_priority (layout), нормализация high/low по HTML LS §2.5.7; потребление приоритета шеллом (сортировка фетчей) и загрузка iframe — Phase 1 | loading=lazy для <iframe> + fetchpriority |
| P3-viewtransnav | P3 | | planned | | | PARTIAL (сверка с кодом 2026-07-02): SPA startViewTransition + ::view-transition-* готовы (layout); дошить — cross-document (MPA) навигационные переходы | View Transitions для cross-document навигации (MPA) |
| RP | P3 | | active | | | Открывать произвольные сайты так же, как Edge. RP-1…RP-4 done; RP-5…RP-9 заведены по аудиту 14 сайтов vs Edge 2026-07-02 (см. память realworld-site-audit-2026-07): внешние SVG, синтетический bold, анти-бот 403, CPU-растеризатор, print-стили | RP: Рендер-паритет реального веба |
| RP-1 | P3 | RP | done | M | | %-длины width/height/margin/padding в block-потоке резолвятся против containing-block (горизонталь+vertical pad/margin → cb-width; height → cb-height-if-definite). Уже было реализовано в движке (cb=available_width с 1B.1, definite-height threading с BUG-136); RP-1 закрепил поведение 5 регресс-тестами (box_tree.rs mod rp1_percentage_sizing) | RP-1: Проценты в block-потоке |
| RP-2 | P3 | RP | done | M | | layout-viewport отслеживает живой inner_size окна. Resized→resize→relayout уже был; RP-2 закрепил: relayout берёт CSS-viewport из content_layout_viewport (живой surface минус tab-strip + workspace-switcher по высоте), а не из полного окна — vw/vh/%/@media следуют за окном, headless остаётся 1024×720; 6 регресс-тестов | RP-2: Relayout под живой размер окна |
| RP-3 | P3 | RP | done | S | | gzip/deflate ContentDecoder (flate.rs) + объявлены в Accept-Encoding (`br, gzip, deflate`); GzipContentDecoder=MultiGzDecoder, DeflateContentDecoder=ZlibDecoder с raw-fallback; зарегистрированы на всех prod-сайтах HttpClient | RP-3: HTTP gzip/deflate декодер |
| RP-4 | P3 | RP | done | L | | float:left/right + clear для любых блоков: float-контекст пробрасывается во вложенные не-BFC блоки (line-box'ы сужаются вместо клипа), BFC-блоки сдвигаются, clear/вложенные float на глубине; drop-cap не регрессировал | RP-4: Общий float-поток |
| RP-5 | P3 | RP | done | L | | 2026-07-02: внешние SVG рисуются — `lumen_image::is_svg` сниффит разметку, шелл (`svg_image.rs`) оборачивает её в минимальный HTML и рендерит через обычный детерминированный pipeline (`parse_and_layout` → `paint_ordered` → `render_to_image_cpu`) в intrinsic-размере (width/height → viewBox → 300×150). Покрыты `<img>` (decode_image) и `background-image` (fetch_and_decode_background_images). Отложено: перерастеризация под CSS-размер бокса, currentColor хоста. Бриф docs/tasks/rp5-external-svg-images.md | RP-5: Внешние SVG-картинки |
| RP-6 | P3 | RP | done | M | | Срез A 2026-07-02: синтетический bold (двойной блит/повторный fill_text со сдвигом ≤2px, advance-метрики не меняются) + fake-italic (shear 12°: CPU — наклон контура глифа, femtovg — Transform2D вокруг baseline) как fallback в обоих текст-путях; настоящий bold/italic-фейс от FontProvider имеет приоритет (resolve_font_chain возвращает true_bold/true_italic). Срез B (Inter-Bold/-Italic в бандл, системный подбор в headless) отложен. Бриф docs/tasks/rp6-synthetic-bold-font-match.md | RP-6: Синтетический bold + подбор шрифта |
| RP-7 | P3 | RP | done | M | | 2026-07-04: root cause найден по коду — **HTTP/2-путь не слал fingerprint-заголовки**. `build_request_headers` (UA/Accept/Accept-Language/Accept-Encoding/Sec-Fetch/DNT) вызывался только в HTTP/1.1-ветке (`write_request`); H2-запрос (`h2.fetch`) уходил с одними псевдо-заголовками + cookie/cache/CORS → к Cloudflare-сайту приходил запрос без User-Agent = очевидный бот → 403. Фикс: `http::h2_fingerprint_headers` (тот же per-profile набор, что и H1, минус Host/Connection, lowercase) + `build_h2_headers` вклеивает в оба H2-пути (свежий и pooled) с dedup по имени и advertise Accept-Encoding (заодно включило сжатие на H2). Гипотеза 1: Chrome/Edge Accept навигации `*/*`→полный `text/html,...` (`CHROME_NAVIGATE_ACCEPT`). Гипотеза 3: порядок псевдо-заголовков H2 per-family (Chrome `:method :authority :scheme :path`). Cloudflare JS-challenge (`cf_clearance`) намеренно не трогали — отдельная связка cookie-jar+JS. Бриф docs/tasks/rp7-antibot-403.md | RP-7: Устойчивость к анти-бот 403 |
| RP-8 | P3 | RP | done | L | BUG-267 | Срез A 2026-07-03: dirty-bbox на слоях (CpuLayer), PopFilter/PushBackdropFilter кропают blur до bbox ink ⊕ 3r (бенч теней 169.5с → 1.5с, ×113). Срезы B–D 2026-07-03: clip/mask/opacity/blend-композиты по dirty-окну ⊕ 2px AA (бенч clip+mask+opacity+blend 40.1с → 0.34с, ×117); PNG байт-в-байт, 145/145 graphic_tests идентичны; transform-композит не кропается (bilinear не бит-идентичен). См. BUG-267 | RP-8: Ускорение CPU-растеризатора (layer bbox) |
| RP-9 | P3 | RP | planned | S | BUG-268 | Аудит 2026-07-02: шелл грузит `<link rel=stylesheet media="print">` в экранный каскад → print-стили протекают (URL после ссылок на w3.org). Гейт по media у `<link>`. См. BUG-268 | RP-9: Фильтр print-таблиц стилей |
| P3-tlshard | P3 | | planned | M | | phases.md §Фаза 3: OCSP stapling + Certificate Transparency log enforcement + invalid-cert UI. Negotiate/NTLM + client-cert mTLS отложены. Бриф docs/tasks/ph3-tls-security-hardening.md | TLS hardening: OCSP + CT + invalid-cert UI |
| P3-gpusandbox | P3 | | planned | L | | phases.md §Фаза 3: GPU process / sandbox (seccomp / AppContainer / App Sandbox), расширение site isolation. Бриф docs/tasks/ph3-gpu-process-sandbox.md | GPU process / sandbox |
| P3-gcjsdom | P3 | | planned | L | | phases.md §Фаза 3: cycle collector между Rust DOM и JS-движком (cross-boundary циклы). Бриф docs/tasks/ph3-gc-js-dom.md | GC integration JS ↔ DOM |
| P3-downloadui | P3 | | planned | S | | phases.md §Фаза 3: Download UI поверх существующего downloads storage (permission prompt UI уже готов). Бриф docs/tasks/ph3-permission-download-ui.md | Download UI |
| SDC | P1 | | done | | | Shell-as-driver-client: управление ЖИВЫМ видимым окном Lumen напрямую командами (Selenium/WebDriver-подобно, без headless-прокладки) — закрытие 8A.7 Фазы 4 по `docs/plans/8A.7-shell-as-driver-client-plan.md` + ADR-006. Разрез по владению: SDC-1a (P1 driver) → SDC-1b (P3 shell) → {SDC-2 (P1 фронты), SDC-3 (P3 графтест)}. Цепочка зависимостей строгая | SDC: Управление живым окном (8A.7 Ф4) |
| SDC-1a | P1 | SDC | done | M | | 8A.7 Ф4 (driver-часть): `click/type_text/scroll/eval/wait` в `crates/driver/src/winit_session.rs` реализованы headless-семантикой (click follows `<a href>`/toggles checkbox-radio `checked`; type_text пишет `value`; eval — one-shot QuickJS под `--features quickjs`, `install_dom` на снимке текущего DOM) + `AutomationCommand`/`AutomationReply` опубликованы из `lumen-driver`. Юнит-тесты `crates/driver/tests/cases/test_automation_commands.rs` | SDC-1a: WinitSession-команды (driver) |
| SDC-1b | P3 | SDC | done | M | | 8A.7 Ф4 (shell-часть): `AutomationCommand` подключён в живое окно (`main.rs` automation-dispatch блок, ~8000): Navigate/Click/Type/Scroll реальны; `Selector`-таргетинг заработал через `resolve_automation_target` (`lumen_layout::selector_query::find_all_by_selector`); Eval возвращает реальный `AutomationReply::Eval(json)` (`PersistentJs::eval_js_value`, использует `JsValue::to_json_string` из SDC-1a); Screenshot рендерит текущий `display_list` в PNG (`render_current_page_to_png`, CPU-путь); Wait поллится раз/кадр через `pending_waits` (не блокирует event loop) | SDC-1b: канал команд в живое окно (shell) |
| SDC-2 | P1 | SDC | done | M | | Протокол-фронты на живое окно через канал SDC-1b. `lumen-bidi-server` (`--bidi-port` + окно): `browsingContext.navigate`/`captureScreenshot`, `script.evaluate`, `input.performActions` (pointer+key) против `LiveWindowSession`; `lumen-mcp --mcp-live-port <N>` на тот же путь (`screenshot`/`eval`/`query`/`a11y_tree` больше не `Err`). Общий адаптер `lumen_driver::LiveWindowSession: BrowserSession` + `AutomationHandle`/`AutomationRequest` (запрос несёт свой канал ответа — SDC-1b реплаи раньше уходили в никуда). По ADR-006 BiDi+MCP, CDP опц./последним | SDC-2: BiDi/MCP на живое окно (MVP) |
| SDC-3 | P3 | SDC | done | M | | `graphic_tests/run.py --live`: один живой `lumen --mcp-live-port` процесс/окно на весь прогон вместо kill+relaunch на каждый тест (`LiveWindowClient`, MCP JSON-RPC — `navigate`+`wait(document_ready)` вместо блокирующего `time.sleep(LUMEN_WAIT_SEC=5)`); пиксельный снимок остался gdigrab-ом реального femtovg-окна (не CPU `resource://screenshot`, тот же разрыв паритета что у `--ipc`), поэтому проходят и JS-тесты (57, 129–138). TEST-00 калибрует crop offset один раз, как раньше. Валидировано полным прогоном: результат идентичен baseline (`Изменений нет`), 139 тестов за ~10.5 мин. По пути найдены и исправлены 2 бага живого окна, важных и для SDC-2: `AutomationCommand::Navigate` не поддерживал `file://` (всегда `PageSource::Url` → сетевой `HttpClient` → "unsupported scheme: file"); команда с BiDi/MCP-потока не будила запаркованный `ControlFlow::Wait` event loop (добавлен `LoadEvent::AutomationWake` + `AutomationHandle::set_wake`) | SDC-3: графтест на одном живом окне |
| P4-webgl | P4 | | active | | | | Подмножество WebGL (базовый CPU-контекст готов) |
| P4-mobile | P4 | | planned | | | | Mobile (Android NDK) |
| P4-sync | P4 | | planned | | | | Sync через E2E (self-host / P2P) |
| P4-kgraph | P4 | | planned | | | | Граф знаний (визуализация коллекции) |
| P4-l10n | P4 | | planned | | | | Локализация UI |
