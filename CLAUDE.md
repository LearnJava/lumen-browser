# CLAUDE.md

Project context for Claude Code. Auto-loaded each session. Keeps the assistant oriented without re-asking questions answerable from code or adjacent docs.

**This file is English-only.** All edits — including gotchas added by other sessions — must be written in English. Translate before committing.

Update this file whenever you change architecture, invariants, or policies.

---

## What is this

**Lumen** — private, lightweight, transparent browser in Rust with a custom engine. Not a Chromium/WebKit wrapper; a standalone rendering engine with an embedded JS engine.

Current phase: **Phase 2 — v0.5 «Interactive» (complete)**, app version **v0.5.0**. Phase 0 (prototype) closed 2026-05-26; Phase 1 «Reader» largely complete. Phase 2 delivered: QuickJS, Canvas 2D, CSS Grid, Shadow DOM, accessibility tree, forms, find-in-page, DevTools/CDP, knowledge layer.

**JS engine: V8 (`rusty_v8`) is the ONLY JS engine, full stop.** S12b-F1 (2026-08-04) removed the `quickjs` shell rollback feature; `QuickJsRuntime` itself was deleted in S12b-F2 (2026-08-04); `dom.rs::install_primitives` (the rquickjs native-registration entry point, 2736 lines) was deleted in S12b-F3 (2026-08-04); S12b-F4 (2026-08-04, closing the `P3-v8-s12b` track) removed the `rquickjs`/`rquickjs-core`/`rquickjs-sys` dependency from `crates/js/Cargo.toml` and `Cargo.lock` outright — `rquickjs` is gone from the workspace entirely, not just unused. Never target new functionality, fixes, or investigation at the rquickjs path (it no longer exists); the JS shim (`WEB_API_SHIM` in `crates/js/src/dom.rs`, `#[cfg(feature = "v8-backend")]`) is evaluated only by the V8 install path (`v8_runtime.rs::install_dom`) and is the right place for engine-independent fixes. Validate JS work against the default (V8) build.

### Versioning & phase policy

Single source of truth for the version is `[workspace.package] version` in `Cargo.toml`. All machine-readable version strings (User-Agent, Sec-CH-UA, CDP `Browser.getVersion`, window title, startup banner) derive from `CARGO_PKG_VERSION` — do **not** hardcode a version number in code. The one manual-bump site is the `navigator.userAgent` literal in `crates/js/src/dom.rs` (JS shim string).

Version↔phase mapping (from `docs/plan/phases.md`): Phase 1 → v0.1, **Phase 2 → v0.5** (target on phase completion), Phase 3 → v1.0. Mid-phase the version climbs toward the target (Phase 2 reached its **0.5.0** target). Reaching Phase 3 → 1.0.0. Keep the phase label in sync across `README.md`, `docs/plan/phases.md`, this file, and the shell startup banner.

| File | Contents |
|---|---|
| `CAPABILITIES.md` | **Source of truth for "what the browser can do right now"** (per-subsystem, ✅/🟡/⬜, verified against code). Read ONLY this for capability questions — not `docs/plan/*` or `STATUS-PN.md`. Update in the same commit as a feature merge. |
| `README.md` | User-facing: install, commands, what to expect. |
| `STATUS-PN.md` | **Bare pointer lines `<source>:NN` and nothing else** — one line per open task, priority top→bottom, no headers/prose/completed tasks (schema: `docs/dev-roles.md` §Task tracking schema). `<source>` = ROADMAP.md (P1/P2) · BUGS.md (P3) · CSS-SPECS.md (P4) · a code `file:line` for a `// CSS:` / `// BUG-NNN` handoff. Read yours at session start. Detail belongs in the source row, `docs/tasks/<id>.md`, or `bugs/BUG-NNN-*.md` — never here. Exception: `STATUS-P5.md`, whose source is a health sweep rather than a row list (alias→action table, format still provisional). |
| `lumen-plan.md` | TOC index: links to 11 section files in `docs/plan/`. Read for architecture; for daily status use `STATUS-PN.md` instead. |
| `docs/plan/` | Design doc split into 11 files: architecture, tech-stack, engine, web-apis-shell, privacy, features, knowledge, security-performance, testing, phases, meta. (The former `roadmap.md`/`history.md` were deleted 2026-07-02 — task status lives in `ROADMAP.md`, chronology in `git log`.) |
| `CSS-SPECS.md` | Complete CSS property & spec roadmap: all W3C modules, per-property status (✅🟡⬜🚫), P4 priority queue. |
| `docs/wpt-status.md` | WPT readiness: all 277 upstream top-level categories (scope ⬜/🚫, vendored status), plus a per-test detail table for the one vendored category (`dom/nodes`, 168 tests) with pass/fail and an assignable Владелец/Баг column. Regenerate the detail table with `tests/wpt/gen_status_md.py` after a fresh `run_report.py --all` run — read the file's own "Как обновить" section, not this line, for the exact commands. The category index's "Заметка" cell must stay a one-sentence summary + link — full per-category writeups live in `docs/wpt-vendor-notes/<slug>.md` (2026-08-09 split, see that section's own note for why). |
| `docs/wpt-vendor-notes/` | One file per vendored WPT top-level category (`<slug>.md`, `/` → `-`), holding the full vendoring writeup that `tests/wpt/VENDOR.md`'s and `docs/wpt-status.md`'s category rows used to carry inline — split out 2026-08-09 once both files' row cells had grown past the `Read`-tool size limit. Linked from both tables; create alongside a new short row, don't grow the row itself. |
| `docs/build-speed.md` | Compile-time optimization plan: current baseline, measurement protocol (S1–S5), ranked measures (stable / nightly / rejected), benchmark journal. Read before changing build config (profiles, `.cargo/config.toml`, sccache). |
| `docs/ci-offload.md` | **Plan for moving part of the task gate to GitHub Actions** (the repo is public, so standard runners are free and unlimited): what moves and what stays local, cache/checkout/concurrency strategy under the 10 GB cache cap, how the assistant consumes run results via `gh`, and the rollout order. **Mostly implemented as of 2026-08-20** (six jobs in `ci.yml` including `artifact` (CI-5, dev-release upload on push-to-main), cache/checkout/concurrency, `bench-gate.yml` deleted; §8 realigned with the per-commit merge rule, CI-6; PERF-7 landed as its own `perf-gate.yml`, all three 2026-08-20); the only remaining tail is `snapshot-cpu` as a blocking gate, blocked on [BUG-784](bugs/BUG-784-OPEN.md). Read before touching `.github/workflows/*` or proposing "let CI do it". |
| `docs/lint-policy.md` | **Plan for turning this file's prose policies into machine-checked lints** (no `unwrap`/`panic` in prod, `// SAFETY:` on every `unsafe`, `///` on public items): ready-made clippy rules via `[workspace.lints]`, `clippy.toml` deny-lists, custom `dylint` rules, rollout protocol (one lint per commit, `warn` → fix → `deny`) and journal. Also records why forking rustc and the nightly compile-speed measures were rejected. Read before adding a new "must"/"forbidden" rule here — a rule a lint can check belongs there, not in prose. |
| `docs/perf-method.md` | **How to measure, gate and accept a performance change** (census before the fix, profile the path you change, gate by counter/identity not wall-clock, interleaved A/B compared on min, instrumentation traps). Distilled from the 27 slices of BUG-341. Read before a perf task; the numbers themselves stay in `bugs/BUG-NNN-*.md`. |
| `docs/automation.md` | **All automation/introspection surfaces of the browser and when to apply them** (dump modes, `--deterministic`, MCP tools/resources, BiDi, IPC, driver-API, `LUMEN_NO_*` paint-bisect flags, known stubs). Read before writing a debugging script or a new test harness — the capability usually already exists. |
| `docs/roadmap-trees.md` | **How to use the interactive roadmap trees** (`docs/roadmap-*.html`): open in a browser, filters/search, and how to keep them current (`ROADMAP.md` + `python scripts/gen_roadmap.py`, auto-pulls bug status from `BUGS.md`). |
| `ROADMAP.md` | Flat, grep-friendly source of the phase/task tree (two markdown tables: phases + tasks, one task per line). Feeds `gen_roadmap.py`; replaced the old nested `docs/roadmap.json`. Bug↔task links live in its `bugs` column; CSS-module status is live-aggregated from `CSS-SPECS.md` into rows `css-specs-t0`…`t4` (note = `AUTO:CSS-SPECS:T<N>`, do not hand-edit that note). |
| `CLAUDE.md` | (this file) Conventions and invariants for the assistant. |
| `REVIEW.md` | **Code-review policy** — what a reviewer blocks a change on (hard gates), architecture boundaries, style/design rules, docs that must move in the same commit, and what is explicitly out of scope for review. Read before reviewing someone's diff; the rules restate this file and `docs/conventions.md` in reviewer form, so on conflict trust the code and flag the change. |
| `docs/decisions/` | Formal ADR files (one per architectural decision). See README.md + TEMPLATE.md inside. |
| `DECISIONS.md` | Historical decisions (pre-ADR format). Read-only — add new decisions to `docs/decisions/` instead. |
| `samples/page.html` | Test page for pipeline runs. |
| `assets/fonts/Inter-Regular.ttf` | Bundled font (SIL OFL 1.1). |
| `assets/fonts/Ahem.ttf` | Bundled font (3-Clause BSD, `assets/fonts/LICENSE-Ahem.txt`) — deterministic reftest font (TEST-5), vendored unmodified from `tests/wpt/fonts/Ahem.ttf`. Every glyph is a 1em solid square; picked up by lumen-font's asset-dir scan like any other bundled family, no dedicated registration code. |

---

## Working boundary

**Write code only inside the browser folder** — `D:\RustProjects\lumen-browser\` and its worktree copies in `.claude/worktrees/*`. Same applies to docs, configs, snapshot tests. Everything outside — `~/.bashrc`, `~/.config/*`, system dotfiles, sibling projects, **ad-hoc worktrees like `../lumen-<task>/`** — do not touch. If a task requires external changes, describe what the user should do and wait for approval.

`git worktree add` follows the same rule: path must be `.claude/worktrees/<task-name>/` (inside the browser folder), **not** `../lumen-<task>/` or anywhere outside.

Exception: Claude memory (`~/.claude/projects/.../memory/`) lives outside the repo by design — the boundary rule does not apply to it.

---

## Developer assignments

Full role definitions, workflows, collaboration rules, task tracking schema — [`docs/dev-roles.md`](docs/dev-roles.md).

**If the user says "you are developer N" at session start — read `STATUS-PN.md` and take the first pointer line. If a `p<N>-…` branch already exists for you (`git branch`), continue that task instead. If all your tasks are taken — ask the user which task to take next.**

**Take the list strictly top-down — never skip a line because a lower one looks smaller, clearer or more fun.** The order is not a preference list: it encodes dependencies. A lower line is often the *symptom* of a higher one (e.g. BUG-425 «sidebar items on one line» is caused by BUG-333 `height: var()` → `h=0`), so working out of order means fixing a symptom on top of a live defect and re-doing it later. If the first line genuinely cannot be started (blocked by another role, needs a user decision), say so explicitly and ask — do not silently move down the list.

| Developer | Domain | Crates |
|---|---|---|
| **P1** | General feature development (source → layout → paint → shell), taken top-down off `STATUS-P1.md`. Finished tracks: DS (design system v3.3, DS-1…DS-19, `docs/design/lumen-v3_3.html`). Current: the CC track (engine chrome) — `STATUS-P1.md` is ordered as one dependency chain «engine roots → chrome visuals → chrome interaction», engine-root bugs that only the chrome exposes (BUG-333/433/431/343/288) were moved here from P3 on 2026-07-29 so one role owns the whole chain. BUG-341 (incremental restyle) is paused by user decision 2026-07-28 — resume only on explicit request | All crates (coordinated with P2/P4) |
| **P2** | **Reactivated 2026-07-13**: leads P2-wpt (WPT via `wptrunner` + WebDriver BiDi, `docs/tasks/p2-wpt-integration.md`) and the DEVX track (dev-tooling on existing automation surfaces, `docs/automation.md`, ROADMAP.md DEVX-1…6, assigned 2026-07-16). Was reserve (since 2026-06-18). **Owns the CI track since 2026-08-19** (`docs/ci-offload.md`, ROADMAP.md `CI-5`/`CI-6`/`PERF-7`), handed over by P5 whose role forbids the behaviour changes the tail needs. | `lumen-bidi-server`, `lumen-driver`/`lumen-mcp` (DEVX-5), Python tooling `tests/wpt/` + `graphic_tests/run.py` (DEVX-1/4), `.github/workflows/*` |
| **P3** | **Bug fixes ONLY**: BUGS.md OPEN items, graphic test regressions | All crates (read-only except bug fixes) |
| **P4** | **CSS properties ONLY**: parsing, ComputedStyle, cascade, end-to-end wiring | `css-parser`, `layout` (style.rs), `paint` (display_list.rs) |
| **P5** | **Code health ONLY**: audit, workspace-clippy, stub/branch/docs/dep sweeps, safe mechanical cleanup | All crates (read-only except trivial clippy fixes in own crate + branch/worktree/SYMBOLS.md cleanup) |

**Task reservation:** create the `p<N>-<id>` branch — its existence is the reservation signal. A parallel session sees it via `git branch` and skips the task. Details — `docs/dev-roles.md`.

---

## Project Skills

6 skills in `.claude/skills/`. Use them instead of following protocols manually:

| Skill | When to use |
|---|---|
| `/lumen-add-css-property` | Adding a new CSS property to `lumen-layout` |
| `/lumen-task-start <name>` | Starting a new roadmap task (creates worktree + reserves in plan) |
| `/lumen-task-finish <name>` | Task ready to merge (clippy → tests → merge --no-ff → worktree remove) |
| `/lumen-new-crate <name>` | Creating a new Cargo crate in the workspace |
| `/lumen-health-check [target]` | P5 maintenance sweep (`full`/`clippy`/`stubs`/`branches`/`docs`/`deps`/`dupes`) |
| `/lumen-perf-audit` | Real-site performance audit (PERF track): run the corpus, collect per-phase stats, diff vs previous run, journal + file bugs |

`lumen-task-start` — explicit invocation only (`/`).
`lumen-add-css-property`, `lumen-new-crate`, `lumen-health-check`, `lumen-perf-audit`, and `lumen-task-finish` — Claude may invoke automatically from context.

---

## Commands

Full reference (token efficiency, OS detection, PATH setup) — [`docs/commands.md`](docs/commands.md).
Automation & diagnostics (dumps, deterministic mode, MCP/BiDi/IPC drive, paint-bisect env flags) — [`docs/automation.md`](docs/automation.md).

```bash
export PATH="/c/Users/konstantin/.cargo/bin:$PATH"          # Git Bash only

cargo check -p lumen-layout                                  # fast check, 1-2s
cargo clippy -p lumen-layout --all-targets -- -D warnings   # required before commit
cargo test -p lumen-font                                     # crate tests
cargo run -p lumen-shell -- samples/page.html               # run with test page
cargo run -p lumen-shell -- --dump-layout samples/page.html # headless layout dump
cargo run -p lumen-shell -- --dump-display-list samples/page.html  # headless paint dump
```

**Session start protocol:**
1. **`git pull origin main` first, before reading STATUS files or creating a branch/worktree.** Parallel Claude Code sessions push to `origin/main` independently, so local `main` can lag behind — starting from stale state means stale `STATUS-PN.md`/`ROADMAP.md` reads and, if the lag is large, a big multi-file merge conflict later instead of a small one now. If `pull` reports a diverged history with real conflicts (not just a fast-forward), resolve them file-by-file with full understanding of both sides' intent — do not blindly take one side — and re-verify with `cargo check`/`clippy`/scoped tests for every touched crate before committing the merge.
2. Read `STATUS-PN.md` — pointer lines to open tasks; `git branch` shows any `p<N>-…` task in progress
3. Run `git branch` — verify you're on main
4. Architecture context → `docs/plan/architecture.md` §1, §3; decisions → `docs/decisions/README.md`

**Push after every commit, not at session end.** Each commit is merged into `main` and pushed to `origin/main` as soon as it is made (see "Git workflow" below) — nothing waits for the end of a task or a session, because other parallel sessions rely on seeing it to avoid duplicate or conflicting work.

**Cargo output rules:** always `-p <crate>`, never `--workspace` (exception: P5). Success → 1 line. Errors → full `error[...]` block, skip all warnings. Test failure → test name + first 10 lines.

**Run discipline (details in `docs/commands.md`):** one cargo run — one log file (`> .tmp/<name>.log 2>&1`, then grep the file; never re-run cargo just to re-filter output). During iteration `cargo check -p` only; one `clippy -p` + targeted tests before the commit; full gates (workspace clippy + scoped-test) run exactly once inside `/lumen-task-finish`, synchronously in the foreground — never as background tasks. Don't hand-run expensive crate tests before the gate — `scoped-test.sh` repeats them.

**Long runs you are not blocked on** (baseline builds, `test --no-run`, the graphic pipeline) go to the background via the Bash tool's `run_in_background: true` — the harness notifies you on exit, so keep working meanwhile. Never `cmd &` **plus** `run_in_background` (silently kills the process) and never poll with `sleep N` — that pattern burned 76 of 188 minutes on 2026-07-27. Foreground-only exceptions: the final gates, and the full graphic pipeline (gdigrab needs a focused window; backgrounded, TEST-00 fails "magenta marker not found" and cascades).

---

## Graphic tests

Full documentation (magenta frame pattern, test layers, run flags, KNOWN_DEBTORS, run rules) — [`docs/graphic-tests.md`](docs/graphic-tests.md).

`graphic_tests/NN-*.html` — 70+ pages, viewport 1024×720. Graphics only, no text.

```bash
python graphic_tests/run.py --continue-on-fail   # run all, collect results
python graphic_tests/run.py --only 03            # single test
python graphic_tests/run.py --bisect 100         # diagnose interaction test
```

**Adding a new CSS property** (same commit as implementation):
1. Add to relevant test in `02–20` (or new file with magenta frame pattern)
2. Add demo to `graphic_tests/1000000-final.html`
3. Update `graphic_tests/COVERAGE.md`
4. Add entry to `TESTS` in `graphic_tests/run.py`
5. **If the property affects paint/rasterization**, regenerate the deterministic CPU snapshot references in the **same commit**: `SAVE_CPU_SNAPSHOTS=1 cargo test -p lumen-driver --features cpu-render cases::snapshot_cpu` (then review the changed PNGs are correct, not garbage). Skipping this drifts `graphic_tests/snapshots/cpu/` on unrelated pages and later red-lights the `scoped-test.sh` gate for someone else — the recurring BUG-118 / BUG-149 / BUG-297 / BUG-316 staleness. **The PNGs are not the only golden set** — a change to the *geometry or order* of emitted commands also drifts the textual display-list snapshots in `crates/engine/paint/tests/snapshots/*.snap` (`UPDATE_SNAPSHOTS=1 cargo test -p lumen-paint --test all <name>`, then read the diff — it is three lines, so a wrong rect is obvious). Those are a plain `cargo test -p lumen-paint` away, i.e. they red-light step 1 of the task checklist for every role that touches paint, not just the graphic gate: BUG-816 left `main` red for a day that way.

**When the full ~20-min run is required:** anything that can move pixels — paint/display list, layout geometry, a CSS property, font/text/image. For changes that cannot alter the display list (pure perf, refactors, tooling, docs) the gate is `scripts/scoped-test.sh` (includes the deterministic CPU snapshots) + `python graphic_tests/dump_golden.py`; claiming display-list neutrality requires showing an empty `dump_golden.py` diff. Details — [`docs/graphic-tests.md`](docs/graphic-tests.md).

**Hard rules:** never edit test pages to work around engine limits; never change thresholds (0.5% for all); no screenshots committed.

---

## Architecture

Dependency graph and crate scope — in [`docs/plan/architecture.md`](docs/plan/architecture.md) §3. Direction: `lumen-core` → dom/font/parsers → layout → paint → shell. No cycles.

### Extension traits (`lumen-core::ext`)

Full list with implementations — [`subsystems/core.md`](subsystems/core.md). Planned: `WindowingBackend`, `RenderBackend`, `TlsBackend`.

---

## Principles

Full list (8 items) — [docs/plan/architecture.md](docs/plan/architecture.md) §1.

---

## Dependency policy

Full tables (permanent + provisional + Lumen core) — [docs/plan/tech-stack.md](docs/plan/tech-stack.md) §5.

### No new dep without justification

Every new `[dependencies]` entry requires this in the commit body:

> **Why this dependency:** \<category (permanent / provisional), trait-anchor, graduation criterion if provisional\>

---

## Code conventions

Full details (style, tests, error handling, unsafe) — [`docs/conventions.md`](docs/conventions.md).

- **Rust pinned to 1.97.0** (`rust-toolchain.toml`, since 2026-08-19 — it was a floating `"stable"`, which handed developers and CI runners different lint sets and made clippy green locally / red in CI). Edition 2024, resolver "3", MSVC on Windows. Raise the pin in its own commit after a clean `RUSTC_WRAPPER= cargo clippy --workspace`: a version bump invalidates every artifact in `target/` (stale ones surface as `E0786 "only metadata stub found for rlib dependency core"`, which reads like a broken dependency but is cured by `cargo clean`) and can add new lints.
- `cargo clippy -p <crate> --all-targets -- -D warnings` must pass before every commit.
- **sccache is off** (`rustc-wrapper` commented out in `.cargo/config.toml`, 2026-08-19): version 0.15.0 crashes every compiler invocation under toolchain 1.97.0 with `STATUS_STACK_BUFFER_OVERRUN` (`0xc0000409`) on crates with a long command line — `rustc` as well as `clippy-driver`, so it breaks plain `cargo test`, not just linting. Re-enable only after sccache is updated and `cargo test -p lumen-layout` passes through it. CI never used the wrapper.
- **`///` doc comments on all public structs, fields, and functions** — mandatory and **machine-checked** since 2026-08-18 (`missing_docs = "deny"` in `[workspace.lints.rust]`). Pre-existing debt (1866 items) is held behind **file-scoped** `#![allow(missing_docs)]` in 119 files, so a *new* file must document its public API even in a crate that still owes docs; the per-crate counts live in `docs/lint-policy.md` §10.
- No `panic!` / `unwrap()` / `expect()` in production code; allowed in tests. **All three machine-checked** since 2026-08-18 (`clippy::panic`, `clippy::unwrap_used`, `clippy::expect_used`, all `deny` in `[workspace.lints]`). `clippy.toml`'s `allow-*-in-tests` frees only the body of a `#[test]` fn, so test roots and `#[cfg(test)] mod`s carry their own `#![allow]`. Grandfathered sites carry a function-scoped `#[allow]` (an `impl`-scoped one where the function could not be identified), never a crate-scoped one; every allow is listed in `docs/lint-policy.md` §10.
- `unsafe` forbidden outside FFI boundaries; every block requires `// SAFETY:` comment. **Machine-checked** since 2026-08-18: `clippy::undocumented_unsafe_blocks = "deny"` in `[workspace.lints]`, so a missing comment is a build error, not a review finding. One comment above two adjacent `unsafe impl`s does not count as documenting the second.

All three prose policies above are now enforced by `[workspace.lints]` — tier 0 of [`docs/lint-policy.md`](docs/lint-policy.md) was completed on 2026-08-18 (five rules enabled, one rejected on measurement). What remains is the grandfathered debt behind `#[allow]`, listed per crate with owners in that file's §10. Rules are enabled in `[workspace.lints]` (root `Cargo.toml`), configured in `clippy.toml`; every member crate opts in with `[lints] workspace = true`. **A new crate must carry that stanza** or it silently escapes every project lint — `/lumen-new-crate` adds it.
- Names: `snake_case` functions/fields, `PascalCase` types, `SCREAMING_SNAKE` constants.

---

## Git workflow

Full protocol (commits, worktree isolation, zombie worktree, 7-step checklist) — [`docs/git-workflow.md`](docs/git-workflow.md).

**All work happens in feature branches. Direct commits to `main` are forbidden.**

Branch naming: `p<N>-<task-name>` (P1–P5 prefix mandatory). `--no-ff` required on merge. Commit message in Russian, subject under 80 chars, body explains *why*.

**Forbidden:** direct commit to main · force-push · rewriting history · `git config` · `--no-verify` · `git push` without explicit user request.

**Every session MUST work in its own `git worktree`** — use your **persistent pool slot**, one per developer, instead of creating a worktree per task:

```bash
cd "$(bash scripts/worktree-pool.sh p<N>-work p<N>-task-name | tail -1)"   # occupy
bash scripts/worktree-pool.sh list                                        # who holds what
bash scripts/worktree-pool.sh release p<N>-work                           # free (step 3 of the checklist)
```

The slot (`.claude/worktrees/p1-work` … `p5-work`, plus `perf-base`) is created once and reused — only the branch changes, so `target/` stays warm and rebuilds are incremental. `git worktree remove` after every task deleted that cache and cost 9–15 min of cold build per task, plus 3 min for the `add` itself. Build **only `dev-release`** inside a slot (warm `dev-release` ≈ 4.7 GB; five slots on `debug` would be ~70 GB). The script refuses to switch a slot with uncommitted or unmerged work. Details — [`docs/git-workflow.md`](docs/git-workflow.md).

**Merge and push after EVERY commit** (user, 2026-08-19). Do not accumulate commits on a branch until the task is done: local gate → commit → `git merge --no-ff` into `main` → `git push origin main`, per commit. Feature branches and `--no-ff` stay mandatory; what changed is the cadence. Rationale: unpushed work does not exist for anyone else — on 2026-08-19 several roles held unmerged branches at once, the root checkout trailed `origin/main` by 21 commits, and parallel sessions duplicated work and collided on bug numbers. Frequent small merges also trade one large conflict for several trivial ones.

**Do not wait for CI before merging** (same decision). The local gate is the only pre-merge check; watch CI on `main` afterwards and fix it if it goes red. Waiting per-commit would cost ~30 min each.

**Task-completion checklist** (all mandatory, full details in `docs/git-workflow.md`). Steps 1–3 already ran per commit under the rule above; at task end they cover whatever the last commit left:
1. `cargo clippy -p <crate> -- -D warnings` + `cargo test -p <crate>`
2. `git merge --no-ff p<N>-task-name -m "Merge …"`
3. `git push origin main`
4. Delete pointer line from `STATUS-PN.md`, commit — then merge and push it too
5. `bash scripts/worktree-pool.sh release p<N>-work` then `git branch -d p<N>-task-name` (a slot holding the branch makes `branch -d` fail)
6. Pool slot — nothing to remove (freed in step 5). Ad-hoc worktree — `git worktree remove .claude/worktrees/<task-name>`
7. `git push origin --delete p<N>-task-name` if the branch was ever pushed — the remote task branch has served its purpose

**When the root checkout blocks the merge.** `main` is checked out in the repo root and often carries someone else's uncommitted files, so `git merge` there fails on paths you touched. Merge in a throwaway worktree instead — `git worktree add .claude/worktrees/merge-tmp -b <tmp> origin/main`, merge, `git push origin HEAD:main`, remove it. Note the local `main` then trails `origin/main`, which also makes `worktree-pool.sh release` refuse to free the slot (it compares against local `main`); free it with `git checkout --detach origin/main` inside the slot.

---

## Communication

- **Reply language: Russian.** The user speaks Russian.
- **Tone: technical, no emoji** unless the user uses them.
- **Brief and direct.** Short answer + what was done. No marketing text.
- **Files as clickable links:** `[lumen-plan.md](lumen-plan.md)`, `[crates/engine/font/src/rasterizer.rs:48](crates/engine/font/src/rasterizer.rs)`.

### Banned words

"Wikipedia" / "Википедия" — user explicitly asked not to use. Say "reference article", "external article", "external page" instead.

---

## Doc sync rules — update matrix

Full rules (what NOT to update, what needs no update) — [`docs/doc-sync.md`](docs/doc-sync.md).

Update docs **in the same commit** as the code change. Use `grep -n` to find the line, then targeted `Read offset=N limit=10` + `Edit`.

| Change type | Files to update | What exactly to do |
|---|---|---|
| New feature / capability | `CAPABILITIES.md` + `subsystems/<crate>.md` | ⬜/🟡 → ✅; append bullet to Done section |
| New feature / capability | `STATUS-PN.md` | delete completed task's pointer line |
| Bug fixed | `BUGS.md` | `OPEN` → `FIXED <date>` |
| CSS property (P4) | `CSS-SPECS.md` + `CAPABILITIES.md` | ⬜ → ✅ |
| New dependency | `docs/plan/tech-stack.md` | append row |
| Architectural decision | `docs/decisions/ADR-NNN.md` | new file from TEMPLATE.md; update index |
| Known gotcha found/fixed | `CLAUDE.md` → "Known gotchas" | append/remove bullet |
| Perf slice merged with a transferable lesson | `docs/perf-method.md` | append the rule (one paragraph, slice ref in brackets); per-slice numbers stay in `bugs/BUG-NNN-*.md` |
| New public API — **or any edit that shifts line numbers under `crates/`** | `SYMBOLS.md` | `python scripts/gen_symbols.py`. The index stores `file:line`, so inserting a comment or an attribute drifts it just as much as adding an item. Since 2026-08-19 the CI `doc-drift` job is **blocking** and fails on the diff, so this is no longer optional bookkeeping. |
| Roadmap/bug/CSS-module status change | `ROADMAP.md` → `python scripts/gen_roadmap.py` | edit ROADMAP.md if structure changed; CSS-module status alone needs no edit — the script re-pulls it from CSS-SPECS.md |

---

## Subsystem state

Per-crate state (scope, done, deferred, invariants) — [SUBSYSTEMS.md](SUBSYSTEMS.md) (index) → `subsystems/<crate>.md`. Update the relevant crate file on every plan-item commit.

---

## Decisions log

**New decisions** — one ADR file per decision in [`docs/decisions/`](docs/decisions/), using the template at [`docs/decisions/TEMPLATE.md`](docs/decisions/TEMPLATE.md). Update the index table in [`docs/decisions/README.md`](docs/decisions/README.md).

**Historical decisions** (pre-ADR format) — [`DECISIONS.md`](DECISIONS.md). Do not add new entries there.

---

## Unique features (§12)

Full list with phases — [docs/plan/knowledge.md](docs/plan/knowledge.md) §12.

---

## Known gotchas

- **Cargo.lock is committed** (workspace includes a binary).
- **The committed `.cargo/config.toml` sets `build.rustc-wrapper = "sccache"`, so *any* environment without `sccache` on PATH fails instantly on every cargo command** — `error: could not execute process 'sccache …/rustc -vV' (never executed)`, before a single crate is compiled. This kept GitHub Actions red for two months: the config landed 2026-06-17 (`2ffbee511`), and `ci.yml` failed on all three OS at `cargo check -p lumen-shell` in every one of 1187 runs on `main` — nobody looked, because nothing surfaced the failure locally. Found 2026-08-18 (P5) while auditing CI for `docs/ci-offload.md`. Fixed by `RUSTC_WRAPPER: ""` in the `env:` of `ci.yml`/`release.yml`/`bench-gate.yml` (the last of these was itself deleted on 2026-08-19, `docs/ci-offload.md` §11.5 — don't go looking for it; an empty value overrides the config and disables the wrapper — verified A/B against `sccache --show-stats`, remembering that the counter lives on the server at `SCCACHE_SERVER_PORT=4444` set by that same config, not on the default port). **Consequence for any new automation** — a fresh container, a second machine, a colleague's checkout: install sccache or export an empty `RUSTC_WRAPPER`, otherwise cargo dies with a message that names sccache but reads like a toolchain failure.
- **A newline inside any `ROADMAP.md` table cell silently truncates the whole task table.** `gen_roadmap.py::_table_rows` collects rows "until the first line not starting with `|`", so a row split across two lines makes every row below it invisible to the generator — no error, no warning, the trees just render fewer tasks. Found 2026-08-19: the `WPT-RUN-3` row had been broken in two, hiding 19 tasks (the generator's own summary went 247 → 273 once repaired), and rows appended later landed in the dead zone. One task = exactly one line; after editing `ROADMAP.md` check the generator's final "всего задач: N" line, not just that it exited cleanly.
- **Line endings:** `.gitattributes` enforces LF. Git warning about CRLF→LF is normal.
- **Archives in repo root are gitignored** (`/*.zip`, `/*.tar*`). Downloaded files won't accidentally get committed.
- **Portable user data dir (`<exe_dir>/data/`).** The ad-block external-filter-list subsystem stores its data under `<exe_dir>/data/adblock/` (SQLite `adblock.db` for subscriptions + list metadata; `lists/<slug>.txt` bodies; `custom-rules.txt`) — see `shell/src/adblock.rs::browser_data_dir`. This is a **provisional** convention (user decision 2026-06-16): keep everything in the browser folder, do **not** use OS dirs (`%APPDATA%`/`~/.config`/`~/.cache`) or `lumen_cache_dir()`/`config_path()` for portable data. New subsystems needing portable data should add their own `data/<subsystem>/` subfolder via `browser_data_dir()`.
- **`gdigrab` captures the whole desktop, not just the Lumen window — any OS focus change during a `graphic_tests/run.py` run (live-window default, SDC-4) silently corrupts every screenshot from that point on.** Found 2026-07-28 while validating SDC-4: a live-window full run showed 72/149 tests FAIL at 85–93% diff, all starting from one point onward; the "Lumen" screenshots turned out to be pictures of the Claude Code chat window itself — the OS foreground had shifted there (a chat message arrived mid-run) and `_bring_pid_to_front`'s alt-key trick never reclaimed it for the rest of the run. Not a regression from SDC-4 — the same `gdigrab`+bring-to-front mechanism was already used by the old per-test-process default, so the risk is inherent to the whole pipeline, not new. Unlike the backgrounded case (which fails loudly at TEST-00, "magenta marker not found"), a mid-run focus steal fails silently as a wall of unrelated-looking high-diff FAILs — don't diagnose those as engine regressions before checking whether anything touched the desktop (including typing into this chat) while the run was in flight. Re-run clean (no desktop interaction) before trusting a graphic-tests result with many simultaneous large-diff failures.
- **A top-level `/resources/testdriver.js` (repo root, sibling of `tests/`/`crates/`, not under `tests/wpt/`) is not clutter — do not delete or relocate it.** `tools/wptrunner/wptrunner/environment.py::get_routes` hardcodes the `/resources/testdriver.js` static route as `[<repo_root>/resources/testdriver.js, executors/message-queue.js, testdriver-extra.js]` concatenated, where `repo_root` is computed by walking three `os.pardir`s up from `environment.py`'s own path. In upstream wpt that lands on the wpt checkout root (where `resources/testdriver.js` really lives); in this repo, where `tools/wptrunner` is vendored under `tools/` instead of at the repo root, the same walk lands on the *Lumen* repo root instead — an accident of vendoring depth, not a deliberate layout choice. Not overridable via `env_options()` the way `testharnessreport` is (no such hook on this route) and `tools/wptrunner` is not patched (unmodified-vendor rule), so this is the only place the file can live for `test_driver.*` support (WPT-RUN-2) to work at all — see `tests/wpt/VENDOR.md`.
- **Parallel sessions in the same working tree = disaster.** Two sessions doing `git checkout` of different branches causes git to stash one session's work. Recovery via `git stash pop` is fragile. **Solution: mandatory `git worktree`s** (see Worktree isolation above). If you find yourself on a foreign branch — check `git stash list` before running `git restore .`.
- **~~Live-window BiDi/MCP `script.evaluate` can hang indefinitely~~ — misdiagnosis, root-caused 2026-07-17 (P2-wpt S4).** The "`script.evaluate` reports `JS context not available` forever" symptom previously attributed to an environment-dependent JS-runtime-install race was actually three separate, fully deterministic engine/shell bugs, none of them environment-flaky: [BUG-298](bugs/BUG-298-FIXED.md) (`Element`/`DocumentFragment`/`ShadowRoot`.querySelector(All) searched the whole document instead of the calling node's subtree, so any code building a detached DOM subtree and querying into it — e.g. `testharness.js`'s own results-table renderer — silently got nothing), [BUG-299](bugs/BUG-299-FIXED.md) (`Element.prototype.insertAdjacentText` was missing entirely, thrown from the same code path), and [BUG-300](bugs/BUG-300-FIXED.md) (`browsingContext.navigate`'s `DocumentReady` wait could ACK using the *previous* page's stale `layout_box` before the new page had even started loading). All three are now fixed. The `run_smoke.py`-only follow-up timeout was [BUG-301](bugs/BUG-301-FIXED.md), **now fixed too (2026-07-18)**: `wptrunner` registers a static route for `/resources/testharnessreport.js` serving its *own* `__wptrunner_message_queue` report, which wins over the on-disk file — so Lumen's vendored report (the one that sets `window.__lumen_wpt_results`, the global `LumenTestharnessExecutor` polls) was never served under `wptrunner`+`wptserve`, hence "works manually over a plain HTTP server, times out under wptrunner". Fixed by `browsers/lumen.py::env_options` pointing `testharnessreport` at Lumen's own report file. `--bidi-port`/`--mcp-live-port` session-restore racing (a separate, unrelated mechanism) was already fixed by [BUG-296](bugs/BUG-296-FIXED.md). Gotcha when re-checking WPT: a **stale `target/dev-release/lumen.exe`** will mimic an unrelated empty-subresource-fetch bug — rebuild before trusting a `run_smoke.py` failure.

- **~~A navigation that fails to load reports *success* and silently keeps the previous document~~ — fixed 2026-08-23** ([BUG-438](bugs/BUG-438-FIXED.md)). `browsingContext.navigate` (BiDi) and the MCP `wait{document_ready}` used to answer success for an unreachable URL exactly as they do for a real one, because `check_wait_condition`'s BUG-308 early-out ("a settled navigation error is done loading, so a hung wait doesn't block forever") resolved the wait as a plain `Ack`, not distinguishing "loaded" from "settled in error". Root-caused to `about_to_wait`'s `pending_waits` drain (`crates/shell/src/main.rs`): it now sends `AutomationReply::Error` there instead, the same way a wait *timeout* already did — `bc_navigate` and the MCP `wait` tool already turned an `Err` into a protocol error, so neither needed a change. Covers any failing load, not just `data:` — closed port, missing file, malformed URL all funnel through the same `load_failed`/`LoadEvent::LoadError` mechanism. **Residual, not fixed:** `LiveWindowSession::navigate` (`crates/driver/src/live_session.rs`) still writes the *requested* URL into its local `current_url` fallback before the load's outcome is known (inherent to `navigate()` being fire-and-forget) — that fallback is read only when no JS context exists yet, so it stays a minor trap for a harness that reads `current_url()` instead of trusting the `wait` error. **Consequence for any script or harness driving the browser:** a bare `navigate` still only means "request accepted", not "page loaded" — always follow it with `wait{document_ready}`/`browsingContext.navigate`'s own wait and check *that* for an error; don't infer success from `navigate`'s own reply or from a URL comparison (a server redirect breaks that either way) — asserting on document identity, the way `executorlumen.py`'s `RESET_EXPRESSION`/`STALE_GLOBAL` does, remains the most robust pattern.
- **Any paint/scroll performance number is meaningless without the wgpu backend it was measured on — check the `[wgpu] adapter: … (…, Vulkan|Dx12)` line in the run's stderr before comparing runs.** On the same machine and the same adapter (Intel Iris Plus), one scroll run of `lenta.ru` costs 116 ms of frame time on DX12 against 53 ms on Vulkan: `drop(pass)` 0.57 vs 0.20 ms, `prep` 31.4 vs 6.1, `submit` 20.0 vs 8.3, while our own CPU phase (`collect`) is identical — so a backend switch between two measurements looks exactly like a huge engine regression or win ([BUG-405](bugs/BUG-405-OPEN.md) slice 14). The backend is not fixed by the build: it is chosen at startup by `backend_probe::pick_backend` and cached in `<exe_dir>/data/paint/backend_probe.txt`, so two checkouts of the same commit can run different backends. `WGPU_BACKEND=vulkan|dx12|gl` pins it for a measurement (it also disables the probe). Before slice 14 the cache could pin a machine to a backend permanently — a cached winner was probed first, passed, and candidates ahead of it in the Vulkan-first order were never probed again.
- **`fetch()` / `XMLHttpRequest` do nothing in the headless dump modes and on a `file://` page — the page's scripts run, but every request fails as a bare `TypeError: fetch: network error` with nothing on stderr.** The JS runtime installed by `--dump-layout` / `--dump-display-list` is handed `fetch_provider = None` (`crates/shell/src/main.rs`, the `#[cfg(feature = "v8")]` install block), and `_lumen_fetch_sync` answers `false` without logging when the provider is absent, so the failure is indistinguishable from a blocked or refused request. Found 2026-08-17 while validating [BUG-749](bugs/BUG-749-FIXED.md): a probe page loaded from `file://` in a live MCP window reported five network errors against a server that `curl` was reaching at that very moment. **Consequence for any probe that needs real network from page JS:** drive a live window (`--mcp-live-port`) *and* serve the probe page over http from the same origin as the requests — do not load it as `file://`, and do not read a `TypeError` from such a page as evidence about the network stack.
- **A `file://` URL passed as the initial CLI page argument does not load** ([BUG-651](bugs/BUG-651-OPEN.md)) — `--dump-layout`/`--dump-display-list`/`--screenshot`/`--print-to-pdf`, and the initial `<src>` of `--mcp-live-port N <src>`/`--bidi-port N <src>`, all resolve their source via `PageSource::from_arg`, which never strips the `file://` scheme (unlike the sibling `page_source_for_automation_url` used by JS-navigation and BiDi/MCP `navigate` calls, which does this correctly, drive-letter gotcha included) — the whole `file://...` string reaches `PathBuf`/`File::open` and fails (`os error 123` on Windows). Every script in this repo already works around it by starting with `about:blank` then calling the `navigate` MCP/BiDi tool with the real `file://` URL — do the same in any new probe script; do not pass a `file://` URL as the initial CLI/`--mcp-live-port` argument.
- **Any `tests/wpt/run_report.py` run can fail before executing a single test with `OSError: Servers failed to start: wss:18889` / `module 'ssl' has no attribute 'wrap_socket'`, even for a category that itself needs nothing but plain `http`.** `WPT-VENDOR-websockets` (2026-08-18) permanently enabled the `ws`/`wss`/`h2` ports in `tests/wpt/config.json` (previously `null`, which made `tools/serve/serve.py::start_servers` skip those schemes); `wptserve` starts **every configured scheme unconditionally**, so a `wss` startup failure now aborts the whole run regardless of category. The root cause is `pywebsocket3` 4.0.2's `websocket_server.py:160` calling `ssl.wrap_socket()`, removed in Python 3.12+. The fix (replace it with `ssl.SSLContext(PROTOCOL_TLS_SERVER)` + `load_cert_chain` + `wrap_socket(sock, server_side=True)`) lives only in `tests/wpt/.venv/Lib/site-packages/pywebsocket3/websocket_server.py` — **not committed** (`.venv/.gitignore` is a bare `*`) — so it does not survive a venv reinstall or a different pool slot's venv; `WPT-VENDOR-webstorage` (same day) hit it again on a fresh `.venv` despite `websockets` already having "fixed" it locally. Re-apply the same patch whenever this error appears; a durable fix would pin an older `pywebsocket3`/Python in `tests/wpt/requirements.txt` or carry the patch outside `.venv`, neither done yet.

- **`cargo-fuzz`/libFuzzer cannot actually run on this dev machine yet — only builds/links up to the point that needs a runtime it doesn't have.** Found 2026-08-18 building TEST-1 (`fuzz/`, see `docs/tasks/p2-test-track.md#test-1-состояние`). Native `x86_64-pc-windows-msvc` nightly: rustup doesn't ship `librustc-nightly_rt.asan.a`, so `cargo fuzz build` fails at the final link step even though all target crates compile cleanly; dropping to `-s none` (SanitizerCoverage without ASan) instead fails on unresolved `__start___sancov_*`/`__stop___sancov_*` — those boundary symbols are an ELF/Mach-O section-aggregation trick that PE/COFF (Windows' object format) doesn't support the same way, so this isn't a toolchain-version bug, it's a structural gap in Windows fuzzing support (matches upstream `cargo-fuzz`'s own "basic"/experimental framing of `--no-include-main-msvc`). WSL (Ubuntu, the default distro here) has `rustup`+nightly+`cargo-fuzz` installable fine, but has no C toolchain and `sudo apt install build-essential` needs a password nobody scripted has. **Consequence:** any task needing to actually execute a libFuzzer/sanitizer-coverage binary needs either a human running `sudo apt install -y build-essential clang` in WSL once, or a genuine Linux box/CI container — don't spend time on further native-Windows or portable-MinGW workarounds without a concrete reason to expect the PE/COFF section-symbol gap doesn't apply (GNU `ld` targeting the same `-pc-windows-gnu` triple still emits PE/COFF, so it likely doesn't). **Settled on 2026-08-19 by taking the CI route:** `.github/workflows/fuzz.yml` runs the harnesses on `ubuntu-latest` (push touching `fuzz/**` → 60 s per target, weekly cron → 300 s, `workflow_dispatch` → any duration). Reach for `gh workflow run fuzz.yml -f duration=1800 -f targets=fuzz_css_parser` instead of rebuilding a local sanitizer setup. **WSL now works — the "WSL is dead" half of this gotcha is obsolete since 2026-08-19.** The user installed the toolchain there after freeing up `C:`; verified the same day: `rustc 1.100.0-nightly`, `cargo-fuzz 0.13.2`, `clang 18.1.3`, 950 GB free on `/`. The earlier blockers (`rustup` failing on an `llvm-tools-preview` / `llvm-tools` file conflict over `bin/llc`, a 100 %-full `C:` keeping `ext4.vhdx` from growing and the root filesystem read-only) are all gone. So a local run is available again — recipe and measured timings in [`fuzz/README.md`](fuzz/README.md) — and CI is no longer the *only* way, just the unattended one. **Native Windows remains impossible** for the PE/COFF reason above; that part of this gotcha stands.

- **On Linux, `graphic_tests/dump_golden.py` cannot find the binary and reports 6 of its 12 checks as mismatches no matter what you changed.** Two independent Windows assumptions: the path is hardcoded as `target/<profile>/lumen.exe` (there is no `os.name` branch anywhere in the script), and the committed references in `graphic_tests/dump-golden/` were generated on Windows, so the text metrics differ from whatever fontconfig resolves here — `samples/page.html`, `samples/test-06-layout.html` and `graphic_tests/65-flex-align-content.html` fail on both dump kinds with per-glyph advance shifts (`6.67` → `7.63`), while the three JS-free geometry pages pass. Measured 2026-08-21 (P3, BUG-413 slice 1): the same 6 mismatches appear for a build of unmodified `main`, so they say nothing about the change under test. `ln -sf lumen target/dev-release/lumen.exe` + `LUMEN_PROFILE=dev-release` gets the script to run, but the useful evidence of display-list neutrality on this machine is an **A/B of the dumps themselves**: capture `--dump-layout`/`--dump-display-list` for the script's six pages, `git stash` the change, rebuild `lumen-shell`, capture again, and `diff -rq` the two directories — byte-identical output is the claim the protocol asks for, and it is unaffected by the reference drift. Do not `--update` the references to make the gate green: that would overwrite the Windows baseline every other machine compares against.

- **A live WPT run does not see your edit to `tests/wpt/resources/testharnessreport.js` (or `testdriver.js`, or `testharness_runner.html`) — those six URLs are served from bytes frozen when the server process first answered them.** `TestEnvironment.get_routes` registers them as `wptserve` `StaticHandler`s, which read their file(s) on the first request and cache the content for the process's lifetime; the four `%(...)s` placeholders in the report template (`output`, `timeout_multiplier`, `explicit_timeout`, `debug`) are substituted even earlier, from the options of the run that *built* the handler. Because `tests/wpt/config.json` pins every port, a server orphaned by a dead run keeps serving the next one (WPT-RUN-5 slices 18/22), so both hazards are routine rather than exotic: an edited harness silently does nothing, and a squatter left by e.g. `run_report.py --timeout-multiplier 3` silently gives every test of the next run a triple timeout. Nothing surfaces either in `wptreport.json`. Test *files* are not affected — those are read off the doc root per request. Before trusting a run that could not bind its own ports, run `tests/wpt/route_audit.py`: it diffs each static route against what the checkout would build and reads the substituted values back out of the served body; `tests/wpt/port_guard.py --reclaim` kills the orphan so a fresh server picks the edit up.

- **A top-level synchronous `throw` now fires the window `error` event too — both halves of [BUG-591](bugs/BUG-591-FIXED.md)/[BUG-716](bugs/BUG-716-FIXED.md) are wired as of 2026-08-22, for most but not all call sites.** `unhandledrejection`/`rejectionhandled` dispatch via `v8::Isolate::set_promise_reject_callback` (`v8_runtime.rs`, BUG-716, 2026-08-22); a synchronous exception from a **timer, `requestAnimationFrame`, `queueMicrotask`, or a classic `<script>`** (both the initial page-load loop — `V8JsRuntime::eval_and_report` — and any script inserted later via the DOM) now goes through the shim's `_lumen_report_exception`/`window.dispatchEvent`'s `'error'` branch (BUG-591, P1, 2026-08-22), including `window.onerror`'s spec-correct 5-argument calling convention. **An exception thrown by an `addEventListener`/`on<type>` DOM event listener is also now reported** (P1, 2026-08-22, same day): `_lumen_dispatch`/`_lumen_dispatch_bubble`/`_lumen_dispatch_rich` (native element/document listeners), `document.dispatchEvent`, and `EventTarget.prototype.dispatchEvent` (the pure-JS base class, via a `typeof`-guarded `_lumen_et_report` wrapper since this shim is also spliced into `WorkerGlobalScope`, which lacks the page-only `_lumen_report_exception` native) all route their listener-invocation `catch` into it now — except `window.dispatchEvent`'s own `'error'` branch, deliberately left bare to avoid a self-rethrowing `window.onerror`/`'error'` listener recursing into itself. **A dedicated Worker's own uncaught exceptions are wired too, same day (P1, 2026-08-22):** `run_worker_thread_v8`'s top-level `rt.eval(&script)` failure and the three previously-bare `catch(e){}` sites in `worker_global_shim` (message dispatch, timer flush) now post through a new `WorkerErrorQueue` (`worker.rs`), drained by `V8JsRuntime::pump_workers` into a new `_lumen_deliver_worker_errors` that fires `ErrorEvent` `'error'` on the owning `Worker` object — both `worker.onerror` and `addEventListener('error', …)` see it now. **A module script's top-level runtime error is wired too (P1, 2026-08-23):** `V8JsRuntime::eval_module_and_report`/`eval_module_at_and_report` report only a `ModuleFailure::Runtime` (the module body started evaluating and threw) through the same `_lumen_report_exception` path — a `ModuleFailure::Load` (compile/link/instantiate never got to evaluate) stays unreported here, since that belongs to the script element's own `load`/`error` event instead, and reporting it would misfire `window.onerror` on an ordinary 404. **`SharedWorker`'s equivalent mechanism is wired too (P1, 2026-08-23):** unlike a dedicated `Worker` (one client), the report must broadcast `error` to *every* connected client's `SharedWorker` object, not just the one whose message triggered it — `shared_worker.rs` tracks a second `error_ports` map (parallel to its message-outbox map) and broadcasts into all of it from four sites (top-level script, `onconnect`, a port's `onmessage`, timer flush); this also surfaced and fixed a pre-existing, unrelated gap where the client-side `SharedWorker` class had no `addEventListener`/`removeEventListener` at all. **The window `load`/`DOMContentLoaded`/`visibilitychange` dispatch path is wired too (P1, 2026-08-23):** measured 2026-08-22 by WPT-RUN-6 slice 24 — an exception thrown from `addEventListener('load', …)`, from `window.onload` or from the `<body onload>` attribute (which forwards into `window.onload`) used to produce no `'error'` event, no `window.onerror` and not even a line on stderr, because `_lumen_apply_ready_state` called each handler inside a bare `catch (e) {}` (`crates/js/src/dom.rs`, the loops over `_domcontentloaded_win_listeners`/`_load_listeners` and the `window.onload` call), with the same shape around window `visibilitychange` listeners in `_lumen_apply_visibility`. All four now call `_lumen_report_exception` — a separate call site from `window.dispatchEvent`'s own `'load'`/`'error'` branches (already wired), because this engine-driven dispatch loops its listener arrays directly instead of going through `dispatchEvent`, to preserve the "document, then window" ordering HTML LS §8.5 requires. This closed the 9 `css/css-shapes/spec-examples` ids that used to go NOTRUN + TIMEOUT instead of FAIL. **The whole `dom.rs` page shim is wired as of 2026-08-23 (P1):** the rIdle residual measured by slice 27 turned out to be one of **33** sites where the shim invokes a page callback inside a bare `catch(e) {}` — `requestIdleCallback`, `MessagePort` and `MediaQueryList` (the two this bug had itself listed as out of scope), `MutationObserver`/`IntersectionObserver`/`ResizeObserver`, `PerformanceObserver` (which is also half of [BUG-840](bugs/BUG-840-OPEN.md)), `hashchange`/`popstate`/`pageshow`/`pagehide`/window `message`, `AbortSignal`, `WebSocket`, the focus-event dispatch, the late-subscription `load`/`DOMContentLoaded` microtask, `Animation`, `WakeLockSentinel`, `DataTransferItem.getAsString`. `MessagePort` alone goes through a new typeof-guarded `_lumen_mc_report`, because `MESSAGE_CHANNEL_SHIM` is the one part of the page shim the *service-worker* scope also evaluates (`sw_worker.rs`), and that scope has neither reporting function. The per-feature shims *outside* `dom.rs` were swept the same day — **50** more sites in 21 files (`worker.rs`/`shared_worker.rs`, `web_audio.rs`'s `oncomplete` — the one BUG-828 names as the reason `webaudio/*` goes quiet — `xhr.rs`, `broadcast_channel.rs`, `video_bindings.rs`, `speech.rs`, …), each its own `rt.eval` that a `WEB_API_SHIM` fix never reaches (the BUG-780 lesson). Those use an inline `typeof` guard rather than a direct call: several are installed into runtimes with no page DOM at all (`--dump-*`, SVG rasterization, their own unit tests), where a direct reference would throw a `ReferenceError` from inside the `catch` and take the rest of the delivery loop with it — worse than the swallow. **BUG-591 is FIXED as of 2026-08-23**; the two mechanisms still silent are out of the engine's reach and now tracked separately: `<body onerror>` forwarding for a foreign document (needs `<iframe>`, [BUG-480](bugs/BUG-480-OPEN.md)) and cross-origin "muted errors" redaction ([BUG-900](bugs/BUG-900-OPEN.md)). `testharness.js` reports a failure *through* the `error`/`unhandledrejection` events (`resources/testharness.js:5074`), so a failed assertion inside a promise chain, a timer, rAF, queueMicrotask, or top-level/DOM-inserted script body should now surface as an ordinary FAIL instead of the harness going quiet until wptrunner's timeout (~9 s of wall clock against 0.05 s for a resolved test) — pending re-measurement, the historical WPT-RUN-5/6 TIMEOUT numbers elsewhere in this file predate the fix. Instrument a live run with `python tests/wpt/rejection_trace.py --on` (adds a JS-level rejection tracker to Lumen's `testharnessreport.js`, prints `LUMEN_UNHANDLED_REJECTION: …`, classified by `tests/wpt/timeout_audit.py`), then `--off` — never leave it on for a run whose numbers are meant to be trusted, it wraps `Promise.prototype.then`. **The complement matters just as much:** a TIMEOUT whose log *does* carry a `script error:`/`module error:` line has already named its own cause — 313 of the 1 488 unexplained TIMEOUTs in the WPT-RUN-5 snapshot printed an exception naming a missing API (`el.style = "..."` in a strict-mode helper, `getClientRects`, `document.fonts.load`, `CSS.px`), and they were TIMEOUTs rather than FAILs only because the (then-missing) `error` event kept `testharness.js` from seeing them. `timeout_audit.py` maps those texts onto the owning bugs and files the rest under `swallowed_errors` (`--json`), so grep the browser output for `error:` before assuming a hang (WPT-RUN-6 slice 14).

- **A wall of TIMEOUTs in one WPT shard is normally ONE hung page, not N broken tests — and the page is often the reftest's *reference*, which never appears in any test id.** A browser is reused across the tests of a shard and is not restarted after a timeout, so the first page that wedges it takes the whole remainder of the shard with it; those victims print nothing at all, which is what makes them look like a distinct silent mechanism. Measured 2026-08-21 (WPT-RUN-6 slice 11) on the WPT-RUN-5 Linux snapshot: 695 of 15 592 TIMEOUTs (the entire `no-output` bucket) came from exactly **16** wedged processes, and not one of them was the first TIMEOUT of its process. Two of the three culprits were new bugs found this way ([BUG-802](bugs/BUG-802-FIXED.md), [BUG-803](bugs/BUG-803-OPEN.md)); five of the sixteen hang in the `-ref.html` page while the test itself lays out in 0.13 s. `tests/wpt/timeout_audit.py` now separates this as the `hung-browser` mechanism and prints the culprit list (`hung_browsers` in `--json`) — read that list before treating any TIMEOUT cluster as a per-test finding, and when triaging a reftest always probe the reference page too.

- **~~An `<audio>` element that is given a `src` freezes the whole page~~ — fixed 2026-08-25 ([BUG-799](bugs/BUG-799-FIXED.md)); what survives is that a relative `src` on that element still never reaches the network, and that the `about:blank` trap next to it was never a bug at all.** The freeze was `PlatformAudioPlayer::load` taking `self.handles` a second time inside the body of an `if let` whose scrutinee still holds a guard on that same non-reentrant `std::sync::Mutex` (edition 2024 drops the scrutinee temporary only before `else`); everything is taken under one binding now, and the report's one defect turned out to be four — a reload not clearing `has_error`, `readyState` compared as a level rather than an edge (40 `loadedmetadata` for one two-second load), and a load opening at `HAVE_METADATA` for metadata nothing had requested. **The residual is a different mechanism from the one the fix's own writeup first named.** "`loadeddata` never arrives" is the symptom; measured 2026-08-25, `loadeddata` arrives on *every* framing an absolute URL can have — 284 ms plain, 2 048 ms delayed two seconds under `Content-Length`, 2 046 ms under `Transfer-Encoding: chunked`. What fails is URL resolution: `startLoad` hands the attribute value to `__lumen_audio_load` verbatim, so `/media/x.mp3` and `x.mp3` die as `MEDIA_ERR_SRC_NOT_SUPPORTED` (`error.code` 4) in ~55 ms **with no request on the server at all** ([BUG-924](bugs/BUG-924-OPEN.md)) — the [BUG-780](bugs/BUG-780-FIXED.md)/[BUG-858](bugs/BUG-858-OPEN.md) shape a third time, and for the same reason: `audio_element.rs` is its own `rt.eval` that a `WEB_API_SHIM` fix never reaches. That single defect is why the whole `audio-loading-*` family loads nothing, since every one of its tests spells the URL `/media/sine440.mp3`. Two more measured the same day: `loading` is not an IDL attribute on `<audio>`/`<video>` at all and defers nothing, so a `display:none` or below-viewport `loading=lazy` element loads in full ([BUG-925](bugs/BUG-925-OPEN.md) — and on `<img>`/`<iframe>`, where the reflection *does* exist, it is declared `'string'`, so an invalid value reads back verbatim instead of `'eager'`), and `duration` is `Infinity` on a fully loaded file while `currentSrc` is `undefined`. **Three consequences for a probe.** An `<audio src>` on a probe page is safe again, but spell its URL absolutely or it will measure BUG-924 instead of your subject. Three `audio-loading-*-deferred` ids are green *because* of BUG-924 (they assert `HAVE_NOTHING` at 1 000 ms against a resource that never loads) and will go red on BUG-925 when it is fixed — that is progress, not a regression. And the `about:blank` half of this entry is not a bug and never was: a window opened there never gets a JS context, so *every* `eval` fails regardless of what you navigate to afterwards — start the browser on a real http page (`--mcp-live-port N http://…`). Slice 6 hit both at once and recorded "the live window is broken in this environment"; it was not.

- **An `.xhtml`/`.xht`/`.svg` page that prints nothing is not a hung page — its scripts were dropped at parse time, and the file's own markup tells you which of three ways.** Navigation has no XML path at all: `crates/shell/src/main.rs:5365` runs `lumen_html_parser::parse` on every response and only stamps `document.contentType` from the header afterwards ([BUG-786](bugs/BUG-786-OPEN.md), third facet, measured WPT-RUN-6 slice 16 by `tests/wpt/verify_document_and_record_gaps.py`). A prefixed `<h:script src>` never becomes a script — the external file is not even requested, so *no* error appears anywhere; a self-closing `<script src="…"/>` is not self-closing in HTML, so everything up to the next `</script>` becomes its text and the rest of the document silently ceases to exist; `<![CDATA[` at the head of an inline script is `Unexpected token '<'`, which BUG-591 then swallows. The content type is irrelevant — the same bytes served as `text/html` fail identically — and an SVG-namespaced `<script>` inside an ordinary HTML page runs fine, so do not generalize this to "scripts in `<svg>`". **Consequence for triage:** before probing an XML test's JS behaviour, check whether its harness loaded at all (grep the run log for a `GET` of `testharness.js`); and never write a probe page whose script is CDATA-wrapped, or it will measure this bug instead of your subject.

- **Any script that greps WPT sources for markers has two blind spots that fail silently: a UTF-16 test file and an `.mjs` helper.** Both were found in `tests/wpt/timeout_audit.py` (WPT-RUN-6 slice 16) and are fixed there — copy its `_decode_source` (BOM sniff, `\xff\xfe`/`\xfe\xff` → `utf-16`) rather than re-deriving it. Read as UTF-8 with `errors="replace"`, a UTF-16 file keeps its ASCII characters but separates every one of them with a replacement byte, so no regex can ever match and the file reads as "contains nothing" — that is how all 20 `html/infrastructure/urls/resolving-urls/query-encoding/utf-16{le,be}.html` ids stayed unattributed. And a modern WPT test increasingly pulls its helper in as an ES module with an `.mjs` extension (`import {createIframe} from "./resources/helpers.mjs"`), so an extension check written as `endswith(".js")` skips exactly the file the evidence is in. Third, less obvious: a `?include=<key>` id (`common/subset-tests-by-key.js`) runs only the `subsetTestByKey('<key>', …)` blocks of its file, so reading the whole helper attributes one variant's hang to a sibling variant's code.

- **Two things a WPT probe must not take on trust: that a `PerformanceObserver` entry type in `supportedEntryTypes` is actually delivered, and that a `test_driver.*` call does anything.** Both were measured 2026-08-21 (WPT-RUN-6 slice 17) and both fail *silently*, so they read as engine hangs. `PerformanceObserver.supportedEntryTypes` lists `layout-shift` and `observe()` accepts it, yet no entry is ever produced — the shell-side trigger `deliver_layout_shift` (`crates/shell/src/main.rs:2925`) is `#[allow(dead_code)]` with no call site in layout/reflow at all ([BUG-809](bugs/BUG-809-OPEN.md)); the advertisement is what makes these tests TIMEOUT rather than FAIL, because WPT's `ScoreWatcher` helper only throws when the type is *absent*. The list is a promise about the type, not about delivery — check for a call site before believing it. And of the ~30 `test_driver_internal` actions, `executorlumen.py::_handle_action` implements exactly two (`click`, `generate_test_report`); every other one is rejected — at the time of this measurement (2026-08-21) the page never saw the rejection at all (BUG-716, fixed 2026-08-22), so the test hung to the runner timeout with an empty log ([BUG-810](bugs/BUG-810-OPEN.md)) — 193 ids of the WPT-RUN-5 snapshot; with BUG-716 fixed this should now surface as a fast FAIL naming the unimplemented action instead, pending re-measurement. An element-targeted action does not even reach the executor: `testdriver-extra.js::get_context` throws on the still-missing `document.defaultView` ([BUG-622](bugs/BUG-622-OPEN.md)). **Consequence:** never write a probe whose result depends on `test_driver.Actions()`/`send_keys` working, and when a category of input-driven tests times out wholesale, check the executor's action table before filing an engine bug.

- **Three things a probe page cannot use on this engine without measuring itself instead of its subject: CSP, a relative-URL `XMLHttpRequest`, and anything a worker has to say.** All measured 2026-08-21 (WPT-RUN-6 slice 18, `tests/wpt/verify_csp_url_worker_gaps.py`). CSP is parsed (`crates/network/src/csp.rs`, `crates/storage/src/csp_policies.rs`) and never enforced — `script-src 'self'` does not stop an inline script, and `securitypolicyviolation` is never dispatched although `SecurityPolicyViolationEvent` exists, so a wait on it can only hang ([BUG-811](bugs/BUG-811-OPEN.md)); the `_lumen_dispatch_csp_violation` hook has no caller outside its own unit test. ~~`XMLHttpRequest.open()` keeps the URL verbatim~~ — **fixed 2026-08-23** ([BUG-780](bugs/BUG-780-FIXED.md), which absorbed the duplicate BUG-812): `open()` now resolves against the document base the way `fetch()` has since BUG-347, at `open()` time rather than at `send()` as XHR §4.5.1 step 6 requires, so a relative XHR URL no longer dies in the network layer as `invalid url: missing scheme`. The *shape* still has live instances elsewhere — `navigator.sendBeacon` is the same defect one module over ([BUG-858](bugs/BUG-858-OPEN.md)), and `xhr.rs` being its own `rt.eval` outside `WEB_API_SHIM` is why it never inherited the fix in the first place: check any other standalone shim before assuming a URL-resolution fix reached it — spell XHR URLs out absolutely in any harness or probe. And a worker is silent in three separate ways: ~~an exception inside a *started* worker fires `error` nowhere~~ — **fixed 2026-08-24** ([BUG-813](bugs/BUG-813-FIXED.md)): the whole HTML LS §8.1.3.6 → §10.2.6 order is wired now — the exception first becomes an `error` event in the worker's **own** scope (`onerror` with the five legacy arguments, plus `addEventListener('error', …)` with a real `ErrorEvent`), and only if nothing there cancelled it does it reach the owning `Worker` object on the page. **The three things to know about the shape of that fix.** The worker scope's `ErrorEvent` is a *local* class (`WORKER_ERROR_EVENT_SHIM`), not a slice of the page shim — the page's `Event` hierarchy lives in `WEB_API_SHIM_MID`, which drags in `document`/`window` — so inside a worker `errorEvent instanceof Event` is **false**. The top-level script is routed through `V8JsRuntime::eval_and_report_via` (the reporter's name became a parameter, since a worker has no `_lumen_report_exception`) rather than wrapped in a `try`/`catch`, because a wrapper would lose the real thrown value and `v8::Message`'s structured location and would move top-level `let`/`const` out of the global scope — and two WPT subtests need exactly this, since they assign `onerror` one line above a throwing `eval('1 + ;')`. And a classic worker script is compiled with **no resource name**, so both `v8::Message` and `Error.stack` report `<anonymous>`; the scope's own `location.href` is substituted, which is what `Worker_ErrorEvent_filename.htm` asserts. **The residual is the opposite defect and lives one interface over:** a `SharedWorker` must *not* hear a runtime error at all (only a load failure, and that as a plain `Event`), yet Lumen broadcasts it to every connected client ([BUG-905](bugs/BUG-905-OPEN.md), pre-dating BUG-813 — the broadcast is BUG-591's) — so never read a `SharedWorker.onerror` firing as evidence about the worker's script. ~~the worker global scope defines neither `navigator` nor `location`~~ — **fixed 2026-08-24** ([BUG-776](bugs/BUG-776-FIXED.md), which absorbed the duplicate BUG-814): all three worker flavours now get a real `WorkerLocation` (the worker's own script URL — the opaque URL itself for a `blob:`/`data:` worker) and a `WorkerNavigator` whose values match the page's, `platform`/`language`/`languages` included, since they come from the same `NavigatorProfile`. **Two residuals a probe must still know:** an `http` redirect of the worker script does not move `location` (it reports the pre-redirect URL — the synchronous script-fetch bridge never surfaces the final URL), and `WorkerNavigator` carries only the mixin members, so `serviceWorker`/`sendBeacon`/`hardwareConcurrency`/`userAgentData` are absent there although the page has them, and ~~worker timers are flushed only when a message is dispatched to that worker, with `setInterval` aliased to `setTimeout`~~ — **fixed 2026-08-24** ([BUG-815](bugs/BUG-815-FIXED.md)): the queue had no deadlines and was drained only from the message-delivery loop, so a worker nobody wrote to ran no timer at all, a delay was ignored, and an interval fired exactly once. Both worker flavours now share one deadline-ordered queue (`WORKER_TIMERS_SHIM`) with a microtask queue of its own, and the thread's wait is bounded by its next deadline (`recv_timeout` on what `_lumen_worker_run_tasks()` returns) instead of blocking on the channel. **Two things a probe should know about the shape of that fix.** The delay goes through the HTML LS §8.6 nesting clamp, which is not decoration: a repeating timer re-runs the initialization steps with the nesting level incremented, and that 4 ms floor is the only thing keeping `setInterval(fn, 0)` from re-arming itself as already-due forever inside one flush — so a zero-delay interval yields after five cycles rather than at the rate you asked for. A string body (`setTimeout("code")`) was dropped in a worker exactly as on the page until [BUG-831](bugs/BUG-831-FIXED.md) was fixed on 2026-08-25 — both shims compile it now; the service-worker scope keeps its own synchronous stub, where a string still throws. **Consequence:** a worker probe no longer needs a `postMessage` to get its timers to run, and it *can* report a failure by throwing — from a **dedicated** worker, where the page hears it as `worker.onerror`/an `error` listener; from a shared one the throw is heard only inside the worker's own scope once BUG-905 is fixed, so post the failure through the port instead (reading `navigator`/`location` is safe on both); and never read a silent `<img>` as evidence that a policy blocked it — a parser-written `<img>` fires neither `load` nor `error` regardless of CSP ([BUG-804](bugs/BUG-804-OPEN.md)).

- **A page that does not scroll under a probe is usually the probe's fault, and a page that scrolls without firing `scroll` is the engine's.** Both halves were measured 2026-08-21 (WPT-RUN-6 slice 19, `tests/wpt/verify_stream_scroll_message_gaps.py`). The shell derives `content_height` from the **display list**, so a spacer that paints nothing — the reflexive `<div style="height:4000px"></div>` — leaves `max_scroll()` at 0 and `window.scrollTo(0, 300)` genuinely does nothing; give the spacer a background and the same call moves the page and `window.scrollY` reports 300. ~~What it never does is dispatch a `scroll` event~~ — **fixed 2026-08-24** ([BUG-821](bugs/BUG-821-FIXED.md)): `fire_window_scroll` used to have exactly one call site in the whole workspace, the mouse-wheel branch, so the event was bound to an input device rather than to a position change. It now fires from step 1 of `RedrawRequested`, the once-per-frame `window.scrollY` sync — CSSOM-View §14 «run the scroll steps» — so *every* movement produces it (wheel, keys, scrollbar drag, touchpad momentum, find-in-page, `scrollTo`/`scrollBy`, and each frame of a smooth animation), and a wheel notch at the very bottom no longer produces a phantom one. **The one thing to know about the shape of that fix:** the «did it move» comparison lives in `V8JsRuntime::set_page_scroll_y` (which now returns `bool`), not in the shell — the previous position must be per-*document*, or a navigation resetting `scroll_y` to 0 would compare against the *outgoing* document's position and fire a phantom `scroll` on the new one. `scrollIntoView` with no scrollable ancestor now scrolls the page too (its arguments are still ignored — [BUG-479](bugs/BUG-479-OPEN.md)). ~~`scrollend` is still worse — no function to dispatch it exists at all~~ — **fixed 2026-08-24** ([BUG-822](bugs/BUG-822-FIXED.md)), on the viewport and on an overflow container alike, and `'onscrollend' in window` now answers true. **The one thing to know about the shape of that fix:** the spec wants one `scrollend` per *sequence*, not per movement, so BUG-821's «did it move» is not enough — the debt lives in `V8JsRuntime::page_scrollend_due(moved, settled)`, per document for the same reason `page_scroll_y` is, while the shell supplies `settled` = nothing is still driving the page (`scroll_anim`/`momentum_anim`/`scroll_drag` all `None`). So an instant scroll gets both events in one frame, a smooth one gets `scroll` per frame and a single `scrollend` on the frame that finishes it (measured `scroll=16, scrollend=1`), and a last momentum frame that clamps at the edge without moving still pays the debt. The element path is instant at both of its write sites, so `scrollend` follows `scroll` immediately there. **Residuals a probe must still know:** a touchpad gesture with no momentum has no "finger down" flag, so a pause mid-gesture ends one sequence and starts another; a wheel notch over a container ends its own sequence (the engine has no debounce timer, and the alternative was no event at all for user-driven container scrolling); `document.onscrollend = fn` is never invoked, the general [BUG-874](bugs/BUG-874-OPEN.md) (`window` works in both forms); and `window.scrollX` is still a hardcoded 0, so horizontal page movement produces neither event. **Consequence for any probe or harness:** a page-level `scroll` *and* `scrollend` are now both legitimate ways to sequence a probe — but the spacer still has to paint, or nothing moves and no event is due.
- **Three more surfaces that answer a probe with silence rather than an error: the Streams error paths, `<video>`, and `window.postMessage`'s modern overload.** Measured the same day (same probe). ~~Streams work on the happy path only — every error/close/cancel path leaves its promise pending forever~~ — **fixed 2026-08-25** ([BUG-823](bugs/BUG-823-FIXED.md)): `WritableStream` now runs the spec's own state machine, and the thing that was missing was a *registry* — the stream keeps its write requests, close request, abort request and the writer's `ready`/`closed` deferreds, so a transition to `errored`/`closed` settles all of them at once instead of only the one the error arrived on; the sink's `abort()` is called (from the spec's `AbortSteps`), a thenable from `start()` is awaited on both sides, and `controller.error()` reaches `reader.closed` and not just the standing reads. **Two things to know about the shape of that fix.** The sink is no longer called in the same turn as `write()`/`close()` — the controller advances its queue only once the promise wrapping `start()` settles (§4.8.3) — so a unit test may not assert the sink's effect in the same `eval()`, and a probe must not read a written value on the next statement. And every promise the engine rejects on the page's behalf goes through `_stream_mark_handled` (the spec's `[[PromiseIsHandled]]`), because otherwise an errored stream nobody asked about would surface as a page-level `unhandledrejection` now that BUG-716 is wired. ~~**Residual:** `tee()` still clones a snapshot of the queue rather than branching, and BYOB/`Symbol.asyncIterator`/`TextDecoderStream`-close are missing outright~~ — **fixed 2026-08-25** ([BUG-824](bugs/BUG-824-FIXED.md)), the day after. `tee()` now runs §3.2.6: the source is taken under one shared reader and stays readable **and locked**, both branches pull through a single algorithm, and the source is cancelled only once *both* branches are, with the reasons aggregated — a branch that cancels alone no longer takes the source with it. `for await (const c of stream)` works, and leaving the loop by `break` cancels the stream and releases the lock rather than leaving it locked for good. Byte streams are there in full (`type: 'bytes'`, `autoAllocateChunkSize`, `ReadableByteStreamController`, `ReadableStreamBYOBReader`, `ReadableStreamBYOBRequest`), and asking for `{mode:'byob'}` on a non-byte stream is now a `TypeError` instead of a default reader handed back with different semantics. **Three things a probe must know about the shape of that fix.** A BYOB read does **not** detach the caller's buffer — `ArrayBuffer.prototype.transfer` is not wired in this engine, so the same buffer is reused and a page that kept its pre-read view still sees the bytes, where a spec browser would have detached it; do not use detachment to prove a read happened. A stream handed to `new Response(rs)` is now the response's body verbatim (`resp.body === rs`, and `arrayBuffer()` returns its bytes instead of the zero it used to), so `clone()` on such a response *tees* it and both halves are readable — but the original object's `body` is silently replaced by one of the branches, so a probe holding `rs` directly is no longer holding what the response reads from. And the fifth surface the bug listed, `TextDecoderStream` never closing its readable side, was already fixed by BUG-823 itself — a re-measure before the work found it green, so the sink's `close` reaching `_ts_flush` is what closes a transform's readable end. ~~`DecompressionStream` still emits nothing until the writable side is closed~~ — **fixed 2026-08-25** ([BUG-846](bugs/BUG-846-FIXED.md), own entry below). Because `testharness.js` runs `promise_test`s sequentially, one unsettled promise still takes the whole rest of a file with it — which is what made 35 of 36 residual `streams/*` files stop mid-run before this fix. ~~`<video>` is a thinner stub than `<audio>`~~ — **fixed 2026-08-25** ([BUG-825](bugs/BUG-825-FIXED.md)): the element had no *loading state at all* (`readyState` was the expression `_gifBacked ? 4 : (_src ? 0 : 4)`, i.e. HAVE_ENOUGH_DATA on an untouched element, and a non-GIF `src` synchronously dispatched a fabricated `loadedmetadata` + `canplay` pair for a file nothing had requested). HTML §4.8.11.5's media load and resource selection algorithms run now, both branches, so `src =`/`load()`/`<source>` produce the real `loadstart` → `error` sequence with a real `MediaError`, and `networkState`/`currentSrc`/`error`/`playbackRate` exist. **Three things a probe must know about the shape of that fix.** Every media event is **queued** as a task, never dispatched inline — `e.volume = 0.5; e.onvolumechange = …` is the order `event_volumechange.html` and much of WPT's media suite use, so a synchronous dispatch reaches nobody (BUG-808's lesson from the other side) — which means nothing has been delivered by the statement after the assignment. A failing `<source>` candidate fires `error` **at the `<source>`, never at the media element**, while a failing `src` attribute fires it at the element; do not read one as the other. And `play()` on anything but a GIF now *rejects* with `NotSupportedError` rather than resolving, since the engine has no other decoder — while `play()` on an element with no resource at all still resolves, deliberately, because a pending promise takes the rest of a `testharness.js` file with it. **Residuals:** `<audio>` keeps its own older model (`audio_element.rs`) and still dispatches its events synchronously, so the two tags are still not interchangeable in a probe — run against **both**; `<video poster>` is still never fetched ([BUG-848](bugs/BUG-848-OPEN.md)); and a `<source>` added after the element has already settled with no resource does not restart selection. And `window.postMessage` implements only the legacy string `targetOrigin`: the one-argument form, the `WindowPostMessageOptions` dictionary and a URL-shaped origin all drop the message with no error, and `transfer` never produces `e.ports` ([BUG-717](bugs/BUG-717-OPEN.md)) — a self-`postMessage` handshake, the cheapest way to sequence a probe page, therefore hangs unless it passes the literal `'*'`.

- **~~A subresource a page asks for through a `<link>` hint is never fetched~~ — fixed 2026-08-25 ([BUG-826](bugs/BUG-826-FIXED.md)); the half of this entry that survives is that the browser's own log still says otherwise.** Measured 2026-08-22 (WPT-RUN-6 slice 20, `tests/wpt/verify_preload_script_audio_gaps.py`): a `rel=preload`, `rel=modulepreload` or `rel=prefetch` link — created from script or written by the parser — produces no HTTP request at all and fires neither `load` nor `error`, while the `rel=stylesheet` control on the same page loads and fires ([BUG-826](bugs/BUG-826-FIXED.md)). The hint reaches `Event::SubresourceHintFound`, whose only consumer in the workspace is the stderr logger at `crates/shell/src/main.rs:285`, so `⤷ preload js [medium] <URL>` appears in the log for a request that was never made — do not read that line as evidence of a fetch. **The fix, and why it landed where it did.** The fetch went into the JS shim on the element itself (`_lumen_link_hint_prepare`, `crates/js/src/dom.rs`), not into the shell — `load`/`error` belong to the element and the shell has no per-node completion signal to forward, exactly the reasoning the `rel=stylesheet` path already used (BUG-703/BUG-722). A parser-written hint never passes through the insertion hook, so a document pass at `readyState='interactive'` picks those up, with a per-node flag keeping the two paths to one fetch. **Three things a probe must know about the shape of that fix.** §4.6.7's refusals are *silent*: an `as` in no state (absent or an unknown keyword), a non-matching `media` and a `type` the destination cannot consume obtain no resource and fire **neither** event — so a `rel=preload` that reports nothing is not necessarily broken, check its `as` first; `modulepreload` inverts one of those, firing `error` for a valid-but-non-script destination while an *unknown* keyword falls back to `script` and loads. `link.relList`/`a.relList` exist now, which matters beyond preload: WPT's `preload_helper.js` opens with `link.relList.supports('preload')` and used to throw out of every test in the family before reaching its subject, so historical numbers for `preload/*` say nothing about preload. And the **residual keeps the log honest**: the shell still issues no request of its own, so `⤷ preload js [medium] <URL>` still means «a hint was found», never «a resource was requested» — the request now happens, but on the JS side and only once the DOM is parsed ([BUG-907](bugs/BUG-907-OPEN.md) — no preload cache either, so the eventual real use of the URL fetches it a second time), while the `Link:` **response header** form still produces nothing at all ([BUG-906](bugs/BUG-906-OPEN.md)). **Consequence for any probe:** the way to prove a request happened is still to record it on the server side; this probe's own http server logs every path it is asked for, which is what separated "the element fired no event" from "nothing was ever requested". Reuse that pattern instead of believing the page or the log.
- **The parser→JS boundary reports an insertion now, but still not a load — and the half that was fixed is a *replay*, because there is no streaming parser to hook.** ~~`MutationObserver` armed on `document.documentElement` with `{childList, subtree}` hears nothing about a `<div>`/`<script>` the parser writes right below it~~ — **fixed 2026-08-25** ([BUG-827](bugs/BUG-827-FIXED.md)). The shape of that fix is the part worth carrying: the shell parses the whole document (`lumen_html_parser::parse`), walks the finished tree and only then runs the scripts, so by the time a page can even construct an observer there is nothing left to insert — «notify at the insertion step» has no site to live at. So `ParserInsertLog` (`crates/shell/src/main.rs`, next to `ResolvedScript`) snapshots the `(parent, child)` pairs in tree order **before** `doc` moves into its `Arc`, cuts them at each executable classic `<script>`'s own subtree, and replays each segment through `_lumen_mo_parser_inserted` (`crates/js/src/dom.rs`) into the same `_mo_notify` `appendChild` uses, right before that script runs; the document's tail goes out in one segment after the last classic script, since deferred modules run when parsing is already over (HTML LS §8.1.3.1). **Three consequences for a probe.** The callback fires *before* the script that closes the segment, not after it, so a page can still touch an element ahead of its execution but the ordering is not a real parser's; a text node arrives as one whole `childList` record rather than a stream of `characterData` ones; and a page with no observer pays only a tree walk — `flush_parser_inserts` asks `_lumen_mo_observing()` first and skips building the argument entirely, because a record queued before `observe()` is dropped by spec anyway. Still silent, and a *different* signal: a parser-inserted `<script src>`/`<link rel=stylesheet>` runs/loads but dispatches `load` to neither `addEventListener` nor `onload` ([BUG-804](bugs/BUG-804-OPEN.md)) — that needs a completion signal, which insertion replay cannot supply. A `<style>` fires `load` however it was inserted, and its `.sheet` stays `null`. **Consequence:** waiting on a parser-inserted node to *appear* is a legitimate way to sequence a page again; waiting for it to *load* is not.

- **A `<script src="">` reports `error` now — and the reason to keep reading is that the same defect sat on a second, silent path and the fix landed on neither of the places the symptom pointed at.** [BUG-838](bugs/BUG-838-FIXED.md), fixed 2026-08-25: `_lumen_script_prepare` collapsed «no `src` attribute» and «`src` is the empty string» into one `''`, so the element took the inline branch, found no body and returned in silence — no event, no request, not a line on stderr, while HTML LS §4.12.1 asks for a queued task that fires `error`. The report measured `createElement('script')`; the **parser** path has the same hole one crate over, where `main.rs::collect_scripts_ordered` drops an empty `src` with the identical `if !src.is_empty()` … `return`. **Both halves are fixed in the JS shim, not in the shell**, for the reason `rel=stylesheet` (BUG-703) and `rel=preload` (BUG-826) already used: `load`/`error` belong to the element, and «parse everything, then run the scripts» leaves the shell no insertion point to report from — so `_lumen_script_empty_src_scan()` runs from `_lumen_apply_ready_state('interactive')` beside `_lumen_link_hints_scan()`, with a per-node flag keeping an element a head script already appended from reporting twice. **Three things a probe must know about the shape of that fix.** The event is a queued task, not an inline dispatch: `script.onerror` is assigned *before* the `appendChild` that triggers it, yet `fetch-src/empty.html` asserts `assert_true(queued)` — so nothing has been delivered by the next statement. `src="   "` is treated as empty although the spec would URL-parse it — the URL parser strips exactly that whitespace and resolves it to the document itself, i.e. fetching the page's own HTML only to fail parsing it as JS, which ends at the same `error` with one wasted request. And the third defect the report did not name is the **event object**: `_lumen_resource_fire` built it without `isTrusted` and without `target` (`_lumen_dispatch` sets no target at all — [BUG-873](bugs/BUG-873-OPEN.md)), so a handler could not tell two pending scripts apart; both are filled in now for every `load`/`error` on that path, not just for an empty `src`. **Residual:** the three `fetch-src` ids went TIMEOUT → FAIL and now stop on one shared assertion — `assert_class_string(ev, "Event")` answers `[object Object]`, because `Symbol.toStringTag` is present on exactly one of the shim's 26 event constructors ([BUG-912](bugs/BUG-912-OPEN.md)); fixing that needs a tag **per class**, since one tag on `Event.prototype` is inherited and would make `MouseEvent` claim `[object Event]`. A parser-written `<script src>` that *does* load still fires no `load` ([BUG-804](bugs/BUG-804-OPEN.md)) — insertion replay cannot supply a completion signal.
- **A `PerformanceObserver` on `resource` finally fires — and the reason to read on is that the report named one defect while there were five, four of them of a kind no amount of reading the named function would have found.** [BUG-839](bugs/BUG-839-FIXED.md), fixed 2026-08-25. `_lumen_record_resource_timing` existed and was correct, with no caller outside the shim's own unit tests, so `resource` was advertised in `supportedEntryTypes`, accepted by `observe()`, and produced for nothing at all. **The loads split in two, and the halves had to be fixed in different crates.** What the page starts itself records inside the shim, at the one place `performance.now()` of the call is real: a single point in `_lumen_fetch` covers `fetch()`, `<script src>`, `<link rel=stylesheet>` and the whole `rel=preload` family, because since BUG-703/BUG-826 all of them load *through* `fetch()` — which is exactly why `initiatorType` is passed as a parameter (`init._lumenInitiatorType`) rather than guessed from the URL, or a stylesheet would report itself as `fetch`. `XMLHttpRequest` needed its own fix in `xhr.rs`, being its own `rt.eval` that no `WEB_API_SHIM` change reaches (the BUG-780 lesson, a third time). What the *engine* fetches for the document — images, cascade stylesheets, `@font-face` bodies, parser scripts — runs on threads with no JS context, so it publishes an `Event::ResourceTimed` from `HttpClient::fetch_subresource`; **the reason that is one event per resource and not two is that both of the shell's caches sit above that method** (`PREFETCH_CACHE::fetch_current` only calls in on a miss), so the streaming thread's speculative warm-up and the final UI pass cannot both emit. **Three defects the report did not name, each of which means the entry has nowhere to go even once it exists:** the Resource Timing L2 §4.4 buffer did not exist at all (`setResourceTimingBufferSize` was `function(_maxSize) {}` — and `clearResourceTimings` not resetting the count is half the operation, not bookkeeping, since that reset is the *only* way a page makes room from inside the `resourcetimingbufferfull` handler); `onresourcetimingbufferfull` was a plain expando, so `'onresourcetimingbufferfull' in performance` answered `false` while assignment "worked"; and the observer callback took two arguments where §6.2.1 wants three, so `options.droppedEntriesCount` threw a `TypeError` *inside* the callback that the surrounding catch then ate. **The fifth defect was found by measuring, not by reading**, and it is the one to carry: the per-navigation reset of the queue was put in `set_js_ctx`, «the one place every navigation path funnels through» — but that runs *after* `source.load`, i.e. after the document has already fetched its own stylesheets, scripts and images, so it discarded exactly the rows the page was owed. A page whose three subresources the probe's own server had served still measured 0 entries. The reset belongs where the *load* starts. **Consequences for a probe.** The buffer and the observer stream are two independent sinks: with `setResourceTimingBufferSize(0)` the callback still runs while `getEntriesByType('resource')` stays empty — do not read one as evidence about the other. `initiatorType` is `css` for a `@font-face` body (its initiator is the stylesheet) and `other` for anything `<audio>`/`<video>`/`<track>`, because a Fetch destination cannot always name the element. And three things still produce **no** entry: `EventSource` (its long-lived connection bypasses both paths), a load that fails, and any per-phase milestone — `domainLookupStart`…`requestStart` are all collapsed onto `fetchStart`, `transferSize` is the spec's fixed `encodedBodySize + 300` guess. Delivery to the observer is still synchronous rather than a queued task ([BUG-648](bugs/BUG-648-OPEN.md)), so `buffered-does-not-sync-invoke.html` still fails; `droppedEntriesCount` does not depend on that, its flag being per-observer.

- **~~Web Audio renders silence and hides the evidence~~ — fixed 2026-08-25 ([BUG-828](bugs/BUG-828-FIXED.md)), and the shape of the fix carries one rule worth more than the feature.** `OfflineAudioContext.startRendering()` used to resolve at once with an all-zero buffer of the right length (`web_audio.rs`, "Phase 0: immediately resolve with a silent buffer"), a source node never fired `ended`, and `AudioParam` automation changed neither `.value` nor the output. It now runs a real pull-based graph traversal in 128-frame render quanta, entirely in the JS shim (there is no native audio graph to move it into): oscillator/buffer-source/`ConstantSourceNode` synthesize, gain/delay/biquad/stereo-panner/wave-shaper/merger/splitter process, and the `AudioParam` timeline (`set`/ramp/`setTargetAtTime`/`setValueCurveAtTime`, §1.6.3 boundary rules, a-rate sampling, plus any node connected *into* the param) shapes it. `suspend(t)` stops the loop at a render-quantum boundary and `resume()` continues from the same frame. **The rule:** every callback the shim makes on the page's behalf is queued as a task via `_wa_task` (BUG-808's lesson) — **except `statechange`**, deliberately, because this engine only pumps timers when it redraws, so on a static page a task waits up to a second and the handler for the `running` transition ends up reading `closed`; measured with `verify_preload_script_audio_gaps.py --variant audio-context-state`, which is the reason to run a *control* variant at all. Two consequences for a probe or a unit test: `ended`/`complete` have not been delivered by the statement after the call that causes them, and in the DOM-less runtimes (`--dump-*`, SVG rasterization, unit tests) there is no `setTimeout` at all, so `_wa_task` falls back to an inline call — that fallback, not the promise, is how a unit test reads a rendered buffer, since V8 drains microtasks only when the whole script ends. **Still not there:** no FFT, so `AnalyserNode.get*FrequencyData` report the floor (the time-domain half is real); `decodeAudioData` still returns a second of silence; a realtime `AudioContext` still makes no sound — its `currentTime` advances (quantized wall clock) purely so scheduling and `ended` have something to hang on.
- **~~`history.pushState` puts the raw argument into `location`~~ — fixed 2026-08-25 ([BUG-829](bugs/BUG-829-FIXED.md)), and what the fix is worth remembering for is that one reported defect turned out to be three, on three different boundaries.** `pushState`/`replaceState` now resolve their argument against the **document base URL** (HTML LS §7.4.6 step 3 — not `location.href`, which differs on a page with `<base href>`) and throw the spec's `SecurityError` for a URL that does not parse or that this document may not be rewritten to, *before* anything reaches the session history, since a refused call must leave no trace. Two consequences beyond the headline: `""` as the third argument is now an ordinary relative reference resolving to the base URL, where it used to collapse into the "argument omitted" branch; and a `file://`, `about:` or `data:` page may now only change its **fragment**, so an SPA opened from the filesystem gets a `SecurityError` where it used to get a silently broken `location` — that is the spec answer, not a regression. **The second defect was the traversal**, and it was in the shell: before the first `pushState` the document URL lives in `source` and `display_url` is empty, so the back-stack entry was stored with no URL at all and `_lumen_deliver_popstate` read the empty string as «keep the current URL». **The third is the one to carry**, because nothing about the symptom pointed at it: `fire_popstate` built `_lumen_deliver_popstate({state_json}, '{url}')` by embedding the JSON **bare**, on the reasoning that valid JSON is a valid JS expression — but the shim takes JSON *text* and runs `JSON.parse` on it, so it received an object literal, the parse threw, and **every** traversal delivered `state: null`. It survived unnoticed for exactly one reason: `null` is the single value that round-trips through that confusion unchanged. **Consequence for any engine↔shim call you write:** a JSON payload crossing that boundary must be encoded to match the receiver's own reading of it (`serde_json::to_string` of the text → a JS string literal), and a working `null` proves nothing about any other value. What stays true for a probe: `history.pushState(state, "")` with **no** URL argument still fires no `popstate` on traversal ([BUG-886](bugs/BUG-886-OPEN.md)) — that entry never reaches the shell's stack at all.

- **Three ways a navigation probe silently re-runs itself forever, and one way it freezes.** Measured 2026-08-22 (WPT-RUN-6 slice 21, `tests/wpt/verify_navigation_form_import_gaps.py`). ~~A click on a fragment-only `<a>` is a *full* navigation~~ — **fixed 2026-08-23** ([BUG-833](bugs/BUG-833-FIXED.md)): link activation called `_lumen_navigate` directly, bypassing the working `_lumen_navigate_or_fragment`, so the document restarted from scratch, `hashchange` never fired, and a page that clicked such a link from script reloaded endlessly; the `A`/`AREA` branch of `_lumen_run_activation_behavior` now enters the same path `location.href =` uses, because HTML LS §7.4.2 decides «fragment or load» from the URL and not from how the navigation started. **Residual, and the same for every script-initiated fragment navigation** (`location.hash =` included): the JS side updates `location`/history/`hashchange` but never asks the shell for the visual half, so `:target` and the scroll to the target still happen only for a *real mouse click*, which reaches `navigate_fragment` (`crates/shell/src/main.rs`) instead — do not read "the hash changed" as "the page moved". ~~`sessionStorage` is empty on every new document~~ — **fixed 2026-08-23** ([BUG-836](bugs/BUG-836-FIXED.md)): `install_dom` built its own empty store per document, so the reflexive «set a flag, reload once» brake never engaged and `location.reload()` looped too. The store now belongs to the **tab** (`Lumen::ss_storage`, keyed by origin exactly like `ls_storage`, carried in the tab snapshot and emptied only when a new tab is opened), reaching the runtime through the `V8JsRuntime::with_session_storage` builder rather than through yet another `install_dom` argument — which also means a runtime nobody hands a store to (unit test, `--dump-*`, SVG rasterization) still gets a private one. **Consequence for a probe:** `sessionStorage` is now a legitimate brake again, but only within one tab — and it still has no quota, so a `while (true)` filling it hangs the page ([BUG-870](bugs/BUG-870-OPEN.md)). ~~And `history.back()` across a document boundary freezes the calling document permanently~~ — **fixed 2026-08-23** ([BUG-835](bugs/BUG-835-FIXED.md)), and the mechanism was not the one the symptom suggested: the traversal did reach the shell and `bfcache_thaw` ran to completion — it installs a **fresh, empty** JS runtime over the deserialized DOM (`js_heap: Vec::new()`, «JS heap suspend is gated on 10C.2»), so the restored document had no timer, listener or closure and was inert. A page with a live runtime is now **parked whole** (`ParkedPage`, `Lumen::park_current_page`/`restore_parked_page`, cap 2), which is cheap because each `V8JsRuntime` owns its own thread and isolate and `route_task_js`/`route_query_js` only ever reach the *active* handle — nothing pumps a parked runtime, so its timers and rAF pause for exactly as long as the page is in the back/forward cache. The frozen-DOM path stays for pages with no JS. **Consequence for a probe:** a restored page's own timers resume (`tickA 1` → `tickA 2`, closure state intact), while every timer of the document that was just left is *paused*, not lost — so never expect a marker armed in the outgoing document to fire after a back, and never read its absence as a hang. ~~Only `pagehide` marks a page leaving; `unload`/`beforeunload`/`visibilitychange` are never dispatched at all~~ — **fixed 2026-08-25** ([BUG-834](bugs/BUG-834-FIXED.md)): the whole HTML LS §7.4.5–§7.4.6 sequence runs now on all three navigation paths (`navigate_to`, `navigate_back`, `navigate_forward`) — `beforeunload`, then `pagehide` → `visibilityState = 'hidden'` → `unload`. **The half worth carrying is the second defect, which the report did not name and the symptom pointed away from:** the `persisted=true` everyone read as «the shell keeps this page» was set by the *fallback* branch of `navigate_to`, «store an HTML snapshot if the freeze failed», which never checked `bfcache_eligible` at all. A snapshot is the page's **source text** — coming back re-parses the document, so no listener, timer or closure survives, and HTML LS §7.4.6 calls that discarded, not salvageable. That one flag decides two things at once, which is why the bug looked like one: `pagehide.persisted`, and whether `unload` fires at all (the spec sends it *only* to a discarded document). Salvageable is now exactly the two branches that retain something — parking whole (BUG-835) and freezing the DOM arena — and they agree with `_lumen_bfcache_blocked()`, which already refuses the freeze to any page carrying an `unload`/`beforeunload` listener. **Three things a probe must know about the shape of that fix.** `pageshow` raises the spec's «page showing» flag *and* calls `_lumen_apply_visibility(false)`: without that pair a page restored into the **same** runtime would stay `hidden` for good and never pay a second `pagehide`. `beforeunload` is dispatched by its own loop rather than through `window.dispatchEvent`, because only the on<type> handler's *return value* sets `returnValue` — an `addEventListener` callback's does not. And the page's answer to `beforeunload` is computed and deliberately **not honoured**: cancelling needs a user-facing confirm dialog, and this engine's `confirm()` is a stub that always answers `false`, so treating «asked to stay» as «cancel» would wedge every page that sets a `returnValue`. **Residuals:** `location.reload()` still runs no unload sequence (it is called from inside the navigation paths themselves, so dispatching there would double-fire) and neither does `window.close()` ([BUG-887](bugs/BUG-887-OPEN.md)); `unload` carries no legacy target override, so `event.target` is empty like every window event ([BUG-873](bugs/BUG-873-OPEN.md)); and WPT observability is still behind [BUG-480](bugs/BUG-480-OPEN.md), since all eight residual ids navigate inside an `<iframe>`.
- **~~A string timer handler is dropped~~ — fixed 2026-08-25 ([BUG-831](bugs/BUG-831-FIXED.md)); what survives of this entry is the click, and one thing about the shape of the fix.** `setTimeout("code", 0)`/`setInterval` used to open with `if (typeof fn !== 'function') return 0;` on the page **and** in a worker (`WORKER_TIMERS_SHIM`'s shared `_schedule`), so the string was never compiled, a plausible id came back and nothing threw — the same hole in two shims, because §8.6 is `WindowOrWorkerGlobalScope`, not the window. Both compile it now, with **indirect** eval (`(0, eval)(src)`), and that choice is the part to carry: the spec wants a *classic script*, i.e. global scope, so `setTimeout('var x = 1')` must create a global — `new Function(src)` would lose the declaration in its own scope and a direct `eval(src)` would run the code inside `setTimeout`'s. The string is compiled when the timer **fires** (afresh per firing of an interval), so a syntactically broken one queues without complaint and fails inside the timer loop, and the returned id is a real one now instead of the `0` the spec reserves for "no timer created". **Residuals a probe must still know:** the page shim drops the trailing `setTimeout(fn, delay, …args)` arguments the worker one passes ([BUG-909](bugs/BUG-909-OPEN.md)), `requestAnimationFrame` still answers a non-function with the same silent `0` where WebIDL wants a `TypeError`, the service-worker scope keeps its synchronous stub (a string throws there), and an `import()` inside such a string resolves against the document URL rather than `<base href>` — which is the whole `string-compilation-base-url-*` family, and equally true of `eval`/`Function` ([BUG-910](bugs/BUG-910-OPEN.md)). ~~And activation behaviour is looked up on the clicked node rather than on the nearest activatable ancestor~~ — **fixed 2026-08-25** ([BUG-837](bugs/BUG-837-FIXED.md)), and the half worth carrying is that the report named one defect while the same one lived on **two** click paths. The JS half is `_lumen_activation_target` (`dom.rs`), a walk up the ancestor chain shared by `click()` and the wrapper's `dispatchEvent`, with the pre-click flip and its undo moved onto the target too — DOM §2.9 computes the target *before* dispatch, so the halves of one sequence cannot sit on different nodes. The second was the **native mouse click**: `forms::classify_click` (`crates/shell/src/forms.rs`) keys on the clicked node's tag the same way and had no `<label>` branch at all, so a real click on a caption toggled nothing even when it landed on the `<label>` itself — invisible to the report, which measured `.click()` from script, and invisible on links because `links::find_link_href` next door already walked ancestors for `<a>`. **Two boundaries decide whether such a walk is a fix at all:** interactive content with no behaviour of its own (`SELECT`/`TEXTAREA`/`IFRAME`/`EMBED`/`OBJECT`) must *stop* the walk (HTML LS §4.10.20 — otherwise a click in the `<textarea>` inside `<label for=cb>` toggles `cb`), and the `disabled` check has to move inside the activation, since the old one covered only the clicked node. **Residuals for a probe:** a synthetic click still moves no focus, which is spec-correct (`active=` stays empty — the synthetic click activation steps contain no focusing), and a *native* click on a control still dispatches neither `click` nor `change` to JS, because the shell flips the attribute itself — an older, separate gap in the native path. Related shape worth knowing when reading a WPT residual: a test whose wait lives in the *child* document (`parent.done()` from an `<iframe>`) shows no wait at all in its own source — `timeout_audit.py` now has a fifth `classify_source` stage that reads the subframe for exactly that reason (47 ids, all BUG-480).

- **`hashchange` is a queued task now, so a page may register its listener after the assignment that causes it — and nothing has been delivered by the next statement.** [BUG-832](bugs/BUG-832-FIXED.md), fixed 2026-08-25: `_lumen_fire_hashchange` ran the listener list from inside the `location.hash` setter, so the reflexive `location.hash = 'x'; addEventListener('hashchange', …)` — the shape all four residual `scroll-to-fragid` ids use — heard nothing at all, while the same two lines in the other order worked perfectly. The dispatch is now a task in `_lumen_timers` with `nesting: 0` (the `_ro_schedule_initial`/`Animation._fire` pattern, not `setTimeout` — the §8.6 4 ms clamp is about timer nesting, not about an engine-queued task). **Two things to know about the shape of that fix.** A microtask would not have worked and is not a smaller version of the same idea: it runs before the event loop is reached, i.e. before the end of the very script that registers the listener. And the event object is built at **queueing** time, not at delivery — two writes in one turn must produce two events with their own `oldURL`/`newURL` pair, which is exactly what `006.html` asserts and what a delivery-time read of `location` would flatten into two copies of the final URL. **Consequence for a probe or a unit test:** a page-level `hashchange` is a legitimate way to sequence a probe again, but a unit test must pump (`_lumen_tick_timers()`) — `link_activation.rs` and the six `location.hash`/`_lumen_deliver_popstate` tests in `dom.rs` all had to grow that call. Still synchronous, and a separate mechanism: `popstate` on traversal (and an entry made by `pushState(state, "")` never reaches the shell's stack at all, [BUG-886](bugs/BUG-886-OPEN.md)). And do not read a delivered `hashchange` as «the page moved» — the visual half of a script-initiated fragment navigation (`:target`, the scroll to the target) still only happens for a real mouse click, the residual of [BUG-833](bugs/BUG-833-FIXED.md).

- **Four APIs that answer a probe by working just well enough to mislead it: IndexedDB, `EventSource`, Compression Streams and the Performance Timeline.** Measured 2026-08-22 (WPT-RUN-6 slice 22, `tests/wpt/verify_perf_idb_sse_gaps.py`), and in two of the four the family name pointed at the wrong thing. IndexedDB's basics are fine (open, upgrade, `put` → `oncomplete`, the spec order of two transactions, `TransactionInactiveError`, `deleteDatabase`, `createIndex`), but a transaction with **no requests in it never commits** — only a request queues one ([BUG-841](bugs/BUG-841-FIXED.md)) — ~~there is no connection queue at all, so `versionchange`/`blocked` never fire~~ — **fixed 2026-08-25** ([BUG-843](bugs/BUG-843-FIXED.md)), and the half worth carrying is what a *correct* queue costs a probe. An upgrade or a `deleteDatabase` now broadcasts `versionchange` to every other live connection, fires `blocked` at itself, and **waits for `close()` — forever, exactly as a real browser does**; the request is parked rather than re-queued, since putting it back would spend an event-loop turn per spin, i.e. the unbounded drain BUG-842 fixed. **Consequence for a probe or a harness: a page that leaks an open connection now stalls every later upgrade and delete on that name.** Measured on the WPT category: 7 ids went OK → TIMEOUT, of which five are only *slowness* (green again at `--timeout-multiplier 4` with byte-identical subtest verdicts) and two wait on a connection the page never closes, because an unrelated pre-existing defect breaks the promise chain that would have closed it — telemetry on `idb-explicit-commit` counted 12 parks and **zero** `close()` calls. Two more things the fix carried, neither of which the report named: everything an open/delete decides (version comparison, `VersionError`, the connection, the deletion) is now resolved when the queue reaches the request rather than when it was called — otherwise a request queued behind a delete operates on a database that no longer exists — and `IDBDatabase` finally has `addEventListener` at all (it had none, so `db.addEventListener('versionchange', …)` threw `TypeError`; dispatch still exists for `versionchange` only, [BUG-914](bugs/BUG-914-OPEN.md)). The category's wall time went 406 s → 1231 s because a blocked delete keeps the database in the snapshot that every dirty flush re-serializes whole ([BUG-917](bugs/BUG-917-OPEN.md)), and ~~WPT's own `keep_alive` idiom (a request re-armed from its own `onsuccess`) **freezes the page**~~ — **fixed 2026-08-23** ([BUG-842](bugs/BUG-842-FIXED.md)): `_idb_flush_txn` drained the queue in a `while` inside one microtask, so 16.3 M iterations ran in 6 s with no timer, no rendering and no later script of the document. A flush now spends a budget (`_IDB_FLUSH_BUDGET`, 1024 dispatches) and hands the remainder to a **task** — an entry written straight into `_lumen_timers` with `nesting: 0`, because `setTimeout` would put the §8.6 4 ms clamp (BUG-271) on every step of a long cursor walk — while `_idb_schedule_flush` stays silent for the duration of a flush, since a microtask queued from a microtask never returns to the loop and no budget alone would have helped. **Consequence for a probe:** a bounded IndexedDB job (a cursor walk, a batch of `put()`s) still completes inside one `_lumen_idb_flush()` as before, but an unbounded one now needs the event loop to turn — poke it with `_lumen_tick_timers()` in a unit test, and never expect a self-rearming request to have spun a given number of times by the next statement. The service-worker scope, which evaluates this same shim with a synchronous `setTimeout` stub, keeps the old unbounded drain: there is nothing there to yield to. ~~`EventSource` never treats a stream ended by the response body as ended~~ — **fixed 2026-08-25** ([BUG-844](bugs/BUG-844-FIXED.md) and [BUG-845](bugs/BUG-845-FIXED.md), one fix: the second is unreachable until the first exists, because there is no «end of stream» to clean up at). The reported defect was that a stream ended by `Content-Length` + keep-alive — every `.py` handler under `wptserve` — never ended: no `error`, no reconnect, `readyState` 1 forever. **The thing to carry is where that turned out to live:** `fill_queue` read until the socket closed, which RFC 7230 §3.3.3 makes correct for exactly one of the three framings, so the fix is a `SseFraming` (`Length`/`Chunked`/`Eof`) chosen from the response headers and a `read_body` that reports the end of the *body* — a stream can end on a socket that stays open, and nothing in the old code could express that. Two more defects the report did not name. `open` was pushed **by hand** at session creation while reconnection lives inside `next_event`, so no reconnect could ever announce itself; it now comes from `Event::SseConnected`, which the connect path already published per connection, through a new `JsSseControlSink` — which is why the queue is built before the handshake rather than inside the session. And `JsSseEvent::Close` meant two different things at once: the shim answered it by firing `error` *and* scheduling its own `setTimeout` reconnect, so reconnection existed in two places that did not know about each other — split into `Reconnecting` (`readyState = CONNECTING` + `error`, HTML LS §9.2.5 step 1) and a terminal `Close`, with the shim's timer and `_reconnecting` flag deleted. **Consequence for a probe:** an `EventSource` reconnect is a legitimate way to sequence one again, and `open` now fires on every connection — but a non-200 or a refused port on a *reconnect* is terminal (§9.2.5 «fail the connection»), not retried forever. **Two traps worth copying from the measurement rather than rediscovering.** A probe server that holds the socket open must accept on a **separate thread**, or the reconnection sits in the TCP backlog and «1 connection» reads as «no reconnect» — which is what the first run of this probe recorded. And a live test of this must carry a **deadline**: reading until the socket closes also ends eventually, just 10 s later, so a mutation reverting the framing to `Eof` kept the test *green* at 20 s — time is the only thing that distinguishes the fixed behaviour, so assert on it. `DecompressionStream` decodes correctly but ~~emits nothing until `writer.close()`, so the reflexive write-then-read hangs~~ — **fixed 2026-08-25** ([BUG-846](bugs/BUG-846-FIXED.md), own entry below), and `new Response(stream).arrayBuffer()` resolved with 0 bytes until BUG-824 was fixed on 2026-08-25 (it returns the stream's bytes now; the `DecompressionStream` half of that line stands). `PerformanceObserver` delivers `mark`/`measure` including `buffered: true`, while ~~`resource` — advertised in `supportedEntryTypes` and accepted by `observe()` — is never produced on a live page~~ (the BUG-809 shape again) ~~and the callback gets two arguments instead of three~~ — both **fixed 2026-08-25** ([BUG-839](bugs/BUG-839-FIXED.md), own entry below), and **an exception it throws is swallowed** by the shim's own `try/catch` ([BUG-840](bugs/BUG-840-OPEN.md)) — the BUG-828 trap in a second place, and one the 2026-08-22 BUG-591 fix does not reach, because the exception is eaten before any host-side reporting path sees it. **Consequence for a probe:** close the writable side before reading a compression stream, never sequence anything on an `EventSource` reconnect or on a page-level IDB spin, and treat "the API responded" as saying nothing about whether it delivered.
- **~~A `setTimeout` delay above 2^31-1 never fires~~ — fixed 2026-08-25 ([BUG-847](bugs/BUG-847-FIXED.md)), and the half worth carrying is what else was hiding in the same expression; an image source that is not an `<img>` is still never fetched.** `(typeof delay === 'number' && delay > 0) ? delay : 0` implemented **neither** half of the WebIDL `long` the argument is declared as (ToNumber, then ToInt32, and only then §8.6 step 5 «negative → 0»), so four separate inputs were wrong at once: `Math.pow(2, 32)` armed a timer 49 days out, `Math.pow(2, 31)` 24.8 days (the same modulo from the negative side), `Infinity` produced a deadline nothing can reach, and — the one a live page actually hits — `setTimeout(fn, '100')` or an object with a `valueOf` fired **immediately**, because a delay that was not already a number was taken as 0 instead of being converted. The fix is `_lumen_timer_delay`, verbatim the worker shim's `_toDelay`: §8.6 is `WindowOrWorkerGlobalScope`, so the page and a worker must answer identically, and letting those two shims drift already cost [BUG-831](bugs/BUG-831-FIXED.md). **The same defect sat one function over on both shims:** `clearTimeout`/`clearInterval` compared `id === ` the raw argument, so `clearTimeout(String(id))` cancelled nothing, although `cancelAnimationFrame` twenty lines below already converted its own handle. **Consequence for a probe:** a stringified or fractional delay now means what it says, and «the callback ran on the next tick» is no longer the engine's answer to a delay it could not read. And `collect_requests_inner` (`crates/engine/layout/src/box_tree.rs:2262`) matches `name.local == "img"` and nothing else: `<video poster>`, `<input type=image>`, an SVG `<image href>` and `<link rel=icon>` produce no HTTP request and no `load`/`error` at all ([BUG-848](bugs/BUG-848-OPEN.md)) — proven by recording requests on the probe's own server, since the browser log is not evidence here (BUG-826). **Consequence:** a probe that needs a subresource must use `<img>`, `<link rel=stylesheet>`, `<script src>` or `fetch()`; anything else measures this bug.
- **Compression Streams decode as they go now — and the reason to read on is that the reported defect was one of four, three of which only a run of the whole category surfaced.** [BUG-846](bugs/BUG-846-FIXED.md), fixed 2026-08-25: the shim buffered every chunk and called a one-shot native at `flush`, so a `reader.read()` before `writer.close()` never resolved. **Where the fix had to land is the transferable part:** a `bytes -> bytes` native has nowhere to keep a half-finished codec, so the codec itself moved into the host as *state* — a `thread_local` registry keyed by an opaque `u32` handle (`crates/js/src/compression.rs`, the `subtle_crypto` `CryptoKey` shape), with the one-shot pair and `dom.rs::_decompress_status_prefixed` deleted along with their last caller. **Three defects the report did not name.** A chunk that is not a BufferSource was silently read as an empty chunk instead of erroring both sides (48 subtests). A **truncated** body decoded as a *success*, because `flate2::write::ZlibDecoder::finish()` cannot tell «the adler32 trailer never arrived» from «the stream ended» — which is why `deflate`/`deflate-raw` now run on the low-level `flate2::Decompress`, where `Status::StreamEnd` is explicit, while `gzip` stays on `write::GzDecoder`, whose `finish()` checks CRC32+ISIZE itself. And junk past the end of a stream must enqueue what was already decoded **before** erroring: `ReadableStreamDefaultControllerError` resets the queue, so «error, then enqueue» loses the chunk a standing `read()` was owed. **The fourth was found by a test failing for the wrong reason, and it is the one to carry beyond this bug:** flate2's `zio::Writer` — which backs *every* `write::*` wrapper — moves its staging buffer into the sink at the **start of the next call**, not at the end of the current one, so a full gzip stream pushed in one chunk answered with zero bytes and only surrendered them at `finish`, i.e. the very symptom being fixed, surviving on one format of three; an empty `write(&[])` performs the dump. **Two consequences for a probe or a test.** The *compressor* streams too, so the first gzip chunk is its header — a reader must concatenate all chunks (WPT's `concatenate-stream.js` pattern) and four of the engine's own unit tests passed for two formats of three while reading only the first. And a stream abandoned without `close()` leaks its codec until the runtime dies (no `cancel` hook on the shim's `TransformStream`), bounded by one document. Measured A/B over the vendored `compression` category: 187/322 subtests and 16/19 harness OK → **242/322 and 18/19**; the residual is brotli, which the engine does not implement while WPT's `formats.js` lists it alongside the three that work, so a quarter of that category can never pass. **One file got worse for a reason that is not this bug and is worth knowing on its own:** `compression-bad-chunks` gained 15 PASS and went OK → ERROR, because an unhandled-rejection report fires at the end of a **microtask** rather than of the task ([BUG-918](bugs/BUG-918-OPEN.md)) — so a promise handled one `await` later, the ordinary shape of `promise_test`, is wrongly reported. Never read a page-level `unhandledrejection` (or a `[unhandled-rejection]` line on stderr) as proof that nothing handled the promise.

- **A residual TIMEOUT that prints nothing is a page the engine never finished laying out — run it standalone before reading its source, and grep `bugs/` before filing what you find.** Measured 2026-08-22 (WPT-RUN-6 slice 23, `tests/wpt/verify_layout_hangs.py`): every residual id can be run on its own under `--dump-layout` against a plain http server with a timeout, so a hang is provable in 15 s without wptrunner and without `testharness.js` — the only way to reach the part of the residual a source marker cannot, because a reftest has no harness output by construction and its *reference* page never appears in any test id (5 of the snapshot's 16 wedges are the reference, not the test). Of the five mechanisms found this way, **three were already filed** by slice 11 from the opposite direction — [BUG-801](bugs/BUG-801-OPEN.md) (Grid auto-placement loops forever when an item reaches past the last explicit line), [BUG-802](bugs/BUG-802-FIXED.md) (nested flex costs 2× per level: 16 → 0.27 s, 18 → 1.21 s, 20 → 4.91 s), [BUG-803](bugs/BUG-803-OPEN.md) (`parse_svg_transform` loops on any token not shaped `alpha(`, and panics outright on `transform=","`) — so grep `bugs/` for the symptom before spending a bug number; what a re-measurement adds belongs in the existing file. The two genuinely new ones kill the process instead of failing a test: `document.createElement` costs ~142 µs and interns a wrapper nothing frees (`_lumen_element_wrappers` is purged only by `_lumen_gc_collect` over *released* arena nids), so 40 000 elements reach a 1.4 GB heap and a fatal V8 OOM with rc=133 ([BUG-849](bugs/BUG-849-OPEN.md)), and `backdrop-filter: blur(100000px)` is rasterized at face value — 16 s for one screenshot, because the box-blur accumulator is primed with `for i in -r..=r` per row and the radius is never clamped to the layer ([BUG-850](bugs/BUG-850-OPEN.md)). **Consequences:** `timeout_audit.py` now carries the measured `id → mechanism` table as its second stage (`MEASURED_HANGS`, disable with `--no-measured`), which outranks the source-marker stage — a page proven to hang beats anything its text suggests, and for the Grid loop no source rule can exist at all (whether an item reaches past the last explicit line depends on the resolved track count, which `repeat(auto-fill, …)` makes a function of the container width); and to sample a wedged browser's stack here gdb must **launch** it (`ptrace_scope=1` allows tracing a descendant only, so `gdb -p` is refused, and a running inferior is stopped with `SIGUSR1` rather than `interrupt`).

- **A TIMEOUT that already ran some subtests has told you where it stopped — read `wptreport`'s partial subtest list before reading the test.** `wptrunner` writes every subtest it collected before the timeout into `<shard>.json`, and 203 of the 281 residual ids of the WPT-RUN-5 snapshot name their own hung `async_test` that way, 22 of them next to subtests that reached PASS (so the page was alive and the harness had loaded). This is the only evidence that says *where inside the file* the run stopped, and it reaches what a grep cannot: `query-encoding/resources/resolve-url.js` builds `<frame>`, `<iframe>`, `<object>` and `<embed>` in one loop over `createElement(tag)`, so no source pattern can name the element that hung while the subtest name (`load nested browsing context <frame src>`) names it outright. `tests/wpt/timeout_audit.py` reads it as its third stage (`SUBTEST_MARKERS`, above the source stage, disable with `--no-subtests`), attributing a file to the mechanism claiming the *majority* of its hung subtests, and keeps the rest as `residual_hung_subtests` in `--json` (WPT-RUN-6 slice 24).
- **Four more silences measured 2026-08-22 (WPT-RUN-6 slice 24, `tests/wpt/verify_frame_load_media_gaps.py`), and one correction to what "quiet" now means.** ~~No nested browsing context is ever *fetched*~~ — half of that is gone: `<iframe src>` loads since [BUG-480](bugs/BUG-480-OPEN.md) slice 1 (2026-08-23) and `<frame src>` since [BUG-854](bugs/BUG-854-FIXED.md) (2026-08-25) — the `<frame>` fix is *only* an element and a tag in the collector, because `load_frame_sub_documents` was already tag-independent, so a `<frame>` now fetches its child, runs its scripts, fires `load` on the host and answers `window[name]`/`contentDocument`/`contentWindow` exactly as an `<iframe>` does, wherever it stands (`<frameset>` or plain `<body>` — WPT's own `name-attribute.window.js` says «this works without `<frameset>`, so great»). `<object data>` and `<embed src>` still produce no HTTP request at all ([BUG-798](bugs/BUG-798-OPEN.md)) — proved on the probe's own server, since neither the page ([BUG-438](bugs/BUG-438-FIXED.md)) nor the browser log ([BUG-826](bugs/BUG-826-FIXED.md)) is evidence here. **What still holds:** `contentWindow`/`contentDocument` return objects rather than `undefined` even for a frame that loaded nothing, so the reflexive `if (iframe.contentDocument)` guard passes and the probe hangs instead of throwing — do not read "no TypeError" as "the frame loaded". And nothing of a frame's *content* is painted yet, so a fired `load` says nothing about the screen. **One adjacent trap the `<frame>` fix left behind:** `frame.src` is the resolved absolute URL (the spec's `[ReflectURL]`) while `iframe.src` is still the raw attribute, because `iframe_element.rs` puts an own property on each element that shadows the correct prototype row — the BUG-796 shadowing shape in a second place ([BUG-920](bugs/BUG-920-OPEN.md)), which also costs `iframe.referrerPolicy` its enum normalization; never compare the two tags' `src` in one probe and read the difference as a URL bug. ~~`<details>` never opens from script at all~~ — **fixed 2026-08-25** ([BUG-851](bugs/BUG-851-FIXED.md)), and the half worth carrying is that the report named two defects while there were six, four of which only a measurement could name. `open` was flipped in **two** places on one scripted click (a `click` listener on `document`, which also dispatched `toggle`, and `_lumen_run_activation_behavior`), so the handler saw `open=true` and the next statement saw the attribute gone; a script *write* to `open` reported nothing at all, there being no other dispatch site. **The third defect is the one the report could not see**, because it measured `.click()` from script: the **native mouse click** double-flips too — the shell dispatches the trusted `click` into JS, that reached the same document listener, and then the shell's own `FormClickAction::ToggleDetails` flipped the attribute back and fired a *second* `toggle` — so `<details>` did not open by any means available to a user either. Everything now funnels through the HTML LS §4.11.1 attribute change steps, hooked as a pair of wrappers over `_lumen_set_attr`/`_lumen_remove_attr` (the `_mo_notify` shape), with the flip itself left in the shell for the native path — the attribute has to be in the document before the `relayout_form()` that follows. **Four things a probe must know about the shape of that fix.** `toggle` is a **queued task**, so nothing has been delivered by the statement after a write, and two changes in one turn coalesce into ONE event spanning both (`open = true; open = false` → a single `closed` → `closed`) — that is the spec's own «queue a details toggle event task», not an optimization. The event carries a `target` now, because `_lumen_dispatch` sets none ([BUG-873](bugs/BUG-873-OPEN.md)) and a page watching several `<details>` through one listener cannot otherwise tell them apart — the defect surfaced only as `name-attribute.html` going TIMEOUT → **ERROR**, since after BUG-591 an assertion thrown from a listener reaches the harness. An `<a>` with **no `href`** no longer stops the activation-target walk (HTML LS §4.6.1 makes it a placeholder, not interactive content): BUG-837's tag table had never been consulted for this, the deleted listener having walked ancestors itself, and removing it turned `anchor-without-link.html` 2/2 → 0/2 until the walk was fixed — after which `anchor-with-inline-element.html` went TIMEOUT → OK as well. And only the **first** `<summary>` child is the disclosure control (§4.11.2). **Consequence:** a `<details>` opening is a legitimate way to sequence a probe again — but only if the `open` write goes through a live document; one inside a `DOMParser` document or an `innerHTML` fragment still fires nothing ([BUG-919](bugs/BUG-919-OPEN.md)), `<details>` has no `beforetoggle` at all, and a `preventDefault()` on a native click does not stop the shell's flip. ~~`ResizeObserver` delivers no initial observation, so a callback armed on a static element is never entered~~ — **fixed 2026-08-23** ([BUG-661](bugs/BUG-661-FIXED.md)): delivery was piggybacked entirely on the relayout pipeline, so a page that observed something and then sat still got nothing at all; `observe()` now queues its own delivery pass as an event-loop task, waiting (bounded) for the first layout snapshot rather than reporting a wrong 0×0 first entry to a document the shell has not laid out yet. Three neighbours went with it: `observe()` throws `TypeError` on a non-`Element`, `contentBoxSize`/`contentRect` are the content box rather than a copy of the border box, and a detach+reattach at the same size notifies again. **Consequence for a probe:** an RO callback is now a legitimate way to sequence one — and, since [BUG-807](bugs/BUG-807-FIXED.md) (fixed 2026-08-24), so is an `IntersectionObserver` one: `observe()` queues its own first delivery the same way instead of waiting for a relayout, and a target with no box is now reported as an empty box rather than skipped, so «observe and wait» no longer hangs on a static page (what it reports is still governed by BUG-626/627/628 — `root`/`scrollMargin` are ignored and `isVisible` is `undefined`, so never *read* a value off that first entry, only use its arrival) — but only for what the engine can measure: the scrollbar gutter is still not subtracted and a `%`/`calc()` padding is still not resolved, so a content-box size on such an element stays equal to its border box. ~~`content-visibility`/`contain-intrinsic-size` are absent from `getComputedStyle` while `contentvisibilityautostatechange` has no dispatch site at all~~ — **fixed 2026-08-25** ([BUG-852](bugs/BUG-852-FIXED.md)), and the half worth carrying is that draining the queue the way the report proposed would have delivered *wrong* events. The state itself was computed from the wrong thing: `collect_cv_skipped` called a box skipped when its child list was empty, which mismatches in both directions — an empty `content-visibility: auto` element (exactly what `content-visibility-auto-state-changed-first-observation.html` builds) looked skipped wherever it stood, while layout never consulted the rule for it at all, `cv_should_skip` being gated on `!children.is_empty()` since there is nothing to skip otherwise. So the relevance rule moved out of layout's thread-local path into `lumen_layout::cv_is_skipped(relevant, top_y, scroll_y, viewport_h)` — one rule, two callers — and the diff base became a `NodeId → skipped` map of **every** auto box, because «new node ⇒ skipped, gone ⇒ not skipped» cannot express what CSS Contain L2 §4.1 asks for: the first observation fires in **both** directions, and a node that left the tree fires nothing (`…-removed.html`). **Three things a probe must know about the shape of that fix.** Delivery is once per frame from `RedrawRequested`, not a task per transition, because `refresh_cv_state` has four call sites and **two of them run before a JS context exists** — so events queue and go out on the first frame that has one (a page declaring `content-visibility: auto` in its markup would otherwise lose exactly its own first observation), and two state changes between frames coalesce into one event. The IDL accessor `oncontentvisibilityautostatechange` was missing while the content-attribute half already worked (`_lumen_is_on_attr_name` accepts any `on*`), which is what `'oncontentvisibilityautostatechange' in el` asks about. And the event carries a `target`, since `_lumen_dispatch` sets none ([BUG-873](bugs/BUG-873-OPEN.md)). **Residual:** `/common/rendering-utils.js` is not vendored, so `waitForAtLeastOneFrame` is a `ReferenceError` that takes `…-removed.html` whole and two «content attribute» subtests with it — a vendoring gap, not the engine; the event itself fired there. **What does work, and is worth knowing before blaming it:** `<body onload>`, `window.onload`, `readystatechange`, inline `on*` attributes, and attribute writes made from an ordinary click listener — an exception thrown by a window `load`/`DOMContentLoaded`/`visibilitychange` handler used to be swallowed whole ([BUG-591](bugs/BUG-591-FIXED.md), four bare `catch` in `_lumen_apply_ready_state`/`_lumen_apply_visibility`), which is what turned the 9 `css/css-shapes/spec-examples` FAILs into NOTRUN + TIMEOUT — **fixed 2026-08-23**, see the BUG-591 entry above.

- **`test_driver.click(element)` cannot work on this engine, and it fails one step earlier than this file used to say.** Measured 2026-08-23 (WPT-RUN-6 slice 25, `tests/wpt/verify_focus_mutation_animation_gaps.py --variant testdriver-click-path`, which replays `resources/testdriver.js::click` verbatim): the call starts with `inView(element)` → `getPointerInteractablePaintTree` → `element.getClientRects()`, which **does not exist** ([BUG-478](bugs/BUG-478-OPEN.md), duplicated as BUG-551/BUG-580), so it throws *synchronously* — `document.elementsFromPoint` ([BUG-464](bugs/BUG-464-OPEN.md)/[BUG-477](bugs/BUG-477-OPEN.md)) is the next missing link and `testdriver-extra.js::get_context`'s `document.defaultView` ([BUG-622](bugs/BUG-622-OPEN.md), still undefined) only the third. That correction matters for triage: fixing `defaultView` alone unblocks nothing, and the whole `css/selectors/focus-visible-*` cluster (12 ids, 18 with its neighbours) hangs here rather than in anything focus-related. Related split found the same day: after the shell's next pump the `:focus` **style** is applied, while `Element.matches(':focus')` (and `:focus-visible`/`:focus-within`) still answers `false` — the selector-matching path does not resolve dynamic pseudo-classes at all, which [BUG-560](bugs/BUG-560-OPEN.md) had explicitly left un-investigated.
- **Four more ways a probe page measures the engine instead of its subject, all measured 2026-08-23 (WPT-RUN-6 slice 25, same probe).** `new WebSocket(url)` **blocks the whole document** until the handshake settles — against a server that accepts TCP and stays silent the page never runs another statement, timer or paint (0 ticks, [BUG-856](bugs/BUG-856-OPEN.md); the refused-port half of the same synchronous connect was [BUG-772](bugs/BUG-772-FIXED.md), **fixed 2026-08-24** — the Fetch §3.9 "bad ports" list is now enforced in the shared URL-shape validators `require_http_scheme`/`require_ws_scheme` (`crates/network/`), i.e. ahead of DNS and of any TCP connect, for `fetch()`/XHR/subresources/navigation/`EventSource`/`WebSocket` alike, so a probe can no longer reach 22/25/6667/… on any of them and gets an instant `blocked port: N` instead of a ~2.5 s OS timeout), and `send()` throws `TypeError` on anything that is neither string nor buffer instead of stringifying it ([BUG-862](bugs/BUG-862-OPEN.md)) — so a probe may only open a WebSocket against a server it controls *and* must send strings. ~~`Animation` is not an `EventTarget`~~ — **fixed 2026-08-24** ([BUG-808](bugs/BUG-808-FIXED.md), which absorbed the duplicate [BUG-860](bugs/BUG-860-DUPLICATE.md)): `anim.addEventListener('finish'|'cancel'|'remove', …)` works now, the events are `AnimationPlaybackEvent`s, and `cancel` carries the spec's null `currentTime`. **Two things a probe must know about the shape of that fix, both of which decided whether it was a fix at all.** The events are **queued as event-loop tasks** (§4.4.3), so nothing has been delivered by the statement after `finish()`/`cancel()` — a unit test must pump (`_lumen_tick_timers()`), and a synchronous dispatch is actively *worse* than none: WPT's `EventWatcher` is armed **after** the triggering call (`new EventWatcher(t, anim, 'finish'); anim.finish(); await w.wait_for('finish')`), so an immediate event arrives with no wait registered and fails the subtest as «Not expecting event» — measured at −2 subtests in `scroll-animations` synchronously against +1 as a task. And `web-animations/interfaces/Animation/style-change-events.html` builds one subtest per `Object.keys(Animation.prototype)` entry, so **anything added to that prototype must be non-enumerable** or the fix invents failing subtests named after engine internals (`constructor`/`_fire`/`_onRemove` did, until they moved to `Object.defineProperty`); the older `_scheduleRaf`/`_tick`/`_applyAtP`/… are still enumerable there and still generate such subtests. **Residuals:** `remove` still has no trigger (no automatic replacement — `persist`/`commitStyles`/`replaceState` are absent too, [BUG-704](bugs/BUG-704-OPEN.md)), `event.timelineTime` is `null` until the first rAF tick (`_wa_current_time` moves only there), and a bare `currentTime` seek does not un-finish an animation ([BUG-861](bugs/BUG-861-OPEN.md)). `MutationObserver` is silent for *script* mutations too, not just parser ones ([BUG-855](bugs/BUG-855-OPEN.md) — `removeAttribute`, `insertBefore` and `replaceChild`'s insertion queue nothing, record siblings are always `null`; `appendChild`/`removeChild`/`characterData` do work), so never sequence a probe on those records. And `selectionchange` is dispatched nowhere in the engine ([BUG-857](bugs/BUG-857-OPEN.md)) although `select` is.
- **Two things about requests that only the server side can tell you, and one of them contradicts the project's own privacy plan.** `navigator.sendBeacon` returns `true` for a **relative** URL and sends nothing at all (the BUG-780 shape a second time), and an `ArrayBuffer`/`TypedArray` body arrives empty with `Content-Type: text/plain` ([BUG-858](bugs/BUG-858-OPEN.md)) — an absolute URL with a string or `Blob` body works. And **no outgoing request carries `Referer` or `Origin`** — not a subresource, not `fetch()`, not a beacon, not even a same-origin POST ([BUG-859](bugs/BUG-859-OPEN.md)), while `docs/plan/privacy.md` promises `strict-origin-when-cross-origin`. Both were found only because the probe's own http server records the method, path and headers of everything it is asked for; the page cannot see either, and the browser log is not evidence (BUG-826). Reuse that pattern for anything header-shaped.
- **Two traps in `timeout_audit.py` itself, both hit while extending it (slice 25).** A new module-level predicate silently **shadows** an existing one of the same name — `_resource_timing_marker` was defined twice and the second definition quietly disabled slice 22's rule; only the `--selftest` caught it, so run it after every edit and never assume a new helper name is free. And the subtest stage **outranks** the source stage, so a loosely-worded `SubtestMarker.name` inverts precedence that the source table already got right: a name matching a bare `document.open` took 32 ids away from `iframe-no-nested-context`, where "the frame never loads" is the earlier cause. Keep a subtest name as narrow as the thing you actually measured. Third, on mutation-testing the rules: a source rule and a subtest rule for the same mechanism need **separate** selftest cases, or a mutation of the lower stage is unobservable (4 of 12 mutants survived the first pass for exactly that reason).

- **~~A WPT test that declares `<meta name=timeout content=long>` still only gets the harness's 10 s~~ — fixed 2026-08-24 ([BUG-796](bugs/BUG-796-FIXED.md)), and the shape it belonged to is the reason to keep reading this entry.** `WindowTestEnvironment.prototype.test_timeout` (`resources/testharness.js:225`) walks `getElementsByTagName("meta")` and reads `metas[i].content`, and that reflection used to be shadowed on every element by the wrapper's own `content` accessor — the `HTMLTemplateElement.content` getter, which returned `undefined` for any non-`<template>`. **The general lesson: `_LUMEN_WRAPPER_MEMBERS` (`crates/js/src/dom.rs`) is installed on a per-interface prototype that sits one link BELOW the interface prototype (BUG-849), so anything declared there outranks every `_lumen_install_reflection` row and every hand-written `HTML*Element.prototype` accessor** — a tag-specific member in that table silently swallows the same-named IDL attribute of every other interface, which is exactly how `meta.content` came to answer `undefined` while `getAttribute('content')` worked. `content` now lives on `HTMLTemplateElement.prototype`; nothing tag-specific belongs in the shared table. Two things the fix carried: it also closed `[SameObject]` on `template.content` (the fragment *node* was already stable, but the wrapper was minted per read, so `t.content !== t.content` and expandos on the fragment were lost), and the `HTMLMetaElement.charset` the bug report asked for was **not** added — that name has no IDL attribute on the interface at all (checked against `tests/wpt/interfaces/html.idl`), the obsolete member is `scheme`. **Consequence that outlives the fix:** a corpus run gets *more expensive*, not cheaper — a `long`-declared test that still hangs now hangs for 60 s instead of 10, ≈ +42.8 h over the full corpus (measured WPT-RUN-5 slice 30, which also showed the fix buys **zero** extra passes), so a flat `--shard-timeout-per-id` must not be used after it. The split in a run's records stays the way to tell the two timeout sources apart: a `test_status` carrying the harness's own `"Test timed out"` at ~10 s means the page decided, while `test_timeout: 60` in `test_end`'s `extra` with no `test_status` at all means `wptrunner` cut it at 60 + 5 s. In the WPT-RUN-5 snapshot **2 933 of the 3 558 `timeout=long` TIMEOUTs are the first kind**. **Consequence for triage:** every ~10 s TIMEOUT recorded on a `long`-declared test *before* 2026-08-24 — that is, the whole WPT-RUN-5/6 snapshot — says nothing about the subject under test and must be re-measured, `websockets/keeping-connection-open/001.html` (20 s by design, filed against WebSocket backpressure) included.

- **Two more silent blind spots in `timeout_audit.py`, both found in slice 26 and both fixed there — copy the fix rather than the shape.** `_GENERATED_SUFFIXES` lacked `.any.sharedworker-module.html`, `.any.serviceworker-module.html` and `.any.window-module.html`, so `source_path` left those ids pointing at a file that does not exist and the source stage read them as empty — 20 ids invisible, no error anywhere, exactly the UTF-16 failure mode one entry above it. And a mechanism's `ref` is free text that nothing validates: `mixed-content-blocked` carried `BUG-796` (an unrelated file) for 17 slices while claiming 34 ids; the real owner is `WPT-RUN-10`, because the blocked URLs are the run's own `www1.127.0.0.1`/`not-web-platform.test` aliases — an `http://127.0.0.1` page is a *potentially trustworthy* origin (`origin.rs::is_potentially_trustworthy` treats loopback as secure) and a plain-hostname alias is not, so every cross-origin subresource is blocked as mixed content before DNS is consulted. **Consequence:** when a mechanism's count looks stable and uninteresting, check its `ref` against the bug file's actual title once — a wrong ref reads as "owned" in every report the tool prints.

- **An event dispatched from script reaches only the node it was dispatched on — no ancestor, no `document`, no `window`, in either phase — and a real click reaches `document` but still not `window`.** Measured 2026-08-23 (WPT-RUN-6 slice 27, `tests/wpt/verify_callback_import_preload_gaps.py --variant bubble-detail`): `inner.dispatchEvent(new Event('x', {bubbles: true}))` fires the listener on `inner` and nothing else, because the element wrapper's `dispatchEvent` (`crates/js/src/dom.rs:4696`) calls the single-node `_lumen_dispatch`, while the native-input path (`_lumen_dispatch_bubble`, `:1112`) walks ancestors and ends at `document` with `window` nowhere in the chain ([BUG-873](bugs/BUG-873-OPEN.md)); `event.eventPhase` is `undefined` and `composedPath()` is missing ([BUG-577](bugs/BUG-577-OPEN.md)). Adjacent and just as invisible: `document.on<type> = fn` sticks as a property and is never invoked — `document.dispatchEvent` reads only the listener registry, and the engine's own `readystatechange` delivery goes through that same method — while `'onerror' in window`, `'onresize' in document` and `'onnavigate' in navigation` all answer `false` although assignment works ([BUG-874](bugs/BUG-874-OPEN.md)). **Consequence for any probe or harness:** never listen on a common ancestor for an event your script dispatches, never install a page-level handler as `document.on<type>`, and never feature-detect with `'onX' in Y` — `addEventListener` on the target itself is the only combination that works.
- **Three surfaces that look implemented and are inert: a shadow tree's slots and scripts, an import map's relative values, and the Navigation API outside `navigation.navigate()`.** All measured 2026-08-23 (same probe). `attachShadow`, `root.innerHTML`, `root.querySelector('slot')` and `HTMLSlotElement` all work, yet no slottable is ever assigned (`assignedNodes()` stays empty for a light-DOM child carrying the matching `slot=`) and `slotchange` is dispatched nowhere in the workspace ([BUG-876](bugs/BUG-876-OPEN.md)); `host.shadowRoot` returns a *fresh* wrapper on every read, so `host.shadowRoot !== host.shadowRoot` and a listener attached through one read is unreachable from another ([BUG-877](bugs/BUG-877-OPEN.md)); and a `<script src>` appended into a shadow root is never even requested ([BUG-878](bugs/BUG-878-OPEN.md)). An import map *is* applied, but a relative value in it (`{"m": "./x.js"}` — how nearly all of WPT's `import-maps/` is written) is never resolved against the map's base and dies as `module './x.js' not found`, while an absolute value and a plain `import('./x.js')` both work ([BUG-879](bugs/BUG-879-OPEN.md)). `navigation.navigate()` dispatches `navigate`/`currententrychange` properly — but `location.href = '#1'` performs the navigation and fires nothing at all, and `navigation` has no `on<type>` properties to hang a handler on ([BUG-881](bugs/BUG-881-OPEN.md)). **Consequence:** a probe must not sequence anything on slot assignment, on a shadow-tree script, on a relative import-map value, or on a navigation it did not start through `navigation.navigate()`.
- **~~`requestIdleCallback` is the last engine-driven callback whose exception still goes nowhere~~ — fixed 2026-08-23, and it was never the last one; `<img>` still fires no `load` in any of the four ways a test can ask for it.** Re-measured 2026-08-23 (same probe): the rIdle callback ran (`deadline.timeRemaining` is a function) and its exception reached no `error` listener. A `grep` for the bare `catch(e) {}` shape then found **33** such sites in the page shim, not one — see the [BUG-591](bugs/BUG-591-FIXED.md) entry above for the list and for what is still swallowed outside `dom.rs`. For images, the `onload=` attribute and an `addEventListener('load')`, on a parser-written `<img>` and on a script-created one, are all silent although the probe's server serves both files, and `img.complete` is `undefined`, so the synchronous fallback is out too ([BUG-630](bugs/BUG-630-OPEN.md)). Two neighbours measured the same way: a `Link: <...>; rel=preload` **response header** produces no request either, on the document or on a subresource — this half outlived BUG-826's fix and is now [BUG-906](bugs/BUG-906-OPEN.md), and ~~the Resource Timing buffer stays empty for `<img>`, `fetch()`, `XMLHttpRequest` and `EventSource` alike, with no `performance.onresourcetimingbufferfull`~~ — **fixed 2026-08-25** ([BUG-839](bugs/BUG-839-FIXED.md), see its own entry below; `EventSource` is the one initiator still silent). **Consequence:** a probe that needs to know a subresource arrived must record it on its own server — the page cannot be told, and the browser log is not evidence.

- **A probe that opens a second window has already killed itself, and one that leaves a frame to script never loads it.** Measured 2026-08-23 (WPT-RUN-6 slice 28, `tests/wpt/verify_window_history_jsurl_gaps.py`). `window.open()` — and a click on an `<a target=_blank>` — replaces the *calling* document instead of creating an auxiliary context: the opened page is fetched and runs, while the opener's own `setInterval` never fires again (5 heartbeats before the call, none in the following 8 s, no `visibilitychange`) ([BUG-883](bugs/BUG-883-OPEN.md)), which is a barrier standing *earlier* than [BUG-797](bugs/BUG-797-OPEN.md)'s channel-less stub. And of the ways to point a frame at a document, the boundary is **not** the one that entry was filed under: `createElement` is fine, *timing* is what decides. The shell loads sub-documents in a single pass inside `parse_and_layout`, which runs **after** the page's own scripts — so a frame built and appended by an inline `<script>` loads perfectly (measured on `<frame>` and `<iframe>` together, P1 2026-08-25, correction appended to [BUG-885](bugs/BUG-885-OPEN.md)), while anything inserted after that pass — from a `load` handler, a timer, rAF — or a `src` assigned to an already-inserted frame produces no request at all, which is the live half of BUG-885. Proved on the probe's own server, since neither the page nor the browser log is evidence here. **Consequence for triage:** a WPT test that builds its frame inside `onload = function () {…}` (all of `query-encoding/resources/resolve-url.js`) is blocked by BUG-885; one that builds it at top level of a classic script is not. Inside a frame that *did* load, `window.parent === window`, so the `parent.foo()` idiom half of WPT's frame tests use lands in the child's own global, `iframe.contentDocument` is empty while `contentWindow` is an object, and `window.length` stays 0. **Consequence:** never sequence a probe on a second window, and write any frame you need into the markup with its final URL.
- **Three more silences in the same neighbourhood, and two things that turned out to work.** A `javascript:` URL is never executed anywhere — parser `<iframe src>`, `iframe.src =`, an `<a href>` click, `location.href =` — and `open()` sends it to the network as `unsupported scheme: javascript` ([BUG-884](bugs/BUG-884-OPEN.md)); watch the log there, because the URL text contains whatever marker your probe printed inside it. `history.back()` onto an entry made by `history.pushState(state, "")` — the form nearly every WPT history test uses — dispatches **no `popstate` at all**, while the same call with a third (URL) argument does, and `history.state` updates synchronously either way ([BUG-886](bugs/BUG-886-OPEN.md), which corrects BUG-829's "popstate delivery works"). `window.close()` is a no-op and `window.closed`/`window.name` are `undefined` ([BUG-887](bugs/BUG-887-OPEN.md)); `document.open()`/`document.close()` do not exist ([BUG-888](bugs/BUG-888-OPEN.md)). What does work, and was suspected before it was measured: `location.reload()` and `history.go(0)` fully — three generations of the document cost three requests on the server — and `window.postMessage` to self for exactly three spellings of `targetOrigin` (`'*'`, the exact origin, `'/'`), while a trailing slash, a doubled slash, both dictionary forms and the one-argument form drop the message without a word ([BUG-717](bugs/BUG-717-OPEN.md)).
- **A probe's own request log must count, not de-duplicate.** The slice-28 probe recorded served paths into a set before printing, which turned "the document was re-requested three times" into "the document was requested" and nearly produced a bug about `location.reload()` not revalidating. The distinction between *was it fetched* and *was it fetched again* is exactly what a reload/cache/`no-cache` question turns on — print counts (`GET /x.html x3`), and remember that a probe page filtered out of the log to keep it readable cannot answer anything about itself.

- **What the page can *read back* is missing far more often than what it can do — and one of those gaps answers a probe with a confirming echo.** Measured 2026-08-23 (WPT-RUN-6 slice 29, `tests/wpt/verify_cssom_svg_interface_gaps.py`). The CSSOM is not wired at all: `document.styleSheets` and `<style>/<link>.sheet` are `undefined`, no rule class exists as a global, `new CSSStyleSheet()` throws ([BUG-471](bugs/BUG-471-OPEN.md)/[BUG-746](bugs/BUG-746-OPEN.md), [BUG-897](bugs/BUG-897-OPEN.md)) — and `adoptedStyleSheets` is a plain expando: assigning `[null]` to it neither throws nor does anything, and then reads back as an array of length 1, so «assign → read → matches» proves *nothing* there. An SVG element written by the parser is an `HTMLUnknownElement` — `instanceof SVGElement` is false and `getBBox` does not exist — while `createElementNS` gets the typed prototype but none of the animated-attribute reflection, because `svg.rs` declares `x`/`width`/`viewBox` in constructor bodies the prototype swap never runs ([BUG-889](bugs/BUG-889-OPEN.md)): `el.x.baseVal` throws on both paths. Also absent: `DOMRect`/`DOMPoint`/`DOMMatrix` (and `getBoundingClientRect()` returns a plain `Object` with no `toJSON`, [BUG-522](bugs/BUG-522-OPEN.md)), `StaticRange` ([BUG-533](bugs/BUG-533-OPEN.md)), `CustomElementRegistry` as a global though `customElements` itself works ([BUG-890](bugs/BUG-890-OPEN.md)), `XSLTProcessor`/`document.evaluate` ([BUG-891](bugs/BUG-891-OPEN.md)), `document.forms`/`scripts`/`links` though `document.images` is live ([BUG-892](bugs/BUG-892-OPEN.md)), `input.valueAsNumber`/`valueAsDate` ([BUG-893](bugs/BUG-893-OPEN.md)), `shadowRoot.append` and `document.append` ([BUG-895](bugs/BUG-895-OPEN.md)), and CSS/text module scripts, where the file *is* fetched and then the type is rejected ([BUG-896](bugs/BUG-896-OPEN.md), JSON modules work). **Consequence for a probe:** never feature-detect the engine through these and never build a fixture on one of them; and never put a malformed WebAssembly module on a probe page — a 32-byte one with an unbounded declared local count costs 6.95 s of CPU ([BUG-898](bugs/BUG-898-OPEN.md)), which reads as a hang.
- **A `timeout_audit.py` mechanism key may be shared by two stages, but two different `ref`s under one key silently rename the row's owner.** `print_report` builds its owner map by `.update()`ing the stages over one another, so whichever table is applied last wins — the slice-26 «a wrong ref reads as owned» trap arrived at from the other side. Sharing the key itself is deliberate and pre-existing (`iframe-no-nested-context`, `img-no-load-event`, and since slice 29 `cssom-stylesheets-missing`): one mechanism, a noisy form proven by the browser's own error text and a silent one inferred from the source. A `--selftest` assertion now guards the ref half, so add the same `ref` string to both entries rather than a variant of it.

- **A `<track>` a probe builds with `createElement` now loads and answers — one the parser wrote still does not, and neither one's cues are painted.** [BUG-775](bugs/BUG-775-FIXED.md), fixed 2026-08-24 (it absorbed [BUG-795](bugs/BUG-795-DUPLICATE.md), which had named the missing `HTMLTrackElement.track` as a second symptom of the same hole). The reason the split falls exactly there is worth carrying: `<track>` loading used to live *only* in `crates/shell/src/tracks.rs::load_video_tracks`, which walks the **parsed** document once per navigation — so a script-created track was never fetched at all, and a parser-written one was fetched before any script could arm a handler. The model now runs in the JS shim (`video_bindings.rs`), which is where the element is visible at insertion time, so `track.onload`/`onerror`, `track.track`, `track.readyState` and `video.textTracks` all work for the script-created case; the parser-written case keeps the older shape (loaded, listed, silent — the [BUG-804](bugs/BUG-804-OPEN.md) class). **Consequences for a probe:** build the track from script, and give it `default` + `kind=subtitles` if you want to read `cues` (a track whose mode is `disabled` reports `cues === null` per spec, even though the file was parsed — the load itself is deliberately *not* gated on the mode, since nothing re-runs it on a mode change). Do not read a fired `load` as evidence that anything is on screen: cues loaded this way never reach the shell's overlay store. And the cue object is thinner than it looks — `getCueAsHTML()` and every cue setting (`align`/`line`/`position`/`size`/`region`) are absent ([BUG-902](bugs/BUG-902-OPEN.md)), while `parse_vtt` itself mis-cuts blocks that are not separated by blank lines and accepts a form feed after the `WEBVTT` signature ([BUG-903](bugs/BUG-903-OPEN.md)) — so a probe must not use a hand-written `.vtt` as a fixture without checking it against those two.
- **Never round-trip a repo markdown file through Python text mode — `BUGS.md` contains raw CR bytes inside table cells, and universal-newline reading turns each one into a row split.** Hit 2026-08-24 editing the BUG-775 row: `io.open(p, encoding='utf-8')` (read) + `newline=''` (write) silently exploded the *untouched* BUG-792 row into four lines, because its text contains `\read_body_to_eof` where the backslash-r had been stored as an actual 0x0D. Nothing errors; `wc -l` grows by three and `git diff` shows a hunk in a row you never opened. That is the same shape as the ROADMAP gotcha above (a newline inside a table cell), and here it also invalidates every `STATUS-PN.md` pointer below the damage, since those are bare line numbers. Pass `newline=''` on **read** as well as on write, or stay with `Edit`/`sed`.

- **A window `load` handler runs *concurrently* with the UI thread's own pass over the document, so anything on the JS side that gives up on lock contention instead of waiting misbehaves there and nowhere else.** [BUG-794](bugs/BUG-794-FIXED.md), fixed 2026-08-25 — and the reason to read on is that neither half of the report survived measurement. `element.focus()` from a `load` handler does not hang and never did; the report's repro reached its element through the window's **named access** (`el`, no `var` — HTML LS §7.3.3), and *that* answered `ReferenceError` inside a `load` handler while working in the top-level script, in `DOMContentLoaded`, in rAF and in a timer. The mechanism is not a lifecycle phase at all: since ADR-023 the `load` dispatch is routed through the engine thread (`route_task_js`), so it executes in parallel with the UI thread finishing the load, and `named_access_lookup`'s deliberate `try_lock` — deliberate because the interceptor fires on *any* global-name miss, including one made by JS that a native called while holding the same lock, where a blocking `lock()` is a self-deadlock — simply lost that race (measured: the other thread holds the document 3.9 ms; `LUMEN_NO_ENGINE_THREAD=1` makes the defect vanish). The fix is `lock_document_bounded` (`crates/js/src/v8_runtime.rs`): a bounded wait (20 ms, 100 µs steps) that still declines rather than blocking, so the self-deadlock case costs latency instead of the browser. **Three consequences.** A `ReferenceError` from a `load` handler was invisible until [BUG-591](bugs/BUG-591-FIXED.md) (2026-08-23) wired `_lumen_apply_ready_state`'s bare `catch` — every "hangs in a `load` handler" report predating that date is really "threw and was swallowed", so re-measure before believing the mechanism. `document.getElementById` kept working at that same instant, because its native *waits* for the lock — do not read a working DOM call as proof that the document was uncontended. And this is the only `try_lock` in `crates/js`; any new one on a JS-visible path inherits the same race.

When you discover a non-obvious implementation detail in a specific subsystem, add it to [`subsystems/<crate>.md`](subsystems/) under the relevant crate section (in English), not here.

---

## When in doubt

- **Architecture / scope** — `docs/plan/architecture.md` (§1 Principles, §3 Architecture).
- **Dependency policy** — `docs/plan/tech-stack.md` (§5).
- **How to build / run** — `README.md`.
- **Current code state** — `git log --oneline`.
- **Why a decision was made** — `docs/decisions/ADR-*.md` or `DECISIONS.md` (historical).

If the question isn't answered by these sources — ask the user, don't assume.
