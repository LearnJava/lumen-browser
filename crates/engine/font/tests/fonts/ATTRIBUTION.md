# Test font fixtures

- `FiraMono-Regular.woff2` — Fira Mono Regular, © 2014 Mozilla Foundation,
  Carrois Apostrophe. Licensed under the SIL Open Font License 1.1
  (<https://scripts.sil.org/OFL>). Used only as a WOFF2-decoder integration
  fixture (BUG-160): real-world TrueType (`glyf`) font in WOFF2 form with the
  `glyf`/`loca` transform applied, so the decoder's triplet reconstruction is
  exercised against real data.
