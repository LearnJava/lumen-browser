# lumen-paint 🟡 (fill rects + rounded rects SDF + textured text + textured images + FontMeasurer)

- **ADR-008 Invariant 3 compliance (10D.2 audit, 2026-05-27):** paint-pure-audit verified that `build_display_list()` and all display-list builder functions are pure functions (depend only on parameters, no hidden global state). **No forbidden patterns** (`static mut`/`lazy_static`/`OnceCell`) found. Renderer caching (font faces, image cache, layer snapshots) stored via `&mut self` (not hidden state). See `crates/engine/paint/src/display_list.rs:1-12` module doc + `STATUS-P1.md` for full ADR-008 rationale.
- **Done:** `DisplayCommand` enum (FillRect, DrawBorder, DrawText, **DrawOutline**, **DrawImage**), `build_display_list` walk of LayoutBox with painter's order. Per-fragment text-shadow emission (CSS Text Decoration L3 §6): for each `frag.style.text_shadow` element — DrawText copy with offset and shadow color. Multiple shadows emitted in reverse CSS-list order (painter's order: first shadow on top of others). **text-shadow blur (P2):** `blur > 0` → DrawText wrapped in `PushFilter { filters: vec![FilterFn::Blur(sigma)] }` / `PopFilter`, where `sigma = blur / 2.0`; reuses the Gaussian blur GPU pass from `box-shadow` / CSS `filter: blur()`. **CSS Backgrounds L3 §4.6 box-shadow** (outset + inset): outset shadows emitted as `FillRect` BEFORE background; inset shadows emitted BETWEEN background and border, **without clip-pipeline**: outer = padding-box, inner = offset+inset by spread; visible shadow = outer minus intersection of outer and inner, expanded to up to 4 FillRect ring segments (top/bottom/left/right). Phase 0: `blur` ignored for both outset and inset. **CSS Backgrounds L3 §3.8 background-clip**: helper `background_clip_rect(b)` determines rect for background FillRect by `style.background_clip`: `BorderBox` (initial) — `b.rect`; `PaddingBox` — shrink by border-widths; `ContentBox` — shrink by border + padding. `Text` (L4) Phase 0 fallback to `BorderBox`. **CSS Display L3 §4 visibility:hidden** in paint emission: `is_paint_visible(b)` gate on self-emission. Block: children always traversed. InlineRun: per-frag visibility check. **CSS Color L3 §3.2 opacity:0** Phase 0 skip: `is_opacity_subtree_painted(b)` — early-return for entire subtree (opacity > 0 && < 1 Phase 0 not composited — requires off-screen pass, P2 task 4+). **CSS Basic UI L4 §5 — outline rendering**: emits `DrawOutline { rect, width, style, color, offset }` after children. Renderer draws outline as 4 fill-quads. Phase 0: `Solid` / `Auto` as single fill-quad, `Dashed` / `Dotted` via `emit_outline_side` with `dash_segments`. wgpu Renderer with **three** pipelines: fill (vertex pos + color), text (vertex pos + uv + color), **image (vertex pos + uv, RGBA-texture per image)**. Three WGSL shaders, common uniform (viewport), bind group for atlas (R8 texture + linear sampler) and `image_bgl` for RGBA images. `GlyphAtlas` 512×512 with shelf packer. `FontMeasurer<'a>` — implementation of `TextMeasurer` based on TTF hmtx/cmap for shell. Per-glyph metadata cache. Atlas uploaded to GPU only when dirty. **`DrawBorder` renderer (CSS Backgrounds L3 §6, dashed/dotted/double):** `DisplayCommand::DrawBorder { rect, widths, colors, styles }` — `styles: [BorderStyle; 4]` per-side. `emit_border_side` per edge: `Solid`/`None` → one fill-quad; `Dashed` → pattern `(2·w, w)`; `Dotted` → pattern `(w, w)`; **`Double` → two parallel rects ~1/3 border-width each, gap ~1/3 (w < 3px falls back to Solid)**. `BorderStyle` extended with `Double` variant; parser updated (`double` keyword). 6 unit tests in `renderer.rs`. Phase 0 without mitre-joints — edges rectangular, overlap at corners.
- **Done (CSS Fonts L4 §3.1 plumbing):** `DisplayCommand::DrawText` carries `font_family: Vec<String>`, `font_weight: FontWeight` and `font_style: FontStyle` from `InlineFrag.style`. Snapshot serializer prints them only for non-default values.
- **Done (real face switch in renderer):** `Renderer` holds `Vec<LoadedFace>`, `face_id_by_path: HashMap<PathBuf, usize>` for dedup, `font_provider: Option<Arc<dyn FontProvider>>`. `cached_glyphs` key is now `(face_id, glyph_id)`. Method `resolve_face_id(families, weight, style)`: iterates names in CSS Fonts L4 §3.1 order, for each calls `provider.pick_face`. Generic CSS-family (`serif`/`sans-serif`/`monospace`/`cursive`/`fantasy`/`system-ui`) skipped — Phase 0 no per-generic tables; ultimate fallback to face 0.
- **Done (codepoint cascade, CSS Fonts L4 §5.3):** at start of `render()` all loaded faces parsed into `Vec<Option<ParsedFace>>` once per frame. `push_text_glyphs` for each char tries primary face: if `cmap.glyph_index(cp)` returns `Some(non-zero)` — primary selected; otherwise iterates other loaded faces; if none have it — renders `.notdef` from primary. Per-DrawText cache `HashMap<char, (face_id, glyph_id)>` avoids repeat lookups for same characters in one line. Cascade only covers already-loaded faces. `pub const CURATED_FALLBACK_FAMILIES: &[&str]` in `lumen-paint::fallback` (24 names: Noto Color Emoji / Apple Color Emoji / Segoe UI Emoji / Twemoji Mozilla / Noto Sans CJK SC|JP|KR|TC / Microsoft YaHei / Yu Gothic / Malgun Gothic / Hiragino Sans / PingFang SC|TC / Noto Sans Arabic|Hebrew|Devanagari|Tamil|Bengali|Telugu|Gujarati|Thai + Segoe UI; emoji listed before CJK). `Renderer::preload_curated_fallbacks()` — shortcut. 7 unit tests on list.
- **Done (image pipeline + register_image API):** new wgpu pipeline for RGBA-texture quad with per-image bind group. `register_image(src, &Image)` loads decoded pixels to GPU; re-registration replaces old entry. `unregister_image(src)`, `clear_images()`, `has_image(src)`. `ImageRegisterError` enum: `EmptyImage` / `TooLarge { w, h, max }`. `convert_to_rgba`: Gray8/GrayAlpha8/Rgb8/Rgba8 conversions. Without cache hit — fallback to light-gray placeholder fill-quad.
- **Done (object-fit / object-position, CSS Images L3 §5.5):** `DisplayCommand::DrawImage` carries `object_fit: ObjectFit` + `object_position: ObjectPosition`. `fit_image_quad(box_rect, intrinsic_size, fit, position) -> Option<(Rect, [f32;2], [f32;2])>` computes visible rect + UV-bounds; for `cover` / `none` with image larger than box — box-clipped quad with clamped UV.
- **Done (text-decoration):** FillRect-s emitted for each fragment with non-empty `TextDecorationLine`: underline — below baseline (+10% fs), line-through — above baseline (-30% fs), overline — near line top (-78% fs). **text-decoration-style (CSS Text Decoration L3 §2.2)**: `emit_decoration_line` expands one line into set of FillRect-s by `frag.style.text_decoration_style`: `Solid` — one rect; `Double` — two parallel; `Dotted` — series of squares `t × t`, step `2·t`; `Dashed` — dashes `2·t × t`, step `3·t`; `Wavy` — series of narrow axis-aligned columns (`emit_wavy_line`) with sin-offset of center. **text-decoration-thickness (CSS Text Decoration L3 §2.3)**: `resolve_decoration_thickness` — `Auto`/`FromFont` ≈ 7% × font-size (min 1px), `Length(px)` — px directly, `Percentage(frac)` — `frac × font_size`.
- **Done:** `serialize_display_list(&[DisplayCommand]) → String` — deterministic text format for snapshot tests. 6 integration golden tests. Mechanism `UPDATE_SNAPSHOTS=1` for regenerating golden files.
- **Done (Compositor scaffolding + two-buffer commit, P2 1B):** new module `lumen-paint::compositor` — traits `Layer`, `LayerTree`, `Compositor`. **Two-buffer model (like Chromium):** `commit(trees, layer_tree)` puts state in pending-buffer without touching active; `flush_pending() -> bool` atomically promotes pending → active. Concrete Phase 0 impls: `BasicLayer` / `BasicLayerTree`, `InProcessCompositor` (synchronous single-thread promotion).
- **Done (Compositor thread, P2 1B step b):** `ThreadedCompositor` + `ThreadedCompositorHandle` — thread-safe variant on `Arc<Mutex<ThreadedState>>`. Multi-thread tests: `cross_thread_commit_and_flush`, `cross_thread_concurrent_commits_last_wins`.
- **Done (Vsync tick-loop 60fps, P2 1B.2):** `VsyncNotifier` — `Mutex<bool>` + `Condvar` shared between main and compositor threads via `Arc`. `commit()` calls `notifier.notify()` — compositor thread wakes immediately instead of sleeping up to poll interval. Without commits: thread sleeps `TARGET_FRAME_DURATION` (16_667 µs = 60 fps). Dirty-flag prevents lost wakeups. `CompositorThread::shutdown()` notifies the condvar before join. 2 new tests: `compositor_thread_wakes_on_commit_faster_than_full_frame`, `vsync_notifier_dirty_flag_prevents_lost_wakeup`.
- **Done (DisplayCommand layer-ops definitions, P2 1B):** `BlendMode` enum (17 modes) with `from_keyword(&str)`. Variants `DisplayCommand::PushClipRect { rect } / PopClip`, `PushOpacity { alpha } / PopOpacity`, `PushBlendMode { mode } / PopBlendMode`.
- **Done (PushBlendMode/PopBlendMode pipeline Phase 0, P2 1B.3):** renderer holds `blend_mode_stack: Vec<BlendMode>`. `PushBlendMode { mode }` pushes, `PopBlendMode` pops. Helper `current_blend_mode(stack) -> BlendMode` returns top-of-stack or `Normal` for empty. Phase 0: all modes render with Normal pipeline (ALPHA_BLENDING); real pipeline switch deferred to P2 1B.4. 5 unit tests.
- **Done (DisplayCommand layer-ops emission, P2 1B step a):** `build_display_list_ordered` emits `PushOpacity`/`PopOpacity` for `opacity<1`, `PushBlendMode`/`PopBlendMode` for `mix-blend-mode != Normal`, `PushClipRect`/`PopClip` for `overflow` ∈ {hidden, clip, scroll, auto}. 13 unit tests.
- **Done (DPR / scale_factor support, [P3] 3A step a):** `Renderer` stores `scale_factor: f64`, initialized in `new_async` from `Window::scale_factor()`. Surface stays in physical pixels, but in `render()` viewport uniform recalculated as `(config.w / scale_factor, config.h / scale_factor)` — gives shader CSS-px coordinates. Pub API: `set_scale_factor(f64)` + getter.
- **Done (P2 item 2B — stacking-aware hit testing):** new module `lumen-paint::hit_test` — `hit_test(point: Point, root: &LayoutBox) -> Option<HitTestResult>`. Returns top DOM-node under cursor + `local_point` + `path: Vec<NodeId>`. Algorithm — reverse CSS painting order. **Transform inversion:** for boxes with `style.transform` builds forward-matrix and inverts via `Mat4::invert_2d_affine()`. 14 unit tests.
- **Done (P2 item 2A renderer-side — paint order consumer):** `build_display_list_ordered(root, &StackingTree, &PaintOrder) -> DisplayList`. Bucket-per-SC. Phase 0: phases 3/4/5 lumped into one bucket. Old `build_display_list` kept for backward compat. 4 unit tests.
- **Done (multi-size + variation-aware glyph atlas, P2 quality):** `SIZE_BINS = [8, 12, 16, 20, 24, 32, 48, 64]`. `size_bin_for(font_size)` rounds up to nearest bin. Atlas cache key — `AtlasKey { face_id: u16, glyph_id: u16, size_bin: u16, coords_hash: u64 }`. ATLAS_DIM raised from 512 to 1024. **`DisplayCommand::DrawText` field `font_variation_axes: Vec<([u8;4], f32)>`** — user-space (tag, value) pairs from CSS `font-variation-settings`. Renderer normalizes via `normalize_variation_axes(face, axes)` — clamping + linear [-1,0,1] + avar — and passes normalized coords to `glyph_resolved_with_coords`. Per-face normalization cached in `norm_coords_cache` within each DrawText call. 16 unit tests.
- **Done (PushClipRect/PopClip → wgpu scissor rect, P2 1B step c part 1):** renderer transitioned from three flat draw-stages to ordered `Vec<DrawOp>`. `sync_scissor_to_stack` lazily pushes `DrawOp::SetScissor(DeviceScissor)`. DPR-aware. Phase 0: real clipping for `overflow: hidden` / `clip-path: inset` now works.
- **Done (PushOpacity/PopOpacity → alpha-multiply, Phase 0 approximation, P2 1B step c part 2):** renderer holds `opacity_stack: Vec<f32>`. Before each fill/text/image draw-command computes `effective_alpha(opacity_stack)` (product of all). Phase 0 limitation: overlapping elements within one opacity-group rendered with pairwise alpha-blend instead of single-pass off-screen composite. 9 unit tests.
- **Done (DrawOutline dashed/dotted rendering, CSS Basic UI L4 §5.3):** `emit_outline_side(out, side_rect, horizontal, width, color, style)`. Pure function `dash_segments(total_length, dash_len, gap_len) -> Vec<(offset, length)>` centers pattern. `OutlineStyle::Auto` rendered as Solid. 9 unit tests.
- **Done (GPU texture upload for layer snapshots, P2 1B.5):** new `GpuLayerSnapshot` struct (Rgba8Unorm texture + `image_bgl` bind group + dimensions). `SnapshotUploadError` enum: `EmptySnapshot` / `TooLarge` / `InvalidDataSize`. `Renderer::upload_layer_snapshot(id, pixels, w, h)` — validates and uploads CPU RGBA pixels to GPU; `evict_layer_snapshot(id)` / `clear_layer_snapshots()` / `has_layer_snapshot(id)` / `snapshot_dimensions(id)`. New `DisplayCommand::DrawLayerSnapshot { id: u64, rect: Rect, alpha: f32 }` — compositor emits to composite a cached snapshot without re-rendering the layer. Render loop: looks up by id in `layer_snapshots`, renders full-texture quad via image-pipeline with given rect and alpha. Serializer: `DrawLayerSnapshot id=N (x, y, w, h) alpha=A.AAA`. 3 new snapshot/unit tests (serialization, zero-alpha, full-alpha). Total: 258 unit tests + 11 snapshot tests = 269 tests.
- **Done (CSS Filter Effects L1 — GPU filter pipeline):** `DisplayCommand::PushFilter { filters: Vec<FilterFn> } / PopFilter` added to enum. `box_layer_ops` emits PushFilter/PopFilter when `!s.filter.is_empty()`. Renderer: two new wgpu pipelines — `filter_pipeline` (color matrix filters, ALPHA_BLENDING) + `blur_pipeline` (separable Gaussian, REPLACE). `FilterCompositePlan` in render plan. `PopFilter` → `RenderPlanItem::FilterComposite` with from_level + filters. Execution: if `Blur(sigma)` in list → two-pass separable blur (H: src_level→scratch, V: scratch→src_level, REPLACE); then color filter pass (src_level→parent, ALPHA_BLENDING). WGSL `FILTER_SHADER_SRC`: applies chained `FilterEntry` (kind:u32, amount:f32) in loop (max 8); kinds: 1=Brightness, 2=Contrast, 3=Grayscale, 4=HueRotate(rad), 5=Invert, 6=Opacity, 7=Saturate, 8=Sepia. WGSL `BLUR_SHADER_SRC`: separable Gaussian, radius=ceil(3σ) capped at 32, uses `textureDimensions` for step. `FilterParamsCpu` / `BlurParamsCpu` repr(C) structs. Graphic test 30-css-filter.html added (14 test objects).
- **Done (CSS Filter Effects L1 §2 — backdrop-filter GPU result cache, P2 2026-06-02):** `backdrop_cache.rs` — `BackdropCache` holds only metadata (`ordinal → {input_hash, bytes, last_used}`), LRU budget 64 MB, methods `lookup`/`store`/`invalidate`/`clear`/`on_memory_pressure`; the matching GPU textures live in `Renderer.backdrop_cache_textures: HashMap<u32, OffscreenLayer>` keyed by the same ordinal (assigned in `PopBackdropFilter`, paint order). Invalidation is **total and conservative**: `display_list::hash_display_list(content, overlay, scroll_x, scroll_y, w, h)` folds every field of every command via its `Debug` repr through a non-allocating `HashFmt`→`DefaultHasher` adapter, so new `DisplayCommand` variants/fields can never produce a false cache hit (stale pixels). Computed only when `contains_backdrop_filter()` is true (pages without backdrop-filter pay nothing). On a HIT the parent→scratch copy and the H/V Gaussian blur passes (the expensive part) are skipped; the colour-filter blit and element composite always run. On a MISS the blur V-pass writes into the cache texture (instead of scratch) and a filter-only backdrop copies parent→cache directly; the cache texture (usage `RENDER_ATTACHMENT|TEXTURE_BINDING|COPY_DST`) is always the blit source. Renderer API: `set_backdrop_cache_enabled`/`clear_backdrop_cache`/`backdrop_cache_len`/`backdrop_cache_on_memory_pressure` (ADR-008 §10D.3/§10H — P3 wires into the `MemoryPressureSource` poll loop). The hash never influences pixels (only the skip decision), so cross-OS bit-identity is unaffected. 17 unit tests (12 cache + 5 hash/contains).
- **Done (CSS Masking L1 §9 — clip-path rendering, Phase 0):** `clip_path_to_rect(clip: &ClipPath, r: Rect) -> Rect` helper computes axis-aligned bounding box for any ClipPath variant. `Inset(sides)` — exact CSS shorthand expansion (1–4 values). `Circle`/`Ellipse` — center defaults to box center, bounding box. `Polygon` — vertex bounding box. `walk()` emits `PushClipRect`/`PopClip` just after mask-push when `style.clip_path.is_some()`. `box_layer_ops` (for `build_display_list_ordered`) emits it as outermost clip. 6 unit tests. Graphic test 31-clip-path.html added.
- **Done (border-radius SDF rendering, P2):** `CornerRadii { tl, tr, br, bl: f32 }` struct + `FillRoundedRect { rect, color, radii }` display command. SDF WGSL shader (`sdf_rrect`) with per-vertex `center/half_size/radii` attributes — anti-aliased `smoothstep(-0.5, 0.5, d)` alpha. New `rrect_pipeline` (5 vertex attributes: `pos[2] + color[4] + center[2] + half_size[2] + radii[4]`, 56-byte `RRectVertex`). `emit_box_self` emits `FillRoundedRect` when any corner radius > 0, else falls back to `FillRect`. `DrawBorder` extended with `radii: CornerRadii` — rounded corners via tessellated 8-segment quarter-annulus arcs (`emit_border_arc`) + shortened straight sides. Graphic test 35-border-radius.html added (22 objects: uniform/pill/circle/asymmetric per-corner/clamped/nested).
- **Done (GPU linear/radial gradient pipeline, P2):** `GRADIENT_SHADER_SRC` WGSL — single shader for both gradient kinds (kind=0: linear, kind=1: radial/elliptical). `GradVertex { pos, uv }` vertex format. `GradStopCpu` / `GradParamsCpu` repr(C) 16-byte aligned structs. `GradParamsCpu` layout: p0+p1 (UV endpoints for linear; center+semi-axes for radial), n_stops, kind, repeating, padding, then 16×`GradStop` array. `gradient_bgl` (group 1, Uniform) + `gradient_pipeline` (ALPHA_BLENDING). Per-gradient `DrawOp::Gradient { v_start, v_count, grad_batch_idx }`. Helpers: `push_grad_quad`, `resolve_gradient_stops` (CSS Images L3 §3.5.1 position assignment: first=0, last=1, intermediate linear interpolation), `linear_gradient_uv_endpoints` (CSS angle convention: 0°=to top, half_len = (w·|sin θ|+h·|cos θ|)/2), `radial_gradient_uv_params` (farthest-corner: rx=max(cx,1-cx), ry=max(cy,1-cy)), `build_grad_params`. Both `repeating-linear-gradient` and `repeating-radial-gradient` supported via `repeating: bool` → WGSL `t = t - floor(t)`. Phase 0 average-stop-color stub removed. Graphic test 38-gradients.html added.
- **Done (SVG `<path>` GPU rendering, P2 2026-05-29):** new module `lumen-paint::svg_path` — `parse_svg_path(d) -> Vec<PathSegment>` (all SVG commands M/L/H/V/C/S/Q/T/A/Z, relative + absolute), `flatten_path(segs, tol) -> Vec<Vec<[f32;2]>>` (De Casteljau cubic/quadratic flattener + W3C SVG §B.2.4 arc-to-cubic conversion), `tessellate_fill(contours) -> Vec<[f32;2]>` (ear-clipping O(n²) triangulator). `DisplayCommand::DrawSvgPath { vertices: Vec<[f32;2]>, color }` — pre-tessellated triangle list. `emit_svg_shape` now fills `SvgShapeKind::Path { d }` arm (was `deferred to P2`). Renderer: `DrawSvgPath` handled via existing fill pipeline (`FillVertex` + `DrawOp::Fill`), supports scroll offset + transform stack. 20 unit tests in `svg_path::tests`.
- **Done (CSS 3D transform primitive — perspective-correct rendering, P2 2026-05-29):** `Mat4` (in `lumen-layout::property_trees`) gained 3D constructors — `perspective(d)` (`M[3][2] = -1/d`, gives `w' = 1 − z/d`), `rotate_x/rotate_y/rotate_z`, `rotate_3d(x,y,z,θ)` (normalizes axis, css-transforms-2 §3.2 half-angle formula), `translate_3d`, `scale_3d`, `from_3d` (`matrix3d` column-major), `project_point(x,y,z)` (full 4×4 apply + perspective divide by `w'`), `is_2d_affine()` (renderer fast-path flag). Renderer `apply_affine_to_verts` / `apply_affine_to_rrect_verts` now branch on `is_2d_affine()`: 2D affine keeps the old bit-identical path; any non-affine (3D/perspective) matrix is projected perspective-correctly per vertex. This renders flattened 3D transforms (card flips, perspective tilts, rotateX/Y) correctly. 18 unit tests in `property_trees::tests` + 3 in `renderer::tests`. `// CSS:` marker in `forward_box_transform` for P4 to add 3D `TransformFn` variants + `perspective` property wiring (see STATUS-P4 "Needs wiring").
- **Done (3D depth-sort compositor — `transform-style: preserve-3d`, P2 2026-05-29):** `display_list.rs` gained the painter's-algorithm depth sort for 3D rendering contexts (CSS Transforms L2 §6.2). `depth_sorted_child_order(children) -> Vec<usize>` returns child indices back-to-front by transformed z (smallest/farthest painted first, nearest last); split into the pure `depth_order_by_z(&[f32])` (stable sort, NaN→coplanar) + `child_z_depth(box)` (applies `forward_box_transform` to the box center, takes raw z via `Mat4::transform_z`, no perspective divide — sort happens in 3D-context space). Gated behind `establishes_3d_rendering_context(b)` (currently `false`; `// CSS: transform-style` marker — P4 flips it to `b.style.transform_style == Preserve3d`). Wired into both `walk` and `walk_with_anim` child loops; the flat path is bit-identical to before (document order). New `Mat4::project_point_z` / `Mat4::transform_z` in `lumen-layout::property_trees`. 11 unit tests (5 layout + 6 paint).
- **Done (GPU depth buffer for `preserve-3d`, P2 2026-05-29):** `FillVertex` gained a `z: f32` field (CSS depth in pixels, positive=closer). FILL_SHADER_SRC maps it to WebGPU NDC depth `clamp(0.5 - z/20000, 0, 1)` — z=0→depth=0.5 (2D elements, painter's order via LessEqual), positive z→front, negative z→back. `fill_pipeline` now has `DepthStencilState { Depth32Float, depth_write: true, compare: LessEqual }`. Renderer holds `depth_texture/depth_view` (`Depth32Float`, surface-sized), created in `init_pipelines` and recreated on `resize()`. The main frame render pass (`target_level==0`) attaches the depth view with `Clear(1.0)` on frame start or `Load` for subsequent same-frame batches. `apply_affine_to_verts` extended: 2D affine path keeps z=0 (depth=0.5, painter's order unchanged); 3D/perspective path now calls `project_point_z` and stores the projected z via `VertexPos::set_depth` (default no-op, overridden by FillVertex). Correct GPU occlusion for intersecting `preserve-3d` planes once P4 wires `transform-style`. 5 new unit tests (z default, 2D leaves z=0, rotateX sets z, perspective propagates z, NDC mapping correctness).
- **Done (Print PDF renderer side, P2 2026-05-30):** `DisplayCommand::PageBreak` marker separates pages in print display lists. `build_print_display_list(pages: &[Page]) -> DisplayList` walks each `PageFragment.layout_box` with a `PushTransform { matrix: Mat4::translation_2d(0, dy) }` to shift fragments from document-flow y to page-local y; `PageBreak` inserted between pages. `split_at_page_breaks(cmds: Vec<DisplayCommand>) -> Vec<Vec<DisplayCommand>>` splits a flat print DL into per-page slices (PageBreak consumed). `Renderer::render_print_pages(font_bytes, pages, page_w, page_h) -> Result<Vec<Image>, _>` creates a headless renderer and calls `render_to_image()` per page. 6 unit tests. P3 handoff: shell `--print-to-pdf` collects images and writes to PDF via a PDF-writer crate.
- **Done (GPU depth buffer extended to text/image/rrect, P2 2026-05-30):** `TextVertex`, `ImageVertex`, `RRectVertex` все получили поле `z: f32` (CSS depth px). WGSL `TEXT_SHADER_SRC`/`IMAGE_SHADER_SRC`/`RRECT_SHADER_SRC` мапят z в WebGPU NDC через ту же формулу `clamp(0.5 - z/20000, 0, 1)`, что и `FillVertex`. text/image/rrect pipelines получили `DepthStencilState { Depth32Float, depth_write: true, compare: LessEqual }` — идентично fill pipeline. `VertexPos::set_depth` реализован для TextVertex/ImageVertex/RRectVertex, поэтому `apply_affine_to_verts` автоматически прокидывает projected z через 3D-путь (`project_point_z`). `apply_affine_to_rrect_verts` отдельная функция — тоже обновлена: 3D-путь пишет `RRectVertex.z`. Теперь cross-type depth testing полный: 3D-transformed текст/картинки/SDF-rrect корректно перекрываются с background-rect под `preserve-3d`. 8 новых unit-тестов (z карриит поля, push_image/push_rrect эмитят z=0, 3D affine прокидывает z в Text/Image/RRect).
- **Done (CPU software rasterizer — tiny-skia, feature `cpu-render`, P3 8A.5+8A.6):** `crates/engine/paint/src/cpu_raster.rs` — deterministic pure-software renderer for cross-OS pixel-identical snapshot tests (no GPU, no driver variance). Covers: `FillRect`, `FillRoundedRect`, `DrawBorder`, `DrawOutline`, `DrawLinearGradient`/`DrawRadialGradient` (incl. repeating), `DrawConicGradient` (per-pixel libm-free atan2 sweep), `DrawSvgPath` (tessellated triangles, Winding), `DrawImage` (grey placeholder), `DrawText` (bundled Inter via `lumen_font::Rasterizer`), rectangular clipping (`PushClipRect`/`PopClip` + `PushScrollLayer`/`PopScrollLayer`), group opacity (`PushOpacity`/`PopOpacity`), 2D transforms (`PushTransform`/`PopTransform` bilinear), **mix-blend-mode** (`PushBlendMode`/`PopBlendMode` — all 16 CSS modes via `tiny_skia::BlendMode`, `plus-lighter` → `Plus`), **CSS `filter` chain** (`PushFilter`/`PopFilter` → `LayerComposite::Filter`; **Gaussian blur** via SVG Filter Effects three-box-blur approximation `r = round((√(4σ²+1)−1)/2)`, three separable running-sum passes per axis, integer-only so cross-OS bit-identical — no `f32::exp`; **colour filters** brightness/contrast/grayscale/hue-rotate/invert/opacity/saturate/sepia mirror the GPU `apply_filter_fn` on un-premultiplied sRGB, `hue-rotate` uses a libm-free sin/cos minimax polynomial; `walk` emits `PushFilter { Blur(σ) }` around box-shadow `FillRect` and text-shadow `DrawText`; `walk` also emits element-level `PushFilter`/`PopFilter` when `style.filter` is non-empty), **`backdrop-filter`** (`PushBackdropFilter`/`PopBackdropFilter`, CSS Filter Effects L1 §6.2; on Push the painted backdrop under the element bounds is filtered in-place — base layer cloned, filter chain applied, blitted back through a rect `Mask`, `Source` blend; `PopBackdropFilter` no-op), **gradient `mask-image`** (`PushMaskLinearGradient`/`PushMaskRadialGradient`/`PushMaskConicGradient`/`PushMaskImage` → `PopMask`, CSS Masking L1 §4; mask = gradient ALPHA channel × layer ALPHA, mirrors GPU `MASK_COMPOSITE_SHADER`; `PushMaskImage` maps to identity mask since CPU path has no decoded pixels; `mask-mode: luminance` for gradient masks is a CSS gap owned by P4). Opacity/transform/blend/filter/backdrop/mask share one `LayerComposite` stack (`LayerComposite::Opacity/Transform/Blend/Filter/Mask`). `snapshot_cpu.rs` in lumen-driver covers 33 pages pixel-for-pixel (incl. `15-box-shadow`, `52-text-shadow-blur`, `30-css-filter`, `26-mask-image`). 30+ unit tests in `cpu_raster::tests`.
- **Done (Software WebGL backend, P1 §7F 2026-06-02):** `crates/engine/paint/src/webgl.rs` — `SoftwareWebGl`, a pure-Rust WebGL 1.0 state machine + CPU rasterizer behind `canvas.getContext('webgl')` (consistent with `cpu_raster`'s deterministic CPU path). Owns the RGBA framebuffer (top-left origin), vertex buffers, shader/program objects, vertex-attribute pointers (with captured `ARRAY_BUFFER` binding, byte stride/offset), uniform state and viewport. `draw_arrays` reads clip-space NDC positions from attribute 0 and flat-fills `TRIANGLES`/`TRIANGLE_STRIP`/`TRIANGLE_FAN`/`POINTS`/`LINES`/`LINE_STRIP` via barycentric triangle rasterization with source-over alpha blending. **No GLSL execution** — the fragment colour is the most recent `uniform4f` value (white if unset). NDC→pixel mapping flips GL's bottom-left Y to the framebuffer's top-left. Exported as `lumen_paint::SoftwareWebGl`; JS side in `lumen-js::webgl_canvas`. 19 unit tests.
- **Done (PA-1 shared scalar modules, P2 2026-06-11):** four cross-backend scalar-math modules extracted per the paint-pipeline review (docs/paint-pipeline-review-2026-06.md, Key finding 2 — triple duplication across femtovg/cpu_raster/renderer). `gradient_math.rs`: `resolve_stop_positions` (canonical CSS Images L3 §3.3 stop resolution, **no [0,1] clamp** — repeating gradients need out-of-range positions; femtovg wrapper clamps for its library calls), `sample_gradient_color`, `lerp_color`, `conic_sample_t`, `atan2_det` (deterministic libm-free atan2). `dash_math.rs`: `dashed_border_offsets` / `dotted_border_offsets` (border, Edge/Skia counts) + `dash_segments` (outline, leading=gap/2) — PA-5 wires these into cpu_raster BorderStyle. `matrix_util.rs`: `mat4_to_2d_affine` (column-major Mat4 → `[a,b,c,d,e,f]`). `blend_modes.rs`: canonical CSS Compositing & Blending L1 §9–10 formulas in Rust — `blend_channel` (separable), `blend_rgb` (incl. non-separable Hue/Saturation/Color/Luminosity via SetLum/SetSat), `mix_blend_rgba` (§5 simple alpha compositing; PlusLighter = L2 §6 additive), helpers `lum`/`clip_color`/`set_lum`/`sat`/`set_sat`; mirrors the WGSL `BLEND_SHADER_SRC` (incl. Rec.601 lum weights) — femtovg offscreen blend (PA-3) must consume these. Call sites converted: cpu_raster (resolve/sample/atan2/transform), renderer (`resolve_gradient_stops` wrapper, `emit_border_side` dash/dot, `dash_segments` re-export), femtovg (`resolve_stops` wrapper, conic sampling, dash fns, PushTransform). CPU snapshot gate stayed bit-identical. 55 unit tests in the new modules.
- **Deferred:** GPU-pipeline for shadows, exact breakdown of `build_display_list_ordered` into phases BlockBackgrounds / Floats / InlineContent, `BorderStyle::Groove` / `Ridge` / `Inset` / `Outset`, exact polygon masking for clip-path (Phase 1: stencil buffer), border-radius elliptical (rx≠ry). CPU cpu_raster deferred: `PushMaskLayer`/`PopMaskLayer` (SVG mask sources with `MaskMode`; gradient `mask-image` is done, only the SVG-mask-source path remains). WebGL deferred: GLSL execution (per-vertex colour/texture sampling — currently flat `uniform4f` fill), `drawElements`/indexed draws, real textures.
- 340 unit tests + 21 snapshot tests = 361 tests (default build); 464 tests with `--features cpu-render`.

## Planned: RenderBackend abstraction (ADR-010, Phase 2–3)

See [ADR-010](../docs/decisions/ADR-010-render-backend-abstraction.md) for full rationale.

### Goal

Replace the single concrete `Renderer` (wgpu) with a `RenderBackend` trait so that
femtovg, vello, wgpu, and the existing cpu_raster are interchangeable at compile time
and testable side-by-side at runtime.

### Stable contract — `RenderBackend` trait

```rust
// crates/engine/paint/src/backend.rs
pub trait RenderBackend: Send {
    fn render(&mut self, content: &[DisplayCommand], overlay: &[DisplayCommand],
              scroll_y: f32, scroll_x: f32) -> Result<(), RenderError>;
    fn resize(&mut self, width: u32, height: u32);
    fn set_scale_factor(&mut self, scale: f64);
    fn register_image(&mut self, src: String, image: &Image) -> Result<(), String>;
    fn clear_images(&mut self);
    fn set_font_provider(&mut self, provider: Option<Arc<dyn FontProvider>>);
    // Extended API (RB-4, Phase 1):
    fn viewport_size(&self) -> Size { Size { width: 1024.0, height: 720.0 } }
    fn scale_factor(&self) -> f64 { 1.0 }
    fn preload_curated_fallbacks(&mut self) {}
    fn on_layer_memory_pressure(&mut self, _level: MemoryPressureLevel) {}
    fn screenshot_rgba(&mut self) -> Option<Vec<u8>> { None }  // headless only
}
```

`DisplayCommand` does not change. Layout and shell code never sees GPU-API types.

**RB-4 (2026-06-03) — Shell wired to `Box<dyn RenderBackend>`:**
- `Lumen.renderer: Option<Renderer>` → `Option<Box<dyn RenderBackend>>`
- `backend_factory::create_backend(window, font_bytes)` reads `LUMEN_BACKEND` env var and creates the right backend (Phase 1: wgpu only; femtovg/vello log a warning and fallback to wgpu)
- `WgpuBackend` implements the 4 new trait methods by delegating to inner `Renderer`
- Shell no longer imports `Renderer` directly (except in `do_print_to_pdf` local scope for `render_print_pages`)

### Backends

| File | Feature flag | Status | Notes |
|---|---|---|---|
| `backends/wgpu_backend.rs` | `backend-wgpu` | ✅ exists (current `Renderer`) | wrapped, stays as fallback |
| `backends/femtovg_backend.rs` | `backend-femtovg` | ⬜ Phase 2 | new default; OpenGL ES 2.0; no custom shaders |
| `backends/vello_backend.rs` | `backend-vello` | ⬜ Phase 3 | compute-based; stub until vello API stabilises |
| `backends/cpu_backend.rs` | `backend-cpu` | ✅ exists (`cpu_raster`) | CI / no-GPU |
| `backends/compare_backend.rs` | `compare` | ⬜ Phase 2 | renders with two backends, pixel diff |

### Feature flags

```toml
default        = ["backend-femtovg"]
backend-wgpu   = ["dep:wgpu"]
backend-femtovg = ["dep:femtovg", "dep:glutin"]
backend-vello  = ["dep:vello", "dep:wgpu"]
backend-cpu    = []
compare        = []   # requires two other backend features
```

### vello isolation strategy

ALL vello imports are confined to `backends/vello_backend.rs`. The file has two
internal functions: `translate_to_scene(&[DisplayCommand]) -> vello::Scene` and
`submit_scene(scene, renderer, surface)`. When vello releases a breaking API change,
only these two functions change — zero impact on trait, shell, or layout code.

### CompareBackend — parallel rendering

`CompareBackend { primary, secondary }` renders the same `DisplayCommand` slice with
both backends, calls `screenshot_rgba()` on each, and computes pixel diff percent.
Used in `lumen-driver` compare tests to validate a new backend against the current
reference before promoting it to default.

```bash
# validate vello against femtovg across all 57 snapshot pages
cargo test -p lumen-driver --features compare-femtovg-vello
```

### Migration path

```
Phase 1 (now):  wgpu (DX12 on Win, BUG-057 fix)  fallback: cpu
Phase 2:        femtovg default                    fallback: wgpu → cpu
Phase 3:        vello default (once API stable)    fallback: femtovg → wgpu → cpu
```

Shell reads `LUMEN_BACKEND` env var to override at runtime.

### Backend selection at runtime (shell)

```
LUMEN_BACKEND=wgpu    cargo run -p lumen-shell   # Phase 1: wgpu (default)
LUMEN_BACKEND=femtovg cargo run -p lumen-shell   # Phase 2: not yet implemented (RB-5)
LUMEN_BACKEND=vello   cargo run -p lumen-shell   # Phase 3: not yet implemented (RB-10)
LUMEN_BACKEND=cpu     cargo run -p lumen-shell   # headless only — use lumen-driver
```

**Completed RB tasks:**
- ✅ RB-1: `RenderBackend` trait + `RenderError` in `paint::backend`
- ✅ RB-2: `WgpuBackend` wrapper over `Renderer`
- ✅ RB-3: feature flags in `lumen-paint/Cargo.toml`
- ✅ RB-4: Shell uses `Box<dyn RenderBackend>` + `LUMEN_BACKEND` env var + `backend_factory`
- ⬜ RB-5: `FemtovgBackend` skeleton
- ⬜ RB-6: `FemtovgBackend` full
- ⬜ RB-7: `VelloBackend` stub
- ⬜ RB-8: `CompareBackend`
- ⬜ RB-9: FemtovgBackend → default
- ⬜ RB-10: `VelloBackend` full

## Gotchas

- **Tests in `lumen-paint::display_list` and `lumen-paint::atlas`** are unit tests. `renderer.rs` is visual, no auto-tests — verify via `cargo run`. Display list snapshot tests live in `tests/snapshot_tests.rs`.
- **BUG-057 (FIXED 2026-06-03):** wgpu Vulkan double-panic on first render (Windows). Fix: `Backends::DX12` default in `new_async` + `new_headless_async`. `WGPU_BACKEND` env var overrides.
