# lumen-image 🟡 (PNG + JPEG baseline/progressive + sniff-dispatch)

- **Done (sniff-dispatch — `decode(bytes)`):** public `decode(bytes: &[u8]) -> Result<Image, ImageError>` distinguishes PNG (`89 50 4E 47 0D 0A 1A 0A`) and JPEG (`FF D8 FF`) by first bytes and calls the corresponding decoder. `JPEG_SIGNATURE_PREFIX = [0xFF, 0xD8, 0xFF]` — third byte `FF` (start of next marker per ISO/IEC 10918-1 §B.2.4) makes sniff reliable. `ImageError` enum: `UnknownFormat` / `Png(DecodeError)` / `Jpeg(JpegError)` + `From`-impls + `Display`. Old `decode_png` / `decode_jpeg` kept for backward compat.
- **Done (PNG pipeline):** own CRC32 (IEEE 802.3 reflected, table precompiled as `const fn`); chunk reader per PNG §11.2.2; parser IHDR (13 bytes, validation per table 11.1). `Image { width, height, format, data }` stored dense row-major without padding.
- **Done (DEFLATE/zlib):** own inflate per RFC 1951 + zlib wrapper per RFC 1950. Three DEFLATE block types: stored (00), fixed Huffman (01, tables 7/8/9 bits from §3.2.6), dynamic Huffman (10, code-length-codes from §3.2.7 + re-run codes 16/17/18). LZ77 with sliding window up to 32 KB via byte-by-byte copy (correctly handles overlapping back-references like `distance=1`). zlib header: CMF.CM=8, check `(CMF<<8|FLG) % 31 == 0`, FDICT=0. Adler-32 in trailer. `BitReader` LSB-first; canonical Huffman via ranges-of-codes with Kraft-McMillan validation.
- **Done (filter undo):** all 5 PNG scan-line filters (PNG §9.2): None / Sub / Up / Average / Paeth. Wraparound u8 arithmetic; Paeth predictor in i16 for correct abs comparison.
- **Done (palette, PNG §11.2.3 + §11.3.2):** `PLTE` parser — 1..=256 RGB-triples; `tRNS` in palette context — alpha-table up to `len(PLTE)` bytes, missing entries padded with 255 (opaque); expansion of palette indices into `Rgb8` (without `tRNS`) or `Rgba8` (with `tRNS`). Orchestrator validates ordering. `PaletteError` with detailed variants. Phase 0 limitation — only `bit_depth = 8`; 1/2/4-bit palette requires bit-unpacking and is a separate task.
- **Done (sub-byte depth, PNG §7.2 + §13.12):** new module `png::sub_byte`. `unpack_bits(packed, w, h, bit_depth)` unpacks scan-lines with 1/2/4-bit depth into one byte-sample per pixel, **MSB-first**. `scale_grayscale_to_8bit(samples, bit_depth)` scales: 1-bit × 255, 2-bit × 85, 4-bit × 17.
- **Done (orchestrator `decode_png`):** checks 8-byte signature; first chunk must be IHDR; auxiliary chunks (sRGB / gAMA / pHYs / tEXt / iCCP / cHRM) ignored; IDAT-s concatenated into one zlib stream; IEND marks end. Supported `PixelFormat::{Gray8, GrayAlpha8, Rgb8, Rgba8}` at `bit_depth ∈ {8, 16}` without interlacing, **palette at `bit_depth = 1/2/4/8`**, **grayscale at `bit_depth = 1/2/4/8`**.
- **Done (16-bit downsample, PNG §11.2.2 table 11.1):** at `bit_depth = 16` for color_type 0/2/4/6 — `filter_bpp = channels × 2`, then `downsample_16bit_to_8bit` leaves only high-byte of each pair. Equivalent to `PNG_TRANSFORM_STRIP_16` in libpng.
- **Done (tRNS for non-palette, PNG §11.3.2.1):** new module `png::trns_nonpalette`. For color_type 0 (Grayscale) — `parse_trns_grayscale` reads 2 bytes as u16 (gray-sample considered transparent); for color_type 2 (RGB) — `parse_trns_rgb` reads 6 bytes as (r,g,b) triple u16. `normalize_trns_value_to_8bit(raw, bit_depth)` converts to 8-bit equivalent. Known Phase 0 limitation: at 16-bit downsample two distinct u16 values with same high byte both match.
- **Done (Adam7-interlacing, PNG §8.2 + §13.10):** `decode_adam7` handles interlace_method=1 — 7-pass layout (`ADAM7_PASSES` const). `SampleTransform` extracted as capture of common parameters for post-unfilter transformations. `UnsupportedReason::Interlaced` removed.
- **Done (JPEG baseline + progressive, ISO/IEC 10918-1):** module `jpeg/` with own decoder baseline DCT (SOF0) **and progressive DCT (SOF2)**. API: `decode_jpeg(bytes) -> Result<Image, JpegError>` — auto-dispatch by SOF marker. Output format: `Gray8` for 1-component files, `Rgb8` for 3-component YCbCr. No third-party crates. Submodules:
  - **`marker.rs`:** reader of marker-segments. Parses SOI/EOI/SOF0/**SOF2**/DHT/DQT/SOS/DRI/APPn/COM/RSTn. `Frame { width, height, components, h_max, v_max, is_progressive }`. **`ScanInfo { components, ss, se, ah, al }`** — parameters of one scan after SOS. Progressive: DC scan `Ss=Se=0`, AC scan `1≤Ss≤Se≤63`, `Al` — successive approximation bit-shift. 19 unit tests.
  - **`huffman.rs`:** canonical Huffman build (Annex C). Kraft-McMillan validation. 4 unit tests.
  - **`bit_reader.rs`:** MSB-first bit reader with byte-stuffing. On `0xFF` — peeks next byte: `0x00` → returns `0xFF` as data; otherwise marker. 8 unit tests.
  - **`idct.rs`:** direct 2D IDCT 8×8 with fixed-point ×1024. Row-pass then column-pass. Level shift +128 and clamp to `0..=255`. 4 unit tests.
  - **`color.rs`:** YCbCr → RGB per ITU-R BT.601 (JFIF §7). Integer arithmetic. 4 unit tests.
  - **`scan.rs`:** baseline entropy-coded scan loop. Chroma upsampling — nearest neighbor. Restart intervals (DRI). 1 unit test.
  - **`progressive.rs`:** multi-scan progressive decoder (§G). Allocates coefficient buffers per-component. Supports 4 scan types: **DC initial**, **DC refinement**, **AC initial** (with EOBn-extension), **AC refinement** (§G.1.2.3 — the most subtle: refine non-zero coefficients by 1 bit, new non-zero inserted between them after skipping `run` zero-positions). 2 unit tests.
- **Deferred:** arithmetic coding (SOF9-11 + DAC), lossless JPEG (SOF3), hierarchical (SOF5-7), 12-bit precision, CMYK (4-component), ICC color profiles from APP2, WebP / AVIF (Phase 2+).
- 130 unit tests (PNG: 90; JPEG: 31; sniff-dispatch: 9) + 57 integration tests (PNG: 33; JPEG: 18 — including progressive solid 4:4:4 / 4:2:0 / grayscale / DC refinement via jpegtran / rejected lossless; sniff-dispatch: 6).
