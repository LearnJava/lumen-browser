# lumen-shell 🟡 (window + render + network)

- **Done:** winit 0.30 with `ApplicationHandler` API. Three window modes: `lumen` (empty window 1024×720), `lumen <path.html>` (file → encoding → HTML → layout → paint), `lumen <http(s)://...>` (network via `HttpClient` → same stages). External CSS: `<link rel="stylesheet" href="...">` loaded from disk (relative to HTML file) or over network (relative to base URL). `ResourceBase` enum isolates relative URL resolution logic. Inter-Regular.ttf bundled via `include_bytes!`. Handlers for Resized + RedrawRequested.
- **Done (fetch+decode `<img src>` + srcset/picture):** between HTML parse and layout, shell resolves image URLs via `lumen_layout::collect_image_requests(doc, viewport)` — same picker (`resolve_image_source`) as `BoxKind::Image { src }`. Downloads bytes (file:// or http via `HttpClient`), decodes via `lumen_image::decode` (PNG/JPEG sniff-dispatch). `fetch_and_decode_images(doc, base, sink, viewport) -> Vec<(String, lumen_image::Image)>`. Key in result Vec is the picked URL — always matches `DisplayCommand::DrawImage.src` including `<picture>`/`srcset` candidates. **Intrinsic dimensions (HTML5 §10 "mapped attributes"):** if `<img>` has neither `width` nor `height` attribute, both are set from decoded image as presentational hints via `apply_intrinsic_size`; author CSS overrides. Errors silently skipped with `eprintln!` — broken image doesn't crash the whole page, paint draws grey placeholder. `LoadedPage`/`ParsedPage` now carry `images: Vec<(String, lumen_image::Image)>`.
- **Done (RP-5 external SVG rasterization, `svg_image.rs`, 2026-07-02):** `rasterize_svg(bytes, base, sink) -> Option<lumen_image::Image>` renders external SVG bytes by wrapping the markup in a minimal HTML document (`html,body{margin:0;padding:0;overflow:hidden}`) and running the same headless pipeline as `render_source_to_png`: `parse_and_layout` (deterministic, JS off, `content_type = text/html`, relative refs resolve against the image URL via `base`) → `paint_ordered` → `Renderer::render_to_image_cpu` at the intrinsic size. `svg_intrinsic_size` (CSS Images §5.1): root `<svg>` `width`/`height` (plain or `px`; `%`/units fall through) → `viewBox` w/h (comma- or space-separated) → 300×150; rounded, clamped 1..=4096; `attr_value` matches only standalone attribute names (whitespace-preceded — `stroke-width` never matches `width`). Wired into BOTH decode paths: `decode_image` (`<img>`, streaming + final) and `fetch_and_decode_background_images` (`background-image`), gated by `lumen_image::is_svg`. Does NOT call `IMAGE_CACHE.reset_new()` (an outer page render owns the cache). 6 unit tests. Deferred: re-raster at CSS box size, host `currentColor`. Implemented via Laguna M.1 (poolside) under P1 review (mixed programming); the bg-path wiring and final `svg_intrinsic_size` compile fixes are reviewer work.
- **Done (WebVTT slice 3 — shell integration, `tracks.rs`, 2026-07-03):** `load_video_tracks(doc, fetch) -> PageTracks` walks `collect_video_tracks`, picks one `<track>` per `<video>` (kind `subtitles|captions` case-insensitive, first `default` wins, else first suitable), fetches via injected closure (`fetch_vtt_text` in main.rs: `ResourceBase::resolve` -> fs read or `fetch_subresource(Media)`, utf8-lossy), drops unfetchable/invalid/empty tracks. `build_cue_overlay(tracks, video_rects, t, measure)` — for each active cue: `strip_cue_markup`, split lines, `resolve_cue_box`; auto-line cues stack UPWARD from the video bottom (`auto_offset`), explicit `line` clamps into the box; per line emits backdrop `FillRect` (black a=170, pad 0.3em) + white `DrawText` (font_size = clamp(video_h*0.06, 12..26)). `collect_video_rects` maps `BoxKind::Video` -> `(NodeId, Rect)`. Wiring: `ParsedPage`/`LoadedPage`/`Lumen`/`PageSnapshot` carry `page_tracks`; paint block injects cue commands into the scrolling page strip (or `anim_dl` under compositor offload) and keeps `request_redraw` ticking while tracks exist; `t` = seconds since `nav_start` (no real playback clock yet). 10 unit tests + live MCP/gdigrab acceptance. Mixed-programming round (step-3.7-flash worker: r1 iterator-exhaustion bug in `choose_track` — `find` consumed the candidates iterator so non-default tracks were never picked, 4/10 tests red; r2 clean fix; reviewer fixed header imports + did all main.rs wiring).
- **Done (WebVTT slice 4 — TextTrack JS API + playback clock, 2026-07-04):** `PageTracks` gained `tracks_by_video: HashMap<NodeId, Vec<LoadedTrack>>` mirroring **all** `<track>` elements of each `<video>` (kind/label/language/mode + cues); `load_video_tracks` now enumerates every track (`choose_track_index` replaces the iterator-consuming `choose_track`), fetches cues only for the chosen `subtitles|captions` track (mode `showing`), leaves the rest `disabled` with empty cues. Shell mirrors this into the process-global `lumen_js::TextTrackStore` via `Lumen::sync_text_track_store()` (clear+repopulate, keyed by `NodeId::index() as u32`), called after every `page_tracks` assignment (fresh load + bfcache restore) so navigating to a track-less page empties it. `build_cue_overlay` signature changed `t: f64` → `time_for: &dyn Fn(NodeId) -> f64`; the paint block builds the closure from the real `VideoGifStore` playback clock (`VideoPlaybackState::current_ms(epoch_ms)/1000`) per video, falling back to `nav_start` elapsed for non-GIF/not-started videos. JS side (`crates/js/src/video_bindings.rs` VIDEO_SHIM): `video.textTracks` (lazy, rebuilt while empty to pick up late shell population), `TextTrack.{kind,label,language,mode,cues,activeCues,addEventListener('cuechange'),oncuechange}`, `TextTrackCue.{id,startTime,endTime,text}`, `cuechange` fired from the timeupdate loop + seek + a deferred t=0 check; native bridge `__lumen_texttracks_json(nid)`. 1 new tracks test (all tracks exposed w/ modes) + 2 JS tests. Deferred: `addTextTrack()`, `mode`-setter re-render, non-GIF `<video>` playback clock.
- **Done (Spell slice 2 — shell integration, `spellcheck.rs`, 2026-07-03):** `spell_data_dir()` = `browser_data_dir()/spell` (portable, user drops `<locale>.aff`+`<locale>.dic` pairs there — LibreOffice dictionaries work as-is, UTF-8). `load_dictionaries(dir) -> MultiDictionary` scans sorted `*.aff` with paired `.dic` (utf8-lossy, broken pairs silently skipped); `MultiDictionary` implements `SpellChecker` (check = any dict OK; empty set = everything OK so nothing is underlined; suggest merged/deduped cap 8; locale = stems joined `+`). `extract_words(text) -> Vec<(byte_start, byte_end)>` — alnum runs plus internal `'`/`’`/`-`, digit-bearing tokens skipped, edge apostrophes/hyphens trimmed (Cyrillic-safe: byte ranges from `char_indices`). `misspelled_ranges` caps at 100. `build_spell_overlay` re-implements the paint wavy geometry (A=1.5t, λ=4t, red 221/30/30) under each bad word at `y = text_y + 0.95*font_size`, x-offsets via injected `measure` closure. Wiring: `SPELL_DICTS: OnceLock` filled by a background thread at `run_window_mode` start (russian affix expansion takes seconds); paint block underlines every `DrawText` inside the FOCUSED node's rect when `spell_target_placeholder(nid)` says it's a `<textarea>`/text-ish `<input>` (password excluded; placeholder text skipped by string equality). 12 unit tests + live MCP/gdigrab acceptance on real LibreOffice en_US+ru_RU. Mixed-programming round (nemotron-3-ultra worker: r1 logic fully green 12/12, only defect — non-exhaustive `match` on `DisplayCommand` in tests; reviewer fixed that + attribute/doc-comment placement in wiring).
- **Done (Spell slice 3 — suggestions menu + contenteditable, `page_context_menu.rs`, 2026-07-04):** Squiggle pass generalized from `spell_target_placeholder` to `spell_target(nid) -> Option<(target_node, placeholder)>` which now also matches a **contenteditable** editing host via `lumen_dom::find_editing_host` (host rect used for the DrawText scan, empty placeholder); ranges now go through `spellcheck::misspelled_ranges_with(dicts, text, &allow)` where `allow` = `spell_allow_set()` (union of the persistent user dictionary + session "ignore" set, all lowercase). New `page_context_menu::PageContextMenu` (modelled on `tabs::context_menu`, but dynamic row count): right-click on a misspelled word in a focused **single-line** `<input>` (`is_single_line_spell_input`, text/search/email/url only — textarea/contenteditable excluded because in-place value edit of one visual line is unsafe) opens `SpellMenuAction::{Use(String)×≤5, AddToDict, Ignore}`. `try_open_spell_menu` converts cursor→page coords (`page_point`), finds the DrawText under the cursor and the word via `spellcheck::word_at_x`, bails if that word is correct/allowed, else `dicts.suggest`. `exec_spell_menu_action`: `Use` rebuilds the value (`SpellTarget::apply`) → `forms::set_value` + `form_state` + `relayout`; `AddToDict` appends lowercase to `data/spell/user_words.txt` (`spellcheck::add_user_word`, loaded at startup into `spell_user_words`) ; `Ignore` inserts into session `spell_ignored`. Menu drawn in the overlay layer next to the tab context menu; dismissed by any left-click, a fresh right-click, or Escape. 25 unit tests (10 menu geometry/dispatch + 15 spellcheck incl. `word_at_x`, allow-set, user-dict persistence). Deferred: apply inside textarea/contenteditable (multi-line value editing), OS `ISpellChecker` provider.
- **Done (Spell slice 4 — apply in textarea/contenteditable, 2026-07-06):** `is_single_line_spell_input` removed; `spell_target` now returns `(target_node, placeholder, SpellTargetKind)` (`Input`/`Textarea`/`ContentEditable`) so the menu opens for all three. Problem: multi-line fields paint one `DrawText` per wrapped visual line, so a word's byte offset within that one line is meaningless against the field's full value. Fix: `try_open_spell_menu` walks the field's rendered lines in document order, collecting every line *before* the clicked one (`prior_lines`); `spellcheck::locate_line_word_in_full_text(full_text, prior_lines, line_text, word_start, word_end)` locates each prior line inside `full_text` via a forward substring search (cursor never rewinds, so a duplicate line elsewhere resolves to the *next* occurrence, not an earlier one), then finds `line_text` itself from that cursor and adds the in-line word offset — giving a global byte range into `full_text`. `full_text` is the `value` attribute for `Input` (byte-identical to the old single-line path — `prior_lines` is always empty there, so behavior is unchanged) or `lumen_dom::node_text_content` (new: DFS `textContent`) for `Textarea`/`ContentEditable`. `SpellTarget` gained `kind: SpellTargetKind`; `exec_spell_menu_action`'s `Use` branches on it: `Input`/`Textarea` rebuild the full corrected value via the existing `SpellTarget::apply` and write it back — `forms::set_value` (attribute) for input, new `forms::set_textarea_text` (detaches existing children, appends one new text node — textarea's content model is text-only, no attribute) for textarea. `ContentEditable` does **not** touch the whole subtree (would destroy nested elements/formatting) — new `lumen_dom::locate_text_offset_range(doc, host, start, end)` DFS-walks descendant text nodes (mirroring `node_text_content`'s traversal) and returns `(text_node, local_start, local_end)` only when the whole range sits inside one text node (`None` on a boundary-crossing word — skipped, not guessed), then reuses the existing contenteditable-editing primitives `delete_range` + `insert_text_at` (`Range`/`DomPosition`) to splice just that node. No `input` event dispatched after any of the three apply paths — matches the pre-existing (undocumented) precedent of the picker-overlay apply paths (color/date/select), which also mutate DOM/form_state silently. +13 unit tests (4 `lumen_dom::locate_text_offset_range`, 4 `spellcheck::locate_line_word_in_full_text`, 2 `forms::set_textarea_text`, plus updated `page_context_menu` fixtures) — all 1482 lumen-shell + 4 lumen-dom tests green. Deferred: OS `ISpellChecker` provider.
- **Done (window title):** `extract_title(&Document)` finds first `<title>` in tree, joins text children, collapses whitespace via `split_whitespace().join(" ")`. `LoadedPage { display_list, title }`. `window_title(Option<&str>)` formats: with title — `"<title> — Lumen"`, without — fallback to `Lumen <version>`. `Lumen::reload` updates window title on reload.
- **Done (keybindings):** `PageSource` enum (`Empty` / `File(PathBuf)` / `Url(String)`) stored in `Lumen`. `KeyCommand` enum (`Reload` / `Exit`) and `keybinding_for(KeyCode, ModifiersState) -> Option<KeyCommand>` — isolated from winit mapper (F5 / Ctrl+R → Reload, Esc / Ctrl+W → Exit), unit-testable. `WindowEvent::ModifiersChanged` and `WindowEvent::KeyboardInput` handled. Force-reload (`Ctrl+Shift+R`) intentionally not bound — no cache to bypass yet; reserved for future.
- **Done (runtime — integration in winit loop):** `Lumen` stores `runtime: runtime::EventLoop` and `epoch: Instant` for DOMHighResTimeStamp. Three dispatch points: `about_to_wait` → `step()` in loop `while StepResult::Ran` until `Idle` (cap 256 tasks per tick — runaway protection), then `run_idle_callbacks(remaining_ms, now_ms)`; `WindowEvent::Resized` → `deliver_observer_records(ObserverKind::Resize)`; `WindowEvent::RedrawRequested` → `run_rendering_step(epoch.elapsed_ms())` before `Renderer::render()`. In Phase 0 nobody is registered (JS engine absent), but dispatch points are in place.
- **Done (runtime — HTML event loop framework):** Module `lumen-shell::runtime` (Phase 0 framework, without winit-loop integration). Implements HTML Living Standard §8.1.4 "Event loops" + §8.1.5.1 "Update the rendering" in minimal form. **`TaskQueue`** — **per-source** FIFO queues `Box<dyn FnOnce>` (array `[VecDeque<Task>; 7]`, index via `TaskSource::as_index`). `pop` iterates sources in `TaskSource::PRIORITY_ORDER` (`UserInteraction > DomManipulation > HistoryTraversal > Networking > Timer > Rendering > IdleTask`). **`MicrotaskQueue`** — drain-all semantics. **`EventLoop::step()`** — one task per step + microtask checkpoint after it. **rAF** per §8.1.5.1: snapshot via `mem::take` before iteration; cancelled handles checked before each invocation. **requestIdleCallback** W3C §3: `IdleDeadline { time_remaining(), did_timeout() }`; `timeout_ms` optional. **Observer registries** three types (`ObserverKind::Resize / Intersection / Mutation`). **Reentrancy:** state under `Rc<RefCell<…>>` + thin `EventLoopHandle::clone()`. **Threading:** single-threaded (`Rc`/`RefCell`), like HTML event loop itself. 19 unit tests.
- **Done (headless dump modes):** three CLI flags run pipeline without winit and wgpu, print phase result to stdout, diagnostics to stderr. `--dump-source <path-or-url>` → `lumen_encoding::decode` after `detect`. `--dump-layout <path-or-url>` → `lumen_layout::serialize_layout_tree`. `--dump-display-list <path-or-url>` → `lumen_paint::serialize_display_list`. `parse_cli(args) -> Result<CliMode, String>` — isolated CLI parser, unit-testable. `parse_and_layout(bytes, ct, base, sink) -> ParsedPage` extracted from `render_bytes` — shared entry point for both window and dump modes.
- **Done (find in page — Ctrl+F):** module `lumen-shell::find`. `FindState { open, query, active }` stored in `Lumen`. `find_matches(&DisplayList, &str, &dyn TextMeasurer) -> Vec<FindMatch>` walks `DisplayCommand::DrawText`, matches case-insensitive (ASCII via `to_ascii_lowercase`, Unicode via `char::to_lowercase().next()`), computes `prefix_w` / `match_w` via `TextMeasurer`, returns absolute `Rect` + `dl_index`. Non-overlapping matches (after match skip `i += query.len()`). `build_with_overlay(base, state, matches, BarOverlay { window_size })` builds final display list: highlights inserted before their `DrawText` (active match — orange `#ff9632`, inactive — yellow `#ffeb5a`), plus UI bar in top-right corner. Bindings (`handle_find_key`): Ctrl+F opens; while bar is open — Esc closes, Backspace erases, Enter/F3 go to next (Shift+Enter/Shift+F3 — previous), printable symbols from `KeyEvent.text` go to query. Phase 0 limitations: letter-spacing/word-spacing not accounted in match rect, find bar fixed 480×40 in top-right corner.
- **Done (transform-first zoom — M0.3, ADR-016):** Ctrl+`=`/`-`/`0` update `zoom_factor` and call `App::begin_zoom_preview` instead of relayouting immediately. That scales the retained display list on the backend via `RenderBackend::set_preview_scale(zoom::preview_scale(zoom_factor, laid_out_zoom_factor))` for an instant response and arms `pending_zoom_relayout = now + zoom::ZOOM_RELAYOUT_DEBOUNCE_MS` (180 ms). `about_to_wait` folds that deadline into the `WaitUntil` wakeup and, once elapsed, runs a single `relayout()`; a burst of key presses therefore reflows only once. Every `relayout()` (from any source) re-syncs `laid_out_zoom_factor = zoom_factor`, clears the pending deadline and resets the backend preview to 1:1, so the fresh display list matches the requested zoom. `zoom::preview_scale` (identity/ratio/degenerate-guard, 3 unit tests) is the pure ratio helper.
- **Done (scroll state):** `Lumen { scroll_y: f32, content_height: f32 }`. `Renderer::render(content, overlay, scroll_y)` — page portion of display list gets Y-translate `-scroll_y`, overlay portion rendered without offset (viewport-locked). `content_height_of(&dl)` — `max(rect.y + rect.height)` across all rect-bearing commands. **Input:** `WindowEvent::MouseWheel` via `MouseScrollDelta` — `LineDelta(_, lines)` → `lines × 40 CSS px`, `PixelDelta(p)` → `p.y / DPR`. Keyboard: `ArrowDown/Up` (40 px line-step), `PageDown/PageUp/Space/Shift+Space` (90% viewport), `Home/End` (to `0.0` / `f32::INFINITY` → clamp to max_scroll). `scroll_by(delta)` / `scroll_to(target)` via `clamp_scroll`. 9 unit tests.
- **Done (scroll-to-match for find):** pure function `find::scroll_to_match(match_rect: Rect, viewport_height: f32, current_scroll: f32) -> Option<f32>`. If match already fully visible — `None`. Otherwise `target = max(0, match.y − viewport_height × SCROLL_MARGIN_FRACTION)` where `SCROLL_MARGIN_FRACTION = 0.25`. Called from `handle_find_key` after Backspace, Enter/F3, text input. 8 unit tests.
- **Done (vertical scrollbar overlay):** module `lumen-shell::scrollbar`. `build_scrollbar_overlay(scroll_y, content_height, viewport_width, viewport_height) -> DisplayList` — pure function, returns 2 `FillRect` (track + thumb) or empty `Vec` if scrollbar not needed / args are NaN-Inf. `thumb_geometry(scroll_y, ch, vh) -> (top, height)`: `thumb_h = max(MIN_THUMB_HEIGHT=24, vh²/ch) clamp to vh`, `thumb_top = (vh − thumb_h) × ratio`. Scrollbar commands inserted into overlay buffer **before** find-bar commands (painter's order: scrollbar below, bar above). Phase 0 limitations: Y-axis only, no fade-in/out. 12 unit tests.
- **Done (scrollbar drag + track-click page-jump):** `scrollbar::classify_track_click(x, y, scroll_y, ch, vw, vh) -> TrackClick { None | Thumb | Above | Below }`. `Thumb` → start `ScrollDrag { start_scroll_y, start_mouse_y }`; `Above` → `scroll_by(-page_step(vh))`; `Below` → `+page_step(vh)`; `None` → no-op. `ScrollDrag::scroll_for(current_mouse_y, ch, vh) -> f32` returns unclamped `scroll_y`. `Lumen` extended with `cursor_position: Option<PhysicalPosition<f64>>` and `scroll_drag: Option<ScrollDrag>`. Single-click track = single page-jump. 17 unit tests. Phase 0 limitations: held-track-click continuous mode not implemented.
- **Done (cursor-icon feedback on hover scrollbar thumb):** `Lumen::update_cursor_icon` called from `WindowEvent::CursorMoved` and `MouseInput Released`. Pure function `cursor_icon_for_hover(scrollbar::TrackClick, drag_active: bool) -> winit::CursorIcon`: drag active → `Pointer` regardless of position; hover thumb → `Pointer`; hover track or None → `Default`. `Lumen.last_cursor_icon: Option<CursorIcon>` caches previous value — `set_cursor` called only on change. Reuses `scrollbar::classify_track_click` for hit-test. 5 unit tests.
- **Done (smooth scroll):** module `lumen-shell::scroll_anim`. `ScrollAnim { start_y, target_y, start_time_ms }` + `ease_out_cubic(t)` + `sample(now_ms) -> (scroll_y, done)`; `DURATION_MS = 200.0` ms. `Lumen.scroll_anim: Option<ScrollAnim>` started via `start_smooth_scroll(target)` and `scroll_by_smooth(delta)` (additive over active animation target — repeated wheel/key-repeat appends delta to destination). `RedrawRequested` ticks `advance_scroll_anim()` before building display list and requests another redraw while not done. Wheel, arrow-keys, PageUp/Down/Space, Home/End, track-click, find-scroll-to-match — all go through smooth variants; thumb drag remains instant. 12 unit tests.
- **Done (preload scan + dispatch + URL resolution + dedup + priority):** `lumen_html_parser::scan_preload_hints(&source)` called before `parse()` (4B.1). `dispatch_preload_hints(hints, base, sink)` (1) resolves each raw `href`/`src` via `ResourceBase::resolve_str`, (2) stable-sorts by `FetchPriority::for_kind` (High → Medium → Low), (3) deduplicates by resolved URL (first = highest priority wins), (4) emits `Event::SubresourceHintFound { url, kind: SubresourceKind, priority: FetchPriority }`. `FetchPriority` enum added to `lumen-core::event` (High=CSS/font/preconnect, Medium=script, Low=img). `StdoutEventSink` logs `⤷ preload <kind> [<priority>] <url>`. `srcset`-strings emitted as-is. 4 tests for resolve_str + 3 for dispatch (emits, dedup, priority-sort).
- **Done (streaming pipeline — task 4B):** `EventLoop::<LoadEvent>` replaces bare `EventLoop<()>`. `LoadEvent { HtmlChunk(String) | LoadDone(RawPage) | LoadError(String) }`. Window created immediately with empty display list; `resumed()` calls `start_streaming_load()` — background thread fetches all bytes, decodes UTF-8, splits into 8 KB chunks, sends via `EventLoopProxy::send_event`. Main thread `user_event` handler: each `HtmlChunk` → `IncrementalTreeBuilder::feed` + paint every ≥150 ms using UA-stylesheet-only layout (`Stylesheet::default()`); `LoadDone` → full `render_bytes` pipeline (CSS + images); `LoadError` → log. `IncrementalTreeBuilder::as_doc()` added to `lumen-html-parser` for partial-DOM snapshots. `apply_loaded_page` helper extracted from `reload` for reuse. `paint_partial_dom` clones partial Document, calls `layout_measured` + `build_display_list`, requests redraw.
- **Done (U-1 stage 1 — non-blocking navigation):** `reload()` no longer runs the whole fetch+parse+JS+layout pipeline synchronously on the UI thread. When a window/renderer exists (i.e. every navigation after the first frame — link click, address bar, back/forward, JS `location.href=`, palette reload), it resets the streaming state (`stream_builder`/`stream_sheet`/`stream_layout_seeded`/dedup sets) and delegates to `start_streaming_load()` — the same async path the initial load uses in `resumed()`. The HTML body streams off-thread, the window keeps painting progressive frames, and the heavy final pipeline (`render_bytes`) runs once on the UI thread at `LoadEvent::LoadDone` via `apply_loaded_page`. A synchronous fallback (the old GPU-session / `source.load` body) is kept for the window-less path (headless/tests). **Load-generation guard:** `Lumen.load_generation` is bumped per navigation and stamped onto every streaming `LoadEvent` (`EarlyPreloadHints`/`HtmlChunk`/`CssLoaded`/`LoadDone`/`LoadError`); `user_event` drops events whose generation is stale, so a slow superseded load can't corrupt the new DOM or paint over the newer page (a race now reachable because the UI no longer blocks during a load). **Scroll restoration:** back/forward (and bfcache restore) stash the target offset in `Lumen.pending_restore_scroll` before `reload()` and `apply_loaded_page` applies it after resetting scroll to the top — the old code set `scroll_x/y` right after the (then synchronous) `reload()` returned, which would now be clobbered when the async `LoadDone` arrives. Stage 2 (the final `render_bytes` — incl. QuickJS — off the UI thread, ADR-006) remains, blocked by QuickJS not being `Send`.
- **Done (Web Storage persistence):** `Lumen` stores `ls_storage: HashMap<String, Arc<Mutex<WebStorage>>>` keyed by SOP origin (`scheme://host:port`). `ls_store_for_base(&ResourceBase, &mut HashMap) -> Option<Arc<Mutex<WebStorage>>>` — for URL-based pages: constructs origin key, inserts fresh `WebStorage` on first visit, returns `Arc::clone`; for file/snapshot/empty — returns `None` (no persistent storage). `origin_str()` on `PageSource` extracts the key via `lumen_core::url::Url::parse`. `ls_store` threaded through `render_bytes → parse_and_layout → run_scripts_with_dom → install_dom`. `sessionStorage` stays fresh per page load (created inside `install_dom`, never stored in shell). Phase 0: in-memory only, no disk persistence.
- **Done (persistent JS runtime + event bubbling):** `LayoutSource::document` and `ParsedPage::document` changed to `Arc<Mutex<Document>>` so QuickJS closures and Rust layout code share the same DOM without copying. `run_scripts_with_dom` no longer drops the runtime after initial script execution — instead returns `Option<Box<dyn PersistentJs>>` (the live context). `PersistentJs` trait (`eval_js`, `take_navigate_request`) encapsulates `QuickPersistentJs` behind a trait object — no `#[cfg]` on `Lumen` struct fields. `Lumen::js_ctx: Option<Box<dyn PersistentJs>>` stores the live context; dropped before `layout_source` on navigation to release Arc clones in QuickJS closures. Click events dispatched via `_lumen_dispatch_bubble(nid, 'click')` in JS, which walks the ancestor chain and fires `addEventListener` handlers at each node and at document level. `document.addEventListener/removeEventListener` now wire to `_LUMEN_DOC_LISTENER_NID = -1` sentinel. `Event.cancelBubble` added; `stopPropagation` now sets it; `stopImmediatePropagation` sets both. After JS click dispatch, shell polls `take_navigate_request` to handle `location.href=` navigation triggered by handlers.
- **Done (link click navigation + fragment anchors):** module `lumen-shell::links`. `find_link_href(doc, node_id) -> Option<String>` walks up the DOM ancestor chain from the hit-tested node to find the nearest `<a>` element with a non-empty `href` attribute (HTML5 activation behavior). `is_navigable_href(href) -> bool` suppresses `javascript:`, `mailto:` and fragment-only hrefs (`#id`). **Fragment-only navigation:** `fragment_only(href) -> Option<&str>` detects `#`-prefixed hrefs; `find_element_by_id(doc, id_value) -> Option<NodeId>` walks DOM by `id` attribute (HTML LS §3.2.6 case-sensitive); `Lumen::navigate_fragment(fragment)` updates `doc.set_target`, calls `relayout()` so `:target` CSS takes effect, then scrolls to the element rect. **`:target` on initial load:** `parse_and_layout` extracts URL fragment via `Url::fragment()` and calls `doc.set_target(fragment)` before `layout_measured`, so `:target` pseudo-class matches on first paint. 12 unit tests.
- **Done (event-loop parking + rAF pump — BUG-271, 2026-07-07):** `about_to_wait` now sets the winit control flow explicitly every pass: `WaitUntil(min(js_timer_deadline, raf_deadline))` when a deadline exists, otherwise `ControlFlow::Wait`. **Invariant: never leave a stale `WaitUntil` installed** — a past-deadline `WaitUntil` behaves like `Poll` (the loop span at ~22k iterations/s, each doing several blocking JS round-trips: `tick_timers`, `pump_websockets`, `pump_sse`, `flush_canvas_updates`). Pending `requestAnimationFrame` batches are fired by an rAF pump in `about_to_wait` (shared vsync gate `last_raf_batch_ms` with `RedrawRequested` step 3.1, ≤60 Hz combined); a repaint is requested only when a callback dirtied the DOM (`take_dom_dirty` → `relayout()`), so a pure rAF loop no longer forces a 60 fps full display-list rebuild + GPU paint. JS timer shim (`crates/js/src/dom.rs`) implements the HTML LS §8.6 timer-nesting-level clamp: nesting >5 → timeout/interval ≥4 ms.
- **Done (async JS timer integration):** `PersistentJs` trait extended with `tick_timers()` and `take_timer_wakeup() -> Option<f64>`. `about_to_wait` calls `js.tick_timers()` (runs `_lumen_tick_timers()` in JS) then reads next timer deadline via `take_timer_wakeup()`; if present, sets `ControlFlow::WaitUntil(now + delay + 1ms)` so winit wakes the event loop precisely when the next `setTimeout`/`setInterval` fires without busy-polling. `QuickJsRuntime` gains `timer_wakeup: Arc<Mutex<Option<f64>>>` field populated by `_lumen_request_wakeup` native function. After timer callbacks, any `location.href=` navigation is captured via `take_navigate_request` in the same `about_to_wait` pass.
- **Done (observers wiring):** `collect_layout_rects(lb: &LayoutBox) -> HashMap<u32,[f32;4]>` walks `LayoutBox` tree after every `relayout_page` and populates `QuickJsRuntime::layout_rects` via `js.update_layout_rects(...)`. `js.update_viewport_size(width, height)` updated on `Resized` and after every layout. `js.deliver_layout_observers()` calls `_lumen_deliver_resize_observers(); _lumen_deliver_intersection_observers()` in JS after each `relayout_page`. `PersistentJs` trait extended with `update_layout_rects`, `update_viewport_size`, `deliver_layout_observers`. All three methods gated `#[cfg(feature = "quickjs")]` at call site; `#[allow(dead_code)]` on trait declarations to silence dead_code lint in the non-quickjs default build.
- **Done (fts-omnibox):** `OmniboxSuggestion { HistoryFts { url, title, snippet }, SearchQuery { query, frequency } }` enum + `parse_omnibox_prefix()` in `address_bar.rs`. `AddressBarState` extended with `suggestions: Vec<OmniboxSuggestion>`, `selected_idx: Option<usize>`. Methods: `set_suggestions`, `select_next`, `select_prev`, `selected_idx`. `commit()` now uses selected suggestion's `commit_value()` if one is highlighted. Dropdown in `build_bar_overlay`: up to 7 rows, two-line layout for HistoryFts (title + snippet/url), one-line for SearchQuery; frequency shown as `×N`. `query_omnibox_suggestions()` in `Lumen`: `@history` → FTS only (7 hits); plain → `SearchHistory::prefix_match` (4) + FTS (4). ArrowDown/Up handle in `handle_address_bar_key`. Plain-text commits recorded to `SearchHistory`. `HistoryFts` + `SearchHistory` in `Lumen` struct, opened in-memory (Phase 0). `lumen-knowledge` added to `lumen-shell` deps. 22 new tests in address_bar module.
- **Done (F2-5 omnibox @read-later / @tabs):** added `OmniboxPrefix::{ReadLater, Tabs}` + `OmniboxSuggestion::{ReadLater { url, title, snippet }, Tab { title, url, switch_value }}`. `@read-later <query>` → `read_later_store.search()` (FTS5, §12.3, 7 hits) → `commit_value()` = URL → navigate. `@tabs <query>` → case-insensitive substring match over `tab_strip.tabs` (title + URL; active tab URL from `self.source`, background from `bg_tabs[id].source`; empty query lists all, capped 8) → `commit_value()` = `switch-tab:<id>` sentinel, intercepted in `handle_omnibox_commit` → `switch_tab(idx)` (id→index resolution survives reorder). Note: `@read-later <url>` still saves at commit time via `omnibox::resolve` (`SaveReadLater`); the new prefix only adds search-while-typing. Russian Porter stemmer + vector/HNSW index remain ⬜ (optional). 7 new tests.
- **Done (system-font fallback chain wiring):** `apply_loaded_page` calls `Renderer::preload_curated_fallbacks()` once, after the first page's `FontProvider` (its `FontRegistry`, wrapping the system font index) is set on the renderer. Guarded by `Lumen.fallbacks_preloaded: bool`. This activates the GPU renderer's CSS Fonts L4 §5.3 codepoint cascade (`pick_face_for_codepoint`): emoji / CJK / RTL / Indic / Thai faces are loaded into `Renderer.faces`, so glyphs the primary font (bundled Inter) lacks are resolved from system fonts (`Segoe UI Emoji`, `Microsoft YaHei`, … per `paint::fallback::CURATED_FALLBACK_FAMILIES`) instead of rendering as `.notdef`. One-time and idempotent: faces persist across pages and the curated families are system fonts identical for every page. Note: bundled Inter already covers Latin **and** Cyrillic (U+0400–04FF), so the original "Cyrillic fallback" framing was moot — the real gap was non-Latin/Cyrillic scripts. The CPU raster path (`cpu_raster`) intentionally has no fallback (no `FontProvider` on that path).
- **Done (Tor circuit CLI — 9F.3, 2026-06-15):** `extract_tor_mode(args)` → `(Option<u16>, Vec<String>)` (extracts `--tor` + `--tor-port N`, default port 9050). `check_tor_connectivity(port)` — TCP connect to `127.0.0.1:<port>` with 2 s timeout; prints a clear error and exits when Tor is unreachable. When `--tor` is present: overrides `FingerprintProfile` in `config::global()` with `http_profile = TorBrowser`, `socks5_proxy = "socks5://127.0.0.1:<port>"`, `no_persistent_state = true`. Config wiring passes transparently through `config::apply_http(client)` → `effective_socks5_proxy()` → `Socks5Proxy { host:"127.0.0.1", port }` → SOCKS5 tunnel for all HTTP/HTTPS. Supports Tor Browser bundle (`--tor-port 9150`). 6 unit tests for `extract_tor_mode`. This completes all 6 layers of ADR-007.
- **Done (fingerprint profile config — 9F.1):** module `lumen-shell::config`. `FingerprintProfile` unifies anti-fingerprint layers 2/3/4 into one user-facing config loaded from `fingerprint.toml` (Windows: `%APPDATA%/lumen/fingerprint.toml`; else `$XDG_CONFIG_HOME` or `~/.config/lumen/fingerprint.toml`). Hand-rolled flat `key = value` parser (no new dependency — the config has no tables/arrays): `#` comments outside quotes, optional quoting, unknown keys ignored, invalid values fall back to the field default. Keys: `http_profile` (chrome/firefox/safari/edge/tor/lumen/strict → `lumen_network::HttpProfile`), `tls_profile` (standard/strict/tor; default derived from `http_profile` via `http_to_tls_profile`), `screen_width`/`screen_height`/`color_depth`, `timezone_offset` (minutes, `getTimezoneOffset` convention), `hardware_concurrency`, `device_memory`, `platform`, `languages` (comma-separated). `apply_http(client)` stamps `with_fingerprint_profile` + `with_tls_profile` onto every `HttpClient` builder (main page fetch, subresources, downloads); `install_navigator()` (quickjs) pushes navigator/screen/timezone into the process-global `lumen_js::NavigatorProfile`. Process-global `config::global()` is initialised once in `main()` from `config::load()`; absent config → engine defaults (identical to the previous hardcoded values), so behaviour is unchanged out of the box. 15 unit tests.
- **Done (WebDriver BiDi stub — 8H.1/8H.2/8H.4, §6.11, ADR-006):** module `lumen-shell::bidi`. `lumen --bidi-port N` (`extract_bidi_port` in `main.rs`) starts `bidi::spawn(port)` — a background-thread TCP listener on `127.0.0.1:N`, one thread per connection. WebSocket upgrade + RFC 6455 text-frame codec reused from `lumen-devtools::ws` (`upgrade` / `read_text_frame` / `write_text_frame`). `bidi::protocol::dispatch(msg, &mut BidiState) -> DispatchResult { frames, close }` parses W3C BiDi commands `{"id","method","params"}` and returns one-or-more frames to write (success/error + emitted events). Implemented (Phase 0 stub): `session.status` (`ready` flips false after a session exists), `session.new` (returns `sessionId` + minimal capabilities, then emits a `browsingContext.created` event for the default context), `session.subscribe`/`session.unsubscribe` (ACK, subscriptions not stored), `session.end` (ACK + closes connection), `browsingContext.getTree` (single default context). Unknown methods → `unknown command` error; bad JSON / missing id → `invalid argument` error with `id:null`. Per-connection `BidiState` is isolated (no cross-connection sharing); context/session ids are deterministic pseudo-UUIDs from a monotonic counter (stable for tests). `script` / `network` / `input` modules and the standalone `crates/bidi/` extraction are deferred. 11 unit tests.
- **Done (tab lifecycle tier badges + restore spinner — 10K):** `shell/src/tabs/strip.rs`: T2 (BackgroundOld) tabs rendered with amber `z` badge + `TAB_T2_BG` dimmed background; T3 (Hibernated) — grey `Z` badge + `TAB_T3_BG` darker background. `build_tab_tooltip(strip, hovered_idx)` → tooltip overlay "Вкладка спит — клик восстановит (~1 сек)" for non-Active tier badges. `shell/src/panels/restore_spinner.rs`: `build_spinner(elapsed_ms, win_w, win_h)` — 8-dot animated spinner overlay at center after 200 ms threshold; `shell/src/panels/sleep_hint.rs`: `build_sleep_hint(elapsed_ms, win_w)` — top-bar tint for T2 restore >100 ms (10I). Wired in `RedrawRequested` via `restore_spinner_start_ms` / `t2_restore_start_ms` fields. 3 tests in restore_spinner + 4 in sleep_hint.
- **Done (`content-visibility: auto` shell wiring — BB-4, 2026-06-13):** `Lumen.cv_skipped: Vec<(NodeId, f32)>` (skipped auto-subtrees of the last layout pass, `(node, page top_y)`), `cv_relevant: HashSet<NodeId>` (ratchet set forwarded to layout via `set_cv_relevant`; reset on page load), `cv_events: Vec<ContentVisibilityChange>` (skip-state diffs, cap 256; consumer: P3 wires `contentvisibilityautostatechange` to JS). `collect_cv_skipped(lb)` tree scan (works for background-thread page-load layout where layout-crate thread-locals are unreachable) + `diff_cv_skipped(prev, next)` → events; `refresh_cv_state()` after every `layout_box` change; `maybe_expand_cv_relevant()` in `RedrawRequested` step 1.6 — when a skipped top enters `scroll_y + vh*(1+CV_SLACK_FACTOR)`, ratchets it relevant and triggers relayout. `relayout()` and tab-restore prime `set_cv_scroll`/`set_cv_relevant` before `relayout_page` and reset them after (sidebar/background layouts must not inherit foreign scroll/relevant). 5 tests.
- **Done (HTML5 Drag and Drop — PH3-9, HTML LS §9.3.3, §9.10):** `DndState { src_nid, press_x, press_y, active, over_nid }` + `DND_THRESHOLD = 4.0` px. `dnd_state: Option<DndState>` field in `Lumen`. `js_drag_event(nid, type, x, y)` dispatches via `_lumen_dispatch_drag_event` JS shim. Three wiring points: (1) `MouseInput::Pressed` — calls `lumen_dom::is_element_draggable()` on hovered node, sets candidate if draggable; (2) `CursorMoved` — activates drag after threshold (`dragstart`), then per-move fires `drag` on source + `dragleave/dragenter` on target change + `dragover` on current target; (3) `MouseInput::Released` — fires `drop` on current over-target + `dragend` on source, clears `dnd_state`. Borrow-safe: collects events into `DndMoveEvents` struct before releasing `&mut self.dnd_state`, then fires JS calls.
- **Done (IPC tab control + screenshot — TAB-4/TAB-5):** `--ipc-server` runs the shell as a headless tab-control TCP server (`run_ipc_server`, `CliMode::IpcServer`). It prints `LUMEN_IPC_PORT=<port>` to stdout (OS-assigned), then serves `lumen_ipc` requests over the framing layer: `CreateTab` → `TabCreated{tab_id}`, `NavigateTab{tab_id,url}` → `Navigated`, `Screenshot{tab_id}` → `Screenshot{png}` (PNG bytes), `CloseTab` → `TabClosed`, plus `Ping`/`Shutdown`. A "tab" here is a headless render context holding only its `PageSource`; the actual load → layout → CPU-raster runs **lazily on `Screenshot`** via `render_source_to_png` (extracted from `do_screenshot`, the same path as `--screenshot`). No winit/wgpu — deterministic, CI-safe, gdigrab-free PNGs for the graphic-test harness (TAB-7). Single-client sequential; tab state survives client reconnects; the server exits on `Shutdown`. Integration test `crates/shell/tests/ipc_server.rs` spawns the real binary and drives a full create→navigate(file)→screenshot→close→shutdown sequence.
- **Done (subresource prefetch cache — BUG-171 этап 1, 2026-06-19):** module `lumen-shell::prefetch`. Process-global `PREFETCH_CACHE: LazyLock<PrefetchCache>` warms external **scripts** and **linked stylesheets** off the UI thread while the HTML is still streaming, so the final `parse_and_layout` pass reads them instantly instead of blocking the winit event loop on the socket. `feed_preload_and_emit` (streaming thread) spawns a warm-up per `PreloadHint::Script`/`Stylesheet` using the **same** `http_client_for_subresource(...).fetch_subresource(url, dest)` the consumer uses, so a cache hit returns byte-identical content — script order (HTML LS §8.1.3.1) and the CSS cascade are unaffected; a miss simply re-fetches in `parse_and_layout`. CSS warm-ups additionally parse to feed the progressive-frame layout (replaces the old bare-client `load_css_for_streaming`, which also removes a redundant CSS double-fetch). The cache is generation-scoped (`reset(generation)` at navigation start in `start_streaming_load`) with in-flight dedup (first caller fills, concurrent callers block and share one `Arc<Vec<u8>>`) and stale-generation bypass. Removes network wait from the UI thread; the adblock toggle's `reload()` is already async (U-1 stage 1). 5 unit tests in `prefetch`. (Stage 2 — the full pipeline incl. QuickJS off the UI thread — landed next; see below.)
- **Done (off-UI-thread final pipeline — BUG-171 этап 2, 2026-06-19):** the heavy final render no longer runs synchronously on the winit event-loop thread. The `LoadEvent::LoadDone` handler now reads UI-only inputs (viewport, storage handles via `&mut self.ls_storage`), clones the `Send` rest (`event_sink`, `cookie_jar`, `sw_worker_store`, `cache_store`, `hyp_provider`, `load_proxy`), `std::mem::take`s `preload_dispatched`, and spawns a `std::thread` that calls `render_bytes` (fetch scripts → QuickJS → fetch+decode images/CSS/fonts → layout). The worker posts the result back as a new `LoadEvent::RenderDone(Box<RenderOutcome>, generation)`; the UI thread applies it via `apply_loaded_page` (+ Navigation Timing delivery) exactly as `LoadDone` used to. While the worker runs, the event loop keeps spinning — the window repaints the last streaming frame and stays responsive instead of freezing for the ~1.9 s JS + layout CPU phase. Unblocked by **B-1/ADR-014**: `QuickJsRuntime` is now a `Send` handle (the runtime lives on its own thread), so the JS context is created on the worker and shipped to the UI thread inside `RenderDone`. Supporting changes: `PersistentJs: Send` supertrait (so `Box<dyn PersistentJs>` crosses the thread boundary); `hyp_provider: Arc<KnuthLiangHyphenation>` (shared with the worker without losing the warmed dictionary cache); type alias `RenderedPage = (LoadedPage, LayoutSource, Option<Box<dyn PersistentJs>>)`. The U-1 **generation guard** drops a `RenderDone` from a superseded navigation (its page + JS handle drop, joining the JS thread); a current-generation `RenderDone` restores the taken `preload_dispatched`. `render_bytes`/`parse_and_layout` were already pure free fns taking only `Send`/`Arc` inputs, so no behavioural change to the pipeline itself — only *where* it runs. Compile-time regression guard: `render_pipeline_payload_is_send` asserts `RenderOutcome`/`LoadEvent`/`Box<dyn PersistentJs>`/`RawPage` are `Send`.
- **Done (decoded-image cache — BUG-172, 2026-06-19):** module `lumen-shell::image_cache`. Process-global `IMAGE_CACHE: LazyLock<DecodedImageCache>` makes the two `<img>` loaders share one fetch+decode per image per navigation. Before this, `spawn_stream_image_loads` (PH1-2c progressive streaming) and the final `fetch_and_decode_images` (in `parse_and_layout`) independently took `collect_image_requests` and re-downloaded+re-decoded the same `src`s. The cache mirrors `prefetch::PrefetchCache`: generation-scoped, in-flight dedup via `Condvar` (first caller decodes, concurrent callers block and share the result), caches success *and* failure (`Option<DecodedImage>`), stale generation bypasses. Value is `DecodedImage::{Static, Animated}` behind `Arc`, so a hit clones a pointer not the pixels (the consumer makes the owned copy). The shared fetch+decode logic of both paths was extracted into the free fn `decode_image()` (single source of truth, original logging preserved). Streaming threads call `get_or_decode(self.load_generation, …)` then emit `LoadEvent::ImageDecoded`; the final pass calls `get_or_decode_current(…)` in `parallel_map`, skipping network+decode on a hit and only computing `wants_intrinsic`/`is_lazy` + cloning pixels. Reset: `IMAGE_CACHE.reset(generation)` at navigation start (next to `PREFETCH_CACHE.reset` in `start_streaming_load`) and `reset_new()` per render in headless `render_source_to_png` (`--screenshot` / `--ipc-server` Screenshot) — bounds memory in the long-lived IPC server and prevents stale cross-page reuse. Lazy `<img>` (on-scroll) and `background-image` are separate paths, never part of the double-load, and untouched. 6 unit tests in `image_cache`.
- **Done (chrome theme palette — F2-6 stage 1, 2026-06-22):** `panels::themes::Palette` is a flat struct of role-named chrome colour tokens (tab-strip backgrounds, overlay/input/item surfaces, text/dim/divider, accent) with two const variants `Palette::DARK` / `Palette::LIGHT`; `ShellTheme::palette(os_dark) -> Palette` resolves the variant (honouring `ThemeBase::System` vs explicit Light/Dark) and overrides `accent` with the selected preset. `Palette::DARK` reproduces the previous hard-coded dark constants of `tabs/strip.rs` + `address_bar.rs` exactly, so the dark theme is pixel-identical; `LIGHT` is a soft neutral-grey set with dark text. `build_tab_bar(strip, w, pal, drag)` and `build_bar_overlay(state, bar, pal)` now take `&Palette` instead of a bare accent `Color`; the two call sites in `main.rs` pass `&self.shell_theme.palette(self.dark_mode)` so picking Light in the Appearance settings actually recolours the tab strip and the Ctrl+L omnibox. Theme-invariant indicator colours (ad-block checkbox, lifecycle Z-badges, container strips, group bars, focus ring, caret, result-tag) intentionally stay constant. 5 palette tests in `themes`.
- **Done (per-panel theming — F2-6 stage 2 part 1, 2026-06-22):** all ~22 secondary panels now follow the active theme. `Palette` gained two role tokens — `header_bg` (panel title/toolbar bar) and `row_alt_bg` (zebra rows) — with DARK/LIGHT values. Every panel render fn (`privacy_panel`, `settings_panel`, `history_panel`, `workspace_panel`, `print_panel`, `a11y_panel`, `read_later_panel`, `note_viewer`, `bookmark_panel`, `shortcuts_panel`, `command_palette`, `tree_tabs`, `cert_panel`, `vertical_tabs`, `permission_panel`, `ai_panel`, `shields_panel`, `focus_panel`, `sidebar_panel`, `pip_window`, `split_view`) takes a trailing `pal: &Palette` and maps its former hard-coded surface constants to palette tokens (bg→`overlay_bg`, header→`header_bg`, zebra→`row_alt_bg`, item→`item_bg`, selection→`item_selected_bg`, input→`input_bg`, outer border→`overlay_border`, separators→`divider`, text→`text`/`text_dim`, accent→`accent`). **Semantic colours stay constant** (status red/green/amber, container/group/category identity colours, the PiP video letterbox, modal scrims). The redraw path computes `let pal = self.shell_theme.palette(self.dark_mode)` once and passes `&pal` to every panel call site. Dark theme is close to before (per-panel bespoke shades are now unified onto the shared palette tokens — a small intentional change, not pixel-identity). **Pending (F2-6 stage 2 part 2):** user-rearrangeable panel drag&drop / docking (ADR-009 `Surface`/`SurfaceManager` + layout persistence).
- **Deferred:** horizontal scroll, momentum (free-flick on trackpad), held-track-click continuous page-jump, mouse-button input (drag-select text on page), pixel-dump (render to headless wgpu and save PNG — requires `lumen-image` encoder), Russian Porter-stemmer, profile-based persistent DB for HistoryFts/SearchHistory.
- 56 unit tests shell + **22 address_bar** + **32 find** + **34 runtime** + **29 scrollbar** (12 overlay/thumb + 9 classify_track_click + 8 ScrollDrag) + **12 scroll_anim** + **12 links** + **11 bidi** + **6 extract_tor_mode**.

## Gotchas

- **HiDPI / DPR partially supported.** Renderer stores `scale_factor` from winit. `ScaleFactorChanged` updates it on-the-fly. **Not yet supported:** layout viewport is hardcoded 1024×720 — `inner_size` not passed to layout, no relayout on `Resized`. Requires structural pipeline refactor (layout runs before window creation) — P1 concern.
- **Scroll state implemented** (Y-axis, smooth scroll out-cubic 200 ms, scrollbar drag + track-click, find-scroll-to-match, cursor feedback). Limitations: no X-axis, no momentum, no relayout-on-resize.
- **Streaming pipeline (task 4B) implemented.** Window opens immediately; HTML is parsed incrementally with `IncrementalTreeBuilder`. Intermediate frames rendered every 150 ms without CSS (UA defaults). Final frame after full pipeline (CSS + images). HTTP fetch is still blocking in background thread — TCP-level streaming is a separate task.
- **Done (F2-6 docked-panel resize + layout persist):** `panel_layout::PanelLayout` replaces the per-panel `PANEL_WIDTH` constants with runtime, drag-resizable widths keyed by panel id (`vertical-tabs`/`tree-tabs`/`ai`/`sidebar`). Widths persist in a flat `key = value` file `<exe_dir>/data/ui/panel_layout.txt` (no new dependency, mirrors `config.rs`), clamped to `[120, 600]` CSS px. The 4 docked panels' `build_panel`/`hit_test` take a `width` param; `main.rs` reads it via `left_dock()`/`right_dock()` and feeds `page_content_width_css()` + the page-shift transform. A press within `RESIZE_GRAB` (±4 px) of a dock's inner edge (`resize_edge_at`) starts a drag stored in `panel_resize: Option<(Dock, &'static str)>`; `CursorMoved` calls `drag_panel_resize` (→ `set_width` + `relayout`), release persists via `PanelLayout::save`, and the cursor shows `EwResize` over edges/while dragging.
- **Done (F2-6 cross-dock for tab sidebars, 2026-06-22):** the vertical-tabs and tree-tabs sidebars can be moved to either window edge and the choice persists. `PanelLayout` stores a per-panel dock side (`<id>.dock = left|right`, in the same flat file, backward-compatible parsing); `panel_layout::default_dock(id)` gives the compiled side (tabs→left, ai/sidebar→right); API `dock_for`/`set_dock` + `Dock::opposite`/`as_token`/`from_token`. In `main.rs`, `left_dock()`/`right_dock()` resolve the *effective* side; helpers `tab_dock_side`, `dock_origin_x` and the assoc. fn `offset_overlay_x` (shift a left-relative panel DL onto the right edge) wire paint, and the two tab hit-tests translate window-x into panel-local space. `Ctrl+Alt+B` (`KeyCommand::FlipActiveDock` → `flip_active_tab_dock`) moves the active tab sidebar to the opposite edge with a "target edge free" guard, persisting + relaying out. The default (no-override) layout is bit-identical to before (`default_dock` matches the old fixed sides, `dock_origin_x(Left)=0`). (`tab_dock_side`/`flip_active_tab_dock` were later generalised to `sidebar_dock_side`/`flip_active_sidebar_dock` — see the AI/web cross-dock bullet below.)
- **Done (F2-6 cross-dock for AI/web sidebars, 2026-06-23):** the AI assistant and web sidebar are now cross-dockable too. Both panels' `build_panel`/`hit_test` were re-based on an `origin_x` (panel left edge in CSS px) instead of a hard `window_w − width`, so they paint and hit-test dock-agnostically (right edge = `origin_x + width`). In `main.rs` the dock resolution was generalised: `tab_sidebars`→`dockable_sidebars` (all four) and `tab_dock_side`→`sidebar_dock_side`; `left_dock`/`right_dock` pick the first visible sidebar whose *effective* side matches (priority vertical→tree→ai→web), replacing the old hard-coded AI-then-web right-dock branch. AI/web paint + hit-test pass `origin_x = dock_origin_x(side, w)`. `flip_active_tab_dock`→`flip_active_sidebar_dock`: `Ctrl+Alt+B` now flips the active sidebar — a tab sidebar when one is open, otherwise the AI or web sidebar — keeping the "target edge free" guard. The page-shift transform and drag-and-drop page-coordinate conversion now use the generalised `left_dock()` (precomputed before the renderer / `dnd_state` borrows), so a left-flipped AI/web panel shifts the page correctly. +6 panel tests (`origin_x` at the left edge: hit inside/outside, close button, background rect at `x=0`). **Pending (F2-6 stage 2 remainder):** full `SurfaceManager` (ADR-009) migration of the live shell — infrastructure-only, no new user-facing capability.
- **Done (F2-6c `SurfaceManager` drag-redock + persistence foundation, 2026-06-24):** the ADR-009 `surface::` module (the still-unwired panel framework) gained the slice needed to redock panels and persist the layout, in preparation for the eventual live-shell migration off the ad-hoc `panel_layout::PanelLayout` path. **No new user-facing capability** (the running shell still uses `PanelLayout`); this is infrastructure for steps 6–8 of the migration. Added: `PanelEvent::DragStart/DragMove/DragEnd` (window-local coords) + `DragData{source_panel, grab_offset}` in `surface/types.rs`; `EventCtx::start_drag`/`requested_drag` in `surface/ctx.rs`; in `surface/manager.rs` a per-`PanelEntry` `slot_override` + `effective_slot()`, a `slot_size_overrides` map, public `move_panel_to_slot`/`set_slot_size`/`panel_slot`, a mouse-driven drag state machine (`route_mouse_down` on a `HitElement::DragHandle` → `begin_drag`; `route_mouse_move` tracks pointer + edge-zone drop slot via `slot_at`; `route_mouse_up` commits the redock), drop-highlight accessor `drop_target_rect`, and compact text-format `serialize_layout`/`apply_layout` (version-tagged, forward-compatible, no serde dep). Persisted via a new `BrowserSettings::panel_layout`/`set_panel_layout` + `BrowserSettingsSnapshot.panel_layout` field (mirrors `tab_layout`). Drop slots are resolved by window quarter-bands so empty slots stay reachable. +10 `surface::manager`, +3 `surface::types`, +2 `surface::ctx`, +2 `storage::browser_settings` tests. **Pending (steps 6–8):** implement `Panel` for the vertical-tabs/sidebar panels, register a live `SurfaceManager` in `App`, drive the content x-offset from `slot_rect("content")`.
- **Done (F2-6 web-sidebar reflow on resize, 2026-06-23):** dragging the web sidebar's edge now reflows its page content to the new width instead of stretching a frozen display list. `Lumen.sidebar_source: Option<LayoutSource>` retains the parsed DOM + stylesheet (moved in by `open_sidebar_page`, Arc-cheap); `relayout_sidebar()` re-runs `relayout_page` at the panel's current `panel_layout` width and calls `SidebarPanel::update_page(dl, content_height)` (replaces the DL, keeps the title, clamps `scroll_y` to the new content height). The resize-release handler reflows only when the dragged id is `ID_SIDEBAR` (the AI panel is drawn procedurally and already reflows during the drag via `relayout()`). 2 new `sidebar_panel` tests.
- **Done (cross-navigation HTTP cache wiring — BUG-234, 2026-06-23):** the RFC 7234 cache that already existed in `lumen-network` (`HttpCache` in-memory LRU / `DiskHttpCache` SQLite) was never attached in the shell — `with_http_cache` was only called in network tests, so every navigation (incl. repeat visits) re-downloaded all subresources. Fixed in `config.rs`: process-global `HTTP_CACHE: OnceLock<Option<Arc<dyn HttpCacheBackend>>>` lazily built by `build_http_cache(private)` — a persistent `DiskHttpCache` at `<exe_dir>/data/cache/http_cache.db` (browser-folder storage, survives restart) for normal sessions, or an in-memory `HttpCache` when `no_persistent_state` / `http_profile == TorBrowser` (nothing written to disk); `None` (caching off) if the DB cannot be opened. The wiring lives in `apply_http` — the single choke point through which **all** clients are built (main page, `http_client_for_subresource`, `fetch()`), so one cache (shared via `Arc`) covers everything; the DoH-bootstrap client bypasses `apply_http` and is intentionally uncached. `HttpClient::fetch_subresource` already implemented the cache protocol (fresh-hit → return; stale+validators → conditional GET → 304 revalidate / 200 store). 2 new config tests (`private_http_cache_is_in_memory_and_present`, `http_cache_is_shared_via_arc`); RFC 7234 behaviour covered by existing `lumen-network` MockTransport tests.
- **Done (live-window automation channel — SDC-1b, 8A.7 Ф4, 2026-07-01):** the `AutomationCommand`/`AutomationReply` channel (`lumen-driver`, published by SDC-1a) is drained once per frame in the automation-dispatch block (`main.rs`, right before the native-input drain) against the *live* tab, not a headless session. `Navigate`/`Click`/`Type`/`Scroll` were already wired (SDC-1a-era work); this pass closed the remaining gaps: `resolve_automation_target`'s `Target::Selector` arm was a `None`-returning stub — now resolves via `lumen_layout::selector_query::find_all_by_selector(layout_box, &document, selector)` against the live `LayoutSource::document` (same helper used by `check_wait_condition`, below). `Eval` used to always reply `Ack` and discard the JS result — the `PersistentJs` trait gained `eval_js_value(&self, script) -> Result<String, String>`, implemented via `JsRuntime::eval` + `JsValue::to_json_string` (the JSON serializer added in SDC-1a), so `AutomationReply::Eval(json)` now carries the real value. `Screenshot` used to reply bare `Ack` with no pixels — `render_current_page_to_png` CPU-rasterizes `self.display_list` (content only, no browser chrome) at the current window's content-viewport size and scroll offset via `Renderer::render_to_image_cpu` (same renderer as `--screenshot`/`--ipc-server`), encoded with `lumen_image::encode_png_rgba8`; images are not yet registered (empty images slice — `<img>` boxes paint their placeholder, matching existing headless CPU-render convention). `Wait` used to reply `Ack` immediately without checking anything — real `Wait` cannot block the event loop (that would freeze rendering and starve the very state — layout, JS ticks — the condition depends on), so it is queued in `Lumen.pending_waits: Vec<PendingWait>` and re-polled once per frame via `check_wait_condition` until satisfied (`Ack`) or its deadline passes (`Error`); `DocumentReady`/`NetworkIdle` approximate to "a layout tree exists" (no in-flight-request counter exists in the shell yet), `Visible` checks a matched box's rect is non-zero-sized, `Stable` only checks the selector currently matches (no cross-frame rect history yet), `JsIdle` checks `PersistentJs::has_raf_pending()` — all documented simplifications mirroring `InProcessSession::check_wait_condition`'s headless equivalents in `lumen-driver`. 2260+ existing shell tests unaffected; scoped clippy/test both clean.
- **Done (automation channel request/reply + protocol fronts — SDC-2, 2026-07-01):** SDC-1b wired command *execution* but not replies — every `AutomationReply` was sent to a `Sender` whose `Receiver` was immediately discarded (`let (automation_reply_tx, _) = channel()`), so no caller outside the shell process could ever read a result. Fixed: the channel now carries `lumen_driver::AutomationRequest = (AutomationCommand, Sender<AutomationReply>)` — each request brings its own one-shot reply channel — and `main()` builds it *before* dispatching to any `CliMode` (not inside `run_window_mode`), so `--bidi-port`/new `--mcp-live-port <N>` (spawned right after, alongside `--devtools-port`) already hold a valid `AutomationHandle` once the window's event loop starts draining it. Two new automation commands: `Query(selector)` (`query_automation_nodes` — reuses `lumen_layout::selector_query::find_all_by_selector` against `layout_box`/`layout_source.document`, same pattern as `resolve_automation_target`) and `A11yTree` (`automation_a11y_tree` — reuses `lumen_a11y::build_ax_tree` + a local `automation_ax_node` converter, same shape as `update_platform_ax_tree`). `lumen_driver::LiveWindowSession` (new, in `lumen-driver`) adapts this channel to the full `BrowserSession` trait; `lumen-bidi-server`'s `BidiState::with_live_session` and `lumen-mcp`'s new `spawn_live`/`--mcp-live-port` both drive a real window through it — see `subsystems/driver.md` for what's real vs. stubbed. `BrowserSession::current_url` changed from `&str` to owned `String` (all four implementors + the MCP test mock updated) to avoid `LiveWindowSession` having to leak memory to satisfy the old borrow. `automation_sender()` renamed `automation_handle()` (unused today — kept for a future in-process caller, e.g. `--ipc-server` mode).
- **Done (two automation live-window bugs found validating SDC-3 — 2026-07-01):** (1) `AutomationCommand::Navigate` always built `PageSource::Url(url)`, which — regardless of scheme — goes through the network `HttpClient`; a `file://` URL (what graphic_tests and most BiDi/MCP test clients actually send) failed with "unsupported scheme: file". New `page_source_for_automation_url(url)` parses `http(s)://` → `PageSource::Url`, `about:blank` → `PageSource::AboutBlank`, and `file://`/bare strings → `PageSource::File` (stripping a Windows drive-letter's extra leading slash — `file:///D:/foo` → `D:/foo`, not `/D:/foo` which doesn't resolve — while leaving a POSIX `file:///home/x`'s slash alone). (2) A command enqueued from a BiDi/MCP thread had no way to interrupt a parked `ControlFlow::Wait` event loop — an `mpsc` send from an unrelated thread is not an OS event, a scheduled timer, or a redraw request, so a `wait`/`navigate` call from an idle window (the common case for unattended automation, i.e. always) could sit undrained until *something else* happened to pump the loop. Fixed with a new no-op `LoadEvent::AutomationWake` variant: `AutomationHandle` (SDC-2) now carries a `set_wake` callback (`Arc<Mutex<Option<WakeFn>>>>` — shared so handles cloned out to `bidi_spawn`/`lumen_mcp::spawn_live` *before* the window exists still pick it up once `run_window_mode` calls `automation_handle.set_wake(move || { let _ = load_proxy.send_event(LoadEvent::AutomationWake); })` right after creating `load_proxy`); `execute()` calls it after every enqueue. Found and fixed by manually driving a live `--mcp-live-port` window end-to-end (not caught by unit tests — none of them run the real winit event loop).
- **Done (third automation live-window bug — `Click`/`Type` clicked the wrong point — 2026-07-01):** `resolve_automation_target()` returned a resolved element's box center in *page* (document) space straight from the layout tree, but `handle_click_at(x_css, y_css)` expects *viewport* space (what a real OS mouse event reports) and converts it to page space itself via `page_point()` (subtracts tab-bar height / left-docked-panel width, adds scroll). Feeding page coordinates into a function expecting viewport coordinates meant a click landed wherever page-space coordinates happened to fall in viewport space — silently "worked" only by coincidence at zero scroll with a target inside the tab-bar-height band. Found manually: clicking a real `<a href>` via MCP (`--mcp-live-port`) returned `success` but never navigated (verified via `resources://a11y_tree` + `query()` showing the old page still loaded). Fixed by applying the inverse of `page_point()` (add tab-bar height/panel offset, subtract scroll) to the `NodeId`/`Selector` branches before returning.
- **Done (fourth automation live-window bug — `Target::Point` clicked the wrong point too — 2026-07-01):** the previous fix assumed `Target::Point` was already OS-window-space (matching BiDi's `input.performActions` pointer coordinates as originally documented) — wrong. Manually testing a BiDi click at coordinates read straight off a `captureScreenshot` PNG (the natural thing a tester does — "click at the pixel where I see the button") missed by exactly `TAB_BAR_HEIGHT` (36px): `Target::Point` coordinates are in *content-viewport* space (same space the screenshot renders — no tab bar, no chrome), not OS-window space. Fixed: `resolve_automation_target` now adds the tab-bar-height/panel offset to `Target::Point` too, but *not* a scroll subtraction (unlike `NodeId`/`Selector`, which come from absolute document position) since pointer coordinates are already relative to the current, already-scrolled visible viewport.
- **Open gap found while writing tester docs (2026-07-01, not yet fixed):** `eval`/`script.evaluate` over the automation channel only works against the page the window was **launched with** (`lumen --mcp-live-port N somepage.html`) — after any `AutomationCommand::Navigate`-driven reload (i.e. any `navigate` tool call / `browsingContext.navigate`), `self.js_ctx` is `None` and eval fails with `"JS context not available"`, even in a `--features quickjs` build. Root cause not yet traced (candidates: the streaming/background-thread reload path in `start_streaming_load`/`apply_loaded_page` not wiring up `PersistentJs` the same way the initial `resumed()` load does, or a thread-affinity issue). Repro: launch with a real page, `eval` works; call the `navigate` tool once (even to the same URL), `eval` then fails for the rest of the session. Workaround for testers: avoid `eval`/`script.evaluate` after the first navigation — use `click`/`type`/`query`/`wait`/`screenshot` instead (all verified working post-navigate).
