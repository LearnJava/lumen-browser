# Ветка p1-exp-wgpu-only — экспериментальный полигон производительности

**СТАТУС: ЭКСПЕРИМЕНТАЛЬНАЯ ВЕТКА. В `main` НЕ ВЛИВАЕТСЯ. НИКОГДА.**
(решение пользователя 2026-07-08). Здесь разрешено тестировать любые технологии
и алгоритмы, ломать совместимость, снимать инварианты main-ветки. **Цель
(уточнение пользователя 2026-07-10): работать НЕ МЕДЛЕННЕЕ Chrome, а лучше —
быстрее**, на сопоставимых метриках (тёплый кадр под вводом, время до первого
кадра, CPU/GPU/RAM процесса); «100 и 1000 раз» из ранних формулировок — образ,
не число. Эталонные замеры Chrome снимать на тех же страницах и фиксировать
здесь. Удачные находки переносятся в main **отдельными чистыми ветками**,
не merge'ем этой.

Worktree: `.claude/worktrees/exp-wgpu-only`. Работать здесь, коммитить сюда.
`git push` — только по явной просьбе пользователя.

**Принцип эксперимента (директива пользователя 2026-07-09): пробовать ВСЕ
технологии и СМЕШИВАТЬ их, добавлять СВОИ решения — и искать скорость.**
Не ограничиваться одним образцом: приёмы из «Карты заимствований» ниже
(WebRender, GPUI, Slint, smithay, cosmic-text, …) можно и нужно комбинировать
в гибриды (например: SDF-эффекты GPUI внутри тайлов WebRender + damage-возраст
smithay + собственный patch_scroll_layer), а где чужого решения нет —
изобретать своё. Критерий отбора один: замер ДО/ПОСЛЕ тем же скриптом.
Неудачные комбинации откатывать и фиксировать в этом файле (как п.7),
удачные — оставлять и двигаться дальше.

---

## Что уже сделано (7+ коммитов, см. git log)

1. **OpenGL (femtovg/glutin) удалён полностью** (−4961 строк). Единственный
   рендер — wgpu `Renderer` (WGSL), `default = ["backend-wgpu"]`.
2. **BUG-274 диагностирован до корня**: wgpu/DX12 на Intel Iris Plus берёт
   ~2.3 мс CPU за закрытие КАЖДОГО render pass (вся стоимость в `drop(pass)`),
   не зависит от площади кадра (проверено `LUMEN_WINDOW=512x378` vs `2048x1512`),
   плюс разовый скачок памяти +500 МБ. Это фикс. оверхед пасса, не clear.
3. **Skip-identical-frame** в `Renderer::render`: тотальный хэш кадра +
   `content_generation` (бампается register_image/снапшотами/шрифтами/resize).
   Идентичный кадр не рисуется вовсе. Выкл: `LUMEN_NO_FRAME_SKIP=1`.
   Проверка: 5 с движения мыши над статикой = 31 skip / 2 рендера.
4. **patch_scroll_layer**: скролл overflow-контейнера правит DL in-place
   (PushScrollLayer + DrawScrollbar thumbs) вместо полной пересборки
   paint_ordered. 12×: 1.54 → 0.13 мс/тик. Эквивалентность пересборке
   закреплена 5 тестами `patch_scroll_layer_*` + бенч (ignored).
5. **Безаллокационный HashFmt** в hash_commands / diff_display_lists / TileGrid.
6. **Пофазная диагностика кадра** (см. env-переменные ниже).
7. Слияние соседних Draw-батчей уровня 0 — испробовано и **откачено**:
   в реальных планах соседних level-0 батчей не бывает (между ними composite).
8. **Ярус 1, срез 1 — кэш шрифтовых метрик + мемоизация резолва** (2026-07-09):
   `FaceMetrics` (owned: units_per_em, ascent/descent, `OwnedCmap`, advances)
   строится один раз при загрузке face; codepoint-cascade и advance больше
   не парсят шрифты. `Font::parse` остался только на промахе глиф-атласа и
   для variation axes — лениво через per-frame memo (`LazyParsedFaces`).
   Плюс мемоизация `resolve_face_id` (хэш families+weight+style → face_id,
   сброс в `set_font_provider`) — раньше КАЖДЫЙ DrawText каждого кадра гонял
   `to_lowercase` + `FontProvider::pick_face` (2 Vec-аллокации + матчинг).
   Замер (1000000-final.html, dev-release): CPU к t=5s **3218.8 → 2359–2547 мс
   (−21%)**; холодный кадр faces 201.6 → 172.9 мс. **Вскрытая правда: посылка
   очереди «Font::parse 2–4 мс тёплый» была неверна** — тёплый парс был <1 мс;
   тёплый остаток фазы faces (~1.4 мс) — это skip-frame `hash_display_list` +
   image-pre-pass, а холодные 172 мс — `fs::read`+WOFF-декод lazy-загрузки
   face-ов внутри первого кадра (новые пункты очереди).
9. **Ярус 1, срез 2 — быстрый box-фильтр + параллельный image pre-pass**
   (2026-07-09, 2ff7183e). **Вскрытая правда №2 (faces-sub тайминг): посылка
   «холодные 172 мс фазы faces = fs::read+WOFF face-ов» была неверна** —
   шрифтовые prefetch/resolve ~0.02 мс (прогреты preload_curated_fallbacks);
   реальный тормоз — ПОСЛЕДОВАТЕЛЬНЫЕ CPU-ресайзы картинок в
   `ensure_image_gpu_key` (~219 мс), из них 147 мс — один `resize_area_avg`
   1024²→1000². Сделано: (1) `resize_area_avg` переписан — наивный 2D
   box-фильтр O(N·k²)+f64+roundf → сепарабельный двухпроходный O(N·k),
   предвычисленные веса по осям, векторизуемые zip-циклы, `+0.5` вместо
   `.round()`; изолированно 131.8 → 15.6 мс (8.4×), бенч `area_avg_bench`;
   (2) `prefetch_image_resizes_parallel` — resize/RGBA/ICC кадра в
   scoped-потоках, текстуры на UI-потоке; (3) `prefetch_faces_parallel` —
   та же схема для face-ов (защита для холодных @font-face, здесь no-op);
   (4) диагностика `faces-sub`/`imgjob` под `LUMEN_FRAME_LOG=2`.
   Замер: CPU к t=5s **2390.6 → 2078.1 мс (−13%)**; холодный imgpre
   155.5 → 24.2 мс (кадр 1), 63.4 → 32.5 мс (кадр 2). Тёплый остаток фазы
   faces теперь = только `hash_display_list` (1.2–2.5 мс/кадр).
10. **Ярус 0 — авто-проба бэкенда** (`crates/engine/paint/src/backend_probe.rs`):
   при старте для кандидатов Vulkan → GL → DX12 рисуется пробный кадр (clear
   в характерный цвет) в поверхность окна; два сигнала — texture readback
   (COPY_SRC + map) и **захват реальной презентации** через
   `PrintWindow(PW_CLIENTONLY|PW_RENDERFULLCONTENT)` — именно он ловит BUG-275
   (белое Vulkan-окно при «исправном» рендере). Кандидат принят, если
   презентация совпала с пробным цветом. Стоимость ~0.5 с на старте, окупается
   до t=5s. Выкл: `LUMEN_NO_BACKEND_PROBE=1`; `WGPU_BACKEND=...` главнее пробы.
   Замер (1000000-final.html, dev-release): idle-10s CPU 765.6 → **250 мс (3×)**,
   CPU к t=5s 4469 → 2219 мс, скачок памяти +185 МБ → нет (−28 МБ),
   private к t=15s 993 → 720 МБ. **Попутная находка: BUG-275 в этот вечер НЕ
   воспроизводится** — форс-Vulkan рисует корректно (транзиентный драйверный
   глюк); проба как раз и защищает от его возврата.
11. **Ярус 1, срез 3 — структурный хэш display-list'а** (2026-07-09):
    `hash_command_into` фолдит команду по сырым байтам полей (тег варианта —
    `mem::discriminant`), вместо прогона `{cmd:?}` через `HashFmt`. Стоимость
    была не в хэшировании, а в `Debug` для `f32`: алгоритм кратчайшего
    представления на каждый флоат, тысячи флоатов на кадр. Быстрые ветки —
    `FillRect`, `FillRoundedRect`, `DrawBorder`, `DrawOutline`, `DrawText`,
    `PushClipRect`, `PushOpacity`, `PushTransform`; остальные варианты падают в
    прежний Debug-fallback. Безопасность по построению: горячие ветки разбирают
    все поля без `..` (новое поле = ошибка компиляции), новый вариант попадает в
    fallback и хэшируется корректно с первого дня. Битовый фолд флоатов строже
    текстового — ошибка только в сторону лишней перерисовки, не устаревшего
    кадра. Тесты `structural_hash_is_stable_and_collision_free_on_corpus` и
    `structural_hash_refines_debug_hash` (второй закрепляет инвариант «всё, что
    различал Debug, различает и структурный» — то есть ложных skip нет). Тем же
    фолдом пошли `hash_commands` (кэш поддеревьев) и `hash_one_command`
    (`diff_display_lists`, `TileGrid`). Попутно в `hash_display_list` добавлены
    длины полос: раньше `content=[a,b], overlay=[]` и `content=[a], overlay=[b]`
    хэшировались одинаково.
    Замер: бенч `hash_display_list_bench` (1060 команд × 200) —
    **1.085 → 0.133 мс/кадр (8.1×)**; `faces-sub: hash` по кадрам
    3.49 → 0.71 / 1.43 → 0.63 / 1.30 → 0.40 мс.
    **Вскрытая правда №3: на idle-CPU это НЕ влияет.** `t=5s` 2093.8 → 1859.4 мс
    выглядело как −11%, но три прогона ОДНОГО И ТОГО ЖЕ после-патчевого бинарника
    дали 1859.4 / 1984.4 / 2140.6 мс — разброс 281 мс, и «до»-цифра лежит внутри
    него. Причина: за 15 с простоя рисуется **4 кадра**, ни одного
    `skip (identical frame)` — без ввода шелл вообще не зовёт `render`. Значит
    суммарная экономия за прогон = 6.40 → 1.83 мс, то есть 4.6 мс при CPU ~2000 мс
    (0.2%), и она на порядок меньше шума. Посылка «тёплый остаток faces =
    1.2–2.5 мс/кадр» верна ПРО КАДР, но кадров в простое почти нет: ярус-0 и
    skip-identical-frame уже свели их к четырём. Реальная цена хэша — на кадрах
    **от ввода** (по п.3: 5 с движения мыши = 31 skip / 2 рендера), где каждый
    skip платит за хэш перед решением: 1.3 → 0.4 мс на событие. Патч оставлен:
    строго быстрее, покрыт тестами, нулевой риск — но idle-CPU он не двигает.
    **Методический вывод: одиночный `measure_idle` не отличает сигнал от шума.**
    Порог значимости на этой машине — не меньше 300 мс по `t=5s`; всё, что ниже,
    требует 3+ прогонов на конфигурацию. Раннер `scripts/exp/run_hash_experiment.ps1`
    собирает полный цикл ДО/ПОСЛЕ в `.tmp/exp-hash/` (по файлу на шаг).

12. **Диагностика encode (`LUMEN_FRAME_LOG=3`), 2026-07-09 — вскрытая правда №4.**
    Добавлены: `alloc:` (создано текстур за кадр, время в `create_texture`+view+bg,
    hit/miss пула), гистограмма длительностей элементов плана, топ-12 самых
    дорогих с их `drop(pass)`. Три прогона, все совпадают.
    - **Аллокация ни при чём:** `created 14 tex in 0.04ms`. Пул render-target
      текстур (`pool hit 0 miss 1`) чинить бессмысленно — экономить нечего.
    - **Распределение плоское:** кадр 3 — `top 12 items = 44.8ms of 250.0ms`
      (18%), остальные 246 элементов по 0.2–5 мс. «Единиц дорогих пассов» нет.
    - **Фиксированной цены пасса не существует.** Кадр 2 и кадр 4 — один и тот
      же план (128 элементов, 869 ops, 2 слоя): encode **116.10 мс против
      3.88 мс**, 30×. Значит холодная цена — это разовый прогрев (похоже на
      ленивое создание `VkRenderPass`/framebuffer в wgpu-hal под каждую новую
      комбинацию attachment'а), гаснущий к четвёртому кадру. Заодно: бэкенд
      сейчас **Vulkan** (проба принимает его), а «~2.3 мс за закрытие каждого
      render pass» в BUG-274 мерили на DX12 — формулировку надо переписать.
    - **Следствие: пасс-схлопывание (RenderTaskGraph) НЕ окупится.** На тёплом
      кадре 128 пассов = 3.88 мс encode; схлопывание до 2–5 пассов выиграет
      максимум эти 3.88 мс. Строка очереди «это добивает остаток BUG-274 на
      любом бэкенде» — неверна.
    - **Следствие 2: `t=5s cpu ≈ 2000 мс` — это СТАРТ, а не простой.** Слагаемые:
      проба бэкенда 435 мс, холодный encode 116+250 = 366 мс, `collect`
      77.7+15.1 = 93 мс, adblock 107923 правил, imgpre ~22 мс, парсинг/layout.
      Простой = 4 кадра за 15 с, ни одного `skip (identical frame)`.
      Метрика «idle-CPU за 10 с» больше ничего не измеряет — она измеряет хвост
      старта. Для целей 100–1000× нужны другие метрики: время до первого кадра
      и время тёплого кадра под вводом (скролл/анимация).

13. **Стенд тёплого кадра + сравнение с Chrome, 2026-07-09 — вскрытая правда №5,
    самая важная.** Добавлен `crates/shell/src/bench_frames.rs`
    (`LUMEN_BENCH=hover|scroll:N:W`) и счётчики `FRAMES_RENDERED`/`FRAMES_SKIPPED`,
    которыми отчёт проверяет сам себя (первая версия стенда молча мерила скип-путь
    вместо перерисовки и показывала одинаковые медианы — счётчик это ловит).
    - **hover** (`rendered 0 skipped 600`): медиана 0.033–0.047 мс. Это цена
      `hash_display_list` на событие ввода. Тем самым п.11 наконец получил
      измеримый эффект: ~0.25 мс на кадр, ~15 мс CPU/с при движении мыши.
    - **scroll** (`rendered 600 skipped 0`): медиана **46.6 мс** (три прогона:
      46.54 / 46.71 / 46.65 — разброс 0.2%, не шум). Скролл = 21 fps.
    - **Пофазно:** `total 46.68 | faces 0.37 collect 0.64 prep 0.40 acquire 36.18
      encode 7.42 submit 1.67`. **`acquire` — 36 из 46 мс.** Present-режим
      подтверждён Immediate, значит это не ожидание vsync, а блокировка на
      `get_current_texture()`, пока GPU дорисовывает предыдущий кадр.
      **Кадр упирается в GPU: CPU ~10 мс, GPU ~46 мс.**
      Гипотеза «backdrop-кэш инвалидируется scroll_y» — ОПРОВЕРГНУТА: `bdrop 5x0.5ms`.
    - **Chrome на той же странице:** медиана **16.70 мс**, p95 17.0, max 17.2,
      кадров >20 мс — ноль, на вьюпорте 1357×881 (в 1.6× большей площади).
      Chrome упёрт в vsync, поэтому его истинная цена кадра ≤16.7 мс и точно не
      измеряется; нижняя граница нашего отставания при равной площади —
      **не менее 4.5×**. Страница НЕ патологическая: виноват движок.
    - **Причина структурная.** Chrome при скролле не перерастеризует страницу:
      контент лежит в растровых тайлах, скролл = сдвиг offset при композиции +
      дорисовка въехавшего края. Мы каждый кадр заново гоняем 80 draw-пассов и
      30 полноэкранных фильтр-пассов. Это ровно WebRender picture caching из
      «Карты заимствований» — единственный пункт очереди, ни разу не тронутый.
    - **Следствие: ярус 2 (рендер вон из UI-потока) для этой нагрузки бесполезен.**
      Время кадра = `max(CPU, GPU)`, а не сумма. Сейчас `max(10, 46) = 46`;
      после выноса в поток `max(10, 46) = 46`. Параллелизм окупится только после
      того, как GPU-часть подешевеет настолько, что CPU снова станет узким местом.
    - **Следствие: п.12 («пасс-схлопывание не окупится») сужается.** Тот вывод
      сделан из тёплого `encode` = 3.88 мс, то есть из **CPU-цены записи** пассов.
      GPU-цена их исполнения не измерена до сих пор. Полноэкранные offscreen-слои
      под каждый фильтр — это fillrate, и они вполне могут стоить тех самых 36 мс.
      Отменяется не приём, а обоснование его отмены.
    - **Следствие: цель «100–1000×» требует честной арифметики.** От 46.6 мс до
      бюджета vsync — 2.8×. До Chrome-подобного композиторного скролла (~1 мс) —
      порядка 40×. Тысячекратного ускорения на этой метрике не существует.

14. **Fillrate vs число пассов + обычная страница, 2026-07-09 — вскрытая правда №6.**
    `scripts/exp/run_gpu_bound_probe.ps1`, `LUMEN_BENCH=scroll:300:30`.

    | Прогон | Страница | Окно | Медиана | rendered |
    |---|---|---|---|---|
    | A1 | 1000000-final | 1024×720 | 46.66 мс | 300 |
    | A2 | 1000000-final | 512×360 | 13.11 мс | 300 |
    | B | samples/page | 1024×720 | — | **0 (INVALID: max_scroll 0)** |
    | C | samples/page | 512×360 | **0.274 мс** | 300 |

    - **GPU упирается в площадь, не в число пассов.** A1/A2 = 3.56× при
      четырёхкратном уменьшении площади — почти линейно по пикселям.
    - **Обычная страница: 0.274 мс/кадр** (прогон C). В 170× дешевле стресса,
      шестидесятикратный запас до бюджета vsync. Прогон B стенд сам пометил
      невалидным (`rendered 0 skipped 300`, страница не скроллится) — ровно то,
      ради чего добавлялись счётчики.
    - **Диагноз уточнён, и picture caching бьёт мимо.** Геометрия A1:
      `content_height 711` при вьюпорте 684 — ход скролла 27 px, контента почти
      нет. Дорог не скролл и не размер страницы, дороги **30 фильтр-пассов,
      каждый в offscreen-текстуру размером с весь экран** (`create_layer_texture`
      получает размеры поверхности), хотя размывается элемент 200×150.
      Тайловый кэш экономит на перерисовке при скролле — здесь дорог сам кадр,
      и кэш его не удешевит.
    - **Настоящая цель: offscreen-слои по bbox вместо full-frame** (WebRender
      RenderTaskGraph, offscreen-задача размером с bbox + клип по viewport).
      При bbox 200×150 против 1024×720 площадь падает в 24×, а время идёт за
      площадью (доказано A1/A2). Это единственная оптимизация на этой ветке,
      у которой ДО начала работ есть и механизм, и коэффициент.
    - Грабля стенда: строка `geometry` печаталась при первом непустом display
      list, то есть посреди стриминга парсера (`dl 59` при итоговых 1062).
      Исправлено — печать после прогрева.

15. **Scroll-cache целого кадра (эксперимент на zcode) — замерен и похоронен,
    2026-07-10 — вскрытая правда №7.** В рабочем дереве zcode лежал незакоммиченный
    «GPU scroll-layer compositing»: quantized scroll cache (бакет 20 px, хэш кадра →
    UV-сдвиг кэшированной текстуры), разделение base layer + animation overlay
    (`render_with_anim_overlay`), femtovg skip-identical-frame + dirty-rect scissor.
    A/B-замер на `1000000-final.html` (55 MCP-scroll × 120 px, `.tmp/scroll_bench.py`
    в корневом дереве, медиана `[frame] total`, dev-release):
    - clean wgpu **35.5 мс** → dirty wgpu **1067.8 мс — 30× РЕГРЕССИЯ**. Ключ кэша
      включает квантованный scroll → шаг 120 px пересекает 6 бакетов → miss каждый
      кадр; путь miss = ДВА полных рендера (base-capture без present + композит с
      оверлеем) + двойная сборка display list в шелле. Плюс позиционный баг: anim
      overlay рисуется при scroll (0,0) и без сдвига таб-бара.
    - femtovg skip+dirty-rect: 665.8 → 666.5 мс, **эффект 0**. Scroll входит в хэш
      кадра → skip при прокрутке не срабатывает никогда; scissor не сокращает
      исполнение команд (650 мс/кадр — это CPU-композитинг 30 фильтров femtovg).
      Dirty-rect к тому же небезопасен: scroll+анимация в одном кадре оставили бы
      остальной экран со старым смещением.
    - Попутно: на стресс-странице wgpu (35 мс) в **19× быстрее femtovg** (666 мс) —
      ещё один аргумент курса ветки на отказ от OpenGL.
    - **Уроки:** (а) кэш, в чей ключ входит scroll, при скролле мёртв по построению —
      ключ должен быть scroll-инвариантен (тайлы в координатах scroll root, как в
      WebRender picture caching); (б) base+overlay разделение верно по идее, но
      живёт только при дешёвом miss (один рендер, не два); (в) бенч-харнесс обязан
      читать stderr конкурентно — 4 КБ pipe-буфера дают ложный «дедлок» браузера
      (0% CPU, «Не отвечает», лог обрезан посреди строки на ≈4144 байтах).
    - Патч сохранён: `.tmp/zcode-scroll-cache-experiment.patch` (корневое дерево).
      В эту ветку и в main не переносить.

16. **bbox-scissor фильтр-пассов — 3.2–4× на скролле стресс-страницы, 2026-07-10.**
    Реализация §2 протокола прошлой сессии. Все три пасса каждого фильтра
    (blur H → scratch, blur V → обратно, filter-composite → родитель) получили
    `set_scissor_rect(bbox контента уровня + радиус блюра)`:
    - **bbox считается по фактическим вершинам**, не по `PushFilter.bounds`:
      `LevelBounds` (Empty/Rect/Unbounded) на уровень; `flush_batch!` объединяет
      позиции вершин всех draw-ops уровня (они уже в CSS px, после
      transform/scroll); дочерние Composite/FilterComposite объединяют свой
      bbox в родительский. Mask/backdrop-композиты в v1 не отслеживаются —
      родитель помечается Unbounded (безопасный фолбэк = полноэкранные пассы).
      `bounds` из display list не годится: контент слоя (дети, box-shadow)
      законно выходит за border-box.
    - Инфляция = `min(ceil(3σ),32)+2` текселя — ровно ядро нашего блюр-шейдера;
      сверено с WebRender (`BLUR_SAMPLE_SCALE = 3.0`, `picture.rs`:
      `inflate(ceil(σ)*3)`). За пределами scissor блюр читает прозрачность,
      гарантированную полными `LoadOp::Clear` (clear не подчиняется scissor —
      это цена ~0.04 мс и одновременно гарантия корректности; UV-кламп
      WebRender'а не нужен). Пустой слой (Empty) пропускает все три пасса.
    - Замер (run_warm_frame_bench, медианы 3 прогонов): scroll
      **53.1/51.4/57.6 → 15.2/17.2/17.7 мс (3.2×)**; исторический baseline
      46.6 мс мерился на тихой машине, сегодняшний ДО-замер — при работающих
      Brave/Warp (см. ниже про фон). gpu_bound_probe: A1 46.66 → **11.67 мс
      (4×)**, A2 13.11 → 6.67, прогон C (обычная страница) 0.274 → 0.321 мс —
      в пределах шума фона. hover без изменений.
    - **Пиксели бит-в-бит идентичны**: kill-switch `LUMEN_NO_BBOX_SCISSOR=1`
      позволил PIL-дифф A/B на одном бинарнике — 0 отличающихся пикселей на
      30-css-filter, 31-clip-path, 1000000-final (проверено, что страницы
      реально отрисовались, не два белых окна).
    - **GPU-занятость процесса** (новый `scripts/exp/proc_stats.ps1`, те же
      счётчики, что колонка GPU диспетчера задач): vsync-скролл стресса
      **88.5% → 61.2%** при росте fps ~21 → ~60; immediate-бенч остаётся
      ~75–85% занятости по построению (кадры встык — ускорение уходит в
      throughput: CPU шелла 3.1% → 10.1% = кадров подаётся ~3× больше).
    - Из свежего обзора исходников WebRender/vello (файлы в `.tmp/wr-research/`):
      (а) **downscale-цепочка блюра** — WR клампит σ ≤ 4 на пасс, деля σ и
      таргет пополам bilinear-скейлом (`MAX_BLUR_STD_DEVIATION=4.0`,
      `new_blur`); наше ядро до 32 текселей = до 65 сэмплов/пиксель — кандидат
      следующего среза для больших σ; (б) **аналитический blurred-rounded-rect
      vello** (`draw_blurred_rounded_rect`, erf7-аппроксимация Эвана
      Уоллеса/Рафа Левиена, O(1)/пиксель, ноль offscreen-пассов) — кандидат
      для box-shadow вместо PushFilter{Blur} вообще.

17. **Viewport-cull целых невидимых слоёв — ещё 1.27× на скролле, 2026-07-10.**
    Идея пользователя: «не запускать ненужное». После п.16 фильтры за экраном
    уже пропускали свои блюр/композит-пассы, но их КОНТЕНТ продолжал
    обрабатываться: draw-пасс + полноэкранный clear + вершины (на стрессе ~95%
    контента вне вьюпорта: content_height 12471 при viewport 684). Теперь
    Push(Opacity|BlendMode|Filter) запоминает `render_plan.len()`, а Pop, если
    слой невидим (Empty, alpha ≤ 0, bbox целиком вне surface), делает
    `render_plan.truncate(mark)` — из плана выбрасываются ВСЕ пассы слоя,
    включая вложенные. Безопасно по построению: offscreen-текстуры имеют
    размер окна, контент за его пределами и так не попадает ни в одну текстуру
    (растеризация клипует), а прозрачный src у всех CSS-блэндов оставляет
    backdrop неизменным (co = cs + cb·(1−as)). Для фильтров тест видимости
    учитывает инфляцию на радиус блюра (за-экранный контент, чей блюр
    дотягивается до вьюпорта, НЕ отсекается). Kill-switch тот же
    (`LUMEN_NO_BBOX_SCISSOR=1` выключает всю механику LevelBounds).
    Замер (run_warm_frame_bench, 3 прогона, ДО перемерен в тот же день —
    фон тише вчерашнего): scroll **11.4/10.8/11.1 → 8.6/8.8/8.8 мс**;
    gpu_bound_probe A1 11.67 → 9.42 мс, прогон C — в шуме. PIL-дифф A/B —
    0 пикселей на 30-css-filter, 56-mix-blend-mode, 102-opacity-stacking,
    1000000-final. **Суммарно п.16+п.17: скролл стресса 53 → 8.8 мс (6×).**
    Пофазно после п.16 (`run_scroll_phases.ps1`): total ~9-10, acquire 2.3-4.2
    (было 36), encode 3-4, collect 0.7-1.2 — GPU больше не единственный
    боттлнек, кадр сбалансирован; дальше давить придётся и CPU-часть (encode).

18. **Эталоны референсного браузера сняты (Edge 150 = Chromium), 2026-07-10.**
    Chrome на машине НЕ установлен; референс = **Edge 150** (тот же Blink/cc-
    композитор; Brave есть как кросс-чек). Инструменты:
    `scripts/exp/chromium_baseline.py` (CDP-харнесс, зеркалирует
    `LUMEN_BENCH=scroll:600:30`: scrollBy 1 CSS px на rAF с отскоком на краях,
    30 прогрев + 600 замер, медиана/p95/max, self-validation `max_scroll==0 →
    INVALID`; та же страница, тот же вьюпорт 1024×684, dpr 1; `--novsync` =
    `--disable-gpu-vsync --disable-frame-rate-limit` — аналог
    `LUMEN_PRESENT=immediate`; заодно печатает launch→FCP по Paint Timing API)
    и `scripts/exp/chromium_stats.ps1` (те же счётчики, что proc_stats.ps1, но
    суммой по ВСЕМУ дереву процессов браузера). Lumen-контрчисла сняты В ТОТ ЖЕ
    ДЕНЬ тем же паттерном (фон сегодня тяжелее вчерашнего: стресс-скролл
    10.6–10.8 мс против вчерашних 8.6–8.8 — сравнивать внутри дня).
    В шелл добавлена метрика старта: `[bench] first non-empty frame: Nms since
    process start` (bench_frames::log_first_frame_once, печатается всегда) —
    контрчасть launch→FCP Эджа. Результаты — таблица §4. Главные факты:
    - **Самое чистое сравнимое число — GPU-занятость под одинаковым
      vsync-скроллом 60 fps: Lumen 52.7% против Edge 5.8% — разрыв 9×.**
      У rAF-цифр novsync есть оговорка (конвейер Chromium многопоточный,
      rAF-дельта = main-thread pacing, нижняя оценка цены кадра), у
      GPU-занятости оговорок нет: одна работа, один счётчик.
    - Тёплый кадр стресса uncapped: Lumen 10.6–10.8 мс, Edge 2.0–2.2 мс (~5×).
    - Обычная страница: 0.277 против 0.200 мс — почти паритет.
    - **Старт до первого кадра: ПАРИТЕТ** (Lumen 1279–1400 мс, Edge 1322–1387;
      на samples/page 1212 против 1198). Оценка «старт ~2 с» из п.12 была
      пессимистична; ярус-0 проба (~0.5 с) уже в этих цифрах.
    - CPU под vsync-скроллом: 6.3% против 7.0% (всё дерево) — паритет.
    - RAM: Lumen private 740 МБ против 350 МБ у ВСЕГО дерева Edge — 2×
      (перекликается с BUG-272/274; текстурный зоопарк `src@WxH` из очереди).
    - Грабли харнесса (уже наступали): (а) порты 9222/9333 на этой машине в
      excluded-диапазонах Windows (`netsh int ipv4 show excludedportrange tcp`,
      bind → WSAEACCES) — взят 9250; (б) зомби-инстанс держит IPv4-порт →
      новый Chromium молча биндит только `[::1]`, а CDP-вызов к мёртвому
      таргету висит бесконечно — харнесс матчит URL таргета и опрашивает оба
      loopback; (в) перекрытое окно останавливает BeginFrames → rAF замирает
      навсегда — обязательны `--disable-backgrounding-occluded-windows`
      + `--disable-renderer-backgrounding` и watchdog-таймаут на eval;
      (г) вывод python буферизуется при редиректе — запускать `python -u`,
      иначе после kill лог пуст.

19. **Скролл-композитор страницы, срез 1 — персистентная полоса + blit,
    статика 6–8×, 2026-07-10.** Реализация §2: `render()` разделён на
    оркестратор (skip-хэш, выбор пути) и `render_impl` с режимами
    Normal/Band/Compose. Полоса = вьюпорт + по 0.75 вьюпорта запаса (≤768
    CSS px), рисуется обычным рендером контента в offscreen-текстуру в
    документных координатах; кадр скролла внутри полосы = blit-квад со
    сдвигом + overlay (первым op-ом level 0, после Clear). Ключ полосы
    scroll-инвариантен (хэш контента при scroll 0 + `content_generation` +
    геометрия — урок п.15), промах = ОДИН рендер контента. Guard-ы: только
    оконный рендер, scroll_x == 0, нет `BeginStickyLayer` (единственная
    команда, нелинейная по scroll_y; fixed спец-обработки в рендере не имеет
    и воспроизводится полосой бит-в-бит). Kill-switch
    `LUMEN_NO_SCROLL_COMPOSITOR=1`.
    - **Вскрытая правда №8: стресс-страница — анимационная нагрузка, а не
      скролльная.** Первая версия рисовала полосу на каждый промах ключа и
      дала 10.7 → **21 мс (2× РЕГРЕССИЯ)**: 511 из 629 кадров меняют display
      list (CSS-анимации/GIF), и каждый платил за полосу 1.75× вьюпорта.
      Правило среза: **полоса рисуется только по стабильному контенту** (ключ
      совпал два кадра подряд) — анимационные кадры идут монолитом без
      штрафа. После этого стресс = нейтрально (9.3–12.2 против ДО 10.6–10.8,
      в шуме), hover без изменений.
    - **Статичная страница** (новая `samples/bench-static-scroll.html`,
      сопоставимый вес кадра: 40 секций с blur/градиентами/тенями/текстом,
      ноль анимаций): скролл **11.2–16.2 → 1.74–1.92 мс (6.4–8.4×)** — уровень
      Edge (2.0–2.2 на его rAF-метрике).
    - **Пиксельный A/B** (`scripts/exp/compose_ab_diff.py`: MCP-live скролл
      обоих конфигов в одну позицию + PrintWindow существующего окна через
      `scripts/exp/capture_pid.ps1` + PIL-дифф): 0.57% пикселей, целиком в
      полосе y 672–720 — класс «blur элемента, пересекающего край вьюпорта»:
      монолит растеризует слой, обрезанный краем surface, полоса содержит
      реальный контент за краем — её блюр ПОЛНЕЕ (как Chrome с тайлами за
      вьюпортом). Осознанное отличие, не дефект.
    - Грабли: (а) depth-attachment обязан совпадать по размеру с целью —
      на Band-рендер подменяется полосной depth-текстурой (иначе wgpu
      validation «Encoder is invalid» — так упал первый smoke); (б) прочие
      персистентные текстуры (layer/scratch/backdrop-cache) уже валидируют
      размер в своих ensure-функциях и пересоздаются сами.

20. **Fast-scroll деградация (принцип пользователя), срез 2 — быстрый скролл
    анимированной страницы 4×, 2026-07-10.** Принцип пользователя: «чем
    быстрее пользователь прокручивает, тем меньше ему важно содержимое» —
    то же, что low-res тайлы/checkerboarding Chromium при флинге. Реализация:
    EMA-скорость скролла (по дельте scroll_y между RedrawRequested, коэфф.
    0.6/0.4) с гистерезисом вход ≥48 / выход <12 CSS px/кадр; в режиме
    быстрого скролла замораживаются ИСТОЧНИКИ изменений контента — тик
    CSS-анимаций/transitions (Step 2), обновление GIF-текстур (Step 2.5,
    register_image бампал content_generation каждый тик) и video-GIF
    (Step 2.6). Display list становится scroll-стабильным → композитор п.19
    попадает в полосу. Анимации time-based: на выходе из режима догоняют
    текущее время сами; при заморозке цикл кадров поддерживается
    request_redraw-ом, чтобы выход случился без ввода. JS rAF НЕ трогаем
    (spec-visible поведение). Kill-switch `LUMEN_NO_FAST_SCROLL_DEGRADE=1`.
    Бенч расширен 4-м полем: `LUMEN_BENCH=scroll:N:W:STEP` (px/кадр).
    Замеры (1000000-final, step 60 ≈ флинг 3600 px/s, 3 прогона):
    - immediate: медиана **8.4–11.2 → 2.6–3.0 мс (~4×)**, mean 10.2 → 5.7
      (p95 ~27 мс = перерисовка полосы раз в ~17 кадров);
    - vsync-флинг (proc_stats, 20 с): GPU **58.7% → 31.1%**, CPU 6.2 → 3.8%;
    - медленный скролл (step 1) не тронут: 11.6 мс, анимации в полном
      качестве — деградация включается только на реальной скорости.
    - **Цена: RAM 746 → 943 МБ во время флинга** — offscreen-слои фильтров
      (~30 шт. на стрессе) пересоздаются в размер полосы (1.75× площади).
      Хвост: полосу можно рисовать вьюпортными полосками либо офскрины по
      bbox (п.14 «настоящая цель») — тогда цена уйдёт.

## Замеры бэкендов (idle-CPU за 10 с, graphic_tests/1000000-final.html, dev-release, Intel Iris Plus)

| Бэкенд | idle 10 с | Прогретый кадр | Картинка |
|---|---|---|---|
| femtovg/OpenGL (эталон main) | 375 мс | ед. мс | ок |
| wgpu **DX12** (текущий дефолт) | ~1500–2500 мс | 450–950 мс (wall) | ок |
| wgpu **Vulkan** | 203 мс | **7 мс** | **БЕЛОЕ ОКНО — BUG-275** |
| wgpu **GL** (`WGPU_BACKEND=gl`) | **156 мс — лучший** | 104–122 мс (wall, блокирующий GL) | ок |

Выбор бэкенда: **ярус-0 авто-проба** (Vulkan → GL → DX12, принимается первый,
чей пробный кадр реально виден в DWM-захвате). Статическая цепочка
DX12 → Vulkan → GL осталась резервом (`LUMEN_NO_BACKEND_PROBE=1` / все
кандидаты отклонены). Вопрос «GL дефолтом» снят: проба сама берёт Vulkan,
когда он презентует честно (сейчас — да), и GL, когда Vulkan белеет.

## Чужие решения — карта заимствований (обзор успешных Rust-проектов, 2026-07-09)

Обзор WebRender/Servo, Zed GPUI, egui, Vello, rerun, Bevy, Slint, alacritty,
wezterm, smithay/niri, iced, cosmic-text/swash/glyphon. Сгруппировано по нашим
проблемам; у каждого приёма — конкретные имена типов для поиска в исходниках.

### Проблема «~270 render pass'ов на кадр» (корень BUG-274) → 2–5 пассов

- **WebRender**: display list → **RenderTaskGraph**; топологическая сортировка
  (`RenderTaskGraphBuilder`, `render_on: PassId`) сливает все независимые
  offscreen-задачи в ОДИН пасс — число пассов ≈ глубине вложенности эффектов
  (типично 2–5), не числу эффектов. Offscreen-задачи размером с **bbox**
  (клип по viewport, кламп blur ≤300px), пакуются `GuillotineAllocator`-атласом
  в общие таргеты ~2048×2048 из **пула** (`return_render_target_to_pool`).
  Плюс opaque pass front-to-back с z-буфером + alpha pass back-to-front;
  цель «≤100 draw calls на кадр».
- **GPUI (Zed)**: вообще без offscreen-слоёв на эффект — фиксированные
  примитивы (shadows → quads → paths → glyphs → sprites → images), каждый тип =
  один instanced draw call. Скругления/рамки — SDF в шейдере квада; box-shadow —
  замкнутая формула Эвана Уоллеса (erf-аппроксимация гаусса) прямо в шейдере,
  никаких blur-пассов; один общий MSAA-таргет на все пути кадра. Показательно:
  Windows-бэкенд Zed — **DX11, не DX12** («сложность не окупается») — наш
  wgpu-GL-фолбэк идейно то же самое.
- **WebRender, opacity**: простая `opacity` — «opacity collapse» в модуляцию
  alpha вершинных данных, слоя нет вовсе; маски — только сегменты с углами
  (`BrushSegment`) в общий alpha-атлас с кэшем между кадрами (`RenderTaskCache`);
  mix-blend читает backdrop из тайла кэша, а не копирует кадр.
- **re_renderer (rerun)**: фиксированные draw phases, фаза ≈ один пасс,
  drawable ≈ один draw call — число пассов константно и не зависит от сцены.

### Проблема «скролл/GIF перерисовывают весь кадр» (ярус 1)

- **WebRender picture caching**: контент растеризуется в крупные тайлы
  (~2048×512) в координатах **scroll root** (`TileCacheInstance`) — скролл =
  сдвиг offset'а при композиции, рисуются только въехавшие тайлы. Инвалидация:
  per-tile списки зависимостей (примитивы/клипы/image keys/opacity bindings) +
  интернинг примитивов → сравнение по id кадр-к-кадру → dirty rect + scissor
  внутри тайла. GIF в углу инвалидирует один тайл. Итог Mozilla: скролл
  16.4 Вт → 9.4 Вт.
- **smithay `OutputDamageTracker`** — каноничный damage-алгоритм: элементы с
  commit-счётчиками; damage = изменившиеся + появившиеся/сдвинувшиеся (старый
  И новый rect); **накопление damage по возрасту буфера** (при double/triple
  buffering перерисовывается union damage последних age-1 кадров); при
  фрагментации регион упрощается до extents.
- **alacritty**: построчный damage (`LineDamageBounds`), damage двух кадров,
  старый курсор/выделение добавляются в damage. **iced-урок**: их GPU-путь до
  сих пор без damage, но софтверный `iced_tiny_skia` диффит примитивы — диффинг
  display-list'а работает без реактивной системы свойств (наш
  `diff_display_lists` уже есть — его надо ПОДКЛЮЧИТЬ к рендеру).
- **Slint**: `PropertyTracker` → `DirtyRegion` (до 3 прямоугольников, старая +
  новая геометрия), история damage на 3 буфера.

### Проблема «Font::parse всех face каждый кадр» (ярус 1)

- **cosmic-text `FontSystem`** — эталон: fontdb парсит метаданные ОДИН раз при
  загрузке, данные в `Arc`; кэш `Font`-объектов по font_id (внутри распарсенный
  Face), кэши font-matching и codepoint→face (fallback). Поверх —
  `ShapeRunCache` (кэш шейпинга целых run'ов, Bevy получил 82→90–100 fps) и
  `SwashCache` (растр глифа по ключу face_id+glyph+size+subpixel).
- **GPUI/egui/glyphon**: глиф-атлас персистентный, alpha-only, тонирование
  цветом в шейдере; glyphon разделяет `prepare()` (CPU) и `render()` (один
  instanced draw в чужом пассе — middleware-паттерн wgpu).

### Проблема «idle ≠ 0» (BUG-271/274)

- **alacritty / niri / Slint / egui reactive mode** — общий принцип: нет
  таймера кадров вообще; redraw только от источников изменений (ввод, сеть,
  анимации, GIF-таймер); статичная страница = ноль wakeup'ов. Continuous-режим
  существует только как debug-флаг. Наш skip-identical-frame — полумера:
  кадр всё ещё СТРОИТСЯ и хэшируется; цель — не строить.

### Рендер вне UI-потока (ярус 2)

- **WebRender**: 3 потока — scene builder (тяжёлый CPU: picture/spatial/clip
  tree, интернинг) / render backend (culling, task graph, батчи) / renderer
  (единственный трогает GPU). Скролл и анимируемые свойства — property
  bindings в render backend, минуя пересборку сцены.
- **Bevy pipelined rendering**: фазы Extract → Prepare → Queue → Render; Extract —
  единственная точка синхронизации (копирование в render-мир), render-поток
  рисует кадр N, пока main считает N+1. **GPUI**: пул instance-буферов с triple
  buffering и асинхронным возвратом — нет CPU↔GPU stall'ов.

### Ярус 3 (Vello) — статус

Vello classic: клипы/слои/блэнды — команды PTCL внутри compute-конвейера
(вложенные эффекты не порождают пассов), но на конец 2025 — **alpha**:
worst-case GPU-память, conflation-артефакты, фильтры не доделаны. Зреет ветка
sparse strips: `vello_cpu` (быстрейший CPU-рендерер в Rust) и `vello_hybrid`
(CPU-геометрия + лёгкий GPU-финал, до WebGL2). Servo взял Vello для canvas.
Вердикт: **следить, не брать**; наш порядок ярусов подтверждается.

### Порядок внедрения (как шла Mozilla)

1. Граф задач + bbox-слои в атласе из пула → схлопывает 270 пассов;
2. тайловый кэш на scroll root → скролл почти бесплатен;
3. per-tile/damage инвалидация → GIF стоит свой прямоугольник;
4. вынос рендера в поток (extract-паттерн Bevy) — последним, когда кадр уже дёшев.

## Очередь работ (по приоритету, цель — быть быстрее Chrome)

1. ~~Ярус 0 — авто-проба бэкенда~~ **СДЕЛАНО 2026-07-08** (см. «Что уже
   сделано» п.10): idle-CPU 3×, память −273 МБ, BUG-275 обезврежен пробой.
2. **Ярус 1 — не рисовать лишнее** (главный рычаг; приёмы — см. «Карта
   заимствований» выше):
   - ~~кэш `parsed_faces`~~ **СДЕЛАНО 2026-07-09** (см. «Что уже сделано»
     п.8: FaceMetrics + LazyParsedFaces + resolve-memo, CPU к t=5s −21%).
   - ~~вынос загрузки face-ов/ресайзов картинок с render-пути~~ **СДЕЛАНО
     2026-07-09** (п.9: сепарабельный box-фильтр 8.4× + параллельные
     пре-пассы; «172 мс шрифтов» оказались ресайзами картинок).
     Открытые хвосты:
     - ~~тёплый остаток фазы faces = `hash_display_list`~~ **СДЕЛАНО 2026-07-09**
       (п.11: структурный фолд, 8.1× на хэше). Вариант «поколение DL на стороне
       шелла» ОТКЛОНЁН: кадровый список собирается заново каждый кадр из шести
       источников (`self.display_list`, `overlay_buf`, `anim_dl`, `page_buf`,
       split-view `build_combined_dl`, inspector-оверлей) плюс `page_x_offset` и
       `canvas_bg` — счётчик поколений пришлось бы бампать в каждом, и один
       пропущенный бамп даёт ложный skip (устаревшие пиксели), проявляющийся
       редко и невоспроизводимо. На idle-CPU выигрыш не виден (см. п.11);
     - imgpre можно добить GPU-путём: рисовать оригинальную текстуру с
       mip-цепочкой (blit-каскад при register_image) вместо CPU-ресайза
       на каждый placed-размер — убирает и текстурный зоопарк `src@WxH`
       (память), и остаток 24–32 мс холодного кадра. **Приоритет выше**, чем у
       хэша: по логу это 22–32 мс на холодных кадрах против 4.6 мс за прогон;
   - dirty-rect до конца: `TileGrid`/`diff_display_lists` пишутся, но не
     читаются рендером — подключить scissor-ограниченную перерисовку
     изменившегося региона (GIF перерисовывает свои 200×150, не весь кадр);
     union damage по возрасту буфера — как smithay `OutputDamageTracker`;
   - ~~**пасс-схлопывание** (RenderTaskGraph, opacity collapse, SDF-тени)~~
     **ОТЛОЖЕНО по замеру 2026-07-09** (п.12): тёплый кадр = 128 пассов за
     3.88 мс encode, схлопывание выиграет ≤3.88 мс. Холодные 366 мс — разовый
     прогрев framebuffer-кэша, пассы тут ни при чём. Вернуться, если появится
     сцена, где тёплый encode дорог (много слоёв/фильтров в анимации);
   - ~~**offscreen-слои эффектов по bbox**~~ **СДЕЛАНО 2026-07-10** (п.16:
     scissor всех трёх фильтр-пассов по bbox контента уровня; scroll стресса
     3.2–4×, пиксели идентичны). Открытые хвосты той же жилы:
     - scissor для Composite-пассов opacity/blend (инфраструктура LevelBounds
       уже всё считает — осталось прокинуть в CompositePlan и замерить);
     - downscale-цепочка блюра при σ > 4 (WebRender MAX_BLUR_STD_DEVIATION);
     - аналитический blurred-rounded-rect (vello/Эван Уоллес) для box-shadow —
       убирает PushFilter{Blur} у теней полностью;
     - mask/backdrop-композиты отслеживать в LevelBounds (сейчас Unbounded);
   - ~~**скролл-композитор страницы — ГЛАВНЫЙ ПРИОРИТЕТ**~~ **СРЕЗЫ 1–2
     СДЕЛАНЫ 2026-07-10** (п.19: полоса+blit, статика 6–8×; п.20: fast-scroll
     заморозка анимаций, флинг стресса 4×, GPU 58.7→31.1%). Открытые хвосты:
     - анимационные страницы при МЕДЛЕННОМ скролле всё ещё монолит (86%
       кадров стресса меняют DL — п.19 правда №8). Путь Chrome: анимируемые
       элементы в отдельные слои, статика в полосе/тайлах — следующий крупный
       срез («static/animated split», идея base+overlay из п.15 была верной);
     - RAM во флинге +197 МБ (offscreen-слои в размер полосы) — лечится
       офскринами по bbox (п.14) или полосой из вьюпортных полосок;
     - полоса одна и вертикальная: scroll_x != 0 и `BeginStickyLayer`
       выключают композитор; тайлы в координатах scroll root (WebRender
       picture caching) — финальная форма;
     - пороги fast-scroll (вход 48 / выход 12 px/кадр, EMA 0.6) подобраны на
       глаз — при жалобах на «замирающие анимации» перекалибровать.
3. ~~**Ярус 2 — параллелизм**: рендер вон из UI-потока~~ **ОТЛОЖЕНО по замеру
   2026-07-09** (п.13): время кадра = `max(CPU, GPU)`; сейчас CPU 10 мс, GPU
   46 мс, и вынос рендера в поток не меняет ничего. Вернуться после того, как
   GPU-часть подешевеет. Параллельный style/layout (rayon) — отдельная история,
   меряется на фазе `collect`/layout, а не на кадре.
4. **Ярус 3 — Vello** (compute-растеризатор поверх wgpu; заглушка
   `vello_backend.rs` есть) — только после ярусов 0–2 и по замерам.
   Статус 2025: alpha, «следить, не брать»; смотреть также vello_hybrid.
5. Профиль боевой сборки: release + thin-LTO + PGO + mimalloc (10–20% всюду).
6. BUG-275: обновить драйвер Intel / проверить на другой машине.

## Диагностические env-переменные (добавлены в этой ветке)

| Переменная | Действие |
|---|---|
| `LUMEN_FRAME_LOG=1` | адаптер + параметры поверхности + шелл-лог `[frame]` |
| `LUMEN_FRAME_LOG=2` | + фазы кадра (faces/collect/prep/acquire/encode/submit), разбивка encode по RenderPlanItem, разбивка Draw-пасса, счётчик текстур |
| `LUMEN_FRAME_LOG=3` | + пер-элементный профиль encode: аллокации текстур за кадр (`alloc:` — сколько создано, за сколько мс, hit/miss пула), гистограмма длительностей пассов, топ-12 самых дорогих элементов с их `drop(pass)`. Отвечает на вопрос «дорог каждый пасс или единицы пассов» |
| `WGPU_BACKEND=vulkan\|dx12\|gl` | явный выбор HAL-бэкенда wgpu |
| `LUMEN_NO_FRAME_SKIP=1` | выключить skip-identical-frame |
| `LUMEN_NO_BBOX_SCISSOR=1` | выключить bbox-scissor фильтр-пассов (п.16) — A/B картинки и скорости на одном бинарнике |
| `LUMEN_NO_SCROLL_COMPOSITOR=1` | выключить скролл-композитор страницы (п.19) |
| `LUMEN_NO_FAST_SCROLL_DEGRADE=1` | выключить fast-scroll заморозку анимаций/GIF (п.20) |
| `LUMEN_NO_BACKEND_PROBE=1` | выключить ярус-0 авто-пробу (статическая цепочка DX12 → Vulkan → GL) |
| `LUMEN_PRESENT=mailbox\|immediate` | present mode (дефолт Fifo) |
| `LUMEN_BENCH=hover:N[:W]` | стенд тёплого кадра: N перерисовок без изменения состояния (все уходят в skip-identical) + W кадров прогрева; печатает медиану/p95/max и выходит |
| `LUMEN_BENCH=scroll:N[:W[:S]]` | то же, но скролл страницы на S CSS px за кадр (дефолт 1; 60 ≈ флинг — нагрузка fast-scroll деградации п.20) — настоящая перерисовка. **Обязательно с `LUMEN_PRESENT=immediate`**, иначе acquire блокируется на vsync и все сэмплы = 16.7 мс |
| `LUMEN_WINDOW=WxH` | размер окна (эксперименты с площадью кадра) |

## Методика замеров

- Сборка: `cargo build --profile dev-release -p lumen-shell` (НЕ `--release` —
  запрещено правилами репо для тестов, dev-release 2–3× быстрее собирается).
- Idle-CPU: `powershell -ExecutionPolicy Bypass -File scripts/exp/measure_idle.ps1
  -FrameLog 2` — CPU/WS/private на t=5s и t=15s, stderr в `.tmp/idle_stderr.log`.
- Скриншот окна: `scripts/exp/printwindow.ps1 -Backend <hal> -Out <png>`
  (PrintWindow PW_RENDERFULLCONTENT). gdigrab полного стола — второй способ.
- Бенч скролл-патча: `cargo test -p lumen-paint --release patch_scroll_layer_bench
  -- --ignored --nocapture`.
- **Тёплый кадр (основная метрика с 2026-07-09):**
  `powershell -ExecutionPolicy Bypass -File scripts/exp/run_warm_frame_bench.ps1`
  — hover/scroll × 3 прогона, всё в `.tmp/exp-warmframe/`. Отчёт — медиана/p95/max,
  не среднее: у распределения времени кадра есть холодный хвост (§12), и среднее
  его прячет. `idle-CPU` больше не годится как метрика per-frame оптимизаций —
  она меряет хвост старта (§12, следствие 2).

## Грабли (уже наступали — не повторять)

- **«Белая страница + таб-бар» = страница НЕ ЗАГРУЗИЛАСЬ** (os error 2 при
  относительном пути из PowerShell), а не баг рендера. Проверяй stderr на
  «Ошибка загрузки» и заголовок окна.
- **Vulkan-окно белое целиком** (и таб-бар тоже) — это BUG-275, рендер при
  этом «исправен» по всем логам. Оба способа захвата показывают белое.
- graphic_tests/run.py калиброван под femtovg-пиксели — на этой ветке его
  пороги не показательны; визуальную проверку делать скриншотом + глазами
  (Read PNG) против DX12-эталона.
- `LUMEN_FRAME_LOG` читается один раз за процесс (OnceLock) — менять уровень
  между кадрами нельзя.
- Правки делать по пути worktree (`.claude/worktrees/exp-wgpu-only/...`),
  не по main-root — иначе уедут в чужое дерево.
- **`git stash push` без парного `pop` = замер baseline вместо патча.** Симптом:
  тесты проходят, но новых тестов НЕТ в списке (`running 9 tests` вместо 11), а
  `--ignored` бенч даёт `0 tests`. Проверять `git stash list` до паники.
  `run_hash_experiment.ps1` теперь отказывается стэшить, если под `crates/` и
  `subsystems/` нет изменений.
- **В PowerShell `git stash pop stash@{0}` не работает** — `@{0}` парсится как
  литерал хэш-таблицы, git получает мусор (`unknown switch 'e'`). Писать
  `git stash pop` (берёт верхний) или `git stash pop 'stash@{0}'`.
- **`$ErrorActionPreference = "Stop"` + cargo = скрипт падает на первой же
  строке прогресса.** Cargo пишет `Checking …` в stderr, `*>&1` превращает это в
  `ErrorRecord`. Успех определять по `$LASTEXITCODE`, префенс держать `Continue`.
- **`rust-lld: undefined symbol: anon.<hash>.llvm.<N>`** при линковке
  интеграционных тестов (`headless_tests`, `snapshot_tests`,
  `texture_pool_integration`) — протухший инкрементальный кэш, не ошибка в коде
  (клиппи проходит, ссылки идут из `std::sync::mpmc`, `core::slice::sort`).
  Лечится `cargo clean -p lumen-paint`, в упор — `CARGO_INCREMENTAL=0`. Юнит-тесты
  крейта гонять с `--lib`, чтобы вообще не линковать эти бинарники.
- **Один `measure_idle` ничего не доказывает.** Разброс `t=5s` между прогонами
  одного и того же бинарника — до 281 мс (замерено: 1859.4 / 1984.4 / 2140.6).
  Любой эффект меньше ~300 мс требует 3+ прогонов на конфигурацию, иначе шум
  выдаст себя за −11%.

## Протокол следующей сессии

### 0. Ориентация

1. `git branch` → убедиться, что `p1-exp-wgpu-only` существует; работать в
   worktree `.claude/worktrees/exp-wgpu-only`. `git stash list` — проверить, что
   там нет чужого патча (уже наступали, см. «Грабли»).
2. Прочитать этот файл целиком, особенно пункты 11–14 «Что уже сделано».
   `bugs/BUG-274-OPEN.md` читать с поправкой: его формулировка про «2.3 мс за
   закрытие каждого render pass» получена на DX12 и не воспроизводится на
   Vulkan (п.12). Переписать её — отдельная задача.
3. **Главное правило этой ветки, выведенное кровью: шесть посылок подряд
   оказались ложными, и все шесть — потому что оптимизацию начинали до замера.**
   Ни одной строки оптимизации без замера ДО тем скриптом, которым будет
   сделан замер ПОСЛЕ.

### 1. Метрики (какой скрипт что меряет)

| Скрипт | Меряет | Когда применим |
|---|---|---|
| `scripts/exp/run_warm_frame_bench.ps1` | **основная метрика**: тёплый кадр под вводом, hover (скип-путь) и scroll (перерисовка), медиана/p95 | всегда |
| `scripts/exp/run_gpu_bound_probe.ps1` | площадь против числа пассов; стресс против обычной страницы | при изменениях, влияющих на GPU |
| `scripts/exp/run_scroll_phases.ps1` | пофазная разбивка скроллящегося кадра (`LUMEN_FRAME_LOG=2`) | когда непонятно, куда ушло время |
| `scripts/exp/run_encode_probe.ps1` | пер-элементный профиль encode + аллокации (`LUMEN_FRAME_LOG=3`) | при работе с пассами/текстурами |
| `scripts/exp/measure_idle.ps1` | **почти бесполезен**: idle-CPU = хвост старта (п.13, следствие 2) | только для стартовых оптимизаций |
| `scripts/exp/proc_stats.ps1` | CPU% / GPU% / RAM процесса lumen.exe (счётчики диспетчера задач), семплирование во время запуска; комбинировать с `LUMEN_BENCH` | всегда сопровождать замеры — директива пользователя 2026-07-10: отслеживать эти параметры при каждом запуске |
| `scripts/exp/chromium_baseline.py` | эталон референсного браузера (Edge/Brave) по CDP: тот же скролл-паттерн, что LUMEN_BENCH=scroll, + launch→FCP; `--novsync` = аналог immediate; `--hold-seconds` = длинный vsync-скролл под семплирование | при сверке счёта §4 (в один день с Lumen-замером) |
| `scripts/exp/chromium_stats.ps1` | CPU% / GPU% / RAM суммой по дереву процессов браузера (`-RootPid` из вывода chromium_baseline) | вместе с `--hold-seconds` |

Стенд проверяет сам себя: строка отчёта содержит `rendered N skipped M`, и при
`scroll ... rendered 0` печатает `INVALID`. Если её нет — стенду верить нельзя.
`measure_idle` разброса до 281 мс: любой эффект меньше ~300 мс требует 3+ прогонов.

### 2. Задача: static/animated split (слои для анимируемых элементов)

Сессия 2026-07-10 (вечер) закрыла ТРИ пункта: эталоны Chromium (п.18),
скролл-композитор срез 1 — полоса+blit (п.19), fast-scroll деградация по
принципу пользователя (п.20). Статика 6.4–8.4× (1.74–1.92 мс = уровень Edge),
флинг анимированного стресса 4× (2.6–3.0 мс, GPU 58.7→31.1%), пиксельный A/B
0.57% (осознанный класс «блюр у края вьюпорта»). Старт и CPU — паритет с Edge.

**Рекомендация следующей сессии — static/animated split, и вот почему.**
Правда №8 (п.19): 86% кадров стресс-страницы меняют display list анимациями,
поэтому при МЕДЛЕННОМ скролле анимированных страниц композитор не работает —
кадр идёт монолитом (~11 мс). Chrome решает это слоями: анимируемые элементы
живут в отдельных compositor-слоях, статика — в тайлах, и анимация не трогает
растр страницы. Наш путь:

1. **Разделить DL на статичную часть и анимируемые элементы.** Шелл уже
   строит `anim_dl` отдельным проходом (`build_display_list_ordered_with_anim`,
   main.rs ~11440) — точка врезки там: статичная часть в полосу композитора,
   анимируемые элементы рисовать поверх каждый кадр (маленькие bbox). Ключ
   полосы должен считаться ТОЛЬКО по статичной части. Внимание на painter's
   order: анимируемый элемент между статичными по z — согласовать со
   StackingTree (это главная сложность, у Chrome она называется layerization).
   Идея base+anim overlay из п.15 была верной — хоронил её только дорогой miss.
2. **RAM-хвост полосы** (п.20): во флинге +197 МБ — offscreen-слои фильтров
   создаются в размер полосы (1.75× площади). Лечится офскринами по bbox
   (п.14 «настоящая цель») или полосой из вьюпортных полосок.
3. **Хвосты фильтр-жилы** — только при подтверждении профилем
   (`run_encode_probe.ps1`): downscale блюра σ>4, vello-аналитика box-shadow,
   scissor для Composite-пассов opacity/blend.
4. Пороги fast-scroll (48/12 px/кадр, EMA 0.6/0.4) подобраны на глаз — если
   пользователь заметит «замирающие анимации» при обычной прокрутке,
   перекалибровать.

**Перед любой работой:** замер ДО тем же скриптом, что и ПОСЛЕ
(`run_warm_frame_bench.ps1`, ДО и ПОСЛЕ в один день — фоновая нагрузка
двигает медиану на десятки процентов: 15→11 мс на одном бинарнике), плюс
`proc_stats.ps1` для CPU/GPU/RAM процесса — пользователь требует отслеживать
эти параметры при каждом запуске (2026-07-10).

### 3. Что НЕ делать (закрыто замерами)

- **Пул render-target текстур** — аллокация 0.04 мс/кадр (п.12). Нечего экономить.
- **Ярус 2, рендер в отдельный поток** — время кадра = `max(CPU, GPU)` = 46 мс
  при CPU 10 мс (п.13). Ноль выигрыша, пока GPU не подешевеет.
- **Picture caching / тайловый кэш скролла как первый шаг** — экономит на
  перерисовке при скролле, а дорог сам кадр (п.14). bbox-слои СДЕЛАНЫ (п.16),
  блокер снят — кэш теперь легален как задача §2, но только с
  scroll-инвариантным ключом (урок п.15).
- **Смена оконной библиотеки или графического API** — CPU простаивает 36 мс из
  46 в `acquire`. Менять нечего (п.13).
- **Оптимизации под метрику idle-CPU** — она меряет хвост старта (п.13).
- **Quantized scroll-cache целого кадра + base/anim overlay «в лоб»** — замерено
  2026-07-10 (п.15): 30× регрессия на wgpu, ноль на femtovg. Композиторный скролл
  делать только тайлами в координатах scroll root (scroll-инвариантный ключ кэша)
  и только ПОСЛЕ bbox-слоёв.
- **Рендер полосы композитора на КАЖДЫЙ промах ключа** — замерено 2026-07-10
  (п.19, правда №8): на анимированной странице ключ протухает в 86% кадров, и
  полоса 1.75× вьюпорта на каждый промах дала 10.7 → 21 мс (2× регрессия).
  Полоса — только по стабильному контенту (ключ совпал два кадра подряд);
  нестабильный контент дешевле рисовать монолитом.
- **Замер бенча с `LUMEN_FRAME_LOG=2`** — пофазный eprintln сам стоит
  миллисекунды и двигает медиану (16.7 против 11.6 на одном бинарнике);
  цифры для таблиц снимать только с `LUMEN_FRAME_LOG=0`.

### 4. Цель ветки (решение пользователя 2026-07-10)

«100 и 1000 раз» из ранних формулировок — образное выражение. Настоящая цель:
**работать не медленнее Chrome, а лучше и быстрее** — на одних и тех же
страницах, по одним и тем же метрикам. Chrome на машине не установлен;
референс = **Edge 150 (Chromium)**. Счёт снят 2026-07-10, оба браузера в один
день, один фон, вьюпорт 1024×684, скролл 1 CSS px/кадр (методика и
инструменты — п.18):

| Метрика (1000000-final) | Lumen | Edge (Chromium) | Счёт |
|---|---|---|---|
| Тёплый кадр скролла, uncapped | **10.6–10.8 мс** (на тихом фоне вчера 8.8) | 2.0–2.2 мс (rAF main-thread — нижняя оценка, конвейер многопоточный) | **отстаём ~5×** → тайлы (§2) |
| GPU-занятость, vsync-скролл 60 fps | **52.7% avg** | 5.8% avg / 7.5% max | **отстаём 9×** — самое чистое число, без оговорок про rAF |
| CPU, vsync-скролл | 6.3% | 7.0% (всё дерево процессов) | паритет |
| RAM (private) | 740 МБ | 350 МБ (всё дерево) | отстаём 2× (BUG-272/274, `src@WxH`-зоопарк) |
| Первый кадр: launch→FCP, стресс | 1279–1400 мс | 1322–1387 мс | **паритет** |
| Первый кадр: samples/page | 1212 мс | 1198 мс | паритет |
| Обычная страница, кадр (512×324) | 0.277 мс | 0.200 мс | почти паритет |

Вывод: старт, CPU и обычные страницы уже на уровне Chromium. Весь разрыв —
в цене кадра стресс-скролла (GPU 9×, кадр 5×) и памяти. Ровно это и закрывают
§2 (тайловый скролл-композитор) и mip-цепочка картинок из очереди работ.
Перемер после каждого среза — теми же `chromium_baseline.py` (Lumen-сторона:
`run_warm_frame_bench.ps1` + `proc_stats.ps1`) в один день с эталоном.

### 5. Дисциплина

- В `main` НЕ вливать. Удачные находки — списком в этот файл (раздел «Кандидаты
  на перенос в main» — завести при первом кандидате).
- Каждая вскрытая правда — новым пунктом в «Что уже сделано», даже (особенно)
  если она хоронит собственную работу. Их уже шесть; они и есть главный
  результат ветки.
