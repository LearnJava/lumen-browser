# BUG-1006 — `--check`/`--update-expected` «не доводился до конца»: краш `[bidi] frame error` и зависание после SIGTERM (причина — внешний `timeout` короче прогона, не движок)

**Статус:** FIXED 2026-09-20 (P2)
**Заведён:** 2026-09-05 (P2, WPT-RUN-7 срез 6 — попытка перегенерации `websockets`)
**Область:** тулинг (`tests/wpt/run_smoke.py`, `tests/wpt/shutdown_guard.py`) + диагностика
`crates/bidi-server/src/transport.rs`; движок ни при чём
**Владелец:** P2 (тулинг)

## Корневая причина и фикс (2026-09-20)

**«Краша» не было — все репродукции оборваны внешним `timeout`, выбранным короче прогона.**
Сверка внутреннего времени `[bidi] frame error` с аргументом `timeout` в каждой репродукции:

| Репродукция | `timeout` | Момент «краша» / конца |
|---|---|---|
| `websockets`, прогон 1 | `timeout 500` (8:20) | 8:17 + запуск ≈ 8:20 |
| `long-animation-frame` (срез 8) | `timeout 280` (4:40) | 4:37 |
| `html/rendering` (срез 23) | `timeout --kill-after=20 480` (8:00) | 7:57, через 0.01 с после `Main thread got signal` |
| `websockets`, прогон 2 | `timeout -k 30 2000` (33:20) | «~34 минуты», 782/786 |

`[bidi] frame error: io: failed to fill whole buffer` печатает **Lumen**, когда клиент BiDi
(python-раннер) исчез — `read_exact` на оборванном TCP даёт `UnexpectedEof`. Раннер же умирал
потому, что групповой SIGTERM от `timeout` убивал его вместе с python. Это следствие, а не
причина; сообщение выглядело как краш движка и три сессии искали не там. На Windows то же
приходит как `ConnectionReset` («Удаленный хост принудительно разорвал…»).

**Без внешнего `timeout` те же прогоны доходят до вердикта** (Windows, `dev-release`,
`--processes 4`, 2026-09-20): `long-animation-frame` — 41/41 `TEST_END` за 5:44, exit 0
(дольше, чем обрывавший её `timeout 280`); `websockets` `--update-expected` — 786/786 за 24:33,
191 `.ini`, ни одного `CRITICAL`/`frame error`; `websockets` `--check` — 786/786 за 25:09,
вердикт `check: 3 regression(s)…` (три плавающих `TIMEOUT`: `?wss`-варианты `bufferedAmount-*`
и `readyState/005.html?default` — отдельный класс, как [BUG-999](BUG-999-OPEN.md)/
[BUG-1003](BUG-1003-OPEN.md), к этому багу не относится). «Слишком длинный прогон» — не блокер;
блокером была сама схема запуска.

**Вторая половина — зависание после SIGTERM** (`futex_wait` в главном потоке,
`anon_pipe_read` в двух потоках; `timeout` без `-k` не добивал процесс ~27 мин). Механизм:
апстримный `termination_handler` поднимает `KeyboardInterrupt`, главный поток ждёт менеджеры
10 с и завершается, но `TestRunnerManager` и читатели `mozprocess` — **не-daemon** потоки, и
`threading._shutdown` ждёт их. `mozprocess` запускает `lumen` в **отдельной группе**
(`os.setpgid(0, 0)`), поэтому групповой SIGTERM оставляет браузер живым сиротой с открытыми
пайпами, `ProcessReader` вечно блокирован в `read()`. На Linux это не воспроизводилось в
этой сессии (Windows-прогон с SIGBREAK через 40 с завершился штатно за 11.6 с без сирот), так
что фикс — страховка по механизму, проверенная имитацией зависшего не-daemon потока плюс
сквозным прогоном на нормальном пути.

**Что сделано:**
- [`tests/wpt/shutdown_guard.py`](../tests/wpt/shutdown_guard.py) — daemon-watchdog: через 30 с
  после сигнала/`KeyboardInterrupt` убивает всех потомков (осиротевший `lumen` тоже) и
  `os._exit(143)`. Подключён в `run_smoke.run()` (общая точка `run_report.py`/`run_suite.py`/
  `run_corpus.py`); на нормальном пути не взводится. Апстримный `wptrunner` не тронут —
  оборачивается `wptrunner.handle_interrupt_signals`.
- [`crates/bidi-server/src/transport.rs`](../crates/bidi-server/src/transport.rs) —
  `peer_hung_up`: обрыв клиента без Close-кадра печатается как
  `[bidi] client disconnected without a Close frame: …`, а не `frame error`. Иначе следующая
  сессия снова прочитает убитый раннер как краш браузера.
- Пины: [`tests/wpt/verify_bug1006_shutdown_guard.py`](../tests/wpt/verify_bug1006_shutdown_guard.py)
  (4 случая), `transport::tests` (2 теста).

**Правило (→ `docs/probe-method.md`):** «краш» на отметке, равной аргументу внешнего `timeout`,
— это `timeout`, а не краш. Перед разбором сверять время события с таймаутом запуска.

Остальное ниже — исторический журнал наблюдений; гипотезы «краш движка», «размер/длительность»
и «порядок причины/следствия сигнал↔краш» им опровергнуты/разрешены.

## Симптом

`websockets` — крупнейшая категория выборки, требующая полной перегенерации (786 id,
до фикса выборки среза 4 было 123 неисполнимых). `--update-expected` на ней прошёл
штатно один раз (786/786 `TEST_END`, 185 `.ini` записано, ~32 минуты). Два последующих
независимых прогона `--check` на том же бинаре и baseline **ни разу не дошли до печати
`check: N regression(s)`**:

1. **Прогон 1** (пайп `| tail -15`, `timeout 500` без `-k`): краш реального движка —
   `[bidi] frame error: io: failed to fill whole buffer` на тесте
   `#150`-ish (`Create-protocols-repeated.any.js?wpt_flags=h2`, внутреннее время 8:17),
   `wptrunner` сам зафиксировал `CRITICAL Last test did not complete`, `Tests left in
   the queue: ... and 634 others` — то есть тест-раннер корректно обнаружил смерть
   браузера, но затем **завис** вместо завершения (главный поток блокирован на
   `futex_wait`, два потока — на `anon_pipe_read` от уже-зомби дочерних процессов,
   см. `/proc/<pid>/task/*/wchan`). `timeout 500` без `-k` не смог его добить: SIGTERM
   не разбудил заблокированные потоки, процесс провисел ещё ~27 минут до ручного
   `kill -9`.
2. **Прогон 2** (redirect в файл, `timeout -k 30 2000`): без единого движкового краша
   дошёл до **782 из 786** `TEST_END` за ~34 минуты, после чего застрял в той же точке
   стека — `testrunner.py::wait_for_all_managers_done` → `threading.Event.wait` →
   `wptrunner.py::termination_handler` (это уже СЛЕДСТВИЕ моего `timeout`'а, не причина
   зависания: стек показывает, что `wait()` уже блокировался, когда пришёл SIGTERM).
   Дошёл до истечения `timeout -k 2000/30` и был убит по `-k` — на сей раз таймаут
   сработал, но результата (`check: ...`) так и не увидели.

## Почему это важно

Обязательный порядок среза (`--update-expected`, сразу `--check`, exit 0) для `websockets`
**не выполним технически**, а не потому что baseline плохой — сам гейт не доживает до
вердикта дважды подряд разными способами. Baseline (185 `.ini` от прогона 1) **не
закоммичен** этим срезом: изменения к `tests/wpt/metadata/websockets/` полностью
откачены. Категория остаётся долгом WPT-RUN-7 (как и `content-security-policy`/`css` —
см. ROADMAP.md строку WPT-RUN-7, срез 6), но с другим блокером: не флакующий результат
(как [BUG-1003](BUG-1003-OPEN.md)/[BUG-1004](BUG-1004-OPEN.md)/[BUG-1005](BUG-1005-OPEN.md)),
а невозможность вообще получить финальный вердикт на прогоне такой длины/размера.

## Срез 8 (2026-09-06): гипотеза «размер/длительность» опровергнута

`WPT-RUN-7` срез 8 (P2) наткнулся на тот же класс дважды на **маленьких** новых
категориях — размер и 30-минутная длительность из `websockets` тут ни при чём:

- **`long-animation-frame`** (54 файла, 25 id) — `--update-expected` (`--all --root
  long-animation-frame --recursive`) вылетел на **4:37** внутреннего времени (не 8:17
  как в `websockets`) с тем же `[bidi] frame error: io: failed to fill whole buffer`,
  тест-раннер зафиксировал `CRITICAL Last test did not complete`, `Tests left in the
  queue: '/long-animation-frame/loaf-layout-duration.html' and 24 others`, затем
  завис (тот же `futex_wait`/`anon_pipe_read`, `timeout 280` без `-k` не добил —
  снят вручную `kill -9` после ~14 минут провисания). Baseline не написан
  (`--update-expected` не успел ничего закоммитить), категория пропущена этим срезом.
- **`signed-exchange`** (74 файла) — тот же прогон (`--update-expected`) не дал ни
  одной строки `TEST_END` в выводе (буферизация `| tail -N` в фоновой команде съела
  весь прогресс, так что момент краша/зависания не виден), но `timeout -k 10 280`
  оборвал его на идентичном стеке `wait_for_all_managers_done` →
  `threading.Event.wait` → `wptrunner.py::termination_handler` — тот же паттерн, что
  и «прогон 2» по `websockets`. Категория тоже пропущена этим срезом.

Обе категории на порядок меньше `websockets` (25 и не более ~100 id против 786) и
укладывались бы в единицы минут при штатном прогоне — то есть блокер не «слишком
длинный/большой прогон», а нечто в конкретных тестах (или в самом BiDi-соединении,
роняющемся на случайных запросах вне зависимости от типа теста). Категории, прошедшие
чисто в том же срезе (`storage-access-api` — 32 с, `eventsource` — 2:18) не тронуты
этим классом, так что триггер не универсален для каждого прогона.

## Срез 23 (2026-09-07): третья независимая репродукция, новая деталь по таймингу bidi-краша

`WPT-RUN-7` срез 23 (P2) наткнулся на тот же класс третий раз, на этот раз на **самой маленькой**
из трёх категорий (`html/rendering`, 150 harness id против 786 у `websockets`/25 у
`long-animation-frame`) — попутная проверка гипотезы «wall-clock-чувствительность» из
[BUG-1011](BUG-1011-OPEN.md) §Локализация (`--update-expected --all --root html/rendering
--recursive --processes=6 --timeout-multiplier=3`, обёрнуто `timeout --kill-after=20 480` вместо
голого `timeout`, ровно из-за уже известного по этому багу риска не добиться завершения):

- Все 6 воркеров дошли до `INFO No more tests` (тест-лог-время 3:19–4:55) — **ни один краш
  движка не потребовался**, чтобы упереться в тот же класс: пятеро воркеров закрылись чисто, а
  шестой (обслуживающий директорию `replaced-elements/svg-embedded-sizing/`) продолжал грузить
  новые тесты ещё **как минимум до 6:15** внутреннего времени (третий по счёту TIMEOUT-файл той
  же директории на этом прогоне — `svg-in-object-fixed.html`, см. ниже) — то есть при
  `--timeout-multiplier=3` тесты, которые и без множителя виснут на полный дефолтный тайм-аут
  (`svg-in-img-auto.html`/[BUG-630](BUG-630-FIXED.md), `svg-in-object-*`/[BUG-798](BUG-798-OPEN.md),
  см. ниже), теперь стоят воркеру **втрое
  больше времени каждый**, и именно это — а не размер категории — не даёт прогону завершиться
  в разумный срок. Внешний `timeout --kill-after=20 480` сработал на 480 с (7:57 внутреннего
  времени) ровно на середине следующего файла директории (`svg-in-object-percentage.html`,
  который так и не получил `TEST_END`) — тот же стек `wait_for_all_managers_done` →
  `threading.Event.wait` → `termination_handler` → `KeyboardInterrupt`, что и в обоих прежних
  репродукциях.
- **Новая деталь, сужающая гипотезу о `[bidi] frame error`:** в этом прогоне запись
  `CRITICAL Main thread got signal; waiting for TestRunnerManager threads to exit` (реакция на
  мой `SIGTERM`) и следующая за ней `pid:375648 [bidi] frame error: io: failed to fill whole
  buffer` разделены **0.01 с** (`7:57.15` → `7:57.16`) — то есть краш bidi-соединения произошёл
  не спонтанно посреди теста (как читались оба предыдущих случая), а **в ту же миллисекунду**,
  что и получение сигнала на завершение. Правдоподобнее не «браузер падает сам по себе на
  случайном тесте», а «прерывание живого bidi-соединения посреди `--kill-after`/`SIGTERM`-
  завершения ломает протокол кадров» — оба прежних случая (`websockets`/`long-animation-frame`)
  тоже били по `timeout` БЕЗ `-k` или с `-k`, то есть тоже несли внешний сигнал завершения где-то
  рядом по времени с крашем, просто раньше это не сопоставлялось построчно. Не проверено:
  воспроизводится ли `wait_for_all_managers_done`-зависание совсем БЕЗ внешнего `timeout`
  (естественное завершение долгого прогона) — во всех трёх известных репродукциях внешний
  сигнал уже присутствовал до появления `[bidi] frame error`, порядок причины и следствия между
  «сигнал» и «крах» не разделён экспериментально.
- Побочная находка того же прогона (не этот баг, разбор — [BUG-1011](BUG-1011-OPEN.md) §Срез 23):
  `svg-in-object-fixed.html` из той же директории даёт детерминированный (3/3 изоляции) TIMEOUT
  по отдельной, уже открытой причине ([BUG-798](BUG-798-OPEN.md)/`GAP-LOADEV`), не связанной с
  этим тулинговым багом.

## Что не проверялось

Не установлено, что именно роняет `[bidi]`-соединение — общий признак двух известных
крашей (`websockets` тест `Create-protocols-repeated.any.js?wpt_flags=h2`,
`long-animation-frame` где-то до/на `loaf-layout-duration.html`) не найден: разные
категории, разные типы тестов. Не проверено, восстанавливается ли `wptrunner` сам при
достаточно большом `timeout -k` (в обоих новых случаях процесс убит вручную/через `-k`
до появления `check:`/`expectations:` строки — то есть неизвестно, ждёт ли он вечно
или просто дольше разумного). ~~Первый шаг для локализации — `--processes N`
(шардирование) вместо одного прогона~~ — испробовано срезом 23 (`--processes=6`) и не
предотвратило зависание, только даёт по нему более чистую (без движкового краша) картину —
шардирование само по себе не блокер; второй шаг (редирект в файл, не `| tail -N`) применён
срезом 23, подтверждён рабочим. Не проверено: воспроизводится ли зависание совсем БЕЗ внешнего
`timeout`/сигнала (срез 23 §Новая деталь) — во всех трёх известных репродукциях сигнал уже
присутствовал до/одновременно с крашем `[bidi]`, порядок причины и следствия не разделён.

## Воспроизведение

```
LUMEN_PROFILE=dev-release python tests/wpt/run_report.py \
  --binary target/dev-release/lumen --all --root websockets --recursive --check \
  > /tmp/websockets-check.log 2>&1 &
# наблюдать: TEST_END-счётчик в логе, kill -0 <pid>; если завис —
# смотреть /proc/<pid>/task/*/wchan, не просто ждать
```
