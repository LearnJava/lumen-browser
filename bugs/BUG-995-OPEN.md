# BUG-995 — после свопа листа OneTrust релейаут fandom.com не оседает минутами

**Статус:** OPEN
**Заведён:** 2026-09-04 (P3, при проверке фикса BUG-987)
**Область:** не локализовано — shell (`crates/shell/src/relayout.rs::refresh_dynamic_css`
+ полный рестайл после правки `<style>`)
**Владелец:** P3

## Симптом

На живом fandom.com после инъекции стилей OneTrust (`CSS пересобран после
правки <style>: 2731 правил`) процесс уходит в релейаут, который жжёт ~2 ядра
и не завершается за 4.5+ минут: сетевых действий нет (лог замер, последняя
строка — та самая пересборка), память стабильна ~1.33 ГБ, окно живо.

## Почему виден только сейчас

До фикса BUG-987 (этап 1 — стеки) процесс в этой же точке умирал крэшем
(`thread 'main' has overflowed its stack`), поэтому «что происходит после
свопа» никогда не наблюдалось. Фикс убрал крэш — и вскрылся этот слой.

## Что говорит измерение

- Живой прогон 2026-09-04 с фиксированным бинарём: CPU 132.7 с → 190.8 с за
  30 с стены (~1.9–2.5 ядра), затем 190.8 с → 474.8 с за 150 с (~1.9 ядра),
  лог заморожен на строке пересборки с ~65 с от старта; процесс убит
  принудительно на ~5-й минуте.
- Не глубина дерева: оффлайн-страницы `<div>`×N (150…5000) на тех же стеках
  оседают за секунды (dump-layout, screenshot).
- Кандидат — полный рестайл без инкрементальности: `refresh_dynamic_css`
  сбрасывает `page_prev_cascade_styles = None` (комментарий в коде: «против
  нового листа они недействительны»), дальше матчинг ~2731 правил × DOM.
  Родственная зона — BUG-341 (инкрементальный рестайл; пауза по решению
  пользователя 2026-07-28, возобновлять только по явному запросу).

## Первый шаг

Замерить, что именно горит: `--trace-nav` на fandom.com (headless, тот же
путь инъекции) — если воспроизводится headless, дальше
`LUMEN_FRAME_LOG=1`/профилировщик: раскладка vs матчинг правил vs повторные
проходы (relayout-loop). Разделить «один очень дорогой проход» и «много
проходов, каждый дешёвый» (второе — отдельная форма, сродни BUG-341 churn).

## Сырые данные

`.tmp/bug987/fixlive.err` (лог живого прогона с фиксированным бинарём),
замеры CPU — наблюдение P3 2026-09-04.