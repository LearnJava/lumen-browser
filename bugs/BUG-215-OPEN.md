# BUG-215

**Статус:** OPEN (DEBTOR)
**Компонент:** layout
**Тест:** TEST-113 (diff 1.41%)

## Описание

CSS Shapes `shape-outside: path()` — float обтекание треугольного SVG-контура.

> **Ревизия 2026-06-23 (дрейф трекера):** **реализован** — `parse_shape_path_px` флэттит
> path()→полигон, текст обтекает диагональ через `FloatContext`
> (`float_context_path_left_float` + `parse_shape_path_*` тесты). Diff-картинка TEST-113
> подтверждает: фича работает, остаток 1.41% = AA вдоль диагональной кромки shape + font-parity
> обтекающего текста (rule 2/3). Внесён в KNOWN_DEBTORS (1.41%).

> **Ревизия 2026-08-05 (P3, BUG-277 срез 16):** запись временно перекочевала под BUG-277 при
> full-run ратчете P1-wgpu-flip (2026-07-16), затем улучшилась до 1.30% срезом 8 (точная форма
> `clip-path` в wgpu). Перепись «wgpu-окно против headless-CPU» (1.30% против 1.36%, в пределах
> шума) подтвердила: дефекта wgpu-исполнителя не осталось — остаток того же класса, что и
> исходный 1.41% (AA диагональной кромки + font-parity), перецелён обратно на этот баг.
> KNOWN_DEBTORS baseline снижен 1.41%→1.30%.

## Воспроизведение

`python graphic_tests/run.py --only 113` → DEBTOR 1.30%
