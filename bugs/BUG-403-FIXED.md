# BUG-403 — Профиль-поповер невидим под дефолтным движковым хромом (регрессия CC-14)

**Статус:** FIXED 2026-07-28 (P1, CC-15-1)
**Компонент:** shell (`crates/shell/src/main.rs`, `panels::profile_menu`)
**Найден:** P1, скоупинг CC-15 (2026-07-28), при аудите зависимостей `toolbar.rs`/`panels/*` от флага `css_chrome_enabled`

## Симптом

Клик по аватар-кнопке нового движкового хрома (`ChromeAction::ToggleProfileMenu`, `main.rs:9553-9554`)
безусловно вызывает `self.profile_menu.toggle()`, выставляя `profile_menu.visible = true`. Хит-тест
поповера (`main.rs:14616-14624`, `panels::profile_menu::hit_test`) тоже безусловен — гоняется вне
зависимости от `css_chrome_enabled`. Но **рендер** поповера (`panels::profile_menu::build_panel`,
`main.rs:16975`) лежит внутри легаси-only блока `if !self.focus.active && !self.css_chrome_enabled`
(`main.rs:16869`), который под дефолтным хромом (`css_chrome_enabled == true` начиная с CC-14, если
не выставлен `LUMEN_LEGACY_CHROME=1`) никогда не выполняется.

Итог: клик по аватару переключает внутреннее состояние (`visible = true`), но ничего не рисуется —
поповер невидим. Хит-тест при этом продолжает съедать клики в области, где поповер *был бы* нарисован
(та же логика класса бага, что CC-5/CC-10a чинили для других невидимых легаси-панелей — «ест клики под
собой»), только тут наоборот: реальный элемент невидим, а хит-тест жив.

`crates/chrome/src/model.rs` не содержит ни одной привязки для `#profileMenu` — CC-9's собственная
закрывающая заметка (`docs/tasks/p1-css-chrome.md:271-273`) уже фиксировала «`SetProfile`-клик в новом
хроме остаётся no-op — поповер профиля ещё legacy-оверлей», но это было безопасно, пока легаси-хром
был дефолтом (легаси-рендер и легаси-хит-тест совпадали). CC-14 флипнул дефолт на движковый хром, не
трогая этот путь — регрессия эта не была замечена явным чек-листом паритета CC-14, т.к. чек-лист
(`docs/tasks/p1-css-chrome.md:442`) числит профиль-поповер как уже мигрированный панелью
CC-9/CC-10b, чего фактически не произошло.

## Причина

Незавершённая миграция: `ChromeAction::ToggleProfileMenu` подключён к переключателю состояния (CC-5/6),
но экран `#profileMenu` не получил своего `ChromeProfileModel`/`bind_profile_menu` в `crates/chrome/src/model.rs`
(в отличие от `#downloadsPanel`/`#permPopover`/`#omniDropdown` и т.д. из CC-9/CC-10a/CC-10b) — рендер
остался привязан к легаси-only ветке.

## Фикс (CC-15-1, 2026-07-28)

Выбран вариант 2 — `panels/profile_menu.rs` явно закреплён легаси-оверлеем навсегда (не кандидат на
привязку к `ChromeModel`/удаление в CC-15): полная миграция на движковую разметку (`#profileMenu` уже
существует в `assets/chrome/chrome.html`, но `ChromeAction::SetProfile` — намеренный no-op, см. коммент
CC-9/CC-10 в `dispatch_chrome_action`) означала бы новый `ChromeProfileModel`/`bind_profile_menu` +
реальную привязку клика по `data-profile`-слагу к `profiles.set_active` — задача заметно шире XS-среза
и вне блокера CC-15-3.

Вместо этого — минимальный корректный фикс симметрии рендер/хит-тест:

1. Рендер поповера (`build_panel`, был внутри `if !self.focus.active && !self.css_chrome_enabled`,
   `main.rs:16869`) вынесен в отдельный безусловный `if !self.focus.active && self.profile_menu.visible`
   блок сразу после закрытия легаси-only ветки — рисуется в обоих хромах, как и хит-тест уже делал.
2. Обе точки геометрии (рендер `main.rs` + `panels::profile_menu::hit_test`) переведены с захардкоженной
   `toolbar::CHROME_H` на `self.page_offset().1` — то же лекарство, что CC-14 применила для
   `resolve_automation_target`, чтобы не завести под дефолтным движковым хромом *ещё один* экземпляр
   класса бага BUG-404 (расхождение измеренной высоты хрома `chrome_page_host_rect` и константы
   `CHROME_H`) в том самом коде, который этот фикс делает живым.

`docs/tasks/p1-css-chrome.md` CC-14 checklist (профиль-поповер, ранее ошибочно строка «движковый») уже
поправлен на «legacy-оверлей навсегда» в ходе скоупинга CC-15 — оставлен как есть, финализирован этим
срезом. Верификация: `cargo check -p lumen-shell`, `cargo test -p lumen-shell` (полный прогон, дефолт —
движковый хром) — вручную живой клик по аватару не гонялся (тот же пробел OS-инжекции, что CC-5/CC-14
задокументировали), риск закрыт code review + существующим тестовым покрытием `panels/profile_menu.rs`
(hit-test/render не меняли сигнатуры, только вызывающие аргументы).

## Связанные

* [BUG-404](BUG-404-FIXED.md) — тот же класс: захардкоженная `toolbar::CHROME_H`-геометрия, не
  прошедшая через `page_offset()`, не замеченная чек-листом CC-14. Всё ещё OPEN — `flush_pointer_moves`
  не тронут этим срезом (другой вызывающий сайт, вне DoD CC-15-1).
* CC-14 (`ROADMAP.md`) — флип дефолта, который сделал это наблюдаемым.
* `docs/tasks/p1-css-chrome.md` §CC-14 checklist / §CC-15 — чек-лист паритета, поправленный в скоупинге
  CC-15 и финализированный этим срезом.
