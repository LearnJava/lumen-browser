# BUG-935 — M4 incremental restyle for the rAF-dirty pump is dead code under the default (engine-thread-on) build; a page with an active rAF+DOM-mutation loop pays a full off-thread cascade recompute on every tick

**Статус:** OPEN
**Компонент:** shell (`crates/shell/src/relayout.rs` — `relayout_raf_dirty`, `relayout_raf_dirty_readback`, `submit_relayout_job`, `try_relayout_raf_incremental`)
**Найден:** 2026-09-01 (P3), при ревизии BUG-286 (DEBTOR, `content-visibility:auto` scroll stopor) — свежий живой замер на ria.ru показал не одну паузу ~580мс, а 33 полных off-thread relayout'а (по 850–2600мс каждый) за ~15–20с сессии, все при неизменном `dl=1690 styled=1744` (без роста DOM)

## Симптом

Живой прогон (`--mcp-live-port`, `LUMEN_PROFILE_TREE=1`+`LUMEN_FRAME_LOG=1`,
метод BUG-286 «Замер 2026-08-06») на https://ria.ru, повторённый 2026-09-01
на свежем `dev-release`: **33** записи `[engine] relayout Nms (off-thread)
dl=1690 styled=1744` за один запуск (9 — ещё до первого MCP `scroll`-вызова,
во время начальной загрузки/settle; 24 — во время и после 20 тиков скролла).
Все с одинаковым `dl`/`styled` — размер DOM не растёт, т.е. это не
раскрытие `content-visibility` (тот срабатывает один раз и увеличивает
`styled`), а нечто, дёргающее relayout непрерывно на одной и той же
странице.

Соответствующие MCP `scroll`-вызовы получали RTT 850–1400мс вместо
ожидаемых 1–5мс (BUG-286's 2026-08-06 замер: «scroll держит 2-6мс без
повторов») — round-trip блокируется, пока идёт relayout (вероятно,
блокировка на `Document`-мьютексе, которым relayout тоже пользуется через
свой снимок).

## Корень

`ria.ru` держит на главной странице `videojs`-плеер (лог содержит `[JS
error] VIDEOJS: ERROR: ...` — сам плеер не критичен, но подтверждает его
активность), который гоняет `requestAnimationFrame`-цикл и на каждом тике
трогает DOM (UI-контролы плеера). Каждый такой тик помечает `dom_dirty` и
вызывает `Lumen::relayout_raf_dirty()` (`about_to_wait.rs:392`).

`relayout_raf_dirty` (`relayout.rs:337`):

```rust
pub(crate) fn relayout_raf_dirty(&mut self) {
    if !self.submit_relayout_job() && !self.try_relayout_raf_incremental() {
        self.relayout();
    }
}
```

`&&` короткого замыкания: `submit_relayout_job()` вызывается первым и, пока
движковый поток жив (`LUMEN_ENGINE_THREAD=1` — дефолт с ADR-023,
2026-07-28), **всегда** возвращает `true` (единственное условие отказа —
отсутствие `LayoutSource`/вырожденный viewport). Поэтому `||`-ветка с
`try_relayout_raf_incremental()` — M4-инкрементальный путь
(`layout_mutation_incremental`/`relayout_page_incremental_restyle`,
переиспользующий `self.layout_box` вместо полного `compute_layout`) —
**никогда не выполняется на дефолтной сборке**. Симметричная функция
`relayout_raf_dirty_readback` (`relayout.rs:486`, RedrawRequested-путь)
устроена так же: `readback_relayout_job()` (тоже полный, тоже
приоритетный, пока движковый поток жив) закрывает `&&` раньше, чем
инкрементальный путь успевает сработать.

Старый doc-комментарий `relayout_raf_dirty` утверждал обратное — «tries the
incremental path first ... before falling back to ... submit_relayout_job
(engine thread)» (не совпадало с кодом ни в одном билде; поправлен в этой
же сессии, см. «Что уже сделано» ниже). Сестринская функция
`relayout_raf_dirty_readback` документирована **корректно** («in the
single-thread fallback path, tries the incremental layout ... before the
full relayout») — т.е. везде, где движковый поток включён (дефолт),
`try_relayout_raf_incremental` — код для `LUMEN_NO_ENGINE_THREAD=1`, а не
для основной конфигурации.

`make_relayout_job` (`relayout.rs:726`, тело задания `submit_relayout_job`)
зовёт `compute_layout(&document, &stylesheet, viewport, ...)` — полный
пересчёт каскада для ВСЕГО документа (та же стоимость, что и корень
BUG-286: `docs/tasks/p3-cascade-perf.md` «Задача 3»), просто исполняемый
асинхронно на движковом потоке вместо UI-потока. Async ⇒ UI не блокируется
(картинка не подвисает), но движковый поток остаётся занят полным
пересчётом на каждый rAF-тик с DOM-мутацией — на странице с активным
rAF+DOM-цикл (видеоплеер, карусель, «живая лента» и т.п.) это превращается
в непрерывный, а не разовый расход: пока цикл идёт, движковый поток почти
всегда занят одним relayout'ом и сразу берёт следующий (сообщение
`EngineThread::submit`'а coalescing'ит промежуточные заявки — очередь не
растёт бесконечно, — но полезная работа M4 всё равно теряется: каждый
принятый к исполнению relayout всё равно **полный**, не инкрементальный).

## Почему не тот же баг, что BUG-286

BUG-286 — про **маршрутизацию** (`self.relayout()` → `self.relayout_raf_dirty()`
для конкретного триггера `content-visibility` relevance) и её конечный вывод
был «одна пауза ~580мс, повторный full recompute сайт не запрашивает».
Замер 2026-09-01 подтверждает, что маршрутизация content-visibility сама по
себе не сломана (никакого регресса в её логике нет), но показывает, что
**тот же** `relayout_raf_dirty`, куда BUG-286 маршрутизировал триггер,
обслуживает и rAF-DOM-мутации — и там M4-инкрементальный путь,
специально построенный, чтобы такие мутации не стоили полного пересчёта
каскада, не работает на дефолтной сборке. BUG-286 остаётся закрытым в
своём скоупе (маршрутизация верна); это отдельная, точно
локализованная находка.

## Что уже сделано в этой сессии (P3, 2026-09-01)

Поправлен doc-комментарий `relayout_raf_dirty` (`relayout.rs:334-338`) —
описывал не тот порядок вызовов, который реально исполняется; приведён в
соответствие с кодом и с формулировкой сестринской функции.

## Предполагаемый фикс (НЕ сделан этой сессией — нужен перф-замер, не точечная правка)

Наивно — поменять порядок в обеих функциях местами
(`try_relayout_raf_incremental` до `submit_relayout_job`/`readback_relayout_job`),
чтобы M4 реально работал под дефолтной сборкой. Это меняет задокументированный
инвариант «async-safe триггер уходит off-thread, UI-поток не блокируется» —
`try_relayout_raf_incremental` синхронный и исполняется НА UI-потоке (мутирует
`self.layout_box`/`self.page_prev_cascade_styles`, которые UI-поток
единолично владеет), т.е. переключение сделает rAF-DOM-мутации ЧАСТИЧНО
UI-блокирующими вместо полностью асинхронных — компромисс «дешевле, но на
UI-потоке» вместо «дороже, но всегда off-thread». Нужен тот же протокол, что
и любая правка на этом файле по `docs/perf-method.md`: census ДО, интерливед
A/B (стоимость инкрементального прохода на UI-потоке per rAF-tick против
частоты пропущенных кадров) на представительном наборе сайтов с активным
rAF-циклом (ria.ru — готовый стенд), не только на синтетике. Масштаб и риск
измерения — сравним с BUG-341 (27 срезов), не точечный P3-фикс; следующая
сессия должна начать с `docs/perf-method.md` и живого замера на ria.ru по
методу этого файла.

## S1 (P3, 2026-09-20) — census заблокирован: ria.ru не докрашивает контент в этой сессии

Начат перф-протокол (`docs/perf-method.md` §1 «до первой правки — перепись»):
написан `scripts/bug935_raf_relayout_census.py` (MCP-клиент по образцу
`scroll_perf.py`, считает `[engine] relayout … (off-thread) dl=… styled=…` по
трём окнам — до `document_ready`, во время settle, во время scroll-тиков — и
RTT самого `scroll`).

Живой прогон на `https://ria.ru` (репро-команда файла, `--maximized`) не
дошёл до состояния, которое описывает симптом бага: за 130+с (`wait
document_ready` таймаут 20с + явное ожидание первого кадра с непустым
content-display-list таймаут 90с + settle 20с) `[frame] total … dl 0 cmds`
не меняется — красится только chrome (128 cmds), самого документа нет ни в
одном кадре. Сетевой GET на `https://ria.ru` с той же машины напрямую
(`urllib.request`) отвечает `200` за 0.4с — проблема не в сети до сайта, а
где-­то в пути загрузки/рендера самой страницы внутри Lumen. Тот же скрипт на
`https://example.com` красит контент почти мгновенно (`painted=True` мгновенно) —
инструмент рабочий, стенд рабочий, блокер специфичен для `ria.ru` в текущей
сессии.

stderr-лог обрывается на одном и том же месте во всех повторах (после
`Загружен скрипт: …/libs/jquery.fileupload.js` и пары `[JS error] Uncaught
TypeError: Cannot read properties of undefined (reading 'length')`) — далее
только периодические `[frame]` от собственных `scroll`-вызовов census'а, ни
одного нового `→ GET`/`← NNN`/`Загружен скрипт`. Похоже на зависание где-то
после этой точки загрузки (JS-цикл, зависший fetch без обработчика, что-то
ещё) — не исследовано глубже в этой сессии, эта находка **отдельна** от
исходного симптома BUG-935 (который предполагает, что страница уже
дорисована и только relayout-путь неоптимален): пока не дорисовано, до
проверки самого M4-инкрементального пути дело не доходит вовсе.

Правки кода не делались (только новый скрипт census'а, `.rs` не тронут).
Следующий срез — сначала локализовать сам стопор загрузки `ria.ru` (возможно,
отдельный баг сам по себе; проверить на другом rAF-тяжёлом сайте, указанном
как альтернативный стенд, и/или пройти путь загрузки чтением, раз живой
прогон не воспроизводится стабильно новой сессией) — только потом собственно
census relayout'ов, ради которого этот срез затевался.

## S2 (P3, 2026-09-20) — зависание НЕ на загрузке: первый кадр документа рисуется, дальше тишина при почти нулевой загрузке CPU

Пересборка `dev-release` в этой сессии (тот же слот, тот же коммит census-скрипта
из S1) и повтор репро-команды файла (без изменений в коде) дал **другую**
картину, чем S1: `[wgpu] pipeline …` warm-up для `mask`/`circle`/`mipgen`/
`cross-fade` пайплайнов (эти инициализируются лениво при первом реальном
использовании) — то есть, в отличие от S1 (`dl 0 cmds`, красится только
chrome), в этой сессии контент документа **дорисовался** хотя бы раз. Тем не
менее после строки `pipeline cross-fade-pipeline: 33ms` — полная тишина: ни
одной новой `[frame]`, `[JS]`, `→ GET`/`← NNN` строки за 45+с (два опроса лога
с интервалом 20с и 30с, ноль новых строк).

Процесс (`lumen.exe`, не `lumen-shell.exe` — итоговый бинарь называется иначе)
жив всё это время (виден в `tasklist`), но `CPU-время` за 15с реального
ожидания выросло всего на ~1.3с (`765484`→`765616` в единицах `tasklist`,
т.е. почти простой) — значит это не busy-loop/спин на JS-стороне (там было бы
почти 100% одного ядра), а настоящая блокировка где-то (мьютекс, channel
`recv`, синхронный I/O) уже ПОСЛЕ первого кадра. `taskkill /F` подтвердил,
что процесс всё ещё был жив и ничего не обрабатывал сам (без внешнего
`SIGKILL`-аналога не завершился).

Два разных застоя в двух разных сессиях на одной и той же команде (S1: до
первого кадра; S2: сразу после первого кадра) говорят, что дефект,
блокирующий репро этого бага, недетерминирован по месту — воспроизводится
где-то в широком окне «загрузка скриптов → первый рендер → далее», не в одной
фиксированной точке. `[JS error] Uncaught TypeError: Cannot read properties
of undefined (reading 'length')` (`<anonymous>:4:57`, дважды, оба раза до
зависания в обеих сессиях) — стабильный маркер, но причинно-следственная
связь с зависанием не установлена (ошибка одна и та же что в S1, но там
зависание было РАНЬШЕ неё по логу, здесь — позже).

Правки кода снова не делались (только этот раздел документа). Следующий
срез — не пытаться воспроизвести живьём вслепую ещё раз (третий раз подряд
даёт третью картину, стенд нестабилен), а: (1) подключиться MCP-клиентом
СРАЗУ после первых признаков зависания и проверить, отвечает ли сам MCP-порт
(отличит «весь процесс встал» от «встал только render/JS-поток, IPC жив»);
(2) если процесс просто зависает на каждом повторе — снять стек потоков
живого `lumen.exe` внешним отладчиком (`windbg`/`cdb -p <pid>`) в момент
зависания, а не читать код вслепую.

## S3 (P3, 2026-09-20) — пункт (1) выполнен: подтверждён настоящий deadlock UI-потока на lenta.ru, отдельно от ria.ru-стопоров S1/S2

Написан `scripts/bug935_hang_probe.py`: запускает репро, ждёт первую покраску
контента (как в census-скрипте), затем детектирует «тишину» в stderr-логе
(15с без новой строки) и в этот момент шлёт MCP `resources/read` на
`resource://console` (лёгкий запрос — читает уже накопленный буфер
console-логов через `AutomationCommand::ConsoleLog`, без нового relayout) с
собственным сокетным таймаутом, отдельным от `DEFAULT_TIMEOUT=30с` в
`live_session.rs`, — чтобы отличить «сервер не ответил вовсе» от «ответил
дольше моего таймаута».

**На `https://ria.ru`** (четвёртый живой прогон подряд, четвёртая картина: в
этот раз контент так и не дорисовался за 90с — форма S1, не S2) проба
ответила МГНОВЕННО (0.0с) на тишину в логе после блока `Service worker
register` (`fb_permission_denied` — see лог): IPC-поток и обработчик
`AutomationCommand` в `winit`-event loop живы и дренируют канал, тишина в
stderr — не деадлок процесса, а просто ничего интересного не логируется,
пока страница либо ждёт сеть, либо крутит JS, который ничего не пишет в
`[JS]`/`[frame]`. Это **не** воспроизводит зависание BUG-935 (relayout-путь
никогда не активировался — `dl` так и не стал ненулевым), а закрывает
незавершённый пункт (1) предыдущего среза для этого конкретного стенда:
процесс НЕ завис в смысле «весь UI-поток встал».

**На `https://lenta.ru`** (взят как альтернативный rAF/карусель-стенд —
`docs/perf/corpus.txt`, тот же класс страницы: новостная лента с
каруселью/видео) картина другая и воспроизводится настоящим deadlock:
контент дорисовался (`painted=True`, `[frame] total 319ms dl 1552 cmds`
и ещё два кадра `Identical`/`band blit`), после чего пошла пачка
рекламных/аналитических fetch (top100.ru, `easylist`-блокировки
`sentry-saas.rambler-co.ru`/`tns-counter.ru`/`kraken.rambler.ru`), лог
замолкает — и MCP `resources/read(resource://console)` **не отвечает за все
60с** пробы (`Console log error: automation command timed out` —
`AutomationHandle::execute`'s собственный `DEFAULT_TIMEOUT=30с` истёк
первым, что подтверждает: `winit`-event loop не обработал ни один
`AutomationCommand` минимум 30 секунд подряд). Это первое воспроизведение в
этом баге, где отличие «весь процесс встал» от «встал только JS/render»
разрешено однозначно в пользу первого — UI-поток **не** дренирует свой
командный канал, то есть либо сам заблокирован (мьютекс/канал/синхронный
I/O на UI-потоке), либо событийный цикл `winit` не докручивается до обработки
пользовательских событий по другой причине. И `tasklist` на PID процесса,
поднятого из-под git-bash, сам зависал на 5-секундном таймауте (отдельная
шероховатость среды, не бага — PID, который возвращает `subprocess.Popen`
под MSYS-питоном, не всегда совпадает с нативным Windows PID достаточно
надёжно для `tasklist`/`taskkill` по нему; не мешает диагнозу выше, но
следующая попытка снять стек живого процесса должна найти PID независимо,
например через `wmic process where "name='lumen.exe'"` или Task Manager
вручную).

Правки кода не делались (только скрипт-проба и этот раздел). В среде этой
сессии не оказалось ни `cdb`/`windbg` (Debugging Tools for Windows), ни
`gdb`, ни `procdump` — пункт (2) предыдущего среза («снять стек потоков
внешним отладчиком») невыполним без внешней установки инструмента (`winget
install Microsoft.WinDbg` или аналог), а это системное изменение вне
`D:\RustProjects\lumen-browser\`, которое протокол требует согласовать с
пользователем, а не делать самостоятельно. Следующий срез должен либо (а)
получить подтверждение пользователя на установку Debugging Tools for
Windows и снять стек `lumen.exe` в момент зависания на `lenta.ru` (стенд
теперь известен как воспроизводимый и более стабильный, чем `ria.ru`), либо
(б) продолжить локализацию без отладчика — по логике кода, окно тишины
начинается сразу после блока рекламных `fetch`, часть из которых
блокируется `easylist` (`network error: blocked`) — стоит проверить, не
держит ли обработка `fetch`-ошибки/CSP-блокировки на UI-потоке блокирующий
примитив (мьютекс сети, `Sender`/`Receiver` без второй стороны), которым
затем оказывается заблокирован и `winit`'s `about_to_wait`/событийный цикл
целиком.

**Попытка установки отладчика (эта же сессия, с согласия пользователя):**
`winget install Microsoft.WinDbg` нашёл уже установленную Store-версию — её
`cdb.exe`/`dbgeng.dll` (`WindowsApps\Microsoft.WinDbg_...\amd64\`) лежат в
MSIX-песочнице: прямой запуск извне пакета даёт «отказано в доступе» даже на
`-version`, то есть эта копия для `cdb -p <pid>` из скрипта/шелла не годится.
Попытка поставить классический "Debugging Tools for Windows" через
`Microsoft.WindowsSDK.10.0.26100` (`winsdksetup.exe /features
OptionId.WindowsDesktopDebuggers /quiet`) зависла сама: 0% CPU, ноль сетевых
соединений, ни одного файла лога за 90+с — сессия работает под
неповышенным токеном (`net session` → отказано в доступе), а инсталлятору
для системной установки нужна elevation, запрос которой в этой безголовой
среде показать (и подтвердить) некому; процессы пришлось прибить
(`taskkill`). Пользователь подтвердил, что поставит Debugging Tools for
Windows сам на десктопе (кликнув UAC вручную) — следующая сессия должна
**начать** с проверки `where cdb`/`where windbg` (или прямого пути
`C:\Program Files (x86)\Windows Kits\10\Debuggers\x64\cdb.exe`), и только
если он появился — переходить к снятию стека `lumen.exe` в момент
зависания на `lenta.ru` (репро-команда та же, стенд подтверждён в этом
срезе). Если инструмента всё ещё нет — идти путём (б) выше (чтение кода
вокруг обработки `fetch`/`easylist`-блокировки на UI-потоке).

## S4 (P3, 2026-09-20) — найден и починен реальный deadlock-механизм из S3 (без отладчика, чтением кода); исходный M4-incremental-путь бага пока не тронут

Отладчик (`cdb`/`windbg`) в среде так и не появился (S3's два пути установки
не сработали, пользователь ставит вручную на десктопе). Пошёл путём (б) из
S3 — чтение кода вокруг обработки `fetch` на UI-потоке, без живого прогона.

Нашёл: `crates/network/src/lib.rs` `connect_inner` (`:1169`, `:1202`) вызывал
`TcpStream::connect(addr)` — **без какого-либо таймаута** на сам TCP-connect
(SYN-хендшейк). `FETCH_READ_TIMEOUT=60s` (введён BUG-307, тот же файл,
`:1100`) бьёт по чтению уже установленного соединения, но не по самому
`connect()`. Хост, который на попытку соединения отдаёт "packets dropped, no
RST" (blackhole — типично для части рекламных/аналитических доменов за
firewall/adblock-инфраструктурой хостера, не самого easylist-блока, который
у нас отклоняется раньше, до сети, `network/src/lib.rs:2280-2325`), блокирует
`TcpStream::connect` на платформенный таймаут SYN-retry (может быть больше
минуты).

Путь до UI-потока: `fetch()` без `AbortSignal` (обычный вызов, каким рекламные
скрипты и зовут beacon-запросы) идёт синхронно
(`web_api_shim_mid_b2.js:1102-1104` → `_lumen_fetch_sync`,
`v8_runtime/install/net.rs:408-441`, без `AbortToken`/таймаута) внутри
**ordered** (не коалесцирующегося) `EngineThread`-таска
(`about_to_wait.rs:260-269`, `engine_bridge.rs:104`). `EngineThread::query()`
(`engine_thread.rs:230-245`) блокируется на `reply_rx.recv()` без таймаута —
и очередь FIFO, так что любой последующий `query()` (в т.ч.
`drain_query_js` на КАЖДОМ `on_about_to_wait()`, включая обработку
`AutomationCommand::ConsoleLog`, `about_to_wait.rs:972`) блокируется позади
застрявшего connect — что и объясняет S3-находку «`winit`-event loop не
дренирует `AutomationCommand`-канал 30+с при 0% CPU»: это не спин, это
цепочка блокирующих `recv()` за один незавершённый TCP-connect.

**Фикс:** `CONNECT_TIMEOUT = 10s` (новая константа, `lib.rs:~1101`),
применена в обоих местах `TcpStream::connect(addr)` (прямой путь и SOCKS5-
прокси) через `TcpStream::connect_timeout(addr, CONNECT_TIMEOUT)`. В отличие
от `read_timeout`, это не `Option`-параметр по вызывающей стороне — connect
всегда должен быть ограничен по времени, независимо от того, живёт ли после
него соединение долго (WebSocket/SSE) или недолго (обычный fetch); только
после установления соединения `read_timeout` расходится на "None" для
долгоживущих кейсов.

Deterministic unit-тест на сам connect-timeout не добавлен — воспроизвести
управляемый blackhole (no RST) на localhost без внешней нероутируемой сети
не получилось надёжно в этой среде (в отличие от read-timeout-теста
`connect_with_read_timeout_bounds_a_stalled_server`, где peer кооперативен и
просто не пишет байты — здесь peer должен НЕ ответить на TCP уровне, что
песочница либо эмулирует недетерминированно, либо возвращает "unreachable"
мгновенно вместо тайм-аута). `cargo test -p lumen-network --lib` (2293
теста) зелёный без регрессий, `clippy -p lumen-network --all-targets -D
warnings` чист.

**Не тронуто** этим срезом: сам исходный симптом BUG-935 (M4-incremental
relayout-путь мёртв под движковым потоком) — это отдельная, ещё не
затронутая находка того же файла (см. «Предполагаемый фикс» выше); S1-S3
были блокированы на нестабильности стенда/невозможности воспроизвести
исходный симптом, а не на этом deadlock'е напрямую — но обнаруженный
deadlock — правдоподобный кандидат на ОДНУ из причин, почему живой прогон на
рекламно-тяжёлых сайтах (ria.ru, lenta.ru) не доходит до состояния,
которое исходный симптом описывает (полное зависание раньше, чем
relayout-путь успевает провериться). Следующая сессия должна повторить S1-
S3 репро на `lenta.ru` С этим фиксом — если зависание исчезло, это
подтверждает причину и открывает дорогу к собственно M4-инкрементальному
census'у, ради которого баг заведён; если зависание осталось — искать
другой блокирующий примитив тем же методом.

## S5 (P3, 2026-09-20) — S4-фикс подтверждён живьём на lenta.ru; найден и не тронут соседний источник того же класса зависания (FIFO `EngineThread::query()` без таймаута)

Повторил репро S3 (`scripts/bug935_hang_probe.py https://lenta.ru`) на
собранном `dev-release` этой ветки (уже содержит S4 — `CONNECT_TIMEOUT=10s`
влит в `main` до старта этой сессии). Результат прямо противоположен S3:
`painted=True`, тишина в логе детектирована через 15с как и раньше, но на
этот раз `resources/read(resource://console)` **ответил за 4.7с** (S3: не
ответил вовсе за 60с). `tasklist` до/после пробы показывает штатный рост
RSS, процесс не завис. Это подтверждает диагноз S4: TCP-connect без
таймаута был как минимум одним реальным источником deadlock'а на этом
стенде, и его фикс наблюдаемо чинит хотя бы часть картины.

Дальше пошёл на более длинный прогон (не 90с probe-скрипта, а прямой запуск
на ~5 минут с тем же `LUMEN_PROFILE_TREE=1 LUMEN_FRAME_LOG=1`), чтобы
проверить не только «первую тишину», но и устойчивость дальше по времени
жизни вкладки — ровно то, что нужно для census'а M4-incremental-пути, ради
которого баг заведён. За первые ~220с — 4 `[engine] relayout` (146-242мс,
все full off-thread — `try_relayout_raf_incremental` по-прежнему не
исполняется под дефолтным движковым потоком, «Корень» раздела всё ещё
живой), заметно меньше исходных «33 по 850-2600мс», но страница и трафик от
рекламных доменов за сессию/день отличаются — это не количественное
опровержение исходного репорта, просто другой снимок того же живого сайта.

Затем прямой MCP-запрос (отдельный скрипт с одним переиспользуемым
`socket.makefile()`, не пересоздающимся на каждый вызов — попытка с новым
`makefile()` на каждый `call()` даёт ложный "connection error" на стороне
клиента и не годится для диагностики) к тому же процессу спустя ~230с работы
показал: `initialize` отвечает мгновенно (0.008с — TCP/IPC-приёмник жив), но
`resources/read(resource://console)` **не ответил за 25с**. Лог в этот
момент показывал незавершённые попытки к `kraken.rambler.ru`/`kraken-mdt.
rambler.ru` (часть блокируется easylist до сети, часть — TLS-ошибками
сертификата, которые возвращаются быстро) непосредственно перед тишиной.

Это **не новый независимый deadlock** — это тот же механизм, который S4 уже
назвал и явно пометил как нетронутый: `fetch()` без `AbortSignal` идёт
синхронно внутри **ordered** (не latest-wins) `EngineThread`-таска
(`_lumen_fetch_sync`), а `EngineThread::query()` (`engine_thread.rs:230-245`,
`reply_rx.recv()` без таймаута) исполняется по очереди FIFO — если очередь
уже держит синхронный fetch к серверу, для которого действует
`FETCH_READ_TIMEOUT=60s` (не `CONNECT_TIMEOUT=10s`, тот случай S4 не
покрывает — соединение уже установлено, зависание в чтении ответа/TLS-
рукопожатии на уже открытом сокете), **любой** последующий `query()`,
включая `AutomationCommand::ConsoleLog`, блокируется на срок до 60с. S4
починил один из двух путей входа в этот класс (connect), а не саму FIFO-
очередь без таймаута — она и есть настоящий корень «непредсказуемо долгого
зависания MCP/automation-канала», а не то, где именно застревает конкретный
сетевой вызов внутри неё.

**Правки кода не делались** (только измерение и этот раздел) — по тем же
причинам, что и раньше: точечно добавить таймаут в `EngineThread::query()`
затронуло бы ВСЕ его вызовы (`eval_js_value`, `take_dom_dirty`,
`take_raf_pending`, не только automation-путь), и `docs/plan/architecture.md`
инвариант "исполняется по порядку" для этого канала не обсуждает поведение
при таймауте не пришедшего ответа (спасательный откат на дефолт для каждого
вызывающего места пришлось бы проектировать отдельно) — ровно тот случай
«не точечная правка, нужен перф-протокол», о котором предупреждает раздел
«Предполагаемый фикс» выше, только теперь применительно к `query()`, а не
к `relayout_raf_dirty`.

**Итог:** S4 подтверждён рабочим на живом стенде — не откатывать. Основной
симптом бага (M4-incremental-путь не используется под движковым потоком)
по-прежнему воспроизводится (4 relayout'а по 146-242мс за ~220с). Найден
второй, отдельный от `relayout_raf_dirty`, вход в тот же класс «FIFO-
очередь блокируется без таймаута на синхронном сетевом вызове» —
`EngineThread::query()`. Следующая сессия должна решить, в каком порядке
брать три независимые находки этого бага (M4-incremental-роутинг,
`EngineThread::query()`-таймаут, оставшиеся сетевые пути без таймаута) —
это архитектурное решение, не входящее в scope одного среза.

## S6 (P3, 2026-09-20) — таймаут в `EngineThread::query()` (первая из трёх находок S5)

Из трёх независимых находок S5 взята первая: `EngineThread::query()`
(`crates/shell/src/engine_thread.rs`) больше не блокируется на
`reply_rx.recv()` без предела. Добавлена константа `QUERY_TIMEOUT = 5s`
(запас над наблюдёнными full-relayout 146–2600мс из S3/S5, много меньше
`FETCH_READ_TIMEOUT=60s`) и переход на `reply_rx.recv_timeout(QUERY_TIMEOUT)`.
Оба исхода — `Disconnected` (поток завершён) и `Timeout` (задание застряло в
FIFO позади долгого синхронного соседа) — возвращают `None`, который
вызывающая сторона (`route_query_js` и все его потребители: `take_dom_dirty`,
`take_raf_pending`, `eval_js_value` и т.д.) уже трактует как «без JS» —
новый режим отказа не заведён, существующий лишь ограничен сверху. Задание
из очереди никуда не девается и всё равно исполнится (мутация состояния `S`
не теряется) — теряется только ответ, который уже некому принять.

Добавлен регрессионный тест
`engine_thread::tests::query_times_out_when_stuck_behind_long_task`:
блокирующий `task()` (сон `QUERY_TIMEOUT + 1s`) перед `query()` — раньше
`query()` ждал бы весь срок соседа, теперь возвращает `None` не позже
`QUERY_TIMEOUT`. `cargo clippy -p lumen-shell --all-targets -- -D warnings`
и `cargo test -p lumen-shell -- engine_thread` — зелёные (35/35).

**Не сделано** (осталось для будущих срезов, из списка S5): M4-incremental-
роутинг всё ещё не исполняется под движковым потоком (основной симптом
бага); остальные сетевые пути (`fetch`/XHR) внутри `Task` по-прежнему без
собственного таймаута на чтение — `QUERY_TIMEOUT` бьёт только по стороне
ожидания вызывающего, а не по длительности самого сетевого вызова.

## S7 (P3, 2026-09-20) — тот же класс дедлока найден и починен в WebSocket-хендшейке

Продолжение аудита «синхронный сетевой вызов без таймаута блокирует ordered
`EngineThread`-задание» (S4/S5/S6 закрыли TCP-connect и `EngineThread::query()`).
Прочитан `crates/network/src/websocket/mod.rs::connect`/`connect_deflate`: оба
вызывают внутренний `connect(..., read_timeout: None)` с комментарием «No read
timeout — WebSocket сидит idle дольше любого fetch», что верно для **уже
установленного** соединения, но тем же `None` неявно накрывало и сам
`upgrade::perform`/`perform_with_deflate` — открывающий HTTP-хендшейк
(`GET ... Upgrade: websocket` → ожидание `101`), который читает построчно
через `read_exact` (`websocket/upgrade.rs::read_line`) без собственного предела.
Сервер/прокси, принявший TCP/TLS-соединение (проходит `CONNECT_TIMEOUT=10s` из
S4), но не ответивший `101` (зависший прокси, сервер, не понимающий Upgrade и
просто держащий сокет), блокирует этот `read_exact` навсегда — тот же класс,
что S4 (TCP-connect) и S5 (`fetch` без таймаута на чтение), только третий
самостоятельный вход, ранее не проверенный ни одним срезом.

**Фикс:** `connect`/`connect_deflate` (`websocket/mod.rs`) теперь выставляют
`FETCH_READ_TIMEOUT` (60s, та же константа, что бьёт по `fetch`/XHR) на сокет
ПЕРЕД вызовом `upgrade::perform`/`perform_with_deflate` и снимают его
(`set_read_timeout(None)`) сразу после — независимо от исхода — прежде чем
хендшейк-результат уходит наверх через `?`. Долгоживущий пост-хендшейк сокет
остаётся без таймаута, как и было (комментарий на месте уточнён — раньше он
говорил «No read timeout» без оговорки, что это только про фазу ПОСЛЕ
хендшейка).

Regression-тест `ws_handshake_read_times_out_against_a_stalled_server`
(`crates/network/src/lib.rs`): сервер принимает TCP и держит соединение 2с, ни
разу не отвечая `101`; клиентский `TcpStream` получает короткий
`read_timeout=150ms` (не настоящий `FETCH_READ_TIMEOUT=60s` — тест бьёт по
тому же механизму `upgrade::perform`, не ждёт реальный таймаут), `upgrade::perform`
обязан вернуть `Err` быстрее 1с вместо зависания. `cargo test -p lumen-network
--lib` — 2294/2294 зелёных (было 2293 — тест новый), `cargo clippy -p
lumen-network --all-targets -- -D warnings` чист.

**Не тронуто:** основной симптом бага (M4-incremental routing мёртв под
движковым потоком) по-прежнему не тронут — это отдельная, самая крупная из
трёх находок S5, требующая полного перф-протокола (census + A/B), не
точечного фикса. Не проверено, есть ли аналогичный непокрытый хендшейк-путь у
`EventSource`/SSE (`crates/network/src/sse.rs`) — SSE тоже открывает
соединение и держит его долго, но не было включено в скоуп этого среза
(другой сервер-паттерн: чистый HTTP-ответ с `Content-Type:
text/event-stream`, а не отдельный протокол-хендшейк как WS); стоит
проверить в следующем срезе, если аудит сетевых таймаутов продолжится.

## S8 (P3, 2026-09-20) — тот же класс дедлока найден и починен в хендшейке SSE (`EventSource`)

Продолжение аудита S4/S5/S6/S7 («синхронный сетевой вызов без таймаута
блокирует ordered `EngineThread`-задание»). S7 явно оставил непроверенным
`EventSource` (`crates/network/src/sse.rs`) — «стоит проверить, если аудит
сетевых таймаутов продолжится».

`EventSource::open_connection` читал статус-строку и заголовки ответа через
`BufReader::read_line` вообще без таймаута — комментарий на месте («No read
timeout: an EventSource connection is meant to sit idle between server-sent
events far longer than any bounded fetch») верен только для уже установленного
потокового тела, но по ошибке накрывал и сам открывающий HTTP-обмен. Путь до
UI-потока идентичен S7: `_lumen_sse_connect` (`crates/js/src/v8_runtime/install/net.rs:1403`)
вызывает `provider.connect_sse(&url)` **синхронно** прямо из JS-биндинга —
внутри того же ordered `EngineThread`-таска, что и `fetch`/WS. Сервер/прокси,
принявший TCP/TLS-соединение (проходит `CONNECT_TIMEOUT=10s` из S4), но не
приславший ни строки ответа, блокирует `read_line` навсегда — третий
самостоятельный вход в тот же класс (первый — TCP-connect S4, второй —
`EngineThread::query()` S6, третий — WS-хендшейк S7).

**Фикс:** `open_connection` выставляет `FETCH_READ_TIMEOUT` (60s, та же
константа, что WS-хендшейк) на сырой сокет ПЕРЕД чтением статус-строки и
заголовков и снимает его (`set_read_timeout(None)`) сразу после — до того, как
поток становится долгоживущим idle-ридером тела, сохраняемым в
`self.stream`. Логика чтения статус-строки/заголовков вынесена в отдельную
функцию `read_response_head<R: BufRead>` — по тому же соображению, что и
`websocket::upgrade::perform` в S7: чтобы регрессионный тест мог прогнать её
напрямую на сыром `TcpStream` с коротким `read_timeout`, не дожидаясь
настоящих 60с.

Regression-тест `sse_handshake_read_times_out_against_a_stalled_server`
(`crates/network/src/sse.rs`): сервер принимает TCP и держит соединение 2с, ни
разу не отвечая; клиентский `TcpStream` получает `read_timeout=150ms`,
`read_response_head` обязан вернуть `Err` быстрее 1с вместо зависания.
`cargo test -p lumen-network --lib` — 2295/2295 зелёных (было 2294 — тест
новый), `cargo clippy -p lumen-network --all-targets -- -D warnings` чист.

**Не тронуто:** основной симптом бага (M4-incremental routing мёртв под
движковым потоком) по-прежнему не тронут — самая крупная из находок S5,
требует полного перф-протокола (census + A/B), не точечного фикса. Аудит
«синхронный сетевой вызов без собственного таймаута на ordered
`EngineThread`-задаче» пройден по всем путям, которые называли S4/S5/S6/S7 —
TCP-connect, `EngineThread::query()`, WS-хендшейк, SSE-хендшейк; следующая
сессия должна решить, остались ли ещё непроверенные сетевые входы этого
класса (например TLS-хендшейк отдельно от TCP-connect — не проверялся ни
одним срезом), или переходить к самому M4-incremental census'у.

## S9 (P3, 2026-09-20) — тот же класс дедлока найден и починен в самом TLS-хендшейке (не только в WS/SSE HTTP-обмене поверх него)

Продолжение аудита S4/S5/S6/S7/S8. S8 явно оставил открытым вопрос
«TLS-хендшейк отдельно от TCP-connect — не проверялся ни одним срезом».

Прочитан `connect_inner` (`crates/network/src/lib.rs:1154`): `read_timeout`
выставляется на сокет ДО TLS-хендшейка, но только когда вызывающая сторона
передала `Some(...)`. `websocket::mod.rs::connect`/`connect_deflate` и
`sse.rs::connect` (все три места, где вызывается сетевой `connect()`) зовут
его с `read_timeout: None` — намеренно, чтобы уже установленное
долгоживущее соединение могло простаивать сколь угодно (idle между
кадрами/событиями). Побочный эффект: `None` действовал и на сам
`conn.complete_io(&mut tcp)` (rustls-хендшейк) — TLS-версия того же
дефекта, что S7/S8 нашли в HTTP-обмене НАД хендшейком (`upgrade::perform`/
`read_response_head`), только на уровень раньше. Сервер/прокси, принявший
TCP-соединение (проходит `CONNECT_TIMEOUT=10s` из S4), но не завершивший
TLS-рукопожатие (не прислал `ServerHello`, завис после `ClientHello`),
блокировал `complete_io` навсегда — четвёртый самостоятельный вход в тот
же класс «синхронный сетевой вызов без таймаута на ordered
`EngineThread`-задаче» (после TCP-connect S4, `EngineThread::query()` S6,
WS-хендшейк S7, SSE-хендшейк S8).

**Фикс:** `connect_inner` (`lib.rs`) выставляет `FETCH_READ_TIMEOUT` (60s,
та же константа) на сокет перед `conn.complete_io(&mut tcp)`, когда
`read_timeout` — `None`, и снимает его (`set_read_timeout(None)`) сразу
после — независимо от исхода, до того как соединение уходит наверх как
долгоживущее. Когда `read_timeout` — `Some(...)` (обычный `fetch`/XHR),
поведение не меняется: тот таймаут и так уже покрывал хендшейк.

Regression-тест `tls_handshake_read_times_out_against_a_stalled_server`
(`crates/network/src/lib.rs`): реальный `rustls::ClientConnection` против
сервера, который принимает TCP и держит соединение 2с, не отправляя ни
байта; `complete_io` с коротким `read_timeout=150ms` на сокете обязан
вернуть `Err` быстрее 1с вместо зависания — драйвит тот же механизм,
что и фикс, напрямую (не через полный `connect_inner`, которому нужна
живая DNS-резолюция). `cargo test -p lumen-network --lib` — 2296/2296
зелёных (было 2295 — тест новый), `cargo clippy -p lumen-network
--all-targets -- -D warnings` чист.

**Не тронуто:** основной симптом бага (M4-incremental routing мёртв под
движковым потоком) по-прежнему не тронут — самая крупная из находок S5,
требует полного перф-протокола (census + A/B), не точечного фикса. Аудит
«синхронный сетевой вызов без собственного таймаута на ordered
`EngineThread`-задаче» пройден по всем путям, названным S3/S5/S8:
TCP-connect, `EngineThread::query()`, WS-хендшейк, SSE-хендшейк,
TLS-хендшейк. Не проверено: остаются ли ещё сетевые примитивы этого
класса (SOCKS5-хендшейк `socks5_connect` в `connect_inner` — вызывается
ПОСЛЕ `TcpStream::connect_timeout`, до всякого `read_timeout`, не
проверялся). Следующая сессия должна решить, продолжать ли аудит (SOCKS5)
или переходить к самому M4-incremental census'у на подтверждённом стенде
lenta.ru (S5).

## S10 (P3, 2026-09-20) — тот же класс дедлока найден и починен в SOCKS5-хендшейке (последний непроверенный вход из списка S9)

Продолжение аудита S4/S5/S6/S7/S8/S9. S9 явно оставил открытым вопрос:
«остаются ли ещё сетевые примитивы этого класса (SOCKS5-хендшейк
`socks5_connect` в `connect_inner` — вызывается ПОСЛЕ
`TcpStream::connect_timeout`, до всякого `read_timeout`, не проверялся)».

Прочитан `crates/network/src/socks5.rs::socks5_connect`: метод-негоциация,
опциональная RFC 1929 username/password авторизация и CONNECT-запрос/ответ
читаются через несколько `read_exact` подряд на сокете, который приходит из
`connect_inner` (`lib.rs:1179`) сразу после `TcpStream::connect_timeout` —
**вообще без какого-либо read timeout**, ни временного, ни постоянного (в
отличие от TLS/WS/SSE путей S7-S9, где `read_timeout: None` хотя бы
устанавливался явно намеренно для долгоживущего тела). SOCKS5-прокси
(например Tor daemon), который принял TCP-соединение (проходит
`CONNECT_TIMEOUT=10s` из S4), но завис на любой из трёх фаз обмена (не
ответил на приветствие, запросил авторизацию и не проверил её, не ответил на
CONNECT), блокирует `read_exact` навсегда — пятый и последний
самостоятельный вход в тот же класс «синхронный сетевой вызов без
собственного таймаута на ordered `EngineThread`-задаче» (после TCP-connect
S4, `EngineThread::query()` S6, WS-хендшейк S7, SSE-хендшейк S8,
TLS-хендшейк S9).

**Фикс:** `connect_inner` (`lib.rs`, SOCKS5-ветка) выставляет
`FETCH_READ_TIMEOUT` (60s, та же константа, что и остальные четыре фикса) на
`proxy_tcp` ПЕРЕД вызовом `socks5::socks5_connect` и снимает его
(`set_read_timeout(None)`) сразу после успешного туннелирования — до того,
как дальше по функции `connect_inner` применяется вызывающий `read_timeout`
(для обычного `fetch`/XHR) или собственный `FETCH_READ_TIMEOUT` на TLS-
хендшейк (S9, для WS/SSE). `socks5_connect` сам не тронут — таймаут целиком
снаружи, на уровне сокета, как и во всех предыдущих срезах этого аудита.

Regression-тест `socks5_handshake_read_times_out_against_a_stalled_server`
(`crates/network/src/socks5.rs`): сервер принимает TCP и держит соединение
2с, не отвечая на приветствие; клиентский `TcpStream` получает
`read_timeout=150ms` (тест бьёт по `socks5_connect` напрямую, не через
полный `connect_inner`, которому нужен настоящий SOCKS5-прокси), функция
обязана вернуть `Err` быстрее 1с вместо зависания. `cargo test -p
lumen-network --lib` — 2297/2297 зелёных (было 2296 — тест новый), `cargo
clippy -p lumen-network --all-targets -- -D warnings` чист.

**Итог аудита S4-S10:** все известные синхронные сетевые примитивы на
ordered `EngineThread`-задаче теперь ограничены по времени: TCP-connect,
`EngineThread::query()`, WS-хендшейк, SSE-хендшейк, TLS-хендшейк,
SOCKS5-хендшейк. **Не тронуто:** основной симптом бага (M4-incremental
routing мёртв под движковым потоком) — самая крупная из находок S5, требует
полного перф-протокола (census + A/B на живом стенде, S5 подтвердил
`lenta.ru` как воспроизводимый и стабильный). Следующая сессия должна
перейти к собственно M4-incremental census'у — аудит таймаутов сетевых
примитивов этого класса исчерпан, дальнейшая работа по этому багу — census,
не поиск новых точек дедлока.

## S11 (P3, 2026-09-20) — первый успешный M4-incremental census: подтверждён и количественно измерен основной симптом, попутно найден и починен новый блокер census'а (`DEFAULT_TIMEOUT` короче `FETCH_READ_TIMEOUT`)

Продолжение с того места, на котором остановился S10 («аудит таймаутов
сетевых примитивов исчерпан, следующая сессия должна перейти к собственно
M4-incremental census'у»). Запущен `scripts/bug935_raf_relayout_census.py
https://lenta.ru --settle-s 20 --ticks 20` на подтверждённом S5 стенде.

**Блокер census'а (найден и починен первым):** прогон падал с `Scroll
error: automation command timed out` посреди 20 scroll-тиков. Причина —
`crates/driver/src/live_session.rs::DEFAULT_TIMEOUT = 30s` (таймаут
одного automation-round-trip'а) меньше `lumen_network`'s
`FETCH_READ_TIMEOUT = 60s` (S4-S10 ограничили этим значением все сетевые
примитивы на том же ordered `EngineThread`-FIFO). Automation-команда
(`scroll`), поставленная в очередь позади синхронного сетевого вызова,
который легитимно (не бесконечно, но и не мгновенно) занимает до 60с,
физически не может успеть за 30с — не деадлок, а бюджетное несоответствие
между двумя независимо выбранными константами одного и того же тракта.
Подтверждено пробой `bug935_hang_probe.py --probe-timeout-s 70`: MCP
ответил за 13.5с на «тишину» — канал жив, просто медленный.

**Фикс:** `DEFAULT_TIMEOUT` поднят с 30с до 65с (`live_session.rs`) — с
запасом над `FETCH_READ_TIMEOUT=60s`, задокументирована связь двух
констант в разных крейтах (нет прямой ссылки, `FETCH_READ_TIMEOUT`
приватна в `lumen-network`, значение продублировано в комментарии).
`cargo clippy -p lumen-driver --all-targets -- -D warnings` чист, `cargo
test -p lumen-driver --lib` — 83/83 зелёных, без регрессий.

**Собственно census (после фикса):** первый успешный прогон, дошедший до
конца всех 20 scroll-тиков на `lenta.ru`.

```
relayout до document_ready:     0
relayout за 20с settle (без scroll): 1  (312мс, dl=1583 styled=1144)
relayout за 20 scroll-тиков:    3  (419-1812мс, все off-thread ПОЛНЫЕ —
                                    dl/styled не растут между ними, т.е.
                                    не content-visibility, тот же класс,
                                    что открывающий репорт бага)

scroll RTT ms: min=388.7 avg=18030.2 max=59154.5
все значения (20 тиков по порядку):
15699, 33271, 30964, 48101, 2481, 389, 593, 32461, 59155, 14288,
15047, 10658, 13571, 11728, 11901, 11979, 11688, 12055, 12481, 12096
```

Это первое количественное подтверждение основного симптома бага на
живом сайте с этим срезом кода (S4-S10 не трогали M4-роутинг вовсе):
`scroll` RTT держится в диапазоне **10-33с** для 17 из 20 тиков (не
850-1400мс, как в исходном 2026-09-01 замере — сайт/трафик другие, но
порядок хуже, не лучше), с двумя выбросами до 48с и 59с. Три полных
off-thread relayout'а за 20 тиков — не «один на каждый тик», значит
`EngineThread` coalescing (S5's «сообщение `submit`'а коалесцирует
промежуточные заявки») действительно работает и не даёт очереди расти
неограниченно, но даже 3 полных пересчёта каскада достаточно, чтобы почти
весь 20-тиковый прогон (17/20 RTT выше 10с) провёл в состоянии «движковый
поток занят», подтверждая диагноз «Корня» раздела buга буквально на
цифрах, не только по логике кода.

**Не сделано:** сама правка M4-роутинга (смена порядка `||`-веток в
`relayout_raf_dirty`/`relayout_raf_dirty_readback`) по-прежнему не
внесена — census теперь ЕСТЬ (что и было условием для перехода к правке
по `docs/perf-method.md`), но интерливед A/B «до/после» смены порядка
на этом же стенде — отдельный следующий шаг, не входящий в этот срез
(его размер и риск сопоставимы с отдельным полноценным срезом, а не
довеском к уже большому S11). Следующая сессия должна: (1) внести
смену порядка веток, (2) прогнать этот же census-скрипт на этой же
странице интерливед несколько раз до/после, сравнить RTT/relayout-счётчик,
(3) отдельно измерить долю пропущенных кадров/задержку ввода на
UI-потоке от синхронного `try_relayout_raf_incremental` — именно тот
компромисс, который «Предполагаемый фикс» выше называет ценой перехода.

## S12 (P3, 2026-09-20) — смена порядка веток измерена живьём и ОТКАЧЕНА: подтверждённая регрессия, найден настоящий блокер M4-fast-path

Продолжение с того места, на котором остановился S11 («следующая сессия должна
(1) внести смену порядка веток, (2) прогнать census interleaved, (3) измерить
долю пропущенных кадров»). Сначала добавлено логирование под
`LUMEN_FRAME_LOG=1` в `try_relayout_raf_incremental` (`relayout.rs`) — до этого
среза путь не печатал вообще ничего, так что census S11 не мог увидеть его
собственную стоимость (`docs/perf-method.md` §2 «профилируй ТОТ путь, который
меняешь»). Новая строка: `[engine] relayout {ms}ms (incremental, on-thread)
dl=... styled=... restyle={0|1}` — последнее поле отличает дешёвую
cascade-skip ветку (`page_prev_cascade_styles` был `Some`) от дорогой полной
`relayout_page_incremental` без cascade-skip. Census-скрипт
(`scripts/bug935_raf_relayout_census.py`) расширен: `RELAYOUT_RE` дополнен
`ANY_RELAYOUT_RE`, разбивка по типу (`off-thread` / `incremental, on-thread`)
вместо одной колонки.

Затем внесена сама правка (1) — `||`-порядок в `relayout_raf_dirty`/
`relayout_raf_dirty_readback` поменян местами, `try_relayout_raf_incremental`
пробуется первым. Прогнан census (`lenta.ru`, `--settle-s 20 --ticks 20`) на
собранном `dev-release` этой ветки.

**Результат — регрессия, не выигрыш:** все 8 инкрементальных relayout'ов за
прогон вышли `restyle=0` (0 из 8 — cascade-skip путь ни разу не сработал),
т.е. каждый тик шёл через дорогую ветку `relayout_page_incremental` (без
cascade-skip) **синхронно на UI-потоке** вместо off-thread. Стоимость по
логу: 650–850мс на тик (6 записей), и один провал в 7293.78мс +
7363.17мс подряд (`[frame] total 14295.82ms`, `js 12923.45`) — это не шум
станка, это НАСТОЯЩАЯ блокировка UI-потока на 14+с, вызванная именно этой
правкой (при исходном порядке та же ситуация ушла бы off-thread и не
блокировала бы MCP/automation-канал). Census не дошёл до конца 20 тиков —
упал на очередном `scroll` с `automation command timed out`, тем же классом
симптома, который S3-S10 чинили для СЕТЕВЫХ вызовов на ordered
`EngineThread`-FIFO, только на этот раз источник другой: `scroll`
обрабатывается на самом UI-потоке (`winit`'s `about_to_wait`/`RedrawRequested`),
который эта правка теперь занимает синхронным layout'ом — лог не даёт точно
сказать, какой из восьми тиков не уложился в `DEFAULT_TIMEOUT=65s`, но
направление однозначно: on-thread путь **хуже** исходного off-thread для
этого сайта, не лучше.

**Корень регрессии — не "стоимость try_relayout_raf_incremental" сама по
себе, а то, что дешёвая часть его логики недостижима с этого call site:**
cascade-skip ветка (`fresh_cascade_styles = Some(...)`) требует
`self.page_prev_cascade_styles` быть `Some` НА ВХОДЕ в
`try_relayout_raf_incremental` (`relayout.rs:315`, `.take()`). Но это поле
устанавливается в `Some` ровно в одном месте кода — в конце самой этой
cascade-skip ветки (после её успешного прохода), и обнуляется в `None` во
всех остальных производителях: `relayout()` (полный синхронный),
`readback_relayout_job`, `poll_engine_commit`'s `apply_relayout_result`
(`relayout.rs:789`, доккомментарий там прямо говорит: «forcing the next
incremental attempt onto the safe full-cascade-plus-graft fallback for one
cycle»). До этого среза комментарий читался как обещание, что ПОСЛЕ одного
цикла на полной ветке следующий цикл получит `Some` и пойдёт по дешёвому
пути — но `else`-ветка самого `try_relayout_raf_incremental`
(`relayout_page_incremental`, не `_restyle`) тоже не заполняет
`fresh_cascade_styles` (она статически `None` для этой ветки, `relayout.rs`
строка с `(dl, lb, None)`), поэтому цепочка «полная попытка → следующая
дешёвая» никогда не замыкается через ЭТОТ путь: если `page_prev_cascade_styles`
уже `None` на входе (обычный случай — предыдущий цикл шёл через `relayout()`
или через `submit_relayout_job`/`readback_relayout_job`, что при старом
порядке было ВСЕГДА, а при новом порядке — с первого же тика, раз
предыдущий тоже не восстановил кэш), он остаётся `None` бесконечно, и
`try_relayout_raf_incremental` навсегда застревает в дорогой ветке.
Другими словами: без дополнительной правки, заполняющей
`page_prev_cascade_styles` из non-restyle-ветки (или без отдельного
источника, который умеет его сеять), «дешёвый M4-путь» в этом коде **вообще
недостижим** при input вида «непрерывный rAF+DOM-цикл», единственном
сценарии, ради которого баг заведён — 0/8 в этом прогоне, не выборка, а
структурная невозможность по коду.

**Действие:** правка (1) отменена (`git` в этом же срезе — обе функции
вернули исходный порядок `submit_relayout_job`/`readback_relayout_job`
первыми), логирование в `try_relayout_raf_incremental` и расширение
census-скрипта **оставлены** (безвредны под `LUMEN_FRAME_LOG`, и это они
обнаружили находку — без них регрессия осталась бы невидимой числом, только
«страница подвисла один раз» на глаз). `cargo clippy -p lumen-shell
--all-targets -- -D warnings` чист, `cargo test -p lumen-shell --bins --
relayout` — 10/10 зелёных на откаченном коде.

**Не сделано / следующий срез:** (2) и (3) из плана S11 больше не имеют
смысла в исходной формулировке — интерливед A/B "старый порядок vs новый
порядок" при 0/8 cascade-skip сравнивал бы "off-thread всегда" с "on-thread
всегда чуть дороже + иногда 10x дороже", результат предсказан этим срезом
без полного протокола. Настоящий следующий шаг — НЕ повторная попытка
смены порядка, а починка самого условия входа в cascade-skip: либо (а)
научить non-restyle-ветку `try_relayout_raf_incremental`
(`relayout_page_incremental`) тоже возвращать/строить `CascadeStyles` для
следующего цикла (структурная правка алгоритма, не однострочная), либо (б)
завести отдельный путь заполнения `page_prev_cascade_styles` из
`relayout()`/`readback_relayout_job`, если это вообще безопасно (эти пути
не гарантируют то же самое множество узлов, что ждёт cascade-skip —
нужна отдельная проверка инварианта, не точечная правка). Только после
одного из этих двух — есть смысл возвращаться к смене порядка `||`-веток
и к пунктам (2)/(3) плана S11.

## S13 (P3, 2026-09-20) — починен корень регрессии S12: не-restyle ветка `try_relayout_raf_incremental` теперь тоже сеет `page_prev_cascade_styles`

Продолжение с того места, где остановился S12 («настоящий следующий шаг — НЕ
повторная попытка смены порядка, а починка самого условия входа в
cascade-skip»). S12 нашёл два возможных направления: (а) научить не-restyle
ветку `try_relayout_raf_incremental` (`relayout_page_incremental`) тоже
возвращать/строить `CascadeStyles` для следующего цикла, либо (б) отдельный
путь заполнения из `relayout()`/`readback_relayout_job`. Взято (а) — оно
безопаснее: не-restyle ветка и так уже гоняет полный каскад внутри
`layout_mutation_incremental` (`layout_streaming_incremental` →
`precompute_counters`), просто выбрасывала результат.

**Находка при чтении:** `grep` по всему дереву `page_prev_cascade_styles`
показал, что ДО этого среза поле не устанавливалось в `Some` вообще нигде в
кодовой базе, кроме самой restyle-ветки `try_relayout_raf_incremental` (и то
только после того, как restyle-ветка уже успешно исполнилась). Раз вход в
restyle-ветку сам требует, чтобы поле уже было `Some`, а единственный
производитель `Some` — сама эта ветка, цепочка не может начаться ни разу ни
при каком порядке вызовов и ни при какой сборке (не только под
`LUMEN_ENGINE_THREAD=1`, как формулировал корень бага раньше) — дешёвый путь
был мёртв структурно, с первого коммита BUG-341 S7. S12's эксперимент со сменой
`||`-порядка ничего не мог изменить именно поэтому: 0/8 `restyle=1` — не
статистическая случайность конкретного прогона, а гарантированный исход при
любом количестве тиков.

**Фикс** (три места, `crates/engine/layout/src/box_tree/entry.rs` +
`crates/shell/src/relayout.rs`):

1. `layout_streaming_incremental_with_counters` — рядом с существующей
   `layout_streaming_incremental` (тонкая обёртка над ней, отбрасывающая
   `CounterMap`, чтобы не трогать её единственного другого вызывающего —
   `page_load.rs`'s стриминговый путь загрузки, которому кэш не нужен).
2. `layout_mutation_incremental_with_counters` — аналогично для
   `layout_mutation_incremental`.
3. `relayout_page_incremental`/`compute_layout_incremental` (`relayout.rs`)
   теперь возвращают `CounterMap` третьим элементом кортежа (единственный
   вызывающий — `try_relayout_raf_incremental`, сигнатуру можно было менять
   без обёртки).
4. `try_relayout_raf_incremental`: `fresh_cascade_styles` для обеих веток
   (restyle и full) теперь именно `CascadeStyles` (не `Option`), и
   `self.page_prev_cascade_styles = Some(fresh_cascade_styles)` пишется
   безусловно после `apply_relayout_result`, а не только для restyle-ветки.
   `self.page_prev_interactive = new_interactive` — туда же (раньше тоже
   писалось только restyle-веткой; без синхронного обновления после
   full-ветки следующий цикл сравнивал бы новое hover/focus/active состояние
   не с тем, что реально было на входе в last full pass).
   Различение «какая ветка сработала» для отладочного лога
   (`restyle={0|1}`) вынесено в отдельный `bool used_restyle`, заданный по
   месту в каждой ветке — раньше он читался как `fresh_cascade_styles.is_some()`,
   что после этой правки было бы всегда `true` и потеряло бы смысл.

**Почему это корректно (не просто компилируется):** `CascadeStyles`,
которую строит full-cascade ветка (`layout_mutation_incremental_with_counters`
→ `layout_streaming_incremental_with_counters` → `precompute_counters`), —
самый обычный полный каскад того же вида, что и самый первый проход страницы
(`layout_measured_hyp_with_counters`, тот же путь, который сегодня сеет
кэш для потомков `layout_mutation_incremental_restyle` при инициализации
других циклов, например chrome-хоста, BUG-341 S6/S7). Контракт restyle-пути
(`delta.prev_styles` должен быть «`CounterMap::styles()`, который тот же
документ произвёл на прошлом цикле») ничего не говорит про то, каким путём
тот прошлый цикл был получен — только что он полный и актуальный. Full-ветка
даёт именно это: `precompute_counters` не пропускает ни одного узла (в
отличие от restyle-пути, который сознательно скипает чистые поддеревья), so
её результат — даже более надёжный источник кэша, не менее. Пройденный
пасс-ординал (`CascadeStyles::pass`) при этом стартует с 0 у каждого
свежепостроенного `CounterMap` (`with_capacity`) — ровно то же значение,
что и у самого первого прохода страницы, так что цепочка `pass 0 (full) →
pass 1 (restyle, reuse valid) → …` работает без специального случая.

**Тест:** `mutation_incremental_with_counters_seeds_a_cache_matching_a_fresh_full_cascade`
(`crates/engine/layout/src/incremental.rs`) — сравнивает
`layout_mutation_incremental_with_counters`'s `CounterMap::into_styles()` с
`CounterMap`, которую даёт `layout_measured_hyp_with_counters` над тем же
итоговым DOM с нуля; `CascadeStyles::eq` сравнивает результат каскада, не
пасс-ординалы (как и differential-тесты BUG-341), так что это именно
проверка «то же содержимое, независимо от того, каким путём получено».
`cargo test -p lumen-layout --lib -- incremental` — 52/52 зелёных (включая
новый), `cargo clippy -p lumen-layout --all-targets -- -D warnings` и
`cargo clippy -p lumen-shell --all-targets --features v8 -- -D warnings` —
чистые.

**Эффект под дефолтной сборкой:** по-прежнему нулевой сам по себе —
`try_relayout_raf_incremental` вызывается из `relayout_raf_dirty` только
когда `submit_relayout_job()` вернул `false`, а под `LUMEN_ENGINE_THREAD=1`
(дефолт с ADR-023) она возвращает `true` почти всегда — см. «Корень» раздела
выше, этот срез его не трогал. Значение этого среза — убрать структурный
блокер, который делал ЛЮБУЮ попытку задействовать дешёвый путь (в том числе
S12's смену порядка) заведомо провальной независимо от порядка вызовов;
S12's «650-850мс на тик, 0/8 restyle» был прямым следствием именно этого
отсутствия, не самой смены порядка.

**Не сделано / следующий срез:** сама смена `||`-порядка в
`relayout_raf_dirty`/`relayout_raf_dirty_readback` по-прежнему не внесена —
теперь у неё есть шанс не повторить регрессию S12 (после одного full-цикла
кэш будет `Some`, второй тик того же rAF-цикла уже может пойти по
cascade-skip), но это нужно измерить заново тем же протоколом (census +
интерливед A/B на `lenta.ru`, S5/S11's подтверждённый стенд), не
предполагать. Следующая сессия должна повторить S11/S12's эксперимент на
этом фиксе: (1) внести смену порядка, (2) прогнать
`scripts/bug935_raf_relayout_census.py` на `lenta.ru`, (3) проверить долю
`restyle=1` по логу `[engine] relayout … (incremental, on-thread) … restyle=…`
и RTT `scroll` — если `restyle=1` теперь ненулевая доля тиков и RTT не хуже
исходного off-thread baseline (S11: 10-33с на 17/20 тиков), это первое
реальное измерение выигрыша M4; если снова 0/N — искать следующий блокер тем
же методом, а не повторять S12 вслепую.

## S14 (P3, 2026-09-20) — смена порядка веток повторена под S13-фиксом: `restyle=1` наконец достигается, но census снова не дошёл до конца — найден новый кандидат-блокер (contention на `document.lock()` с движковым потоком)

Продолжение с того места, где остановился S13 («следующая сессия должна
повторить S11/S12's эксперимент на этом фиксе»). Сначала расширен
`scripts/bug935_raf_relayout_census.py`: `ANY_RELAYOUT_RE` теперь опционально
разбирает поле `restyle=` (S12/S13 добавили его в лог, но census его не читал),
разбивка по `incremental, on-thread` печатает долю `restyle=1`.

Затем внесена сама смена `||`-порядка (`try_relayout_raf_incremental` первым в
обеих функциях, `relayout_raf_dirty`/`relayout_raf_dirty_readback`). Прогнан
census (`lenta.ru`, `--settle-s 20 --ticks 20`) на собранном `dev-release`
этой ветки.

**Хорошая новость — S13-фикс работает:** первый тик пошёл по дорогой
non-restyle ветке (`restyle=0`, 251.92мс — сеет кэш), все следующие пять тиков
подряд — `restyle=1` (121–173мс каждый), т.е. cascade-skip путь теперь
реально достижим на живом rAF+DOM-цикле, чего не было ни разу за S12 (0/8).

**Но census снова не дошёл до конца — тот же класс отказа, что S12:** после
шести дешёвых тиков подряд — два `[engine] relayout` по **7047.49мс** и
**6752.74мс**, ОБА с `restyle=1`, после чего `automation command timed out`
(`DEFAULT_TIMEOUT=65s` исчерпан кумулятивно за несколько `scroll`-вызовов, тем
же способом, что S12 остановил прогон). Резкая аномалия: `[profile]`-разбивка,
напечатанная непосредственно перед строкой `relayout`, показывает, что все
внутренние стадии cascade-skip ветки (`layout_mutation_incremental_restyle` +
`build_flat_tree`/`build_shadow_sheets`/`precompute_counters`/`build_box`/
`post_build_tree_walks`/`graft_geometry`/`lay_out`/`post_layout_passes`) в
сумме дают **<1мс** — то есть измеренные 7047/6752мс тратятся НЕ на сам layout
(та часть функции, что профилируется), а на что-то ДО начала профилируемых
стадий внутри `try_relayout_raf_incremental` (после `incr_t0`, до
`layout_mutation_incremental_restyle`): кандидаты — `refresh_dynamic_css()`,
`src.document.lock().unwrap()` (`relayout.rs:333`), или сами вызовы
`restyle_state_index`/`restyle_root_set_for_state_change`.

Совпадение по времени показательно: та же `[frame]`-строка (тот самый
`scroll`, что подвис) отчиталась `total 13372.82ms` с `js 12059.20` —
двенадцать секунд внутри этого кадра ушло на JS. Под дефолтной сборкой
(движковый поток, ADR-023) JS исполняется НЕ на UI-потоке (`fire_raf_turn_async`
шлёт `run_animation_frame` в `EngineThread` асинхронной задачей), а
`try_relayout_raf_incremental` теперь (после смены порядка) исполняется НА
UI-потоке и берёт `src.document.lock()` для чтения дерева перед восстроением
root-set. Правдоподобная гипотеза (не подтверждена инструментально в этом
срезе — не измерялось время до/после `lock()` отдельно): пока движковый поток
удерживает `Document`-мьютекс во время долгого (12с — вероятно, тяжёлый
рекламный/аналитический скрипт, судя по `easylist`-блокировкам в логе вокруг
этого места) JS-прохода, UI-поточный `try_relayout_raf_incremental` блокируется
на этом же мьютексе на весь оставшийся срок хода — ровно то время, что и
наблюдаемые 7047/6752мс. Если верно, это плата, которую off-thread
`submit_relayout_job` НИКОГДА не перекладывает на UI-поток напрямую (он тоже
конкурирует за мьютекс, но делает это на СВОЁМ, не на UI-потоке, поэтому
automation-канал не блокируется тем же способом).

**Действие:** правка (смена `||`-порядка) отменена (обе функции вернули
исходный порядок), доккомментарии дополнены итогом этого среза. Расширение
census-скрипта (`restyle=` разбор) **оставлено** — безвредно и уже показало
свою ценность (без него S13-фикс остался бы неизмеренным числом). `cargo
clippy -p lumen-shell --all-targets --features v8 -- -D warnings` чист,
`cargo test -p lumen-shell --bins --features v8 -- relayout` — 10/10 зелёных
на откаченном коде.

**Не сделано / следующий срез:** гипотеза «UI-поточный `try_relayout_raf_incremental`
блокируется на `document.lock()` контенцией с движковым JS-потоком» не
подтверждена инструментально — нужен отдельный таймер ВНУТРИ
`try_relayout_raf_incremental` (до и после `src.document.lock()`, до и после
`refresh_dynamic_css()`), чтобы локализовать, какая именно операция съедает
7с при <1мс профилируемого layout. Если гипотеза подтвердится, смена
`||`-порядка структурно небезопасна без дополнительной защиты (например,
`try_lock()` с быстрым откатом на `submit_relayout_job`, если мьютекс занят) —
это не однострочная правка, а отдельная архитектурная развилка. Основной
симптом бага (M4-incremental routing не работает под дефолтной сборкой)
по-прежнему открыт.

## S15 (P3, 2026-09-20) — S14's `document.lock()` hypothesis instrumented and REFUTED; the real cost is a synchronous engine-thread round-trip in `apply_relayout_result` that S14's swap experiment exposed on the UI thread

Продолжение с того места, где остановился S14 («гипотеза не подтверждена
инструментально — нужен отдельный таймер ВНУТРИ `try_relayout_raf_incremental`,
до и после `src.document.lock()`, до и после `refresh_dynamic_css()`»).

**Инструментирование (оставлено под `LUMEN_FRAME_LOG=1`, безвредно):**
таймер вокруг `src.document.lock()` в restyle-ветке `try_relayout_raf_incremental`
(`lock_wait_ms`) и вокруг построения `dirty_roots`/индексов (`dirty_roots_ms`),
добавлены в итоговую строку `[engine] relayout … (incremental, on-thread)`.
Затем повторён S14's эксперимент (`||`-порядок временно свопнут, сборка
`dev-release`, census на `lenta.ru`).

**S14's гипотеза опровергнута числами:** `lock_wait_ms=0.00` на **всех**
измеренных тиках этой сессии, включая outlier в 5880.54мс общего `incr_ms` —
ожидание на `document.lock()` в этой конкретной точке не является причиной
задержки. S14 предположил контенцию по мьютексу, не измерив её напрямую;
измерение показывает, что мьютекс здесь не виноват.

**Найден второй `document.lock()`, отдельный от первого:** `compute_layout_incremental_restyle`
(`relayout.rs:1414`, вызывается ИЗ `relayout_page_incremental_restyle` уже
ПОСЛЕ того, как вызывающий снял свой лок на строке 365) берёт **свой
собственный** лок на тот же `Mutex<Document>`. Добавлен таймер
(`[engine] restyle-wrapper …ms total: font_parse=…ms lock2_wait=…ms
paint_ordered=…ms`) — в одном прогоне `lock2_wait` доходил до 857.03мс, что
показывает реальную, но вторичную контенцию (не главный источник — см. ниже).

**Настоящий доминирующий источник — `apply_relayout_result`, не сам layout:**
таймер вокруг всего вызова `self.apply_relayout_result(...)` в
`try_relayout_raf_incremental` (`apply_ms=`) показал, что эта функция съедает
**почти весь** `incr_ms` на КАЖДОМ тике, не только на outlier'ах:
нормальные тики — `apply_ms` 300-450мс при `incr_ms` 320-460мс (restyle-wrapper
сам — 10-12мс); outlier — `apply_ms=6766.39мс` при `incr_ms=7635.48мс`.
Чтение `apply_relayout_result` (`relayout.rs:889-936`) нашло механизм:
секция JS-observer push (`self.js_present` gate) собирает rects/client
rects/hit-test-tree/computed styles/pseudo styles/custom properties/
stylesheet/viewport/scroll-states и одним пакетом зовёт
`route_query_js(self.engine_thread.as_ref(), self.js_ctx.as_ref(), move |js| {…})` —
**синхронный ordered round-trip на движковый поток** (тот же механизм,
`EngineThread::query()`, что S6 ограничил `QUERY_TIMEOUT=5s`), исполняющий
там `deliver_layout_observers`/`deliver_media_query_changes`/
`deliver_lazy_images` и прочий JS-пуш.

**Почему это объясняет и S12, и S14 разом:** под исходным (не свопнутым)
`||`-порядком `apply_relayout_result` тоже вызывается — из
`submit_relayout_job`'s асинхронной engine-thread задачи — но там она уже
исполняется НА движковом потоке (сам себе round-trip не нужен, или он
локальный) и не блокирует UI-поток. Смена `||`-порядка (S12, повторно S14)
переносит `try_relayout_raf_incremental` целиком на UI-поток, включая эту
секцию — и теперь UI-поток синхронно ждёт `route_query_js`'а ответа от ТОГО ЖЕ
движкового потока, чья ordered FIFO-очередь может быть занята долгим
синхронным `fetch`/сетевым вызовом (тот же класс, что S4-S10 чинили с
таймаутами) — вплоть до `QUERY_TIMEOUT=5s` на один запрос, что и даёт
наблюдаемые 5.9-7.6с. Это не контенция на мьютексе (S14's гипотеза) и не
дороговизна самого layout'а (S12/S13 уже показали <1мс на профилируемые
стадии) — это структурная разница между «async-safe off-thread» и
«on-thread» путём, которую «Предполагаемый фикс» в начале файла предсказывал
абстрактно («переключение сделает rAF-DOM-мутации ЧАСТИЧНО UI-блокирующими»),
теперь локализована до конкретного вызова с конкретным механизмом.

**Действие:** `||`-порядок снова откачен (обе функции вернули исходный
порядок). Все три новых таймера (`lock_wait_ms`/`dirty_roots_ms` в
`try_relayout_raf_incremental`, `restyle-wrapper` в
`compute_layout_incremental_restyle`, `apply_ms` вокруг
`apply_relayout_result`) оставлены под `LUMEN_FRAME_LOG=1` — без них это
открытие осталось бы недоступным следующему срезу. `cargo clippy -p
lumen-shell --all-targets --features v8 -- -D warnings` чист, `cargo test -p
lumen-shell --bins --features v8 -- relayout` — 10/10 зелёных на откаченном
коде.

**Не сделано / следующий срез:** основной симптом бага (M4-incremental
routing не работает под дефолтной сборкой) по-прежнему открыт — но теперь
понятно, ПОЧЕМУ прямая смена `||`-порядка не годится: `apply_relayout_result`
должна либо (а) остаться асинхронной даже когда сам layout выполнен на
UI-потоке (разделить «синхронный layout» и «асинхронный JS-push» вместо
текущего одного вызова, вынести `route_query_js`-секцию в отдельную
engine-thread задачу, не дожидаясь её на UI-потоке), либо (б) для этого
конкретного JS-push использовать неблокирующий/latest-wins механизм вместо
ordered `query()`, раз результат (`lazy_reqs`) уже допускает `unwrap_or_default()`
при таймауте. Оба варианта — архитектурная правка, не однострочная. Вторичная
находка (второй `document.lock()` в `compute_layout_incremental_restyle`,
857мс в одном прогоне) тоже не устранена — отдельный, менее приоритетный
источник задержки на том же пути.

## S16 (P3, 2026-09-20) — doc-comment drift fixed: `relayout_raf_dirty`'s warning still named S14's refuted lock-contention guess, not S15's confirmed root cause

Продолжение с того места, где остановился S15 («основной симптом бага
по-прежнему открыт — понятно, ПОЧЕМУ прямая смена `\|\|`-порядка не годится»).
Прочитан doc-comment [`Lumen::relayout_raf_dirty`](../crates/shell/src/relayout.rs)
(строки 456-470 на момент этого среза): он всё ещё пересказывал S14's
гипотезу «блокируется на `src.document.lock()` contention с движковым
потоком» как ДЕЙСТВУЮЩЕЕ предупреждение — то есть следующая сессия,
читающая только этот комментарий (не весь файл бага), пошла бы проверять
уже опровергнутую S15 гипотезу заново, а не к настоящему найденному
источнику (`apply_relayout_result`'s `route_query_js`).

**Правка (только doc-comment, `crates/shell/src/relayout.rs`):**
предупреждение переписано на S15's подтверждённый механизм — оба
пути (`poll_engine_commit` и `try_relayout_raf_incremental`) зовут один и
тот же `apply_relayout_result` → `route_query_js`, разница только в МОМЕНТЕ
вызова: `poll_engine_commit` берёт готовый `EngineCommit` (`take_committed`),
то есть к этому моменту предыдущий долгий пункт FIFO — rAF JS-ход/синхронный
`fetch` — уже отработал, и запрос попадает в пустую очередь; этот же
(on-thread incremental) путь зовёт `route_query_js` СРАЗУ после того самого
rAF JS-хода, ещё выполняющегося, — и попадает в очередь ЗА ним. Указано,
что не стоит повторять смену `\|\|`-порядка без предварительного разделения
JS-push и синхронного layout'а (S15's варианты (а)/(б)).

**Проверено:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` — чист (5 минут, полная пересборка зависимостей после смены
ветки — сам крейт компилировался ~секунды, правка чисто документационная).
Код поведения не менял, тестов не запускал целенаправленно — правка не
затрагивает исполняемый путь.

**Не сделано:** сама архитектурная правка (S15's варианты (а)/(б) — вынести
`route_query_js`-секцию `apply_relayout_result` из синхронного пути, чтобы
смена `\|\|`-порядка стала безопасной) по-прежнему не сделана — она
требует полного перф-протокола (census + интерливед A/B на `lenta.ru`,
docs/perf-method.md), масштаб сравним с BUG-341, не входит в этот срез.
Вторичная находка S15 (второй `document.lock()` в
`compute_layout_incremental_restyle`, 857мс в одном прогоне) тоже не
устранена. Следующая сессия должна взять один из двух вариантов S15 и
провести измерение по протоколу — не пытаться сразу менять `\|\|`-порядок
вслепую (S12/S14 уже дважды показали, зачем именно это не работает).

## S17 (P3, 2026-09-20) — S15's option (б) implemented: the JS-observer push in `apply_relayout_result` is now decoupled from the synchronous on-thread incremental path

Продолжение с того места, где остановился S16 («следующая сессия должна взять
один из двух вариантов S15 и провести измерение по протоколу»). Взято S15's
вариант (б) — не переписывать сам `route_query_js`-механизм для всех
вызывающих, а дать конкретному проблемному call site (`try_relayout_raf_incremental`)
неблокирующий путь, раз его результат (`lazy_reqs`) и так уже трактуется как
опциональный (`unwrap_or_default()`).

**Фикс** (`crates/shell/src/relayout.rs`, `crates/shell/src/lumen/state.rs`,
`crates/shell/src/window_mode.rs`):

1. `apply_relayout_result` получил новый параметр `defer_js_push: bool`. Три
   существующих вызывающих (`relayout()`, `readback_relayout_job`,
   `poll_engine_commit`) передают `false` — байт-идентичное поведение, тот же
   блокирующий `route_query_js`, что и раньше. Четвёртый — `try_relayout_raf_incremental` —
   теперь передаёт `true`.
2. Под `defer_js_push == true` JS-observer push (rects/styles/`deliver_layout_observers`/
   `deliver_media_query_changes`/`deliver_lazy_images`/scroll-states — тот же
   набор вызовов, что и раньше) уходит через уже существующий fire-and-forget
   роутер [`route_task_js`] (`engine_bridge.rs`, использовался раньше только
   для `eval_js`/`tick_timers` и подобных void-действий) вместо блокирующего
   `route_query_js`. Вызывающий (`try_relayout_raf_incremental`, на UI-потоке)
   больше не ждёт ответа от движкового потока в этой точке — снимается ровно
   тот блокирующий раунд-трип, который S15 нашёл источником 5.9-7.6с
   зависаний при свопнутом `||`-порядке.
3. `take_lazy_image_requests()`'s результат недоступен вызывающему синхронно
   в этой ветке — добавлено новое поле `Lumen::pending_lazy_image_reqs`
   (`Arc<Mutex<Vec<(u32, String)>>>`, тот же класс хендова, что
   `dom_dirty_flag`/`raf_pending_flag`, только очередь, а не булев флаг):
   fire-and-forget задача складывает в него найденные lazy-image-запросы,
   а `apply_relayout_result` **безусловно** (для всех четырёх производителей,
   не только для деferred-ветки) дренирует эту очередь перед вызовом
   `fetch_and_register_lazy_images` — так что запрос, отложенный одним
   тиком, подхватывается следующим relayout'ом любого рода, максимум с
   задержкой в один цикл.

**Эффект под дефолтной сборкой: нулевой сам по себе, как и S13.**
`try_relayout_raf_incremental` по-прежнему не вызывается под
`LUMEN_ENGINE_THREAD=1` (дефолт, ADR-023) — `relayout_raf_dirty`'s `||`-порядок
не менялся в этом срезе, `submit_relayout_job()` всё ещё выигрывает первым.
По той же причине живой census на `lenta.ru` в этом срезе не запускался
(тот же довод, что у S13 — правка не может проявиться, пока путь
недостижим; ценность этого среза — снять блокер, найденный S15, ПЕРЕД
следующей попыткой свопа, а не измерить эффект самого свопа). Следующая
сессия должна повторить S14/S15's эксперимент (`||`-порядок временно
свопнут, census + интерливед A/B на `lenta.ru`) — теперь без ожидаемого
блокера apply_relayout_result's route_query_js.

**Тесты:** `cargo test -p lumen-shell --bins --features v8 -- relayout
engine_thread` — 45/45 зелёных (тот же набор, что и S6/S11-16 гоняли),
включая существующие `thread2_defer_query_tests` (не затронуты — другой
механизм, несмотря на похожее имя). Нового unit-теста на сам
`pending_lazy_image_reqs` drain не добавлено — сценарий требует живого
движкового потока с реальным `PersistentJs` (`route_task_js`'s веткование
между `Some(engine)`/`None` уже покрыто существующими тестами `route_task_js`'s
двойника `route_query_js`), а синтетический мок ничего не проверил бы
сверх того, что тип `Arc<Mutex<Vec<_>>>` компилируется и дренируется —
следующая сессия, которая доведёт `||`-своп до реального измерения на
`lenta.ru`, — куда более сильная проверка этого механизма, чем юнит-тест
на голой структуре данных. `cargo clippy -p lumen-shell --all-targets
--features v8 -- -D warnings` — чист.

**Не сделано / следующий срез:** сама смена `||`-порядка (S12/S14's
эксперимент) по-прежнему не переиспробована — этот срез был предпосылкой,
которую S15/S16 назвали обязательной ДО следующей попытки. Вторичная
находка S15 (`compute_layout_incremental_restyle`'s собственный второй
`document.lock()`, 857мс в одном прогоне) тоже не устранена — отдельный,
менее приоритетный источник задержки на том же пути.

## S18 (P3, 2026-09-20) — `||`-своп повторён третий раз под S17-фиксом: fast-path теперь достижим (S17 подтверждён), но census снова не дошёл до конца — растущий `lock2_wait` в `compute_layout_incremental_restyle` (S15's нетронутая вторичная находка) — самый правдоподобный кандидат на новый блокер

Продолжение с того места, где остановился S17 («следующая сессия должна
повторить S14/S15's эксперимент — теперь без ожидаемого блокера
apply_relayout_result's route_query_js»).

Сначала прогнан baseline census (исходный `||`-порядок, без правок) на
`lenta.ru` для интерливед-сравнения: `relayout за 20 scroll-тиков: 2`
(off-thread, 157-158мс каждый), `scroll RTT ms: min=42.7 avg=10223.8
max=23548.2` — 17/20 тиков в диапазоне 10-11с, тот же порядок, что S11
(10-33с). Baseline подтверждает, что симптом бага воспроизводится на этой
сессии/трафике так же, как в предыдущих срезах — не регрессировал и не
исчез сам по себе.

Затем внесена сама смена `||`-порядка (`try_relayout_raf_incremental`
первым в обеих функциях, `relayout_raf_dirty`/`relayout_raf_dirty_readback`,
идентично S12/S14) поверх уже влитого S17-фикса и прогнан census заново на
том же стенде.

**Хорошая новость — S17 подтверждён живьём:** `apply_ms` (время внутри
`apply_relayout_result`, тот самый параметр, который S15 нашёл источником
5.9-7.6с) теперь **150-850мс** на каждом из восьми зафиксированных
инкрементальных тиков — ни одного значения, близкого к прежним 6-7с. Fast
path реально достигается: первый тик — дорогая non-restyle ветка
(`restyle=0`, 804.36мс, сеет кэш), следующие пять — `restyle=1` подряд
(146-329мс каждый), т.е. и S13-фикс (cascade-skip достижим), и S17-фикс
(JS-push не блокирует) работают вместе, как и предсказывалось.

**Но census снова не дошёл до конца — тот же класс отказа, что S12/S14,
новый механизм:** после шести дешёвых тиков — седьмой и восьмой
инкрементальные relayout'ы дают `restyle-wrapper` с растущим
`lock2_wait_ms` (S15's вторичная находка — второй, отдельный
`document.lock()` внутри `compute_layout_incremental_restyle`, до этого
среза не устранённый): 286.24мс, затем 332.46мс на следующем тике сразу
после. Восьмой (последний залогированный) тик даёт `[frame] total
7030.03ms` с разбивкой `js 6723.87ms` — поле `js` в этой строке (см.
`redraw_requested.rs:1880` и комментарий на `:1868-1877`) измеряет именно
шаги 3/3.1/4/5, то есть **сам вызов `relayout_raf_dirty`/`_readback`**, не
что-то постороннее. Из них на сам `[engine] relayout` (`apply_ms=727.37`
плюс профилируемые стадии) приходится ~1.07с — оставшиеся **~5.65с внутри
шага «js» этого кадра не покрыты ни одним из существующих таймеров**
(`lock_wait_ms`, `dirty_roots_ms`, `restyle-wrapper`, `apply_ms`) — они
измеряют части `try_relayout_raf_incremental`, а не сам код вокруг вызова
в `about_to_wait.rs`/обработчике кадра, который эту функцию зовёт. После
этой строки лог обрывается — census упал на очередном `scroll` с
`automation command timed out` (`DEFAULT_TIMEOUT=65s`), тем же способом,
что S12/S14.

**Не подтверждено инструментально в этом срезе** (в отличие от S15,
которая явно измерила и опровергла свою гипотезу до записи её как вывода):
растущий `lock2_wait` (286→332мс на двух тиках подряд, непосредственно
перед необъяснённым 5.65с) — правдоподобный, но не доказанный кандидат на
причину: contention на `Mutex<Document>` за движковым потоком, который в
эти же секунды гоняет тяжёлые рекламные/аналитические скрипты (лог вокруг
этого места полон `✗ https://kraken.rambler.ru/... (easylist)`,
`✗ https://cnt.rambler.ru/... (easylist)`, `TLS handshake: invalid peer
certificate` — та же картина трафика, что видел S3 на `lenta.ru`). Прямого
таймера ВОКРУГ вызова `relayout_raf_dirty`/`_readback` целиком (снаружи
`try_relayout_raf_incremental`, на уровне кадрового хендлера) не
существует — только сумма его внутренних стадий, которая на этом тике не
сходится с общим временем шага `js` почти на порядок.

**Действие:** `||`-порядок откачен (`git checkout --
crates/shell/src/relayout.rs`, обе функции вернули исходный порядок).
Правок кода в этом срезе не оставлено — единственная постоянная находка
этого среза текстовая (этот раздел). `cargo build -p lumen-shell --profile
dev-release --features v8` — чист после отката (используется тот же
бинарь, что и baseline census).

**Не сделано / следующий срез:** нужен таймер СНАРУЖИ
`try_relayout_raf_incremental` — вокруг самого вызова
`self.try_relayout_raf_incremental()` в `relayout_raf_dirty`/`_readback`
(или в кадровом хендлере, где считается шаг `js`) — чтобы отличить «время
ушло внутри уже известных подстадий, просто не залогировано на этом
конкретном тике» от «время ушло СНАРУЖИ вызываемой функции, до/после неё,
в чём-то ещё на UI-потоке между шагами 3 и 5». Только после этого —
повторять сам `||`-своп с census'ом в третий раз. Вторичная находка S15
(растущий `lock2_wait` в `compute_layout_incremental_restyle`) теперь
главный подозреваемый, но остаётся неустранённой и неподтверждённой как
единственная причина — тот же статус, что был после S15, только с новым
живым замером, сузившим окно поиска до последних ~2 тиков перед сбоем
вместо всего прогона.

## S19 (P3, 2026-09-20) — outer timer added around the call site, wrapping `try_relayout_raf_incremental` from OUTSIDE in both callers (S18's prerequisite, no live census run this slice)

Продолжение с того места, где остановился S18 («нужен таймер СНАРУЖИ
`try_relayout_raf_incremental` — вокруг самого вызова … — только после
этого повторять сам `||`-своп с census'ом в третий раз»).

**Правка** (`crates/shell/src/relayout.rs`, `relayout_raf_dirty` и
`relayout_raf_dirty_readback`): обе функции переписаны с `&&`-цепочки на
ранний `return`, чтобы вызов `self.try_relayout_raf_incremental()` можно
было обернуть отдельным таймером на стороне вызывающего — снаружи всех
внутренних измерений самой функции (`lock_wait_ms`/`dirty_roots_ms`/
`apply_ms`, S12-S15). Поведение под дефолтной сборкой не меняется
(`submit_relayout_job()`/`readback_relayout_job()` по-прежнему решают
всё под `LUMEN_ENGINE_THREAD=1` — короткое замыкание сохранено, просто
явным `if … { return; }` вместо `&&`). Новая строка лога (под тем же
`LUMEN_FRAME_LOG=1` гейтом, что и вся остальная инструментация этого
бага): `[engine] relayout_raf_dirty outer_ms=… (try_relayout_raf_incremental
call, handled=…)` и симметрично для `_readback`.

**Зачем именно так:** S18 нашёл двенадцатикратный разрыв (~7.0с шаг `js` в
`[frame]` против ~1.07с суммы внутренних таймеров функции на, предположительно,
том же тике) и explicitly не смог решить, сидит ли разрыв ВНУТРИ функции
(неинструментированный участок, который её же `incr_ms` должен был бы уже
покрыть) или СНАРУЖИ (что-то в вызывающем коде), потому что сравнивал две
независимо напечатанные строки лога, которые могли относиться к разным
тикам при интерливинге вывода. Таймер, обёрнутый непосредственно вокруг
вызова, печатает `outer_ms` рядом с местом вызова — при следующем прогоне
census'а `outer_ms` этой новой строки можно сопоставить впритык с `incr_ms`
самой функции (они всегда для одного и того же вызова, не для соседних
строк лога): если `outer_ms ≈ incr_ms` — разрыв внутри функции, в
неинструментированном участке; если `outer_ms` заметно БОЛЬШЕ `incr_ms` —
разрыв снаружи, в самом вызывающем коде (`relayout_raf_dirty`/`_readback`
до/после вызова, либо в разнице между временем входа в функцию-обёртку и
фактическим стартом `incr_t0` внутри неё).

**Не сделано в этом срезе:** сам `||`-своп НЕ вносился (S18 явно требует
сначала добавить внешний таймер, только потом повторять эксперимент) — эта
сессия ограничена инструментированием, живой census на `lenta.ru` не
запускался. Гипотеза S18 (растущий `lock2_wait` в
`compute_layout_incremental_restyle`, S15's вторичная находка) по-прежнему
не устранена и не подтверждена как единственная причина. Следующая сессия
должна: (1) внести `||`-своп в третий раз (идентично S12/S14/S18) поверх
уже влитого S19-таймера, (2) прогнать census на `lenta.ru`, (3) на первом
же тике с большим `outer_ms` прочитать соседнюю строку `incr_ms` того же
вызова и решить по вышеописанному критерию, где именно теряется время —
внутри функции или снаружи неё.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист, `cargo test -p lumen-shell --bins --features v8 --
relayout engine_thread` — 45/45 зелёных (тот же набор, что S6/S11-18
гоняли, без регрессий).

## S20 (P3, 2026-09-20) — `||`-своп повторён в четвёртый раз поверх S19-таймера: S18's разрыв не воспроизвёлся, S18's `lock2_wait`-гипотеза подтверждена и точно локализована

Продолжение с того места, где остановился S19 (own prerequisite): (1) внёс
`||`-своп в четвёртый раз (`relayout_raf_dirty`/`_readback` в
`crates/shell/src/relayout.rs`, `try_relayout_raf_incremental` теперь
пробуется ПЕРВЫМ, `submit_relayout_job`/`readback_relayout_job` — только как
fallback), (2) собрал `dev-release` в слоте `p3-work`, (3) прогнал живой
репро на `https://lenta.ru` (`LUMEN_PROFILE_TREE=1 LUMEN_FRAME_LOG=1`, без
MCP-скрипта — просто наблюдение stderr ~4 минуты) идентично S12/S14/S18.

**Итог по пункту (3) S19 (разрешить, где теряется время — внутри функции или
снаружи):** на каждом залогированном тике этого прогона `outer_ms`
(`relayout_raf_dirty`, снаружи вызова) и `incr_ms` (`[engine] relayout …
(incremental, on-thread)`, внутри `try_relayout_raf_incremental`) совпали
впритык — включая выбросы: `outer_ms=1285.79` / `incr_ms=1285.43`,
`outer_ms=2281.55` / `incr_ms=2281.24`. S18's ~7x/~12x разрыв (`outer`-подобный
`js`-шаг ~7.0с против `incr_ms`~1.07с) **не воспроизвёлся** в этом прогоне —
похоже, S18 действительно сопоставил две строки от разных тиков, как сам и
подозревал; на этом стенде и в этом прогоне разрыва между таймером снаружи
и таймером внутри функции нет.

**Что реально доминирует в выбросах:** `restyle-wrapper` (`relayout.rs:1536`,
`compute_layout_incremental_restyle`) логирует `lock2_wait_ms` — время
`document.lock()` (`relayout.rs:1563`), ВТОРОГО, независимого захвата того
же `Mutex<Document>`, который вызывающий (`try_relayout_raf_incremental`)
уже отпустил перед вызовом этой функции (S15's находка, ещё не устранённая).
На спокойных тиках `lock2_wait_ms=0.00`; на выбросах —
`lock2_wait_ms=531.07/394.22/512.83/586.99/293.76`мс, что почти один в один
объясняет соответствующий рост `apply_ms` (315→744→1252→1759→…мс) и, через
него, `incr_ms`/`outer_ms` (328→1286→1656→2282→2420→1321мс). Ряд не
монотонный — `lock2_wait`/`apply_ms`/`outer_ms` растут и падают вместе
(последний замер в серии: `lock_wait_ms=258.85` — другой, первый лок,
впервые за прогон не ноль, `apply_ms=757.58` упал обратно), то есть это не
утечка/накопление, а контеншн: что-то на движковом потоке периодически
держит `Mutex<Document>` по 0.3-0.6с одновременно с тем, как UI-поток здесь
пытается взять его повторно.

**Не установлено в этом срезе:** ЧТО именно на движковом потоке держит лок
в эти окна (рекламный трафик — `kraken.rambler.ru`/`cnt.rambler.ru`/
`top100.ru` — идёт непрерывно все эти тики, но прямой причинности с
конкретным held-lock интервалом не показано; кандидат — синхронный
`_lumen_fetch_sync`/JS rAF turn на движковом потоке, тот же класс, что S4/S5
нашли для `EngineThread::query()` FIFO, но здесь захват `Document`, не
`reply_rx.recv()`). Один `[frame] total 7272.56ms` с `js=6895.39ms` замечен
в этой же серии — на порядок больше, чем сам `incr_ms` этого тика
(1655.79мс) — подтверждает, что параллельно с M4-путём на странице
продолжает происходить отдельный, гораздо более крупный синхронный JS-стопор
(тот же механизм, что S3-S5 уже документировали для рекламных
fetch/FIFO-цепочки), который census этого бага не должен путать с
собственно M4-инкрементальным путём — они складываются, но это два разных
источника задержки.

Своп кода **отменён** после снятия замера (`git checkout -- relayout.rs`) —
как и в S12/S14/S18, это не финальный фикс: код вернулся к S19-состоянию
(короткое замыкание `submit_relayout_job`/`readback_relayout_job` первым,
`try_relayout_raf_incremental` недостижим на дефолтной сборке). Правок,
которые остаются в этом срезе, нет — только раздел документа.

**Следующий срез должен:** инструментировать вызывающую сторону
`compute_layout_incremental_restyle`'s `document.lock()` (`relayout.rs:1563`)
— найти, какой именно код на движковом потоке удерживает `Mutex<Document>`
в 0.3-0.6с окнах, коррелирующих с рекламным трафиком (добавить лог с
таймстампом захвата/отпускания на стороне движкового потока, не только
времени ожидания на UI-стороне) — прежде чем снова трогать `||`-порядок.
Если этот лок и есть последний блокер для завершения census'а, устранение
его (или хотя бы понимание его природы) — предпосылка того, ради чего весь
трек затевался: сравнить частоту/стоимость M4-инкрементального пути против
полного `submit_relayout_job` на устойчивом, а не постоянно
переоткрываемом стенде.

## S21 (P3, 2026-09-20) — engine-thread `Task`-таймер снаружи (S20's предпосылка) нашёл не lock-contention, а сами `Task`-замыкания, блокирующие движковый поток на секунды-минуты — вероятная настоящая доминанта симптома, отдельная от M4-incremental-пути

Продолжение с того места, где остановился S20 («инструментировать вызывающую
сторону `compute_layout_incremental_restyle`'s `document.lock()` — найти,
какой именно код на движковом потоке удерживает `Mutex<Document>` в 0.3-0.6с
окнах»).

**Правка** (`crates/shell/src/engine_thread.rs`, `run_batch`): внешний таймер
вокруг **каждого** `EngineMsg::Task`-замыкания (generic-обёртка — модуль
намеренно не знает, что внутри: JS-тик, DOM-мутация, `document.lock()` и
т.п., см. заголовок файла). Под `LUMEN_FRAME_LOG=1` печатает `[engine] task
{ms}ms (engine-thread Task, generic wrapper)` для абсолютно любого `Task`,
независимо от того, какой вызывающий (`tick_timers`/`run_animation_frame`/
`eval_js`/`set_js_ctx`/…) его поставил — единая точка, через которую проходят
все они (`engine_thread.rs::run_batch`).

**Живой замер** (`lenta.ru`, `--maximized`, ~4 минуты стенда S3/S5/S18-S20,
дефолтная сборка — `||`-порядок НЕ менялся, M4-incremental-путь в этом срезе
недостижим как обычно): из 200+ `Task`-строк подавляющее большинство — доли
мс (JS-пуш observer'ов и т.п., ожидаемо дёшево), но **10 записей превысили
1000мс**, включая:

```
3307.92, 3613.26, 3797.86, 5212.50, 20882.73, 23428.71, 23888.07, 233934.20
```

Последнее число — **233.9 секунды** на ОДНОМ `Task`-замыкании. Процесс не
падает и не виснет насовсем — после каждой такой строки лог продолжается
штатно (новые `[frame]`/сетевые записи идут дальше), то есть это не deadlock
(S3/S4's находка), а очень долгая, но конечная синхронная работа,
выполняемая **на движковом потоке внутри одного `Task`**, которая блокирует
абсолютно всё остальное, что стоит в FIFO-очереди позади неё — relayout
`Run`-задания (S20's «Corень» напрямую от этого страдает: `submit_relayout_job`
коалесцируется, но должен ДОЖДАТЬСЯ своей очереди после текущего `Task`),
`query()`-чтения (уже ограничены `QUERY_TIMEOUT=5s` с S6, но само задание
внутри очереди никуда не девается, просто отвечает `None` раньше срока —
S6's doc-комментарий), automation-канал.

**Правдоподобный механизм (не подтверждён инструментально до конца этого
среза, но согласуется по порядку величины):** S4-S10 добавили таймаут на
КАЖДЫЙ отдельный синхронный сетевой примитив (TCP-connect, WS/SSE/TLS/SOCKS5-
хендшейк, `EngineThread::query()`) — но ни один из них не ограничивает
суммарное время ОДНОГО JS-тика/`Task`, который может внутри себя вызвать НЕСКОЛЬКО
таких примитивов подряд. Конкретный, уже найденный чтением кода пример —
`DohResolver::resolve()` (`crates/network/src/doh.rs::resolve`, RFC 8484): на
каждое разрешение НОВОГО хоста уходит **два последовательных** HTTP GET-запроса
к DoH-эндпоинту (AAAA, потом A, `fn query`, `:316`) — каждый идёт через тот же
`connect_inner`, что и обычный `fetch` (то же самое `CONNECT_TIMEOUT=10s`/
`FETCH_READ_TIMEOUT=60s` из S4/S9), но НИЧЕГО не ограничивает их СУММУ. Два
подряд ~10-12с исхода дают наблюдаемые 20.9/23.4/23.9с почти точь-в-точь;
234с ближе к 3-4 последовательным `FETCH_READ_TIMEOUT=60s`-исходам подряд —
согласуется с гипотезой «страница выполняет НЕСКОЛЬКО последовательных
синхронных сетевых вызовов (рекламные/аналитические `fetch`/резолвы к разным
хостам — лог вокруг всех крупных `Task` полон `kraken.rambler.ru`/
`cnt.rambler.ru`/TLS `UnknownIssuer`-ошибок к `kraken-mdt.rambler.ru`,
`✗ … (easylist)`) в ОДНОМ JS-тике, и каждый шаг платит свой уже
ограниченный, но НЕ агрегированный таймаут».

**Значение для остального бага:** это, вероятно, **более крупный источник**
наблюдаемых зависаний, чем сам M4-incremental-роутинг (S1-S20's фокус) —
относительно скромные «3 полных relayout'а по 419-1812мс» из S11 меркнут
перед engine-thread `Task`, блокирующим весь поток на десятки-сотни секунд.
Не отменяет находку S1-S20 (M4-путь всё ещё мёртв под движковым потоком,
отдельная, независимо верная проблема), но объясняет, ПОЧЕМУ census на
рекламно-тяжёлых сайтах (`ria.ru`, `lenta.ru`) так труден для чистого
измерения на протяжении всего трека S1-S20 — стенд сам периодически
проваливается в многосекундные-многоминутные паузы по независимой от
relayout причине.

**Не сделано / не подтверждено в этом срезе:**
- Гипотеза про `DohResolver`/цепочку последовательных сетевых вызовов внутри
  одного JS-тика — правдоподобна по порядку величины, но НЕ подтверждена
  прямым логом (какой именно вызов исполнялся во время каждого конкретного
  долгого `Task`, инструментация этого среза этого не различает — `run_batch`
  не знает, что внутри замыкания). Следующий срез должен либо (а) добавить
  метку вызывающего в сам `Task`-message (например, `&'static str` label на
  `EngineMsg::Task`, проброшенный через `route_task_js`/`route_eval_js`/
  `route_query_js` на все call-site'ы) — самый прямой путь, либо (б)
  временно залогировать вход/выход из `DohResolver::query`/`resolve` под тем
  же `LUMEN_FRAME_LOG`, чтобы увидеть, действительно ли долгие `Task`
  совпадают по длительности с DNS-резолвом, а не с чем-то ещё.
- Никакой фикс не внесён — это диагностическая находка, не точечная правка:
  если гипотеза подтвердится, вероятные направления — распараллелить
  AAAA/A-запросы `DohResolver::resolve()` (сейчас строго последовательны),
  и/или завести общий бюджет на суммарное время синхронных сетевых вызовов
  ОДНОГО JS-тика (архитектурное решение, требует протокола
  `docs/perf-method.md`, не входит в этот срез).
- Сам `||`-порядок `relayout_raf_dirty`/`_readback` не трогался — это
  предпосылка S18/S19/S20's плана («сравнить `outer_ms`/`incr_ms`, повторить
  своп»), которая теперь встаёт в очередь ПОСЛЕ этой более крупной находки:
  бессмысленно измерять эффект M4-fast-path на стенде, где сам движковый
  поток непредсказуемо замирает на десятки-сотни секунд по независимой
  причине — сначала нужно понять/ограничить это, иначе любой census
  интерливед A/B (S11-S20) продолжит давать шумные, невоспроизводимые числа
  ровно как в S1-S20.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист, `cargo test -p lumen-shell --bins --features v8 -- relayout
engine_thread` — 45/45 зелёных (тот же набор, без регрессий). Инструментация
gated `LUMEN_FRAME_LOG` — под дефолтной сборкой (флаг выключен) поведение
байт-идентично, добавлен только `Instant::now()`+условный `eprintln!` внутри
уже существующей точки диспетчеризации `Task`.

## S22 (P3, 2026-09-20) — S21's DoH-гипотеза ОПРОВЕРГНУТА для дефолтной сборки; найден настоящий механизм каскада «один долгий `Task` → десятки секунд `query()`-таймаутов на КАЖДОМ следующем кадре»

Продолжение с того места, где остановился S21 (два пути: (а) метка
call-site'а на `Task`, (б) инструментировать `DohResolver::query`/`resolve`).
Пошёл путём (б) — дешевле, проверяет гипотезу напрямую.

**Инструментация:** `crates/network/src/doh.rs` — `[doh] query {hostname}
qtype={qtype} {ms}ms ok={bool}` вокруг `DohResolver::query` (`transport.fetch`
— единственный сетевой вызов метода) и `[doh] resolve {hostname} total {ms}ms`
вокруг пары AAAA+A в `resolve()`, оба под `LUMEN_FRAME_LOG>=1` (свой
локальный хелпер `frame_log_enabled()` в `doh.rs` — `lumen-network` лежит
ниже `lumen-paint` по слоям, заводить зависимость только ради этого флага
нельзя). `cargo clippy -p lumen-network --all-targets -- -D warnings` чист,
`cargo test -p lumen-network --lib doh` — 44/44 зелёных.

**Гипотеза ОПРОВЕРГНУТА:** живой репро на `lenta.ru` (`--maximized`,
`LUMEN_PROFILE_TREE=1 LUMEN_FRAME_LOG=1`, ~4 минуты, идентично S20/S21) дал
**ноль** строк `[doh]` за весь прогон, при том что `[engine] task` снова
показал те же порядки долгих замыканий (31384мс, 12118мс, 18568мс,
46305мс). Причина: `DohResolver` подключается только явным `--doh-url`
(`crates/shell/src/config.rs::apply_http`, `:313-329`, комментарий «Wire DoH
resolver if configured») — в дефолтной сборке (без флага) DNS идёт через
`SystemDnsResolver`, а не через `DohResolver`. S21's гипотеза «два
последовательных DoH-запроса без общего таймаута» верна как код (уязвимость
реальна при явном `--doh-url`), но **не объясняет** дефолтный репро этого
бага — она была построена по правдоподобию порядка величины, не по прямому
наблюдению, и прямое наблюдение её отвергло для этого стенда.

**Побочная (более крупная) находка, ради которой стоило инструментировать:**
в окне, где шли долгие `[engine] task` (строки ~550-925 лога), **23 из 31**
строк `[frame] total` показали поле `js` (шаг 3/3.1/4/5 обработчика
`RedrawRequested` — rAF, релейаут по грязному DOM, paint-timing,
`redraw_requested.rs:1871`) в диапазоне **5045–6266мс**, устойчиво
повторяясь кадр за кадром (не один выброс, а минимум 16 кадров подряд).
Это почти точно совпадает с `QUERY_TIMEOUT = Duration::from_secs(5)`
(`engine_thread.rs`, константа S6). Механизм: `route_query_js`
(`engine_bridge.rs`) на UI-потоке зовёт `EngineThread::query`, которая
**блокирует вызывающий (UI) поток** на `reply_rx.recv_timeout(QUERY_TIMEOUT)`
(`engine_thread.rs:267`) — если движковый поток занят одним из долгих
`Task`-замыканий S21 (FIFO, не коалесцируется), любой `query()`, отправленный
ПОЗЖЕ него в ту же очередь, физически не может получить ответ раньше, чем
движковый поток освободится, и упирается в свой собственный таймаут. Пока
долгий `Task` сидит в очереди, **каждый** следующий кадр, чей `js`-шаг зовёт
`query()` (а `RedrawRequested` зовёт его почти всегда — `take_dom_dirty`/
`take_raf_pending`/`eval_js_value` маршрутизированы через него), платит
**весь** `QUERY_TIMEOUT` как чистый простой UI-потока — не одна пауза, а
столько 5-6-секундных пауз подряд, сколько кадров успевает нарисоваться,
пока движковый поток занят.

**Значение:** S6's фикс (`QUERY_TIMEOUT`) заменил «UI-поток виснет НАВСЕГДА
позади одного долгого соседа» на «UI-поток виснет на `QUERY_TIMEOUT` на
КАЖДОМ кадре, пока сосед не освободится» — строго лучше (процесс отвечает,
можно закрыть вкладку/приложение), но с точки зрения отзывчивости почти
неотличимо от исходного зависания: 20+ кадров по 5-6с каждый — это
десятки-сотня секунд субъективного фриза, тот же порядок, что S21's
233.9с single-`Task`-замер, только размазанный по многим кадрам вместо
одного. Это и есть механизм, из-за которого «нескольких долгих `Task`»
(S21) достаточно, чтобы весь живой прогон выглядел подвисшим — не сам факт
долгого `Task`, а то, что КАЖДЫЙ кадр редро после него платит `query()`-налог
по отдельности. `route_query_js`/`EngineThread::query` — синхронный,
блокирующий UI-поток вызов ИЗНУТРИ `RedrawRequested`, что противоречит
заявленной цели ADR-016 M2.2c «UI-поток не блокируется движковой работой»
ровно тогда, когда движковый поток занят чем-то долгим — до сих пор этот
компромисс нигде явно не документирован как компромисс, только как
«таймаут ограничивает уже существующий отказ» (doc-комментарий `query`).

**Не установлено в этом срезе:** что именно исполняется ВНУТРИ самих долгих
`Task`-замыканий (12-46с). Между двумя последними (18568мс и 46305мс,
строки 594-595 лога) — **ноль** новых `→ GET`/`←`/`✗`-строк (событие
`RequestStarted` эмитится ДО DNS/connect, `lib.rs:2427/2553` — если бы это
был сетевой fetch, «→ GET» появилось бы до блокировки), значит эти два
конкретных замыкания не тратят время на инструментированный сетевой путь
(TCP/TLS/WS/SSE/SOCKS5 — все ограничены S4-S10) — кандидаты: CPU-связанная
JS-работа (тяжёлый скрипт/GC), синхронная работа не по сети (например,
работа с Worker'ами, font-парсинг — первому долгому `Task` в 31384мс
предшествует пачка `FontLoaded`-строк). Путь (а) из S21 (метка call-site'а
на `EngineMsg::Task`) остаётся нужным и теперь однозначно оправдан —
DoH-путь исключён, следующая сессия должна разметить `Task`, чтобы узнать,
какой именно вызывающий (`tick_timers`/`run_animation_frame`/`eval_js`/
`set_js_ctx`/…) стоит за конкретным долгим замыканием.

**Следующий срез должен:** (1) добавить `&'static str`-метку на
`EngineMsg::Task`/`EngineThread::task`/`query`, проброшенную через
`route_task_js`/`route_eval_js`/`route_query_js` и прямые вызовы
(`relayout.rs:613,1286,1307,1322,1344`) — самый прямой путь узнать, что
стоит за 12-46-секундными замыканиями; (2) отдельно рассмотреть, не стоит
ли задокументировать (или устранить) находку этого среза — что
`route_query_js`, вызываемый из `RedrawRequested`, блокирует UI-поток на
`QUERY_TIMEOUT` при каждом кадре, пока движковый поток занят, а не только
один раз.

**Тесты:** `cargo clippy -p lumen-network --all-targets -- -D warnings`
чист, `cargo test -p lumen-network --lib doh` — 44/44 зелёных (без
регрессий, тесты новых полей не заводили — инструментация не меняет
возвращаемые значения).

## S23 (P3, 2026-09-20) — call-site-меткой (`#[track_caller]`) подтверждено: долгие `Task`-замыкания — это доставка JS-колбэков (timers/pump/observers), не сетевой ввод-вывод и не lock-contention

Продолжение с того места, где остановился S22 (два открытых вопроса: что
исполняется внутри долгих `Task`, и почему `route_query_js`/`RedrawRequested`
каскадно платит `QUERY_TIMEOUT` на каждом кадре). Пошёл первым путём S21/S22
— пункт (а), метка call-site'а на `EngineMsg::Task`.

**Правка:** вместо ручного протаскивания `&'static str`-лейбла через
`route_task_js`/`route_eval_js`/`route_query_js` и все ~25 вызывающих в
`page_load.rs`/`relayout.rs` (что и предлагал S22) использован штатный
Rust-механизм `#[track_caller]`: атрибут добавлен на `EngineThread::task`/
`EngineThread::query` (`engine_thread.rs`) и на все три `route_*_js`-обёртки
(`engine_bridge.rs`) — `#[track_caller]` прозрачно распространяется сквозь
цепочку вызовов `#[track_caller]`-функций, поэтому `Location::caller()`,
захваченный внутри `EngineThread::task`/`query`, указывает на настоящий
UI-сторонний call site (`page_load.rs`/`relayout.rs`), а не на сам
`engine_thread.rs`. `EngineMsg::Task` стал структ-вариантом с полем `caller:
&'static Location<'static>`, лог `[engine] task` печатает его вместо
статичной строки «generic wrapper». Прямые вызовы `engine.task`/`.query` в
`relayout.rs:613,1286,1307,1322,1344` не потребовали правок — `#[track_caller]`
уже покрывает их без обёртки. Дешевле и надёжнее, чем ручной лейбл на
каждом из ~25 call site'ов (S22's исходный план), и не может разойтись с
реальным местом вызова.

**Живой замер** (`lenta.ru`, `--maximized`, `LUMEN_PROFILE_TREE=1
LUMEN_FRAME_LOG=1`, ~2.5 минуты, идентично стенду S18-S22): из 201 строки
`[engine] task` шесть превысили секунду:

```
[engine] task 19840.26ms (engine-thread Task, from crates\shell\src\page_load.rs:2186:13)
[engine] task  5400.06ms (engine-thread Task, from crates\shell\src\app\about_to_wait.rs:260:13)
[engine] task  6354.72ms (engine-thread Task, from crates\shell\src\app\about_to_wait.rs:260:13)
[engine] task 28782.85ms (engine-thread Task, from crates\shell\src\relayout.rs:1023:33)
[engine] task  4096.49ms (engine-thread Task, from crates\shell\src\app\about_to_wait.rs:260:13)
[engine] task 28159.24ms (engine-thread Task, from crates\shell\src\relayout.rs:1023:33)
```

Все три call site'а — не сетевой код и не `document.lock()` (S20's
гипотеза), а доставка JS-колбэков через `route_task_js`/`route_query_js`:

- `about_to_wait.rs:260` — per-tick pump-батч (`j.tick_timers();
  j.pump_websockets(); j.pump_sse(); j.pump_workers();
  j.pump_broadcast_channels();`) — `tick_timers` исполняет due
  `setTimeout`/`setInterval`-колбэки, `pump_websockets`/`pump_sse` доставляют
  `message`-события синхронно, все — произвольный JS на движковом потоке;
- `relayout.rs:1023` и `page_load.rs:2186` — `deliver_layout_observers`/
  `deliver_media_query_changes`/`deliver_lazy_images` — доставка
  `IntersectionObserver`/`ResizeObserver`/`matchMedia`-колбэков, тоже
  произвольный синхронный JS.

**Вывод:** долгие `Task`-замыкания (S21's находка) — это не единичный
тяжёлый системный вызов (DNS/lock/IO), а **синхронное исполнение JS-кода
самой страницы** внутри штатной доставки timer/pump/observer-колбэков.
Согласуется с S22's наблюдением «ноль `→ GET` в окне блокировки»
(колбэк может не делать сетевых вызовов вовсе — просто тяжёлый/зависший
рекламный скрипт) и объясняет, почему S21's DoH- и S20's lock-гипотезы обе
были частично неверны: единой системной причины нет, каждый долгий `Task`
может быть другим JS-колбэком с другой ценой. Root cause на уровне «что
именно в рекламном JS исполняется по 5-28с» не установлен и, вероятно, не
устанавливаем без профилировщика V8 (за пределами этого трека) — но теперь
достоверно известно, что причина внутри JS-исполнения, а не в
Rust-инфраструктуре engine-thread/relayout/сети.

**Значение для архитектуры:** ни один из существующих таймаутов
(`QUERY_TIMEOUT`, `FETCH_READ_TIMEOUT`, `CONNECT_TIMEOUT`, S4-S10) не
ограничивает время исполнения самого JS-колбэка — синхронный JS на
движковом потоке в принципе не может быть прерван таймером изнутри
однопоточного V8 без кооперативной точки останова (`SetInterruptCallback`)
внутри страницы, которой ни у одного из pump/observer/timer-путей сейчас
нет. Это отдельная, более глубокая архитектурная работа (execution budget
per JS turn), не входит в этот срез.

**Не сделано в этом срезе:** не внесён никакой лимит/interrupt на длину
самого JS-колбэка — только диагностика подтвердила его как источник.
`||`-своп `relayout_raf_dirty`/`_readback` (S18-S20's предпосылка к
census'у M4-incremental-пути) по-прежнему не тронут — census на
рекламно-тяжёлых сайтах остаётся зашумлён этим независимым источником пауз
(S21's вывод не изменился, теперь с прямым подтверждением механизма вместо
предположения).

**Следующий срез должен** одно из: (1) сравнить `page_load.rs:2186`/
`relayout.rs:1023`/`about_to_wait.rs:260` — сколько раз каждый встречается
среди долгих `Task` на более длинном прогоне, чтобы понять, какой из трёх
путей (timers/pump vs observers) доминирует численно, а не только по одному
образцу каждого; (2) исследовать `V8::Isolate::SetInterruptCallback` (или
эквивалент `rusty_v8`) как механизм кооперативного прерывания
runaway-JS-колбэка per pump/observer/timer-вызов — если жизнеспособно,
это первый реальный кандидат-фикс для самого симптома бага (а не только для
диагностики), а не диагностика; (3) вернуться к S18-S20's `||`-свопу
M4-incremental-пути теперь, когда источник шума в census'е понятен
(документировать его как известный confound, а не пытаться устранить).

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист, `cargo test -p lumen-shell --bins --features v8 -- relayout
engine_thread` — 45/45 зелёных (тот же набор, что S6/S11-S22 гоняли, без
регрессий). Инструментация не меняет поведение под дефолтной сборкой без
`LUMEN_FRAME_LOG` — `#[track_caller]` не имеет рантайм-стоимости при
отключённом логе (тот же `Instant::now()`-гейт, что уже был), добавляет
только размер `&'static Location` (два указателя) в `EngineMsg::Task`.

## S24 (P3, 2026-09-20) — на длинном прогоне посчитана частота/суммарное время трёх call site'ов из S23: доминирует `relayout.rs:1023` (блокирующий `route_query_js`, не `route_task_js`), несмотря на наименьшее число вызовов

Продолжение с того места, на котором остановился S23 (три варианта следующего
шага; выбран пункт (1) — «сравнить `page_load.rs:2186`/`relayout.rs:1023`/
`about_to_wait.rs:260` — сколько раз каждый встречается среди долгих `Task` на
более длинном прогоне»). Код не менялся, инструментация S23
(`#[track_caller]`-метка) использована как есть.

Живой прогон (`lenta.ru`, `--maximized`, `LUMEN_PROFILE_TREE=1
LUMEN_FRAME_LOG=1`, PID через `wmic process where "name='lumen.exe'"` —
`tasklist`/`taskkill` по PID из git-bash `$!` не совпадает с нативным Windows
PID, см. S3's заметку про среду) шёл 3 минуты, дал 191 строку `[engine]
task`. Разбивка по call site (все вызовы, не только долгие):

```
     76 relayout.rs:581:9              (1.5ms суммарно — дешёвые, не источник)
     42 app/user_event.rs:180:17        (4.7ms суммарно)
     13 app/about_to_wait.rs:1391:13    (1.2ms суммарно)
     10 app/about_to_wait.rs:260:13     (26828.6ms суммарно ← S23's pump-путь)
      9 window_event/redraw_requested.rs:80/182 (~1ms суммарно)
      2 relayout.rs:1023:33             (51695.9ms суммарно ← S23's observer-путь)
      1 page_load.rs:2186:13            (24124.0ms суммарно ← S23's media-query/lazy-image путь)
```

Суммарное время по трём «тяжёлым» путям S23: `relayout.rs:1023` — **51.7с** за
всего 2 вызова (среднее 25.8с/вызов — самый дорогой per-call), `about_to_wait.rs:260`
(pump-батч `tick_timers`/`pump_websockets`/`pump_sse`/`pump_workers`/
`pump_broadcast_channels`) — 26.8с за 10 вызовов (среднее 2.7с/вызов, чаще, но
дешевле), `page_load.rs:2186` (`deliver_media_query_changes`/`deliver_lazy_images`
на пути загрузки страницы) — 24.1с за 1 вызов. **`relayout.rs:1023` доминирует
по суммарному заблокированному времени**, хотя вызывается реже всех троих —
за 3-минутный прогон он один держит движковый (и, что важнее, см. ниже,
временами UI-) поток занятым дольше, чем оба других пути вместе.

**Важное уточнение, не отмеченное в S21-S23:** `relayout.rs:1023` — это
именно `else`-ветка `apply_relayout_result` (`relayout.rs:1000-1023`),
которая идёт через **блокирующий `route_query_js`**, а НЕ через
`route_task_js` (тот — ветка `defer_js_push` строкой выше, `:1002-1021`,
введена S17 специально, чтобы отвязать доставку JS-колбэков от
синхронного ожидания). Из четырёх мест, зовущих `apply_relayout_result`
(`relayout.rs:188,410,1204,1232`), только `:410` передаёт `defer_js_push =
true` — остальные три (в т.ч. и путь, доминировавший в этом прогоне)
по-прежнему используют блокирующий `route_query_js`. Т.е. S17's фикс
(«отвязать доставку JS-колбэков от `route_query_js`») применён лишь к
одному из четырёх вызывающих мест — большая часть трафика доставки
observer/media-query/lazy-image-колбэков всё ещё идёт по пути, где
затраты JS-колбэка напрямую ложатся в критический путь `query()`, которым
UI-поток дожидается ответа (S22's механизм — `QUERY_TIMEOUT`-налог на
каждый последующий кадр).

**Не сделано:** перевод оставшихся трёх мест на `defer_js_push = true` —
это меняет асинхронный/синхронный контракт данных, которые опираются на
свежесть `layout_rects`/`client_rects`/`hit_test_tree` сразу после вызова
(некоторые вызывающие, вероятно, полагаются на синхронный возврат
`lazy_reqs`/`take_lazy_image_requests()` для немедленного `fetch`
ленивых изображений — нужно прочитать каждый из трёх call site'ов
(`:188` `relayout()`, `:1204`/`:1232`) и убедиться, что переход на
асинхронный push не ломает их предположения, прежде чем менять); не
входит в этот срез — требует отдельного анализа три вызывающих места +
теста на регресс.

**Следующий срез должен:** прочитать три оставшихся вызывающих места
`apply_relayout_result` (`:188`, `:1204`, `:1232`) и определить, для
каждого ли из них перевод на `defer_js_push = true` (по образцу S17's
`:410`) безопасен — если да, это прямой кандидат-фикс на снижение
`QUERY_TIMEOUT`-налога на UI-поток без замены самого механизма доставки
JS-колбэков; если для какого-то из трёх синхронный ответ (лениво
загружаемые изображения/актуальные rects) действительно нужен вызывающей
стороне немедленно — задокументировать это как осознанный компромисс и
искать другой путь (например, кооперативное прерывание JS, S23's пункт
(2)).

**Тесты:** правок кода не было — только живой замер и чтение
`apply_relayout_result`'s call site'ов. `cargo clippy -p lumen-shell
--all-targets --features v8 -- -D warnings` не перезапускался (код не
менялся с S23).

## S25 (P3, 2026-09-20) — прочитаны все три оставшихся вызывающих `apply_relayout_result`; `:1204` мёртв в проде, `:188`/`:1232` небезопасны переводить на `defer_js_push=true` без независимого дренажа `pending_lazy_image_reqs`

Продолжение S24's «следующий срез должен» пункта (1): прочитать `:188`
(`relayout()`), `:1204` (`readback_relayout_job`), `:1232`
(`poll_engine_commit`) и решить, безопасен ли для каждого перевод на
`defer_js_push = true` по образцу S17's `:410`. Код не менялся — только
чтение `relayout.rs` и всех вызывающих трёх функций.

**Находка A — `:1204` (`readback_relayout_job`) не исполняется в проде.**
Единственный вызывающий — `relayout_raf_dirty_readback` (`relayout.rs:727`),
единственный вызывающий ЕГО — `redraw_requested.rs:415`, внутри `else`-ветки
`if self.engine_thread.is_some() { .. } else { ..
self.relayout_raf_dirty_readback(); .. }` (строка 373). Т.е. этот код-путь
достижим только когда движковый поток ВЫКЛЮЧЕН — но `readback_relayout_job`
сам начинается с `if self.engine_thread.is_none() { return false; }`
(`relayout.rs:1194`), поэтому при каждом фактическом вызове функция
немедленно возвращает `false`, так и не дойдя до `apply_relayout_result` на
`:1204`. Нет ни одного другого вызывающего и ни одного теста, который бы
this исполнял. Вопрос «безопасен ли `defer_js_push=true` для `:1204`» не
имеет практического значения — путь мёртв структурно, отдельно от M4-пути,
которым уже занят весь трек бага.

**Находка B — `:188` (`relayout()`) прочитан по всем 9 прямым вызывающим,
ни один не читает JS-сторонний результат push синхронно.** Вызывающие:
`about_to_wait.rs:480,1606`, `app/mod.rs:237`, `redraw_requested.rs:435`
(тоже только под `engine_thread.is_none()`), `bfcache.rs:132,231`,
`viewport_sync.rs:54`, `page_load.rs:769,2246`. Всё, что идёт следом за
`self.relayout()` в каждом из них, либо не трогает layout-результат вовсе
(перерисовка, event-лог), либо читает **Rust-сторонний** `self.layout_box`/
`self.display_list` (например `page_load.rs:774-781`'s `forms::find_box_rect`
для scroll-to-fragment) — оба поля `apply_relayout_result` выставляет
синхронно независимо от `defer_js_push` (`relayout.rs:829,902`), только
JS-сторонний push (`update_layout_rects` и Co.) уходит в
`route_task_js`/`route_query_js`. Ни один вызывающий не делает блокирующий
JS-запрос (`route_query_js`) сразу после `relayout()`, который бы увидел
устаревшие данные.

**Находка C (блокер) — независимо от находки B, перевод `:188`/`:1232` на
`defer_js_push=true` небезопасен по другой причине: контракт «дренажа»
`pending_lazy_image_reqs`.** S17's механизм (строки 1000-1021, 1054-1056)
складывает `take_lazy_image_requests()` в `self.pending_lazy_image_reqs` и
рассчитывает, что его заберёт **следующий** вызов `apply_relayout_result` —
любой, не обязательно того же producer'а (комментарий на `:1048-1053`:
«pick up whatever a *previous* deferred push … already stashed»). Это
предположение верно для единственного сегодняшнего `true`-вызывающего,
`try_relayout_raf_incremental`, потому что он вызывается из
continuous rAF+DOM-mutation цикла — по определению следующий тик почти
всегда придёт. Оно НЕ верно для `relayout()` (`:188`) или
`poll_engine_commit` (`:1232`): любой из них может оказаться **последним**
relayout'ом за время жизни страницы — финальный resize перед тем, как
пользователь просто скроллит статичную страницу, или финальный
off-thread commit перед простоем. `pending_lazy_image_reqs` не имеет
независимого таймера/опроса (`grep pending_lazy_image_reqs` — читается
только внутри `apply_relayout_result` самого, `relayout.rs:1054`); если
после такого «последнего» вызова ни один producer больше не позовёт
`apply_relayout_result`, только что открывшиеся близко-к-viewport
`<img loading=lazy>` останутся навечно в очереди и НИКОГДА не
зафетчатся — не просто устаревшее чтение (как в находке B), а тихая потеря
данных, которую ничто не восстановит без нового relayout'а.

**Вывод:** ни `:1204` (мёртв, вопрос не стоит), ни `:188`/`:1232` (небезопасны
из-за находки C) сегодня НЕ являются безопасными кандидатами на
`defer_js_push=true` — вопреки первоначальной гипотезе S23-S24 («перевод
оставшихся трёх мест — прямой кандидат-фикс»). Находка C — предпосылка,
которую нужно закрыть ПЕРЕД любой такой конверсией, независимо от того, что
находка B её не блокирует сама по себе.

**Не сделано:** код не менялся — только чтение. Независимый дренаж
`pending_lazy_image_reqs` (например, опрос в `about_to_wait`'s per-tick
пампе, тем же способом, каким уже опрашивается `poll_engine_commit`) не
спроектирован и не написан.

**Следующий срез должен:** спроектировать и добавить независимый дренаж
`pending_lazy_image_reqs`, не завязанный на «будет ли ещё один
`apply_relayout_result`» (например: `about_to_wait`'s parked-loop пампа,
после `poll_engine_commit()`, проверяет `pending_lazy_image_reqs` и зовёт
`fetch_and_register_lazy_images` сама, если очередь не пуста — тем же
паттерном, каким она уже дренирует `dom_dirty`/`raf_pending`-флаги). Только
после этого `:188`/`:1232` становятся реальными кандидатами на
`defer_js_push=true`, и то отдельным экспериментом с census'ом (как
S18-S20 делали для M4), а не одной правкой — S15/S18-S20's история этого же
файла показывает, что «выглядит безопасным по чтению кода» и «безопасно на
живом census'е» здесь расходились уже дважды.

**Тесты:** правок кода не было — `cargo clippy -p lumen-shell --all-targets
--features v8 -- -D warnings` не перезапускался (код не менялся с S23/S24).

## S26 (P3, 2026-09-20) — добавлен независимый дренаж `pending_lazy_image_reqs` (предпосылка S25 закрыта); `:188`/`:1232` пока НЕ переведены на `defer_js_push=true`

Продолжение S25's «следующий срез должен спроектировать и добавить
независимый дренаж `pending_lazy_image_reqs`, не завязанный на «будет ли ещё
один `apply_relayout_result`»».

**Правка:** новый метод `Lumen::drain_pending_lazy_image_reqs`
(`relayout.rs`, `#[cfg(feature = "v8")]`) вызывается из
`about_to_wait.rs` сразу после `self.poll_engine_commit()` — тем же
паттерном, каким уже дренируются `dom_dirty`/`raf_pending` на каждом тике
парковочного цикла, как и предлагал S25. Чистая половина логики вынесена в
свободную функцию `take_pending_lazy_image_reqs(&Arc<Mutex<Vec<(u32,
String)>>>) -> Vec<(u32, String)>` (тот же приём, что `should_defer_query`
у THREAD-2 — тестируется без живого `Lumen`/сетевого стека), метод только
зовёт её и, если очередь не пуста, `fetch_and_register_lazy_images`. Теперь
`pending_lazy_image_reqs` дренируется ДВУМЯ независимыми путями: старый
(строка `apply_relayout_result`'s `:1054`, «подбирает, если следующий
relayout всё же случится») и новый (per-tick, не зависит от того, будет ли
следующий relayout вообще) — оба безвредно наступают на одну и ту же
пустую очередь, если другой уже забрал содержимое (`std::mem::take` на
`Mutex`-guard, без двойной отправки).

Доккомментарий поля `pending_lazy_image_reqs` (`lumen/state.rs`) обновлён —
теперь описывает оба пути дренажа и мотив независимого (страница может не
дождаться следующего relayout'а).

**Тесты:** три новых юнит-теста
(`relayout::bug935_s26_pending_lazy_image_drain_tests`) на чистую функцию —
дренаж пустой очереди, дренаж непустой (порядок сохранён, слот пуст после),
повторный дренаж после первого — пуст. `cargo clippy -p lumen-shell
--all-targets --features v8 -- -D warnings` чист, `cargo test -p lumen-shell
--bins --features v8 -- relayout engine_thread` — 48/48 зелёных (было 45,
три новых теста).

**Не сделано:** сама конверсия `:188` (`relayout()`)/`:1232`
(`poll_engine_commit`) на `defer_js_push=true` — этот срез закрывал ровно
предпосылку («находка C» S25), которая её блокировала, саму конверсию не
делал (S25's «то только после этого... и то отдельным экспериментом с
census'ом»). Основной симптом бага (M4-incremental routing мёртв под
движковым потоком, трек S11-S20) по-прежнему не тронут этим срезом.

**Следующий срез должен:** теперь, когда независимый дренаж существует,
перевести `:188`/`:1232` на `defer_js_push=true` (по образцу S17's `:410`)
— находка B (S25) уже подтвердила отсутствие синхронных читателей
результата push для обоих вызывающих, находка C закрыта этим срезом — и
затем измерить эффект на `QUERY_TIMEOUT`-налог census'ом (S18-S20's метод)
на `lenta.ru`.

## S27 (P3, 2026-09-20) — `:188`/`:1232` переведены на `defer_js_push=true`; census на `lenta.ru` показывает снижение среднего scroll RTT

Продолжение S26's «следующий срез должен... перевести `:188`/`:1232` на
`defer_js_push=true`».

**Правка:** `relayout()` (`relayout.rs:188`) и `poll_engine_commit`
(`relayout.rs:1251`, было `:1232` до S26's дополнений) теперь зовут
`apply_relayout_result(.., true)` вместо `false` — по образцу S17's `:410`.
`readback_relayout_job` (`:1223`) НЕ тронут — S25's находка A показала, что
путь мёртв в проде (единственный вызывающий гарантированно возвращается
раньше по `engine_thread.is_none()`), менять там нечего в смысле живого
поведения. Doc-комментарий `apply_relayout_result` обновлён: раньше
утверждал, что `true` передаёт только `try_relayout_raf_incremental` —
теперь перечисляет всех трёх `true`-вызывающих и оставляет явную пометку
про мёртвый `readback_relayout_job`.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист. `cargo test -p lumen-shell --bins --features v8 --
relayout engine_thread` — 48/48 зелёных, без изменений (S25/S26 уже
покрыли дренаж и таймаут юнит-тестами; сама конверсия флага не меняет
чистую логику `apply_relayout_result`, только какой из двух путей push'а
выбирается, что уже покрыто существующими интеграционными путями).

**Замер (census, `scripts/bug935_raf_relayout_census.py https://lenta.ru
--settle-s 20 --ticks 20`, `dev-release`, эта ветка):**

```
relayout до document_ready:     1
relayout за 20с settle:         1  (518ms, off-thread)
relayout за 20 scroll-тиков:    2  (214-250ms, off-thread)

scroll RTT ms: min=88.8 avg=12070.8 max=45844.8
```

Для сравнения — S11's замер ДО этого среза (тот же скрипт, тот же сайт,
те же параметры, до S12-S26): `avg=18030.2`, `min=388.7`, `max=59154.5`,
17/20 тиков выше 10с. Этот прогон: `avg=12070.8` (−33%), 4 тика ниже
10с (186, 89, 4956мс и один краевой 9555мс) против 3 у S11, максимум
45844.8мс против 59154.5мс. Направление — в сторону улучшения, но **не
строгий интерливед A/B**: `lenta.ru` — живой сайт, трафик рекламных
доменов (доминирующий вклад в RTT по S5/S11-S24) меняется от прогона к
прогону, а между S11 и этим срезом легло 15 промежуточных срезов
(S12-S26), часть из которых сама трогала смежный код (M4-роутинг,
`QUERY_TIMEOUT`, сетевые таймауты) — снижение нельзя приписать
исключительно этой конверсии без повторного интерливед-прогона на этой
же ветке против отката только этой правки. Полный stderr-лог прогона не
сохранён в репозиторий (временный `.tmp/`, не коммитится).

Ни одной паники, ни одного нового класса ошибок в логе (`grep -c
"panicked"` — 0; JS-ошибки — те же, что и в предыдущих срезах, от самого
сайта, не от движка).

**Не сделано:** строгий интерливед A/B (эта правка вкл/выкл на одном и
том же прогоне подряд) — S12-S20's метод для M4-роутинга, применимый и
здесь, не проводился в этом срезе из-за объёма (5+ минут живого времени
на каждую сторону, дважды). Основной симптом бага (M4-incremental
routing мёртв под движковым потоком, трек S11-S20) по-прежнему не
тронут — это отдельная, самая крупная находка, требующая собственного
census'а по интерливед-протоколу.

**Следующий срез должен:** либо (а) провести строгий интерливед A/B этой
конверсии (эта ветка vs `main` до неё) на `lenta.ru`, чтобы отделить
эффект правки от шума трафика, либо (б) вернуться к основному симптому
(M4-incremental routing) — S11's census уже подтвердил его количественно,
но интерливед A/B смены `||`-порядка (S12's попытка) откачен как
регрессия; там же остаётся открытым «растущий `lock2_wait` в
`compute_layout_incremental_restyle`» (S20's находка), который блокирует
повторную попытку M4-роутинга.

## S28 (P3, 2026-09-20) — строгий интерливед A/B S27's `defer_js_push=true`-конверсии: эффект неотличим от шума трафика, выигрыш S27 не подтверждён

Продолжение S27's «следующий срез должен либо (а) провести строгий
интерливед A/B этой конверсии (эта ветка vs `main` до неё) на `lenta.ru`».
Взят вариант (а).

**Метод:** в отличие от S27 (один прогон «после» против одного архивного
прогона «до» из S11, разделённых 15 промежуточными срезами и неизвестным
интервалом реального времени), в этом срезе оба состояния кода собраны и
измерены В ОДНОЙ сессии на одном и том же слоте, чередуя `true`/`false` в
`relayout()`'s (`:188`) и `poll_engine_commit`'s (`:1267`) вызовах
`apply_relayout_result`, с полной пересборкой `dev-release` между каждым
переключением — тот же census-скрипт
(`scripts/bug935_raf_relayout_census.py https://lenta.ru --settle-s 20
--ticks 20`), четыре прогона подряд, порядок true→false→true→false:

```
true  (S27-конверсия):  avg=17289.2ms  (run 1)
false (до S27):         avg=9587.5ms   (run 2)
true  (S27-конверсия):  avg=9700.6ms   (run 3)
false (до S27):         avg=13951.2ms  (run 4)
```

Среднее по группе `true`: 13494.9мс. Среднее по группе `false`: 11769.35мс.
Разброс ВНУТРИ каждой группы (9700.6…17289.2 для `true`, 9587.5…13951.2 для
`false`) больше разницы МЕЖДУ группами — тот же класс шума от рекламного
трафика (`kraken.rambler.ru`/`cnt.rambler.ru`/`top100.ru`), который S5/S11-S27
уже документировали как доминирующий вклад в `scroll` RTT на этом стенде.

**Вывод: направление S27 («−33%, avg 12070.8 против 18030.2») не
подтверждается строгим интерливедом — эффект конверсии `defer_js_push=true`
на `:188`/`:1267` неотличим от шума на этом стенде.** Это не значит, что
конверсия вредна (первый `true`-прогон был худшим из всех четырёх, но третий
`true`-прогон — лучшим из всех четырёх) — значит лишь, что `lenta.ru`'s
рекламный трафик слишком нестабилен, чтобы четырьмя прогонами отделить
эффект этой конкретной правки от фонового шума; нужна на порядок большая
выборка (10+ прогонов на сторону) или синтетический стенд без реального
рекламного трафика, чтобы дать окончательный ответ.

**Решение:** конверсия S27 (`:188`/`:1267` → `defer_js_push=true`) оставлена
как есть (код в этом срезе возвращён к состоянию `main` после измерения,
`git diff --stat` пуст) — она обоснована независимо от этого A/B: S23/S24
показали, что путь `route_query_js` был наблюдаемым источником многосекундных
блокировок UI-потока (51.7с за 2 вызова в одном прогоне), S25 подтвердила
отсутствие синхронных читателей результата push для обоих call site'ов, S26
закрыла риск потери `pending_lazy_image_reqs`. Отсутствие измеримого
улучшения RTT на зашумлённом стенде не отменяет устранённый риск затяжной
блокировки в худшем случае (long-tail, не среднее) — просто не подтверждает
конкретную числовую выгоду, заявленную S27.

**Не сделано:** основной симптом бага (M4-incremental routing мёртв под
движковым потоком, S1-S20's трек) по-прежнему не тронут. Найденный S20
кандидат-блокер (растущий `lock2_wait` в `compute_layout_incremental_restyle`,
0.3-0.6с окна, коррелирующие с рекламным трафиком) остаётся неустранённым и
неподтверждённым как единственная причина.

**Тесты:** правок в финальном состоянии кода нет (временные переключения
`true`/`false` для census'а откачены полностью) — `cargo clippy -p
lumen-shell --all-targets --features v8 -- -D warnings` не перезапускался
(код идентичен уже провалидированному S27 коммиту).

**Следующий срез должен:** вернуться к основному симптому бага —
либо (а) провести синтетический стенд без внешнего рекламного трафика
(локальный HTML с искусственным rAF+DOM-циклом и контролируемой задержкой
"сетевых" ответов через `wptserve`/локальный сервер), чтобы измерения этого
бага перестали зависеть от нестабильного живого сайта — этого не делал ни
один из 28 срезов, все использовали `ria.ru`/`lenta.ru`; либо (б) продолжить
S20's нить (инструментировать движковый поток вокруг `document.lock()` в
`compute_layout_incremental_restyle`, найти, что именно держит мьютекс в
0.3-0.6с окнах).

## S29 (P3, 2026-09-20) — синтетический офлайн-стенд построен и подтверждён: воспроизводит основной симптом (M4-роутинг) на порядок стабильнее живых сайтов, но пока НЕ годится для S27's `defer_js_push` A/B (нет JS-колбэков, которые тот путь ускоряет)

Продолжение S28's «следующий срез должен либо (а) провести синтетический
стенд без внешнего рекламного трафика... либо (б) продолжить S20's нить».
Взят вариант (а) — S28's `lenta.ru`-интерливед (S27's `defer_js_push=true`)
не разошёлся из-за шума рекламного трафика, который S5/S11-S28 уже
документировали как доминирующий и нестабильный вклад в `scroll` RTT.

**Стенд:** `scripts/perf-fixtures/bug935_raf_dom_stand.html` — офлайн
`file://`-фикстура (без единого сетевого запроса, по образцу
`scripts/perf-fixtures/input.html`): rAF-цикл трогает DOM каждый кадр
(таймкод + ширина полосы прогресса, та же форма мутации, что
videojs-плеер на `ria.ru`, «Корень» раздел выше) плюс длинный
скроллируемый контент для `scroll`-тиков. `scripts/bug935_raf_relayout_census.py`
принимает `file://`-URL без изменений (уже параметризован по `url`).

**Подтверждение симптома:** первый же прогон (`--settle-s 10 --ticks 10`)
дал 79-86 off-thread relayout'ов за 10с settle (20-30мс каждый, `dl=3484
styled=1162` — не растут, тот же класс, что открывающий репорт бага) —
непрерывный полный пересчёт каскада на rAF-тик, `try_relayout_raf_incremental`
не задействован, ровно диагноз «Корня». Два independent прогона на одной
и той же сборке разошлись всего на ~9% по числу relayout'ов (79 vs 86) и
на ~4% по средней `scroll` RTT (32.7 vs 31.3мс) — на порядки меньший
разброс, чем `lenta.ru`'s S28 (9587.5-17289.2мс между прогонами того же
кода).

**Попытка интерливед A/B S27's `defer_js_push=true`-конверсии на этом
стенде (`:188`/`:1267`, temp-правка на true/false, полная пересборка
между переключениями, 4 прогона true→false→true→false):**

```
true  (run1):  avg=34.8ms
false (run2):  avg=34.0ms
true  (run3):  avg=78.8ms
false (run4):  avg=87.3ms
```

Группы `true`/`false` не разошлись (как и в S28), но абсолютный уровень
шума вырос между run1-2 и run3-4 почти втрое на ОДНОМ и том же коде —
станочный шум за сессию (возможно тепловой троттлинг или фоновая
нагрузка на машине; не исследовано глубже, вне мандата этого среза),
подтверждающий, что даже детерминированный офлайн-стенд не освобождает
от протокола «чередующийся A/B, min, вращение порядка» (`docs/perf-method.md`
§4) — просто снимает НЕЗАВИСИМЫЙ источник шума (рекламный трафик), не
весь шум разом.

**Более важная находка — этот A/B в принципе не мог разойтись содержательно
на данном стенде.** S23/S24 показали, что цена `defer_js_push=false`
(блокирующий `route_query_js`) приходит от исполнения тяжёлых JS-колбэков —
`deliver_layout_observers`/`deliver_media_query_changes`/`deliver_lazy_images`
(`relayout.rs:1023`/`page_load.rs:2186`). Синтетическая фикстура этого
среза не регистрирует ни одного `IntersectionObserver`/`matchMedia`-листенера
и не содержит `<img loading=lazy>` — у неё физически нечему исполняться на
`route_query_js`-стороне push'а, поэтому `true`/`false` для неё эквивалентны
по конструкции, а не по измерению. Расхождение S27/S28 (влияет или нет)
остаётся неотвеченным этим срезом — стенд отвечает только на вопрос M4-
роутинга (для него он валиден и уже дал число), не на вопрос
`defer_js_push`.

**Не сделано:** фикстура не расширена JS-колбэками, которые нагружали бы
`route_query_js` (нужно для честного A/B `defer_js_push`); правка кода
`:188`/`:1267` не внесена (все temp-переключения возвращены,
`git diff --stat` пуст); основной симптом бага (M4-роутинг мёртв под
движковым потоком) по-прежнему не тронут — правка (смена `||`-порядка)
уже пробовалась и была откачена в S12 как регрессия (растущий `lock2_wait`,
S20), эта нить остаётся нетронутой.

**Тесты:** новый файл — офлайн HTML-фикстура, не Rust-код, гейтов не
требует. `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист (код `.rs` идентичен состоянию после S28 после отката
temp-правок).

**Следующий срез должен:** либо (а) расширить `bug935_raf_dom_stand.html`
`IntersectionObserver`/`matchMedia`-листенерами с намеренно небыстрым
колбэком (или `<img loading=lazy>` в достаточном числе), чтобы стенд
стал валидным инструментом именно для S27's вопроса, и повторить
интерливед A/B на нём; либо (б) вернуться к S20's нити (`lock2_wait` в
`compute_layout_incremental_restyle`) — теперь этот же офлайн-стенд можно
использовать и для неё (симптом M4-роутинга уже подтверждён на нём в
этом срезе), без риска, что рекламный трафик `lenta.ru` замаскирует
инструментацию, как это едва не случилось в S20.

## S30 (P3, 2026-09-20) — стенд расширен `IntersectionObserver`-нагрузкой (вариант (а)), интерливед A/B всё ещё не разошёлся — станочный шум растёт внутри самой сессии сильнее, чем в S29, и маскирует любой эффект

Продолжение S29's варианта (а): `scripts/perf-fixtures/bug935_raf_dom_stand.html`
получил `IntersectionObserver` на каждую из 480 `.cell` (60 секций × 8
ячеек) с намеренно небыстрым колбэком — детерминированная CPU-работа
(20000 итераций `Math.sqrt`/`Math.sin` на сработавшую запись, не заданное
время), так что `scroll`-тик census'а реально меняет intersection-состояние
и колбэк исполняется, а не остаётся мёртвым кодом. `matchMedia` сознательно
не добавлен — `bug935_raf_relayout_census.py` не делает `resize`, слушатель
media-query никогда бы не сработал.

**Подтверждение, что нагрузка реальна:** первый прогон на дефолте
(`defer_js_push=true`, как есть после S27) — `scroll RTT avg=44.2мс`
(22-54мс по восьми тикам), заметно выше S29's `31.3-34.8мс` на той же
машине без `IntersectionObserver` — колбэки действительно платят.

**Интерливед A/B `:1267`'s `defer_js_push` (true→false→true→false, полная
пересборка `lumen-shell` между переключениями, census 8с settle / 8 тиков
на каждом):**

```
true  (run1): scroll RTT avg=44.2мс  (relayout settle avg=24.5мс)
false (run2): scroll RTT avg=49.9мс  (relayout settle avg=29.6мс)
true  (run3): scroll RTT avg=66.5мс  (relayout settle avg=28.9мс, max=130.3мс — выброс)
false (run4): scroll RTT avg=219.3мс (relayout settle avg=131.8мс!)
```

Группы снова не разошлись направленно: `false` в run2 выше `true` в run1
(ожидаемо по гипотезе S23/S24), но `true` в run3 выше `false` в run2, и
`false` в run4 — почти в 5 раз выше run2's `false`. Решающая деталь —
**`relayout settle avg` сам вырос с 24.5-29.6мс (run1-3) до 131.8мс
(run4)**, хотя это чистое engine-side время вычисления layout
(`compute_layout_incremental_restyle`/полный cascade), которое `defer_js_push`
не трогает вообще — эта переменная физически не может объяснять рост.
Т.е. источник роста — не код, не `defer_js_push`, а станочный шум,
нарастающий за сессию (тот же эффект, что S29 уже зафиксировал между
run1-2 и run3-4 своей сессии, здесь воспроизведён и усилен третий раз
подряд на разных сессиях одной и той же машины) — вероятный кандидат
"тепловой троттлинг при непрерывной серии `cargo build` + `cargo run`",
не исследован глубже (вне мандата этого среза, нужен внешний термо-монитор
или пауза на остывание между прогонами, которых у среза нет).

**Вывод:** вариант (а) реализован и подтверждён (стенд теперь нагружает
именно `deliver_layout_observers`/`route_query_js`, что и было целью), но
он не решает исходную проблему S28/S29 — эту машину/сессию нельзя
использовать для интерливед A/B без охлаждения между прогонами или
внешнего термо-контроля, симптом маскируется станочным шумом на любом
стенде, офлайн или живом. `defer_js_push`'s эффект остаётся неизмеренным
трижды подряд (S28 живой сайт, S29 офлайн без нагрузки, S30 офлайн с
нагрузкой) по одной и той же причине — шум сессии, не сеть.

**Не сделано:** пауза на остывание/термо-мониторинг между прогонами;
код `:1267` возвращён в `true` (S27's дефолт), `git diff --stat` перед
коммитом показывает только фикстуру. Вариант (б) (S20's `lock2_wait`)
по-прежнему не тронут этим срезом.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист. Только `.html`-фикстура изменена, `.rs`-код идентичен
состоянию после S28/S29.

**Следующий срез должен:** либо (а) повторить этот A/B с паузой на
остывание между прогонами (например 60-120с простоя) или с внешним
термо-мониторингом, чтобы отличить станочный шум от эффекта кода; либо
(б) переключиться на S20's нить (`lock2_wait` в
`compute_layout_incremental_restyle`) — она не требует интерливед A/B
между двумя вариантами кода (это профилирование одного пути), так что
неустойчивость этой машины между прогонами её не блокирует так же, как
блокирует `defer_js_push`'s вопрос.

## S31 (P3, 2026-09-20) — env-var override убирает rebuild из A/B, и НАШЁЛ, что S28-S30's «станочный шум» — на самом деле детерминированная стоимость массового срабатывания IntersectionObserver в самой фикстуре, а не тепловой троттлинг

Продолжение S30's «следующий срез должен либо (а) повторить A/B с паузой на
остывание... либо (б) переключиться на S20's нить (`lock2_wait`)». Начат
путь (а), но иначе: вместо паузы на остывание — устранение самой причины,
по которой S28/S29/S30 вообще нуждались в паузе: `defer_js_push`'s
`true`/`false` был **литералом в коде** (`relayout.rs` `:188`/`:1267`),
так что каждое переключение требовало полной пересборки `lumen-shell`
(минуты) — именно этот межпрогонный интервал, не сама фаза измерения, и
копил «станочный шум» по S28/S29/S30's собственным наблюдениям.

**Правка (мера, не фикс):** новая `fn defer_js_push_override()`
(`relayout.rs`, top-level, `OnceLock`-кэш) читает
`LUMEN_BUG935_DEFER_JS_PUSH` (`"1"`/`"0"`/не задано) один раз за процесс и
подставляется как `.unwrap_or(true)` в обоих `:188`/`:1267` call site'ах —
при не заданной переменной поведение байт-в-байт то же, что и раньше
(оба сайта жёстко `true`). Переключение теперь — переменная окружения +
перезапуск процесса (секунды), не пересборка (минуты). `cargo clippy -p
lumen-shell --all-targets --features v8 -- -D warnings` чист.

**Дальше — интерливед A/B на S30's фикстуре (`bug935_raf_dom_stand.html`,
480 `.cell` с `IntersectionObserver`), без единой пересборки между
прогонами:**

Run1 (`=1`, 8с settle/8 тиков): `scroll RTT avg=13138.4мс`, но состав
значений бимодальный — `8790, 6, 25157, 45712, 34, 40, 157, 25212` (4
почти-мгновенных вперемешку с 4 многосекундными/22-45-секундными).
Run2 (`=0`): упал на первом же `scroll` — `automation command timed out`
(>65с `DEFAULT_TIMEOUT`, S11). Run3 (`=0`, после `taskkill` зависшего
процесса): упал ещё раньше — на самом `wait document_ready` (>20с). Run4
(`=1`, после очередного `taskkill`): снова упал на первом `scroll`.

**Найдено — это не шум, это чтение лога до конца:** stderr первого
упавшего прогона (`.tmp/bug935_census_stderr.log`) содержит буквально
`[engine] task 70359.01ms (engine-thread Task, from
crates\shell\src\relayout.rs:1029:21)` — **70.4 секунды** на одном
engine-thread `Task`-замыкании, на полностью офлайновом `file://`-стенде
без единого сетевого запроса. Это исключает саму гипотезу «шум = тепловой
троттлинг машины/рекламный трафик» для ЭТОГО стенда: тут физически нет
сети, которую можно было бы винить, а троттлинг не даёт замыканиям падать
ровно на строке, которая логически связана с нагрузкой самого теста.

`relayout.rs:1029` — это `route_task_js(...)` внутри `apply_relayout_result`
(`defer_js_push=true`-ветка, S17), чьё замыкание вызывает
`js.deliver_layout_observers()` — **ровно тот путь, который S30 намеренно
нагрузило** `IntersectionObserver`-колбэком (`busyWork(20000)`, 20 000
итераций `Math.sqrt`/`Math.sin` НА КАЖДУЮ пересёкшую запись). Фикстура
держит 480 `.cell` (60 секций × 8), и первый `scroll`-тик census'а — первый
раз, когда viewport вообще сдвигается с исходного положения — переводит
разом МНОГО (потенциально большую часть) из 480 ячеек через порог
пересечения одновременно, а не по одной постепенно, как было бы при
органическом пользовательском скролле. 480 × 20 000 = до 9.6 млн итераций
синхронно в одном V8-тике — на порядок больше, чем «один реалистичный
IntersectionObserver-колбэк», который S30 намеревалось смоделировать.

**Значит:** S28/S29/S30's «станочный шум, растущий за сессию, вероятный
тепловой троттлинг» — вероятно неверный диагноз для стенда S30 (с
IntersectionObserver-нагрузкой); настоящая причина огромного разброса —
детерминированная, зависящая от того, сколько ячеек накопили
непересечённое состояние к моменту первого `scroll` (что само зависит от
`--settle-s`, размера окна, положения скролла при старте) массовая
единовременная стоимость колбэков. Диагноз S29 (тот же паттерн БЕЗ
`IntersectionObserver`-нагрузки — стенд до S30) под вопросом не стоит,
там колбэков не было вовсе; диагноз S30 конкретно для ЭТОГО стенда,
скорее всего, спутал два разных источника разброса.

**Не установлено этим срезом:** не подтверждено количественно (не
поставлен `eprintln!` числа реально сработавших `IntersectionObserver`-
записей за тик), является ли масштаб пересечения единственной причиной
70.4с, или есть также вклад накопления pending-`Task`'ов в FIFO за
предыдущие упавшие/убитые прогоны (`taskkill`'ом) — каждый `taskkill`
обрывает процесс, не давая скрипту корректно завершить предыдущую сессию,
так что часть замера (`false`-Run2/Run3) могла унаследовать разбухшую
очередь от предыдущего процесса, если порт/файл лога не были чистым
new-start (проверено, что порт `free_port()`-случайный и процесс новый —
но накопленный видимый `.cell`-scroll-state внутри ОДНОГО процесса,
не между процессами, поэтому межпроцессное наследование маловероятно,
не исключено полностью).

**Вопрос `defer_js_push` (S27's A/B) по-прежнему НЕ решён** — обе стороны
(`=1` и `=0`) упёрлись в этот же артефакт стенда раньше, чем набралось
достаточно тиков для сравнения средних; отличить их эффект друг от друга
на этом стенде до его исправления нельзя.

**Не сделано:** сама фикстура не исправлена (нужно либо распределить
480 ячеек так, чтобы они пересекали порог постепенно по тикам, а не все
разом на первом, либо снизить `busyWork`, либо сделать `--ticks 1
--settle-s 0` первый прогон отдельным «прогревочным» тиком, который не
считается в статистику); основной симптом бага (M4-роутинг мёртв под
движковым потоком) по-прежнему не тронут. `git diff --stat` перед этим
коммитом: только `relayout.rs` (env-override) и этот раздел документа —
никаких temp-правок не осталось.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист. `.rs`-логика идентична предыдущему состоянию, когда
`LUMEN_BUG935_DEFER_JS_PUSH` не задана — поведенческого изменения для
обычного запуска нет.

**Следующий срез должен:** починить `bug935_raf_dom_stand.html`, чтобы
480 `IntersectionObserver`-ячеек не пересекали порог одним залпом на
первом тике (например расположить их так, чтобы каждый `scroll`-тик
открывал примерно одинаковое, небольшое число новых ячеек), затем
повторить `LUMEN_BUG935_DEFER_JS_PUSH=1`/`=0` A/B этим же
env-var-механизмом (уже не требует пересборки) — теперь это должно быть
быстро и не блокироваться ни станочным шумом, ни этим артефактом.

## S32 (P3, 2026-09-20) — фикстура починена (партиями вместо залпа), S31's 70с-артефакт устранён; найден и НЕ решён новый кандидат-корень: многосекундный Task-таймер не объясняется JS-колбэком (5-9мс), похоже на contention вокруг JS/V8-потока с непрерывным rAF-циклом самой фикстуры

Продолжение S31's «следующий срез должен починить `bug935_raf_dom_stand.html`,
чтобы 480 `IntersectionObserver`-ячеек не пересекали порог одним залпом на
первом тике... затем повторить `LUMEN_BUG935_DEFER_JS_PUSH=1`/`=0` A/B».

**Фикс фикстуры:** `io.observe()` больше не зовётся на все 480 `.cell` сразу
при загрузке — `growObservation()` наблюдает партиями по `OBSERVE_BATCH=8`
(одна секция): первая партия сразу при загрузке (как у реальной страницы,
где верхние ячейки видны сразу), следующие — по одной на каждое DOM-событие
`window`'s `scroll` (`_lumen_fire_window_scroll_event`, срабатывает ровно
раз на каждый MCP `scroll`-вызов — `scroll_by_delta`, `crates/shell/src/lumen/automation.rs:318`,
не анимирует шаг, шлёт один `scroll`-event на вызов).

**Подтверждено инструментально (временная правка, НЕ закоммичена — только
для диагностики этого среза, `.tmp/`-копия фикстуры с `console.log`):**
`entries.length` на каждой доставке — ровно **8**, каждый раз (наблюдено
шесть доставок подряд, `observedCount` растёт 8→48 линейно, ни одного
случая переисполнения уже наблюдаемых ячеек) — S31's диагноз подтверждён
количественно: батч устранил залповую доставку полностью, S31's «70.4с на
одном замыкании» в этом срезе не воспроизводится ни разу за несколько
прогонов.

**Новая находка (инструментальный `performance.now()` вокруг самого
`busyWork`-цикла внутри JS-колбэка, тот же временный `.tmp/`-стенд):** сам
JS-колбэк (8 записей × `busyWork(20000)`) стабильно исполняется за
**5-9мс** — на три порядка меньше, чем время самого Rust-side
`[engine] task NNNNms (engine-thread Task, from relayout.rs:1029:21)`,
которое в параллельных прогонах доходило до **1419мс/3275мс/5980мс/6921мс**
на тех же самых доставках (соседние по логу строки: `relayout NNms
(off-thread)` → `task NNNNms (…relayout.rs:1029:21)`, каждая пара —
один и тот же scroll-тик). Поскольку измерение `cbMs` идёт ИЗНУТРИ того же
самого JS-вызова, который `relayout.rs:1029`'s `route_task_js`-замыкание
оборачивает, разрыв в 1000-6900× между «работа внутри колбэка» и
«длительность всего Task» означает, что стоимость не в
`busyWork`/`deliver_layout_observers`'s самой доставке entries, а
где-то ДО входа в JS (`update_layout_rects`/`update_client_rects`/
`update_hit_test_tree`/`update_computed_styles`/`update_pseudo_computed_styles`/
`update_custom_properties`/`update_stylesheet`/`update_viewport_size` —
все они идут перед `deliver_layout_observers()` в этом же замыкании,
`relayout.rs` `:1029`-окрестность) или в самом входе/выходе в V8-изолят
(получение блокировки/эксклюзивного доступа к JS-потоку).

**Правдоподобный кандидат-механизм (не подтверждён этим срезом, только
гипотеза по форме улик):** фикстура держит непрерывный `requestAnimationFrame`-
цикл (`tick()`, `DURATION_MS=120000` — длиннее любого census-прогона),
который на каждый кадр трогает DOM (`textContent`/`style.width`). Если
`deliver_layout_observers` и весь остальной список `js.update_*`-вызовов
в этом Task-замыкании должны исполниться НА ТОМ ЖЕ JS/V8-потоке, что и
сам `rAF`-цикл (не подтверждено чтением кода в этом срезе — только по
форме симптома), а `rAF`-цикл перепланирует себя на каждый кадр без пауз,
Task может физически ждать своей очереди на JS-потоке между кадрами
`tick()` — то же семейство находок, что и S20's `lock2_wait` в
`compute_layout_incremental_restyle` (тоже необъяснённый многосекундный
contention рядом с активным rAF), только на этот раз наблюдаемый на
детерминированном офлайн-стенде, а не на зашумлённом `lenta.ru`. Этот
офлайн-стенд впервые делает такое измерение воспроизводимым без
рекламного трафика — именно то, чего не хватало S20-S31.

**Не сделано:** сам механизм не идентифицирован до конкретной строки —
нужны суб-таймеры внутри `relayout.rs:1029`'s замыкания (обернуть каждый
`js.update_*`/`deliver_*`-вызов собственным `Instant::now()`, не только
общий) и/или подтверждение, что `rAF`-callback'и `tick()` в фикстуре
действительно исполняются на том же потоке/изоляте, которым JS-биндинг
пользуется для `route_task_js`. Вопрос S27's `defer_js_push` A/B
по-прежнему не решён — обе стороны (`=1`/`=0`) нужно перемерить уже на
этом почищенном стенде, но следующий срез разумнее начать с диагностики
самого этого нового многосекундного Task, а не сразу с A/B, потому что он
доминирует над RTT сильнее, чем разница между `=1`/`=0`, и может
маскировать эту разницу так же, как S28-S31's артефакты маскировали её
раньше.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` чист. `git diff --stat` перед этим коммитом — только
`scripts/perf-fixtures/bug935_raf_dom_stand.html` (батчинг наблюдения) и
этот раздел документа; временная `.tmp/`-копия с `console.log`/`performance.now()`
инструментацией НЕ закоммичена (`.tmp/` в `.gitignore`) — использовалась
только для диагностики внутри этого среза и осталась в рабочем каталоге
как справочный артефакт, не как часть репозитория.

**Следующий срез должен:** обернуть каждый шаг `relayout.rs:1029`'s
Task-замыкания собственным таймером (`update_layout_rects` … `deliver_lazy_images`
… `update_scroll_states`), прогнать census на этом же офлайн-стенде и
найти, какой конкретно шаг съедает секунды — если гипотеза про JS/V8-поток
contention с `tick()`'s rAF-циклом подтвердится, это отдельный, более
крупный кандидат-корень, чем S20's узко описанный `lock2_wait`, и он
может быть тем самым механизмом, который годами маскировал S27's
`defer_js_push` A/B на живых сайтах, а не только рекламный трафик.

## S33 (P3, 2026-09-21) — S32's многосекундный `deliver_layout_observers` объяснён и локализован до конкретного механизма: CSSOM-4's same-tick flush перезапускает ПОЛНЫЙ layout на КАЖДЫЙ `_lumen_get_bounding_rect`, пока страничный `dom_dirty` не потреблён — не точечный баг одного места, а системное дублирование работы

Продолжение S32's «следующий срез должен обернуть каждый шаг
`relayout.rs:1029`'s Task-замыкания собственным таймером... и найти, какой
конкретно шаг съедает секунды».

**Шаг 1 — таймер на каждый под-шаг замыкания (`relayout.rs`, `defer_js_push=true`
ветка, макрос `timed_step!`, гейт `LUMEN_FRAME_LOG`, как и остальная
диагностика в этом файле):** census на S32's починенном офлайн-стенде
(`bug935_raf_dom_stand.html`, партиями по `OBSERVE_BATCH=8`) сразу показал
виновника без всякой неоднозначности — из всех шагов (`update_layout_rects`
… `update_scroll_states`) единственный дорогой это `deliver_layout_observers`
(6231мс и 8230мс в двух соседних тиках одного прогона), все прочие — доли
миллисекунды или единицы миллисекунд (`update_computed_styles` 34.33мс,
`update_hit_test_tree` 4.32мс — заметны, но на три порядка меньше).

**Шаг 2 — почему `deliver_layout_observers` дорог, если JS-колбэк внутри него
(S32) — 5-9мс:** прочитан `_lumen_get_bounding_rect`
(`crates/js/src/v8_runtime/install/platform.rs:187`) — нативный биндинг,
которым `_lumen_deliver_intersection_observers()`
(`web_api_shim_mid_b2.js:2343`) снимает координаты КАЖДОЙ наблюдаемой цели на
КАЖДЫЙ вызов (цикл `for ei in obs._observations`). Первая строка биндинга —
`flush_bcr.maybe_flush()` (CSSOM-4/BUG-493, `crates/js/src/v8_runtime/style_flush.rs:134`)
— «forced synchronous style+layout flush», задуманный как поддержка
same-tick чтения геометрии после DOM-мутации в ТОМ ЖЕ скрипте. Ключевая
деталь конструкции: гейт «нужен ли реальный флаш» смотрит на
**страничный** `self.dom_dirty` (тот же `Arc<AtomicBool>`, который
`pump_raf_engine_thread`/`take_dom_dirty_lockfree()` использует для решения
«нужен ли relayout в следующем тике») **напрямую через `.load()`** — и
`maybe_flush` **никогда его не потребляет** (собственный doc-комментарий
`FlushHandles::dom_dirty`, `runtime.rs:151`: «peeked, never cleared by the
flush»). Значит если флаг `true` в НАЧАЛЕ вызова `deliver_layout_observers`
(рутинно для страницы с непрерывным rAF+DOM-циклом — предыдущий кадр rAF
трогает DOM, а `take_dom_dirty_lockfree()`'s единственное потребление
происходит НА UI-потоке ДО того, как этот же relayout уходит вычисляться
асинхронно, и к моменту, когда `apply_relayout_result` реально добирается
до `deliver_layout_observers` НА ДВИЖКОВОМ потоке, дальнейшие rAF-тики уже
успели выставить флаг снова), то он остаётся `true` **на всю длину цикла**
— КАЖДЫЙ вызов `_lumen_get_bounding_rect` внутри одной доставки
IntersectionObserver запускает СВОЙ полный `lumen_layout::layout_measured_with_counters`
(`style_flush.rs:192`), не один раз на скрипт-тик, а по разу на
наблюдаемую цель.

**Шаг 3 — подтверждено измерением, не только чтением кода:** добавлен
диагностический (постоянный, под `LUMEN_FRAME_LOG`) `eprintln!` в
`maybe_flush` на пути РЕАЛЬНОГО (не no-op) флаша. Тот же census: число
таких «реальных» флашей за один `deliver_layout_observers`-вызов — **8, 8,
16, 24, 32** (пять последовательных вызовов, растёт линейно вместе с
`observedCount` — S32's батчинг добавляет 8 новых `.cell` на каждый
`scroll`), т.е. ровно по одному полному layout'у на каждую наблюдаемую
ячейку, а не один на весь скрипт-тик. Это количественно объясняет и рост
RTT по ходу census'а (543→8357мс в S32, 36→14864мс в этом срезе — разброс
между прогонами ожидаем, механизм тот же): стоимость растёт с числом
наблюдаемых целей, а не зависит от `busyWork`'а самого колбэка.

**Почему это не дефект IntersectionObserver-реализации конкретно:** тот же
`_lumen_get_bounding_rect` — общий бэкенд `getBoundingClientRect()`,
`ResizeObserverEntry`, canvas CSS resize; любой JS-код, читающий геометрию
НЕСКОЛЬКИХ узлов подряд в одном тике на странице с ещё не потреблённым
`dom_dirty` (типично — любая интерактивная страница с rAF-циклом), платит
ту же цену, умноженную на число узлов. `IntersectionObserver` с 8-32
целями просто первый потребитель этого паттерна, который довёл проблему
до многих секунд на офлайн-стенде.

**Правка НЕ внесена** — фикс не точечный по тем же причинам, что
`relayout_raf_dirty`'s M4-роутинг: `self.dom_dirty` — общий бит с ДРУГИМ
потребителем (`pump_raf_engine_thread`'s планировщик relayout'а), так что
`maybe_flush` не может просто `swap(false, ...)`'ить его при первом реальном
флаше — это скрыло бы от планировщика факт «страница мутировала DOM» и
пропустило бы легитимный relayout/перерисовку следующего кадра (тот, что
реально обновляет display list, а не только JS-видимую геометрию).
Корректный фикс — завести ОТДЕЛЬНЫЙ флаг «данные флаша устарели с
последнего успешного `maybe_flush`», выставляемый в `true` в каждом месте,
где сейчас выставляется страничный `dom_dirty` (несколько сайтов в
`dom_core.rs`/`platform.rs`, не проверено, сколько именно), и в `false` —
в конце успешного `maybe_flush`. Это трогает каждый DOM-мутирующий
JS-биндинг, а не одну функцию — требует отдельного среза для
инвентаризации всех call site'ов и последующей проверки, что развязка двух
флагов не ломает CSSOM-4's исходный контракт (BUG-493) ни в одном из уже
покрытых им кейсов.

**Сделано в этом срезе (постоянные правки, `git diff --stat`: только
`crates/shell/src/relayout.rs` — таймер на каждый под-шаг Task-замыкания
`defer_js_push=true`-ветки — и `crates/js/src/v8_runtime/style_flush.rs` —
диагностический лог реального флаша):** обе правки — чистая диагностика
под `LUMEN_FRAME_LOG` (`lumen_paint::frame_log_enabled()`, тот же гейт,
что и вся остальная профилировка в этом файле), нулевая стоимость на
обычном прогоне, тот же паттерн, что S12's `restyle={0|1}`-поле оставило
постоянным. Код вне гейта не менялся.

**Тесты:** `cargo clippy -p lumen-shell --all-targets --features v8 -- -D
warnings` и `cargo clippy -p lumen-js --all-targets --features v8-backend
-- -D warnings` чисты. `cargo test -p lumen-js --features v8-backend --lib`
— 4016/4017 (единственный красный,
`worker::tests_v8::v8_worker_zero_delay_interval_is_clamped_and_yields`,
таймингово-флакующий тест в `worker.rs`, не тронутом этим срезом —
зелёный в изоляции повторным прогоном, не регрессия). `cargo test -p
lumen-shell --features v8 -- relayout` — 13/13 зелёных.

**Следующий срез должен:** (1) инвентаризировать все места, выставляющие
`self.dom_dirty`/`state.dom_dirty` в `true` при DOM-мутации
(`dom_core.rs`, `platform.rs` и, вероятно, другие `install_*`-модули); (2)
завести отдельный `flush_stale: Arc<AtomicBool>` в `FlushHandles`,
выставляемый вместе с каждым таким сайтом и сбрасываемый в конце успешного
`maybe_flush`; (3) заменить прямой `self.dom_dirty.load(...)` в гейте
`maybe_flush` на этот новый флаг, оставив страничный `dom_dirty` только
планировщику; (4) перемерить census — ожидаемый эффект: `deliver_layout_observers`
падает до одного полного layout'а на скрипт-тик (или нуля, если ничего не
менялось), а не по одному на наблюдаемую цель. Вопрос S27's `defer_js_push`
A/B по-прежнему не решён и должен ждать этого фикса — сейчас он
маскируется этим механизмом настолько же, насколько маскировался
S28-S31's артефактами.

**Срез 34 (2026-09-21):** S33's фикс внесён. `V8JsRuntime` и
`FlushHandles` больше не делят один `dom_dirty: Arc<AtomicBool>` между
планировщиком (`take_dom_dirty`/`take_dom_dirty_lockfree`) и same-tick
флашем — заведён отдельный `flush_stale: Arc<AtomicBool>`, выставляемый
в `true` на КАЖДОМ существующем сайте `dom_dirty.store(true, ...)`
(инвентаризация нашла ровно шесть `install_*`-функций, принимающих
`dom_dirty` как параметр: `install_node_properties`,
`install_tree_mutation`, `install_shadow_dom`, `install_selection`,
`install_design_mode` в `crates/js/src/v8_runtime/install/dom_core.rs` и
`install_crypto_and_typed_om` в
`crates/js/src/v8_runtime/install/platform.rs` — все остальные ссылки на
`dom_dirty` в `install/` были либо объявлением параметра, либо чтением в
другом месте, не установкой). `FlushHandles::maybe_flush` теперь читает
и сбрасывает только `flush_stale`; страничный `dom_dirty` этой структурой
больше не хранится (поле удалено как мёртвое — `dead_code` от clippy) и
остаётся исключительно сигналом планировщика
(`V8JsRuntime::take_dom_dirty`/`InProcessSession::take_dom_dirty_lockfree`),
который эта правка не трогает.

Перемер S29's офлайн-стенда (`bug935_raf_dom_stand.html`, с S30-32's
`IntersectionObserver`-нагрузкой) после фикса: scroll RTT min/avg/max =
47.2/62.5/108.3мс на 20 тиках, 23 off-thread relayout'а по 27-38мс каждый
— против S32/S33's 543→8357мс/36→14864мс роста по ходу того же census'а
на том же стенде (там же зафиксирован механизм — один полный layout на
каждую наблюдаемую `IntersectionObserver`-цель). Направление и порядок
величины совпадают с S33's прогнозом: `deliver_layout_observers` больше
не оплачивает N полных layout'ов за один скрипт-тик.

**Тесты:** `cargo clippy -p lumen-js --all-targets --features v8-backend
-- -D warnings` и `cargo clippy -p lumen-shell --all-targets --features v8
-- -D warnings` чисты. `cargo test -p lumen-js --features v8-backend --lib`
— 4007/4017 зелёных в общем прогоне, 10 красных (все в
`dom::tests::v8_webworker`, не тронутом этим срезом) — в изоляции
(`--test-threads=1` на всём модуле) все 51 тестов этого модуля зелёные,
т.е. это тайминговый флак параллельного запуска реальных worker-потоков,
не регрессия. `cargo test -p lumen-shell --features v8 -- relayout` —
13/13 зелёных.

Вопрос S27's `defer_js_push` A/B по-прежнему не решён — он был замаскирован
этим механизмом, теперь маска снята, но перемер самого A/B — отдельный
следующий срез.

**Срез 35 (2026-09-21):** перемер S27's `defer_js_push` A/B на S29's
офлайн-стенде (`bug935_raf_dom_stand.html`, с S30-32's
`IntersectionObserver`-нагрузкой) через `LUMEN_BUG935_DEFER_JS_PUSH`
(`relayout.rs:20`, без пересборки — только рестарт процесса), теперь что
S34 снял маску same-tick-флаша. Шесть чередующихся прогонов (`true`,
`false`, `true`, `false`, `true`, `false`, по 20 scroll-тиков каждый);
первый прогон (`true`) отброшен как разогрев (scroll RTT avg 323.8мс —
кратно выше всех последующих одного и того же режима). Оставшиеся пять:
`true` avg scroll RTT 111.4/127.8мс (2 прогона), `false` avg 107.2/108.6/
137.3мс (3 прогона) — диапазоны перекрываются полностью, разница внутри
одного режима (107.2 vs 137.3мс у `false`) больше разницы между режимами.
Вывод: теперь, когда S34 убрал маскирующий механизм, `defer_js_push` не
даёт измеримого эффекта на этом стенде ни в одну сторону — S28's вывод
(«разброс больше эффекта») воспроизводится и после фикса, не был
артефактом самой маски. Конверсия `relayout`/`poll_engine_commit` на
`defer_js_push=true` (S27) остаётся в силе как есть — обоснование было
снятием риска long-tail блокировки JS-колбэком, не заявленным ускорением,
и это обоснование не зависело от результата A/B. Вопрос закрыт: дальше
искать нечего на этом канале, пока не появится стенд с иным профилем
JS-нагрузки, где `defer_js_push` мог бы проявиться.

## S36 (P3, 2026-09-21) — `||`-своп повторён пятый раз на S29's чистом (без рекламного трафика) стенде: S20's `lock2_wait`-контеншен не воспроизводится, но найден новый, архитектурный источник — `apply_relayout_result` сам стоит 65-104мс независимо от layout'а, и своп переносит ЭТУ стоимость на UI-поток

Продолжение с того места, где остановился S20 (последняя попытка `||`-свопа,
отменена из-за растущего `lock2_wait` — контеншена на `Mutex<Document>` с
движковым потоком, наблюдавшегося только на зашумлённом `lenta.ru` и не
подтверждённого как единственная причина). S21-S35 занимались отдельными
находками того же файла (сетевые таймауты, `defer_js_push`, CSSOM-4
same-tick флаш) и не возвращались к самому свопу. У S29 к этому моменту уже
есть чистый офлайн-стенд без единого сетевого запроса
(`scripts/perf-fixtures/bug935_raf_dom_stand.html`), ускоренный фиксом S34
(scroll RTT 47-108мс вместо роста до 8-15с) — впервые появилась возможность
повторить своп там, где `lock2_wait`-гипотеза S20 (которая требовала
конкурентного тяжёлого JS на движковом потоке — рекламные скрипты) заведомо
не может сработать, и увидеть эффект самого M4-роутинга изолированно.

**Метод:** `||`-порядок в `relayout_raf_dirty`/`relayout_raf_dirty_readback`
временно свопнут (идентично S12/S14/S18/S20 — `try_relayout_raf_incremental`
пробуется первым), `cargo build --profile dev-release -p lumen-shell
--features v8`, census (`scripts/bug935_raf_relayout_census.py` с
`file://`-URL стенда, `--settle-s 5 --ticks 20`) прогнан один раз на
baseline (исходный порядок) дважды подряд и один раз на свопнутом коде —
не строгий интерливед на 10+ прогонов (не оправдан для первой проверки
новой гипотезы на детерминированном стенде — шум между baseline-прогонами
уже показателен сам по себе, см. ниже).

**Baseline (исходный код, без свопа), два прогона подряд:**

```
run 1: scroll RTT avg=78.6ms  (off-thread relayout 23.2-41.9ms each)
run 2: scroll RTT avg=176.4ms (off-thread relayout 19.7-27.7ms each)
```

Сам relayout стабилен (19-42мс на обоих прогонах — детерминированный
стенд ведёт себя предсказуемо на уровне работы движка), но `scroll` RTT
плавает в 2+ раза между двумя прогонами на одном и том же бинарнике без
единого изменения кода — станочный шум (не сетевой, стенд офлайновый)
достаточно велик даже здесь, чтобы обесценить одиночное сравнение
"было/стало"; вывод ниже опирается на структуру лога (`lock2_wait`,
`apply_ms` по отдельным полям), не только на среднее RTT.

**Свопнутый код, один прогон:**

```
relayout за 5с settle: 20 (incremental, on-thread), restyle=1 in 19/20
relayout за 20 scroll-тиков: 34 (incremental, on-thread), restyle=1 in 34/34
  settle:  65.7-140.2ms each (avg ~103.6ms)
  scroll:  65.7-104.6ms each (avg ~79.0ms)
scroll RTT avg=93.9ms
```

**S13/S17-фиксы подтверждены ещё раз:** cascade-skip путь достигается почти
всегда (53/54 тиков `restyle=1`), JS-push не блокирует (`defer_js_push` уже
`true` для этого call site с S17). **S20's `lock2_wait`-гипотеза НЕ
воспроизводится:** `lock2_wait_ms=0.00` на каждой без исключения записи
`restyle-wrapper` за весь прогон (нет реального конкурента за
`Mutex<Document>` на движковом потоке — стенд не открывает ни одного
сетевого соединения и не гоняет тяжёлый сторонний JS) — подтверждает
диагноз S20 «контеншн, а не что-то по природе алгоритма», раз убрать
источник контеншна (рекламный трафик) — и контеншн пропадает вместе с ним.

**Но найден новый, не связанный с контеншном источник:** `apply_ms`
(время внутри `apply_relayout_result`, тот же параметр из S15) — **65-93мс
на КАЖДОМ тике**, не только на выбросах, и это составляет 90%+ всего
`incr_ms` (пример: `relayout 68.10ms … apply_ms=64.76`). Само измерение
кэш-переиспользующей части (`restyle-wrapper`, `layout_streaming_incremental`
и т.д.) остаётся <5мс, как и в S12-S18 — расхождение не в стоимости layout'а
(та по-прежнему почти нулевая благодаря cascade-skip), а в стоимости
самой функции `apply_relayout_result`, вызываемой ПОСЛЕ layout'а.

Чтение `apply_relayout_result` (`relayout.rs:831-` и далее) показывает,
почему она объективно недёшева независимо от того, откуда вызвана: за один
проход она (1) синхронизирует переходы (`transition_scheduler.sync`/
`cancel_missing`) по каждому узлу нового дерева стилей, (2) диффит и кэширует
весь `DisplayList` (`tile_grid.update_from_diff`, `hash_commands`,
`display_list_cache.insert`), (3) промоутит `will-change`-слои, (4)
обновляет snap- и scroll-контейнеры, (5) резолвит `@starting-style` для
входящих узлов — ни один из этих шагов не относится к M4-инкрементальности
layout'а как таковой (та экономит именно на каскаде/раскладке дерева), это
отдельная, самостоятельно дорогая работа, которую ЛЮБОЙ производитель
relayout'а (полный `relayout()`, off-thread `submit_relayout_job`,
инкрементальный `try_relayout_raf_incremental`) обязан заплатить одинаково.

**Почему это не было видно раньше:** под исходным (не свопнутым) порядком
`apply_relayout_result` тоже вызывается на каждый тик — но внутри
`submit_relayout_job`'s асинхронной engine-thread задачи, то есть эти же
65-90мс списываются с ДВИЖКОВОГО потока, который на этом стенде (в отличие
от `lenta.ru`) простаивает между тиками и может себе это позволить без
видимых последствий для UI. Своп переносит `try_relayout_raf_incremental`
целиком на UI-поток — и вместе с ним эту же, никак не сокращённую M4-путём,
стоимость `apply_relayout_result`. Это ТРЕТИЙ самостоятельный механизм,
объясняющий, почему прямой своп `||`-порядка не даёт чистого выигрыша (после
S15's `route_query_js`-раунд-трипа, устранённого S17, и S20's
`lock2_wait`-контеншена, воспроизводимого только с реальным сторонним JS):
сама `apply_relayout_result`, а не что-либо специфичное для инкрементального
пути, стоит порядка одного кадра (60-90мс на этом железе) и не должна
исполняться синхронно на UI-потоке ни при каком порядке веток.

**Действие:** своп отменён (`git checkout -- crates/shell/src/relayout.rs`,
`git diff --stat` пуст перед коммитом этого среза). Новых постоянных правок
кода нет — весь эффект среза текстовый (этот раздел); существующая
инструментация S12-S19 (`outer_ms`/`incr_ms`/`apply_ms`/`lock2_wait_ms`) уже
покрывала всё нужное для этого измерения, ничего нового добавлять не
потребовалось.

**Не сделано / следующий срез:** основной симптом бага (M4-incremental
routing не работает под дефолтной сборкой) по-прежнему открыт. Прямой своп
`||`-порядка теперь дискредитирован ТРИЖДЫ независимыми механизмами
(route_query_js-раунд-трип — устранён S17; `Mutex<Document>`-контеншн с
реальным сторонним трафиком — S20, не устранён; собственная стоимость
`apply_relayout_result` — этот срез, не устранена) — дальнейшие попытки
голого свопа без предварительного устранения хотя бы этой третьей находки
предсказуемо повторят тот же результат. Настоящий следующий шаг — решить,
можно ли и как вынести саму `apply_relayout_result` (или её дорогие
шаги 1-5 выше) на движковый поток НЕЗАВИСИМО от того, каким путём получен
`LayoutBox`/`DisplayList` (то есть отвязать "кто посчитал layout" от "кто
применяет результат", а не просто гонять `try_relayout_raf_incremental`
целиком на UI-потоке) — это архитектурная развилка (аналогичная S15's
вариантам (а)/(б) для JS-push, только теперь для всей функции целиком), не
однострочная правка, и требует отдельного протокола измерения на этом же
стенде до входа в код.

## S37 (P3, 2026-09-23) — `apply_relayout_result`'s 65-93ms найден и разобран по фазам: единственный доминирующий вклад — `collect_computed_styles`, полная ре-сериализация вычисленных стилей ВСЕГО дерева layout на каждый тик

Продолжение с того места, где остановился S36 («настоящий следующий шаг —
решить, можно ли и как вынести саму `apply_relayout_result` ... это
архитектурная развилка ... требует отдельного протокола измерения на этом
же стенде до входа в код»). Прежде чем решать архитектурный вопрос,
измерил, ЧТО внутри `apply_relayout_result` стоит эти 65-93мс — S36 знал
только итоговое число (`apply_ms`), сама функция была чёрным ящиком:
единственная существовавшая инструментация (S33's `task-step`) таймит
ТОЛЬКО отложенный JS-push (`route_task_js`'s замыкание), а не синхронный
префикс, который выполняется ДО него независимо от `defer_js_push`.

**Метод:** в `apply_relayout_result` (`relayout.rs`) добавлен макрос
`apply_step!` (тот же паттерн, что S33's `timed_step!` — гейт
`lumen_paint::frame_log_enabled()`, нулевая стоимость на обычном прогоне)
и обёрнуты все крупные фазы функции: `frame_sync`, `dl_splice_diff_cache`,
`transitions_sync`, `transitions_cancel_missing`, `starting_style`,
`js_geometry_collect` (весь блок `collect_layout_rects`/`collect_client_rects`/
`collect_computed_styles`/`collect_pseudo_computed_styles`/
`collect_custom_properties`/`collect_scroll_containers_for_js_state` —
S37 confirmed this runs synchronously regardless of `defer_js_push`, only
the `js.update_*`/`js.deliver_*` push that consumes it is deferred),
`js_push_spawn_nonblocking`/`js_push_blocking` (обёртка вокруг самого
`route_task_js`/`route_query_js` вызова) и `cv_snap_scroll_state`.
`js_geometry_collect` затем разобран на под-таймеры по каждому
`collect_*`-вызову отдельно (второй проход этого же среза).

`||`-своп `relayout_raf_dirty` (S12/S14/S18/S20/S36's приём) временно
внесён СНОВА ради самого измерения (нужен путь `try_relayout_raf_incremental`
на UI-потоке, где стоит `apply_ms`), census (`scripts/bug935_raf_relayout_census.py`
на S29's чистом офлайн-стенде `bug935_raf_dom_stand.html`, `--settle-s 5
--ticks 10`) прогнан дважды подряд (первый — только с блочным
`apply_step!` на фазах, второй — с уже добавленным разбором
`js_geometry_collect`), своп **отменён** после обоих прогонов
(`git diff --stat` перед коммитом — только сама инструментация).

**Первый прогон (фазовый разбор), 34 записи:**

```
js_geometry_collect          n=33 avg=95.64ms  total=3156.14ms
transitions_sync             n=34 avg=12.14ms  total=412.61ms
dl_splice_diff_cache         n=34 avg=13.58ms  total=461.57ms
cv_snap_scroll_state         n=34 avg=0.30ms
starting_style                n=34 avg=0.00ms
js_push_spawn_nonblocking    n=33 avg=0.01ms
frame_sync                    n=34 avg=0.00ms
transitions_cancel_missing    n=34 avg=0.00ms
```

`js_geometry_collect` — 88% суммарного времени всех фаз этого прогона,
на порядок дороже второго по стоимости шага (`dl_splice_diff_cache`,
tile-grid diff + display-list-cache insert + hash). `js_push_spawn_nonblocking`
подтверждает S17/S35: сам факт постановки `route_task_js`-замыкания в
очередь стоит копейки (0.01мс) — весь эффект `defer_js_push` — это то, что
происходит ВНУТРИ отложенного замыкания, а не задержка на постановку в
очередь, что этот срез и делал предметом дальнейшего разбора не по этой
оси, а по оси «что стоит дорого ДО push'а».

**Второй прогон (разбор `js_geometry_collect` по `collect_*`), 28 записей:**

```
js_geometry_collect            n=28 avg=104.06ms total=2913.81ms
  collect_computed_styles      n=28 avg=92.96ms  total=2602.95ms  (89% js_geometry_collect)
dl_splice_diff_cache           n=28 avg=15.11ms  total=423.00ms
transitions_sync               n=28 avg=11.41ms  total=319.53ms
collect_pseudo_computed_styles n=28 avg=4.65ms
clone_hit_test_tree            n=28 avg=3.51ms
collect_client_rects           n=28 avg=1.18ms
collect_layout_rects           n=28 avg=0.79ms
layout_shift_score             n=28 avg=0.41ms
cv_snap_scroll_state           n=28 avg=0.33ms
collect_custom_properties      n=28 avg=0.10ms
collect_scroll_containers      n=28 avg=0.08ms
```

**Корень найден однозначно: `collect_computed_styles`** (`crates/engine/layout/src/lib.rs:1555`)
— единственный доминирующий вклад, 89-93мс из 92-104мс всей фазы сбора
геометрии/стилей для JS, что совпадает по порядку величины с S36's
`apply_ms=65-93ms` (разница — станочный шум между сессиями, стенд тот
же). Все остальные `collect_*`-функции того же семейства (rects, custom
properties, scroll-контейнеры) на том же дереве стоят **на два порядка
меньше** (0.1-4.7мс) — дороговизна не в самом факте прохода по дереву, а
конкретно в этой функции.

**Почему `collect_computed_styles` так дорога:** `collect_computed_styles_rec`
проходит КАЖДЫЙ `LayoutBox` дерева (плюс каждый инлайн-сегмент) и на
каждом зовёт `computed_style_to_map(&b.style)` — сериализует ~55
CSS-свойств из внутреннего `ComputedStyle` в `HashMap<String, String>`
(строковое представление, готовое к выдаче в `getComputedStyle()`).
Стоимость линейна по числу узлов **и** не кэшируется между тиками: даже
когда cascade-skip путь (`restyle=1`, BUG-341/BUG-935 S13) переиспользует
САМ `ComputedStyle` для непереехавших узлов и не пересчитывает каскад,
`collect_computed_styles` всё равно ре-сериализует его в строки заново на
каждый вызов `apply_relayout_result` — тот же узел с тем же
`ComputedStyle` платит одну и ту же сериализацию снова и снова, пока
существует хоть один JS-наблюдатель (`self.js_present`).

**Почему это не тронуто этим срезом:** это тоже не точечная правка — три
разных направления фикса просматриваются (a) кэш строковой сериализации
по `(NodeId, версия ComputedStyle)`, инвалидируемый только когда узел
реально попал в `dirty_roots` (та же граница, что BUG-341's
restyle-delta уже вычисляет для самого каскада — S37 не проверял,
насколько просто её переиспользовать здесь); (b) ленивая сериализация —
строить `HashMap<String,String>` только для узлов, чьи computed styles
JS реально прочитал в этом тике (нужен способ узнать это ДО прохода,
которого сейчас нет); (c) более дешёвое промежуточное представление,
сериализуемое в строку только на чтение из JS (`getComputedStyle`),
а не заранее на каждый relayout. Каждый вариант — самостоятельный кусок
работы с собственным риском (инвалидация кэша, обратная совместимость
API, риск регрессии на нетронутых сценариях), требующий отдельного среза
и измерения по `docs/perf-method.md`, не довеска к этому.

**Сделано в этом срезе (постоянные правки, `git diff --stat`: только
`crates/shell/src/relayout.rs`):** инструментация `apply_step!` на все
фазы `apply_relayout_result`, плюс под-таймеры внутри
`js_geometry_collect`. Гейт `LUMEN_FRAME_LOG` — нулевая стоимость на
обычном прогоне, тот же паттерн, что S12/S33's таймеры. `||`-своп
`relayout_raf_dirty`, использованный только для самого измерения (нужен
путь `try_relayout_raf_incremental`, где стоимость видна на UI-потоке),
отменён — код функции идентичен состоянию до этого среза.

**Тесты:** `cargo build --profile dev-release -p lumen-shell --bin lumen
--features v8` зелёный. `cargo clippy -p lumen-shell --all-targets
--features v8 -- -D warnings` чист.

**Следующий срез должен:** выбрать один из трёх направлений (a)/(b)/(c)
выше для `collect_computed_styles` — теперь измеримо самостоятельная
находка, независимая от M4-роутинга (S36's вопрос «выносить
`apply_relayout_result` целиком на движковый поток» остаётся открытым
отдельно, но `collect_computed_styles` дорога одинаково на ЛЮБОМ
потоке — фикс здесь снижает стоимость `apply_relayout_result` целиком,
а не только для `try_relayout_raf_incremental`'s UI-thread пути, и
поэтому не требует решать архитектурную развилку S36 первым). Основной
исходный симптом бага (M4-роутинг мёртв под движковым потоком) по-прежнему
открыт и не тронут этим срезом.

**Срез 38 (2026-09-23):** направление (a) выше — кэш строковой сериализации
`collect_computed_styles` по идентичности `Arc<ComputedStyle>`, переживающий
между тиками — реализован и **измерен как регрессия**, откачен целиком
(`git diff --stat` перед коммитом пуст). Устройство: новый тип
`ComputedStyleSerializeCache` в `lumen-layout` (два поколения `prev`/`next`,
ключ — адрес аллокации `Arc::as_ptr`, сама `Arc` держится в записи кэша,
чтобы исключить ABA-коллизию адреса после освобождения), персистентно
хранился в `Lumen`-состоянии (`crates/shell/src/lumen/state.rs`, рядом с
`prev_layout_shift_rects` — тем же паттерном «база для диффа, живущая между
тиками»), подключён в `relayout.rs:1076` вместо голого
`collect_computed_styles`. Замер на S29's чистом офлайн-стенде
(`LUMEN_FRAME_LOG=1`, 15с прогона, `grep 'collect_computed_styles'`) —
**с кэшем 187-242мс за тик, без кэша (та же сборка, тот же стенд,
`git checkout` этого же файла) 44-92мс** — почти четырёхкратное замедление,
не улучшение. Причина: посылка S37 «cascade-skip путь переиспользует ТОТ ЖЕ
`ComputedStyle` для непереехавших узлов» не выполняется на этом конкретном
стенде под `try_relayout_raf_incremental`-путём — каждый тик действительно
пересчитывает каскад заново и заводит новые аллокации `Arc<ComputedStyle>`
даже для узлов, чьё визуальное значение не изменилось (не проверено, чем
именно; кандидаты — либо кэш каскада в `CounterMap`/cascade path не
дедуплицирует по хэшу применённых деклараций на этом пути, либо дедуплицирует,
но результат оборачивается в новый `Arc` на каждом вызове), так что кэш
промахивается на 100% узлов и платит чистый оверхед (`Arc::new` + один
лишний `.clone()` HashMap'а поверх уже существующего) без единого попадания.
**Следующий срез должен** сначала подтвердить/опровергнуть саму посылку —
инструментировать hit/miss самого кэша (не просто включить его вслепую) —
или проверить это на реальном узком месте `apply_relayout_result`'s
cascade-путём напрямую (сравнить адрес `Arc<ComputedStyle>` одного и того же
`NodeId` между двумя последовательными тиками на этом стенде), прежде чем
пробовать направление (a) снова; либо переключиться на направление (b)
(ленивая сериализация — строить карту только для узлов, чьи computed styles
JS реально прочитал) или (c) (дешёвое промежуточное представление), которые
не зависят от стабильности `Arc`-идентичности между тиками.

**Срез 39 (2026-09-23):** S38's посылка проверена напрямую — вместо ещё
одного кэша добавлено чтение уже существующей инструментации BUG-341
(`lumen_layout::counters::take_cascade_stats()`, thread-local, всегда
компилируется, до этого читалась только тестами) в `try_relayout_raf_incremental`
(`crates/shell/src/relayout.rs`), выведено в ту же `LUMEN_FRAME_LOG`-строку
как `cascade_reused=N cascade_recomputed=M`. Два прогона `bug935_raf_relayout_census.py`
на S29's офлайн-стенде (4 scroll-тика, 5с settle):

- **Дефолтная сборка (движковый поток есть, M4-роутинг мёртв — сам
  корневой симптом бага):** ни одной строки `(incremental, on-thread)` в
  логе — `try_relayout_raf_incremental` не вызывается вообще, весь трафик
  идёт через `submit_relayout_job` → `layout_measured_hyp` (полный,
  без-cascade-skip путь). S38's кэш был подключён именно сюда — тестировал
  код, который в проде не выполняется.
- **`LUMEN_NO_ENGINE_THREAD=1` (форсирует `try_relayout_raf_incremental`,
  restyle=1 на каждом тике):** первый (холодный) тик — `cascade_reused=0
  cascade_recomputed=5264` (ожидаемо, кэш ещё пуст); каждый следующий —
  устойчиво `cascade_reused=668 cascade_recomputed=4` (30 тиков подряд,
  0 отклонений) — **99.4% попаданий**, `Arc<ComputedStyle>` для
  непереехавших узлов действительно переживает тик как один и тот же
  аллокейшн.

Вывод: посылка S37 (стабильность `Arc`-идентичности между тиками) верна —
но только на быстром cascade-skip пути, который сам по себе недостижим
из дефолтной сборки (корневой симптом BUG-935, `&&`-короткое замыкание
в `relayout_raf_dirty`/`_readback`, не тронуто ни этим, ни предыдущими
срезами). S38's 4-кратная регрессия — не опровержение направления (a), а
артефакт замера на мёртвом by-default пути: там каждый тик — холодный,
`cascade_recomputed` близко к размеру документа, кэш обязан мазать.
Направление (a) остаётся валидным **только после** починки самого
M4-роутинга — до этого числа от S38 нерелевантны, пробовать эту оптимизацию
изолированно от роутинга бессмысленно. **Сделано в этом срезе (постоянные
правки, `git diff --stat`):** только `crates/shell/src/relayout.rs` —
диагностический `take_cascade_stats()`/`cascade_reused`/`cascade_recomputed`
в `LUMEN_FRAME_LOG`-строке, нулевая стоимость при выключенном флаге (тот же
паттерн, что у остальных `incr_t0`-полей). **Следующий срез должен**
взяться за сам M4-роутинг (`&&`-короткое замыкание в `relayout_raf_dirty`/
`relayout_raf_dirty_readback`) — это по-прежнему единственный путь,
которым `collect_computed_styles`/direction (a) получают шанс окупиться,
а не очередной кэш поверх мёртвого кода.

## Срез 40 (P3, 2026-09-23) — уточнение S36: `apply_relayout_result`'s 60-105мс УЖЕ платится UI-потоком в ДЕФОЛТНОЙ (несвопнутой) сборке через `poll_engine_commit`, а не «списывается с движкового потока»

S36 объяснял отсутствие чистого выигрыша от `||`-свопа третьим
механизмом: «сама `apply_relayout_result` ... стоит 60-90мс и не должна
исполняться синхронно на UI-потоке ни при каком порядке веток», отдельно
утверждая, что «под исходным (не свопнутым) порядком ... эти же 65-90мс
списываются с ДВИЖКОВОГО потока» (т.е. будто дефолтный путь этой платы
избегает). Прежде чем пробовать S36's предложенный архитектурный шаг
(вынести `apply_relayout_result` на движковый поток независимо от
роутинга), проверил это утверждение напрямую чтением кода и замером на
дефолтной сборке (без `LUMEN_NO_ENGINE_THREAD`, без `||`-свопа — то есть
на пути, который сегодня реально исполняется на живых сборках).

**Чтение кода:** `submit_relayout_job`'s движковая задача
(`make_relayout_job`, `relayout.rs:1321-1342`) считает только
`compute_layout` и возвращает голый `EngineCommit{content, layout_box,
viewport, compute_ms}` — никакого `collect_*`/`apply_relayout_result`
внутри замыкания нет. `apply_relayout_result` вызывается из
`poll_engine_commit` (`relayout.rs:1412-1444`), а тот — из
`about_to_wait.rs:468`, части парковыного цикла winit, то есть UI-потока.
Сам код это уже документирует прямым текстом в комментарии над
`self.engine_stats.record(compute_ms)`: «unlike the synchronous path this
excludes the UI-thread apply (observers etc.)» — `compute_ms` не включает
apply-фазу именно потому, что она отдельная UI-side работа.

**Замер:** census (`scripts/bug935_raf_relayout_census.py`, S29's
офлайн-стенд, `--settle-s 5 --ticks 4`) прогнан БЕЗ каких-либо правок кода
и БЕЗ `LUMEN_NO_ENGINE_THREAD` — чистый дефолтный путь. `.tmp/bug935_census_stderr.log`
содержит те же `apply-step ... (js_geometry_collect)` строки (49.8-103.7мс,
26 записей за 21 relayout-цикл), что S37 находил только под временным
`||`-свопом. Порядок строк в логе подтверждает порядок вызова:
`apply-step ... js_geometry_collect` идёт ПЕРЕД `[engine] relayout N ms
(off-thread)` на каждом цикле — `poll_engine_commit` печатает свою строку
(с `compute_ms`) уже ПОСЛЕ завершившегося внутри неё же
`apply_relayout_result`.

**Вывод:** S36's диагноз «своп переносит стоимость `apply_relayout_result`
на UI-поток» неточен — эта стоимость уже лежит на UI-потоке сегодня, на
каждый закоммиченный relayout, независимо от того, свопнут `||`-порядок
или нет. Разница, которую реально вносит своп — не «кто платит», а
«когда и как часто»: под дефолтным порядком apply идёт один раз на
ГОТОВЫЙ коммит (после того как движковый поток асинхронно посчитал
layout, который может занять 0 доп. UI-времени благодаря офф-тредингу),
под свопом весь цикл (layout compute + apply) идёт синхронно на
UI-потоке КАЖДЫЙ тик. Практическое следствие: работа S37-S39
(`collect_computed_styles`'s дороговизна и её будущий кэш) снижает
УЖЕ существующую UI-thread нагрузку дефолтного пути сегодня, а не только
гипотетического будущего свопнутого — её ценность не обусловлена
починкой M4-роутинга, вопреки последней строке S39 (там речь шла о
другом — о применимости самого КЭША direction (a), чей hit-rate зависит
от cascade-skip пути, а не о ценности урезания самого
`collect_computed_styles`'s cost в целом, которое применимо к
`apply_relayout_result` на любом роутинге).

**Не сделано:** правок кода нет (`git diff --stat` — пусто), только
чтение + не-инструментированный census на неизменённом дефолте. Основной
симптом бага (M4-роутинг мёртв под движковым потоком) не тронут. S36's
предложенный следующий шаг («вынести дорогие фазы `apply_relayout_result`
на движковый поток независимо от способа получения `LayoutBox`») остаётся
в силе и теперь имеет более точное обоснование — это оптимизация UI-thread
отклика ДЕФОЛТНОЙ сборки уже сегодня, а не предпосылка, ожидающая починки
M4-роутинга. **Следующий срез должен** спроектировать это разделение:
переместить `js_geometry_collect`'s `collect_*`-вызовы (не требуют
`&mut self`-полей рендерера/фреймов, только `lb_ref`+`doc_guard`) в
движковую задачу `make_relayout_job`, расширив `EngineCommit` собранными
геометрией/стилями, чтобы `poll_engine_commit` на UI-потоке только
раскладывал уже готовые данные вместо их вычисления.

## Срез 41 (P3, 2026-09-23) — `js_geometry_collect` перенесён на движковый поток внутри `make_relayout_job`, реализовано направление из S40

S40's «следующий срез должен» — перенести `collect_*`-вызовы
`apply_relayout_result`'s `js_geometry_collect` (S37-S40: 60-105мс/тик,
доминанта её стоимости, уже платится UI-потоком в дефолтной сборке через
`poll_engine_commit`) в движковую задачу `make_relayout_job`, расширив
`EngineCommit` собранными геометрией/стилями — реализовано.

**Устройство:** блок `collect_layout_rects`/`collect_layout_shift_rects`/
`compute_layout_shift_score`/`collect_client_rects`/`collect_computed_styles`/
`collect_pseudo_computed_styles`/`collect_custom_properties`/
`collect_scroll_containers_for_js_state` вынесен из `apply_relayout_result`
в свободную функцию `collect_js_data` (`crates/shell/src/relayout.rs`),
возвращающую новый тип `PrecollectedJsData` (тот же кортеж полей, что раньше
жил анонимным `let (...)`, плюс `next_layout_shift_baseline` — обновлённый
снимок для `Lumen::prev_layout_shift_rects`, применяемый вызывающей стороной
только когда коммит реально применяется, не раньше). `apply_relayout_result`
получил параметр `precollected: Option<PrecollectedJsData>` (под `#[cfg(feature
= "v8")]`, как и сам блок): `Some` — использует готовые данные напрямую,
`None` — падает на прежний инлайновый путь (вызывает `collect_js_data` сама,
байт-идентично поведению до среза). `make_relayout_job`'s движковая задача
теперь тоже зовёт `collect_js_data` — сразу после `compute_layout`, под тем
же `document.lock()`, что уже был доступен в задаче, — и кладёт результат в
новое поле `EngineCommit::precollected`. `compute_ms` тайминг **не изменён**:
измеряется только до вызова `collect_js_data`, сама коллекция получает
собственные `apply-step`-таймеры (тот же `LUMEN_FRAME_LOG`-гейт) — семантика
метрики, которую уже читает census-скрипт и `ENGINE_SUMMARY`, не сдвинулась.
Оба синхронных вызывающих места (`relayout()`, `try_relayout_raf_incremental`)
передают `None` — у них нет движковой задачи, чтобы отдать эту работу.
`readback_relayout_job`/`poll_engine_commit` пробрасывают `commit.precollected`.

**Почему это безопасно относительно `document.lock()`:** `collect_js_data`
принимает `MutexGuard` **по значению** (не `&Document`) и роняет его сразу
после `collect_computed_styles`/`collect_pseudo_computed_styles` — тем же
местом, где прежний инлайновый блок звал `drop(doc_guard)` — так что
`collect_custom_properties`/`collect_scroll_containers_for_js_state` (им
`Document` не нужен) не держат лок дольше, чем раньше, независимо от того,
движковый это поток или UI-поток.

**Замер** (`scripts/bug935_raf_relayout_census.py`, S29's офлайн-стенд,
`--settle-s 5 --ticks 4`, два прогона на каждую сборку, `dev-release`+`v8`):

| | до (baseline, git stash) | после (этот срез) |
|---|---|---|
| scroll RTT avg, прогон 1 | 116.4мс (6/130/234/95) | 26.1мс (59/14/17/15) |
| scroll RTT avg, прогон 2 | — | 17.5мс (24/14/11/21) |

Не строгий интерливед A/B (S28's урок — рекламный/сетевой шум на живых
сайтах делает одиночные разы неразличимыми от шума), но офлайн-стенд S29
детерминирован по составу работы (нет сети), а разница (~90мс на прогон)
на порядок больше типичного разброса, который S28 находил на этом же
классе замеров (единицы мс), и совпадает по величине с самим удалённым
`collect_computed_styles`'s 65-90мс — не шум, ожидаемый эффект. `apply-step`-
лог подтверждает механизм напрямую: до среза `collect_computed_styles` (66-88мс)
печатался ВНУТРИ UI-side `poll_engine_commit`, сразу перед `[engine] relayout
… (off-thread)`; после среза те же шаги печатаются раньше, среди
движковых `[engine] task` строк, а UI-side `apply_relayout_result` оставляет
только `frame_sync`/`dl_splice_diff_cache`/`transitions_sync`/`cv_snap_scroll_state`
(десятки мс, не 90+).

**Тесты:** `cargo build --profile dev-release -p lumen-shell --bin lumen
--features v8` зелёный (~1.5 мин, инкрементально). `cargo clippy -p
lumen-shell --all-targets --features v8 -- -D warnings` чист. Точечные тесты
модуля (`cargo test --profile dev-release -p lumen-shell --bin lumen
--features v8 -- relayout`) — 13/13 зелёных, включая
`bug935_s26_pending_lazy_image_drain_tests`/`thread2_defer_query_tests`,
которые ближе всего к тронутому коду. Полный `scoped-test.sh`/workspace-clippy
не запускались вручную — по протоколу идут один раз в `/lumen-task-finish`.
Сборка без фичи `v8` (`--no-default-features --features backend-wgpu`) уже
сломана на `main` независимо от этого среза (`lumen_js`/`js_string_literal`
не резолвятся — `csp_enforce.rs`, `window_mode.rs`, `page_load.rs`,
`persistent_js.rs`, `lumen/state.rs`, `lumen/printing.rs`); `git diff` этого
среза не задевает ни один из перечисленных файлов, так что поломка
дособрана до этого среза, не им — не в объёме BUG-935.

**Не сделано:** основной корневой симптом бага (M4-роутинг мёртв под
движковым потоком, `&&`-короткое замыкание в `relayout_raf_dirty`/
`_readback`) по-прежнему не тронут — этот срез снижает UI-thread стоимость
КАЖДОГО коммита (любого роутинга), а не чинит сам роутинг. S38's кэш
сериализации (направление (a) из S37) не возвращён — S39 показал его
применимость зависит от cascade-skip пути, который сам зависит от починки
M4-роутинга; актуальность не изменилась этим срезом. **Следующий срез
должен** взяться либо за сам M4-роутинг (`&&`→`||`-своп плюс разбор его
последствий, начатый и отменённый в S27-S39), либо за direction (b)/(c) из
S37 (ленивая сериализация/дешёвое промежуточное представление
`collect_computed_styles`), которые применимы независимо от роутинга и
теперь пользуются тем же выигрышем «на движковом потоке», что и остальной
`js_geometry_collect`.

**Срез 42 (P3, 2026-09-23) — аудит потребителей `computed_styles`/
`pseudo_computed_styles`/`custom_properties` для direction (b)/(c): найден
непрозрачный (non-`maybe_flush`) читатель, блокирующий самую очевидную
реализацию**

S41's «следующий срез должен» перечисляет direction (b) (ленивая
сериализация — строить `HashMap<String,String>` только для узлов, чьи
computed styles JS реально прочитал) и (c) (дешёвое промежуточное
представление, сериализуемое на чтение). Самая простая версия любого из
двух вариантов — не трогать сам формат, а просто **пропускать** три шага
`collect_js_data` (`collect_computed_styles`/`collect_pseudo_computed_styles`/
`collect_custom_properties`, S37's найденная доминанта) целиком, пока
страница ни разу не прочитала `getComputedStyle`/`computedStyleMap()`/
`getPropertyValue('--x')` — офлайн-стенд S29, на котором мерились S34-S41,
вообще ни разу не зовёт `getComputedStyle` (`grep -c getComputedStyle
scripts/perf-fixtures/bug935_raf_dom_stand.html` = 0), так что эта версия
обещала обнулить всю стоимость на самом же измерительном стенде.

Этот срез — аудит всех читателей трёх кэшей
(`crates/js/src/v8_runtime/{runtime.rs,style_flush.rs,install/platform.rs}`)
**перед** тем, как писать код гейта (тот же порядок, что S37→S39 уже
применяли к направлению (a) — сначала подтвердить посылку, потом
реализовывать). Найдено:

- `_lumen_get_computed_style`/`_lumen_get_computed_style_pseudo`/
  `_lumen_get_computed_style_pseudo_entries`/`_lumen_get_computed_style_entries`
  (`install/platform.rs`) — все четыре зовут `flush.maybe_flush()` **до**
  чтения кэша. `FlushHandles::maybe_flush` (`style_flush.rs:139`) сам не
  зависит от `apply_relayout_result`'s эйджерного пуша: `never_flushed`
  стартует `true`, так что самый первый вызов `getComputedStyle` на
  странице форсирует реальный пересчёт (тот же `collect_computed_styles`,
  вызванный напрямую из `style_flush.rs:256`) независимо от того, добрался
  ли до этого узла хоть один тик `apply_relayout_result`. Для этих четырёх
  нативов гейт «не собирать, пока JS ни разу не спросил» безопасен —
  `maybe_flush` сама восполняет пробел в момент первого реального чтения.
- **`_lumen_request_scroll`** (`install/platform.rs:675`, JS-сторонний
  сеттер `element.scrollLeft`/`scrollTop`) — читает `cs_for_request`
  (тот же `Arc` что `computed_styles`) **напрямую, без `maybe_flush()`**,
  чтобы проверить `overflow-x`/`overflow-y == "clip"` (BUG-975) и занулить
  соответствующую ось запроса. Эта проверка не публична как «читает
  computed style» — снаружи это просто присваивание `scrollLeft`, так что
  ни один из существующих сигналов «JS вызвал getComputedStyle» её не
  увидит. Пропусти этот срез гейт «не собирать, пока не читали
  getComputedStyle» — первое же `element.scrollLeft = x` на
  `overflow: clip`-контейнере до первого вызова `getComputedStyle` получит
  ПУСТУЮ карту (`styles.get(&nid)` → `None` → `is_clip` всегда `false`),
  молча перестанет зануляться и просто применит непроклампленный `x`/`y` —
  тихая регрессия BUG-975 без единого падающего теста в объёме этого бага
  (клип-кламп не покрыт BUG-935's собственными тестами, только BUG-975's).
- `pseudo_computed_styles`/`custom_properties` — прочих не-`maybe_flush`
  читателей не найдено (`custom_properties` читается только двумя уже
  перечисленными нативами, `pseudo_computed_styles` только двумя своими);
  для этой пары направление (b)/(c) в самой простой форме безопасно само
  по себе.

**Вывод:** самая простая реализация (b)/(c) — общий на все три кэша флаг
«JS хоть раз прочитал getComputedStyle-семейство» — некорректна из-за
`_lumen_request_scroll`'s скрытого чтения `computed_styles`. Рабочих
вариантов остаётся два: (i) завести отдельный, более широкий сигнал
«кэш `computed_styles` кому-то нужен», взводимый и на
`getComputedStyle`-семействе, и на первом `_lumen_request_scroll`, что
означает гейт по-прежнему не поможет странице, которая просто скроллит
`overflow: clip`-контейнер и никогда не читает стили explicitly (частый
случай для видео-плееров/каруселей — ровно тот же класс страниц, что и
исходный симптом бага); или (ii) завести гейт `pseudo_computed_styles`/
`custom_properties` отдельно от `computed_styles` (у первых пары
непрозрачных читателей нет), оставив `computed_styles` собираться
эйджерно как сейчас — меньший, но гарантированно безопасный выигрыш
(`collect_pseudo_computed_styles`/`collect_custom_properties` — не
измеренная S37 доминанта, `collect_computed_styles` ей была).

**Сделано в этом срезе:** только этот аудит, `git diff --stat` не
затрагивает ничего вне `bugs/BUG-935-OPEN.md`. Код не менялся — вариант
(i)/(ii) выше остаётся нереализованным.

**Следующий срез должен** выбрать между (i) (шире взводимый флаг,
сохраняет весь выигрыш `collect_computed_styles`, но требует протащить
второй set-site через `_lumen_request_scroll`, который сегодня не имеет
доступа ни к какому «этот кэш используется» сигналу — сам `flush`
недоступен внутри его замыкания без рефактора `install_scroll_state`'s
сигнатуры) и (ii) (безопаснее, меньше выигрыша, не трогает
`_lumen_request_scroll` вообще); либо взяться за сам M4-роутинг, как и
предлагали S39/S41.

**Срез 43 (P3, 2026-09-23) — вариант (ii) реализован: гейт
`pseudo_computed_styles`/`custom_properties` на «страница хоть раз
прочитала»**

Два новых `Arc<AtomicBool>` (`V8JsRuntime::pseudo_styles_needed`/
`custom_props_needed`, `crates/js/src/v8_runtime/runtime.rs`), взводимые
в четырёх нативах, которые S42 нашёл единственными читателями двух кэшей
(`_lumen_get_computed_style_pseudo`/`_lumen_get_computed_style_pseudo_entries`
для `pseudo_computed_styles`; `_lumen_get_custom_property`/
`_lumen_get_computed_style_entries` для `custom_properties` —
`install/platform.rs`). Гейт применён в обоих потребителях, которые сами
зовут `collect_pseudo_computed_styles`/`collect_custom_properties`:

- `FlushHandles::maybe_flush` (`style_flush.rs`, CSSOM-4 same-tick путь) —
  два новых поля `pseudo_styles_needed`/`custom_props_needed` (клоны тех
  же `Arc`), `if`-обёртка вокруг каждого из двух коллекторов. Порядок
  внутри нативов («взвести флаг, потом позвать `maybe_flush`») гарантирует,
  что самый первый читающий вызов страницы видит уже взведённый флаг и
  форсирует реальный сбор — то же соглашение, что `never_flushed` уже
  использует для `computed_styles`.
- `collect_js_data` (`crates/shell/src/relayout.rs`, S41's асинхронный
  engine-thread путь) — два новых параметра `pseudo_styles_needed: bool`/
  `custom_props_needed: bool`; `false` возвращает пустую `HashMap` вместо
  вызова коллектора. Значения читаются на UI-потоке ДО того, как замыкание
  уезжает на движковый поток (`make_relayout_job`) — тот же приём, что уже
  применён к `js_present`/`prev_layout_shift_rects` в S41, потому что сам
  движковый поток не имеет доступа к `Lumen`/JS-рантайму (ADR-016).
  Синхронный путь `Lumen::relayout` читает флаги напрямую перед вызовом.
  Оба флага кэшируются в новых полях `Lumen::pseudo_styles_needed_flag`/
  `custom_props_needed_flag` (`Option<Arc<AtomicBool>>`, `None` →
  `unwrap_or(true)` — деградация в «собирать всегда», как было до среза),
  заполняемых в `set_js_ctx` **в обоих режимах** движкового потока (в
  отличие от `raf_pending_flag`/`dom_dirty_flag`, которые кэшируются
  только когда поток включён — при выключенном потоке `make_relayout_job`
  всё равно строится на UI-потоке и ему нужен готовый снимок, а не живой
  хэндл для чтения).

**Тесты:** `cargo build --profile dev-release -p lumen-shell --bin lumen
--features v8` зелёный (~4 мин). `cargo clippy -p lumen-shell --all-targets
--features v8 -- -D warnings` чист (потребовал `#[allow(clippy::
too_many_arguments)]` на обе тронутые функции — 8-9 параметров). Точечные
тесты: `cargo test --profile dev-release -p lumen-shell --bin lumen
--features v8 -- relayout` — 13/13; `cargo test --profile dev-release -p
lumen-js --features v8-backend -- computedstyle computed_style pseudo
custom_prop` — 50/50, включая
`get_computed_style_pseudo_element_reads_pseudo_map`/
`custom_property_comes_from_its_own_snapshot` и весь `v8_cssom_stylesheets`
модуль — все читающие тесты остаются зелёными, подтверждая, что
«взвести-потом-flush» не теряет первый реальный запрос.

Прямая проверка эффекта: S29's офлайн-стенд
(`scripts/perf-fixtures/bug935_raf_dom_stand.html`, ни одного
`getComputedStyle`-вызова) через
`scripts/bug935_raf_relayout_census.py` — `apply-step`-лог (`LUMEN_FRAME_LOG`)
до среза содержал `collect_pseudo_computed_styles`/`collect_custom_properties`
на каждом из 24 относлейаутов; после среза оба шага отсутствуют в логе
целиком (`grep -c` = 0 на 25 `collect_computed_styles`-строк того же
прогона) — гейт применяется на каждом реальном коммите этого прогона, ни
разу не сработав ложно (readback-тесты выше подтверждают корректность
первого чтения на других фикстурах). Не строгий A/B по времени — S37 уже
установил, что `collect_computed_styles` (не тронутый этим срезом) —
доминанта, а не эти двое, так что ожидаемый выигрыш структурный (два шага
полностью исчезают у страниц, которые их не читают), а не заметный на
таймере этого конкретного стенда.

**Не сделано (до среза 44):** вариант (i) (тот же гейт для `computed_styles`,
самого дорогого из трёх) остаётся нереализованным — S42 показал его
небезопасным без отдельного, более широкого сигнала через
`_lumen_request_scroll`. Основной корневой симптом бага (M4-роутинг мёртв)
по-прежнему не тронут.

**Срез 44 (P3, 2026-09-23) — вариант (i) реализован: `computed_styles_needed`,
на обоих потребителях, плюс закрыта скрытая гонка с другими
`maybe_flush`-триггерами**

Новый `Arc<AtomicBool>` `V8JsRuntime::computed_styles_needed`/
`FlushHandles::computed_styles_needed` (тот же паттерн, что S43's
`pseudo_styles_needed`/`custom_props_needed`), взводимый в трёх местах
(`install/platform.rs`): `_lumen_get_computed_style` (обычный
`getComputedStyle`), `_lumen_get_computed_style_entries`
(`computedStyleMap()`, заодно взводит и `custom_props_needed` — он уже
мержит custom properties) и `_lumen_request_scroll`
(`install_scroll_state`, S42's единственный небезопасный
читатель — оверфлоу-clip проверка BUG-975). `install_scroll_state` уже
получал `flush: FlushHandles` целиком, так что доступ к новому полю не
потребовал смены сигнатуры — только клон `flush.computed_styles_needed`
рядом с уже существующим `cs_for_request` (S42 ошибочно предполагал, что
понадобится новый параметр). Гейт применён в `FlushHandles::maybe_flush`
(синхронный CSSOM-4 путь) и в `collect_js_data`/`make_relayout_job`
(`crates/shell/src/relayout.rs`, S41's асинхронный движковый путь) —
третий параметр `computed_styles_needed: bool`, тем же приёмом, что
S43's `pseudo_styles_needed`/`custom_props_needed`, включая новое поле
`Lumen::computed_styles_needed_flag`, заполняемое в `set_js_ctx`.

**Регрессия, найденная и закрытая в этом же срезе:** первая версия гейта
(симметричная S43 — гейтовать сбор внутри `maybe_flush` голым `if
computed_styles_needed.load(...)`) ломала `v8_bug560_sync_focus`'s два
теста (`get_computed_style_sees_same_tick_focus_call`/`..._focus_within`,
BUG-560's собственная регрессионная защита): `.focus()` в этих тестах
вызывает `_lumen_get_bounding_rect` (scroll-into-view) ДО того, как
`getComputedStyle` успевает взвести `computed_styles_needed`, а тот
native тоже зовёт `flush.maybe_flush()` — эта более ранняя вызов
потребляет единственный «настоящий флаш» (гасит `never_flushed`/
`flush_stale`) с флагом ещё `false`, так что второй, настоящий вызов
`getComputedStyle` находит все прочие условия `maybe_flush`'s
early-return уже погашенными и выходит без сбора, несмотря на то что
флаг к этому моменту уже `true`. **Это не гипотетическая гонка** —
поймано именно существующим регрессионным тестом на BUG-560, не новым.
Тот же класс, вероятно, дремлет и в S43's `pseudo_styles_needed`/
`custom_props_needed` (никакой существующий тест не бьёт по этой
конкретной интерференции для них), но чинить это ретроактивно — вне
рамок этого среза.

Фикс — не откат гейта, а его обобщение по образцу уже существующего
`never_flushed`, но per-cache: новое поле `computed_styles_collected`
(тот же вид `Arc<AtomicBool>`, зеркалящий `computed_styles_needed` по
всей цепочке `runtime.rs`/`style_flush.rs`/`v8_runtime.rs`). Условие
раннего выхода `maybe_flush` получило пятый bypass —
`computed_styles_pending = computed_styles_needed && !computed_styles_collected`
— которое не даёт функции выйти раньше времени, пока флаг взведён, а
реальный сбор для него ещё не проехал; `computed_styles_collected`
взводится сразу после самого сбора, тем же местом, что и запись в кэш.
Это устраняет зависимость от того, КАКОЙ native первым позвал
`maybe_flush` внутри одного скриптового тика.

**Тесты:** `cargo build --profile dev-release -p lumen-shell --bin lumen
--features v8` и `-p lumen-js --features v8-backend` зелёные. `cargo
clippy --all-targets` на обоих чист. `cargo test -p lumen-js
--features v8-backend` — 4169/4173 (4 фейла — `v8_webworker`×3 +
`frame_bridge::inaccessible_bridge_mutation_does_not_mark_dirty` —
подтверждённая параллель-тредовая нестабильность, не связанная с этим
срезом: все четыре зелёные при `--test-threads=1`); прицельно
`v8_bug560_sync_focus`/`computedstyle`/`scroll`/`pseudo`/`custom_prop` —
204/204 включая обе регрессионные пробы BUG-560. `cargo test -p
lumen-shell --bin lumen --features v8 -- relayout` — 13/13.

Прямая проверка эффекта: тот же офлайн-стенд S29
(`scripts/bug935_raf_relayout_census.py` на
`bug935_raf_dom_stand.html`, 20 scroll-тиков + 3с settle, 62
относлейаута/811 `apply-step`-строк) — `grep -c
"collect_computed_styles\|collect_pseudo_computed_styles\|collect_custom_properties"`
на полученном `LUMEN_FRAME_LOG`-логе даёт `0`: все три коллектора
пропущены на каждом коммите фикстуры, которая не делает ни одного
`getComputedStyle`-подобного чтения — структурный эффект, тот же вид,
что S43 показал для двух других (не строгий A/B по времени на этой
фикстуре — S37 уже установил, что именно `collect_computed_styles`
доминанта, так что ожидаемый выигрыш здесь больше, чем у S43, но не
измерен таймером на пустой фикстуре).

**Не сделано:** основной корневой симптом бага (M4-роутинг мёртв)
по-прежнему не тронут — это был последний из трёх коллекторов S37
насчитал, но сам маршрут `try_relayout_raf_incremental` остаётся
недостижимым на дефолтной сборке. **Следующий срез должен** взяться за
сам M4-роутинг (S39/S41/S42/S43 все указывали сюда), либо, если кто-то
хочет закрыть найденный в этом срезе латентный класс гонки, применить
тот же `*_collected`-bypass к `pseudo_styles_needed`/`custom_props_needed`
(сегодня не подтверждённый регрессионным тестом, но структурно тот же
дефект).

**Срез 44 (продолжение, тот же день) — вторая находка: `update_*`
перезаписывает кэш пустой картой, когда гейт молчит на этом тике**

После первого фикса (`computed_styles_collected`) прогон `scripts/scoped-test.sh`
поймал детерминированный (не флак — повторено 3× с полной пересборкой,
включая `cargo clean -p lumen-driver -p lumen-js`) провал
`lumen-driver`'s `cases::snapshot_cpu::cpu_snapshots_match_references`:
~28 из 205 фикстур на 1024-2048 отличающихся байт из 2949120 каждая.
Расследование заняло большую часть среза и в итоге завело в тупик,
важный зафиксировать, чтобы следующая сессия не повторила путь:

1. Гипотеза «асинхронный `collect_js_data`-гейт (S41 путь) перезаписывает
   `computed_styles`/`pseudo_computed_styles`/`custom_properties` пустой
   картой, когда снятый на UI-потоке снапшот флага ещё `false`» —
   подтверждена КАК РЕАЛЬНЫЙ дефект (не только для `computed_styles`, но
   и, латентно, для S43's `pseudo_styles`/`customs` — `update_pseudo_computed_styles`/
   `update_custom_properties` тоже безусловно заменяют карту целиком,
   `crates/js/src/v8_runtime/runtime.rs:930-950`). Пофикшено: три поля
   `PrecollectedJsData::styles`/`pseudo_styles`/`customs` переведены на
   `Option<...>` — `None` значит «этот тик гейт молчал», и оба
   потребителя (`defer_js_push`/`route_query_js` ветки в
   `apply_relayout_result`) теперь пропускают соответствующий `update_*`
   вызов вместо передачи пустой карты. Это исправление осталось в коде
   этого среза независимо от следующего пункта — оно корректно само по
   себе и (бонусом) чинит тот же класс дефекта для `pseudo_styles`/`customs`,
   унаследованный от S43.
2. НО этот фикс **не изменил ни байта** в провале `snapshot_cpu` —
   протестировано явно. Дальнейшая бисекция (`git checkout HEAD~1 -- <8
   файлов>`, т.е. чистый код среза 43 без единой правки этого среза) в
   ЭТОМ ЖЕ worktree дала **тот же самый** набор из ~28 файлов с теми же
   байт-в-байт размерами расхождений — включая после `cargo clean -p
   lumen-driver -p lumen-js` (все профили, 11670 файлов). Эталонные PNG
   (`graphic_tests/snapshots/cpu/*.png`) побайтово идентичны между корневым
   чекаутом и этим worktree (`sha256sum` совпал на проверенных файлах).
   Тот же тест на чистом `main` в корневом чекауте (`D:\RustProjects\lumen-browser`,
   без единой правки) проходит стабильно (3/3 прогона).
   **Вывод: провал `snapshot_cpu` в этом worktree — не регрессия от кода
   этого среза и не от кода среза 43 (оба дают идентичный результат) —
   это отличие окружения/сборки между корневым чекаутом и слотом
   `p3-work` (кандидат — codegen-зависимая от хоста погрешность рендера,
   тот же класс, что `49-background-blend-mode`'s `_mm_rcp_ps`
   комментарий в `snapshot_cpu.rs` уже документирует для другого случая;
   не подтверждено до конца, не обязано быть тем же механизмом).** Не
   чинить в рамках BUG-935 — если провал воспроизводится в других слотах
   или в `--continue-on-fail` полном прогоне `graphic_tests/run.py`, заводить
   отдельным тикетом (P5/тулинг), не приписывать P3-специфичному коду.

**Тесты (после обеих находок):** те же 204/204 прицельных +
lumen-shell -- relayout 13/13, что и в первой части этого среза; сборка
и clippy обоих крейтов чисты; `snapshot_cpu`'s провал в этом worktree
не учтён как гейт этого среза по причине, изложенной выше — задокументирован,
не проигнорирован молча.

## Воспроизведение

```
LUMEN_PROFILE_TREE=1 LUMEN_FRAME_LOG=1 cargo run --profile dev-release -p lumen-shell -- --mcp-live-port 18790 https://ria.ru
```

Подключиться MCP-клиентом (токен в stderr `[mcp] token: ...`), `wait
document_ready`, подождать ~2с, наблюдать `[engine] relayout Nms
(off-thread) dl=... styled=...` в логе даже без единого `scroll`-вызова —
уже на этапе settle. Любая реальная страница с активным
rAF+DOM-мутацией (видео-плеер, карусель, «живая лента») воспроизводит то же.

Синтетическая версия без сети (S29): `scripts/perf-fixtures/bug935_raf_dom_stand.html`
через `file://`-URL, тем же `scripts/bug935_raf_relayout_census.py <file://путь>` —
не требует внешнего сайта, детерминирована по составу работы.
