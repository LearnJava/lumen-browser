# Vendored Web Platform Tests — pin record

P2-wpt S2. Source: [`web-platform-tests/wpt`](https://github.com/web-platform-tests/wpt).

## Pinned commit

```
35be3b44f3111c4d614b5b201e399493d20e7b38
```

Committed 2026-07-13T16:23:05Z ("Cover a few fragment parsing edge cases (#61042)").
This was `master`'s tip at the time of vendoring — re-pin explicitly (do not silently
drift) when re-vendoring; record the new hash + date here.

## Vendoring method: committed snapshot, not a git submodule

A submodule would need `git submodule update` (a network fetch to GitHub) on every
fresh checkout — that violates this task's own "no live network in CI" rule
(`docs/tasks/p2-wpt-integration.md`, Prerequisites). A plain committed snapshot has
no such requirement: once vendored, the suite runs fully offline. The tradeoff is a
manual re-vendor step (re-run the process below) instead of `git submodule update`
to move the pin — acceptable since WPT is pulled in rarely and deliberately here.

## What's vendored (verbatim upstream source, not modified)

| Path (this repo) | Upstream path | Purpose |
|---|---|---|
| `tools/wptrunner/` | `tools/wptrunner/` | The test runner itself (CLI, executors incl. `webdriver-bidi`, manifest loading, expectations, formatters). |
| `tools/manifest/` | `tools/manifest/` | Test manifest generation/parsing — hard import of `wptrunner.testloader`/`wptrunner.metadata`. |
| `tools/serve/` | `tools/serve/` | The local HTTP/WS test server (`wptserve`-based) that serves this vendored test tree during a run — hard import of `wptrunner.environment`. |
| `tools/wptserve/` | `tools/wptserve/` | The HTTP/WS server library `tools/serve/serve.py` is built on. |
| `tools/webdriver/` | `tools/webdriver/` | WPT's own WebDriver/**WebDriver BiDi** Python client (`webdriver.bidi.client`, `webdriver.bidi.modules.browsing_context`, …) — this is what S3's `browsers/lumen.py` product plugin will drive against `lumen --bidi-port`. |
| `tools/metadata/` | `tools/metadata/` | `web-features` YAML schema — imported by `manifest.sourcefile`. |
| `tools/gitignore/` | `tools/gitignore/` | `.gitignore`-pattern matcher — imported by `manifest.vcs`. |
| `tools/localpaths.py` | `tools/localpaths.py` | Repo-root/sys.path bootstrap (`repo_root`, `sys.path` setup for the packages above). **Not modified** — see the sys.path note below. |
| `tests/wpt/resources/testharness.js` | `resources/testharness.js` | The real WPT client-side test harness (assertion/reporting primitives every `testharness.js`-style test imports). |
| `tests/wpt/dom/nodes/` | `dom/nodes/` | One full test category ("start tiny", per this task's Prerequisites) — includes the S4 smoke test, `Element-hasAttribute.html` (`Document-createElement.html`, floated as an "e.g." example when this file was first drafted, turned out to need un-vendored `/common/dummy.xml`/`dummy.xhtml` iframe fixtures and `async_test` — not actually trivial; picked a genuinely self-contained synchronous test instead). |
| `tests/wpt/FileAPI/` | `FileAPI/` | Second full test category, added 2026-07-21 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-FileAPI`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash. A handful of its tests reference helper scripts outside `FileAPI/` (`/common/*.js`, `/resources/idlharness.js`, `/html/anonymous-iframe/resources/common.js`, `/service-workers/service-worker/resources/test-helpers.sub.js`) that were **not** vendored — those specific tests fail with a 404 for the missing helper, a documented survey gap in `docs/wpt-status.md`, not a Lumen engine bug. |
| `tests/wpt/IndexedDB/` | `IndexedDB/` | Third full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-IndexedDB`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 245 files (includes upstream `crashtests/` subdir and `.tentative.` variants, kept as-is). Most tests import in-category `resources/support.js` (vendored); a few reference out-of-category helpers (`/common/*.js`, `/service-workers/*`) not vendored — same documented survey gap as FileAPI. |
| `tests/wpt/WebCryptoAPI/` | `WebCryptoAPI/` | Fourth full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-WebCryptoAPI`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash (reused from the IndexedDB sparse checkout), 185 files. References only the already-vendored `/resources/testharness.js`+`testharnessreport.js` — no out-of-category helpers. 122/125 test files are `.https.`-only (`crypto.subtle` requires a secure context), which the minimal `wptrunner` executor cannot serve (no HTTPS port configured) — a documented survey gap, not a Lumen engine bug; see `docs/wpt-status.md` for the isolated run of the 2 non-`.https.` files. |
| `tests/wpt/accelerometer/` | `accelerometer/` | Fifth full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accelerometer`, `docs/wpt-status.md`), included despite its 🚫 scope note by direct user request. Same pinned commit, `git sparse-checkout` at the same commit hash, 19 files. Almost every test references `/generic-sensor/generic-sensor-tests.js` (shared test body for the whole Sensor API family) plus `/page-visibility/` and `/permissions-policy/resources/` — none vendored, same documented survey gap as FileAPI/IndexedDB; see `docs/wpt-status.md` for the run results. |
| `tests/wpt/accessibility/` | `accessibility/` | Sixth full test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accessibility`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 59 files (`ReadMe.md`, `crashtests/`, one testdriver.js test). No out-of-category helpers. Almost the whole category (58/59 files) is WPT-manifest type `crashtest`, which the minimal `browsers/lumen.py` product plugin has no executor for at all (`Unsupported test type crashtest for product lumen` — excluded before the run queue, not a per-test failure); see `docs/wpt-status.md` for the run results. |
| `tests/wpt/accname/` | `accname/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accname`, `docs/wpt-status.md`, commit `69ab520d`). Same pinned commit, `git sparse-checkout` at the same commit hash, 183 files. Entirely `testdriver.js`-dependent — the minimal executor has no `test_driver.*` support, so all 19 selected ids come back `SKIP`; see `docs/wpt-status.md` for the run results. |
| `tests/wpt/acid/` | `acid/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-acid`, `docs/wpt-status.md`), included despite its 🚫 scope note (historical Acid1/2/3, not an actual spec) by the same standing user decision as `accelerometer`. Same pinned commit, `git sparse-checkout` at the same commit hash, 30 files (`acid2/`, `acid3/`). No out-of-category helpers. Of the 3 manifest-selected ids, 2 (`acid2/reftest.html`, `acid3/test.html`) are WPT-manifest type `reftest` — unsupported by the minimal executor (S8 reftests is still a future slice), same class of gap as `accessibility`'s crashtests; the remaining one (`acid3/numbered-tests.html`, type `testharness`) ran and TIMEOUT — it's Acid3's own multi-window/iframe scoring harness, outside this executor's single-window model. See `docs/wpt-status.md` for the run results. |
| `tests/wpt/ai/` | `ai/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-ai`, `docs/wpt-status.md`), included despite its 🚫 scope note (AI/Writer API, no LLM integration) by the same standing user decision as `accelerometer`/`acid`. Same pinned commit, `git sparse-checkout` at the same commit hash, 180 files (`classifier/`, `language-model/`, `language_detection/`, `proofreader/`, `rewriter/`, `semantic_embedder/`, `summarizer/`, `translator/`, `writer/`). One out-of-category helper (`/common/get-host-info.sub.js`) not vendored, same documented survey gap as `FileAPI`. The whole category is `.https.`-only (163 `*.window.js` + 6 direct `.html`, 0 non-https tests) — worse than `WebCryptoAPI`'s 122/125, the first category with zero isolatable test files; same HTTPS-port gap, but here a full `--all --root ai --recursive` run couldn't even be attempted to completion (10-minute trial run only reached a handful of ids, each TIMEOUT). See `docs/wpt-status.md` for details. |
| `tests/wpt/ambient-light/` | `ambient-light/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-ambient-light`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`. Same pinned commit, `git sparse-checkout` at the same commit hash, 16 files (`AmbientLightSensor*.html`, `idlharness.https.window.js`, `resources/sensor-data.js`). No out-of-category helpers. A full `--all --root ambient-light --recursive` run completed in 1:36 (10 selected ids): 2 SKIP (testdriver.js unsupported), 6 `.https.` TIMEOUT (same HTTPS-port gap as `WebCryptoAPI`/`ai`), 1 ERROR (`idlharness.https.window.html` — result came back tagged with the previous test's URL, same session-reuse artifact as `accelerometer`), 1 FAIL (`AmbientLightSensor-supported-by-permissions-policy.html` — `document.permissionsPolicy.features` doesn't list `"ambient-light-sensor"`, same pattern as `accelerometer`). See `docs/wpt-status.md` for details. |
| `tests/wpt/animation-worklet/` | `animation-worklet/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-animation-worklet`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 50 files (43 root `.html`, `META.yml`, `common.js`, `idlharness.any.js`, `references/`, `resources/`). Two out-of-category helpers not vendored: `/web-animations/testcommon.js` (used by nearly every test) and `/common/reftest-wait.js` (used by the reftest-type tests), same documented survey gap as `FileAPI`/`ai`; `idlharness.any.js` also references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, neither vendored (only `testharness.js`+`testharnessreport.js` live under `tests/wpt/resources/`). A full `--all --root animation-worklet --recursive` run completed in 7:11 (46 selected ids): 15 of the selected `.https.html` files are WPT-manifest type `reftest` — unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`accessibility`'s crashtests, S8 in `docs/tasks/p2-wpt-integration.md` still a future slice), and together with their 8 reference files that's 23 "not run"; the remaining 23 selected ids are type `testharness` (all `.https.`) and actually ran: 21 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`) + 2 ERROR (both session-reuse artifacts of the same class as `accelerometer`/`ambient-light`: one harness result came back tagged with `idlharness.any.html`'s URL instead of the expected test, the other was `eval: live window closed before replying`). 0/23 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-model/` | `annotation-model/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-model`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 290 files (259 JSON fixtures under `definitions/`/`examples/`/`tools/samples/`/`tools/tests/`, `scripts/` third-party libs `ajv.min.js`/`JSONtest.js`/`showdown.min.js`, 7 `*-manual.html`). No out-of-category helpers. Entirely manual by spec design (README: no general automation mechanism, only a textarea-driven `JSONtest.js` harness for a human to paste client JSON output into) — all 7 HTML files are `-manual.html`, and `run_report.py --all --root annotation-model --recursive` explicitly excludes `-manual.html` and found no `.any.js`/`.window.js` either, so it selected 0 tests (`no tests selected`) — the first vendored category with zero automatically runnable tests, a harder gap than the `testdriver.js`/`crashtest` gaps in `accname`/`accessibility`. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-protocol/` | `annotation-protocol/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-protocol`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 58 files (`files/annotations/` — 43 JSON/JSON-LD fixtures + `*.headers`, `files/index.html` — a manual client-testing target page, `server/server-manual.html` — the sole test file, `tools/protocol-server.py` — reference protocol server). One out-of-category helper (`/common/utils.js`, used by `server-manual.html`) not vendored, same documented survey gap as `FileAPI`/`ai`. Entirely manual by spec design like `annotation-model` (README: no general automation mechanism, workflow requires a human driving an annotation client/server) — the sole test file is `-manual.html` and gets excluded by `run_report.py`'s own filter; the one remaining non-`-manual` candidate, `files/index.html`, isn't a test at all (no `testharness.js`, just a fixture page) so the WPT manifest can't find it (`CRITICAL Unable to find any tests at the path(s)`), crashing the run before `suite_end` — 0/0. Second vendored category with zero automatically runnable tests after `annotation-model`, here surfacing as an explicit manifest error instead of a silent `no tests selected`. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-vocab/` | `annotation-vocab/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-vocab`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 10 files (6 `NN-*-manual.html`, `META.yml`, `tools/anno.jsonld`, `tools/README.md`, `tools/vocab_tester.py`). No out-of-category helpers — all 6 HTML reference only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. Entirely manual by spec design like `annotation-model`/`annotation-protocol` — all 6 test files are `-manual.html`, excluded by `run_report.py`'s own filter, and no `.any.js`/`.window.js` exist either (`tools/vocab_tester.py` is a Python reference implementation, not a test); `run_report.py --all --root annotation-vocab --recursive` selected 0 tests (`no tests selected`), the same silent gap as `annotation-model` rather than the manifest crash seen in `annotation-protocol`. Third vendored category with zero automatically runnable tests. See `docs/wpt-status.md` for details. |
| `tests/wpt/apng/` | `apng/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-apng`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, reftest pair `animated-png-timeout.html`+`animated-png-timeout-ref.html`, `supported-in-source-type.html`). Four out-of-category image resources (`/images/apng.png`, `/images/green.png`, `/images/anim-gr.png`, `/images/anim-gr.gif`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root apng --recursive` completed fully in ~9s (2 selected ids): the reftest pair is WPT-manifest type `reftest`, unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`accessibility`/`animation-worklet`) — both "not run"; `supported-in-source-type.html` (testharness) actually ran — 1/1 subtests passed despite a 404 on the out-of-category `/images/anim-gr.gif` (the test only checks `<picture><source>` selection via `currentSrc`, not that the image actually loaded). See `docs/wpt-status.md` for details. |
| `tests/wpt/appmanifest/` | `appmanifest/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-appmanifest`, `docs/wpt-status.md`), included despite its 🚫 scope note (PWA installation) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`. Same pinned commit, `git sparse-checkout` at the same commit hash, 253 files (`META.yml`, `WEB_FEATURES.yml`, 19 `*-member/` subcategories). No out-of-category helpers. Entirely manual by spec design — Web App Manifest is tested by installing a PWA by hand and observing the result, so every real test file is `-manual.html`/`-manual.tentative.html`/`-manual.sub.html`, excluded by `run_report.py`'s own filter. The filter still picked up 5 candidates that aren't tests at all: pass/fail/shortcut landing pages with no `testharness.js` that the manual test opens as a navigation target (`shortcuts-member/shortcut_fail.html`, `shortcuts-member/shortcut_pass.html`, `shortcuts_localized-member/shortcut_pass.html`, `start_url-member/fail.html`, `start_url-member/pass.html`); the WPT manifest can't find them as tests (`CRITICAL Unable to find any tests at the path(s)`), so `run_report.py --all --root appmanifest --recursive` crashes before `suite_end`, 0/0 — the same class of gap as `files/index.html` in `annotation-protocol`. See `docs/wpt-status.md` for details. |
| `tests/wpt/attribution-reporting/` | `attribution-reporting/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-attribution-reporting`, `docs/wpt-status.md`), included despite its 🚫 scope note (ad-tech / Privacy Sandbox) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`. Same pinned commit, `git sparse-checkout` at the same commit hash, 13 files (`WEB_FEATURES.yml`, `resources/` — `helpers.js` + `wptserve` Python handlers, `aggregatable-debug/` — 2 test files). No out-of-category helpers. `run_report.py --all --root attribution-reporting --recursive` completed in ~5 min (24 selected ids, all from 7 `.sub.https.html` files — `referrer-policy.sub.https.html` and `request-format.sub.https.html` each expand into several query-string variants): 19 SKIP (`referrer-policy`/`request-format` variants — `Executor does not support testdriver.js`, same gap as `accname`); the remaining 5 (`aggregatable-debug/simple-source-…`, `aggregatable-debug/simple-trigger-…`, `header-parsing-error-debug-report`, `simple-verbose-debug-report`, `aggregatable-report-no-contributions`) are `.https.`-only and hit the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet` — 4 TIMEOUT (`invalid url: invalid port: "None"`), 1 ERROR (`eval: automation channel closed — no live window running`, the same manifestation of the missing HTTPS port as the `ambient-light`/`animation-worklet` session-reuse-looking errors). 0/24 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/audio-output/` | `audio-output/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-audio-output`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`. Same pinned commit, `git sparse-checkout` at the same commit hash, 12 files (`META.yml`, `idlharness.https.window.js`, 9 `*.https(.sub).html`, `setSinkId-manual.https.html`). Three out-of-category helpers (`/common/get-host-info.sub.js`, `/common/gc.js`, `/permissions-policy/resources/permissions-policy.js`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root audio-output --recursive` completed in ~1 min (10 selected ids, `setSinkId-manual.https.html` excluded by `run_report.py`'s own `-manual` filter): 5 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); 3 `.https.`-only TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`); 2 ERROR — `secure-context.html` (the sole non-`.https.` test, actually served over plain HTTP) fails its own setup assumption `assert_false(window.isSecureContext)`: the spec treats `http://127.0.0.1` as a potentially-trustworthy origin ("localhost"), so `isSecureContext` correctly returns `true`, matching real browsers — the test assumes an environment without that exception, not a Lumen engine bug; `selectAudioOutput-sans-user-activation.https.html` is the same session-reuse artifact as `accelerometer`/`ambient-light`/`animation-worklet` (its result came back tagged with the previous test's URL, `secure-context.html`). 0/10 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |

Each vendored top-level directory carries its own `LICENSE-WPT.md` (WPT is
3-clause BSD, copyright web-platform-tests contributors) alongside the code.

## What's explicitly NOT vendored, and why

- **`tools/third_party/`** (upstream is ~76 MB, dominated by vendored test corpora of
  its own — e.g. `third_party/hpack/test/` alone is ~55 MB of unrelated HTTP/2 header
  compression conformance fixtures). `tools/localpaths.py` unconditionally
  `sys.path.insert`s every `third_party/<pkg>` directory, but **a nonexistent
  `sys.path` entry is a silent no-op in Python** — it only matters if nothing else on
  `sys.path` provides the module. Every leaf package WPT vendors there
  (`pywebsocket3`, `h2`, `hpack`, `hyperframe`, `websockets`, `html5lib`,
  `webencodings`, `certifi`, `atomicwrites`, …) is also a normal, actively maintained
  PyPI package, verified installable and sufficient by actually importing the full
  chain (`localpaths` → `manifest.manifest` → `tools.serve.serve` →
  `wptrunner.wptrunner` → `wptrunner.wptcommandline` → `webdriver.bidi.client`) in a
  clean venv with only `pip install -r` (see `tests/wpt/README.md`) — no
  `tools/third_party` needed. This keeps the vendor commit small and avoids
  committing megabytes of an unrelated project's test fixtures.
- **`tools/wpt/`** (the `wpt` CLI wrapper script/package). Not imported by anything
  vendored above (`wptrunner` itself doesn't depend on it — it's the outer CLI, not
  the library); it also does venv bootstrapping and browser-download machinery this
  project doesn't need (we already have our own venv + a locally built `lumen.exe`).
  S4 calls `wptcommandline`/`wptrunner.run_tests` directly instead
  (`tests/wpt/run_smoke.py`) rather than vendor the whole wrapper for one flag's
  worth of behavior; S7 ("CI wrapper + docs") is where a polished wrapper — either
  growing `run_smoke.py` or actually vendoring `tools/wpt/` — gets decided.
- **Self-test suites of the supporting packages** (`tools/manifest/tests/`,
  `tools/wptserve/tests/`, `tools/serve/test_serve.py`,
  `tools/serve/test_functional.py`) — not imported by anything on the path we
  actually exercise (verified: the import-chain smoke check in
  `tests/wpt/README.md` passes without them), so dropped to keep the vendor
  commit to library code only. One of these (`tools/wptserve/tests/functional/
  docroot/test.asis`) also had a raw-HTTP-response fixture whose bytes are
  line-ending-sensitive — Git's LF normalization (`.gitattributes`) would have
  silently altered it, which is exactly the kind of "modified vendored code"
  this task rules out; not vendoring it sidesteps the problem rather than
  fighting it. **`tools/wptrunner/wptrunner/tests/`** is kept, unlike the
  above — S2 explicitly names `tools/wptrunner/` as vendored whole, it's small
  (186 KB), and it has no such fixtures.
- **`resources/testharnessreport.js`**: intentionally **ours to write**, not
  upstream's — see `docs/tasks/p2-wpt-integration.md` S4 (the per-product results
  shim; there is no generic upstream one to vendor — `tools/wptrunner`'s own
  `wptrunner/testharnessreport.js` is wptrunner's *default*, but S4 calls for a
  Lumen-specific `tests/wpt/resources/testharnessreport.js`).

## Re-vendoring

1. Pick a new upstream commit (or re-pin the same date to check for drift).
2. `git clone --filter=blob:none --sparse --depth 1 https://github.com/web-platform-tests/wpt.git`
   at that commit, `git sparse-checkout set` the paths in the table above.
3. Copy over the destinations in the table (verbatim — no diffing/patching upstream
   files; if Lumen needs different behavior, that's a fork decision to make
   explicitly, not a silent edit).
4. Re-run the import-chain smoke check from `tests/wpt/README.md` in a clean venv.
5. Update the pinned commit hash + date at the top of this file.
