# Vendored Web Platform Tests — pin record

P2-wpt S2. Source: [`web-platform-tests/wpt`](https://github.com/web-platform-tests/wpt).

## Pinned commit

```
35be3b44f3111c4d614b5b201e399493d20e7b38
```

Committed 2026-07-13T16:23:05Z ("Cover a few fragment parsing edge cases (#61042)").
This was `master`'s tip at the time of vendoring — re-pin explicitly (do not silently
drift) when re-vendoring; record the new hash + date here.

## Vendoring method: committed snapshot, not a git submodule

A submodule would need `git submodule update` (a network fetch to GitHub) on every
fresh checkout — that violates this task's own "no live network in CI" rule
(`docs/tasks/p2-wpt-integration.md`, Prerequisites). A plain committed snapshot has
no such requirement: once vendored, the suite runs fully offline. The tradeoff is a
manual re-vendor step (re-run the process below) instead of `git submodule update`
to move the pin — acceptable since WPT is pulled in rarely and deliberately here.

## What's vendored (verbatim upstream source, not modified)

| Path (this repo) | Upstream path | Purpose |
|---|---|---|
| `tools/wptrunner/` | `tools/wptrunner/` | The test runner itself (CLI, executors incl. `webdriver-bidi`, manifest loading, expectations, formatters). |
| `tools/manifest/` | `tools/manifest/` | Test manifest generation/parsing — hard import of `wptrunner.testloader`/`wptrunner.metadata`. |
| `tools/serve/` | `tools/serve/` | The local HTTP/WS test server (`wptserve`-based) that serves this vendored test tree during a run — hard import of `wptrunner.environment`. |
| `tools/wptserve/` | `tools/wptserve/` | The HTTP/WS server library `tools/serve/serve.py` is built on. |
| `tools/webdriver/` | `tools/webdriver/` | WPT's own WebDriver/**WebDriver BiDi** Python client (`webdriver.bidi.client`, `webdriver.bidi.modules.browsing_context`, …) — this is what S3's `browsers/lumen.py` product plugin will drive against `lumen --bidi-port`. |
| `tools/metadata/` | `tools/metadata/` | `web-features` YAML schema — imported by `manifest.sourcefile`. |
| `tools/gitignore/` | `tools/gitignore/` | `.gitignore`-pattern matcher — imported by `manifest.vcs`. |
| `tools/localpaths.py` | `tools/localpaths.py` | Repo-root/sys.path bootstrap (`repo_root`, `sys.path` setup for the packages above). **Not modified** — see the sys.path note below. |
| `tests/wpt/resources/testharness.js` | `resources/testharness.js` | The real WPT client-side test harness (assertion/reporting primitives every `testharness.js`-style test imports). |
| `tests/wpt/resources/check-layout-th.js` | `resources/check-layout-th.js` | Self-contained self-checking layout helper (`WPT-RUN-1`, `ROADMAP.md`): reads `data-expected-*` attributes off elements and asserts them (`expected width = 342, actual = 318, node = <div>`) — the single most-referenced out-of-category helper in the vendored tree, **1119 files** under `css/` alone (`tests/wpt/find_missing_resources.py --root css`, superseding the 56-hit count from the old 1.1%-sample probe in `docs/wpt-status.md`). No further out-of-category dependencies of its own. |
| `resources/testdriver.js` (repo root, **not** under `tests/wpt/`) | `resources/testdriver.js` | `WPT-RUN-2` (`ROADMAP.md`, `docs/tasks/p2-wpt-runner-throughput.md`): the real WPT `test_driver`/`test_driver_internal` polyfill every `<script src="/resources/testdriver.js">` test imports. Lives at the *repo root*, not `tests/wpt/resources/`, because `environment.py::get_routes` (unmodified, not patched) hardcodes `/resources/testdriver.js`'s static route as `[<repo_root>/resources/testdriver.js, executors/message-queue.js, testdriver-extra.js]` concatenated, where `repo_root` is computed by walking three `os.pardir`s up from `tools/wptrunner/wptrunner/environment.py` — in upstream wpt that lands on the wpt checkout root (where `resources/testdriver.js` really does live); in this repo, where `tools/wptrunner` is vendored *under* `tests/wpt`'s sibling `tools/`, the same walk lands on the Lumen repo root instead. Not overridable via `env_options()` the way `testharnessreport` is (see the BUG-301 gotcha in `CLAUDE.md`) — this route has no such override hook. No further out-of-category dependencies of its own (verified — no `src=`/`import(`/`fetch(` inside the file). |
| `tests/wpt/images/` | `images/` | Shared fixture images (`WPT-RUN-3` slice 22, `ROADMAP.md`) — 84 files, 400K, referenced via `../../images/*.{gif,png,jpg}` by out-of-category tests across `css/` (found via `css/css-image-animation`'s `image-animation-pseudo-animated-image-dynamic.html`, but the same 404 gap is documented un-vendored in `apng`/`avif` rows above — vendoring once here removes it for every future category that shares the fixture instead of re-discovering it per category). No further out-of-category dependencies of its own. Does **not** fix the reference in `image-animation-pseudo-animated-image-dynamic.html` itself — that path is `../../images/anim-gr.gif` (two dot-segments), which still 404s under [BUG-346](../../bugs/BUG-346-OPEN.md) (`Url::resolve()` doesn't collapse `..`); confirmed the vendored file *is* correctly served for single-dot-segment references. |
| `tests/wpt/dom/nodes/` | `dom/nodes/` | One full test category ("start tiny", per this task's Prerequisites) — includes the S4 smoke test, `Element-hasAttribute.html` (`Document-createElement.html`, floated as an "e.g." example when this file was first drafted, turned out to need un-vendored `/common/dummy.xml`/`dummy.xhtml` iframe fixtures and `async_test` — not actually trivial; picked a genuinely self-contained synchronous test instead). |
| `tests/wpt/FileAPI/` | `FileAPI/` | Second full test category, added 2026-07-21 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-FileAPI`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash. A handful of its tests reference helper scripts outside `FileAPI/` (`/common/*.js`, `/resources/idlharness.js`, `/html/anonymous-iframe/resources/common.js`, `/service-workers/service-worker/resources/test-helpers.sub.js`) that were **not** vendored — those specific tests fail with a 404 for the missing helper, a documented survey gap in `docs/wpt-status.md`, not a Lumen engine bug. |
| `tests/wpt/IndexedDB/` | `IndexedDB/` | Third full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-IndexedDB`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 245 files (includes upstream `crashtests/` subdir and `.tentative.` variants, kept as-is). Most tests import in-category `resources/support.js` (vendored); a few reference out-of-category helpers (`/common/*.js`, `/service-workers/*`) not vendored — same documented survey gap as FileAPI. |
| `tests/wpt/WebCryptoAPI/` | `WebCryptoAPI/` | Fourth full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-WebCryptoAPI`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash (reused from the IndexedDB sparse checkout), 185 files. References only the already-vendored `/resources/testharness.js`+`testharnessreport.js` — no out-of-category helpers. 122/125 test files are `.https.`-only (`crypto.subtle` requires a secure context), which the minimal `wptrunner` executor cannot serve (no HTTPS port configured) — a documented survey gap, not a Lumen engine bug; see `docs/wpt-status.md` for the isolated run of the 2 non-`.https.` files. |
| `tests/wpt/accelerometer/` | `accelerometer/` | Fifth full test category, added 2026-07-22 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accelerometer`, `docs/wpt-status.md`), included despite its 🚫 scope note by direct user request. Same pinned commit, `git sparse-checkout` at the same commit hash, 19 files. Almost every test references `/generic-sensor/generic-sensor-tests.js` (shared test body for the whole Sensor API family) plus `/page-visibility/` and `/permissions-policy/resources/` — none vendored, same documented survey gap as FileAPI/IndexedDB; see `docs/wpt-status.md` for the run results. |
| `tests/wpt/accessibility/` | `accessibility/` | Sixth full test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accessibility`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 59 files (`ReadMe.md`, `crashtests/`, one testdriver.js test). No out-of-category helpers. Almost the whole category (58/59 files) is WPT-manifest type `crashtest`, which the minimal `browsers/lumen.py` product plugin has no executor for at all (`Unsupported test type crashtest for product lumen` — excluded before the run queue, not a per-test failure); see `docs/wpt-status.md` for the run results. |
| `tests/wpt/accname/` | `accname/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-accname`, `docs/wpt-status.md`, commit `69ab520d`). Same pinned commit, `git sparse-checkout` at the same commit hash, 183 files. Entirely `testdriver.js`-dependent — the minimal executor has no `test_driver.*` support, so all 19 selected ids come back `SKIP`; see `docs/wpt-status.md` for the run results. |
| `tests/wpt/acid/` | `acid/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-acid`, `docs/wpt-status.md`), included despite its 🚫 scope note (historical Acid1/2/3, not an actual spec) by the same standing user decision as `accelerometer`. Same pinned commit, `git sparse-checkout` at the same commit hash, 30 files (`acid2/`, `acid3/`). No out-of-category helpers. Of the 3 manifest-selected ids, 2 (`acid2/reftest.html`, `acid3/test.html`) are WPT-manifest type `reftest` — unsupported by the minimal executor (S8 reftests is still a future slice), same class of gap as `accessibility`'s crashtests; the remaining one (`acid3/numbered-tests.html`, type `testharness`) ran and TIMEOUT — it's Acid3's own multi-window/iframe scoring harness, outside this executor's single-window model. See `docs/wpt-status.md` for the run results. |
| `tests/wpt/ai/` | `ai/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-ai`, `docs/wpt-status.md`), included despite its 🚫 scope note (AI/Writer API, no LLM integration) by the same standing user decision as `accelerometer`/`acid`. Same pinned commit, `git sparse-checkout` at the same commit hash, 180 files (`classifier/`, `language-model/`, `language_detection/`, `proofreader/`, `rewriter/`, `semantic_embedder/`, `summarizer/`, `translator/`, `writer/`). One out-of-category helper (`/common/get-host-info.sub.js`) not vendored, same documented survey gap as `FileAPI`. The whole category is `.https.`-only (163 `*.window.js` + 6 direct `.html`, 0 non-https tests) — worse than `WebCryptoAPI`'s 122/125, the first category with zero isolatable test files; same HTTPS-port gap, but here a full `--all --root ai --recursive` run couldn't even be attempted to completion (10-minute trial run only reached a handful of ids, each TIMEOUT). See `docs/wpt-status.md` for details. |
| `tests/wpt/ambient-light/` | `ambient-light/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-ambient-light`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`. Same pinned commit, `git sparse-checkout` at the same commit hash, 16 files (`AmbientLightSensor*.html`, `idlharness.https.window.js`, `resources/sensor-data.js`). No out-of-category helpers. A full `--all --root ambient-light --recursive` run completed in 1:36 (10 selected ids): 2 SKIP (testdriver.js unsupported), 6 `.https.` TIMEOUT (same HTTPS-port gap as `WebCryptoAPI`/`ai`), 1 ERROR (`idlharness.https.window.html` — result came back tagged with the previous test's URL, same session-reuse artifact as `accelerometer`), 1 FAIL (`AmbientLightSensor-supported-by-permissions-policy.html` — `document.permissionsPolicy.features` doesn't list `"ambient-light-sensor"`, same pattern as `accelerometer`). See `docs/wpt-status.md` for details. |
| `tests/wpt/animation-worklet/` | `animation-worklet/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-animation-worklet`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 50 files (43 root `.html`, `META.yml`, `common.js`, `idlharness.any.js`, `references/`, `resources/`). Two out-of-category helpers not vendored: `/web-animations/testcommon.js` (used by nearly every test) and `/common/reftest-wait.js` (used by the reftest-type tests), same documented survey gap as `FileAPI`/`ai`; `idlharness.any.js` also references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, neither vendored (only `testharness.js`+`testharnessreport.js` live under `tests/wpt/resources/`). A full `--all --root animation-worklet --recursive` run completed in 7:11 (46 selected ids): 15 of the selected `.https.html` files are WPT-manifest type `reftest` — unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`accessibility`'s crashtests, S8 in `docs/tasks/p2-wpt-integration.md` still a future slice), and together with their 8 reference files that's 23 "not run"; the remaining 23 selected ids are type `testharness` (all `.https.`) and actually ran: 21 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`) + 2 ERROR (both session-reuse artifacts of the same class as `accelerometer`/`ambient-light`: one harness result came back tagged with `idlharness.any.html`'s URL instead of the expected test, the other was `eval: live window closed before replying`). 0/23 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-model/` | `annotation-model/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-model`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 290 files (259 JSON fixtures under `definitions/`/`examples/`/`tools/samples/`/`tools/tests/`, `scripts/` third-party libs `ajv.min.js`/`JSONtest.js`/`showdown.min.js`, 7 `*-manual.html`). No out-of-category helpers. Entirely manual by spec design (README: no general automation mechanism, only a textarea-driven `JSONtest.js` harness for a human to paste client JSON output into) — all 7 HTML files are `-manual.html`, and `run_report.py --all --root annotation-model --recursive` explicitly excludes `-manual.html` and found no `.any.js`/`.window.js` either, so it selected 0 tests (`no tests selected`) — the first vendored category with zero automatically runnable tests, a harder gap than the `testdriver.js`/`crashtest` gaps in `accname`/`accessibility`. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-protocol/` | `annotation-protocol/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-protocol`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 58 files (`files/annotations/` — 43 JSON/JSON-LD fixtures + `*.headers`, `files/index.html` — a manual client-testing target page, `server/server-manual.html` — the sole test file, `tools/protocol-server.py` — reference protocol server). One out-of-category helper (`/common/utils.js`, used by `server-manual.html`) not vendored, same documented survey gap as `FileAPI`/`ai`. Entirely manual by spec design like `annotation-model` (README: no general automation mechanism, workflow requires a human driving an annotation client/server) — the sole test file is `-manual.html` and gets excluded by `run_report.py`'s own filter; the one remaining non-`-manual` candidate, `files/index.html`, isn't a test at all (no `testharness.js`, just a fixture page) so the WPT manifest can't find it (`CRITICAL Unable to find any tests at the path(s)`), crashing the run before `suite_end` — 0/0. Second vendored category with zero automatically runnable tests after `annotation-model`, here surfacing as an explicit manifest error instead of a silent `no tests selected`. See `docs/wpt-status.md` for details. |
| `tests/wpt/annotation-vocab/` | `annotation-vocab/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-annotation-vocab`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 10 files (6 `NN-*-manual.html`, `META.yml`, `tools/anno.jsonld`, `tools/README.md`, `tools/vocab_tester.py`). No out-of-category helpers — all 6 HTML reference only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. Entirely manual by spec design like `annotation-model`/`annotation-protocol` — all 6 test files are `-manual.html`, excluded by `run_report.py`'s own filter, and no `.any.js`/`.window.js` exist either (`tools/vocab_tester.py` is a Python reference implementation, not a test); `run_report.py --all --root annotation-vocab --recursive` selected 0 tests (`no tests selected`), the same silent gap as `annotation-model` rather than the manifest crash seen in `annotation-protocol`. Third vendored category with zero automatically runnable tests. See `docs/wpt-status.md` for details. |
| `tests/wpt/apng/` | `apng/` | Test category, added 2026-07-23 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-apng`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, reftest pair `animated-png-timeout.html`+`animated-png-timeout-ref.html`, `supported-in-source-type.html`). Four out-of-category image resources (`/images/apng.png`, `/images/green.png`, `/images/anim-gr.png`, `/images/anim-gr.gif`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root apng --recursive` completed fully in ~9s (2 selected ids): the reftest pair is WPT-manifest type `reftest`, unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`accessibility`/`animation-worklet`) — both "not run"; `supported-in-source-type.html` (testharness) actually ran — 1/1 subtests passed despite a 404 on the out-of-category `/images/anim-gr.gif` (the test only checks `<picture><source>` selection via `currentSrc`, not that the image actually loaded). See `docs/wpt-status.md` for details. |
| `tests/wpt/appmanifest/` | `appmanifest/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-appmanifest`, `docs/wpt-status.md`), included despite its 🚫 scope note (PWA installation) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`. Same pinned commit, `git sparse-checkout` at the same commit hash, 253 files (`META.yml`, `WEB_FEATURES.yml`, 19 `*-member/` subcategories). No out-of-category helpers. Entirely manual by spec design — Web App Manifest is tested by installing a PWA by hand and observing the result, so every real test file is `-manual.html`/`-manual.tentative.html`/`-manual.sub.html`, excluded by `run_report.py`'s own filter. The filter still picked up 5 candidates that aren't tests at all: pass/fail/shortcut landing pages with no `testharness.js` that the manual test opens as a navigation target (`shortcuts-member/shortcut_fail.html`, `shortcuts-member/shortcut_pass.html`, `shortcuts_localized-member/shortcut_pass.html`, `start_url-member/fail.html`, `start_url-member/pass.html`); the WPT manifest can't find them as tests (`CRITICAL Unable to find any tests at the path(s)`), so `run_report.py --all --root appmanifest --recursive` crashes before `suite_end`, 0/0 — the same class of gap as `files/index.html` in `annotation-protocol`. See `docs/wpt-status.md` for details. |
| `tests/wpt/attribution-reporting/` | `attribution-reporting/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-attribution-reporting`, `docs/wpt-status.md`), included despite its 🚫 scope note (ad-tech / Privacy Sandbox) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`. Same pinned commit, `git sparse-checkout` at the same commit hash, 13 files (`WEB_FEATURES.yml`, `resources/` — `helpers.js` + `wptserve` Python handlers, `aggregatable-debug/` — 2 test files). No out-of-category helpers. `run_report.py --all --root attribution-reporting --recursive` completed in ~5 min (24 selected ids, all from 7 `.sub.https.html` files — `referrer-policy.sub.https.html` and `request-format.sub.https.html` each expand into several query-string variants): 19 SKIP (`referrer-policy`/`request-format` variants — `Executor does not support testdriver.js`, same gap as `accname`); the remaining 5 (`aggregatable-debug/simple-source-…`, `aggregatable-debug/simple-trigger-…`, `header-parsing-error-debug-report`, `simple-verbose-debug-report`, `aggregatable-report-no-contributions`) are `.https.`-only and hit the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet` — 4 TIMEOUT (`invalid url: invalid port: "None"`), 1 ERROR (`eval: automation channel closed — no live window running`, the same manifestation of the missing HTTPS port as the `ambient-light`/`animation-worklet` session-reuse-looking errors). 0/24 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/audio-output/` | `audio-output/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-audio-output`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`. Same pinned commit, `git sparse-checkout` at the same commit hash, 12 files (`META.yml`, `idlharness.https.window.js`, 9 `*.https(.sub).html`, `setSinkId-manual.https.html`). Three out-of-category helpers (`/common/get-host-info.sub.js`, `/common/gc.js`, `/permissions-policy/resources/permissions-policy.js`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root audio-output --recursive` completed in ~1 min (10 selected ids, `setSinkId-manual.https.html` excluded by `run_report.py`'s own `-manual` filter): 5 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); 3 `.https.`-only TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`); 2 ERROR — `secure-context.html` (the sole non-`.https.` test, actually served over plain HTTP) fails its own setup assumption `assert_false(window.isSecureContext)`: the spec treats `http://127.0.0.1` as a potentially-trustworthy origin ("localhost"), so `isSecureContext` correctly returns `true`, matching real browsers — the test assumes an environment without that exception, not a Lumen engine bug; `selectAudioOutput-sans-user-activation.https.html` is the same session-reuse artifact as `accelerometer`/`ambient-light`/`animation-worklet` (its result came back tagged with the previous test's URL, `secure-context.html`). 0/10 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/audio-session/` | `audio-session/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-audio-session`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`. Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`audiosession-default-values.https.html`, `audiosession-type-setter.https.html`, `idlharness.window.js`). One out-of-category dependency pair not vendored — `idlharness.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`animation-worklet`. `run_report.py --all --root audio-session --recursive` completed in ~1:30 (3 selected ids, all category-root files): 2 `.https.`-only TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`/`audio-output`); `idlharness.window.js` actually ran over plain HTTP but 404'd on its two out-of-category dependencies (`idl_test is not defined`), so the harness never reported — TIMEOUT. 0/3 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/autoplay-policy-detection/` | `autoplay-policy-detection/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-autoplay-policy-detection`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`. Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `autoplaypolicy.html`, `autoplaypolicy_media_element.html`, `idlharness.window.js`). Two out-of-category dependencies not vendored: `autoplaypolicy_media_element.html` references `/common/media.js`, `idlharness.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root autoplay-policy-detection --recursive` completed in ~13s (3 selected ids, none `.https.` — the first category in this backlog run with no HTTPS-port gap): `autoplaypolicy.html` ran (harness OK) but all 3 subtests FAIL — `window.navigator.getAutoplayPolicy is not a function` (the Autoplay Policy Detection API itself isn't implemented in Lumen, expected on a 🚫-scope category's first pass); `autoplaypolicy_media_element.html` ran (harness OK) but both subtests FAIL (`getVideoURI is not defined`, 404 on the un-vendored `/common/media.js`); `idlharness.window.js` actually ran over plain HTTP but 404'd on its two out-of-category dependencies (`idl_test is not defined`), so the harness never reported — TIMEOUT. 2/3 harness OK, 0/5 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/avif/` | `avif/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-avif`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `WEB_FEATURES.yml`, reftest pair `animated-avif-timeout.html`+`animated-avif-timeout-ref.html`). Two out-of-category image resources (`/images/animated-avif.avif`, `/images/green.avif`) not vendored, same documented survey gap as `apng`/`FileAPI`. The category's sole test is the reftest `animated-avif-timeout.html` (`<link rel="match">`) — WPT-manifest type `reftest`, unsupported by the minimal executor (same class of gap as `acid`/`apng`/`animation-worklet`), while its partner `animated-avif-timeout-ref.html` is a *reference* file, not a test — so `run_report.py --all --root avif --recursive` completed in ~5s having selected 0 runnable tests (`Ran 0 checks` / `No tests ran` CRITICAL, plus a harmless empty-queue `AttributeError` in wptrunner's cleanup thread), 0/0. First vendored category whose *only* test is a reftest — the same net "zero automatically runnable tests" outcome as `annotation-model`/`annotation-vocab` (manual-only) but reached through the reftest gap instead; unlike `apng` (which had a `testharness` test alongside its reftest), there is nothing here for the executor to run at all. See `docs/wpt-status.md` for details. |
| `tests/wpt/background-fetch/` | `background-fetch/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-background-fetch`, `docs/wpt-status.md`), included despite its 🚫 scope note (Service Worker extension / background OS-integration) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`. Same pinned commit, `git sparse-checkout` at the same commit hash, 20 files (`META.yml`, `WEB_FEATURES.yml`, 11 test files, `resources/` — `utils.js`+`upload.py`+`feature-name.txt`, `service_workers/` — 4 SW scripts). Out-of-category helpers not vendored: `/service-workers/service-worker/resources/test-helpers.sub.js` (imported by every `.window.js` test), `/common/get-host-info.sub.js` (`fetch`/`match`), and `idlharness.https.any.js`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`/`audio-session`. The whole category is `.https.`-only (all 11 selected test ids), same class as `WebCryptoAPI` (122/125) and `ai` (all) — the second category after `ai` with zero isolatable non-https files, but unlike `ai` a full `run_report.py --all --root background-fetch --recursive` run completed (~4:13): all 11 ids TIMEOUT with the same HTTPS-port gap (`invalid url: invalid port: "None"`, the minimal executor serves no HTTPS port) as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`/`audio-output`/`audio-session`. 0/11 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/background-sync/` | `background-sync/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-background-sync`, `docs/wpt-status.md`), included despite its 🚫 scope note (Service Worker extension / background OS-integration) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`. Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `WEB_FEATURES.yml`, `idlharness.https.any.js`, `service_workers/sw.js`). One out-of-category helper pair not vendored: `idlharness.https.any.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`/`background-fetch`. Smallest category vendored so far: the manifest selects exactly one id, `idlharness.https.any.html` (`.https.`-only, `META: timeout=long`), which hits the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`/`animation-worklet`/`attribution-reporting`/`audio-output`/`audio-session`/`background-fetch` — `run_report.py --all --root background-sync --recursive` completed in ~1:32, TIMEOUT (`invalid url: invalid port: "None"`). 0/1 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/badging/` | `badging/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-badging`, `docs/wpt-status.md`), included despite its 🚫 scope note (PWA / OS integration) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`. Same pinned commit, `git sparse-checkout` at the same commit hash, 9 files (`META.yml`, `WEB_FEATURES.yml`, `badge-error.https.any.js`, `badge-success.https.any.js`, `idlharness.https.any.js`, `non-fully-active.https.html`, `setAppBadge_cross_origin.sub.https.html`, `resources/badge_iframe.html`, `resources/message-handler.js`). The whole category is `.https.`-only (all 5 selected test ids), same class as `background-sync`/`background-fetch`/`WebCryptoAPI`/`ai` — `run_report.py --all --root badging --recursive` completed in ~2:36: all 5 ids TIMEOUT with the same HTTPS-port gap (`invalid url: invalid port: "None"`, the minimal executor serves no HTTPS port). 0/5 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/battery-status/` | `battery-status/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-battery-status`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`/`badging`. Same pinned commit, `git sparse-checkout` at the same commit hash, 30 files (`META.yml`, `WEB_FEATURES.yml`, 22 test/header files, `resources/` — `battery-status-helpers.js`+`support-iframe.html`+`support-window-open.html`+`window-postmessage-open-close.html`). One out-of-category dependency pair not vendored — `idlharness.https.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`/`background-sync`/`badging`. The manifest selects 17 ids (5 `-manual.https.html` files are excluded as non-automatic, matching upstream convention): `page-visibility.https.html` SKIP (`Executor does not support testdriver.js`, same known executor gap as elsewhere in this backlog); the other 16 (all `.https.`-only) hit the same HTTPS-port gap as `background-sync`/`background-fetch`/`badging`/`WebCryptoAPI`/`ai` — `run_report.py --all --root battery-status --recursive` completed in ~5:00: 16 TIMEOUT (`invalid url: invalid port: "None"`), 1 SKIP. 0/17 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/beacon/` | `beacon/` | Test category, added 2026-07-24 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-beacon`, `docs/wpt-status.md`), in-scope (⬜, Beacon API — `navigator.sendBeacon`). Same pinned commit, `git sparse-checkout` at the same commit hash, 22 files (`META.yml`, `WEB_FEATURES.yml`, 5 root `.https.` test files, `beacon-common.sub.js`, `idlharness.any.js`, `headers/` — 9 test files + `header-referrer.js`, `resources/` — 4 Python handlers). Out-of-category helpers not vendored: `/common/utils.js`, `/common/get-host-info.sub.js`, `/common/redirect.py`, `/common/blank.html`, `/common/text-plain.txt`, and `idlharness.any.js`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`/`audio-session`. First category in this backlog that is not entirely `.https.` (9 of 16 selected ids are plain HTTP), which for the first time surfaced a real effect of the un-vendored `/common/*` helpers on execution rather than just the HTTPS-port TIMEOUT: `headers/header-content-type-and-body.html` harness OK but 0/6 subtests FAIL (`self.token is not a function`, `/common/utils.js` 404); three more non-https tests TIMEOUT (same `self.token`/`get_host_info is not defined` errors thrown before test registration, hanging the harness instead of reporting); `idlharness.any.html` TIMEOUT (`idl_test is not defined`, same pattern as `audio-session`/`animation-worklet`). The 9 `.https.` tests hit the usual HTTPS-port gap (`invalid url: invalid port: "None"`) — 7 TIMEOUT, 2 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`ambient-light`/`animation-worklet`). `run_report.py --all --root beacon --recursive` completed in ~6:16. 1/16 harness OK, 0/6 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/bluetooth/` | `bluetooth/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-bluetooth`, `docs/wpt-status.md`), included despite its 🚫 scope note (hardware API, no device-integration layer) by the same standing user decision as `accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`attribution-reporting`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`/`badging`/`battery-status`. Same pinned commit, `git sparse-checkout` at the same commit hash, 187 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, `resources/` — 4 helpers, `bidi/` — 9 subcategories `adapter`/`characteristic`/`descriptor`/`device`/`getDevices`/`idl`/`requestDevice`/`script-tests`/`server`/`service` + `generate.py`+`generate_test.py`). Out-of-category helper not vendored: `/resources/testdriver.js`+`/resources/testdriver-vendor.js` (only referenced via `META: script=`; doesn't block a run unless a test actually issues a testdriver command). Largest category in this backlog by selected id count (155: 153 `.https.`-only, 2 non-https `idl-*` files) — the full `run_report.py --all --root bluetooth --recursive` did not complete: a bounded trial run (~11 min) processed 10 of the 153 `.https.` ids, all TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`), same limitation class as `ai` (a full run at this pace would take hours). The 2 non-https `idl-*` tests were run in isolation via `run_smoke.py`: 2/2 harness OK, 0/15 subtests (0/14 + 0/1) — `BluetoothUUID is not defined` and similarly for `navigator.bluetooth`, the Web Bluetooth API itself is unimplemented, expected for a 🚫-scope category. See `docs/wpt-status.md` for details. |
| `tests/wpt/browsing-topics/` | `browsing-topics/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-browsing-topics`, `docs/wpt-status.md`), included despite its 🚫 scope note (ad-tech / Privacy Sandbox) by the same standing user decision as `attribution-reporting`/`accelerometer`/`acid`/`ai`/`ambient-light`/`appmanifest`/`audio-output`/`audio-session`/`autoplay-policy-detection`/`background-fetch`/`background-sync`/`badging`/`battery-status`/`bluetooth`. Same pinned commit, `git sparse-checkout` at the same commit hash, 18 files (`WEB_FEATURES.yml`, 13 test files, `resources/` — 3 Python handlers + `pixel.png` + 4 JS helpers). No out-of-category helpers. `run_report.py --all --root browsing-topics --recursive` completed in ~2:37 (13 selected ids: 10 `.https.`-only + 3 non-https): 8 `.https.` TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`) + 2 `.https.` ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`); `iframe-topics-attribute-insecure-context.tentative.http.sub.html` TIMEOUT via a different path — the file itself is served over plain HTTP but embeds a `{{ports[https][0]}}` placeholder for a nested iframe, and wptserve's own template substitution 500s on the empty `ports["https"]` (same root HTTPS-port gap, surfacing server-side instead of client-side). The 2 remaining non-https tests both ran (harness OK) but failed their sole subtest: `document-api-insecure-context.tentative.http.html` expects a `TypeError` from an explicit `try`/`catch` around `document.browsingTopics()` (unimplemented, expected for a 🚫-scope category) but got `assert_unreached` instead — the call didn't throw at all; `fetch-topics-insecure-context.tentative.http.html` got `fetch error: invalid url: invalid url: missing scheme: "./resources/check-topics-request-header.py"` — a relative URL passed to `fetch()` failed to resolve against the document base, a possible engine gap. 2/13 harness OK, 0/2 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |

| `tests/wpt/captured-mouse-events/` | `captured-mouse-events/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-captured-mouse-events`, `docs/wpt-status.md`), in-scope (⬜, Captured Mouse Events — `CapturedMouseEvent`/`CaptureController` extensions for Screen Capture). Same pinned commit, `git sparse-checkout` at the same commit hash, 6 files (`META.yml`, `capture-controller-oncapturedmousechange.https.html`, `captured-mouse-event-constructor.html`, `captured-mouse-event-constructor-inherited.html`, `capturing-mouse-coordinates-manual.tentative.https.html`, `idlharness.https.window.js`). One out-of-category dependency pair not vendored: `idlharness.https.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root captured-mouse-events --recursive` completed fully (4 selected ids, `capturing-mouse-coordinates-manual.tentative.https.html` excluded by the `-manual` filter): the 2 non-https constructor tests ran (harness OK) but all 6 subtests FAIL (`CapturedMouseEvent is not defined` — the API itself isn't implemented in Lumen, expected on a first pass); `capture-controller-oncapturedmousechange.https.html` TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`/`browsing-topics`); `idlharness.https.window.html` ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`browsing-topics`). 2/4 harness OK, 0/6 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |

| `tests/wpt/clear-site-data/` | `clear-site-data/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-clear-site-data`, `docs/wpt-status.md`), in-scope (⬜, Clear-Site-Data API — the `Clear-Site-Data` HTTP response header). Same pinned commit, `git sparse-checkout` at the same commit hash, 28 files (`META.yml`, `WEB_FEATURES.yml`, 13 test files, `support/` — 5 Python handlers + `clear-cache-helper.sub.js`+`test_utils.sub.js`+`iframe_executionContexts.html`+`page_using_service_worker.html`+`page_with_resource.sub.html`+`send_report.html`+`service_worker.js`). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js`, `/common/dispatcher/dispatcher.js`, `/common/utils.js`, `/common/get-host-info.sub.js`, `/html/browsers/browsing-the-web/back-forward-cache/resources/helper.sub.js`, `../service-workers/service-worker/resources/test-helpers.sub.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root clear-site-data --recursive` completed fully (13 selected ids): 4 `clear-cache*` SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); `executionContexts.sub.html` harness OK but 0/2 subtests FAIL (`get_host_info is not defined`, 404 on the un-vendored `/common/get-host-info.sub.js`); `navigation.https.html`/`set-cookie-after-clear-all.https.html`/`set-cookie-after-clear-cookies.https.html`/`set-cookie-before-clear-cookies.https.html` ERROR (the latter two `WebSocket connection closed`); `set-cookie-before-clear-all.https.html`/`storage.https.html` TIMEOUT — all 6 the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`. A new manifestation of that same gap: `navigation-insecure.html` (non-https) and `resource.html` both load the category's own vendored `support/test_utils.sub.js`, which itself embeds a `{{ports[https][0]}}` placeholder — wptserve 500s on the substitution (`AssertionError: deque([])`, empty `ports["https"]`), the first time this surfaces inside a category's own vendored file rather than only in un-vendored out-of-category helpers; `resource.html` harness OK but 0/4 subtests FAIL (`TestUtils is not defined`), `navigation-insecure.html` TIMEOUT (harness hangs instead of reporting). 2/13 harness OK, 0/6 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |

| `tests/wpt/client-hints/` | `client-hints/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-client-hints`, `docs/wpt-status.md`), in-scope (⬜, Client Hints Infrastructure — WICG spec). Same pinned commit, `git sparse-checkout` at the same commit hash, 276 files (`META.yml`, `accept-ch`/`accept-ch-stickiness`/`clear-site-data`/`critical-ch`/`inner-delegation`/`permissions-policy`/`sandbox`/`service-workers`/`viewport-size` subcategories + root-level tests + `resources/`). Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/common/utils.js`, same documented survey gap as `clear-site-data`/`FileAPI`. Manifest selects 133 ids: 127 `.https.`-only + 6 non-https — a large category, comparable in order of magnitude to `WebCryptoAPI` (125) — `run_report.py --all --root client-hints --recursive` did not complete: a bounded trial run (~11 min, stopped manually) processed 17 of the 133 ids: 15 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`), 1 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`captured-mouse-events`/`clear-site-data`); the one non-https test that ran, `accept-ch/non-secure.http.html`, had harness OK but 0/1 subtest FAIL (`fetch error: invalid url: missing scheme` for a relative URL, `resources/echo-client-hints-received.py` — the same relative-`fetch()`-resolution engine gap seen in `browsing-topics`' `fetch-topics-insecure-context` test). 1/17 harness OK, 0/1 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |

| `tests/wpt/clipboard-apis/` | `clipboard-apis/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-clipboard-apis`, `docs/wpt-status.md`), in-scope (⬜, Clipboard API and events — W3C `clipboard-apis` spec). Same pinned commit, `git sparse-checkout` at the same commit hash, 76 files (`META.yml`, root-level tests, `detached-iframe/`+`events/`+`permissions/`+`permissions-policy/` subcategories each with its own `WEB_FEATURES.yml`). Out-of-category helpers not vendored: `/common/subset-tests.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/testdriver.js`+`testdriver-vendor.js`+`testdriver-action.js`+`testdriver-actions.js`, same documented survey gap as `FileAPI`/`ai`. Manifest selects 58 ids: 53 `.https.`-only + 5 non-https — `run_report.py --all --root clipboard-apis --recursive` completed fully (~34s): 51 SKIP (`Executor does not support testdriver.js` — 50 from the `.https.` subset + 1 non-https `events/copy-event.html`, synthetic copy/cut/paste and permission tests need testdriver-driven emulation, same gap as `accname`/`attribution-reporting`/`clear-site-data`); 3 `.https.` ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`captured-mouse-events`/`client-hints`); of the remaining 4 non-https: `drag-multiple-urls.html` TIMEOUT (harness hangs on drag-and-drop `text/uri-list` parsing, unrelated to the HTTPS-port gap); `clipboard-events-synthetic.html` harness OK but 0/9 subtests FAIL (synthetic copy/cut/paste events don't set `composed`); `data-transfer-file-list-change-reference-updates.html` harness OK but 0/1 subtest FAIL (`DataTransfer.files` doesn't reflect changes); `dataTransfer-clearData.html` harness OK but 0/1 subtest FAIL (`DataTransfer.clearData()` doesn't work). 3/58 harness OK, 0/12 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |

| `tests/wpt/close-watcher/` | `close-watcher/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-close-watcher`, `docs/wpt-status.md`), in-scope (⬜, CloseWatcher API — WICG spec, already implemented in Lumen as an engine-agnostic JS shim, `crates/js/src/close_watcher.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 54 files (`META.yml`, `WEB_FEATURES.yml`, root-level tests, `esc-key/`+`iframes/`+`user-activation/` subcategories, `resources/helpers.js`). Out-of-category helpers not vendored: `/common/top-layer.js`, `/resources/testdriver.js`+`testdriver-vendor.js`+`testdriver-actions.js`, same documented survey gap as `FileAPI`/`clipboard-apis`. Manifest selects 65 ids (49 files, several with `?dialog`/`?CloseWatcher` query variants) — `run_report.py --all --root close-watcher --recursive` completed fully (~19s): 63 SKIP (`Executor does not support testdriver.js`, all of `user-activation/`+`esc-key/`); `frame-removal.html` TIMEOUT (detaching an iframe during the `cancel` event hangs the harness, 0/6 unexpected); `inside-event-listeners.html` harness OK but 8/12 subtests, 4 FAIL — surfaced a genuine engine bug rather than an unimplemented-API gap: `CloseWatcher.requestClose()`, called re-entrantly from its own `oncancel` handler, recurses without bound (`this._closed` is only set inside `_fireClose()`, i.e. after `cancel` has already dispatched, so the re-entrant call re-enters the same guard and fires another `cancel`) — recorded event arrays of length 9497/5520 instead of 2. Filed as [BUG-340](../../bugs/BUG-340-OPEN.md) (unlike most first-pass vendor entries, this one *is* filed — it's a functional defect in an implemented feature, not a missing API). 1/65 harness OK, 8/18 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/compat/` | `compat/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-compat`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 56 files — mostly webkit-compat reftest pairs (visual comparison, not testharness.js) plus 11 testharness tests. 9 of the 11 reference only already-vendored `/resources/testharness.js`+`testharnessreport.js`; 2 (`webkit-box-item-shrink-001/002`) additionally reference `/fonts/ahem.css`+`/resources/check-layout-th.js` — not vendored, same documented survey gap as `FileAPI`/`IndexedDB`. `run_report.py --all --root compat --recursive`: 13 selected ids (reftest pairs aren't picked up by this runner — no screenshot-diff support), 10/13 harness OK, 9/71 subtests passed. See `docs/wpt-status.md` for the failure clusters (legacy `webkit*` CSSOM aliases unimplemented, `-webkit-box` vertical centering, lenient `-webkit-gradient()`/`-webkit-radial-gradient()` parsing, `::-webkit-scrollbar` `insertRule` crash). |
| `tests/wpt/compression/` | `compression/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-compression`, `docs/wpt-status.md`), in-scope (⬜, Compression Streams API — WICG spec, `CompressionStream`/`DecompressionStream` already implemented in Lumen, `crates/js/src/dom.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 28 files (`META.yml`, `WEB_FEATURES.yml`, 18 `.any.js` compress/decompress tests, `idlharness.https.any.js`, `resources/` — 4 JS helpers, `third_party/pako/` — reference inflate library the tests use to cross-check our compressed output independent of our own `DecompressionStream`). Two tests reference un-vendored `/media/foo.vtt`+`/media/test-av-384k-…webm`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root compression --recursive` completed fully (19 selected ids, ~2:12): 14/19 harness OK, 20/317 subtests passed; 2 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`client-hints`), 3 TIMEOUT. Unlike most first-pass entries, most of the FAIL volume isn't a missing API — it's a genuine **V8-specific engine bug**: a live `script.evaluate` diagnostic (bypassing the Streams API) showed `_lumen_compress_bytes`/`_lumen_sha_digest` always receive an **empty** `Vec<u8>` argument under V8 regardless of what's actually passed from JS, root-caused to `v8_to_jsvalue` (`crates/js/src/v8_compat.rs`) silently mapping any Array/Object argument to `JsValue::Null` instead of converting it (unlike the parallel `from_v8` in `v8_runtime.rs`, which handles arrays correctly). `cargo test -p lumen-js`'s existing coverage didn't catch this — those unit tests run through `QuickJsRuntime` (rquickjs), never the non-default `v8-backend` feature. Filed as [BUG-342](../../bugs/BUG-342-OPEN.md) (broad impact: any `_lumen_*` native taking a `Vec<u8>`/`Vec<u32>`/`Vec<String>`/`Vec<f64>` argument under V8, including `WebCryptoAPI`'s `crypto.subtle.digest` — not separately re-verified there). See `docs/wpt-status.md` for details. |
| `tests/wpt/compute-pressure/` | `compute-pressure/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-compute-pressure`, `docs/wpt-status.md`), included despite its 🚫 scope note (Compute Pressure API — device CPU/GPU load sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`. Same pinned commit, `git sparse-checkout` at the same commit hash, 37 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, 19 root `.https.` tests, `permissions-policy/` subdir — 9 files + 2 `.headers`, `resources/` — 4 JS helpers). Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`. The whole category is `.https.`-only bar one file (39 selected ids — 19 files × `globalScope=window`/`dedicated_worker` variants). `run_report.py --all --root compute-pressure --recursive` completed fully (~19min): 38 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`ambient-light`); the one non-`.https.` file, `permissions-policy/compute-pressure-supported-by-permissions-policy.html`, ran to completion — harness OK, 0/1 subtest FAIL (`document.permissionsPolicy.features` doesn't list `"compute-pressure"`, same pattern as `accelerometer`). 1/39 harness OK, 0/1 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/connection-allowlist/` | `connection-allowlist/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-connection-allowlist`, `docs/wpt-status.md`), included despite its 🚫 scope note (niche corporate API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`compute-pressure`. Same pinned commit, `git sparse-checkout` at the same commit hash, 183 files — the entire category lives under a single `tentative/` subdir, with no category-level `META.yml`/`WEB_FEATURES.yml`. Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/common/utils.js`, `/common/blank.html`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root connection-allowlist --recursive` completed fully (~11min, 73 selected ids): 46 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`), 10 ERROR (session/crash-restart artifact — `eval: live window closed before replying`/`WebSocket connection closed`, same class as `accelerometer`/`beacon`); 17 harness OK: 14 (6 `early_hints_*` + 8 `link_header_*`) each 0/1 subtest FAIL (`navigateToTestCase is not defined` — the category's own `resources/early-hints-helpers.sub.js` 500s from the same HTTPS-port-template gap as `clear-site-data`'s `support/test_utils.sub.js`, plus the un-vendored `/common/utils.js`); 3 `webrtc-*` — `webrtc-allow` 1/2 (Connection-Allowlist isn't implemented, so "allow" trivially passes), `webrtc-block(-default)` 0/2 each (`assert_equals: expected "NotAllowedError" but got "Error"` — WebRTC isn't gated at all, expected on a first pass), all three additionally FAIL a second subtest on a relative `fetch("/common/blank.html")`, same relative-URL gap as `browsing-topics`/`client-hints`. 1/20 subtests passed. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/console/` | `console/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-console`, `docs/wpt-status.md`), in-scope (⬜, Console API — already partially implemented in Lumen as a plain JS object literal `console = {log, warn, error, info, debug}`, `crates/js/src/dom.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 16 files (`META.yml`, `WEB_FEATURES.yml`, `helper.js`, `idlharness.any.js`, 11 test files). No out-of-category helpers. `run_report.py --all --root console --recursive` completed fully (~55s, 12 selected ids): 5 SKIP (`Executor does not support testdriver.js`); `console-is-a-namespace.any.html` — harness OK, 3/4, 1 FAIL (`console` isn't a proper WebIDL namespace object — no dedicated empty-prototype layer between `console` and `Object.prototype`, so `Object.getPrototypeOf(console)` resolves straight to `Object.prototype`, which carries 12 standard own properties instead of the expected 0); `console-label-conversion.any.html` — harness OK, 0/10, all FAIL (`console.count`/`countReset`/`time`/`timeLog`/`timeEnd` are entirely missing — only `log`/`warn`/`error`/`info`/`debug` exist); `console-log-large-array.any.html` — TIMEOUT (logging a 10-million-element array hangs — the naive `Array.prototype.join.call(arguments, ' ')` implicitly ToStrings the whole array); `console-log-symbol.any.html` — harness OK, 0/1 FAIL (`console.log(symbol)` throws `Cannot convert a Symbol value to a string` instead of the spec's no-throw requirement — same `join`-based stringify can't handle Symbol arguments); `console-namespace-object-class-string.any.html` — harness OK, 0/4 FAIL (no `Symbol.toStringTag` — `Object.prototype.toString(console)` returns `"[object Object]"` instead of `"[object console]"`); `console-tests-historical.any.html` — harness OK, 3/3 passed; `idlharness.any.html` — TIMEOUT. 5/12 harness OK, 6/22 subtests passed. Not filed as a separate BUG-NNN — first pass, several distinct console-implementation gaps rather than one root cause. See `docs/wpt-status.md` for details. |
| `tests/wpt/contacts/` | `contacts/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-contacts`, `docs/wpt-status.md`), included despite its 🚫 scope note (Contact Picker API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`console`. Same pinned commit, `git sparse-checkout` at the same commit hash, 5 files (`META.yml`, `WEB_FEATURES.yml`, `contacts-select.https.window.js`, `resources/` — `helpers.js`+`non-main-frame-select.html`). No out-of-category helpers. The category's sole test is `.https.`-only (1 selected id) — `run_report.py --all --root contacts --recursive` completed fully: TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`compute-pressure`). 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/container-timing/` | `container-timing/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-container-timing`, `docs/wpt-status.md`), in-scope (⬜, an experimental Performance Timeline extension). Same pinned commit, `git sparse-checkout` at the same commit hash, 22 files (`META.yml`, `resources/` — `container-timing-helpers.js`+`square100.png`, `tentative/` — 19 test files). Out-of-category helper not vendored: `/element-timing/resources/element-timing-helpers.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root container-timing --recursive` completed fully (~17s, 19 selected ids): all 19 harness OK, 0/19 subtests, every one FAIL on `assert_implements: PerformanceContainerTiming is not implemented` — the Container Timing API isn't implemented in Lumen at all. Not filed as a separate BUG-NNN — single root cause (API entirely unimplemented), first pass. See `docs/wpt-status.md` for details. |
| `tests/wpt/content-dpr/` | `content-dpr/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-content-dpr`, `docs/wpt-status.md`), in-scope (⬜, a legacy WebKit-specific `Content-DPR` HTTP response header applied to `<img>`). Same pinned commit, `git sparse-checkout` at the same commit hash, 14 files (7 test files, `resources/` — `dpr.py`+`background.png`+`background.svg`+`square.png`), no category-level `META.yml`/`WEB_FEATURES.yml`. No out-of-category helpers. `run_report.py --all --root content-dpr --recursive` completed fully (~14s, 6 selected ids): 5 are WPT-manifest type `reftest`, unsupported by the minimal executor (same class of gap as `acid`/`apng`/`animation-worklet`/`avif`) — not run; the sole `testharness` test, `image-with-dpr-header.html`, ran (harness OK) but its one subtest FAIL — every `<img>`'s `naturalWidth` came back `undefined` rather than a number, because `HTMLImageElement.naturalWidth`/`naturalHeight` aren't implemented in the JS shim at all (`crates/js/`), a gap wider than this category alone. Not filed as a separate BUG-NNN — first pass, a missing getter rather than a functional defect. 1/6 harness OK, 0/1 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/content-index/` | `content-index/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-content-index`, `docs/wpt-status.md`), in-scope (⬜, WICG Content Index API — already partially implemented in Lumen as a Phase 0 in-memory shim, `crates/js/src/content_index.rs`, `ContentIndex.add`/`getAll`/`delete` on `ServiceWorkerRegistration.index`). Same pinned commit, `git sparse-checkout` at the same commit hash, 6 files (`META.yml`, `WEB_FEATURES.yml`, `content-index.https.window.js`, `idlharness.https.any.js`, `resources.js`, `resources/sw.js`). No out-of-category helpers. `run_report.py --all --root content-index --recursive` completed fully (2 selected ids, both `.https.`-only): both TIMEOUT — the same HTTPS-port gap of the minimal executor as `WebCryptoAPI`/`ai`/`compute-pressure`/`contacts`, so the existing shim was never exercised. Not filed as a separate BUG-NNN — first pass, a known infra gap. 0/2 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/content-security-policy/` | `content-security-policy/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-content-security-policy`, `docs/wpt-status.md`), in-scope (⬜, Content Security Policy — already partially implemented in Lumen: directive/source-list parsing in `crates/network/src/csp.rs`, persisted report-only policies in `crates/storage/src/csp_policies.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 1356 files — by far the largest category vendored so far, 44 subdirectories (`base-uri`, `blob`, `child-src`, `connect-src`, `default-src`, `embedded-enforcement`, `font-src`, `form-action`, `frame-ancestors`, `frame-src`, `gen`, `generic`, `img-src`, `inheritance`, `inside-worker`, `media-src`, `meta`, `navigation`, `nonce-hiding`, `object-src`, `parsing`, `plugin-types`, `report-hash`, `reporting`, `reporting-api`, `resource-hints`, `resources`, `sandbox`, `script-src`, `script-src-attr-elem`, `securitypolicyviolation`, `style-src`, `style-src-attr-elem`, `support`, `svg`, `unsafe-eval`, `unsafe-hashes`, `wasm-unsafe-eval`, `webrtc`, `worker-src`, `xslt`) plus root `META.yml`/`WEB_FEATURES.yml`/`README.html`/`README.css`/`spec.src.json`. Out-of-category helpers not vendored: `/common/utils.js`, `/common/security-features/resources/common.sub.js`, `/common/redirect.py`, `/common/subset-tests.js`, `/common/dispatcher/dispatcher.js`, `/common/media.js`, `/media/*`, `/service-workers/service-worker/resources/test-helpers.sub.js`, `/resources/testdriver.js`+`testdriver-vendor.js`, `/fonts/Ahem.ttf`, same documented survey gap as `FileAPI`/`clipboard-apis`. Also the largest by selected-id count: `run_report.py --all --root content-security-policy --recursive` selects 865 ids across the ~40 subdirectories — did not complete (same limitation class as `bluetooth`/`client-hints`, a full run at this pace would take hours); a bounded ~10:25 trial run (stopped manually) processed 67 ids: 13 harness OK, 18 "Test TIMEOUT" (harness ran, partial subtests recorded, overall timed out), 32 bare TIMEOUT (harness never reported at all), 3 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`clipboard-apis`), 1 SKIP (`Executor does not support testdriver.js`). 2/185 subtests passed among the tests with subtest data. Failure clusters in the trial: most FAILs are the same out-of-category-helper 404 gap (`ReferenceError: log is not defined` from the un-vendored `/common/security-features/resources/common.sub.js`, plus `alertAssert.sub.js`/`logTest.sub.js` 404s); a few (`base-uri-allow*`/`base-uri-deny*`) expect the CSP-restricted navigation's resulting URL but got `undefined` — the existing `crates/network/src/csp.rs` parser exists but its `base-uri` directive doesn't appear wired into navigation, a genuine gap in the partial implementation; dynamic `import()` under CSP (`connect-src-*-import-blocked`) fails with `Error: Not supported` — dynamic `import()` itself appears unimplemented in the JS engine, a broader gap than CSP alone. Not filed as a separate BUG-NNN — first pass only vendors + runs a bounded trial. See `docs/wpt-status.md` for details. |

| `tests/wpt/contenteditable/` | `contenteditable/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-contenteditable`, `docs/wpt-status.md`), in-scope (⬜, `contenteditable`/`isContentEditable`/`document.designMode` — already partially implemented in Lumen, `crates/js/src/dom.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 6 files (`META.yml`, `WEB_FEATURES.yml`, `designmode-iscontenteditable.html`, `plaintext-only.html`, `select-text-change-crash.html`, reftest pair `synthetic-height.html`+`synthetic-height-ref.html`). No out-of-category helpers — every test references only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. Small category: of 5 real test files, the manifest only treats 2 as automatically runnable testharness tests — `select-text-change-crash.html` is WPT-manifest type `crashtest` (no `testharness.js`/`match` link, same class of gap as `acid`/`accessibility`'s crashtests) and `synthetic-height.html` is a `reftest` (`<link rel=match>` to `synthetic-height-ref.html`), both unsupported by the minimal executor. `run_report.py --all --root contenteditable --recursive` completed in ~1:08: `designmode-iscontenteditable.html` (declarative Shadow DOM `<template shadowrootmode="open">` + `document.designMode` + 4 `test()`s reading `isContentEditable` across the shadow boundary) — the `browsingContext.navigate` BiDi command itself timed out (`automation command timed out`), the harness never got to run a single `test()`; isolated `--dump-layout` repros of the design-mode-only and shadow-only halves don't reproduce a hang, so this looks like a live/interactive-path-only defect, not a parse-time one — filed as [BUG-345](../../bugs/BUG-345-OPEN.md) (a navigation-command hang is a more severe class of gap than the usual first-pass finding, in the same user-visible category as BUG-307). `plaintext-only.html` — harness OK, 0/2 subtests FAIL: `contentEditable`'s getter/setter don't recognize the spec's third state, `"plaintext-only"` (folds into `inherit` instead) — filed as [BUG-344](../../bugs/BUG-344-OPEN.md). 1/2 harness OK, 0/2 subtests. Both filed explicitly (unlike most first-pass vendor entries) — both are functional defects in already-implemented parts of the engine (`contentEditable` IDL, live navigation), not missing APIs. See `docs/wpt-status.md` for details. |
| `tests/wpt/cookies/` | `cookies/` | Test category, added 2026-07-25 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cookies`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 171 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `attributes/`, `domain/`, `encoding/`, `name/`, `ordering/`, `origin-bound-cookies/`, `partitioned-cookies/`, `path/`, `prefix/`, `resources/`, `samesite/`, `samesite-none-secure/`, `schemeful-same-site/`, `secure/`, `size/`, `third-party-cookies/`, `value/`). One out-of-category helper (`/common/get-host-info.sub.js`) not vendored — used both directly by some tests and transitively by the in-category `resources/cookie-helper.sub.js`/`resources/cookie-test.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root cookies --recursive` completed in ~26:30 (82 selected ids): 25 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`); 40 TIMEOUT + 8 ERROR on `.https.`-only ids, the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`background-fetch` (`invalid url: invalid port: "None"`) — the 8 ERROR ones are the same session-reuse-artifact manifestation as `accelerometer`/`ambient-light` (`Got results from X.html, expected X.https.html`) rather than a plain TIMEOUT; a handful of plain-HTTP tests also TIMEOUT because they depend on multi-window/iframe-navigation flows (`window.open`, form-target navigation, nested iframes) the minimal single-window executor can't drive, a different gap from the HTTPS-port one. 3/82 harness OK: `meta-blocked.html` (1/1 subtest PASS); `navigated-away.html` (0/1, `Cannot read properties of null (reading 'cookie')` — the test itself navigates an iframe to the un-vendored `/common/blank.html`, so the failure isn't trustworthy as a Lumen defect, same class of gap as the `get-host-info.sub.js` dependency); `document-cookie.non-secure.html` and `domain-attribute-missing.sub.html` FAIL with `<fn> is not defined` (`assert_dom_cookie`/`SECURE_SUBDOMAIN_ORIGIN`/`erase_cookie_from_js`) — these are defined in the vendored `resources/cookie-helper.sub.js`, but wptserve 500s serving it (`{{domains[www1]}}`/`{{ports[https]}}` substitution with no https port/extra domains configured), the same `AssertionError: deque([])` substitution gap already documented for `clear-site-data`. 1/7 subtests passed. Not filed as a separate BUG-NNN — every failure traces to an already-documented infra gap (testdriver.js, HTTPS port, `.sub.js` substitution, un-vendored `/common/`), not a fresh Lumen defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/cookiestore/` | `cookiestore/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cookiestore`, `docs/wpt-status.md`), in-scope (⬜, CookieStore API — async `cookieStore.get`/`set`/`delete`/`getAll` + `cookiechange` events, not implemented in Lumen at all). Same pinned commit, `git sparse-checkout` at the same commit hash, 65 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `resources/` — 8 helpers, 54 test files). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js`, `/service-workers/service-worker/resources/test-helpers.sub.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`ai`. The entire category is `.https.`-only — `run_report.py --all --root cookiestore --recursive` completed fully (~12:35, 46 selected ids): all 46 TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth`/`compute-pressure`) — cleanest first-pass outcome so far, no SKIP/ERROR/FAIL, every test blocked by the single already-documented infra gap rather than a mix of causes. 0/46 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/core-aam/` | `core-aam/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-core-aam`, `docs/wpt-status.md`), in-scope (⬜, Core Accessibility API Mappings — how ARIA roles/properties map onto platform accessibility APIs; Lumen already has an accessibility tree, Phase 2). Same pinned commit, `git sparse-checkout` at the same commit hash, 289 files: `META.yml`; `aamtests/` — 104 Python files, a pytest-based platform accessibility API test suite (AT-SPI/IAccessible2/AXAPI wrappers under `aamtests/support/`) — an entirely different test framework from wptrunner/testharness.js, never discovered by the WPT browser-test manifest at all; `manual/` — 169 files (168 `*-manual.html` ARIA/AAM verification pages, WPT-manifest type `manual`, requiring a human tester driving a real AT, `+README.md`); `arianotify/` — 14 files (10 `*-manual.html`, `WEB_FEATURES.yml`, `resources/aria-notify-helpers.js`+`aria-notify-iframe-manual-test.html`+`aria-notify-iframe-test.html` — the last one an iframe helper page, not a standalone test); `role/roles-contextual.html` — the sole automatically-runnable `testharness` test in the whole category. Out-of-category helpers not vendored: `/wai-aria/scripts/aria-utils.js`+`ATTAcomm.js`, same documented survey gap as `FileAPI`/`ai` — `aria-utils.js` is required by the one non-manual test, so even past the gap below it would 404. `run_report.py --all --root core-aam --recursive` selected exactly 1 id (every `-manual.html` excluded by `run_report.py`'s own filter, all 104 `aamtests/*.py` invisible to the WPT manifest, `arianotify/resources/aria-notify-iframe-test.html` not a manifest test either): `role/roles-contextual.html` — SKIP (`Executor does not support testdriver.js`, same known executor gap as `accname`/`attribution-reporting`). 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — the only candidate test is blocked by two independent known gaps (missing `testdriver.js` support, un-vendored `aria-utils.js`) rather than a fresh Lumen defect; this is now the category with the *fewest* automatically runnable tests found so far (1, vs. 0 in `annotation-model`/`annotation-vocab`/`avif`). See `docs/wpt-status.md` for details. |

| `tests/wpt/cors/` | `cors/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cors`, `docs/wpt-status.md`), in-scope (⬜, Cross-Origin Resource Sharing — already partially implemented in Lumen, `crates/network`). Same pinned commit, `git sparse-checkout` at the same commit hash, 45 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, `support.js`, 21 root test files — mostly legacy `.htm`, not `.html` — plus `resources/` — 11 helpers, 8 Python handlers + 3 JS/HTML). Out-of-category helpers not vendored: `/common/get-host-info.sub.js` (defines the `CROSSDOMAIN` global), `/common/utils.js` (defines `token()`), same documented survey gap as `FileAPI`/`ai`. This category surfaced a real gap in `run_report.py` itself (not upstream WPT, not a Lumen engine bug): `all_vendored_test_ids()` only recognized `.html` (+`.any.js`/`.window.js`-generated ids), silently dropping `.htm` — a legitimate WPT test extension the upstream manifest treats identically to `.html`. Fixed in the same commit (extension whitelist widened to include `.htm` in both the flat and `--recursive` modes) since without it the survey would have seen only 5 of 26 runnable tests; verified this doesn't change any other already-vendored category's discovered id count (none of them contain `.htm` files). After the fix, `run_report.py --all --root cors --recursive` selected 26 ids and completed fully (~1:44): 9 TIMEOUT with zero subtest data (`credentials-flag.htm`, `preflight-failure.htm`, `redirect-origin.htm`, `redirect-preflight.htm`, `redirect-userinfo.htm`, `remote-origin.htm`, `script-304.html` — a top-level `ReferenceError` from the un-vendored helpers hangs the harness before it reports, same pattern as `beacon`; `image-tainting-in-cross-origin-iframe.sub.html` — TIMEOUT on a cross-origin-iframe scenario, outside the minimal executor's single-window model; `preflight-cache.htm` — TIMEOUT after 5 FAILs); 17 harness OK, 16 of which fail every subtest on `CROSSDOMAIN is not defined`/`token is not defined` (the un-vendored helpers) and one (`access-control-expose-headers-parsing.window.html`) on `TypeError: fetch: network error` for a relative `fetch("resources/access-control-expose-headers.json")`, the same relative-URL-resolution gap as `browsing-topics`/`client-hints`; the sole genuine PASS in the whole category is `request-headers.htm` 1/6 (its other 5 subtests hit the same `CROSSDOMAIN` gap). 1/168 subtests passed. Not filed as a separate BUG-NNN — every failure traces to an already-documented infra gap (un-vendored `/common/*` helpers, relative-fetch resolution, cross-origin iframe), not a fresh engine defect. See `docs/wpt-status.md` for details. |

| `tests/wpt/cpu-performance/` | `cpu-performance/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cpu-performance`, `docs/wpt-status.md`), in-scope (⬜, `navigator.cpuPerformance` — a WICG proposal exposing a coarse CPU-load indicator, not implemented in Lumen at all). Same pinned commit, `git sparse-checkout` at the same commit hash, 2 files (`META.yml`, `cpu-performance.tentative.https.window.js`). No out-of-category helpers — the sole test references only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. Smallest category vendored so far by file count (2, vs. 3 in `audio-session`). The manifest selects exactly one id, `cpu-performance.tentative.https.window.html` (`.https.`-only) — `run_report.py --all --root cpu-performance --recursive` completed in ~30s: TIMEOUT (`invalid url: invalid port: "None"`, the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`cookiestore`/`core-aam`). 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/credential-management/` | `credential-management/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-credential-management`, `docs/wpt-status.md`), included despite its 🚫 scope note (Credential Management API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`contacts`. Same pinned commit, `git sparse-checkout` at the same commit hash, 21 files (`META.yml`, `WEB_FEATURES.yml`, 13 root test files, `support/` — 5 helpers + `README.md`). Out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/resources/testdriver.js`+`testdriver-vendor.js`, `/resources/WebIDLParser.js`+`/resources/idlharness.js` (only `idlharness.https.window.js`), same documented survey gap as `FileAPI`/`ai`. Manifest selects 13 ids, 12 of them `.https.`-only. `run_report.py --all --root credential-management --recursive` completed fully (~3:52): `non-fully-active.https.html` SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`contacts`); the other 11 `.https.` ids TIMEOUT (`invalid url: invalid port: "None"`, same HTTPS-port gap as `WebCryptoAPI`/`ai`/`cpu-performance`); the sole non-`.https.` test, `require_securecontext.html`, ran to completion — harness OK, 0/1 subtest FAIL (`assert_false: expected false got true` — the test assumes `http://127.0.0.1` is *not* a secure context, but per spec `127.0.0.1` is potentially-trustworthy ("localhost" exception) so `isSecureContext` correctly returns `true`, matching real browsers, same pattern as `audio-output`'s `secure-context.html`). 1/13 harness OK, 0/1 subtests. Not filed as a separate BUG-NNN — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/css/` | `css/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-css`, `docs/wpt-status.md`), in-scope (⬜, the umbrella top-level directory for every CSS spec test suite — `CSS1`/`CSS2`, `WOFF2`, `compositing`, and every `css-*` module: `css-align`, `css-anchor-position`, `css-animations`, `css-backgrounds`, `css-box`, `css-break`, `css-cascade`, `css-color`, `css-conditional`, `css-contain`, `css-display`, `css-flexbox`, `css-fonts`, `css-grid`, `css-transforms`, `css-transitions`, … — already Lumen's largest single implementation surface, `css-parser`+`layout`+`paint`). Same pinned commit, `git sparse-checkout` at the same commit hash. By far the largest category vendored so far: 54350 files, ~209 MB (vs. the previous largest, `content-security-policy`'s 1356 files) — a `git sparse-checkout` of the single upstream `css/` root directory, copied verbatim, no filtering. Out-of-category helpers are not individually enumerated here (impractical at this scale, unlike smaller categories) — the bounded trial run below observed 404s on `/resources/check-layout-th.js` (56 hits) and `/fonts/ahem.css` (3 hits), same un-vendored-helper survey gap as `compat`'s `webkit-box-item-shrink-001/002`; the full un-vendored-dependency surface across all 34607 selected ids is unknown. The manifest selects **34607 ids** — also by far the largest (next: `content-security-policy`'s 865) — so `run_report.py --all --root css --recursive` did **not** complete, same limitation class as `bluetooth`/`client-hints`/`content-security-policy` but far more extreme (at the observed pace, a full run would take on the order of a day, not hours). A bounded ~11:30 trial run (stopped manually) processed 382 of 34607 ids (~1.1%, covering `CSS1`/`CSS2` box-model/floats/normal-flow tests plus the start of `compositing`): 267 SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`clear-site-data`), 62 bare TIMEOUT (harness never reported), 53 harness OK with 1294/2594 subtests passed. Failure clusters in the trial: `background-blend-mode` not recognized in computed style (38 occurrences — the property isn't implemented); `document.elementFromPoint is not a function` (32 occurrences — missing DOM API); several `min-height:stretch`/`calc()` margin-collapse-through cases FAIL (`assert_equals: margins should collapse through expected 50 but got 0`); the `webkit-box-item-shrink`-style layout tests 404 on the un-vendored `/resources/check-layout-th.js`+`/fonts/ahem.css`, same gap as `compat`. Not filed as a separate BUG-NNN — first pass, bounded trial only, failures are a mix of already-known missing-feature classes rather than a single fresh regression. See `docs/wpt-status.md` for details. |

| `tests/wpt/cssom/` | `cssom/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-cssom`, `docs/wpt-status.md`), in-scope (⬜) — the upstream top-level `cssom/` directory, distinct from `css/cssom/` (279 files, already vendored as part of the whole `css/` in the same commit above). Same pinned commit, `git sparse-checkout` at the same commit hash, 2 files: `crashtests/csstext-with-all.html`, `crashtests/delete-rule.html`. No out-of-category helpers. Both files are WPT-manifest type `crashtest` (load-and-don't-crash checks, no `testharness.js`), unsupported by the minimal executor (`Unsupported test type crashtest for product lumen`), same class of gap as `accessibility`/`acid`. `run_report.py --all --root cssom --recursive` selected 2 ids, neither reached the run queue — 0 automatically runnable tests, same outcome as `annotation-model`. 0/0 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — known infra gap (no `crashtest` executor), not an engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/custom-elements/` | `custom-elements/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-custom-elements`, `docs/wpt-status.md`), in-scope (⬜, Custom Elements — already partially implemented in Lumen as a single global `customElements` registry, `crates/js/src/dom.rs`). Same pinned commit, `git sparse-checkout` at the same commit hash, 208 files (`META.yml`, `WEB_FEATURES.yml`, `form-associated/`, `htmlconstructor/`, `parser/`, `reactions/` (+ `customized-builtins/`, `resources/`), `registries/`, `resources/`, `state/`, `upgrading/`). Out-of-category helpers not vendored: `/common/gc.js`, `/common/object-association.js`, `/html/semantics/forms/form-submission-0/resources/targetted-form.js`, `/images/{green-1x1,green,yellow}.png`, `/resources/testdriver{,-actions,-vendor}.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root custom-elements --recursive` selected 176 ids and completed fully (~14:05): 95/176 harness OK, 366/3447 subtests passed, 58 TIMEOUT, 5 SKIP (`Executor does not support testdriver.js`), 1 ERROR (`WebSocket connection closed`, session-restart artifact). Two new engine bugs found and filed — both broad, not WPT-specific: [BUG-346](../../bugs/BUG-346-OPEN.md) (`Url::resolve()` doesn't collapse `.`/`..` dot-segments — a documented, intentional gap; `<script src="../x.js">` resolves to the literal `.../a/../x.js` and 404s; surfaced via `reactions/`+`reactions/customized-builtins/`, ~95 of the run's results), [BUG-347](../../bugs/BUG-347-OPEN.md) (`fetch()` never resolves relative URLs against the document base at all — `missing scheme`; the same symptom had already surfaced without a root cause in `browsing-topics`/`client-hints`/`connection-allowlist`/`cors`, root-caused here). Separately: the entire `registries/` subdirectory (22 files, scoped custom element registries — a newer part of the spec) fails on `CustomElementRegistry is not defined` — a whole unimplemented feature, expected on a first pass, not filed as its own BUG-NNN (same pattern as `container-timing`/`compute-pressure`). The remaining failures are ordinary first-pass reactions/upgrade-semantics gaps. See `docs/wpt-status.md` for details. |
| `tests/wpt/delegated-ink/` | `delegated-ink/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-delegated-ink`, `docs/wpt-status.md`), included despite its 🚫 scope note (Ink API, stylus-only) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`credential-management`. Same pinned commit, `git sparse-checkout` at the same commit hash, 6 files (`WEB_FEATURES.yml`, `delete-presentation-area.html`, `exception-thrown-bad-color.tentative.html`, `exception-thrown-diameter-less-than-or-equal-to-0.tentative.html`, `exception-thrown-untrusted-event.tentative.window.js`, `requestPresenter-returns-valid-promise.tentative.window.js`). No out-of-category helpers. Smallest category vendored so far by file count (6, vs. 2 in `cpu-performance`, tied with `audio-session`'s 3+3). `run_report.py --all --root delegated-ink --recursive` selected 5 ids and completed fully (~1 min): 3 SKIP (`delete-presentation-area.html`, both `exception-thrown-*.tentative.html` — `Executor does not support testdriver.js`, same gap as `accname`/`credential-management`); `exception-thrown-untrusted-event.tentative.window.html` TIMEOUT (a top-level `Cannot read properties of undefined (reading 'requestPresenter')` script error before the harness registers any test, same "un-vendored-helper-style" hang pattern as `beacon`/`cors` — except here the missing thing is `navigator.ink` itself, not a helper); `requestPresenter-returns-valid-promise.tentative.window.html` ran to completion (harness OK) but all 7/7 subtests FAIL, every one on `navigator.ink`/`Ink` being undefined — the whole Ink Delegation API isn't implemented in Lumen at all, expected on a 🚫-scope category's first pass (same pattern as `autoplay-policy-detection`'s `getAutoplayPolicy`). 1/5 harness OK, 0/7 subtests. Not filed as a separate BUG-NNN — a single unimplemented-API class, not a fresh engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/density-size-correction/` | `density-size-correction/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-density-size-correction`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 44 files (`WEB_FEATURES.yml`, `resources/` — 8 EXIF-tagged `.jpg` fixtures + `exify.js` + `third_party/piexif/` (piexif.js + LICENSE), ~22 root test files each with a `-ref.html` and/or `-cross-origin.sub.html` pair: `density-corrected-*`, `image-set-00{1,2,3}*`, `srcset*`). No out-of-category helpers — only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. `run_report.py --all --root density-size-correction --recursive` selected 22 ids; 20/22 are WPT-manifest type `reftest`, unsupported by the minimal executor (`Unsupported test type reftest`, same class of gap as `acid`/`apng`/`avif`/`animation-worklet`) — excluded before the run queue. The remaining 2 `testharness` ids ran: `density-corrected-image-in-canvas.html` TIMEOUT (the very first `canvas.getContext('2d')` call inside an `image.onload` callback throws synchronously, so the enclosing promise never resolves), `density-corrected-natural-size.html` FAIL (harness OK, 0/1 subtest — `TypeError: Cannot set properties of null (setting 'fillColor')`, i.e. `canvas.getContext('2d')` itself returned `null`). Root-caused, not left as first-pass noise: filed as [BUG-348](../../bugs/BUG-348-OPEN.md) — `canvas.getContext('2d')` returns `null` unconditionally in the current default V8 build (`webgl_canvas.rs`'s `document.createElement` monkeypatch installs before `canvas2d` and permanently shadows the working `2d` accessor with a webgl-only-or-null one), confirmed independently of wptrunner (bare BiDi repro) and visually (`--screenshot graphic_tests/57-canvas-2d.html` renders all 6 canvases blank). Broad-impact, not WPT-specific — regression from `P3-v8-s8` (26309f646, 2026-07-14). 1/2 harness OK, 0/6 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/deprecation-reporting/` | `deprecation-reporting/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-deprecation-reporting`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`META.yml`, `__dir__.ini`, `idlharness.any.js`). Upstream's own `__dir__.ini` marks the whole category `implementation-status: not-implementing` — the WICG Deprecation Reporting proposal has no shipping implementation anywhere, Lumen included. One out-of-category helper pair not vendored: `idlharness.any.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`/`autoplay-policy-detection`. Smallest category vendored so far by file count (3, vs. `cpu-performance`'s 2 and `audio-session`'s 3). `run_report.py --all --root deprecation-reporting --recursive` selected 1 id (`idlharness.any.html`) and completed in ~15s: the test ran over plain HTTP but 404'd on both out-of-category dependencies (`idl_test is not defined`), so the harness never registers a result — TIMEOUT, the same "un-vendored-helper-style" hang pattern as `audio-session`/`autoplay-policy-detection`'s own `idlharness.window.js`. 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known survey gap, not an engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/device-bound-session-credentials/` | `device-bound-session-credentials/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-device-bound-session-credentials`, `docs/wpt-status.md`), included despite its 🚫 scope note (niche Device Bound Session Credentials auth API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`delegated-ink`. Same pinned commit, `git sparse-checkout` at the same commit hash, 49 files (27 `.https.html`/`.html` test files, `helper.js`, `url_fetcher.html` — a fixture page, not a test, ~19 Python `wptserve` handler scripts, `excludeInScopeSpecification/`+`includeInScopeSpecification/` subdirs). One out-of-category helper not vendored: `/common/get-host-info.sub.js` (used by 3 tests), same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root device-bound-session-credentials --recursive` selected 27 ids (`url_fetcher.html` correctly excluded by the manifest — not a test) and completed fully (~7:29): 25 of the 26 `.https.` tests TIMEOUT on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`, same class as `WebCryptoAPI`/`ai`/`ambient-light`/…); `refresh-does-not-send-challenge.https.html` ERROR — a session-reuse artifact (result tagged with the previous test's URL), same pattern as `accelerometer`/`ambient-light`; the sole non-`.https.` test, `not-secure-connection.html`, also TIMEOUT but for a NEW reason — root-caused and filed as [BUG-350](../../bugs/BUG-350-OPEN.md): `<script type="module">` has no real module semantics on the default V8 build at all (`V8JsRuntime` never overrides `eval_module`, so the trait default silently runs module bodies through classic `eval()`), failing on the first `export`/`import` token (`Unexpected token 'export'`) — broad-impact, not WPT-specific, 80 vendored files use `type="module"`. 0/27 harness OK, 0/0 subtests. See `docs/wpt-status.md` for details. |
| `tests/wpt/device-memory/` | `device-memory/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-device-memory`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`device-bound-session-credentials`. Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `WEB_FEATURES.yml`, `device-memory.https.any.js`, `idlharness.https.any.js`). One out-of-category helper pair not vendored: `idlharness.https.any.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. The whole category is `.https.`-only (both selected test ids), same class as `WebCryptoAPI`/`ai`/`background-fetch`/`badging` — `run_report.py --all --root device-memory --recursive` completed in ~1:57 (2 selected ids): both TIMEOUT on the already-known HTTPS-port gap (the minimal executor serves no HTTPS port). 0/2 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known survey gap, not an engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/device-posture/` | `device-posture/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-device-posture`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor, form factor) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`device-memory`. Same pinned commit, `git sparse-checkout` at the same commit hash, 8 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, `idlharness.https.window.js`, 4 `device-posture-*.https.html` tests). No out-of-category helpers besides the already-vendored `testharness.js`/`testharnessreport.js`; `idlharness.https.window.js` references `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`device-memory`. `run_report.py --all --root device-posture --recursive` completed in ~52s (5 selected ids): 4 `device-posture-*.https.html` SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`delegated-ink`); `idlharness.https.window.html` TIMEOUT on the already-known HTTPS-port gap. 0/5 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — both gaps already known, no new engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/digital-credentials/` | `digital-credentials/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-digital-credentials`, `docs/wpt-status.md`), included despite its 🚫 scope note (Digital Credentials API) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`device-posture`. Same pinned commit, `git sparse-checkout` at the same commit hash, 32 files (`META.yml`, `dc-types.ts`, `tsconfig.json`, 21 `.https(.sub)?.html`/`.http.html` test files, `historical.any.js`, `permissions-policy/` — 2 iframe fixture pages loaded by other tests, not standalone tests, `support/` — `helper.js`+`iframe.html`, `webdriver/` — 3 test files). Two out-of-category helpers not vendored: `/common/get-host-info.sub.js`, `/permissions-policy/resources/permissions-policy.js`, same documented survey gap as `FileAPI`/`audio-session`. `run_report.py --all --root digital-credentials --recursive` selected 23 ids (`permissions-policy/*.html` and `support/*` correctly excluded by the manifest — not tests) and completed fully (~1:08): 19 `.https.` tests SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`delegated-ink`/`device-posture`); 2 TIMEOUT on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`, same class as `WebCryptoAPI`/`ai`/`device-memory`); `historical.any.html` harness OK (1/1 subtest passed); `non-secure-contexts.http.html` harness OK but 0/1 subtest FAIL — the test expects `window.isSecureContext === false` over plain HTTP, but the spec treats `http://127.0.0.1` as a potentially-trustworthy origin (the "localhost" exception), so Lumen correctly returns `true`, same pattern as `audio-output`'s `secure-context.html`. 2/23 harness OK, 1/2 subtests passed. Not filed as a separate BUG-NNN — both known gaps plus one test-assumption mismatch, no new engine defect. See `docs/wpt-status.md` for details. |

| `tests/wpt/direct-sockets/` | `direct-sockets/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-direct-sockets`, `docs/wpt-status.md`), included despite its 🚫 scope note (Direct Sockets API — `TCPSocket`/`UDPSocket`/`TCPServerSocket`, low-level network/hardware access) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`digital-credentials`. Same pinned commit, `git sparse-checkout` at the same commit hash, 8 files (`META.yml`, `README.md`, `disabled-by-permissions-policy.https.sub.html`+`.headers`, `tcp_socket.https.html`+`.headers`, `udp_socket.https.html`+`.headers`). One out-of-category helper not vendored: `/resources/test-only-api.js` (referenced by `tcp_socket.https.html`+`udp_socket.https.html`), same documented survey gap as `FileAPI`/`ai`. The whole category is `.https.`-only (all 3 selected test ids), same class as `device-memory`/`cpu-performance` — `run_report.py --all --root direct-sockets --recursive` completed fully (~1:22, 3 selected ids): all 3 TIMEOUT on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`, the minimal executor serves no HTTPS port) — cleanest possible outcome, no SKIP/ERROR/FAIL. 0/3 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known survey gap, not an engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/document-picture-in-picture/` | `document-picture-in-picture/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-document-picture-in-picture`, `docs/wpt-status.md`), included despite its 🚫 scope note (Document Picture-in-Picture API — no video media pipeline in Lumen) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`direct-sockets`. Same pinned commit, `git sparse-checkout` at the same commit hash, 27 files (`META.yml`, `WEB_FEATURES.yml`, 24 `.https.html`/`.html` test files, `support/focus-opener.js`, `support/popup-opens-pip.html`). No out-of-category helpers besides the already-vendored `testharness.js`/`testharnessreport.js`. `run_report.py --all --root document-picture-in-picture --recursive` selected 22 ids (`support/*` correctly excluded by the manifest — not tests) and completed fully (~36s): 20 tests SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`device-posture`); `requires-secure-context.html` harness OK but 0/1 subtest FAIL — `documentPictureInPicture` has no implementation at all in Lumen, so `window.documentPictureInPicture` is `undefined` where the test expects an interface object even in an insecure context, the same "API entirely absent" pattern as other 🚫-scope categories; `requires-user-gesture.https.html` ERROR on the already-known HTTPS-port gap (`invalid url: invalid port: "None"`). 1/22 harness OK, 0/1 subtests passed. Not filed as a separate BUG-NNN — both gaps already known, no new engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/document-policy/` | `document-policy/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-document-policy`, `docs/wpt-status.md`), in-scope (⬜, Document Policy — the `Document-Policy` HTTP response header + reflected `iframe.policy`/`document.policy` API restricting features like `sync-xhr` and enforcing a required policy on child frames; not implemented in Lumen at all, `Document-Policy` unrecognized by `crates/network`). Same pinned commit, `git sparse-checkout` at the same commit hash, 27 files (`META.yml`, `echo-policy.py`+`echo-policy-nested.html`(+`.headers`) — a recursive-iframe echo fixture, `experimental-features/` — 3 `.tentative.html` tests + `resources/` (`common.js`, `compressed.css`, `compressed.json`, `video.webm`), `reporting/` — 2 tests + shared `resources/document-policy-report-json.js`, `required-policy/` — 6 tests). No out-of-category helpers — every test references only the already-vendored `/resources/testharness.js`+`testharnessreport.js` (plus the category's own `resources/document-policy-report-json.js`). `run_report.py --all --root document-policy --recursive` selected 11 ids by file scan and completed fully (~1:47): `echo-policy-nested.html` is a plain iframe-recursion fixture (no `testharness.js`), correctly excluded by the manifest as not a test, leaving 10 queued tests. 3/10 harness OK, 0/19 subtests passed, 7 TIMEOUT. Failure clusters: `network-efficiency-guardrails-json.tentative.html` FAIL on `fetch('./resources/compressed.json')` — `TypeError: fetch: network error`, the same relative-URL-resolution gap already root-caused as [BUG-347](../../bugs/BUG-347-OPEN.md) (`custom-elements`, also seen in `browsing-topics`/`client-hints`/`cors`/`connection-allowlist`); its two siblings (`network-efficiency-guardrails.tentative.html`, `-report-only.tentative.html`, which load `<link rel=stylesheet href="./resources/compressed.css">` instead of `fetch()`) TIMEOUT waiting on the same unimplemented Network Efficiency Guardrails signal; `reporting/sync-xhr-report-only.html`+`sync-xhr-reporting.html` FAIL with `Cannot read properties of undefined (reading 'href')` — no Document-Policy violation report is ever generated (`document.policy`/reporting integration absent), so the test's `reports[0]` is `undefined`; all 6 `required-policy/*.html` tests TIMEOUT — `iframe.policy` (the reflected content attribute for `required-document-policy`) isn't implemented, so parent/child required-policy negotiation never happens and the child's postMessage-based report never arrives. Root cause for the whole category is a single unimplemented feature (the `Document-Policy` header/API doesn't exist in Lumen at all) plus one already-known infra gap (BUG-347) for one test — not filed as a separate BUG-NNN. See `docs/wpt-status.md` for details. |
| `tests/wpt/domparsing/` | `domparsing/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-domparsing`, `docs/wpt-status.md`), in-scope (⬜, DOM Parsing and Serialization — `DOMParser`/`XMLSerializer`/`innerHTML`/`outerHTML`/`insertAdjacentHTML` — core DOM functionality, implemented in Lumen). Same pinned commit, `git sparse-checkout` at the same commit hash, 71 files (`META.yml`, `WEB_FEATURES.yml`, root `DOMParser-*`/`XMLSerializer-*`/`innerhtml-*`/`outerhtml-*`/`insert*` tests, `resources/` — 7 helpers, `tentative/` — 22 Sanitizer-API streaming/trusted-types tests + `resources/util.js`). One out-of-category helper not vendored: `tentative/`'s trusted-types tests reference `/trusted-types/support/helper.sub.js`, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root domparsing --recursive` selected 55 ids and completed fully (~3:21): 38/52 harness OK, 59/1124 subtests passed, 9 TIMEOUT (mostly `tentative/` streaming tests hitting `trustedTypes is not defined`/the un-vendored helper's 404), 3 ERROR (session/URL-reuse artifact, same class as `accelerometer`/`beacon`/`clear-site-data`). Two new broad engine bugs found and filed together as [BUG-351](../../bugs/BUG-351-OPEN.md): (1) `insertAdjacentHTML` is missing entirely from the live `Element` (`insert-adjacent.html` 0/4, `insert_adjacent_html.html` 1/31) — its sibling `insertAdjacentText` was fixed under BUG-299, but `insertAdjacentHTML` — the far more common of the two in real-world code — never got the same fix; (2) `outerHTML` only exists on `VElement.prototype` (`dom_parser.rs:111`, the separate "virtual" tree `DOMParser.parseFromString()` builds), never on the live DOM `Element` (`outerhtml-01.html` 0/1, `outerhtml-02.html` 1/5 — assigning it silently no-ops instead of mutating the tree). Separately, `DOMParser`-produced `Document`s themselves are missing several facilities (`compatMode`/`location`/`styleSheets` undefined, `documentElement` typed as a bare `VElement` rather than `HTMLHtmlElement`, per-element `.style` undefined — `DOMParser-parseFromString-html.html` 3/10, `-stylesheets.html` 0/6, `style_attribute_html.html` 0/4) — a documented architectural limitation of the intentionally lightweight "virtual node" design (`dom_parser.rs` module doc comment), not filed as a separate bug. See `docs/wpt-status.md` for details. |
| `tests/wpt/domxpath/` | `domxpath/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-domxpath`, `docs/wpt-status.md`), in-scope (⬜, DOM XPath — `document.evaluate`/`XPathEvaluator`/`XPathExpression`/`XPathResult`/`XPathNSResolver`, not implemented in Lumen at all). Same pinned commit, `git sparse-checkout` at the same commit hash, 39 files (`META.yml`, `README.md`, `WEB_FEATURES.yml`, 34 root test files, `resources/` — `empty-document.html` + `invalid_namespace_test.js`). No out-of-category helpers — every runnable test references only the already-vendored `/resources/testharness.js`+`testharnessreport.js` (plus the category's own `helpers.js`/`resources/`). `run_report.py --all --root domxpath --recursive` selected 34 ids by file scan; wptrunner itself excluded 2 (`variables-in-expression-crash.html`, `xpath-evaluate-crash.html`) as manifest type `crashtest` (`-crash.html` naming convention, no `testharness.js` at all — same "unsupported test type" class as `accessibility`'s/`acid`'s crashtests/reftests), leaving 32 that actually ran, completing fully in ~2min: 28/32 harness OK, 2/126 subtests passed, 4 TIMEOUT (`evaluator-cross-realm.tentative.html`, `expression-cross-realm.tentative.html` — cross-realm `BroadcastChannel`-based helper hangs before the API it's testing is even reached; `xml_xpath_runner.html`, `xpathevaluatorbase-creatensresolver.html` — harness never reports, 0/0). Root cause for essentially the whole category is a single unimplemented feature — `document.evaluate`/`XPathEvaluator`/`XPathExpression`/`XPathResult`/`XPathNSResolver` all throw `ReferenceError`/`TypeError: ... is not a function` — not filed as a separate BUG-NNN (same "whole API missing" pattern as `document-policy`/`custom-elements`'s `registries/`). One incidental non-XPath finding surfaced by `xpath-shadow-dom.html`: its first subtest fails at `host.shadowRoot.getElementById('target')` before ever reaching `XPathEvaluator` — declarative Shadow DOM (`<template shadowrootmode="open">`) content isn't attached/queryable via `getElementById` the way the imperative-shadow-DOM sibling test is; its second subtest is a false PASS (the assertions inside a custom element's `connectedCallback` never execute, so the test trivially succeeds having asserted nothing) — a Custom-Elements/Shadow-DOM lifecycle gap, not an XPath one; not filed separately since `custom-elements`/BUG-341 already tracks related shadow-DOM/custom-element gaps. See `docs/wpt-status.md` for details. |
| `tests/wpt/dpub-aam/` | `dpub-aam/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-dpub-aam`, `docs/wpt-status.md`), in-scope (⬜, Digital Publishing Accessibility API Mappings — `doc-*` ARIA role/computedrole mapping for EPUB/digital-publishing semantics; not separately implemented in Lumen, no accessibility-tree role-mapping for these roles). Same pinned commit, `git sparse-checkout` at the same commit hash, 43 files (`META.yml`, `role/roles.html`, `manual/` — 41 `doc-*-manual.html` fixtures + `META.yml` + `README.md`). One out-of-category helper not vendored: `role/roles.html` references `/wai-aria/scripts/aria-utils.js` (shared ARIA role-verification harness), same documented survey gap as `FileAPI`/`ai`; moot here since the test never reaches it. Entirely manual by spec design except one file — all 41 `manual/doc-*-manual.html` are `-manual.html`, excluded by `run_report.py`'s own filter; `run_report.py --all --root dpub-aam --recursive` selected exactly 1 id, `role/roles.html`, and completed in a few seconds: SKIP (`Executor does not support testdriver.js`, same gap as `accname`/`attribution-reporting`/`document-picture-in-picture` — the test drives role assignment via `test_driver.set_permission`/similar through `aria-utils.js`). 0/1 harness OK, 0/0 subtests. Not filed as a separate BUG-NNN — a known executor gap, not a new engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/dpub-aria/` | `dpub-aria/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-dpub-aria`, `docs/wpt-status.md`), in-scope (⬜, DPUB-ARIA Recommendation — `doc-*` role vocabulary extending ARIA for digital-publishing semantics; not separately implemented in Lumen, same class of gap as `dpub-aam`). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml`, `README.md`, `.editorconfig`, `inuse-manual.html`). No out-of-category helpers. Entirely manual by spec design like `annotation-model`/`annotation-vocab` — the sole test file, `inuse-manual.html` (a free-form textarea harness a human pastes markup into and clicks "Run Test"), is `-manual.html`, excluded by `run_report.py`'s own filter, and no `.any.js`/`.window.js` exist either; `run_report.py --all --root dpub-aria --recursive` selected 0 tests (`no tests selected`), the same silent gap as `annotation-model`/`annotation-vocab` rather than `dpub-aam`'s one automatic `role/roles.html`. Not filed as a separate BUG-NNN — zero runnable tests, nothing to run against the engine. See `docs/wpt-status.md` for details. |
| `tests/wpt/ecmascript/` | `ecmascript/` | Test category, added 2026-07-26 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-ecmascript`, `docs/wpt-status.md`), in-scope (⬜, WPT coverage for ECMA-262/ECMA-402 implementation-defined/Web-Compat-mandated JS behavior — distinct from Test262, which this suite intentionally does not duplicate). Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`README.md`, `locale-compat.html`, `regexp-lookbehind.html`). No out-of-category helpers — both test files reference only the already-vendored `/resources/testharness.js`+`testharnessreport.js`. `run_report.py --all --root ecmascript --recursive` selected 2 ids and completed in ~5s: both PASS in full — `locale-compat.html` 4/4 subtests (`Date.prototype.toLocaleString`/`Intl.DateTimeFormat` locale-specific spacing/format checks), `regexp-lookbehind.html` 17/17 subtests (RegExp lookbehind alternations, back-references, captures, greedy loops, nested lookaround, sticky matches, word boundaries). 2/2 harness OK, 21/21 subtests passed, 0 unexpected — first vendored category with a clean 100% pass rate on every selected test. Not filed as a separate BUG-NNN — nothing failed. See `docs/wpt-status.md` for details. |
| `tests/wpt/editing/` | `editing/` | Test category, added 2026-07-27 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-editing`, `docs/wpt-status.md`), in-scope (⬜, legacy editing — `document.execCommand`/`contenteditable`/`designMode` — plus the modern EditContext API). Same pinned commit, `git sparse-checkout` at the same commit hash, 379 files (`crashtests/` 144, `other/` 97, `run/` 43, `data/` 36, `whitespaces/` 19, `plaintext-only/` 13, `manual/` 9, `edit-context/` 9, `include/` 6, plus `META.yml`/`README`/`event.html`). Out-of-category helpers **not** vendored — `/resources/testdriver.js`+`testdriver-vendor.js`+`testdriver-actions.js`, `/common/subset-tests.js`, `/img/lion.svg` — same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root editing --recursive` selected 700 ids and completed in ~34 min: 86/700 harness OK, 14/2310 subtests passed, 429 SKIP (`Executor does not support testdriver.js` — 61% of the category, since editing inherently needs synthesized input), 183 TIMEOUT, 2 ERROR. Two dominant engine causes: BUG-346 (unresolved `..` dot-segments — 614 of 723 HTTP 404s are the category's *own* vendored `include/`/`data/` files requested through `..`, which is why the entire generated `run/` conformance set never loaded its test engine) and the new BUG-353 (`element.focus()`/`blur()`/`document.activeElement` missing from the shim — 371 subtest failures across 12 files). See `docs/wpt-status.md` for the full breakdown. |
| `tests/wpt/element-timing/` | `element-timing/` | Test category, added 2026-07-27 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-element-timing`, `docs/wpt-status.md`), in-scope (⬜, Element Timing API — the `elementtiming` attribute and `PerformanceElementTiming` entries). Same pinned commit, `git sparse-checkout` at the same commit hash, 70 files (`META.yml`, `WEB_FEATURES.yml`, 49 root `.html` tests, `idlharness.window.js`, `resources/` — `element-timing-helpers.js`, four `iframe-*.html` fixtures, `circle.svg`/`circle-tao.svg`(+`.headers`), `square100.png`/`square20.png`/`square20.jpg`, and the `wptserve` Python handlers `slow-image.py`/`progressive-image.py`/`multiple-redirects.py`/`TAOImage.py`). Incidentally closes a neighbouring category's survey gap: `container-timing` referenced the un-vendored `/element-timing/resources/element-timing-helpers.js` — it is now present. Still not vendored: `/images/black-rectangle.png` (13 references), `/images/green-100x50.png` (8), `/images/blue.png` (4), `/common/redirect.py` (2), plus `idlharness.window.js`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js` — same documented survey gap as `FileAPI`/`ai`/`audio-session`. `run_report.py --all --root element-timing --recursive` completed fully in ~4:16 (52 selected ids, **not a single `.https.` file** — the HTTPS-port gap that dominates most categories in this backlog is absent here): 51/52 harness OK, 1/51 subtests passed. One root cause covers essentially the whole category — 50 subtests FAIL on `assert_implements: PerformanceElementTiming is not implemented` (the Element Timing API is not implemented in Lumen at all; that single string is 100% of the error text in the log); the one non-FAIL is `idlharness.window.html` TIMEOUT (404 on its two un-vendored dependencies → `idl_test is not defined`, same pattern as `audio-session`/`animation-worklet`/`beacon`). The single passing subtest, `supported-element-type.html` → *"supportedEntryTypes contains 'element'"*, is what exposed a genuine defect in an *implemented* feature rather than a missing-API gap: filed as [BUG-354](../../bugs/BUG-354-OPEN.md) — `PerformanceObserver.supportedEntryTypes` is a hardcoded 12-element literal, five of whose entries (`element`, `event`, `first-input`, `longtask`, `soft-navigation`) have no implementation whatsoever, so spec-sanctioned feature detection returns a false positive (and `observe({type:'element'})` silently "succeeds" while never firing). Verified outside WPT with a `--dump-layout` probe. The unimplementedness of Element Timing itself was not filed — ⬜ category, first pass. See `docs/wpt-status.md` for details. |
| `tests/wpt/encoding/` | `encoding/` | Test category, added 2026-07-27 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-encoding`, `docs/wpt-status.md`), in-scope (⬜, Encoding Standard — `TextEncoder`/`TextDecoder`, `TextEncoderStream`/`TextDecoderStream`, and the legacy multi-byte decoder/encoder conformance suites). Same pinned commit, `git sparse-checkout` at the same commit hash, 341 files (`META.yml`, `WEB_FEATURES.yml`, 54 root-level tests/helpers, `resources/` — `encodings.js`, `ranges.js`, `single-byte-decoder.js`, `decode-common.js`, `decoding-helpers.js`, `encode-form-common.js`, `encode-href-common.js`, the `wptserve` handlers `single-byte-raw.py`/`text-html-meta-charset.py`/`text-plain-charset.py`, and the utf-32/BOM HTML+XML fixtures, `streams/` — 16 files, plus the four `legacy-mb-*` families: `japanese/` (euc-jp, iso-2022-jp, shift_jis), `korean/euc-kr`, `schinese/` (gb18030, gbk), `tchinese/big5`). Out-of-category helpers not vendored: `/common/subset-tests.js`, `/common/sab.js`, and `idlharness.any.js`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js` — same documented survey gap as `FileAPI`/`ai`/`audio-session`. By far the longest run in this backlog so far: `run_report.py --all --root encoding --recursive` completed in **3 h 36 min** — 194 selected *files* expand into **1262 ids**, because 93 of them carry `<meta name="variant">` and fan out into 1193 `?N-M` subset variants. Result: **36/1262 harness OK, 146/2274 subtests passed**. The category splits cleanly in two. **(a) The `legacy-mb-*` block (1204 ids, 1197 TIMEOUT)** is dominated by a single survey gap, not an engine defect: every one of those tests loads `/common/subset-tests.js`, which 404s, so `subsetTest is not defined` throws before any test registers, the harness never reports, and the id burns a 10-second TIMEOUT — that block alone is ~3 of the 3.6 hours. [BUG-346](../../bugs/BUG-346-OPEN.md) (`..` segments not collapsed) is visible in the same block, 404ing the category's *own* vendored `../../resources/*.js`. **(b) The root block (58 ids, 30 harness OK)** is the real Encoding-API survey and produced two new bugs. [BUG-356](../../bugs/BUG-356-OPEN.md): `<a>`/`<link>` do not reflect `href` at all and the URL-decomposition IDL attributes (`search`/`pathname`/`protocol`/`host`/`hash`/`origin`) are missing on every element — **451 subtests across 5 files** fail on `a.search.substr(1)` → `Cannot read properties of undefined`, the single largest cause among tests that actually executed. [BUG-357](../../bugs/BUG-357-OPEN.md): `TextDecoder` is a UTF-8-only stub — no label→encoding table (`textdecoder-labels.any.html` 0/222), the constructor never throws `RangeError`, `{fatal:true}` only throws on a truncated tail (50 subtests between them), `decode()` ignores `this.encoding` and always decodes UTF-8 (most of the remaining 694 assertion failures), the BOM is never stripped, and `TextEncoder.encodeInto` does not exist. Both verified outside WPT with `--dump-layout` probes. A further 144 subtests (`encodeInto.any.html` 110, `textdecoder-streaming.any.html` 32, `textdecoder-copy.any.html` 2) fail on `createBuffer is not defined` — the un-vendored `/common/sab.js`, survey gap rather than engine defect. Exactly one file passed completely: `textencoder-constructor-non-utf.any.html` (79/79). See `docs/wpt-status.md` for details. |
| `tests/wpt/encoding-detection/` | `encoding-detection/` | Test category, added 2026-07-27 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-encoding-detection`, `docs/wpt-status.md`), in-scope (⬜, the tentative Encoding Detection spec — heuristic detection of a legacy encoding from content bytes plus page language, observed through `document.characterSet`). Same pinned commit, `git sparse-checkout` at the same commit hash, 109 files: 75 root-level `*.tentative.html`/`.html` tests, `support/` with the 31 `*-late.sub.html` popup fixtures, the two `__dir__.headers` files (`X-Content-Type-Options: nosniff`, which is load-bearing here — it suppresses MIME sniffing so only the encoding heuristic decides), and `LICENSE-WPT.md`. Upstream ships neither `META.yml` nor `WEB_FEATURES.yml` for this category. **No out-of-category dependencies at all** — the tests reference only `/resources/testharness.js`+`testharnessreport.js`, so unlike `FileAPI`/`ai`/`encoding` there is no survey gap here; there are also no `<meta name=variant>` files and no `.https.` tests, which makes this the cheapest category in the backlog so far: `run_report.py --all --root encoding-detection --recursive` finished in **8 min 25 s**, 75 selected files → exactly 75 ids, no fan-out. Result: **44/75 harness OK, 0/44 subtests passed**, and the category splits into two blocks with one cause each. **(a) The 44 executing tests all fail on [BUG-358](../../bugs/BUG-358-OPEN.md)**: the live global `document` exposes none of the document-metadata IDL attributes — `characterSet`, `charset`, `inputEncoding`, `compatMode`, `contentType`, `URL`, `documentURI`, `location` are all absent (`'characterSet' in document` === false). Every test here is the same three lines (`assert_equals(document.characterSet, "<enc>", …)`), so the failure text is uniform: `expected (string) "…" but got (undefined) undefined` ×43, plus one `document.characterSet.toUpperCase()` variant dying with `Cannot read properties of undefined`. Those attributes exist only on detached documents (`_lumen_build_detached_document`, `crates/js/src/dom.rs:4762-4769`); the live `document` is a separate hand-written literal (`dom.rs:6989+`) that never got them. **(b) All 31 TIMEOUTs are exactly the `*-late.tentative.html` set, all on [BUG-359](../../bugs/BUG-359-OPEN.md)**: those tests do `window.open("support/…-late.sub.html")` and the relative URL is never resolved against the document base, reaching the network layer verbatim (`invalid url: missing scheme`) — a third independent site of the same family as [BUG-346](../../bugs/BUG-346-OPEN.md)/[BUG-347](../../bugs/BUG-347-OPEN.md). A second barrier sits behind it: `window.open` returns a stub with no real `opener`/`postMessage` and the shell opens a new tab, so these tests would still time out even with the URL fixed. Both bugs confirmed outside WPT with `--dump-layout` probes. Worth recording for whoever fixes BUG-358: even with `characterSet` wired up at most 2 of the 44 could pass — the tests assert 22 distinct target encodings and `lumen_encoding::Encoding` (`crates/engine/encoding/src/lib.rs:41-54`) implements two of them (windows-1251, IBM866; its third legacy member is koi8-**r** while the category tests koi8-**u**). See `docs/wpt-status.md` for details. |
| `tests/wpt/encrypted-media/` | `encrypted-media/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-encrypted-media`, `docs/wpt-status.md`), in-scope (⬜, Encrypted Media Extensions — `navigator.requestMediaKeySystemAccess`/`MediaKeys`/`MediaKeySession` on top of `MediaSource`). Same pinned commit, `git sparse-checkout` at the same commit hash, 173 files (96 `.https.` root tests — the generated `clearkey-*`/`drm-*` pairs, one scenario × two key systems; `scripts/` — 39 shared test bodies; `util/` — 5 helpers; `polyfill/` — 5 browser polyfills; `content/` — 10 encrypted MP4 fixtures + `content-metadata.js`; `resources/` — 4 popup pages for the persistent-license rounds; `META.yml`/`WEB_FEATURES.yml`/`README.md`). **No out-of-category dependencies at all** — the only external `src=` references are the already-vendored `/resources/testharness.js`+`testharnessreport.js`, and no file uses `<meta name=variant>`, so 99 selected files produced exactly 99 ids with no expansion. `run_report.py --all --root encrypted-media --recursive` completed in **57:32**: **2/99 harness OK, 0/6 subtests**. 96 of the 99 ids are `.https.`-only and hit the same HTTPS-port gap as `WebCryptoAPI`/`ai`/`bluetooth` (95 TIMEOUT `invalid url: invalid port: "None"` + 1 ERROR on `idlharness.https.html`, the session-reuse artifact seen in `accelerometer`/`beacon`) — roughly 56 of the 57 minutes. Exactly 3 non-https tests executed, each surfacing a distinct cause: `media-element-event-handler-attributes.html` (0/4) → new BUG-360 (HTML event-handler content attributes never compile into handlers, and the live dispatch paths ignore the `on<type>` IDL property; the same test also exposed the missing named-access-on-Window mechanism); `encrypted-media-supported-by-permissions-policy.tentative.html` (0/1) → new BUG-361 (`document.permissionsPolicy.features()` returns the header-declared policy keys instead of the UA-supported feature set — the third category on that same line after `accelerometer`/`ambient-light`); `missing-sinf.html` (0/1) → already-filed BUG-347, new manifestation on a path-absolute `fetch()` URL. EME itself is unimplemented in Lumen (`navigator.requestMediaKeySystemAccess`/`MediaKeys`/`MediaSource` all `undefined`), so even with an HTTPS port the 96 `.https.` tests would fail on `assert_implements`. See `docs/wpt-status.md` for details. |
| `tests/wpt/entries-api/` | `entries-api/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-entries-api`, `docs/wpt-status.md`), in-scope (⬜, File and Directory Entries API — `FileSystemEntry`/`FileSystemFileEntry`/`FileSystemDirectoryEntry`/`FileSystemDirectoryReader`, `DataTransferItem.webkitGetAsEntry`, `<input webkitdirectory>`). Same pinned commit, `git sparse-checkout` at the same commit hash, 29 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, 14 `*-manual.html`, `idlharness.window.js`, `idlharness-manual.window.js`, `support.js`, `support/` — 8 drag-and-drop data fixtures, `LICENSE-WPT.md`). One out-of-category dependency pair not vendored: both idlharness tests reference `/resources/WebIDLParser.js`+`/resources/idlharness.js`, same documented survey gap as `FileAPI`/`audio-session`. Manual by spec design (upstream README: "most of the tests are manual and require drag-and-drop of test data files") — 15 of the 16 test files are `-manual`, excluded by `run_report.py`'s own filter, so the manifest selects exactly one id: `idlharness.window.html`. No `<meta name=variant>`, no `.https.` tests, and the only out-of-category `src=` are the already-vendored `/resources/testharness.js`+`testharnessreport.js` — the cheapest survey profile in this backlog: `run_report.py --all --root entries-api --recursive` completed in **1 min 13 s**, the single id TIMEOUT (`idl_test is not defined`, 404 on the two un-vendored IDL-harness files), the same pattern as `audio-session`/`background-sync`/`autoplay-policy-detection`. 0/1 harness OK, 0/0 subtests. A `--dump-layout` probe confirms the API itself is entirely unimplemented (all five `FileSystem*` interfaces `undefined`; `webkitdirectory`/`webkitEntries` absent on `<input>`, `webkitRelativePath` absent on `File.prototype`, `webkitGetAsEntry` absent on `DataTransferItem.prototype` — the latter two interfaces do exist), so even the 15 manual tests would fail on `assert_implements`; no BUG-NNN filed — first pass only vendors + runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/event-timing/` | `event-timing/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-event-timing`, `docs/wpt-status.md`), in-scope (⬜, Event Timing API — `PerformanceEventTiming`, the `event`/`first-input` entry types, `performance.eventCounts`/`interactionCount`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 78 files (69 root `.html` including one `-manual` and two `.tentative.`, `idlharness.any.js`, `idlharness.window.js`, `supported-types.window.js`, `resources/` — `event-timing-test-utils.js`+`crossiframe-childframe.html`+`slow-image.py`, `META.yml`, `WEB_FEATURES.yml`, `LICENSE-WPT.md`). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js` (referenced by 67 of the 69 test files) and `/resources/testdriver-actions.js` (42 files), plus `/resources/WebIDLParser.js`+`/resources/idlharness.js` for the two `idlharness.*` files — same documented survey gap as `accname`/`audio-session`. No `<meta name=variant>` and no `.https.` tests at all, so 69 candidate files gave exactly 71 selected ids and `run_report.py --all --root event-timing --recursive` completed in **40 s** — the cheapest multi-test category in this backlog so far. **The most testdriver-bound category vendored to date: 67 of 71 ids SKIP** (`Executor does not support testdriver.js` — Event Timing measures input-processing latency by definition, so nearly every test synthesizes a click/tap/keypress via `test_driver.click`/`Actions`), 2 TIMEOUT (`idlharness.any.html`, `idlharness.window.html` — `idl_test is not defined` from the un-vendored WebIDLParser pair, same pattern as `audio-session`/`entries-api`), and exactly 2 ids executed: 2/71 harness OK, 0/2 subtests. Both executing tests are feature-detection tests and both fail on the already-filed BUG-354 — `supported-types.window.html` fails `assert_implements(window.PerformanceEventTiming)` (the interface does not exist), and `supported-types-consistent-with-self.html` catches the inconsistency head-on: `PerformanceObserver.supportedEntryTypes` advertises `event`/`first-input` while `'PerformanceEventTiming' in self` is `false`. Second category after `element-timing` to land on that same hardcoded list; no new BUG-NNN filed. See `docs/wpt-status.md` for details. |
| `tests/wpt/eventsource/` | `eventsource/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-eventsource`, `docs/wpt-status.md`), in-scope (⬜, Server-Sent Events — `EventSource`, the `text/event-stream` wire format, reconnection/`Last-Event-ID`). Same pinned commit, `git sparse-checkout add` at the same commit hash, 95 files (33 root `.any.js`, 12 root `.window.js`, 2 root `.htm`, `dedicated-worker/` — 7 `.htm` + 9 worker scripts + 1 `.any.js` + 1 `.worker.js`, `shared-worker/` — 7 `.htm` + 7 worker scripts, `resources/` — 9 `wptserve` Python handlers + 2 `.event_stream` fixtures + 2 `.htm` fixtures, `META.yml`, `WEB_FEATURES.yml`, `README.md`, `LICENSE-WPT.md`). **No out-of-category dependencies at all** — the only external `src=` references are the already-vendored `/resources/testharness.js`+`testharnessreport.js`; no `<meta name=variant>`, no `.https.` tests, no `testdriver.js`, so 61 candidate files gave exactly 61 ids (`run_report.py`'s own glob selects only the window-scope `.any.html`, so the `.any.js` files do not fan out into `.any.worker.html`). `run_report.py --all --root eventsource --recursive` completed in **8 min 1 s**: **16/61 harness OK, 2/100 subtests**. **The first category in this backlog whose failures are engine defects rather than executor gaps** — `EventSource` is genuinely implemented in Lumen (real SSE stack: `crates/network/src/sse.rs`, shim `crates/js/src/dom.rs:8193`, pump wired into the shell tick at `crates/shell/src/main.rs:12241`), so the run produced three new bugs instead of `assert_implements` noise. **(a) [BUG-362](../../bugs/BUG-362-OPEN.md) — the dominant cause, 44 of 61 ids TIMEOUT (~7 of the 8 minutes)**: the constructor never resolves a relative URL against the document base (`dom.rs:8194` stores `String(url || '')` verbatim and hands it to `_lumen_sse_connect`, `v8_runtime.rs:2646`, which parses it as absolute), so every `new EventSource("resources/message.py")` — i.e. almost every test in the category — fails with `missing scheme` and the connection never opens; the `url` attribute likewise returns the raw string instead of the serialized absolute URL. Fourth independent site of the BUG-346/BUG-347/BUG-359 family. **(b) [BUG-363](../../bugs/BUG-363-OPEN.md)** — the interface does not follow its WebIDL binding on seven counts (constants only on the interface object, not the prototype, hence `source.CONNECTING === undefined`; callable without `new`; not an `EventTarget`; writable `url`/`readyState`; own-property event handlers; no `SyntaxError` on an invalid URL; `readyState` set to `CLOSED` synchronously in the constructor instead of staying `CONNECTING`); most of the 34 executed FAIL subtests land here. **(c) [BUG-364](../../bugs/BUG-364-OPEN.md)** — all 14 `dedicated-worker/`/`shared-worker/` ids TIMEOUT because `new Worker("script.js")` with an external URL silently substitutes a comment for the script body (`worker.rs:688`, only `data:`/`blob:lumen/` supported): the script is never fetched and no `error` event is fired. Only 2 subtests pass, and one of the two passing tests is a **false positive worth recording**: `eventsource-url.any.html` asserts only `source.url.substr(-url.length) === url`, which an unresolved relative string satisfies trivially — a fix for BUG-362 must be verified against `eventsource-constructor-empty-url.any.html` instead. Also observed but not filed: the cross-origin tests reference WPT's alternate hostnames (`{{domains[…]}}` → `xn--lve-6lad.127.0.0.1`), which do not resolve without the upstream hosts-file entries — an environment/survey gap, not an engine defect. See `docs/wpt-status.md` for details. |
| `tests/wpt/eyedropper/` | `eyedropper/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-eyedropper`, `docs/wpt-status.md`), out of scope (🚫, the niche EyeDropper API — W3C Color WG screen colour picker) but vendored anyway per the backlog's blanket coverage rule. Same pinned commit, `git sparse-checkout add` at the same commit hash, 7 files (`eye-dropper-abort-signal.tentative.https.html`, `idlharness.https.window.js`, `manual/eye-dropper.tentative.html`, `manual/eye-dropper-srgb.tentative.html`, `manual/resources/eye_dropper_icon.svg`, `WEB_FEATURES.yml`, `LICENSE-WPT.md`; the category has no `META.yml`). **The smallest category in this backlog.** Out-of-category helpers not vendored: `/resources/testdriver.js` + `testdriver-actions.js` + `testdriver-vendor.js` (the single non-idlharness test) and `/resources/WebIDLParser.js` + `/resources/idlharness.js` — the same documented survey gap as `accname`/`audio-session`/`entries-api`. No `<meta name=variant>`. `run_report.py --all --root eyedropper --recursive` completed in **1 min 36 s**: **0/2 harness OK, 0/0 subtests**, with both ids unreachable for different reasons — `eye-dropper-abort-signal.tentative.https.html` SKIP (`Executor does not support testdriver.js`: `open()` requires transient user activation per spec, so the test synthesizes a click through `new test_driver.Actions()`), and `idlharness.https.window.html` TIMEOUT on the HTTPS-port gap (`invalid url: invalid port: "None"`, same class as `WebCryptoAPI`/`ai`/`ambient-light`/`encrypted-media`) — it never even reaches the 404 on the un-vendored IDL-harness pair. **A defect in our own tooling was recorded here:** `run_report.py` printed «running 4 all vendored (eyedropper) WPT tests» while the manifest selected 2 — its `all_vendored_test_ids()` (`run_report.py:94`) filters manual tests by the `-manual` substring in the *filename* only, so both tests living in the `manual/` *directory* without that suffix inflate the id count, even though the upstream `MANIFEST.json` classifies them as `support` (they contain neither `testharness.js` nor a reftest link) and wptrunner never runs them. The already-vendored `accname`, `core-aam`, `dpub-aam` and `editing` categories have `manual/` directories too and are affected by the same inflation; their published counts were not rewritten retroactively — narrowing the filter is a DEVX candidate. The run itself could not produce engine findings, but a `--dump-layout` probe outside WPT produced one, and it is atypical for a 🚫-scoped category: EyeDropper **is implemented** in Lumen (`crates/js/src/eye_dropper.rs`, installed on both the V8 path `v8_runtime.rs:3893` and the rquickjs path `lib.rs:1187`) and 100% broken — `open()` always rejects with `ReferenceError: _lumen_eye_dropper_open is not defined` ([BUG-365](../../bugs/BUG-365-OPEN.md)): the native binding is never registered as a global (Rust only has an unconnected `extern "C"` stub at `eye_dropper.rs:24`), and `_lumen_eye_dropper_open?.call?.(null)` does not help — optional chaining short-circuits on `null`/`undefined`, but resolving an undeclared identifier happens first and throws. The documented «fall back to `#ffffff` when the binding is absent» branch, the JSON parsing and the late-`abort` path are therefore all dead code; only the early return on an already-aborted `AbortSignal` works. Notably the 6 `eye_dropper::tests` unit tests are green throughout (verified in-session, 6/6): they assert only `result instanceof Promise` — true for a rejected promise as well — and put their real assertions inside a `.then(...)` that a rejection never runs. See `docs/wpt-status.md` for details. |
| `tests/wpt/fedcm/` | `fedcm/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-fedcm`, `docs/wpt-status.md`), included despite its 🚫 scope note (Federated Credential Management — no IdP integration layer) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`eyedropper`. Same pinned commit, `git sparse-checkout` at the same commit hash, 192 files (`META.yml`, `WEB_FEATURES.yml`, 39 root `.https.` tests plus their `.headers` files, 12 subdirectories, and a large `support/` tree of 60+ Python `wptserve` IdP endpoints, IdP JSON manifests and `fedcm-helper.sub.js`). Out-of-category helpers not vendored: `/resources/testdriver.js`+`testdriver-vendor.js` (85 files), `/common/get-host-info.sub.js` (4), `/storage-access-api/helpers.js` (1), `/service-workers/service-worker/resources/test-helpers.sub.js` (1), same documented survey gap as `accname`/`audio-session`. `run_report.py --all --root fedcm --recursive` completed in 44 s (81 selected ids, matching wptrunner's own count — no `manual/` directory here, so the `-manual`-substring miscount seen on `eyedropper` does not apply): **all 81 ids SKIP with one and the same reason** (`Executor does not support testdriver.js`) — the first category in this backlog with a perfectly uniform outcome and no TIMEOUT/ERROR/FAIL at all. That is structural, not incidental: FedCM is specified to run through a browser-mediated account chooser, so every test synthesises the user's choice via `test_driver`; as a side effect the HTTPS-port gap that dominates comparable runs (84 of 103 files are `.https.`) never even gets a chance to fire, SKIP happens first. 0/81 harness OK, 0/0 subtests. As with `eyedropper`, the finding came from a `--dump-layout` probe outside WPT — but here the FedCM stub itself turned out to be *correct* (Phase 0, documented in `crates/js/src/credentials.rs:17-19`, really does reject with `NotSupportedError`); the defect is one level up, in the shared `CredentialsContainer` object that the in-scope WebAuthn branch uses too — BUG-366 (methods on the instance instead of the prototype, the internal `_get_original` monkey-patch guard leaked as an enumerable writable web-visible property, interface objects callable without `new`, no `Symbol.toStringTag`, clobbered `constructor`). See `docs/wpt-status.md` for details. |
| `tests/wpt/fenced-frame/` | `fenced-frame/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-fenced-frame`, `docs/wpt-status.md`), included despite its 🚫 scope note (Fenced Frames — ad-tech / Privacy Sandbox) by the same standing user decision as `accelerometer`/`acid`/`ai`/…/`fedcm`. Same pinned commit, `git sparse-checkout add` at the same commit hash, 400 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, 198 root `.html` tests — 197 of them `.https.`, the single non-https one being `insecure-context.html` — 8 `.headers` files, `resources/` with 190 helpers, `wptserve` Python endpoints and nested pages). Widest set of un-vendored out-of-category helpers in this backlog: `/common/utils.js` (201 references), `/common/dispatcher/dispatcher.js` (129), `/common/get-host-info.sub.js` (86), `/resources/testdriver*.js` (59/44), `/fetch/local-network-access/resources/support.sub.js` (5), `/webauthn/helpers.js` (2) — the same documented survey gap as `beacon`/`FileAPI`/`ai`. One `<meta name=variant>` file (`allow-attribute-src.https.html`), so 198 files → 199 ids. `run_report.py --all --root fenced-frame --recursive` completed in 51:48: 54 SKIP (`Executor does not support testdriver.js` — exactly the 54 root tests that pull `testdriver.js`), 143 TIMEOUT (the usual HTTPS-port gap, `invalid url: invalid port: "None"`), 1 ERROR (session-reuse artifact, same class as `accelerometer`/`beacon`) and 1 test that actually ran — `insecure-context.html`, harness OK, 1/2 subtests (the passing one is vacuous: it asserts a `TypeError` from a method that doesn't exist; the failing one 404s on the un-vendored `/common/utils.js`). 1/199 harness OK, 1/2 subtests. Fenced Frames itself is unimplemented in Lumen (no `HTMLFencedFrameElement`/`FencedFrameConfig`/`window.fence`), filed as no bug; the two findings (BUG-367, BUG-368) came from the out-of-WPT `--dump-layout` probe and sit one level up, in the shared live-DOM node wrapper. See `docs/wpt-status.md` for details. |
| `tests/wpt/fetch/` | `fetch/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-fetch`, `docs/wpt-status.md`). Scope ⬜ (candidate) — unlike every category vendored before it, this one's API is actually implemented in Lumen, and the run reflects that. Same pinned commit, `git sparse-checkout add` at the same commit hash, 985 files: `META.yml`, `README.md`, 23 subdirectories (`api/` by far the largest, then `metadata/`, `fetch-later/`, `h1-parsing/`, `http-cache/`, `orb/`, `corb/`, `range/`, `redirects/`, `cross-origin-resource-policy/`, `local-network-access/`, `nosniff/`, `content-encoding/`, `compression-dictionary/`, `stale-while-revalidate/`, `security/`, …), 313 `.html`/`.htm`, 179 `.any.js`, 65 `.window.js`, 1 `.worker.js`, 78 Python `wptserve` endpoints, 48 `.headers` files, plus `LICENSE-WPT.md`. All in-category helpers are vendored (`fetch/metadata/resources/helper.sub.js` — 100 references, `fetch/fetch-later/resources/` — 36, `fetch/orb/resources/utils.js` — 6); the un-vendored out-of-category helpers are the usual survey gap: `/common/get-host-info.sub.js` (138 references), `/common/utils.js` (134), `/resources/testdriver.js`+`testdriver-vendor.js` (36 each), `/service-workers/service-worker/resources/test-helpers.sub.js` (14), `/common/subset-tests-by-key.js` (5), `/html/browsers/…` (6), `/permissions-policy/…` (4), `/webtransport/…` (4), `/common/security-features/…` (3), `/common/dispatcher/dispatcher.js` (3), `/resources/idlharness.js`+`WebIDLParser.js` (1 each) — same documented gap as `beacon`/`fenced-frame`. Five files use `variant` (three of them `.window.js` under `local-network-access/`), so 516 globbed ids vs wptrunner's 481 — report wptrunner's number. `run_report.py --all --root fetch --recursive` completed in **1 h 22 min 48 s: 176/481 harness OK, 364/2692 subtests** — two orders of magnitude better than any other vendored category (`fenced-frame` 1/199, `fedcm` 0/81), because only 3% of files pull `testdriver.js` (16 SKIP) and the tests reach real code instead of being cut off by executor gaps. Outcome shape (473 `TEST_END` records): 168 `Test OK` with subtest breakdown, 42 `Test TIMEOUT` with partial subtests, 190 TIMEOUT, 56 ERROR, 16 SKIP, 1 PRECONDITION_FAILED; 13 tests fully green. The run's main result is a **price tag on two already-filed bugs** rather than a new one: BUG-347 (`fetch()` does not resolve relative URLs) accounts for 201 `invalid url: missing scheme` log lines — this is the most on-point category it could possibly have — and BUG-346 (`Url::resolve()` does not collapse `..`) here hits an **in-category, actually-vendored file** for the first time: 69 404s on paths like `/fetch/api/cors/../resources/utils.js` while `tests/wpt/fetch/api/resources/utils.js` sits on disk perfectly readable. That one file defines `RESOURCES_DIR`, `dirname`, `stringToArray` and `requestForbiddenHeaders`, i.e. every `… is not defined` error in the log is the same single consequence — fixing BUG-346 unblocks the shared helper of the largest subdirectory (`api/`, 178 tests). The remaining bulk is the HTTPS-port gap (147 `invalid url: invalid port: "None"`; 166 of the category's files are `.https.`). Unlike `fedcm`/`fenced-frame`, where every id was unreachable and the `--dump-layout` probe *replaced* the run, here the probe's findings are **confirmed point-by-point by the tests themselves**: BUG-369 (`Headers` is an ES5 constructor over an array, not a WebIDL interface — not iterable, `entries()`/`keys()`/`values()` return `Array`s, `new Headers(headers)` silently loses the headers, no validation/guard/`getSetCookie`, `_map`/`_key` web-visible) ↔ `headers-record` 0/13, `headers-no-cors` 0/8, `header-setcookie` 2/24, `header-values-normalize` 1/62; BUG-370 (`Request` has no Body mixin at all, `url` not absolutized, no `Response.json()`, `Response.error().type` = `default`, `Response.redirect()` sets no `Location`, no argument validation anywhere) ↔ `request-consume` 0/45, `response-static-redirect` 0/11, `response-form-data` 0/14, `forbidden-method` 0/6. See `docs/wpt-status.md` for the full breakdown. |
| `tests/wpt/file-system-access/` | `file-system-access/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-file-system-access`, `docs/wpt-status.md`). Scope ⬜ (candidate). Same pinned commit `35be3b44`, cone-mode `git sparse-checkout`, 43 files: `META.yml`/`WEB_FEATURES.yml`/`README.md`, **5** executable tests (`getDirectory.https.any.js`, `idlharness.https.any.js`, `opaque-origin.https.window.js`, `sandboxed_FileSystemDirectoryHandle-move.https.any.js`, `showPicker-errors.https.window.js`), 29 `local_*-manual.https.html` demos that the picker spec makes impossible to automate, `resources/` (`test-helpers.js`, `local-fs-test-helpers.js`, `opaque-origin-sandbox.html`, `data/testfile.txt`), `script-tests/FileSystemDirectoryHandle-move.js`, `LICENSE-WPT.md`. **The 29:5 manual-to-executable ratio is the category's defining property** — File System Access is gated on a user-chosen file/directory, so almost everything upstream is a manual demo. Here `run_report.py`'s glob and wptrunner agree (both say 5), because every manual file carries the `-manual` filename suffix — unlike `eyedropper`/`accname`/`core-aam`/`dpub-aam`/`editing`, where manual tests sit in a `manual/` directory without the suffix and inflate the glob's count. Out-of-category helpers not vendored: `../fs/resources/messaging-helpers.js` (10 files), `messaging-serialize-helpers.js` (9), `messaging-blob-helpers.js` (7), `/service-workers/service-worker/resources/test-helpers.sub.js` (4), `/common/get-host-info.sub.js` (3), `/common/utils.js` + `/common/dispatcher/dispatcher.js` (2 each), and `idlharness`'s `/resources/WebIDLParser.js`+`/resources/idlharness.js` — the same documented survey gap as `FileAPI`/`fetch`/`fenced-frame`. `run_report.py --all --root file-system-access --recursive` — **2 min 30 s, 0/5 harness OK, 0/0 subtests**: all 5 ids are `.https.` and all 5 TIMEOUT on the HTTPS-port gap (`invalid url: invalid port: "None"`), a uniform outcome with zero executed subtests. Only 29 of 42 upstream files pull `testdriver.js` and every one of them is a `-manual` demo that is never selected, so this category never reaches the testdriver SKIP path at all — it is pure HTTPS-port gap, and at 5 ids that costs 2.5 min instead of `fenced-frame`'s 52 min. All findings therefore came from the `--dump-layout` probe, which paid off unusually well because the API is **implemented** in Lumen (`crates/js/src/filesystem_access.rs`, both engines): BUG-371 (all 10 natives are page-visible globals over process-global registries with sequential ids — cross-origin read of granted files/directories and hijack of a save-handle), BUG-372 (two different classes named `FileSystemDirectoryHandle`; `navigator.storage.getDirectory()` returns the crippled closure-captured stub from `storage_manager.rs`, whose `removeEntry()` silently resolves without removing — measured directly by the vendored `getDirectory.https.any.js`), BUG-373 (all 9 `new DOMException(...)` in the module have `name`/`message` swapped, so `e.name === 'AbortError'` never holds), BUG-374 (10-point WebIDL divergence: no `FileSystemHandle` base, public constructors, writable `name`/`kind`, no async iteration, `FileSystemWritableFileStream` is not a `WritableStream`, picker options and user activation entirely unvalidated — 37/37 subtests of `showPicker-errors.https.window.js`). `CAPABILITIES.md` had to move File System Access from ✅ to 🟡, the same drift class as the `innerHTML`/fetch lines. See `docs/wpt-status.md` for details. |
| `tests/wpt/fledge/` | `fledge/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-fledge`, `docs/wpt-status.md`), included despite its 🚫 scope note (ad-tech / Privacy Sandbox) by the same standing user decision as `attribution-reporting`/`browsing-topics`/`fedcm`/`fenced-frame`. Same pinned commit `35be3b44`, cone-mode `git sparse-checkout`, 77 files. **The whole category lives under `tentative/`** — there is no top-level `META.yml`/`WEB_FEATURES.yml`, unlike every other category vendored so far. 34 `.window.js` tests (33 of them `.https.`; the exception is `insecure-context.window.js`) + 2 `.html` (`fetch-ad-auction-headers.tentative.https.html`, `fetch-ad-auction-headers-insecure-context.tentative.http.html`) = 36 executable files, expanded by `// META: variant=` lines into **183 manifest ids**; `resources/` (29 files: 18 wptserve Python endpoints — bidding/decision logic, trusted signals, request tracker, update-url — plus `fledge-util.sub.js`/`ba-fledge-util.sub.js`, `worklet-helpers.js`, `service-worker-helper.js`, `incrementer.wasm`, `set-cookie.asis`, `ba-public-keys`); `third_party/` with three vendored JS libraries (cbor-js, hpke-js, noble-ed25519), each keeping its own `LICENSE`/`README`; 3 `.headers` files; `LICENSE-WPT.md`. `incrementer.wasm` is the **first `.wasm` in this repository** — git's `text=auto` detects it as binary on its own, so no `.gitattributes` change was needed (verified: `git diff --cached --stat` reports `Bin 0 -> 46 bytes`). `run_report.py --all --root fledge --recursive` — **3 h 16 min 34 s, 2/183 harness OK, 1/2 subtests** (179 TIMEOUT, 2 ERROR, 2 `Test OK`). **This category inverts the `grep -rl testdriver` cost predictor.** All 35 test files pull `testdriver.js`, which by the `fedcm`/`fenced-frame` rule predicts an instant SKIP wall; the run produced **zero** SKIPs, because `MANIFEST.json` sets `testdriver: true` on **0 of the 183 ids** — the manifest generator derives that flag from `<script src=…testdriver.js>` in HTML sources, while `.window.js`/`.any.js` pull the same file via `// META: script=/resources/testdriver.js`, which never becomes the flag. Every id therefore reached the HTTPS-port gap and paid a full ~65 s TIMEOUT. Predict from the manifest flag, not from a source grep. Only the two non-HTTPS ids produced signal: `insecure-context.window.html` passed 1/1 (it asserts the Protected Audience API is unavailable in an insecure context — it passes precisely because Lumen implements none of it), and `fetch-ad-auction-headers-insecure-context.tentative.http.html` failed 0/1 on `fetch: network error for ./resources/empty.html` — BUG-347, not fledge. Each of those two poisoned the id that followed it (BUG-380, a finding of the run itself: the executor neither checks the outcome of `browsingContext.navigate` nor clears `window.__lumen_wpt_results`, so a failed navigation immediately reads the previous test's results and raises `AssertionError` instead of a honest TIMEOUT). Protected Audience has no implementation in the engine at all, so the `--dump-layout` probe went one level up, onto the objects the category's tests stand on, and produced five findings: BUG-375 (`URL` has one working setter out of ten — `prop()` installs an empty function instead of omitting the setter, so `url.search = …`, the idiom the category's central helper builds every URL with, is swallowed silently even under `"use strict"`), BUG-376 (`window.location = 'url'` overwrites the Location object with a string instead of navigating; assigning to `location`'s components does not navigate either and desynchronises the object from `href`), BUG-377 (`Node.baseURI` missing on every node type — `fledge-util.sub.js` dies on its third line), BUG-378 (604 engine-internal names are own properties of `window`, 592 enumerable, all writable, and the shim resolves natives through the global at call time — overwriting `window._lumen_get_attr` hijacks `Element.getAttribute` page-wide), BUG-379 (the anti-automation-detection guard is itself a fingerprint: it defines 15 markers as non-configurable own properties that Chrome and Firefox have zero of). Out-of-category helpers not vendored: `/common/utils.js`, `/resources/testdriver.js`+`testdriver-vendor.js` — the same documented survey gap as `fetch`/`fenced-frame`. Side effect of this run: `tests/wpt/config.json`'s HTTP ports moved 8300/8301 → 18300/18301 after three failed runs — the old pair sat inside this machine's ephemeral range (1024-15000) and was taken by an unrelated process (see the Troubleshooting section of `tests/wpt/README.md`). See `docs/wpt-status.md` for details. |
| `tests/wpt/focus/` | `focus/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-focus`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 116 files (`WEB_FEATURES.yml` — the category has no `META.yml`; 45 top-level files, of which 2 are `-ref.html` reftest references and 2 are crashtests; `support/` with 69 helper documents, 25 of them `.sub.html`; `LICENSE-WPT.md`). No out-of-category helpers beyond `/resources/testharness.js`+`testharnessreport.js`+`testdriver*.js`. **Structurally the cheapest category vendored so far and the first whose wall is the engine, not the ports:** zero `.https.` files, zero variants, only 5 of 115 files pull `testdriver.js` — a full `run_report.py --all --root focus --recursive` completed in **8 min 21 s** with 39 selected ids (the glob counts 45; wptrunner drops the 2 reftest references and 2 crashtests). Result: **7/39 harness OK, 0/7 subtests passed** — 28 TIMEOUT, 4 SKIP, 7 executed-and-failed. 17 of the 39 die on BUG-359 (`window.open` doesn't resolve relative URLs) and ~13 more on `<iframe>` having no browsing context at all (`contentWindow`/`contentDocument` are `null`), so the run's headline value is a price tag on known gaps plus four new bugs from the 7 that executed — BUG-381 (the whole focus API is missing from JS while the shell owns real focus state), BUG-382 (`getComputedStyle()` empty and `getBoundingClientRect()` zero in ~3 of 4 loads — a delivery race, not a serialization bug), BUG-383 (IDL attribute reflection and activation methods largely absent), BUG-384 (named access on Window). One infrastructure note: this category's `.sub.html` files are served as **HTTP 500**, not a navigation error — `tools/wptserve/wptserve/pipes.py:521` asserts on `{{ports[https][0]}}` because `config.json` has `"https": [null, null]`. That is a second, server-side failure mode of the HTTPS-port gap, distinct from the familiar client-side `invalid port: "None"`, and it applies to every category containing `.sub.html`. See `docs/wpt-status.md` for details. |
| `tests/wpt/font-access/` | `font-access/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-font-access`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 20 files (`META.yml`, `WEB_FEATURES.yml`, `README.md`, 6 `font_access_*.tentative.https.window.js`, `permissions-policy/` — 6 html + 2 `.headers`, `resources/` — `font-asserts.js`/`font-data.js`/`font-test-utils.js`). One out-of-category helper (`/permissions-policy/resources/permissions-policy.js`, referenced by the 5 `.sub.html` tests) not vendored — same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root font-access --recursive` completed in 6:51 (12 selected ids): 5 SKIP (`Executor does not support testdriver.js`), 6 TIMEOUT (all six `.window.js`, the usual HTTPS-port gap), 1 `Test OK` with its single subtest FAIL. This category is the smallest clean reproduction of the `fledge` testdriver-predictor inversion and is worth keeping as the reference case: `grep -rl testdriver` matches 11 of 21 files, but `MANIFEST.json` flags `testdriver: true` on exactly the 5 `.sub.html` files that pull it via a `<script src=…>` tag — the 6 `.window.js` files that pull the same file via `// META: script=…` get no flag, reach the HTTPS-port gap and pay a full ~65 s TIMEOUT each, which is the entire runtime. Note also that the *first* run of the same command reported 0/12: the one executed test's result was computed and then discarded past wptrunner's external timeout, after which the browser process could not be stopped (`IO Completion Port failed to signal process shutdown`). Deliberately tested and refuted as a poisoning-by-previous-failed-navigation effect (1 and 6 preceding failed navigations both leave the test green); treat it as an intermittent on the shutdown path, and re-run any category whose whole signal rests on one or two executed tests. See `docs/wpt-status.md` for details. |
| `tests/wpt/forced-colors-mode/` | `forced-colors-mode/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-forced-colors-mode`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 118 files (`README.txt`, `WEB_FEATURES.yml` — no `META.yml`; 92 top-level `.html` files, mostly reftest pairs `-NN.html`/`-NN-ref.html` plus 14 `testharness.js` tests; `backplate/` — 20 files of the same reftest shape; `resources/` — 2 image fixtures; `LICENSE-WPT.md`). **First category where the reftest/testharness split itself is the finding.** 49 of the top-level files are `rel=match` reftests, and the executor (`browsers/lumen.py:36-37`) registers only the `testharness` test type — `reftest`/`print-reftest`/`crashtest` are logged `Unsupported test type for product lumen` and never selected, so `run_report.py --all --root forced-colors-mode --recursive` picked exactly **63 of 117** glob ids (wptrunner drops every reftest/crashtest) and finished in **32 s**: **12/14 harness OK, 10/36 subtests passed**, 2 TIMEOUT. The signal splits three ways: two already-green reference tests (`forced-colors-mode-27.html` 1/1, `forced-colors-mode-41.html` 9/9) show the core color-forcing logic is correct, not a stub; 7 FAIL + both TIMEOUT trace to the already-filed BUG-384 (named access on Window — tests address elements by `id` as bare globals: `a`/`p`/`dark`/`visited_child`/`body`/`div`); and two new bugs from the remaining ids — BUG-387 (`forced-colors-mode-40.html` 0/17: `computedStyleMap()` shares its class and native binding with the mutable `attributeStyleMap` and both read only the inline `style=""` attribute, never the cascade, so `.get(prop)` on a stylesheet-set property returns `undefined` while `getComputedStyle()` for the same property is correct) and BUG-388 (`forced-colors-mode-54.html`/`-60.html`: `apply_forced_colors_mode` forces every other CSS Color Adjustment L1 property correctly but omits `scrollbar-color` → `auto` and `font-variant-emoji` → `text`, the latter not even parsed as a property yet). `CAPABILITIES.md`'s Forced Colors Mode line gained a caveat for both omissions. See `docs/wpt-status.md` for details. |
| `tests/wpt/fullscreen/` | `fullscreen/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-fullscreen`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 111 files (`META.yml`, `WEB_FEATURES.yml`, `api/` — 64 files, `rendering/` — 19 files including 6 `rel=match` reftest pairs, `model/` — 11 files, `crashtests/` — 6 files, `LICENSE-WPT.md`). No out-of-category helpers beyond `/resources/testharness.js`+`testharnessreport.js`+`testdriver*.js`. Unlike the `fledge`/`font-access` testdriver-predictor inversion (`.window.js`/`.any.js` files whose `// META: script=` never sets the manifest flag), here the grep and the manifest mostly agree because the category is 91% `.html` (testdriver.js pulled via `<script src>`, which the manifest generator does see) — so 73% of files (80/110) correctly SKIP instead of paying a full TIMEOUT. `run_report.py --all --root fullscreen --recursive` completed in **1 min 37 s** (83 selected ids — the 6 `rel=match` reftest pairs in `rendering/` are dropped by the executor, same class as `forced-colors-mode`): **12/83 harness OK, 85/112 subtests passed** (64 SKIP, 7 TIMEOUT) — two orders of magnitude better subtest ratio than the typical backlog entry. The 6 exceptions to the testdriver-flag agreement (5 `document-fullscreen-keyboard-lock-*.window.js` plus `rendering/fullscreen-pseudo-class-support.html`, which inlines the same keyboard-lock assertions) executed and failed `ReferenceError: test_driver is not defined` — the familiar inversion class, not a new finding. The 12 tests that actually ran produced two new bugs: BUG-390 (`requestFullscreen()` implements only the happy path — `document.fullscreenEnabled` is hardcoded `true` so its own reject branch is dead code, connected-element/user-activation preconditions are never checked, the promise always resolves, and `fullscreenerror` is never dispatched anywhere in the shim) and BUG-391 (`Element.matches()`/`querySelector(All)`/`closest()` are designed to never throw `SyntaxError` for an invalid or unrecognized selector — `document.body.matches(':halfscreen')` silently returns `false`, breaking the standard WPT feature-detection precondition used across many categories, not just this one). One test (`document-fullscreen-enabled.html`, "same-origin iframe") hit the already-documented (not separately filed) Phase-0 limit that `<iframe>` has no browsing context (`contentDocument === null`, `CAPABILITIES.md`'s DOM line). `idlharness.window.html` TIMEOUT on `idl_test is not defined` — `/resources/idlharness.js`+`WebIDLParser.js` are not vendored, the same documented survey gap as `FileAPI`/`fetch`/`audio-session`, not a Lumen bug. `CAPABILITIES.md`'s DOM line gained both new caveats. See `docs/wpt-status.md` for details. |
| `tests/wpt/gamepad/` | `gamepad/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-gamepad`, `docs/wpt-status.md`), included despite its 🚫 scope note (hardware API — gamepad controllers) by the same standing user decision as `accelerometer`/`fledge`/`eyedropper`. Same pinned commit, `git sparse-checkout` at the same commit hash, 15 files (`META.yml`, `WEB_FEATURES.yml`, 6 `*-manual(.https).html`, 7 non-manual test files, `LICENSE-WPT.md`). No testdriver.js anywhere in the category (zero SKIP), 4 `.https.` files (2 of them manual, excluded). Two out-of-category dependencies not vendored: `idlharness.window.js` → `/resources/WebIDLParser.js`+`idlharness.js` (same documented survey gap as `FileAPI`/`fullscreen`), `permissions-policy-gamepad.html` → `/permissions-policy/resources/permissions-policy.js`. `run_report.py --all --root gamepad --recursive` completed in **1 min 7 s**: the glob found 7 non-manual ids, but wptrunner's manifest selected only 6 — `permissions-policy-gamepad.html` is a bare `<script>` fixture with no `testharness.js` include (a `postMessage` target for other tests' iframes), correctly classified `support` and excluded, not a gap. **1/6 harness OK, 0/3 subtests passed**: `gamepad-default-permissions-policy.https.sub.html` TIMEOUT (ordinary HTTPS-port gap); `idlharness.window.html` TIMEOUT (the un-vendored idlharness deps, not a Lumen bug); `idlharness-extensions.https.window.html` ERROR (session-reuse artifact, same class as BUG-380 — result tagged with the previous test's URL); `gamepad-supported-by-permissions-policy.html` ran (harness OK) but its subtest FAILs on the already-filed BUG-361 (`permissionsPolicy.features()` → `[]`); `gamepad-permissions-policy-event-listener.html` and `not-fully-active.html` both TIMEOUT on the same root cause, confirmed by a live `--mcp-live-port`/`eval` probe: a `srcdoc` `<iframe>`'s `onload` never fires and a `postMessage` posted from inside it never arrives at the parent, because `contentWindow`/`contentDocument` are genuinely `null` (a `typeof` check reports `"object"` because `typeof null === "object"` — verified by chaining a property access, which throws `Cannot read properties of null`) — the already-documented `<iframe>`-has-no-browsing-context gap (BUG-381/BUG-383, category `focus`), not a new finding. The same live probe, run against the Gamepad API itself (implemented, Phase 0, `crates/js/src/gamepad.rs`) on a page with no test scaffolding at all, found a genuine new bug: BUG-392 — `navigator.getGamepads()` always returns 4 `null` slots instead of the spec-required 0 before any device has connected, and `ongamepadconnected`/`ongamepaddisconnected` are missing as `Window` event-handler IDL attributes even though `addEventListener` for the same event types works. See `docs/wpt-status.md` for details. |
| `tests/wpt/generic-sensor/` | `generic-sensor/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-generic-sensor`, `docs/wpt-status.md`), included despite its 🚫 scope note (base hardware sensor API) by the same standing user decision as `accelerometer`/`gamepad`/`fledge`. Same pinned commit, `git sparse-checkout` at the same commit hash, 11 files (`META.yml`, `WEB_FEATURES.yml`, 3 top-level test files, 3 shared `.js` helpers used across the whole Sensor API family — `generic-sensor-tests.js`, `generic-sensor-iframe-tests.sub.js`, `generic-sensor-permissions-policy-test.sub.js`, already partly pulled in by the previously-vendored `accelerometer` — `resources/` of 2 files, `LICENSE-WPT.md`). The cheapest, most homogeneous category in the backlog so far: files == ids, zero variants. `run_report.py --all --root generic-sensor --recursive` completed in **53 s**: **0/3 harness OK, 0/0 subtests** — 1 SKIP (`generic-sensor-permission.https.html`, pulls testdriver.js via `<script src>`), 2 TIMEOUT (`SensorErrorEvent-constructor.https.html`, `idlharness.https.window.js`, ordinary HTTPS-port gap). The API is implemented (Phase 0, `crates/js/src/generic_sensor.rs` — base `Sensor` plus all 8 concrete sensor types), so a headless `--mcp-port`/`eval` probe (no live window needed — the category is entirely synchronous, no async event-loop dependency) reproduced both unreachable tests' assertions directly: BUG-393 (`SensorErrorEvent`'s constructor never validates the required `error` member of its init dict and never throws `TypeError` — reproduces the exact assertion in `SensorErrorEvent-constructor.https.html`) and BUG-394 (`Sensor`/its subclasses use a private hand-rolled `_SensorEventTarget` mixin instead of the shim's global `EventTarget`, so `instanceof EventTarget` is `false`; `new Sensor()` is directly constructible even though its IDL defines no constructor operation — both facts are exactly what the unreachable `idlharness.https.window.js` checks). `CAPABILITIES.md` left untouched — same precedent as `gamepad` (BUG-392): both findings are internal to an out-of-scope hardware API, not shared infrastructure that `CAPABILITIES.md` tracks. See `docs/wpt-status.md` for details. |
| `tests/wpt/geolocation/` | `geolocation/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-geolocation`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 28 files (`META.yml`, `WEB_FEATURES.yml`, 22 `*.https(.sub).html`/`*.https.window.js`, 1 `.http.html`, `resources/`, `support.js`, `LICENSE-WPT.md`). One out-of-category helper (`/permissions-policy/resources/permissions-policy.js`, referenced by the 5 `enabled-by-*`/`disabled-by-*` `.sub.html` tests) not vendored, same documented survey gap as `FileAPI`/`gamepad`. `run_report.py --all --root geolocation --recursive` completed in **1 min 58 s** (20 selected ids): **1/20 harness OK, 4/4 subtests passed** — 12 SKIP (testdriver.js, tag-based `<script src>` so the manifest flag agrees with the grep, same class as `fullscreen`), 6 TIMEOUT (ordinary HTTPS-port gap, same class as `WebCryptoAPI`/`ai`/`ambient-light`), 1 ERROR (`permission.https.html` — session-reuse artifact of the same class as `gamepad`/`fledge`'s BUG-380: its result came back tagged with the previous test's URL, `non-secure-contexts.http.html`). The sole executed test — `non-secure-contexts.http.html`, the category's only non-`.https.` file — passed cleanly, confirming `getCurrentPosition`/`watchPosition` correctly and asynchronously reject with `PERMISSION_DENIED` outside a secure context. The category's real finding came from reading the shim source rather than the run: BUG-395 (`crates/js/src/geolocation.rs`'s `GEO_SHIM` — `getCurrentPosition`/`watchPosition` never throw `TypeError` for a missing/`null`/wrong-type success callback; the vendored `getCurrentPosition_TypeError.https.html`/`watchPosition_TypeError.https.html` test this directly but are themselves unreachable by this run via the HTTPS-port gap, not testdriver — the same "probe even when the run yields nothing" lesson as `generic-sensor`'s BUG-393/394). `clearWatch` matches spec (never throws on an invalid id) — no regression there. See `docs/wpt-status.md` for details. |
| `tests/wpt/geolocation-sensor/` | `geolocation-sensor/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-geolocation-sensor`, `docs/wpt-status.md`), included despite its 🚫 scope note (hardware sensor API) by the same standing user decision as `accelerometer`/`gamepad`/`generic-sensor`. Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`META.yml`, `historical.https.html`, `LICENSE-WPT.md` — no `WEB_FEATURES.yml`). The smallest category in the backlog so far: a single executable file, and not a member of the Sensor API family — a "historical" regression test. The `geolocation-sensor` spec was published and later marked Discontinued Draft by W3C (`https://www.w3.org/TR/2026/DISC-geolocation-sensor-20260203/`); the sole test asserts `GeolocationSensor` must **not** be defined on `window`. `run_report.py --all --root geolocation-sensor --recursive` completed in **33 s**: **0/1 harness OK, 0/0 subtests** (1 TIMEOUT, ordinary HTTPS-port gap — the one file is `.https.html`, no testdriver.js). Since the run produced no signal, a headless `--mcp-port`/`eval` probe (no live window needed — fully synchronous check) confirmed the assertion directly: `'GeolocationSensor' in window` → `false`, `typeof window.GeolocationSensor` → `'undefined'`; a grep of `crates/` confirms the name doesn't appear anywhere in the engine. The only vendored category so far where the probe comes back positive with zero findings — Lumen correctly does not implement the discontinued draft, no bug filed. |
| `tests/wpt/gif/` | `gif/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-gif`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 4 files (`META.yml` — no `WEB_FEATURES.yml`, `reset-no-gce-1.html`+`reset-no-gce-ref.html` — one `rel=match` reftest pair, `reset-no-gce.gif` — the fixture image, `LICENSE-WPT.md`). One out-of-category helper (`/common/reftest-wait.js`) not vendored, same documented survey gap as `FileAPI`. The smallest fully-original category so far: a single reftest, no testharness at all. `run_report.py --all --root gif --recursive` confirmed the expected outcome in ~24 s: the sole test is WPT-manifest type `reftest`, unsupported by the minimal executor (`Unsupported test type reftest for product lumen`, same class as `apng`/`acid`/`animation-worklet`) — **0/0**, no signal from the run. Per the "probe even when the run yields nothing" rule, the fixture GIF itself was fed directly to Lumen's own decoder (`lumen-image`, `include_bytes!` + `cargo test`, since the reftest can't be driven): `decode_gif_animated` on `reset-no-gce.gif` (2×2, 3 frames — this is exactly the GIF disposal-method edge case the WPT test exercises: frame 0 has GCE+`transparent_index=0`(no-op), frame 1 has GCE+`transparent_index=2`(fully transparent), frame 2 has **no preceding GCE at all**, which the upstream test comment calls out explicitly) decodes frame 0 correctly (`[255,0,0,255]`×4) then fails on frame 1 with `DecodeError("invalid code in LZW stream")` — reproduced identically with both `ColorOutput::RGBA` and the default indexed output, so it's not an RGBA-compositing bug. Cross-checked with Pillow (`tests/wpt/.venv`, an independent decoder) on the byte-identical vendored file (confirmed via `xxd` diff against the upstream sparse-checkout, so no vendoring corruption): Pillow decodes all 3 frames cleanly and produces exactly what the upstream comment describes (opaque red → fully transparent → opaque green). New finding: BUG-396 — the `gif` crate (v0.14.2, `Cargo.lock`) that `lumen-image` depends on rejects this spec-valid multi-GCE-configuration GIF; not a bug in Lumen's own `gif.rs` glue code, which calls the crate's public API in its documented order. |
| `tests/wpt/gpc/` | `gpc/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-gpc`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout` at the same commit hash, 10 files (`META.yml`, `WEB_FEATURES.yml`, `global_privacy_control.testdriver.html`, `idlharness.any.js`, `navigator-globalPrivacyControl.https.html`, `sec-gpc.https.html`, `support/` — `getGPC.py`+`navigator-globalPrivacyControl.html`+`.js`, `LICENSE-WPT.md`). No out-of-category helpers. `run_report.py --all --root gpc --recursive` completed in ~15 s (4 selected ids): **0/4 harness OK, 0/0 subtests** — 3 SKIP (`global_privacy_control.testdriver.html`/`navigator-globalPrivacyControl.https.html`/`sec-gpc.https.html` all call the GPC-specific testdriver extension methods `test_driver.get_global_privacy_control()`/`set_global_privacy_control()`, unsupported by the minimal executor, same `Executor does not support testdriver.js` class as `accname`/`fullscreen`); 1 TIMEOUT (`idlharness.any.html` — 404s on the un-vendored `/resources/WebIDLParser.js`+`idlharness.js`, the same known infrastructural gap documented for every other `idlharness.*` file in this backlog). The run itself produced no signal, so per the "probe even when the run yields nothing" rule a plain `grep -rli "globalPrivacyControl\|Sec-GPC\|sec_gpc" crates/ --include="*.rs"` was run instead — **zero matches**. New finding: BUG-397 — Global Privacy Control is entirely unimplemented: no `navigator.globalPrivacyControl` in the JS shim (`crates/js/src/surface_api.rs`, right next to the already-implemented `navigator.doNotTrack`) and no `Sec-GPC` header on any of the 7 `HttpProfile`s (`crates/network/src/http/headers.rs::build_request_headers`) — notable because the legally weaker `DNT` header is already sent by default on the Chrome/Edge profile, and the privacy-focused per-profile HTTP fingerprint infrastructure (ADR-007, `HttpProfile::Strict`/`Lumen`) that would be the natural place for GPC already exists and goes unused. |
| `tests/wpt/graphics-aria/` | `graphics-aria/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-graphics-aria`, `docs/wpt-status.md`), scope ⬜ (candidate), spec `https://w3c.github.io/graphics-aria/`. Same pinned commit, `git sparse-checkout` at the same commit hash, 3 files (`META.yml`, `graphics-roles.html`, `LICENSE-WPT.md` — no `WEB_FEATURES.yml`). Smallest fully-automated category so far: a single `testharness` test, checking the same three roles as the already-vendored (manual/ATTAcomm) `graphics-aam` — `graphics-document`/`graphics-object`/`graphics-symbol` — via `AriaUtils.verifyRolesBySelector` instead of a human-driven ATTAcomm protocol. One out-of-category helper not vendored: `/wai-aria/scripts/aria-utils.js` (`wai-aria` itself isn't vendored yet), same gap as `core-aam`/`dpub-aam`/`graphics-aam`. `run_report.py --all --root graphics-aria --recursive` completed in ~24 s (1 selected id): **0/1 harness OK, 0/0 subtests** — SKIP (`Executor does not support testdriver.js`; the file pulls `testdriver.js`/`testdriver-actions.js`/`testdriver-vendor.js` in `<head>` without ever calling them, but the manifest flags on tag presence, not use). No new finding: the test targets the exact defect already filed as BUG-398 (the three Graphics ARIA roles are absent from `AXRole::parse`, `crates/engine/a11y/src/roles.rs`, falling back to `Generic`), found via a direct Rust probe when `graphics-aam` was vendored — re-probing would only reconfirm the same root cause through a different test file, so it wasn't repeated. See `docs/wpt-status.md` for details. |
| `tests/wpt/gyroscope/` | `gyroscope/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-gyroscope`, `docs/wpt-status.md`), included despite its 🚫 scope note (device sensor) by the same standing user decision as `accelerometer`/`generic-sensor`/`gamepad`. Same pinned commit, `git sparse-checkout` at the same commit hash, 16 files (`META.yml`, `WEB_FEATURES.yml`, 9 `*.https(.html)`/plain `.html` root tests, 2 `.headers` sidecars, `idlharness.https.window.js`, `resources/sensor-data.js`, `LICENSE-WPT.md`). Two out-of-category helpers reused from already-vendored categories: `/generic-sensor/generic-sensor-tests.js` and `/permissions-policy/resources/permissions-policy.js` (the latter itself not vendored — same documented gap as `accelerometer`). `run_report.py --all --root gyroscope --recursive` completed in ~4 min 19 s (10 selected ids): 2 SKIP (testdriver.js), 5 `.https.` TIMEOUT (HTTPS-port gap), 1 ERROR (`idlharness.https.window.html` — session-reuse artifact of the class `accelerometer`/`ambient-light`, result tagged with the previous test's URL), 2 FAIL — the two non-`.https.` files actually ran: `Gyroscope-supported-by-permissions-policy.html` (`document.permissionsPolicy.features()` doesn't list `"gyroscope"`, the same recurring defect as `accelerometer`/`ambient-light`, already filed as BUG-361) and `Gyroscope_insecure_context.html` (`assert_false('Gyroscope' in window)` — not decisive on its own, since this ran over loopback `http://127.0.0.1`, a potentially-trustworthy origin per W3C Secure Contexts §3.1, same environment/spec mismatch already documented for `audio-output`'s `secure-context.html`; reading the code the FAIL led to found a real, broader defect instead — BUG-399, `window.isSecureContext` hardcoded `true` unconditionally rather than computed from the page's actual protocol/host). See `docs/wpt-status.md` for details. |
| `tests/wpt/hr-time/` | `hr-time/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-hr-time`, `docs/wpt-status.md`). Same pinned commit, `git sparse-checkout` at the same commit hash, 19 files (`META.yml`, `WEB_FEATURES.yml`, 13 top-level test files, `resources/` — 8 files, `LICENSE-WPT.md`). Fully self-contained: zero `variant`/`testdriver.js` hits, only out-of-category `src=` refs are the already-vendored `/resources/testharness(report).js`. `run_report.py --all --root hr-time --recursive` completed in 1 min 37 s: 4/13 harness OK, 8/16 subtests passed (2 ERROR — session-reuse artifact of the class BUG-380; 1 TIMEOUT — un-vendored `/resources/idlharness.js`, known infra gap; 1 `.https.` TIMEOUT — ordinary HTTPS-port gap) — most ids actually ran, well above this backlog's recent average. Two new bugs from real FAIL/TIMEOUT subtests: BUG-400 (`performance` doesn't extend `EventTarget`, no `toJSON()`) and BUG-401 (`performance` entirely missing from Worker global scope). Two remaining TIMEOUTs are not a new finding — both hit the already-documented `<iframe>` without a browsing context limit. See `docs/wpt-status.md` for details. |
| `tests/wpt/hsts/` | `hsts/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-hsts`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, 4 files (`WEB_FEATURES.yml`, `third-party-subframe-hsts-upgrade.tentative.sub.html`, `resources/hsts.py`+`resources/is-upgraded.html`, `LICENSE-WPT.md` — this category has no `META.yml`). One out-of-category helper (`/common/get-host-info.sub.js`) not vendored, same documented survey gap as `FileAPI`/`ai`. `run_report.py --all --root hsts --recursive` — ~15 s, 0/1 harness OK; the finding (BUG-402) came from a direct Rust grep of production call sites, not from the run. **This row was missing from the table when `WPT-VENDOR-html` was landed 2026-07-28 (the directory was vendored and committed, but the pin record was not updated) and is reconstructed here from `ROADMAP.md`/`docs/wpt-status.md`.** |
| `tests/wpt/html-aam/` | `html-aam/` | Test category, added 2026-08-04 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-html-aam`, `docs/wpt-status.md`), scope ⬜ (candidate, HTML Accessibility API Mappings — how HTML-specific semantics/roles map onto platform accessibility APIs). Same pinned commit, `git sparse-checkout` at the same commit hash, 19 files (`META.yml`, `WEB_FEATURES.yml`, 16 test files — 15 `.html`/`.tentative.html` + one `.tentative.window.js`, `LICENSE-WPT.md`). Unlike `core-aam`/`dpub-aam`/`graphics-aam` (near-100% `-manual.html`/ATTAcomm), every file here is automatically runnable — the category's own `testharness` mechanism, not a human-driven AT protocol. One out-of-category helper not vendored: `/wai-aria/scripts/aria-utils.js` (shared ARIA role-verification harness), same documented gap as `core-aam`/`dpub-aam`/`graphics-aam`/`graphics-aria` — `wai-aria` itself isn't vendored yet. `run_report.py --all --root html-aam --recursive` completed in ~2:41 (16 selected ids): **2/16 harness OK, 0/7 subtests**. Distinct failure mode from the sibling AAM categories: 13 of 14 files that reference `aria-utils.js` don't get flagged `testdriver` in the manifest despite pulling `testdriver.js`/`testdriver-actions.js`/`testdriver-vendor.js` too (same predictor inversion documented for `fledge`/`font-access`), so instead of an instant SKIP they actually navigate, hit `ReferenceError: AriaUtils is not defined` inside a `script error` (not a thrown exception the harness can catch), and hang to TIMEOUT — a third failure shape for the same un-vendored-helper class, next to `core-aam`'s pre-emptive SKIP and `dpub-aam`'s SKIP. The remaining 2 ids (`aside-in-prefixed-article.html`, not `AriaUtils`-dependent; `roles-dynamic-switch.tentative.window.html`) both reached harness OK and gave the category's only real signal, 7 subtests, all FAIL on the same root cause: `TypeError: element.getRootNode is not a function` — no new finding, this is the already-filed [BUG-599](../bugs/BUG-599-OPEN.md) (`Node.prototype.getRootNode()` missing entirely), independently reconfirmed through a different test file than the `html/interaction` slice that found it. See `docs/wpt-status.md` for details. |
| `tests/wpt/html/` | `html/` | Test category, added 2026-07-28 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-html`, `docs/wpt-status.md`), scope ⬜ (candidate). Same pinned commit, `git sparse-checkout add` at the same commit hash, **14161 files** — by far the largest vendored category (next largest: `fetch`, 985) — in 26 top-level subdirectories (`canvas`, `semantics`, `browsers`, `rendering`, `dom`, `webappapis`, `editing`, `syntax`, `interaction`, `cross-origin-opener-policy`, `cross-origin-embedder-policy`, `infrastructure`, `document-isolation-policy`, `obsolete`, `anonymous-iframe`, `user-activation`, `the-xhtml-syntax`, `links`, `capability-delegation`, `select`, `scripting`, `embedded-content`, `meta`, `iana`, `resources`, `tools`): 10263 `.html`, 1813 `.js`, 523 `.xhtml`, 298 `.png`, 260 `.yml`, 241 `.headers`, 207 `.htm`, 97 Python `wptserve` endpoints, 74 `.vtt`, 65 `.css`, 61 `.dat` (parser fixtures), 59 `.svg`. By `MANIFEST.json` it selects **8399 `testharness` ids** (701 of them flagged `testdriver`), plus 1382 `reftest`, 183 `crashtest` and 357 `manual` ids the minimal executor has no path for. **This is the first category where vendoring and surveying had to be separated**: at the measured rate of the two slices below a full `--all --root html --recursive` run is 40+ hours, which no single session can hold — the tree is pinned in one commit, and the survey proceeds subdirectory by subdirectory, with the remainder carried as explicit `WPT-VENDOR-html-*` rows in `ROADMAP.md` rather than silently dropped. Un-vendored out-of-category helpers (136 distinct paths, the widest gap in the backlog; same documented class as `beacon`/`fenced-frame`/`fetch`): `/resources/testdriver.js`+`testdriver-vendor.js` (641 references each), `/images/green-100x50.png` (460), `/resources/testdriver-actions.js` (443), `/common/slow.py` (202), `/common/utils.js` (197), `/common/get-host-info.sub.js` (197), `/shadow-dom/focus-navigation/resources/focus-utils.js` (130), `/common/dispatcher/dispatcher.js` (130), `/common/media.js` (119), `/common/reftest-wait.js` (113), `/common/blank.html` (93). Two slices surveyed so far: `html/dom` — 12 min 39 s, **192/249 harness OK, 253/4784 subtests** (the best harness-OK share in the whole backlog, 77 % vs `fetch`'s 37 %, because the subdirectory is nearly self-contained — 2 `testdriver` files and 2 `.https.` out of 478); `html/syntax` — 53 min 16 s, **69/379 harness OK, 30/675 subtests** (307 of 379 ids TIMEOUT, dominated by the documented `<iframe>`-without-browsing-context wall and by BUG-415). Seven bugs filed from the two slices: BUG-418 (the shim's DOM-arena overflow guard is dead code on the default V8 build — a page building 50 000+ nodes aborts the whole process instead of throwing `QuotaExceededError`; this alone makes all 9 `html/dom/reflection-*.html`, ~72 000 subtests, unreachable), BUG-412…BUG-416 (missing DOM APIs) and BUG-417 (a `<template>` seen before `<body>` prevents `<body>` from ever being created). See `docs/wpt-status.md` for the full breakdown. |
| `tests/wpt/html-media-capture/` | `html-media-capture/` | Test category, added 2026-08-04 by the WPT-VENDOR backlog (`ROADMAP.md` `WPT-VENDOR-html-media-capture`, `docs/wpt-status.md`), included despite its 🚫 scope note (media pipeline) by the same standing user decision as `accelerometer`/`acid`/`ai`. Same pinned commit, `git sparse-checkout` at the same commit hash, 16 files (`META.yml`, `WEB_FEATURES.yml`, 12 `-manual` test files, `capture_reflect.html`, `idlharness.window.js`, `support/`, `LICENSE-WPT.md`). No out-of-category helpers referenced by the two automatable files. `run_report.py --all --root html-media-capture --recursive` completed in ~30 s (2 selected ids): **1/2 harness OK, 5/6 subtests**. `capture_reflect.html` gave the only new finding — BUG-613 (`HTMLInputElement.capture` wired as plain-string reflection instead of `'enum'` limited-to-known-values; `capture='invalid'` returns `"invalid"` instead of the spec's `""` default — the `'enum'` reflection kind already exists in the same table and is used for `type`/`referrerPolicy`/`dir`, a one-line fix). `idlharness.window.html` TIMEOUT on 404s for the un-vendored `/resources/idlharness.js`+`WebIDLParser.js`, the same documented infrastructural gap as every other `idlharness.*` file in this backlog — not a new finding. See `docs/wpt-status.md` for details. |

Each vendored top-level directory carries its own `LICENSE-WPT.md` (WPT is
3-clause BSD, copyright web-platform-tests contributors) alongside the code.

## What's explicitly NOT vendored, and why

- **`tools/third_party/`** (upstream is ~76 MB, dominated by vendored test corpora of
  its own — e.g. `third_party/hpack/test/` alone is ~55 MB of unrelated HTTP/2 header
  compression conformance fixtures). `tools/localpaths.py` unconditionally
  `sys.path.insert`s every `third_party/<pkg>` directory, but **a nonexistent
  `sys.path` entry is a silent no-op in Python** — it only matters if nothing else on
  `sys.path` provides the module. Every leaf package WPT vendors there
  (`pywebsocket3`, `h2`, `hpack`, `hyperframe`, `websockets`, `html5lib`,
  `webencodings`, `certifi`, `atomicwrites`, …) is also a normal, actively maintained
  PyPI package, verified installable and sufficient by actually importing the full
  chain (`localpaths` → `manifest.manifest` → `tools.serve.serve` →
  `wptrunner.wptrunner` → `wptrunner.wptcommandline` → `webdriver.bidi.client`) in a
  clean venv with only `pip install -r` (see `tests/wpt/README.md`) — no
  `tools/third_party` needed. This keeps the vendor commit small and avoids
  committing megabytes of an unrelated project's test fixtures.
- **`tools/wpt/`** (the `wpt` CLI wrapper script/package). Not imported by anything
  vendored above (`wptrunner` itself doesn't depend on it — it's the outer CLI, not
  the library); it also does venv bootstrapping and browser-download machinery this
  project doesn't need (we already have our own venv + a locally built `lumen.exe`).
  S4 calls `wptcommandline`/`wptrunner.run_tests` directly instead
  (`tests/wpt/run_smoke.py`) rather than vendor the whole wrapper for one flag's
  worth of behavior; S7 ("CI wrapper + docs") is where a polished wrapper — either
  growing `run_smoke.py` or actually vendoring `tools/wpt/` — gets decided.
- **Self-test suites of the supporting packages** (`tools/manifest/tests/`,
  `tools/wptserve/tests/`, `tools/serve/test_serve.py`,
  `tools/serve/test_functional.py`) — not imported by anything on the path we
  actually exercise (verified: the import-chain smoke check in
  `tests/wpt/README.md` passes without them), so dropped to keep the vendor
  commit to library code only. One of these (`tools/wptserve/tests/functional/
  docroot/test.asis`) also had a raw-HTTP-response fixture whose bytes are
  line-ending-sensitive — Git's LF normalization (`.gitattributes`) would have
  silently altered it, which is exactly the kind of "modified vendored code"
  this task rules out; not vendoring it sidesteps the problem rather than
  fighting it. **`tools/wptrunner/wptrunner/tests/`** is kept, unlike the
  above — S2 explicitly names `tools/wptrunner/` as vendored whole, it's small
  (186 KB), and it has no such fixtures.
- **`resources/testharnessreport.js`**: intentionally **ours to write**, not
  upstream's — see `docs/tasks/p2-wpt-integration.md` S4 (the per-product results
  shim; there is no generic upstream one to vendor — `tools/wptrunner`'s own
  `wptrunner/testharnessreport.js` is wptrunner's *default*, but S4 calls for a
  Lumen-specific `tests/wpt/resources/testharnessreport.js`).

## Re-vendoring

1. Pick a new upstream commit (or re-pin the same date to check for drift).
2. `git clone --filter=blob:none --sparse --depth 1 https://github.com/web-platform-tests/wpt.git`
   at that commit, `git sparse-checkout set` the paths in the table above.
3. Copy over the destinations in the table (verbatim — no diffing/patching upstream
   files; if Lumen needs different behavior, that's a fork decision to make
   explicitly, not a silent edit).
4. Re-run the import-chain smoke check from `tests/wpt/README.md` in a clean venv.
5. Update the pinned commit hash + date at the top of this file.
