[internal-import-stylesheet-with-differentorigin-base-url-from-preload.tentative.html]
  expected: TIMEOUT
  [A child stylesheet from inline style should use the document's URL, not the document's base URL, as its referrer, even when the HTML parser preloads the stylesheet.]
    expected: TIMEOUT
