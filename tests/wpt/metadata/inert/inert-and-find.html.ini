# BUG-623: window.find() exists now; the test still fails before the find —
# iframe.contentWindow.getSelection() returns a cross-realm copy without
# removeAllRanges() (BUG-1099 class, frame_bridge wrapWinFacadeGlobals).
[inert-and-find.html]
  [Basic test for inert and find]
    expected: FAIL
