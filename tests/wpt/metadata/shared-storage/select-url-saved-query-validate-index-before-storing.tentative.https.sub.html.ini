[select-url-saved-query-validate-index-before-storing.tentative.https.sub.html]
  expected: TIMEOUT
  [for selectURL(), a saved query result index is verified before storing, and the default is stored if the index is out of range.]
    expected: TIMEOUT
