# Known engine gap, tracked as BUG-481: `window.visualViewport` is not
# implemented at all -- `.scale` throws on `undefined` before the
# assertion runs.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[viewport-clamp-initial-scale-to-max.tentative.html]
  [Page with meta viewport "width=device-width, initial-scale=10.0, maximum-scale=2.0" should scale to 2.0.]
    expected: FAIL
