# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- the bare identifier `reference` (an element with
# `id="reference"`) throws ReferenceError.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[documentElement-clientWidth-on-minimum-scale-size.tentative.html]
  [documentElement clientWidth should be equal to device-width even if overflow:hidden region is visible]
    expected: FAIL
