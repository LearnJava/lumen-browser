# Known engine gap, tracked as BUG-515: `window.devicePixelRatio` is
# `undefined` on the default single window -- only set by the opt-in
# multi-window `getScreenDetails()` API.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[viewport-should-not-affect-devicePixelRatio.html]
  [devicePixelRatio is unaffected by viewport's initial-scale]
    expected: FAIL
