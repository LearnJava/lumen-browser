# Not attributed to a filed bug this slice: the test waits on the iframe's
# 'load' event (addEventListener, then re-triggers via `iframe.src =
# iframe.src`) to drive two sequential test() registrations plus done() --
# the load event apparently never fires (or fires but something downstream
# silently breaks), so the harness times out with zero subtests. Not
# investigated further this slice (10-minute budget for unclear TIMEOUT-with-
# no-subtests cases).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run).

[variable-reference-refresh.html]
  expected: TIMEOUT
