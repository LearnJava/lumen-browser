# Same gap as variable-first-letter.html: BUG-493 (computed-style cache only
# populated post-navigation, not during the page's own synchronous top-level
# <script>), read via getComputedStyle(el, ':first-line').
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands.

[variable-first-line.html]
  [color]
    expected: FAIL
  [font-size]
    expected: FAIL
  [font-weight]
    expected: FAIL
  [position]
    expected: FAIL
  [nested color]
    expected: FAIL
  [abspos]
    expected: FAIL
