# Same two-gap shape: BUG-493 for the "before" subtest (color), BUG-503 for
# the "after" subtest (animationend never fires).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-503 land.

[variable-animation-substitute-within-keyframe.html]
  expected: TIMEOUT
  [Verify color before animation]
    expected: FAIL
  [Verify color after animation]
    expected: TIMEOUT
