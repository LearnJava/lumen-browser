# Two known engine gaps in this file's subtests: - "before animation"
# subtests -- BUG-499 (--value custom property never reaches
# getComputedStyle) and BUG-493 (color, a standard map-present property, read
# from a static element via a synchronous top-level <script> -- the computed-
# style cache is only populated post-navigation, confirmed live;
# document.documentElement.offsetHeight does not force it either). - "after
# animation" subtests -- BUG-503: the animationend event never fires for a
# real (engine-scheduled) CSS animation, so the async_test waiting on it
# never completes.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-493/BUG-503 land.

[variable-animation-from-to.html]
  expected: TIMEOUT
  [Verify CSS variable value before animation]
    expected: FAIL
  [Verify substituted color value before animation]
    expected: FAIL
  [Verify CSS variable value after animation]
    expected: NOTRUN
  [Verify substituted color value after animation]
    expected: NOTRUN
