# Not fully root-caused this slice: all 6 checkLayout() subtests report width
# 0 for every keyframe offset, including the ones that shouldn't be affected
# by the animation at all -- suggests the .test elements aren't being laid
# out with any width, not specifically a custom-property/guaranteed-invalid
# resolution issue. Possibly related to `animation-delay: calc(var(--at) *
# -1s)` (a custom property inside calc() inside animation-delay) failing in a
# way that cascades. Left unattributed pending deeper investigation --
# flagging per this slice's 10-minute budget for unclear cases.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run).

[variable-animation-guaranteed-invalid.html]
  [.test 1]
    expected: FAIL
  [.test 2]
    expected: FAIL
  [.test 3]
    expected: FAIL
  [.test 4]
    expected: FAIL
  [.test 5]
    expected: FAIL
  [.test 6]
    expected: FAIL
