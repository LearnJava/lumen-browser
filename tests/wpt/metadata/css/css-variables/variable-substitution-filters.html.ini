# Known engine gap, tracked as BUG-493: filter (a standard, map-present
# property) is read via getComputedStyle() from static markup through a
# synchronous top-level <script> -- the computed-style cache is only
# populated post-navigation (confirmed live via a two-eval-call A/B probe:
# the same var()-substituted filter value reads correctly when queried from a
# LATER, separate eval() call).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands.

[variable-substitution-filters.html]
  [blur]
    expected: FAIL
  [brightness]
    expected: FAIL
  [contrast]
    expected: FAIL
  [grayscale]
    expected: FAIL
  [invert]
    expected: FAIL
  [sepia]
    expected: FAIL
  [saturate]
    expected: FAIL
