# Known engine gap, tracked as BUG-493: font-family/font-weight (standard,
# map-present properties resolved through a chained, registered font-relative
# custom property) are read via getComputedStyle() from static markup through
# a synchronous top-level <script> -- the computed-style cache is only
# populated post-navigation (confirmed live), not during the page's own
# script execution.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands.

[variable-resolution-during-prioritary-properties.html]
  [#a: font-family resolves through a chained custom property with a registered font-relative custom present]
    expected: FAIL
  [#b: font-family resolves through a chained custom property with a registered font-relative custom present]
    expected: FAIL
  [#c: font-family resolves through a chained custom property with a registered font-relative custom present]
    expected: FAIL
  [#d: font-family resolves through a chained custom property with a registered font-relative custom present]
    expected: FAIL
  [#w: font-weight resolves through a chained custom property with a registered font-relative custom present]
    expected: FAIL
