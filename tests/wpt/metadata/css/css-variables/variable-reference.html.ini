# Two known engine gaps in this file's 10 failing subtests: - 9 of them --
# BUG-484 (extended this slice): the inline style setter never validates a
# var() reference's argument syntax before storing it (e.g. `width:
# var()`/`var(prop)`/`var(--prop 20px)` are all accepted verbatim instead of
# being rejected), and also never normalizes the !important-with-var()
# serialization. - "Variable reference left open at end of stylesheet" --
# BUG-471: `<style>` elements have no `.sheet` property (CSSOM
# stylesheet/rule object model not implemented), so `.sheet.cssRules` throws.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-484/BUG-471 land.

[variable-reference.html]
  [width: var(--prop) !important;]
    expected: FAIL
  [width: var();]
    expected: FAIL
  [width: var(prop);]
    expected: FAIL
  [width: var(-prop);]
    expected: FAIL
  [width: var(--prop 20px);]
    expected: FAIL
  [width: var(--prop, var(prop));]
    expected: FAIL
  [width: var(--prop, var(-prop));]
    expected: FAIL
  [width: var(20px);]
    expected: FAIL
  [width: var(var(--prop));]
    expected: FAIL
  [Variable reference left open at end of stylesheet]
    expected: FAIL
