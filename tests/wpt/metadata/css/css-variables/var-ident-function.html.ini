# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- id=target gives neither window.target nor a bare `target`
# identifier. This masks a second, deeper gap: BUG-500 (ident(), CSS Values
# L5, is entirely unimplemented -- confirmed by source grep alone, since this
# file never reaches that code path).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run once BUG-384 lands -- expect this to then surface BUG-500
# instead.

[var-ident-function.html]
  [Referencing a custom property with ident()]
    expected: FAIL
  [ident() remains unresolved on custom properties]
    expected: FAIL
  [ident() causing lookup of invalid custom property]
    expected: FAIL
  [ident() causing lookup of invalid custom property, fallback]
    expected: FAIL
  [ident() causing lookup of invalid custom property, fallback, CSS-wide keyword]
    expected: FAIL
