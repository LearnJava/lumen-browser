# Two known engine gaps: BUG-493 for the "before animation" subtest (border-
# bottom-color, a standard map-present property, read from a static element
# via a synchronous top-level <script> -- the computed-style cache is only
# populated post-navigation, confirmed live); BUG-503 for the "after
# animation" subtest (animationend never fires for a real CSS animation).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-503 land.

[variable-animation-substitute-into-keyframe-shorthand.html]
  expected: TIMEOUT
  [Verify border-bottom-color before animation]
    expected: FAIL
  [Verify border-bottom-color after animation]
    expected: TIMEOUT
