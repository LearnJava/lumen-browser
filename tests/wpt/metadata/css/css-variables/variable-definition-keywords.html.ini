# Known engine gap, tracked as BUG-499: getComputedStyle().getPropertyValue('
# --var') always returns "" for the 3 "computed style ..." subtests
# (inherit/unset/revert) -- the other 5 subtests in this file (the initial-
# value case, which coincidentally expects "", plus the 4 "specified style"
# subtests reading elem.style.getPropertyValue instead of getComputedStyle)
# already pass.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499 lands.

[variable-definition-keywords.html]
  [computed style inherit]
    expected: FAIL
  [computed style unset]
    expected: FAIL
  [computed style revert]
    expected: FAIL
