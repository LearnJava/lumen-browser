# Two known engine gaps, both from the same-tick create+append+read pattern:
# BUG-499 (--c custom property never reaches getComputedStyle) and BUG-493
# (color, a standard property, read via getComputedStyle() on a node
# created+appended earlier in the SAME script turn -- no entry in the cache
# at all yet). The file's third subtest (inline style getPropertyValue('--
# c'), not getComputedStyle) already passes -- a different, working code
# path.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-493 land.

[variable-created-element.html]
  [Computed variable value appearing in a created element's inline style should work once spliced into the creating document]
    expected: FAIL
  [Variable reference appearing in a created element's inline style should work once spliced into the creating document]
    expected: FAIL
