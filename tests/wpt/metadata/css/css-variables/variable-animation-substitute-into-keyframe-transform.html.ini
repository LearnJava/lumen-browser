# Same two-gap shape as variable-animation-substitute-into-keyframe-
# shorthand.html: BUG-493 for the "before" subtest (transform), BUG-503 for
# the "after" subtest (animationend never fires).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-503 land.

[variable-animation-substitute-into-keyframe-transform.html]
  expected: TIMEOUT
  [Verify transform before animation]
    expected: FAIL
  [Verify transform after animation]
    expected: TIMEOUT
