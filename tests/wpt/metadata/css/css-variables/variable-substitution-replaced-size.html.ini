# Known engine gap, tracked as BUG-493: width/height (standard, map-present
# properties) on IFRAME/INPUT/CANVAS are read via getComputedStyle() from
# static markup through a synchronous top-level <script> -- the computed-
# style cache is only populated post-navigation (confirmed live).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands.

[variable-substitution-replaced-size.html]
  [width on IFRAME]
    expected: FAIL
  [height on IFRAME]
    expected: FAIL
  [width on INPUT]
    expected: FAIL
  [height on INPUT]
    expected: FAIL
  [width on CANVAS]
    expected: FAIL
  [height on CANVAS]
    expected: FAIL
