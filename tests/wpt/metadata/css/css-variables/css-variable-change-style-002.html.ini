# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- id="outer" gives neither window.outer nor a bare `outer`
# identifier (same file family as css-variable-change-style-001.html).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-384 lands.

[css-variable-change-style-002.html]
  [Declaration changes on 'color' propagate to all variable references]
    expected: FAIL
  [Declaration changes on 'background-color' propagate to all variable references]
    expected: FAIL
  [Declaration changes on 'white-space' propagate to all variable references]
    expected: FAIL
