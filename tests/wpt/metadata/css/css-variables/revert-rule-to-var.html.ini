# Known engine gap, tracked as BUG-501: CSS.supports()'s one-arg form never
# retries wrapped in parens, so `CSS.supports('color:revert-rule')` returns
# false and the getComputedStyle() assertion is never reached. Also see
# BUG-487 (revert-rule cascade unimplemented) and BUG-384 (the test reads the
# target element via the bare identifier `target`, currently unreached).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-501 (and possibly BUG-487/384)
# land.

[revert-rule-to-var.html]
  [Using revert-rule to revert to a value containing var()]
    expected: FAIL
