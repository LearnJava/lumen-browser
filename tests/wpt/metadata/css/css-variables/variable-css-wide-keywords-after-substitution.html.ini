# Not attributed to a filed bug this slice: the one failing subtest ("revert-
# layer after var() substitution takes effect on the cascade") reads
# getComputedStyle(el).backgroundColor after a `--x: var(--empty) revert-
# layer;` declaration inside a layered @layer block -- a revert-layer +
# var()-substitution cascade interaction, distinct from BUG-487 (which is
# about the unrelated revert-rule keyword). The other 5 subtests in this file
# pass by coincidence: they compare two custom-property reads that are both
# unconditionally empty per BUG-499, so they trivially match each other
# without exercising the keyword semantics at all.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run).

[variable-css-wide-keywords-after-substitution.html]
  [revert-layer after var() substitution takes effect on the cascade]
    expected: FAIL
