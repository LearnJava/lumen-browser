# Known engine gap, tracked as BUG-472: getComputedStyle() resolves through a
# fixed ~68-key map (computed_style_to_map); box-shadow is not one of the
# keys, so it's silently empty regardless of the var() substitution this file
# is actually probing (a missing-closing-paren fallback recovery case).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-472 lands.

[missing-closing-nested-fallback.html]
  [Variable substitution with missing closing parenthesis]
    expected: FAIL
