# Known engine gap, tracked as BUG-493: color/font-size/font-weight/position
# (standard, map-present properties) are read via getComputedStyle(el,
# ':first-letter') from static markup through a synchronous top-level
# <script> -- the computed-style cache is only populated post-navigation
# (confirmed live), not during the page's own script execution. A live probe
# after the fix's absence (post-navigation getComputedStyle(el, ':first-
# letter')) returned a non-empty but seemingly wrong value (the element's own
# color, not the pseudo-element's) -- a possible second, latent ::first-
# letter/::first-line resolution gap not further isolated this slice.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands (re-check ::first-
# letter resolution specifically afterward).

[variable-first-letter.html]
  [color]
    expected: FAIL
  [font-size]
    expected: FAIL
  [font-weight]
    expected: FAIL
  [position]
    expected: FAIL
  [nested color]
    expected: FAIL
  [abspos]
    expected: FAIL
