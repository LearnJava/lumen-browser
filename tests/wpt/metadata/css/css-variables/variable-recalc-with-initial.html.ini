# Known engine gap, tracked as BUG-493: color (a standard, map-present
# property) is read via getComputedStyle() from static markup through a
# synchronous top-level <script> -- the computed-style cache is only
# populated post-navigation (confirmed live), not during the page's own
# script execution, even though this test explicitly forces target.offsetTop
# first.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493 lands.

[variable-recalc-with-initial.html]
  [Style recalculation picks up “initial” variable declaration]
    expected: FAIL
