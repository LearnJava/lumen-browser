# Two known engine gaps affect this file's two subtests (both read via
# getComputedStyle() on a node spliced in from a second,
# document.implementation.createHTMLDocument()-created document): BUG-499 for
# --c (custom property), BUG-493 for color (standard, map-present property).
# The harness itself is flaky TIMEOUT across re-runs -- one run registered
# both subtests as FAIL before timing out, another registered zero -- so only
# the file-level TIMEOUT is committed here rather than specific subtest
# entries, to avoid a false 'unexpected' on the runs that register none. Not
# investigated further this slice.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-493 land.

[variable-created-document.html]
  expected: TIMEOUT
