# Two known engine gaps in this file's 4 subtests: - "css rule test"/"css
# rule test important" -- BUG-471: document.styleSheets[0] is undefined
# (CSSOM stylesheet/rule object model not implemented). - "inline style
# test"/"inline style test important" -- BUG-484 (extended this slice): the
# cssText getter returns the raw style= attribute text verbatim (e.g. "--
# var3:red") instead of a normalized serialization ("--var3: red;" with a
# space after `:` and a trailing `;`).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-471/BUG-484 land.

[variable-invalidation.html]
  [css rule test]
    expected: FAIL
  [css rule test important]
    expected: FAIL
  [inline style test]
    expected: FAIL
  [inline style test important]
    expected: FAIL
