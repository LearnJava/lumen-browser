# Two known engine gaps: BUG-499 for the "before" subtest (--value custom
# property never reaches getComputedStyle), BUG-503 for the "after" subtest
# (animationend never fires for a real CSS animation, reported TIMEOUT by the
# structured report even though it's the same async_test-never-completes
# mechanism as the NOTRUN cases elsewhere in this cluster).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-503 land.

[variable-animation-to-only.html]
  expected: TIMEOUT
  [Verify CSS variable value before animation]
    expected: FAIL
  [Verify CSS variable value after animation]
    expected: TIMEOUT
