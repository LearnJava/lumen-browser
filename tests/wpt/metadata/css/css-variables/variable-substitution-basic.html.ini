# Two known engine gaps, compounding: BUG-493 (every subtest here does
# document.createElement + style + appendChild + immediate getComputedStyle()
# read, all in the same script turn -- no cache entry at all yet) and BUG-472
# (border-spacing, used by most of these subtests, is additionally missing
# from computed_style_to_map's fixed key list, so it'll stay empty even once
# BUG-493 is fixed).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-472 land.

[variable-substitution-basic.html]
  [Simple substitution test]
    expected: FAIL
  [You can't build up a single token where part of it is provided by a variable]
    expected: FAIL
  [You can't build up a single token where part of it is provided by a variable (percentages)]
    expected: FAIL
  [Multiple variable references in a single property]
    expected: FAIL
  [Multiple variable references in a single property (no spaces)]
    expected: FAIL
  [Fallback value]
    expected: FAIL
  [Fallback value which is also a variable reference]
    expected: FAIL
  [Multiple var references in fallback value]
    expected: FAIL
  [Multiple nested fallbacks]
    expected: FAIL
  [Bad variable reference that should inherit by default]
    expected: FAIL
  [Test that var reference doesn’t overwrite !important]
    expected: FAIL
  [Test that !important on a property that has a variable reference can overwrite !important]
    expected: FAIL
  [Test that !important inside of var reference can't overwrite !important on property]
    expected: FAIL
