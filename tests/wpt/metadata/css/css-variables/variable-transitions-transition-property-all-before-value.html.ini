# Two known engine gaps: BUG-499/BUG-493 for the "before transition" subtests
# (--value custom property, color standard property, both read from static
# markup through a synchronous top-level <script> before the computed-style
# cache is populated), BUG-503 for the "after transition" subtests
# (transitionend -- plausibly the same dispatch gap as animationend -- never
# fires, so the async_test never completes; reported NOTRUN here rather than
# TIMEOUT since the file's other subtests fail first).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-493/BUG-503 land.

[variable-transitions-transition-property-all-before-value.html]
  expected: TIMEOUT
  [Verify CSS variable value before transition]
    expected: FAIL
  [Verify substituted color value before transition]
    expected: FAIL
  [Verify CSS variable value after transition]
    expected: NOTRUN
  [Verify substituted color value after transition]
    expected: NOTRUN
