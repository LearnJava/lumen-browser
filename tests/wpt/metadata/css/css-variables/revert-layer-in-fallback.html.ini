# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- id=child gives neither window.child nor a bare `child`
# identifier. Masks whatever revert-layer-in-var()-fallback behaviour this
# file is actually probing.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-384 lands.

[revert-layer-in-fallback.html]
  [var(--unknown, revert-layer) in custom property]
    expected: FAIL
  [var(--unknown, revert-layer) in shorthand]
    expected: FAIL
  [var(--unknown, revert-layer) in shorthand observed via longhand]
    expected: FAIL
  [var(--unknown, revert-layer) in longhand]
    expected: FAIL
