# Known engine gap, tracked as BUG-484 (extended this slice): the `cssText`
# getter (dom.rs:4287) returns the raw `style="..."` HTML-attribute text
# verbatim instead of a value derived from a real parsed declaration-block
# model -- no per-property deduplication (two declarations for the same
# property in source both survive), no comment stripping, and no
# shorthand/longhand interaction (setting a longhand after a var()-containing
# shorthand never invalidates the shorthand's serializability).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-484 lands.

[variable-cssText.html]
  [target6]
    expected: FAIL
  [target7]
    expected: FAIL
  [target8]
    expected: FAIL
  [target9]
    expected: FAIL
  [target10]
    expected: FAIL
  [target11]
    expected: FAIL
