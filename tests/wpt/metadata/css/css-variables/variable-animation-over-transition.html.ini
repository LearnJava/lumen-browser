# Two known engine gaps, same shape as variable-animation-from-to.html:
# BUG-499/BUG-493 for the "before" subtests (--value custom property, color
# standard property, static markup + synchronous top-level <script>), BUG-503
# for the "after" subtests (animationend never fires).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-499/BUG-493/BUG-503 land.

[variable-animation-over-transition.html]
  expected: TIMEOUT
  [Verify CSS variable value before animation]
    expected: FAIL
  [Verify substituted color value before animation]
    expected: FAIL
  [Verify CSS variable value after animation]
    expected: NOTRUN
  [Verify substituted color value after animation]
    expected: NOTRUN
