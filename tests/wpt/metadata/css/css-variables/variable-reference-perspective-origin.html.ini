# Two known engine gaps, compounding: BUG-493 (the element under test is
# created via document.createElement and read back in the same script turn)
# and BUG-472 (perspective-origin is not one of the keys in
# computed_style_to_map's fixed list, so it stays empty even once BUG-493 is
# fixed).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-472 land.

[variable-reference-perspective-origin.html]
  [var() as first value of perspective-origin]
    expected: FAIL
  [var() as second value of perspective-origin]
    expected: FAIL
  [var() as both values of perspective-origin]
    expected: FAIL
  [same var() for both values of perspective-origin]
    expected: FAIL
  [var() as second value with keyword]
    expected: FAIL
  [var() as first value with keyword]
    expected: FAIL
