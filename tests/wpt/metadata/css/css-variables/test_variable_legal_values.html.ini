# Known engine gap, tracked as BUG-360: <body onload="run()"> content-
# attribute event handlers never fire (on<type> is not compiled into a
# handler). run() -- which calls setup({explicit_done:true}) and would
# eventually call done() -- never executes, so the harness times out with
# zero subtests registered.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-360 lands.

[test_variable_legal_values.html]
  expected: TIMEOUT
