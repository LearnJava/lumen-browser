# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- id="target1" gives neither window.target1 nor a bare
# `target1` identifier. Note: all 3 subtests here expect an empty string for
# a guaranteed-invalid custom property, which BUG-499 (custom properties
# never reach getComputedStyle) already produces unconditionally -- so these
# will likely pass by coincidence once BUG-384 is fixed, without the engine
# having actually detected cycle/reference invalidity.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run once BUG-384 lands.

[variables-substitute-guaranteed-invalid.html]
  [Custom properties in a cycle become guaranteed-invalid]
    expected: FAIL
  [A custom property referencing a cycle becomes guaranteed-invalid]
    expected: FAIL
  [A custom property referencing a non-existent variable becomes guaranteed-invalid]
    expected: FAIL
