# Two known engine gaps, compounding: BUG-493 (static markup + synchronous
# top-level <script> reading getComputedStyle() before the cache is
# populated) and BUG-472 (box-shadow/text-shadow are missing from
# computed_style_to_map's fixed key list entirely, so they'll stay empty even
# once BUG-493 is fixed).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.10, css/css-variables
# mass run). Re-run and flip to PASS once BUG-493/BUG-472 land.

[variable-substitution-shadow-properties.html]
  [box-shadow]
    expected: FAIL
  [box-shadow-with-comment]
    expected: FAIL
  [text-shadow]
    expected: FAIL
