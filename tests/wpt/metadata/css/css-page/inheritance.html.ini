# Known engine gap, tracked as BUG-472: computed_style_to_map is missing
# the `page` longhand entirely -- it is parsed and stored, but
# getComputedStyle() never surfaces it. Observed via the structured
# wptreport (WPT-RUN-3 sl.26, css/css-page mass run). Re-run and flip to
# PASS once BUG-472 lands.

[inheritance.html]
  [Property page has initial value auto]
    expected: FAIL
  [Property page does not inherit]
    expected: FAIL
