# Known engine gap, tracked as BUG-485: document.head is entirely missing
# (no getter, no native binding on either engine). The shared
# test_valid_rule/test_invalid_rule helper (css/support/parsing-
# testcommon.js) does document.head.append(style) as its first step.
# Observed via the structured wptreport (WPT-RUN-3 sl.26, css/css-page mass
# run). Re-run and flip to PASS once BUG-485 lands.

[page-rules-001.html]
  [@page , { } should be an invalid rule]
    expected: FAIL
  [@page { } should be a valid rule]
    expected: FAIL
  [@page a { } should be a valid rule]
    expected: FAIL
  [@page page1 { } should be a valid rule]
    expected: FAIL
  [@page name1, name2 { } should be a valid rule]
    expected: FAIL
  [@page name1\\{\\}name2 { } should be a valid rule]
    expected: FAIL
  [@page a, { } should be an invalid rule]
    expected: FAIL
  [@page ,a { } should be an invalid rule]
    expected: FAIL
