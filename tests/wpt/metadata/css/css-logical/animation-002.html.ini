# Known engine gap, tracked as BUG-506, re-attributed 2026-09-04 to
# CSSOM-4/BUG-493 (see bugs/BUG-506-OPEN.md's 2026-09-04 slice for the
# full investigation). The ORIGINAL symptom this .ini was written against
# (a cross-directory external <script src> never running before
# dependent inline code, ReferenceError: addDiv is not defined) no
# longer reproduces -- re-measured against the real wptrunner
# (`--bidi-port`, `LumenTestharnessExecutor`, unmodified) pipeline and
# `addDiv`/`addStyle` are correctly defined every run. Every subtest
# below still fails, now because of a different, already-tracked cause:
# `FlushHandles::maybe_flush` (crates/js/src/v8_runtime/style_flush.rs)
# is a no-op outside `InProcessSession` (headless/--mcp-port), so the
# live `--bidi-port` shell every real WPT run uses never flushes style/
# layout synchronously before a same-turn JS read -- `addStyle(t, rules)`
# (support/testcommon.js:139) appends a fresh <style> and immediately
# reads back its `.sheet`, which is still `null` (the associated
# CSSStyleSheet hasn't been created yet), so `sheet.insertRule(...)`
# throws `TypeError: Cannot read properties of null (reading
# 'insertRule')` instead of the ReferenceError BUG-506's original filing
# expected. See bugs/BUG-493-OPEN.md/BUG-977-OPEN.md/CSSOM-4 and CSSOM-7
# in ROADMAP.md. Re-run and flip once CSSOM-4's live-shell coverage
# follow-up lands.


[animation-002.html]
  [Logical properties can be animated]
    expected: FAIL
  [Logical properties in animations respect the writing-mode]
    expected: FAIL
  [Logical properties in animations respect the direction]
    expected: FAIL
  [Declaration order is respected within @keyframes declaration blocks]
    expected: FAIL
  [Logical properties are able to override physical properties in @keyframes declaration blocks]
    expected: FAIL
  [Declaration order is respected amongst logical properties within @keyframes declaration blocks]
    expected: FAIL
  [Physical properties and logical properties can be mixed]
    expected: FAIL
  [Declaration order is respected on each keyframe individually]
    expected: FAIL
  [Animations update when the writing-mode is changed]
    expected: FAIL
  [Filling animations update when the writing-mode is changed]
    expected: FAIL
  [The number of interpolating properties can be increased when the writing-mode is changed]
    expected: FAIL
  [The number of interpolating properties can be decreased when the writing-mode is changed]
    expected: FAIL
  [Animations update when the writing-mode is changed through a CSS variable]
    expected: FAIL
  [Logical shorthand with variable references animates correctly]
    expected: FAIL
  [Animations update when the direction is changed]
    expected: FAIL
