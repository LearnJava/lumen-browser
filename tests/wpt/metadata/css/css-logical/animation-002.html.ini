# BUG-506 (closed 2026-09-24): the original cross-directory <script src>
# symptom no longer reproduces -- `addDiv`/`addStyle` are defined every
# run under the real wptrunner (`--bidi-port`) pipeline. Every subtest
# below still fails on ONE remaining cause, owned by BUG-493 (P6):
# `addStyle(t, rules)` (css-animations/support/testcommon.js:139) appends a
# fresh <style> and reads `.sheet` in the same turn; Lumen's
# `_lumen_element_sheet_getter` answers from a registry the shell only
# refills after the cascade, so `.sheet` is still `null` and
# `sheet.insertRule(...)` throws TypeError. Re-run and flip once BUG-493's
# "`.sheet` null right after appendChild" residual is fixed.


[animation-002.html]
  [Logical properties can be animated]
    expected: FAIL
  [Logical properties in animations respect the writing-mode]
    expected: FAIL
  [Logical properties in animations respect the direction]
    expected: FAIL
  [Declaration order is respected within @keyframes declaration blocks]
    expected: FAIL
  [Logical properties are able to override physical properties in @keyframes declaration blocks]
    expected: FAIL
  [Declaration order is respected amongst logical properties within @keyframes declaration blocks]
    expected: FAIL
  [Physical properties and logical properties can be mixed]
    expected: FAIL
  [Declaration order is respected on each keyframe individually]
    expected: FAIL
  [Animations update when the writing-mode is changed]
    expected: FAIL
  [Filling animations update when the writing-mode is changed]
    expected: FAIL
  [The number of interpolating properties can be increased when the writing-mode is changed]
    expected: FAIL
  [The number of interpolating properties can be decreased when the writing-mode is changed]
    expected: FAIL
  [Animations update when the writing-mode is changed through a CSS variable]
    expected: FAIL
  [Logical shorthand with variable references animates correctly]
    expected: FAIL
  [Animations update when the direction is changed]
    expected: FAIL
