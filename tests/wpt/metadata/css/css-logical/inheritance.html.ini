# Known engine gap, tracked as BUG-384: named access on Window (`id="x"`
# should expose a bare global `x`/`window.x`) is not implemented. This
# file references markup elements (`reference`, `container`, `target`) as
# bare identifiers without `getElementById`; the resulting ReferenceError
# aborts the whole top-level <script> before any test() call registers,
# so the harness times out with zero subtests.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.12, css/css-logical
# mass run). Re-run and flip to PASS once BUG-384 lands.


[inheritance.html]
  expected: TIMEOUT
