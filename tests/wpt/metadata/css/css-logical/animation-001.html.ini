# BUG-506 (closed 2026-09-24): the original cross-directory <script src>
# symptom no longer reproduces, and the same-turn getComputedStyle() gap it
# was re-attributed to was closed for the live shell by CSSOM-7. Of the
# 24 subtests, 7 PASS: blockSize animated (object/array notation),
# physical-wins-over-logical (3), and `writing-mode`/`direction is not
# animatable` -- the last two needed getComputedStyle() to serialise
# writing-mode/direction at all (read back "" before 2026-09-24,
# crates/engine/layout/src/selector_query.rs). `Logical properties are NOT
# stored as physical properties` PASSes too (never listed).
#
# Remaining FAILs were not root-caused in that slice. Visible classes:
# logical properties in keyframes ignore the element's writing-mode/
# direction (the documented LTR/horizontal-tb-only scope of CSS Logical L1
# in CSS-SPECS.md), shorthand-vs-longhand priority inside one keyframe, and
# var() inside logical keyframe values.


[animation-001.html]
  [Logical properties in animations respect the writing-mode]
    expected: FAIL
  [Logical properties in animations respect the direction]
    expected: FAIL
  [Physical properties with variables win over logical properties]
    expected: FAIL
  [Physical longhands win over logical shorthands]
    expected: FAIL
  [Logical longhands win over physical shorthands]
    expected: FAIL
  [Physical shorthands win over logical shorthands]
    expected: FAIL
  [Physical shorthands using variables win over logical shorthands]
    expected: FAIL
  [Physical properties and logical properties can be mixed]
    expected: FAIL
  [Physical shorthands and logical shorthands can be mixed]
    expected: FAIL
  [Animations update when the writing-mode is changed]
    expected: FAIL
  [Filling animations update when the writing-mode is changed]
    expected: FAIL
  [Animations with implicit from values update when the writing-mode is changed]
    expected: FAIL
  [Animations with overlapping physical and logical properties update when the writing-mode is changed]
    expected: FAIL
  [Animations update when the writing-mode is changed through a CSS variable]
    expected: FAIL
  [Animations update when the direction is changed]
    expected: FAIL
  [Logical shorthand with variable references animates correctly]
    expected: FAIL
