# BUG-506 (closed 2026-09-24): `addDiv` is defined and same-turn
# getComputedStyle() is fresh since CSSOM-7. The subtest still fails
# (margin-left 0px instead of 100px); not root-caused. Candidates: the
# at-risk `logical` keyword in `margin: logical 300px` (css-logical-1 §4.1)
# is parsed nowhere in the engine, and shorthand-vs-longhand priority
# inside one Web Animations keyframe (same class as animation-001's FAILs).


[logical-shorthand-relative-prioritization-by-number-of-components.tentative.html]
  [Logical shorthands follow the usual prioritization based on number of component longhands]
    expected: FAIL
