# Known engine gap, tracked as BUG-446: the JS runtime runs an inline
# `<script type="module">`'s own top-level body, but does not fetch/
# execute the module graph its `import` statements reference (dynamic
# `import()` and static `import {...} from "..."` alike) -- the harness
# never gets its `test()` calls registered because the imported helper
# module (`./resources/test-box-properties.js` /
# `./resources/test-logical-values.js`) never runs, so it times out with
# zero subtests.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.12, css/css-logical
# mass run). Re-run and flip to PASS once BUG-446 lands.


[logical-box-padding.html]
  expected: TIMEOUT
