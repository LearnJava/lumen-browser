# Known engine gap, tracked as BUG-532: CSS `zoom` (css-viewport) is not
# implemented at all -- no parser recognition, no ComputedStyle field, no
# layout/paint wiring (grep for "zoom" across css-parser/layout is empty).
# Observed via the structured wptreport (WPT-RUN-3 sl.25, css/css-viewport
# mass run). Re-run and flip to PASS once BUG-532 lands.

[computedStyle-zoom.html]
  [getComputedStyle for elements with css zoom]
    expected: FAIL
  [getComputedStyle for elements with css zoom 1]
    expected: FAIL
  [getComputedStyle for elements with css zoom 2]
    expected: FAIL
  [getComputedStyle for elements with css zoom 3]
    expected: FAIL
  [getComputedStyle for elements with css zoom 4]
    expected: FAIL
  [getComputedStyle for elements with css zoom 5]
    expected: FAIL
