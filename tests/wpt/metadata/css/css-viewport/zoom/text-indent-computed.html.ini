# Known engine gap, tracked as BUG-532: `getComputedStyle()` still returns a
# zoomed value for `rem`/`inherit` text-indent (CSS Viewport L1 `#zoom-om`) --
# `px_str`/`length_to_css` un-zooming only covers the `Length::Px` fields
# `cascade.rs::apply_zoom_to_lengths` scales; `text-indent` itself is not on
# that list, and `rem` resolves against the (already zoomed) root font-size.
# Re-run and flip to PASS once the un-zoom covers this case.

[text-indent-computed.html]
  [Property text-indent value '1rem' rem value no zoom]
    expected: FAIL
  [Property text-indent value 'inherit' inherit no zoom]
    expected: FAIL
  [Property text-indent value '1rem' rem value with zoom: 2]
    expected: FAIL
  [Property text-indent value 'inherit' inherit with zoom: 2]
    expected: FAIL
