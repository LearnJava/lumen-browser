# Known engine gap, tracked as BUG-484: the inline style setter
# (CSSStyleDeclaration property setter) does not validate values against
# the property's grammar at all -- any string is accepted verbatim, and
# valid values are not re-serialized into canonical form. Observed via the
# structured wptreport (WPT-RUN-3 sl.28, css/css-images mass run). Re-run and
# flip to PASS once BUG-484 lands.

[gradient-position-valid.html]
  [e.style\['background-image'\] = "radial-gradient(at bottom 10% right 20%, red, blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "radial-gradient(at bottom right, red, blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "radial-gradient(at center left, red, blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "radial-gradient(at top center, red, blue)" should set the property value]
    expected: FAIL
