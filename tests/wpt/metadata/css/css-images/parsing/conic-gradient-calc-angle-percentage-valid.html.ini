# Known engine gap, tracked as BUG-484: the inline style setter
# (CSSStyleDeclaration property setter) does not validate values against
# the property's grammar at all -- any string is accepted verbatim, and
# valid values are not re-serialized into canonical form. Observed via the
# structured wptreport (WPT-RUN-3 sl.28, css/css-images mass run). Re-run and
# flip to PASS once BUG-484 lands.

[conic-gradient-calc-angle-percentage-valid.html]
  [e.style\['background-image'\] = "conic-gradient(red, calc(0deg + 100%), blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "conic-gradient(red calc(0deg + 100%), blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "conic-gradient(red calc(90deg + 50%), blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "conic-gradient(red calc(90deg + 0%), blue)" should set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "repeating-conic-gradient(red calc(90deg + 50%), blue)" should set the property value]
    expected: FAIL
