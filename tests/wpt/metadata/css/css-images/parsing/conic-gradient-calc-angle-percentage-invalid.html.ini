# Known engine gap, tracked as BUG-484: the inline style setter
# (CSSStyleDeclaration property setter) does not validate values against
# the property's grammar at all -- any string is accepted verbatim, and
# valid values are not re-serialized into canonical form. Observed via the
# structured wptreport (WPT-RUN-3 sl.28, css/css-images mass run). Re-run and
# flip to PASS once BUG-484 lands.

[conic-gradient-calc-angle-percentage-invalid.html]
  [e.style\['background-image'\] = "linear-gradient(red calc(50% + 30deg), blue)" should not set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "radial-gradient(red calc(50% + 30deg), blue)" should not set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "conic-gradient(from calc(50% + 30deg), red, blue)" should not set the property value]
    expected: FAIL
  [e.style\['background-image'\] = "conic-gradient(red calc(50% + 0), blue)" should not set the property value]
    expected: FAIL
