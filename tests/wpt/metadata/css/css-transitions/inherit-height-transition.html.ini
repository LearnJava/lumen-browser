# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with `id="x"` gives neither `window.x` nor a
# bare global `x`, so any inline <script> that refers to an id-named
# element as a bare identifier throws ReferenceError. Observed via the
# structured wptreport (WPT-RUN-3 sl.27, css/css-transitions mass run).
# Re-run and flip to PASS once BUG-384 lands.

[inherit-height-transition.html]
  [Transitioned height, explicitly inherited down two DOM levels, should inherit correctly]
    expected: FAIL
