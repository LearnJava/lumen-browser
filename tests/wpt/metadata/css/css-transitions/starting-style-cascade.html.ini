# Known engine gap, tracked as BUG-471: CSSStyleSheet/CSSRule hierarchy is unimplemented (no `new CSSStyleSheet()` constructor). Observed via the structured wptreport (WPT-RUN-3 sl.27, css/css-transitions mass run). Re-run and flip to PASS once BUG-471 lands.

[starting-style-cascade.html]
  expected: ERROR
