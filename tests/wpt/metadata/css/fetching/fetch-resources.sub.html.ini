# Was tracked as BUG-520 (Resource Timing entries never recorded for real
# resource fetches -- performance.getEntriesByType('resource') stays [] forever,
# so PerformanceObserver({type:'resource'}) never fires). That mechanism is
# fixed (BUG-839 landed it as a side effect, BUG-520 closed 2026-09-07 as
# tracker drift). The actual blocker for full PASS here is CORS request-mode
# headers (Origin/Sec-Fetch-Mode) per RequestDestination, which fetch_subresource
# has no concept of at all -- tracked as BUG-1021. Expect TIMEOUT to become FAIL
# on the shape-outside/WebFonts subtests (both want `cors`) once re-run; left at
# TIMEOUT/NOTRUN below because no live re-run was possible in this environment
# (tests/wpt/run_smoke.py cannot start -- ssl.wrap_socket removed in Python 3.14).
# Observed via WPT-RUN-3 slice 23 (css/fetching + css/fill-stroke + css/geometry mass run).
# Re-run and re-triage once BUG-1021 lands or a live run becomes possible.


[fetch-resources.sub.html]
  expected: TIMEOUT
  [Background images should fetch with no-cors]
    expected: TIMEOUT
  [Shape images should fetched with cors]
    expected: NOTRUN
  [WebFonts should be fetched with cors]
    expected: NOTRUN
  [CSS imports should be fetched without cors]
    expected: NOTRUN
