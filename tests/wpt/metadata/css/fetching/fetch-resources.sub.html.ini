# BUG-1021 (fixed 2026-09-25): subresources carry their own request mode
# (Sec-Fetch-Mode/-Dest/-Site, Origin for cross-origin cors) — background-image
# passes; `@import` passes too when run in isolation, but is NOTRUN here
# because the harness times out on the two subtests before it.
# The remaining two never send a request at all:
#   shape-outside: url() — CSS Shapes image shapes are not implemented, the
#     image is never fetched (CSS-SPECS.md, `shape-outside` row);
#   @font-face in a script-inserted <style> is never fetched — BUG-1154.

[fetch-resources.sub.html]
  expected: TIMEOUT
  [Shape images should fetched with cors]
    expected: TIMEOUT
  [WebFonts should be fetched with cors]
    expected: NOTRUN
  [CSS imports should be fetched without cors]
    expected: NOTRUN
