# Known engine gap, tracked as BUG-520: Resource Timing entries are never recorded for real resource fetches -- performance.getEntriesByType('resource') stays [] forever, so PerformanceObserver({type:'resource'}) never fires.
# Observed via WPT-RUN-3 slice 23 (css/fetching + css/fill-stroke + css/geometry mass run).
# Re-run and flip to PASS once the bug lands.


[fetch-resources.sub.html]
  expected: TIMEOUT
  [Background images should fetch with no-cors]
    expected: TIMEOUT
  [Shape images should fetched with cors]
    expected: NOTRUN
  [WebFonts should be fetched with cors]
    expected: NOTRUN
  [CSS imports should be fetched without cors]
    expected: NOTRUN
