# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[path-function-invalid.html]
  [e.style['clip-path'\] = "path(evenodd, 'not-a-path')" should not set the property value]
    expected: FAIL
  [e.style['clip-path'\] = "path(badfillrule, 'M10,10')" should not set the property value]
    expected: FAIL
