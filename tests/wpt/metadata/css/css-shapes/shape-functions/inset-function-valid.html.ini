# BUG-484: inline style setter never parses/validates -- no rejection, no shorthand expansion, no canonicalization
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[inset-function-valid.html]
  [e.style['shape-outside'\] = "inset(0 1px)" should set the property value]
    expected: FAIL
  [e.style['shape-outside'\] = "inset(0px round 0 1px)" should set the property value]
    expected: FAIL
