# BUG-539: getComputedStyle() Proxy has no `has` trap -- `'<prop>' in getComputedStyle(el)` always false
# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[inheritance.html]
  [Property shape-image-threshold has initial value 0]
    expected: FAIL
  [Property shape-image-threshold does not inherit]
    expected: FAIL
  [Property shape-margin has initial value 0px]
    expected: FAIL
  [Property shape-margin does not inherit]
    expected: FAIL
  [Property shape-outside has initial value none]
    expected: FAIL
  [Property shape-outside does not inherit]
    expected: FAIL
