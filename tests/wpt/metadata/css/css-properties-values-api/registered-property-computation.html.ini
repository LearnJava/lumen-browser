# Known engine gap, tracked as BUG-499: getComputedStyle().getPropertyValue('--x')
# always returns '' -- computed_style_to_map never serialises custom_props, so
# reading a registered custom property's own computed value directly always
# fails regardless of what it resolved to.
# Observed via the structured wptreport (WPT-RUN-3 sl.25, css/css-properties-values-api
# mass run). Re-run and flip to PASS once BUG-499 lands.

[registered-property-computation.html]
  [<length> values computed are correctly via var()-reference]
    expected: FAIL
  [<length> values computed are correctly via var()-reference when font-size is inherited]
    expected: FAIL
  [<length> values are computed correctly when font-size is inherited [14em\]]
    expected: FAIL
  [<length> values are computed correctly when font-size is inherited [calc(14em + 10px)\]]
    expected: FAIL
  [<length> values are computed correctly [12px\]]
    expected: FAIL
