# Unattributed engine gap -- not yet root-caused to a specific BUG-NNN.
# Observed via the structured wptreport (WPT-RUN-3 sl.25, css/css-properties-values-api
# mass run, session 2026-08-03). Needs further investigation.

[registered-property-dependency-through-fallback.html]
  [registered color resolves through a var() fallback]
    expected: FAIL
  [registered color resolves through a var() fallback when a prioritary property forces early resolution]
    expected: FAIL
