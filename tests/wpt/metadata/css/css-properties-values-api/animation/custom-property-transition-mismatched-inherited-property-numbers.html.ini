# Known engine gap, tracked as BUG-346: Url::resolve() doesn't collapse
# dot-segments, so the '../resources/utils.js' reference from this file's
# directory is requested literally and 404s -- the shared animation_test()
# helper is never defined and the harness times out.
# Observed via the structured wptreport (WPT-RUN-3 sl.25, css/css-properties-values-api
# mass run). Re-run and flip to PASS once BUG-346 lands.

[custom-property-transition-mismatched-inherited-property-numbers.html]
  [Using a single "transition-property" value set to a custom property and two "transition-duration" values correctly yields a CSS Transition when the transition properties are set on a parent and the child inherits.]
    expected: FAIL
