# Known engine gap, tracked as BUG-530: Animation.pause() + currentTime =
# seeking never re-applies the interpolated style (Animation.prototype._applyAtP
# is only invoked from the RAF-driven _tick, which never runs while paused) --
# confirmed live for both a standard property and a registered custom property.
# Observed via the structured wptreport (WPT-RUN-3 sl.25, css/css-properties-values-api
# mass run). Re-run and flip to PASS once BUG-530 lands.

[custom-property-animation-non-inherited-used-by-standard-property.html]
  [Animating a non-inherited CSS variable is reflected on a standard property using that variable as a value]
    expected: FAIL
