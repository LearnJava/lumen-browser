# Known engine gap, tracked as BUG-346: Url::resolve() doesn't collapse
# '..' dot-segments -- this test's own relative image reference
# (../../images/anim-gr.gif, two levels up) always 404s, so
# :animated-image never matches and the harness times out.
# Observed via WPT-RUN-3 slice 22 (css-image-animation mass run). Re-run
# and flip to PASS once BUG-346 lands.


[image-animation-pseudo-animated-image-dynamic.html]
  expected: TIMEOUT
