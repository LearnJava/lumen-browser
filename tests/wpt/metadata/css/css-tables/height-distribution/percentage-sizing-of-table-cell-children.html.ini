# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# WPT-RUN-3 slice 40: the upstream test computes `desc` via
# `main > hr:last-of-type + p + p`, a selector scoped to the *document's*
# last <hr>, not the block-local one — so all 10 generate_tests() calls in
# this file report under the identical subtest name. wptmanifest looks up
# subtest expectations by name (dict, last-definition-wins for duplicate
# `[name]` blocks), so a single node here is checked against all 10 runtime
# occurrences; the real per-assertion outcome is a genuine mix of FAIL/PASS
# (7 FAIL, 3 PASS), hence the list form below instead of one status.
[percentage-sizing-of-table-cell-children.html]
  expected: ERROR
  [Unresolvable percentage heights are resolved as 0px in first pass (scrollable overflow)]
    expected: [FAIL, PASS]
