# Known engine gap, tracked as BUG-477: document.elementFromPoint /
# elementsFromPoint are entirely unimplemented (no point-to-node hit-test
# on the JS side).
# Observed via the structured wptreport (WPT-RUN-3 sl.8, css/css-borders
# mass run).
# Re-run and flip to PASS once BUG-477 lands.

[border-shape-stacking-context-hit-test.html]
  [point inside child bounds but outside circle does not hit child or target]
    expected: FAIL
  [bottom-right corner outside circle does not hit target]
    expected: FAIL
  [point inside circle and inside child hits child]
    expected: FAIL
  [point inside circle but outside child hits target]
    expected: FAIL
