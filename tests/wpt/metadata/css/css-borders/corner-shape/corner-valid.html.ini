# Known engine gap, tracked as BUG-484: the inline style setter
# (_lumen_make_style, crates/js/src/dom.rs:4264) never routes through the
# real CSS parser -- it stores and echoes back the raw string
# unconditionally, for any property, so invalid values are never rejected
# and valid values are never canonicalized/decomposed into longhands.
# Observed via the structured wptreport (WPT-RUN-3 sl.8, css/css-borders
# mass run).
# Re-run and flip to PASS once BUG-484 lands.

[corner-valid.html]
  [e.style['corner'\] = "round 30%" should set the property value]
    expected: FAIL
  [e.style['corner'\] = "round 10% / 2rem squircle" should set the property value]
    expected: FAIL
  [e.style['corner'\] = "notch 10px / squircle 30% 10px / 1rem bevel / 2px round" should set the property value]
    expected: FAIL
  [e.style['corner'\] = "4px 2% round / bevel 1em / normal" should set the property value]
    expected: FAIL
  [e.style['corner'\] = "normal / superellipse(-0.5) 30% 10px / 10px 1rem notch" should set the property value]
    expected: FAIL
