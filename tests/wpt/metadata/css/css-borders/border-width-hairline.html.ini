# Known engine gap, tracked as BUG-384: named access on Window for
# elements with an id attribute is not implemented -- a bare identifier
# referencing an #id element (e.g. `t` for <div id="t">) is a
# ReferenceError instead of resolving to window.t.
# Observed via the structured wptreport (WPT-RUN-3 sl.8, css/css-borders
# mass run).
# Re-run and flip to PASS once BUG-384 lands.

[border-width-hairline.html]
  [Computed hairline width is within bounds]
    expected: FAIL
