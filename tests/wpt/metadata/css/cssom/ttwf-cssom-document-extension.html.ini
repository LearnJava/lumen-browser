# Genuine engine gap, tracked as BUG-471: the CSSOM stylesheet/rule object
# model (`document.styleSheets`, `<style>`/`<link>.sheet`, `CSSStyleSheet`,
# the `CSSRule` hierarchy, `insertRule`/`deleteRule`, `MediaList`) is not
# wired into the JS shim at all (`crates/js/src/dom.rs`) -- not a broken
# getter, the symbols don't exist. Observed end to end by wptrunner
# (WPT-RUN-3 sl.3, css/cssom mass run). Re-run and flip to PASS once BUG-471
# lands.
[ttwf-cssom-document-extension.html]
  [CSSOM - Extensions to the Document Interface: StyleSheetList length is 0 when no sheets loaded]
    expected: FAIL
