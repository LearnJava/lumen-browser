# Genuine engine gap, tracked as BUG-384: named access on the Window
# object (HTML LS section 7.3.3) is not implemented -- a bare identifier
# matching an element's `id` (e.g. `container`) throws `ReferenceError`
# instead of resolving to that element. Observed end to end by wptrunner
# (WPT-RUN-3 sl.3, css/cssom mass run). Re-run and flip to PASS once
# BUG-384 lands.
[caretPositionFromPoint-audioVideo.html]
  [document.caretPositionFromPoint() should return a CaretPosition over audio elements]
    expected: FAIL
  [document.caretPositionFromPoint() should return a CaretPosition over video elements]
    expected: FAIL
