# Genuine engine gap, tracked as BUG-462: `Node.prototype.contains` is
# missing entirely on live (attached) DOM nodes. Observed end to end by
# wptrunner (WPT-RUN-3 sl.3, css/cssom mass run). Re-run and flip to PASS
# once BUG-462 lands.
[caretPositionFromPoint-in-flex-container.html]
  [caretPositionFromPoint just inside a tall flex child resolves to that child]
    expected: FAIL
  [caretPositionFromPoint well inside a tall flex child resolves to that child]
    expected: FAIL
  [caretPositionFromPoint inside the short flex child resolves correctly]
    expected: FAIL
