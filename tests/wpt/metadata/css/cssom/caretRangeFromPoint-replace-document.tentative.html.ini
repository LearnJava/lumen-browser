# Fails before caretRangeFromPoint is even reached: `document.replaceChild`
# is not implemented on the live document (BUG-474's own note flagged this
# as a distinct, not-yet-filed gap -- "не кластеризуемое по одному
# наблюдению"). Left as a bare baseline; not part of BUG-474's fix.
[caretRangeFromPoint-replace-document.tentative.html]
  [document.caretRangeFromPoint(0, 0)]
    expected: FAIL
