# Known engine gap, tracked as BUG-472: computed_style_to_map doesn't
# expose this property at all -- getComputedStyle() never surfaces it.
# Observed via WPT-RUN-3 slice 24 (css-compositing mass run).

[isolation-computed.html]
  [Property isolation value 'auto']
    expected: FAIL
  [Property isolation value 'isolate']
    expected: FAIL
