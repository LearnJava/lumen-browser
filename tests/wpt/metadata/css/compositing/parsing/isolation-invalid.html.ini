# Known engine gap, tracked as BUG-484: the inline style setter doesn't
# validate/canonicalize values -- invalid values are stored verbatim
# instead of being rejected.
# Observed via WPT-RUN-3 slice 24 (css-compositing mass run).

[isolation-invalid.html]
  [e.style['isolation'\] = "none" should not set the property value]
    expected: FAIL
  [e.style['isolation'\] = "auto isolate" should not set the property value]
    expected: FAIL
