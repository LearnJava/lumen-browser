# Known engine gap, tracked as BUG-539: the getComputedStyle() Proxy has no
# `has` trap, so `property in getComputedStyle(el)` is unconditionally
# false for every property, implemented or not -- computed-testcommon.js's
# feature-detect fails before the real value comparison ever runs.
# Observed via the structured wptreport (WPT-RUN-3 sl.28, css/css-lists mass
# run). Re-run and flip to PASS once BUG-539 lands (properties genuinely
# unimplemented may still need their own fix after that -- see BUGS.md).

[inheritance.html]
  [Property counter-increment has initial value none]
    expected: FAIL
  [Property counter-increment does not inherit]
    expected: FAIL
  [Property counter-reset has initial value none]
    expected: FAIL
  [Property counter-reset does not inherit]
    expected: FAIL
  [Property list-style-image has initial value none]
    expected: FAIL
  [Property list-style-image inherits]
    expected: FAIL
  [Property list-style-position has initial value outside]
    expected: FAIL
  [Property list-style-position inherits]
    expected: FAIL
  [Property list-style-type has initial value disc]
    expected: FAIL
  [Property list-style-type inherits]
    expected: FAIL
