# Known engine gap, tracked as BUG-539: the getComputedStyle() Proxy has no
# `has` trap, so `property in getComputedStyle(el)` is unconditionally
# false for every property, implemented or not -- computed-testcommon.js's
# feature-detect fails before the real value comparison ever runs.
# Observed via the structured wptreport (WPT-RUN-3 sl.28, css/css-lists mass
# run). Re-run and flip to PASS once BUG-539 lands (properties genuinely
# unimplemented may still need their own fix after that -- see BUGS.md).

[list-style-image-computed.sub.html]
  [Property list-style-image value 'none']
    expected: FAIL
  [Property list-style-image value 'url("https://127.0.0.1/")']
    expected: FAIL
  [Property list-style-image value 'linear-gradient(to left bottom, red , blue )']
    expected: FAIL
  [Property list-style-image value 'linear-gradient(calc(90deg - 45deg), rgb(0, 128, 0), rgb(0, 0, 255)']
    expected: FAIL
  [Property list-style-image value 'radial-gradient(10px at 20px 30px, rgb(255, 0, 0), rgb(0, 0, 255))']
    expected: FAIL
  [Property list-style-image value 'radial-gradient(circle calc(-0.5em + 10px) at calc(-1em + 10px) calc(-2em + 10px), rgb(255, 0, 0), rgb(0, 0, 255))']
    expected: FAIL
  [Property list-style-image value 'radial-gradient(ellipse calc(-0.5em + 10px) calc(0.5em + 10px) at 20px 30px, rgb(255, 0, 0), rgb(0, 0, 255))']
    expected: FAIL
  [Property list-style-image value 'radial-gradient(ellipse calc(0.5em + 10px) calc(-0.5em + 10px) at 20px 30px, rgb(255, 0, 0), rgb(0, 0, 255))']
    expected: FAIL
  [Property list-style-image value 'radial-gradient(black 0% 0.5em, 25%, green 30% 60%, calc(100% * 3 / 4), white calc(100% - 20%) 100%']
    expected: FAIL
  [Property list-style-image value 'conic-gradient(gold calc(100% / 2), #f06 calc(360deg * 4 / 5))']
    expected: FAIL
  [Property list-style-image value 'conic-gradient(black 1turn, white']
    expected: FAIL
