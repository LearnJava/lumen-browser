# Unexplained outside the wptrunner/BiDi execution pipeline: both
# --dump-layout and a live --mcp-live-port probe of the same markup compute
# the geometry this assertion expects (verified by hand, WPT-RUN-3 sl.28),
# so this does not look like a real layout/margin-collapse defect -- more
# likely a wptrunner-side execution-timing artifact in the same class as
# BUG-300/BUG-380 (browsingContext.navigate / stale-result races). Not
# filed as a new engine bug; re-run and re-investigate if it keeps
# recurring across future slices.

[list-and-margin-collapse-001.html]
  [list and margin collapse]
    expected: FAIL
