# Known engine gap, tracked as BUG-537: getComputedStyle().getPropertyValue()
# has no whitelist entry for background-image (or most other background-*
# longhands), so the computed value never reflects a set gradient --
# always "". Observed via the structured wptreport (WPT-RUN-3 sl.28,
# css/css-lists mass run, gradient/color-stops-parsing.html [ parsable ]
# cases). Re-run and flip to PASS once BUG-537 lands.

[li-counter-increment-computed-style.html]
  [list-item counter-increment shouldn't be visible from computed style]
    expected: FAIL
