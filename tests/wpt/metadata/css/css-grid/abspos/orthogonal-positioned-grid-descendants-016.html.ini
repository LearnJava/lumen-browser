# BUG-446: <script type=module> import of a same-origin module never resolves -- module graph never loads
# WPT-RUN-3 slice 32 mass run (structured wptreport JSON, css-grid).
# Re-run and flip to PASS once the cited bug(s) land.

[orthogonal-positioned-grid-descendants-016.html]
  expected: TIMEOUT
