# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 32 mass run (structured wptreport JSON, css-grid).
# Re-run and flip to PASS once the cited bug(s) land.

[row-subgrid-auto-repeat-auto-001.html]
  [CSS Grid Lanes Test: intrinsic auto-repeat rows sized by a subgrid item]
    expected: FAIL
