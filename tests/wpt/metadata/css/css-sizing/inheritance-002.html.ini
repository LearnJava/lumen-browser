# Known engine gaps: BUG-539 (getComputedStyle Proxy has no `has` trap -- inheritance-testcommon.js's unmessaged assert_true(prop in getComputedStyle(...)) fails first).
# WPT-RUN-3 slice 33 mass run (structured wptreport JSON), css/css-sizing.
# Re-run and flip to PASS once the cited bug(s) land.

[inheritance-002.html]
  [Property height has initial value 0px]
    expected: FAIL
  [Property height does not inherit]
    expected: FAIL
  [Property width has initial value 0px]
    expected: FAIL
  [Property width does not inherit]
    expected: FAIL
