# Known engine gaps: BUG-493 (no DOM geometry/computed-style accessor forces a synchronous layout flush before reading (same-tick mutate-then-read or static-markup-then-script)).
# WPT-RUN-3 slice 33 mass run (structured wptreport JSON), css/css-sizing.
# Re-run and flip to PASS once the cited bug(s) land.

[abspos-intrinsic-height-inset-percentage-child.html]
  [fit-content + inset:0: child height:100% treated as auto, sizes to content]
    expected: FAIL
  [max-content + inset:0: child height:100% treated as auto, sizes to content]
    expected: FAIL
  [min-content + inset:0: child height:100% treated as auto, sizes to content]
    expected: FAIL
  [fit-content + top only: child height:100% treated as auto (sanity check)]
    expected: FAIL
  [height:auto + inset:0: constraint equation provides definite height]
    expected: FAIL
  [height:stretch + inset:0: stretch provides definite height (regression check)]
    expected: FAIL
