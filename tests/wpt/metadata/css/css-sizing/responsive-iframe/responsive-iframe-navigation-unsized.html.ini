# Known engine gaps: BUG-480 (<iframe> is a placeholder with no browsing context, postMessage/contentWindow never resolve).
# WPT-RUN-3 slice 33 mass run (structured wptreport JSON), css/css-sizing.
# Re-run and flip to PASS once the cited bug(s) land.

[responsive-iframe-navigation-unsized.html?origin=cross]
  expected: [OK, TIMEOUT]
  [Responsive iframe clears size during navigation to non-responsive page]
    expected: [PASS, TIMEOUT]
[responsive-iframe-navigation-unsized.html?origin=same]
  expected: [OK, TIMEOUT]
  [Responsive iframe clears size during navigation to non-responsive page]
    expected: [PASS, TIMEOUT]
