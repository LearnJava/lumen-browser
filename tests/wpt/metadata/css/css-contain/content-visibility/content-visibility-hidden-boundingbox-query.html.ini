# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[content-visibility-hidden-boundingbox-query.html]
  expected: TIMEOUT
  [content-visibility hidden content returns uptodate geometry with sibling content requiring layout]
    expected: TIMEOUT
