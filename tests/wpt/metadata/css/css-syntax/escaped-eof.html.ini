# Known engine gap, tracked as BUG-391: querySelector()/matches()/closest()
# never throw a SyntaxError for a selector that parses per the raw grammar
# but is structurally invalid per a more specific rule -- here, a numeric
# hash token (`#123`) is an "unrestricted" hash token, not the "id"-type
# hash token required for a valid <id-selector>, and should be rejected.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-391 lands.

# Known engine gap, tracked as BUG-471: the CSSOM stylesheet/rule object
# model is not implemented at all -- document.styleSheets is undefined
# (not an empty array), <style>/<link>.sheet is undefined, and the global
# CSSStyleSheet/CSSRule/CSSStyleRule/... constructors don't exist. Any test
# that reaches into document.styleSheets[n].cssRules (or an element's
# .sheet) throws "Cannot read properties of undefined".
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-471 lands.

[escaped-eof.html]
  [Escaped EOF turns into a U+FFFD in a hash token, makes it 'ID' type.]
    expected: FAIL
  [Escaped EOF turns into a U+FFFD in an ident token.]
    expected: FAIL
  [Escaped EOF turns into a U+FFFD in a dimension token.]
    expected: FAIL
  [Escaped EOF turns into a U+FFFD in a url token.]
    expected: FAIL
  [Escaped EOF in a string is ignored.]
    expected: FAIL
