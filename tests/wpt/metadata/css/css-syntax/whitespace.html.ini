# Known engine gap, tracked as BUG-510: the CSS tokenizer's whitespace skip
# (skip_ws_and_comments) uses Rust's char::is_whitespace() (Unicode
# White_Space property) instead of the CSS Syntax spec's fixed 5-character
# definition (TAB/LF/FF/CR/SPACE) -- U+000B VERTICAL TAB and U+0085 NEXT LINE
# are incorrectly treated as a descendant-combinator separator in selectors.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-510 lands.

[whitespace.html]
  [U+000b is *not* CSS whitespace]
    expected: FAIL
  [U+0085 is *not* CSS whitespace]
    expected: FAIL
