# BUG-509 fixed the "determine the fallback encoding" precedence algorithm
# (BOM > HTTP Content-Type charset > leading @charset > <link charset> >
# referring document > UTF-8). The @charset value here is "bogus" (invalid,
# correctly falls through), so the expected decode is the <link charset>
# tier's windows-1250 -- lumen-encoding does not carry that table
# (deliberate scope per docs/plan/tech-stack.md section 5 /
# subsystems/encoding.md, not a Phase-0 stub). Falls through further to
# windows-1251 (this page's own encoding) and hides the wrong element.
# Residual, not re-broken by anything else.
[page-windows-1251-css-at-charset-bogus-charset-attribute-windows-1250.html]
  [CSS charset: page windows-1251, CSS @charset bogus, charset attribute windows-1250]
    expected: FAIL
