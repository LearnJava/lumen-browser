# Known engine gap, tracked as BUG-509: external stylesheet fetch
# (fetch_stylesheet_text, crates/shell/src/main.rs) always decodes the
# response body as UTF-8 lossy -- none of BOM sniffing, a leading
# @charset rule, the HTTP Content-Type charset parameter, a <link
# charset> attribute, or falling back to the referring document's own
# encoding is implemented. Bytes meant to be read under a different
# encoding decode to the wrong codepoints, so an id selector built from
# them never matches its intended (correctly-encoded) DOM id.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-509 lands.

[page-windows-1251-css-http-bogus.html]
  [CSS charset: page windows-1251, CSS HTTP bogus]
    expected: FAIL
