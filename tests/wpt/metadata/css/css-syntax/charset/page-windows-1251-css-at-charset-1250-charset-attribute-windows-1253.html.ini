# BUG-509 fixed the "determine the fallback encoding" precedence algorithm
# (BOM > HTTP Content-Type charset > leading @charset > <link charset> >
# referring document > UTF-8), but this subtest's expected decode is
# windows-1250 (the @charset value, highest tier with no HTTP override
# here) -- lumen-encoding does not carry a windows-1250 table (deliberate
# scope per docs/plan/tech-stack.md section 5 / subsystems/encoding.md, not a
# Phase-0 stub). Falls through to windows-1251 (this page's own encoding)
# and hides the wrong element. Residual, not re-broken by anything else.
[page-windows-1251-css-at-charset-1250-charset-attribute-windows-1253.html]
  [CSS charset: page windows-1251, @charset windows-1250, charset attribute windows-1253]
    expected: FAIL
