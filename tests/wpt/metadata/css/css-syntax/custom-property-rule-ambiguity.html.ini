# Known engine gap, tracked as BUG-384: elements with an `id` attribute are
# not exposed as named properties on `window` (HTML's "named access on the
# Window object"). Tests here reference a <style id=foo> element as the bare
# identifier `foo` and get a ReferenceError before ever reaching the
# CSSOM gap tracked separately as BUG-471 (`foo.sheet...`).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-384 lands.

[custom-property-rule-ambiguity.html]
  [Rule that looks like a custom property declaration is ignored]
    expected: FAIL
  [Rule that looks like an invalid custom property declaration is ignored]
    expected: FAIL
  [Nested rule that looks like a custom property declaration]
    expected: FAIL
  [Nested rule that looks like an invalid custom property declaration]
    expected: FAIL
