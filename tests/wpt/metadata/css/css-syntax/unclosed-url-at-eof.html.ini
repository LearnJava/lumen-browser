# Known engine gap, tracked as BUG-484: the inline `element.style` setter
# path does not canonicalise values before storing/serialising them -- an
# unclosed `url(...` token at EOF (recovered per the CSS Syntax spec, and
# meant to serialise identically to the well-formed `url(...)` form) is
# instead passed through with the missing closing paren intact.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.18, css/css-syntax
# mass run). Re-run and flip to PASS once BUG-484 lands.

[unclosed-url-at-eof.html]
  [Unclosed url token at EOF is valid.]
    expected: FAIL
  [Unclosed empty url token at EOF is valid.]
    expected: FAIL
