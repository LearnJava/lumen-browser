# Known engine gap, tracked as BUG-467 (CSS Font Loading API practically
# unimplemented -- see empty-family-load.html.ini for the full writeup).
# `document.fonts.keys` is not a function -- the shim's document.fonts
# snapshot only has length/item/entries/forEach/Symbol.iterator, none of
# the FontFaceSet Set-like mutation surface (add/delete/clear/has/keys).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.17, css/css-font-
# loading mass run). Re-run and flip to PASS once BUG-467 lands.

[fontfaceset-clear-css-connected.html]
  [fontfaceset-clear-css-connected]
    expected: FAIL
