# Known engine gap, tracked as BUG-467 (CSS Font Loading API practically
# unimplemented). `document.fonts.size` is undefined -- the shim's
# document.fonts snapshot only exposes `length`, not the spec's `size`.
# Whether an unreachable file:// @font-face source is still registered
# (per spec, load failure doesn't remove the CSS-connected FontFace) is
# not verified by this run -- the test fails on the `size` read itself.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.17, css/css-font-
# loading mass run). Re-run and flip to PASS once BUG-467 lands.

[nonexistent-file-url.html]
  [nonexistent-file-url]
    expected: FAIL
