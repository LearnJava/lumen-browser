# Known executor artifact, tracked as BUG-380: the browsing-context is
# reused across the whole run and the executor doesn't verify navigation
# outcome, so a failed/slow navigation into this test can surface the
# PREVIOUS test's (historical.html) results instead of its own --
# `AssertionError: Got results from /css/css-font-loading/historical.html,
# expected /css/css-font-loading/idlharness.https.html`. Not a css-font-
# loading-specific symptom.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.17, css/css-font-
# loading mass run). Re-run and flip to PASS/adjust once BUG-380 lands.

[idlharness.https.html]
  expected: ERROR
