# Known engine gap, tracked as BUG-467 (CSS Font Loading API practically
# unimplemented -- see empty-family-load.html.ini for the full writeup).
# `FontFaceSet` is not defined globally at all, so `new FontFaceSet([])`
# throws `ReferenceError` instead of the spec-required `TypeError`
# ("illegal constructor") -- confirms the API isn't modeled even as an
# intentionally-non-constructible interface.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.17, css/css-font-
# loading mass run). Re-run and flip to PASS once BUG-467 lands.

[historical.html]
  [FontFaceSet constructor should not be supported]
    expected: FAIL
