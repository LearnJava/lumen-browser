# Known engine gap, tracked as BUG-467 (CSS Font Loading API practically
# unimplemented -- see fontfaceset-load-css-wide-keywords.html.ini for
# the same TIMEOUT-vs-NOTRUN mechanics explanation). `document.fonts.load`
# is not a function.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.17, css/css-font-
# loading mass run). Re-run and flip to PASS once BUG-467 lands.

[fontfaceset-load-var.html]
  expected: TIMEOUT

  [Loading "var(--x) serif" causes SyntaxError (document)]
    expected: FAIL

  [Loading "var(--x, 10px) serif" causes SyntaxError (document)]
    expected: FAIL

  [Loading "var(--x) serif" causes SyntaxError (worker)]
    expected: TIMEOUT

  [Loading "var(--x, 10px) serif" causes SyntaxError (worker)]
    expected: NOTRUN
