# Known engine gap, tracked as BUG-472: computed_style_to_map doesn't
# expose this property at all -- getComputedStyle() never surfaces it.
# Observed via WPT-RUN-3 slice 24 (css-scrollbars mass run).

[inheritance.html]
  [Property scrollbar-color has initial value auto]
    expected: FAIL
  [Property scrollbar-color inherits]
    expected: FAIL
  [Property scrollbar-width has initial value auto]
    expected: FAIL
  [Property scrollbar-width does not inherit]
    expected: FAIL
