# Known engine gap, tracked as BUG-360: <body onload=...> is dead -- the
# live event dispatch only checks addEventListener-registered handlers,
# so setup({explicit_done:true}) + onload="performTest()" never calls
# performTest() and the harness hangs until wptrunner's own timeout.
# Observed via WPT-RUN-3 slice 24 (css-scrollbars mass run).

[scrollbar-width-004.html]
  expected: TIMEOUT
