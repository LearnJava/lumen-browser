# Known engine gap, tracked as BUG-529: window.innerWidth/innerHeight/
# outerWidth/outerHeight do not exist at all -- the test's own
# 'does the viewport have a scrollbar' check compares offsetWidth
# against innerWidth and gets undefined on one side.
# Observed via WPT-RUN-3 slice 24 (css-scrollbars mass run).

[scrollbar-width-007.html]
  [viewport does not display a scrollbar]
    expected: FAIL
