# Known engine gap, tracked as BUG-359: window.open() with a relative URL
# doesn't resolve against the document base -- goes to the network as-is and
# fails with "invalid url: missing scheme". Harness never starts (executor-
# level TIMEOUT), zero subtests registered.
# Observed via WPT-RUN-3 slice 24 (css-scroll-anchoring mass run).

[history-restore-anchors.html]
  expected: TIMEOUT
