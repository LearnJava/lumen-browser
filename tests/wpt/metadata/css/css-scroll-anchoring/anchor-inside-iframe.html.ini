# Known engine gap, tracked as BUG-480: <iframe> has no separate browsing
# context -- contentWindow/contentDocument are absent from the shim.
# Observed via WPT-RUN-3 slice 24 (css-scroll-anchoring mass run).

[anchor-inside-iframe.html]
  [Scroll anchoring in an iframe.]
    expected: FAIL
