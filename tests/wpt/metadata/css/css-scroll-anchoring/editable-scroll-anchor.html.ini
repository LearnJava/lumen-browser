# Known engine gap, tracked as BUG-384: named access on Window (bare-id) is
# not implemented -- a bare identifier referencing an element by its id
# throws ReferenceError instead of resolving.
# Known engine gap, tracked as BUG-413: HTMLElement.innerText/outerText do
# not exist at all (neither getter nor setter).
# Observed via WPT-RUN-3 slice 24 (css-scroll-anchoring mass run).

[editable-scroll-anchor.html]
  [Scroll anchoring is suppressed during editing in a textarea]
    expected: FAIL
  [Scroll anchoring is suppressed during editing in a contenteditable element]
    expected: FAIL
