# Known engine gap, tracked as BUG-524: CSS Scroll Anchoring
# (`overflow-anchor`) is entirely unimplemented -- property not parsed,
# no anchor-selection/adjustment logic anywhere in layout.
# Observed via WPT-RUN-3 slice 24 (css-scroll-anchoring mass run).

[position-change-heuristic-display-none-change.html]
  expected: TIMEOUT

  [Scroll offset doesn't change when an element goes back and forth to display: none]
    expected: TIMEOUT
