# Known engine gap, tracked as BUG-523: Element.scrollTop/scrollLeft setter
# is queued and applied asynchronously by the shell -- a synchronous read
# right after the write sees the stale (pre-write) value.
# Known engine gap, tracked as BUG-525: document.scrollingElement is not
# implemented -- always undefined.
# Observed via WPT-RUN-3 slice 24 (css-scroll-anchoring mass run).

[position-change-heuristic-in-nested-scroll-box.html]
  [Position changes in document scroller.]
    expected: FAIL
  [Position changes in scrollable <div>.]
    expected: FAIL
