# BUG-384: named access on Window is missing (`id="..."` gives no bare
# global and no `window.<id>`). The script's own top-level (or, for
# module/event-handler-body variants, its first executed statement)
# throws a synchronous ReferenceError referencing the bare id before any
# test() registers, so testharness.js has 0 tests and only completes via
# its own default ~10s file-level timeout. WPT-RUN-3 slice 43
# (ROADMAP.md) individually attributed this file's TIMEOUT by direct
# --mcp-live-port probe (bugs/BUG-384-OPEN.md, section "Srez 43").
# Re-run and flip to PASS once the cited bug lands.

[position-area-fixed.html]
  expected: TIMEOUT
