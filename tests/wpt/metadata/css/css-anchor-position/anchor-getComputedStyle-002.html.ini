# BUG-564: `document.fonts.ready` is missing entirely (FontFaceSet has
# no `ready` property) -- `document.fonts.ready.then(...)` throws
# `Cannot read properties of undefined (reading 'then')`, preventing
# done()/further test() registration from ever running. WPT-RUN-3
# slice 43 (ROADMAP.md) individually attributed this file's TIMEOUT by
# direct --mcp-live-port probe (bugs/BUG-564-OPEN.md).
# Re-run and flip to PASS once the cited bug lands.

[anchor-getComputedStyle-002.html]
  expected: TIMEOUT
  [getComputedStyle() with fragmented containing block in multicolumn layout]
    expected: FAIL
