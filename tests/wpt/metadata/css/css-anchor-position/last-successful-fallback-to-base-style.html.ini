# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[last-successful-fallback-to-base-style.html]
  [Starts rendering with flip-inline fallback]
    expected: FAIL
  [Base position without fallback now successful]
    expected: FAIL
  [Both base position and flip-inline works, keep base position since it's the last successful option]
    expected: FAIL
