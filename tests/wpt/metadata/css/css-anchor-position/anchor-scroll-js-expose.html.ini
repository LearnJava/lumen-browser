# BUG-384: named access on Window (id=... bare identifier) never installed -- ReferenceError
# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[anchor-scroll-js-expose.html]
  expected: ERROR
  [Element.getBoundingClientRect() returns the actual rendered location]
    expected: NOTRUN
  [Range.getBoundingClientRect() returns the actual rendered location]
    expected: NOTRUN
  [Element.offset* return adjusted offsets]
    expected: NOTRUN
  [IntersectionObserver should fire when anchored element is moved into visible area]
    expected: NOTRUN
