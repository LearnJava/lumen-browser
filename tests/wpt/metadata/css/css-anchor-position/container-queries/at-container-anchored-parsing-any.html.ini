# BUG-562: `position-try`/`position-try-fallbacks`/`position-try-order`/
# `@position-try` (CSSPositionTryRule) are not implemented at all; scripts
# that touch them abort synchronously, leaving later `var`/`let` bindings
# unset (ReferenceError) or subtests entirely unreached (NOTRUN).
# WPT-RUN-3 slice 40 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[at-container-anchored-parsing-any.html]
  [Query condition should be valid: anchored(fallback: any)]
    expected: FAIL
  [Query condition should be valid: anchored(fallback: any any)]
    expected: FAIL
  [Query condition should be valid: anchored(fallback: any top)]
    expected: FAIL
  [Query condition should be valid: anchored(fallback: center any)]
    expected: FAIL
  [Query condition should be valid: anchored(fallback: any self-start)]
    expected: FAIL
  [Query condition should be valid: anchored(fallback: inline-end any)]
    expected: FAIL
  [Query condition should be valid but unknown: anchored(fallback: any none)]
    expected: FAIL
