# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[anchored-fallback-position-area-wm.html]
  [@container anchored(fallback) with 'self-block-end self-inline-start' fallback applied]
    expected: FAIL
  [@container anchored(fallback) with 'block-end inline-end' fallback applied]
    expected: FAIL
