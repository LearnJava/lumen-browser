# BUG-539: getComputedStyle() Proxy has no `has` trap -- `'<prop>' in getComputedStyle(el)` always false
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[container-type-anchored-computed.html]
  [Property container-type value 'anchored']
    expected: FAIL
  [Property container-type value 'anchored size']
    expected: FAIL
  [Property container-type value 'inline-size anchored']
    expected: FAIL
