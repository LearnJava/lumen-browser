# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[anchor-getComputedStyle-004.html]
  [position-area does not affect resolved inset properties]
    expected: FAIL
  ['auto' inset properties resolve to 0px when position-area is non-initial]
    expected: FAIL
