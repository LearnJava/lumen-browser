# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[CSSPseudoElement-view-transitions-scoped.tentative.html]
  [CSSPseudoElement hierarchy works for scoped view transitions]
    expected: FAIL
  [CSSPseudoElement proxies are identity-consistent across multiple view transitions on the same element]
    expected: FAIL
