# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="div" gives neither window.div
# nor a bare `div` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[column-creates-computed-style.html]
  [::column creates computed style on its originating element]
    expected: FAIL
