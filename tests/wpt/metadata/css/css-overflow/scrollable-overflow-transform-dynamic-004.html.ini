# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="container1" gives neither window.container1
# nor a bare `container1` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[scrollable-overflow-transform-dynamic-004.html]
  [Check scrollWidth before and after position and transform chage]
    expected: FAIL
  [Check scrollHeight before and after position and transform chage]
    expected: FAIL
  [Check scrollWidth and scrollHeight after position and transform chage]
    expected: FAIL
