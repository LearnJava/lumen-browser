# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="scroller" gives neither window.scroller
# nor a bare `scroller` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[scroll-marker-group-size-container-query.html]
  [::scroll-marker-group skips originating element for size queries]
    expected: FAIL
