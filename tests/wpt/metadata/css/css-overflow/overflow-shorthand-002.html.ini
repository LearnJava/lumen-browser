# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="horizontal" gives neither window.horizontal
# nor a bare `horizontal` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[overflow-shorthand-002.html]
  [overflow - horizontal writing mode]
    expected: FAIL
  [overflow - vertical writing mode]
    expected: FAIL
