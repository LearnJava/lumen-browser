# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="target" gives neither window.target
# nor a bare `target` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[column-updates-computed-style.html]
  [::column updates computed style on its originating element]
    expected: FAIL
