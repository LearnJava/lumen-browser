# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with id="ref" gives neither window.ref
# nor a bare `ref` identifier inside inline <script>.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-384 lands.

[auto-scrollbar-inline-children.html]
  [Auto scrollbar affects size of children]
    expected: FAIL
