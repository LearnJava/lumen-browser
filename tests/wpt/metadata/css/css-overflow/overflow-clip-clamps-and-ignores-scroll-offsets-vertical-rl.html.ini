# Was tracked as BUG-504 (content_width()/content_height() ignoring
# transform/abspos/clip-margin/RTL in scrollWidth/scrollHeight) — that class
# of defect is fixed (BUG-504 parts 1-11). Root cause is now BUG-977: a
# script that flips `overflow` to `clip` and synchronously scrolls/reads the
# same element in the SAME turn doesn't see the clip applied in the
# interactive shell, because `_lumen_request_scroll`'s clip check reads
# `computed_styles`, which the live window only refreshes asynchronously
# after a real relayout (no live-shell equivalent of CSSOM-4's synchronous
# flush exists yet). BUG-975 part 2 (2026-09-04) fixed a related but
# distinct gap in the same repro (first-line-of-script scroll request before
# any flush) — this file's remaining failure is BUG-977, not BUG-975.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-977 lands.

[overflow-clip-clamps-and-ignores-scroll-offsets-vertical-rl.html]
  [changing a resizable vertical-rl scroller from overflow:hidden to overflow:clip clamps existing offsets and makes future scroll writes no-ops]
    expected: FAIL
