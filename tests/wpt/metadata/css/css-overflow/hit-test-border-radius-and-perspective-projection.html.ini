# Known engine gap, tracked as BUG-477: document.elementFromPoint /
# elementsFromPoint are not implemented at all (no point-based hit-test API
# on the JS side).
#
# Observed via the structured wptreport (WPT-RUN-3 sl.11, css/css-overflow
# mass run). Re-run and flip to PASS once BUG-477 lands.

[hit-test-border-radius-and-perspective-projection.html]
  [Near-Z element projected outside border-radius is clipped]
    expected: FAIL
  [Corner point outside border-radius hits body after perspective]
    expected: FAIL
