# Known engine gap, tracked as BUG-511: `link-parameters` (CSS Linked
# Parameters L1) is not implemented at all -- the property is absent from
# ComputedStyle and getComputedStyle() reports it unsupported.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[link-parameters-computed.html]
  [Property link-parameters value 'none']
    expected: FAIL
  [Property link-parameters value 'param(--a, orange)']
    expected: FAIL
  [Property link-parameters value 'param(--a, orange), param(--b, blue)']
    expected: FAIL
  [Property link-parameters value 'param(--a, )']
    expected: FAIL
  [Property link-parameters value 'param(--a, ), param(--b, )']
    expected: FAIL
