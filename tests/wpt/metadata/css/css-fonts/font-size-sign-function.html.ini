# BUG-539: getComputedStyle Proxy has no `has` trap -- 'prop' in getComputedStyle(el) always false
# WPT-RUN-3 slice 31 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[font-size-sign-function.html]
  [Property font-size value 'calc(sign(1rem - 1px) * 100%)']
    expected: FAIL
