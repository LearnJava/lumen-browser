# Known engine gap, tracked as BUG-384: named access on Window (<div id=x>
# -> window.x) is not implemented -- bare identifier `container` referenced
# at top level, before any test() registers, throws ReferenceError ->
# TIMEOUT. Observed via the structured wptreport (WPT-RUN-3 sl.26,
# css/css-ruby mass run). Re-run and flip to PASS once BUG-384 lands.

[line-break-around-ruby-001.html]
  expected: TIMEOUT
