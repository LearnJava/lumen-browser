# Known engine gap, tracked as BUG-493: getComputedStyle()/offsetWidth/
# offsetHeight read synchronously in a top-level <script> immediately after
# static markup always see a stale/zero same-tick cache -- a forced layout
# pass (e.g. `lumen --dump-layout`) shows the real, non-zero box. Not a
# float/clear layout bug. Observed via the structured wptreport (WPT-RUN-3
# sl.26, css/css-ruby mass run). Re-run and flip to PASS once BUG-493
# lands.

[br-clear-all-001.html]
  [#container 1]
    expected: FAIL
