# Known engine gap, tracked as BUG-535: `ruby-position: alternate` has no
# layout effect -- stacked <rtc> annotation containers never flip
# over/under across siblings (confirmed via `lumen --dump-layout`: no
# alternation logic keyed off sibling <rtc> position). Observed via the
# structured wptreport (WPT-RUN-3 sl.26, css/css-ruby mass run). Re-run and
# flip to PASS once BUG-535 lands.

[ruby-position-alternate.html]
  [ruby-position "" alternate from over]
    expected: FAIL
  [ruby-position "alternate" alternate from over]
    expected: FAIL
  [ruby-position "alternate over" alternate from over]
    expected: FAIL
  [ruby-position "over alternate" alternate from over]
    expected: FAIL
  [ruby-position "alternate under" alternate from under]
    expected: FAIL
  [ruby-position "under alternate" alternate from under]
    expected: FAIL
  [ruby-position alternate from previous ruby text container]
    expected: FAIL
