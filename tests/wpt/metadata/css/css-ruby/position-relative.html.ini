# Known engine gap, tracked as BUG-368: innerHTML is a Phase-0 text-only
# stub on both engines -- the setter never parses HTML fragments at all
# (confirmed live: `div.innerHTML = '<span>x</span>'` produces a single
# #text child, no <span> element, on a plain div -- not ruby-specific).
# Observed via the structured wptreport (WPT-RUN-3 sl.26, css/css-ruby mass
# run). Re-run and flip to PASS once BUG-368 lands.

[position-relative.html]
  [position:relative on a ruby should move the annotation together]
    expected: FAIL
  [position:relative on a ruby parent should move the ruby and the annotation]
    expected: FAIL
  [Accumulate position:relative offsets]
    expected: FAIL
