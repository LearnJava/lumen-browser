# Known engine gap, tracked as BUG-467: CSS Font Loading API is practically
# unimplemented -- document.fonts.load() does not exist, thrown at top
# level -> TIMEOUT. Observed via the structured wptreport (WPT-RUN-3 sl.26,
# css/css-ruby mass run). Re-run and flip to PASS once BUG-467 lands.

[line-spacing.html]
  expected: TIMEOUT
