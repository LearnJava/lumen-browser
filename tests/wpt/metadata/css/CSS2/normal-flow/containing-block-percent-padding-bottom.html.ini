# Genuine engine gap, tracked as BUG-468: percentage margin/padding is not
# correctly re-resolved against the containing block after a JS-driven
# `style.width` mutation on that containing block — `checkLayout()` measures 0
# for the percentage-based spacing instead of the value implied by the new
# width. Observed end to end by wptrunner (WPT-RUN-3 sl.2, css/CSS2 mass run).
# Re-run and flip to PASS once BUG-468 lands.
[containing-block-percent-padding-bottom.html]
  [#container 1]
    expected: FAIL
