# Genuine engine gap, tracked as BUG-360: inline event-handler-content
# attributes (here `<body onload="...">`) are never compiled into a live
# handler — `checkLayout()` inside the `onload=` string never runs, so the
# harness never registers a test and times out. Observed end to end by
# wptrunner (WPT-RUN-3 sl.2, css/CSS2 mass run). Re-run and flip to PASS once
# BUG-360 lands.
[auto-margins-used-values.html]
  expected: TIMEOUT
