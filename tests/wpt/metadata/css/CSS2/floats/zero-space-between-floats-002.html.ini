# Genuine engine gap, tracked as BUG-469: a zero-width new-formatting-context
# box does not fit into a zero-width layout opportunity between floats (CSS2.1
# §9.5.1) — positioned at (0, 0) instead of the offset `check-layout-th.js`
# expects; formalizes the float-positioning defect noted during WPT-RUN-1's
# point-check probe. Observed end to end by wptrunner (WPT-RUN-3 sl.2,
# css/CSS2 mass run). Re-run and flip to PASS once BUG-469 lands.
[zero-space-between-floats-002.html]
  [#container 1]
    expected: FAIL
