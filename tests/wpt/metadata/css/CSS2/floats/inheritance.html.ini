# Genuine engine gap, tracked as BUG-470: `getComputedStyle()` does not expose
# `float`/`clear` at all (`'float' in getComputedStyle(target)` is false) even
# though float layout itself works — a reflection gap in the computed-style
# shim, not a layout defect. Observed end to end by wptrunner (WPT-RUN-3 sl.2,
# css/CSS2 mass run). Re-run and flip to PASS once BUG-470 lands.
[inheritance.html]
  [Property float has initial value none]
    expected: FAIL
  [Property float does not inherit]
    expected: FAIL
  [Property clear has initial value none]
    expected: FAIL
  [Property clear does not inherit]
    expected: FAIL
