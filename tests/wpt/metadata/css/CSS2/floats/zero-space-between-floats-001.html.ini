# BUG-469 (the float-positioning defect this .ini was filed against) is
# FIXED 2026-09-01 — the layout geometry check-layout-th.js probes is now
# spec-correct end to end (verified live via `--mcp-port`/`eval`). Left as
# `expected: FAIL` regardless: the subtest reads `node.offsetLeft`/`offsetTop`, which
# still answers viewport-absolute coordinates instead of the offset from
# `offsetParent` (BUG-476, unrelated and still open) — every fixture here is
# off by exactly the body's default 8px margin. Re-run and flip to PASS once
# BUG-476 lands, not on any future BUG-469 work.
[zero-space-between-floats-001.html]
  [#container 1]
    expected: FAIL
