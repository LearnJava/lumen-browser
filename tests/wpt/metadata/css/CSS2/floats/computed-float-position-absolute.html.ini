# Genuine engine gap, tracked as BUG-384: named access on the Window object
# (HTML LS §7.3.3) is not implemented — a bare identifier matching an
# element's `id` (e.g. `removeMe`, `target`, `container`, `t1`) throws
# `ReferenceError` instead of resolving to that element. Observed end to end
# by wptrunner (WPT-RUN-3 sl.2, css/CSS2 mass run). Re-run and flip to PASS
# once BUG-384 lands.
[computed-float-position-absolute.html]
  [The computed value of float with absolute positioning when there is no box should be "none"]
    expected: FAIL
