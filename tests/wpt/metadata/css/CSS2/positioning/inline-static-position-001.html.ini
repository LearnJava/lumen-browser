# Genuine engine gap, tracked as BUG-467: `document.fonts.ready`
# (FontFaceSet.ready, CSS Fonts Module Level 4 §11.4) is not implemented —
# `_lumen_get_fonts()` (dom.rs) never defines a `ready` property, so
# `document.fonts.ready.then(...)` throws `TypeError` on `undefined` before
# the test ever calls `test()`, timing out the whole harness. Observed end to
# end by wptrunner (WPT-RUN-3 sl.2, css/CSS2 mass run). Re-run and flip to
# PASS once BUG-467 lands.
[inline-static-position-001.html]
  expected: TIMEOUT
