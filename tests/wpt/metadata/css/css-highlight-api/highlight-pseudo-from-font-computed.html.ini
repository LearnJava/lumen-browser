# Known engine gap, tracked as BUG-384: named access on Window (<div id=x>
# -> window.x) is not implemented -- a bare identifier referencing an
# element's id throws ReferenceError. Observed via the structured wptreport
# (WPT-RUN-3 sl.26, css/css-highlight-api mass run). Re-run and flip to PASS
# once BUG-384 lands.

[highlight-pseudo-from-font-computed.html]
  expected: TIMEOUT
