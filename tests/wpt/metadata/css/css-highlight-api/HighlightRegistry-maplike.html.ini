# Known engine gap, tracked as BUG-534: CSS Custom Highlight API's
# Highlight/HighlightRegistry are ad-hoc Phase-0 stubs (crates/js/src/
# highlight_api.rs), not the spec's Setlike/Maplike interfaces -- no
# .size/.add/.delete/.has/.clear/.entries/.keys/.values/.forEach/
# Symbol.iterator on Highlight, no global HighlightRegistry constructor, no
# CSS.highlights.highlightsFromPoint(). Observed via the structured
# wptreport (WPT-RUN-3 sl.26, css/css-highlight-api mass run). Re-run and
# flip to PASS once BUG-534 lands.

[HighlightRegistry-maplike.html]
  [HighlightRegistry initializes as it should.]
    expected: FAIL
  [HighlightRegistry has a maplike interface.]
    expected: FAIL
