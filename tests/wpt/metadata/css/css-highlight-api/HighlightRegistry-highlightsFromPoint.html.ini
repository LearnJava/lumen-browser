# GAP-HLHITTEST: `highlightsFromPoint()` hit-tests against layout's
# per-text-node fragment geometry (`lumen_layout::text_geometry`), so every
# subtest on the main document passes. The one left is the `display:none
# iframe` case: it dies on `iframe.contentWindow === null` (BUG-480) before
# it ever reaches `highlightsFromPoint()`.

[HighlightRegistry-highlightsFromPoint.html]
  [CSS.highlights.highlightsFromPoint() returns empty array when called on a display:none iframe document.]
    expected: FAIL
