# Known engine gap, tracked as BUG-533: StaticRange constructor is entirely
# missing on both engines (Range exists, StaticRange doesn't) -- new
# StaticRange(...) throws ReferenceError. Observed via the structured
# wptreport (WPT-RUN-3 sl.26, css/css-highlight-api mass run). Re-run and
# flip to PASS once BUG-533 lands.

[HighlightRegistry-maplike-tampered-Map-prototype.html]
  expected: TIMEOUT
