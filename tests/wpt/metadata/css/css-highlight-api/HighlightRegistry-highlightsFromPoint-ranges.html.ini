# Known engine gap, tracked as BUG-534 (remaining scope): BUG-533's
# StaticRange no longer blocks this test -- it now fails on the real
# hit-testing assertions (`highlightsFromPoint()` always resolves to zero
# hits, see HighlightRegistry-highlightsFromPoint.html.ini for detail).

[HighlightRegistry-highlightsFromPoint-ranges.html]
  [CSS.highlights.highlightsFromPoint() returns HighlightHitResults with the Highlights and their corresponding Ranges and StaticRanges present at the given point in the right order on multi-line text.]
    expected: FAIL
