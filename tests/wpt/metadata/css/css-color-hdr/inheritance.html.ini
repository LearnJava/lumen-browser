# BUG-508 landed the parse/apply/inherit wiring (verified: layout unit tests
# `initial_value_is_no_limit`/`child_inherits_parent_value` pass). The
# remaining FAIL below is BUG-493, not BUG-508 -- same synchronous
# set+read-in-one-tick gap as `computed.html.ini` in this directory: the
# parent's `style="dynamic-range-limit: standard"` is set and the child's
# `getComputedStyle()` is read before any relayout runs, so it sees the
# pre-mutation snapshot ('no-limit') instead of the inherited value. The
# "has initial value" subtest (not listed below) passes trivially because it
# never mutates anything. Re-run and flip to PASS once BUG-493 lands.

[inheritance.html]
  [Property dynamic-range-limit inherits]
    expected: FAIL
