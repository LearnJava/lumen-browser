# Known engine gap, tracked as BUG-508: CSS Color HDR's `dynamic-range-limit`
# property is not recognized at all -- not parsed, never reaches
# ComputedStyle, so its initial value and inheritance cannot be observed.
#
# Observed via wptrunner (WPT-RUN-3 sl.14, css/css-color-hdr mass run).
# Re-run and flip to PASS once BUG-508 lands.

[inheritance.html]
  [Property dynamic-range-limit has initial value no-limit]
    expected: FAIL
  [Property dynamic-range-limit inherits]
    expected: FAIL
