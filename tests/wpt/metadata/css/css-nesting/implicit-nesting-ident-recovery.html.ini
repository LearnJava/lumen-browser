# Known engine gap, tracked as BUG-384: elements with an `id` attribute are
# not exposed as named properties on `window` (HTML's "named access on the
# Window object"). Tests here reference an element/`<style>` by its bare
# identifier (e.g. `target`, `style`, `p`) and get a ReferenceError before
# ever reaching the rest of the test.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.19, css/css-nesting
# mass run). Re-run and flip to PASS once BUG-384 lands.


[implicit-nesting-ident-recovery.html]
  [Unknown declaration does not consume subsequent declaration]
    expected: FAIL
  [Unknown declaration with blocks does not consume subsequent declaration]
    expected: FAIL
