# Known engine gap, tracked as BUG-493: no DOM geometry/style accessor in
# this engine forces a synchronous layout/style flush before reading --
# getComputedStyle() resolves against whatever snapshot was last externally
# pushed, which for a value that only exists after the page's own initial
# synchronous <script> (i.e. read in the SAME script tick the style was
# declared) is stale or entirely absent (empty string), not the real,
# already-computed value.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.19, css/css-nesting
# mass run). Re-run and flip to PASS once BUG-493 lands.


[top-level-parent-pseudo-specificity.html]
  [CSS Nesting: Specificity of top-level '&']
    expected: FAIL
