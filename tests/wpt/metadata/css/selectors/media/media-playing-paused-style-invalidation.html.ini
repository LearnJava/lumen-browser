# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[media-playing-paused-style-invalidation.html]
  [CSS invalidation fires for :playing when no :paused rule is present]
    expected: FAIL
  [CSS invalidation fires for :paused when no :playing rule is present]
    expected: FAIL
  [CSS invalidation fires for both :playing and :paused rules]
    expected: FAIL
