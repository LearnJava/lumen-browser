# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[nth-child-large-anplusb-clamp.html]
  [b term (NumberToken path) is clamped to INT_MAX]
    expected: FAIL
  [a term (DimensionToken path) is clamped to INT_MAX]
    expected: FAIL
  [b term following an a coefficient is clamped to INT_MAX]
    expected: FAIL
