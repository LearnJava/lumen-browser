# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[user-action-pseudo-top-layer.html]
  expected: ERROR
  [User action pseudo-classes on dialog (modal)]
    expected: FAIL
  [User action pseudo-classes on dialog (non-modal)]
    expected: NOTRUN
  [User action pseudo-classes on popover (popover)]
    expected: NOTRUN
  [User action pseudo-classes on customizable-select (open)]
    expected: NOTRUN
