# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[heading-prefixed.html]
  [:heading(1) matches <h:h1> in the HTML namespace]
    expected: FAIL
  [:heading(2) matches <h:h2> in the HTML namespace]
    expected: FAIL
  [:heading(3) matches <h:h3> in the HTML namespace]
    expected: FAIL
  [:heading(4) matches <h:h4> in the HTML namespace]
    expected: FAIL
  [:heading(5) matches <h:h5> in the HTML namespace]
    expected: FAIL
  [:heading(6) matches <h:h6> in the HTML namespace]
    expected: FAIL
