# Known engine gap, tracked as BUG-360: <body onload="...">
# content-attribute event handlers never fire -- checkLayout() is
# never called, so the harness never registers a test() or calls done().
# Observed via WPT-RUN-3 slice 22 (css-rhythm mass run). Re-run and flip
# to PASS once the bug lands.


[block-level-replaced-elements-affected-by-block-step-size.html]
  expected: TIMEOUT
