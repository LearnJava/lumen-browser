# Known engine gap: BUG-555 (getComputedStyle() returns "" for every
# property when read from a synchronous inline <script> during the initial
# HTML parse). Unreachable by this run at all (HTTPS-port gap -- .https.
# file with no testdriver.js); confirmed via direct re-run over plain HTTP:
# all 5 assertions fail as "expected <real value> but got ''" -- unrelated
# to the display:layout() value under test, every property reads back empty.
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once the cited bug(s) land (and the HTTPS gap).

[computed-style-layout-function.https.html]
  expected: TIMEOUT
