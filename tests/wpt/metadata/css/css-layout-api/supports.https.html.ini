# Pure test-infra gap: unreachable by this run at all (HTTPS-port gap --
# .https. file with no testdriver.js). Confirmed via direct re-run over
# plain HTTP that this test genuinely PASSES: CSS.supports('display',
# 'layout(foo)') correctly returns true (the grammar production is
# recognized even though CSS Layout API itself is unimplemented, matching
# real UA behavior for paint()-style Houdini functions). Not an engine bug --
# recorded only because the real wptrunner run cannot reach it.
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once the HTTPS-port gap is closed.

[supports.https.html]
  expected: TIMEOUT
