# Expected: CSS.layoutWorklet is genuinely absent (CSS Layout API is
# post-Phase-2, CSS-SPECS.md:137, no worklet stub exists at all -- unlike
# CSS Paint API's Phase-0 stub). Confirmed via direct re-run over plain
# HTTP: throws the test's own "CSS Layout API not supported." guard, which
# is the correct/expected behavior for an unimplemented feature, not an
# engine defect. Unreachable by this run at all (HTTPS-port gap -- .https.
# file with no testdriver.js).
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once CSS Layout API is implemented.

[sync-layout-microtasks.https.html]
  expected: TIMEOUT
