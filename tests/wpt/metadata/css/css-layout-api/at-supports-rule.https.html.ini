# Known engine gap: BUG-555 (getComputedStyle() returns "" for every
# property when read from a synchronous inline <script> during the initial
# HTML parse, before the first layout snapshot is published -- this test's
# test() callback runs exactly that way). Unreachable by this run at all
# (HTTPS-port gap -- .https. file with no testdriver.js); confirmed via
# direct re-run over plain HTTP: assert_equals expected "\"pass\"" but got
# "" on getComputedStyle(element).content.
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once the cited bug(s) land (and the HTTPS gap).

[at-supports-rule.https.html]
  expected: TIMEOUT
