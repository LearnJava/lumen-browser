# Known engine gap: minor -- element.style.display accepts syntactically
# invalid layout() values (layout() with no name, layout(test3, invalid)
# with an extra argument) without grammar validation (confirmed via direct
# re-run over plain HTTP: "expected \"\" but got \"layout()\""). Low
# priority: CSS Layout API itself is post-Phase-2 (CSS-SPECS.md:137), not
# filed as its own BUG -- noted in BUG-555's "secondary finding" section.
# Unreachable by this run at all (HTTPS-port gap -- .https. file with no
# testdriver.js).
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once CSS Layout API grammar validation lands.

[inline-style-layout-function.https.html]
  expected: TIMEOUT
