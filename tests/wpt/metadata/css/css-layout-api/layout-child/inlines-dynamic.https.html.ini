# Known test-infra gap: /common/worklet-reftest.js is not vendored --
# confirmed via direct re-run over plain HTTP: "promise_test: Unhandled
# rejection ... ReferenceError: importWorklet is not defined". Also depends
# on CSS.layoutWorklet, genuinely unimplemented (CSS Layout API is
# post-Phase-2, CSS-SPECS.md:137) -- cannot pass in this phase regardless
# of vendoring.
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-layout-api.
# Re-run and flip to PASS once CSS Layout API + /common/worklet-reftest.js land.

[inlines-dynamic.https.html]
  expected: TIMEOUT
