# BUG-380 (browsing-context reuse): each of the 5 preferences-*.tentative.https
# files in this directory races the HTTPS-port gap and BUG-380's stale-result
# reuse -- observed as ERROR on one run, TIMEOUT on the next, non-deterministic
# which file gets which. `expected: [ERROR, TIMEOUT]` accepts either.
# WPT-RUN-3 slice 39 mass run (structured wptreport JSON, delta vs. no
# pre-existing .ini -- verified zero overlap before generation).
# Re-run and flip to PASS once BUG-380 (and the underlying HTTPS-port gap) land.

[preferences-reducedData.tentative.https.html]
  expected: [ERROR, TIMEOUT]
