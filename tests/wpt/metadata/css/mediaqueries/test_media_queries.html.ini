# Known engine gap, tracked as BUG-471: document.styleSheets[0] is
# undefined (the CSSOM stylesheet/rule object model is not implemented),
# so this test's own top-level script crashes on `document.styleSheets[0]
# .cssRules` before testharness.js registers any test -- harness never
# starts, zero subtests registered.
# Observed via WPT-RUN-3 slice 24 (css-mediaqueries mass run).

[test_media_queries.html]
  expected: TIMEOUT
