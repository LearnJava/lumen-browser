# Known engine gap, tracked as BUG-526: MediaQueryList.media doesn't
# serialize per the Media Queries serialization algorithm -- it just
# echoes the raw input string verbatim.
# Observed via WPT-RUN-3 slice 24 (css-mediaqueries mass run).

[aspect-ratio-serialization.html]
  [<ratio> serializes with spaces around the integer.]
    expected: FAIL
