# P3 revision 2026-09-06: BUG-526 (MediaQueryList.media echoed the raw
# input verbatim) landed -- 11 of the 25 subtests below now pass and were
# removed from this file. The remaining 14 split into two independent,
# unrelated engine gaps found while fixing BUG-526, neither is "finish the
# serialization":
# - boolean-context features / unclosed parens / bare-word tokenization
#   not stopping at ')' -- BUG-1020 (7 subtests, the "(color"/"color)" group)
# - `resolution`/`min-/max-resolution` media feature not implemented at all
#   (no units, no calc()) -- BUG-1019 (7 subtests, test_resolution_parsing)
# Observed via WPT-RUN-3 slice 24 (css-mediaqueries mass run).
#
# P3 revision 2026-09-07: BUG-1019 closed -- `resolution`/`min-resolution`/
# `max-resolution` now parse `x`/`dppx`/`dpi`/`dpcm` plus a minimal `calc()`
# (also fixed the feature-value paren scanner, which took the *first* `)`
# instead of the balanced one -- `calc(...)`'s own closing paren was cutting
# the outer feature value short). The 7 subtests below are now green,
# transcribed verbatim as unit tests in
# `crates/engine/css-parser/src/parser/tests/at_rules.rs`. BUG-1020 (the
# other half, unrelated) remains open.

[match-media-parsing.html]
  [Test parsing '(color' with matchMedia]
    expected: FAIL
  [Test parsing ' (color)' with matchMedia]
    expected: FAIL
  [Test parsing ' ( color  )  ' with matchMedia]
    expected: FAIL
  [Test parsing ' ( color   ' with matchMedia]
    expected: FAIL
  [Test parsing 'color)' with matchMedia]
    expected: FAIL
  [Test parsing '  color)' with matchMedia]
    expected: FAIL
  [Test parsing '  color ), ( color' with matchMedia]
    expected: FAIL
