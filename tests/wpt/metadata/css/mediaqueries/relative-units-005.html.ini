# Known engine gap, tracked as BUG-384: named access on Window (bare-id) is
# not implemented -- a bare identifier referencing an element by its id
# throws ReferenceError instead of resolving.
# Observed via WPT-RUN-3 slice 24 (css-mediaqueries mass run).

[relative-units-005.html]
  [ex unit in media queries should match initial font]
    expected: FAIL
  [ch unit in media queries should match initial font]
    expected: FAIL
  [ic unit in media queries should match initial font]
    expected: FAIL
