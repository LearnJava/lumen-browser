# Known engine gaps: BUG-467 (document.fonts.ready is undefined -- .then(...) throws before any test() registers).
# WPT-RUN-3 slice 35 mass run (structured wptreport JSON), css/css-multicol.
# Re-run and flip to PASS once the cited bug(s) land.

[getclientrects-001.html]
  expected: TIMEOUT
