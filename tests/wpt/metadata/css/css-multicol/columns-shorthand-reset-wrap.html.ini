# Known engine gaps: BUG-384 (top-level `style is not defined` -- named access on Window missing).
# WPT-RUN-3 slice 35 mass run (structured wptreport JSON), css/css-multicol.
# Re-run and flip to PASS once the cited bug(s) land.

[columns-shorthand-reset-wrap.html]
  expected: TIMEOUT
