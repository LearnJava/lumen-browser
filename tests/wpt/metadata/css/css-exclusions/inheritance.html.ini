# Known engine gap, tracked as BUG-507: `wrap-flow`/`wrap-through` (CSS
# Exclusions L1) are entirely unimplemented -- not parsed, never reach
# ComputedStyle, so getComputedStyle() reports no value for either
# property. Observed via wptrunner (WPT-RUN-3 sl.13, css/css-exclusions
# mass run). Re-run and flip to PASS once BUG-507 lands.


[inheritance.html]
  [Property wrap-flow has initial value auto]
    expected: FAIL
  [Property wrap-flow does not inherit]
    expected: FAIL
  [Property wrap-through has initial value wrap]
    expected: FAIL
  [Property wrap-through does not inherit]
    expected: FAIL
