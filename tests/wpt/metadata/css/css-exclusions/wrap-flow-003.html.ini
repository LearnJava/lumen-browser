# Known engine gap, tracked as BUG-507: `wrap-flow` (CSS Exclusions L1) is
# entirely unimplemented -- assert_implements('wrap-flow: auto', ...) fails
# as a setup precondition before any test registers, so the harness itself
# reports ERROR with 0 subtests. Observed via wptrunner (WPT-RUN-3 sl.13,
# css/css-exclusions mass run). Re-run and flip to PASS once BUG-507 lands.


[wrap-flow-003.html]
  expected: ERROR
