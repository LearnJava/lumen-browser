# Known engine gap, tracked as BUG-507: `wrap-through`/`wrap-flow` (CSS
# Exclusions L1) are entirely unimplemented -- assert_implements('wrap-
# through: none', ...) fails as a setup precondition before any test
# registers, so the harness itself reports ERROR with 0 subtests. Observed
# via wptrunner (WPT-RUN-3 sl.13, css/css-exclusions mass run). Re-run and
# flip to PASS once BUG-507 lands.


[wrap-through-001.html]
  expected: ERROR
