# Known engine gap, tracked as BUG-519: this @function body construct
# (if()/typed attr()/local vars/@layer nesting/shadow scoping) causes a
# hang instead of a graceful FAIL -- confirmed via wall-clock timing.
# Observed via WPT-RUN-3 slice 22 (css-mixins mass run). Re-run and flip
# to PASS once the bug lands.


[local-var-substitution.html]
  expected: TIMEOUT
