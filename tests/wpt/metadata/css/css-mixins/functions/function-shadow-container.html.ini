# BUG-519 closed 2026-09-24: the hang is gone and @function names are now
# tree-scoped. Remaining gap: conditional group rules (@container) inside an
# @function body are not evaluated (CSS-SPECS.md `@function` row, deferred
# scope) -- the other two subtests pass only because the unconditional
# lookup happens to land on the expected value.


[function-shadow-container.html]
  [::slotted() can see inner named containers]
    expected: FAIL
