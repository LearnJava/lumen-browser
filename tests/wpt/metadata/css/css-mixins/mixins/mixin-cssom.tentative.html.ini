# BUG-518 срез 6 (2026-09-07): BUG-384 (named window access) was already
# fixed by then, and `@mixin`/`@apply`/`@contents` CSSOM serialization
# (`Rule::css_text`/`MixinRule::css_text`, css-parser) closed the remaining
# five read-only subtests -- confirmed end-to-end through the real V8 shim
# (`crates/js/tests/cases/bug518_mixin_cssom.rs`), not just unit-level.
# "@apply is not legal at top level" still needs `CSSStyleSheet.insertRule`
# on an *owned* sheet (today only a constructed one has it, BUG-897/CSSOM-5)
# -- kept FAIL, documented in bugs/BUG-518-OPEN.md.

[mixin-cssom.tentative.html]
  [@apply is not legal at top level]
    expected: FAIL
