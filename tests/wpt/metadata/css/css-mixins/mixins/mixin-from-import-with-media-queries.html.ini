# Known engine gap, tracked as BUG-518: CSS Mixins @mixin/@apply/@contents
# rules are not implemented at all -- mixin declarations never reach
# the cascade.
# Observed via WPT-RUN-3 slice 22 (css-mixins mass run). Re-run and flip
# to PASS once the bug lands.


[mixin-from-import-with-media-queries.html]
  [CSS Mixins: @import with media queries]
    expected: FAIL
