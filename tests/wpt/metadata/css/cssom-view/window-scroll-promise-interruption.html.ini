# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 39 mass run (structured wptreport JSON, delta vs. no
# pre-existing .ini -- verified zero overlap before generation).
# Re-run and flip to PASS once the cited bug(s) land.

[window-scroll-promise-interruption.html]
  expected: ERROR
  [Window.scrollTo: interruption by auto scroll call]
    expected: NOTRUN
  [Window.scrollBy: interruption by auto scroll call]
    expected: NOTRUN
  [Window.scrollTo: interruption by smooth scroll call]
    expected: NOTRUN
  [Window.scrollBy: interruption by smooth scroll call]
    expected: NOTRUN
