# Already-tracked engine gap, BUG-384: named access on `Window` is not
# implemented -- an element with `id="x"` gives neither `window.x` nor a bare
# `x` (in classic-script scope), so any test that refers to a fixture element
# by bare identifier dies with `ReferenceError: x is not defined` before its
# actual assertion runs. Observed end to end by wptrunner (WPT-RUN-3 sl.4,
# css/cssom-view mass run). Re-run and flip to PASS once BUG-384 lands.

[scrollIntoView-nearest-oversized-element.html]
  [scrollIntoView nearest aligns an oversized target before the scrollport to its closest (right/bottom) edge]
    expected: FAIL
