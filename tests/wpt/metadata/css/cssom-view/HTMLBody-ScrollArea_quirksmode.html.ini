# Genuine engine gap, tracked as BUG-482: `HTMLElement.offsetParent`/
# `document.scrollingElement` are missing entirely, and `document.compatMode`
# is hardcoded to `CSS1Compat` (no quirks-mode/doctype detection at all).
# Observed end to end by wptrunner (WPT-RUN-3 sl.4, css/cssom-view mass run).
# Re-run and flip to PASS once BUG-482 lands.

[HTMLBody-ScrollArea_quirksmode.html]
  [document.compatMode should be BackCompat in quirks.]
    expected: FAIL
  [document.scrollingElement should be body element in quirks.]
    expected: FAIL
  [scrollingElement in quirks should be null when body is potentially scrollable.]
    expected: FAIL
  [scrollingElement in quirks should be body if any of document and body has a visible overflow.]
    expected: FAIL
  [When body potentially scrollable, document.body.scrollHeight changes when changing the height of the body content in quirks.]
    expected: FAIL
  [When body not potentially scrollable, document.body.scrollHeight always equals window.innerHeight in quirks. (cond. visible, scroll)]
    expected: FAIL
  [When body not potentially scrollable, document.body.scrollHeight always equals window.innerHeight in quirks. (cond. scroll, visible)]
    expected: FAIL
  [When body not potentially scrollable, document.body.scrollHeight always equals window.innerHeight in quirks. (cond. visible, visible)]
    expected: FAIL
