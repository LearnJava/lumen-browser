# Already-tracked engine gap, BUG-472: `getComputedStyle()` is a fixed
# `HashMap` lookup (`_lumen_get_computed_style`) populated once per layout
# pass by a hand-written ~64-property list (`computed_style_to_map`,
# `selector_query.rs`) -- properties outside that list silently resolve to
# `""` even though the property is genuinely parsed and stored on
# `ComputedStyle` (`scroll-behavior` included: `layout/src/style.rs:3626`).
# Observed end to end by wptrunner (WPT-RUN-3 sl.4, css/cssom-view mass run).
# Re-run and flip to PASS once BUG-472 lands.

[scroll-behavior-computed.html]
  [Property scroll-behavior value 'auto']
    expected: FAIL
  [Property scroll-behavior value 'smooth']
    expected: FAIL
