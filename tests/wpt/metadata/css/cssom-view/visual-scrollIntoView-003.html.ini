# Genuine engine gap, tracked as BUG-481: `window.visualViewport` is not
# implemented at all. Observed end to end by wptrunner (WPT-RUN-3 sl.4,
# css/cssom-view mass run). Re-run and flip to PASS once BUG-481 lands.

[visual-scrollIntoView-003.html]
  [Element.scrollIntoView scrolls visually to an element in nested position: fixed elements]
    expected: FAIL
