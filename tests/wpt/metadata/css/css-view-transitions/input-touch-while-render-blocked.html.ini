# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[input-touch-while-render-blocked.html]
  [Releasing touch during render-blocking targets touched element]
    expected: FAIL
  [While render-blocked, new touch targets root]
    expected: FAIL
