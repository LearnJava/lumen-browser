# Known engine gaps: BUG-384 (named access on Window missing -- elements with id= are not exposed as bare globals).
# Flaky TIMEOUT boundary (confirmed across 3 repeated runs, 2026-08-04): sometimes the whole
# harness times out before any subtest completes, sometimes it finishes OK with both subtests
# FAIL on BUG-384. Same class as the responsive-iframe gotcha from WPT-RUN-3 slice 33 -- list
# syntax matches either form.
# WPT-RUN-3 slice 34 mass run (structured wptreport JSON), css/css-position.
# Re-run and flip to PASS once the cited bug(s) land.

[position-sticky-scrolled-remove-sibling.html]
  expected: [OK, TIMEOUT]
  [Sticky position and its overflow contribution in the vertical axis]
    expected: [FAIL, TIMEOUT, NOTRUN]
  [Sticky position and its overflow contribution in the horizontal axis]
    expected: [FAIL, TIMEOUT, NOTRUN]
