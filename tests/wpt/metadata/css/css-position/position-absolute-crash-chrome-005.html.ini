# Known engine gaps: BUG-503 (animationend/transitionend never fire for real CSS animations/transitions -- async_test waiting on the event hangs).
# WPT-RUN-3 slice 34 mass run (structured wptreport JSON), css/css-position.
# Re-run and flip to PASS once the cited bug(s) land.

[position-absolute-crash-chrome-005.html]
  expected: TIMEOUT
  [Test passes if it does not crash]
    expected: NOTRUN
