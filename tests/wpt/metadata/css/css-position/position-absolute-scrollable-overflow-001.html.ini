# Known engine gaps: BUG-360 (HTML on*= event-handler attributes never compile into handlers -- inline onload="checkLayout(...)" never fires).
# WPT-RUN-3 slice 34 mass run (structured wptreport JSON), css/css-position.
# Re-run and flip to PASS once the cited bug(s) land.

[position-absolute-scrollable-overflow-001.html]
  expected: TIMEOUT
