# Known engine gap, tracked as BUG-516: the logical `overscroll-behavior-
# block`/`-inline` longhands are not recognized by the parser at all
# (unlike the physical `-x`/`-y`, which parse and store correctly).
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[overscroll-behavior-logical.html]
  [Logical overscroll-behavior maps correctly when element has horizontal-tb writing mode]
    expected: FAIL
  [Logical overscroll-behavior maps correctly when element has vertical-rl writing mode]
    expected: FAIL
  [Logical overscroll-behavior maps correctly when element has horizontal-tb writing mode and is not affected by rtl direction]
    expected: FAIL
