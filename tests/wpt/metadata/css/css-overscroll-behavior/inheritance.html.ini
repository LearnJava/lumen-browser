# BUG-516 revision 2026-09-05: `overscroll-behavior-block`/`-inline` are now
# recognized by the parser — the 4 "has initial value auto" subtests (both
# physical and logical) now PASS and are removed from this file. The
# remaining "does not inherit" subtests fail identically for physical
# (`-x`/`-y`) and logical (`-block`/`-inline`) forms alike, confirmed by a
# live rerun (`run_smoke.py`) after the fix landed — the original filing's
# split attribution (logical → BUG-516, physical → BUG-472) does not hold
# for this file; all four properties share the same root cause. Tracked as
# BUG-472 (`getComputedStyle` resolved-value coverage gaps, reclassified
# ДОРАБОТКА → CSSOM-3) alongside `parsing/overscroll-behavior-computed.html`.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[inheritance.html]
  [Property overscroll-behavior-block does not inherit]
    expected: FAIL
  [Property overscroll-behavior-inline does not inherit]
    expected: FAIL
  [Property overscroll-behavior-x does not inherit]
    expected: FAIL
  [Property overscroll-behavior-y does not inherit]
    expected: FAIL
