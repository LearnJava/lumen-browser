# Known engine gap, tracked as BUG-484: the inline `style` setter never
# validates values -- malformed indexed `env(name N, ...)` syntax is
# accepted verbatim instead of being rejected.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[indexed-env.tentative.html]
  [CSS Environment variable value "env(test1 test2, green)" must not successfully parse]
    expected: FAIL
  [CSS Environment variable value "env(test1 10 20 test2, green)" must not successfully parse]
    expected: FAIL
  [CSS Environment variable value "env(test 0.1, green)" must not successfully parse]
    expected: FAIL
  [CSS Environment variable value "env(test -1, green)" must not successfully parse]
    expected: FAIL
