# Known engine gap, tracked as BUG-484: the inline `style` setter never
# validates values -- the explicitly-invalid `env()` (empty argument) is
# accepted and clobbers a previously-valid value instead of being
# rejected.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[seralization-round-tripping.tentative.html]
  [Test style seralization round tripping with CSS env vars]
    expected: FAIL
