# P3 revision 2026-09-05 (BUG-514): env()/var() substitution itself is
# correctly implemented (see substitute.rs) -- this file's own assertion
# reads `getComputedStyle(document.body).backgroundColor`, and `<body>`'s
# own background-color is unconditionally cleared by canvas-background
# propagation (BUG-1007), independent of env()/var(). Re-run and flip to
# PASS once BUG-1007 lands.


[fallback-nested-var.tentative.html]
  [Test that nested var() fallback values work with CSS env vars]
    expected: FAIL
