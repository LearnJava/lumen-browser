# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- the bare identifier `child` (an element with
# `id="child"`) throws ReferenceError.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[env-in-custom-properties.tentative.html]
  [env() is substituted into a custom property]
    expected: FAIL
  [Substitution of unrecognized env() causes guaranteed-invalid]
    expected: FAIL
