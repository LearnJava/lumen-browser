# Known engine gap, tracked as BUG-487: the `revert-rule` CSS-wide keyword
# is not implemented at all -- here tested as an `env()` fallback value.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[env-revert-rule.html]
  [The revert-rule works in an env() fallback]
    expected: FAIL
