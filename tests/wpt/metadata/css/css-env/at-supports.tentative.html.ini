# P3 revision 2026-09-05 (BUG-514): `@supports (background-color: env(test))`
# itself evaluates correctly (confirmed live: the same condition applied to
# `html`/a script-created `div` works). This file's own assertion reads
# `getComputedStyle(document.body).backgroundColor`, and `<body>`'s own
# background-color is unconditionally cleared by canvas-background
# propagation (BUG-1007), independent of env()/@supports. Re-run and flip to
# PASS once BUG-1007 lands.


[at-supports.tentative.html]
  [Test that CSS env vars work with @support]
    expected: FAIL
