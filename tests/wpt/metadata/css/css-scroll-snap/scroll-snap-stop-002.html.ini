# Known engine gap, tracked as BUG-384: named access on Window is absent --
# a bare identifier referencing an element by its `id` attribute throws
# ReferenceError instead of resolving through the Window named-access
# mechanism.
# Observed via the structured wptreport (WPT-RUN-3 sl.29).
# Re-run and flip to PASS once BUG-384 lands.

[scroll-snap-stop-002.html]
  [The closest snap point is preferred than scroll-snap-stop: always where it's further than the destination (the closest one is closer to the scroll start position than the destination)]
    expected: FAIL
  [The closest snap point is preferred than scroll-snap-stop: always where it's further than the destination (the closest one is further than the destination from the start position)]
    expected: FAIL
  [The scroll destination on a large element whose snap area covers the snapport entirely is a valid snap position]
    expected: FAIL
  [The scroll destination on a large element whose snap area covers the snapport entirely is a valid snap position (with two `scroll-snap-stop: always` snap points]
    expected: FAIL
  [`scroll-snap-stop: always` snap point is preferred even if the snap area entire snapport]
    expected: FAIL
  [`scroll-snap-stop: always` snap point is further than the scroll destination]
    expected: FAIL
