# Known engine gap, tracked as BUG-525: document.scrollingElement is not
# implemented at all, always undefined.
# Observed via the structured wptreport (WPT-RUN-3 sl.29).
# Re-run and flip to PASS once BUG-525 lands.

[snap-area-capturing-add-scroll-container.html]
  [Making an element scrollable should make it capture the correct descendant snap areas' snap points.]
    expected: FAIL
  [Attaching a new element that is scrollable should assign the correct snap areas to it.]
    expected: FAIL
