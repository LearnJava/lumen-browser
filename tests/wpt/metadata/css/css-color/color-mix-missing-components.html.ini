# BUG-539: getComputedStyle() Proxy has no `has` trap -- `'<prop>' in getComputedStyle(el)` always false
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[color-mix-missing-components.html]
  [Property color value 'color-mix(in srgb, oklch(0 none none), white)']
    expected: FAIL
  [Property color value 'color-mix(in srgb, rgb(0 255 0 / 0.5), rgb(0 255 0 / none))']
    expected: FAIL
  [Property color value 'color-mix(in srgb, rgb(0 255 0 / none), rgb(0 255 0 / 0.5))']
    expected: FAIL
