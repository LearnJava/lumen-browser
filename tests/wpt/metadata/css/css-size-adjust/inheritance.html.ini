# Known engine gap, tracked as BUG-513: `text-size-adjust` (shipped in
# every mobile browser) is not implemented at all -- absent from
# ComputedStyle.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[inheritance.html]
  [Property text-size-adjust has initial value auto]
    expected: FAIL
  [Property text-size-adjust inherits]
    expected: FAIL
