# Known engine gap, tracked as BUG-484: the inline `style` setter never
# validates values -- malformed `text-size-adjust` values are accepted
# verbatim instead of being rejected.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[text-size-adjust-invalid.html]
  [e.style['text-size-adjust'\] = "reverse" should not set the property value]
    expected: FAIL
  [e.style['text-size-adjust'\] = "0" should not set the property value]
    expected: FAIL
  [e.style['text-size-adjust'\] = "10px" should not set the property value]
    expected: FAIL
  [e.style['text-size-adjust'\] = "-100%" should not set the property value]
    expected: FAIL
