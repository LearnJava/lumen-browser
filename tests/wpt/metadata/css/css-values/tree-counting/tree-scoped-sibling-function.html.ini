# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[tree-scoped-sibling-function.html]
  [sibling-index() and sibling-count() evaluates to 1 from outer tree with ::part]
    expected: FAIL
  [sibling-index() and sibling-count() evaluate as normal from inner tree]
    expected: FAIL
