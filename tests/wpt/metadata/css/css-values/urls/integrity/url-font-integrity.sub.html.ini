# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[url-font-integrity.sub.html]
  [integrity() with correct hash allows font loading]
    expected: FAIL
