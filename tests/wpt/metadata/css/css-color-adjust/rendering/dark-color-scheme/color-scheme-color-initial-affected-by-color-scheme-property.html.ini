# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- a bare identifier referencing an element by its id
# throws ReferenceError.
# Observed via WPT-RUN-3 slice 22 (css-color-adjust mass run). Re-run and flip
# to PASS once the bug lands.


[color-scheme-color-initial-affected-by-color-scheme-property.html]
  [Check that color:initial resolves to the intial color for the dark color-scheme.]
    expected: FAIL
  [Check that color:initial resolves to the intial color for the light color-scheme.]
    expected: FAIL
