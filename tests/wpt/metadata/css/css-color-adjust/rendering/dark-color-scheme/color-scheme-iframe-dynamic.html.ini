# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- a bare identifier referencing an element by its id
# throws ReferenceError.
# Observed via WPT-RUN-3 slice 22 (css-color-adjust mass run). Re-run and
# flip to PASS once the bug lands.


[color-scheme-iframe-dynamic.html]
  expected: ERROR
  [Initially, the preferred color-scheme is the same in both documents]
    expected: NOTRUN
  [Modify color-scheme <meta>]
    expected: NOTRUN
  [Modify iframe color-scheme]
    expected: NOTRUN
