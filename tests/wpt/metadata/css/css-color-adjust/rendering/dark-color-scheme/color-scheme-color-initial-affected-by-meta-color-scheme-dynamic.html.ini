# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- a bare identifier referencing an element by its id
# throws ReferenceError.
# Observed via WPT-RUN-3 slice 22 (css-color-adjust mass run). Re-run and flip
# to PASS once the bug lands.


[color-scheme-color-initial-affected-by-meta-color-scheme-dynamic.html]
  [In light mode as indicated by <meta> color-scheme, explicit color: initial matches CanvasText]
    expected: FAIL
  [In dark mode as indicated by <meta> color-scheme, explicit color: initial matches CanvasText]
    expected: FAIL
