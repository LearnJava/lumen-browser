# Known engine gap, tracked as BUG-485: document.head is entirely missing
# (no getter, no native binding on either engine). support/counter-style-
# testcommon.js does document.head.appendChild(style) at top level, before
# any test() registers, so the throw aborts the whole file -> TIMEOUT.
# Observed via the structured wptreport (WPT-RUN-3 sl.26, css/css-counter-
# styles mass run). Re-run and flip to PASS once BUG-485 lands.

[name-syntax.html]
  expected: TIMEOUT
