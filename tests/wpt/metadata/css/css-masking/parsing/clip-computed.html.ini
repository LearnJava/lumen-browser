# Known engine gaps: BUG-539 (getComputedStyle Proxy has no `has` trap -- 'prop' in getComputedStyle(el) always false).
# WPT-RUN-3 slice 36 mass run (structured wptreport JSON), css/css-masking.
# Re-run and flip to PASS once the cited bug(s) land.

[clip-computed.html]
  [Property clip value 'auto']
    expected: FAIL
  [Property clip value 'rect(10px, 20px, -30px, 40px)']
    expected: FAIL
  [Property clip value 'rect(10px, 20px, calc(-1em + 10px), 1em)']
    expected: FAIL
  [Property clip value 'rect(10px, -20px, auto, auto)']
    expected: FAIL
