# Known engine gaps: BUG-539 (getComputedStyle Proxy has no `has` trap -- 'prop' in getComputedStyle(el) always false).
# WPT-RUN-3 slice 36 mass run (structured wptreport JSON), css/css-masking.
# Re-run and flip to PASS once the cited bug(s) land.

[mask-composite-computed.html]
  [Property mask-composite value 'add']
    expected: FAIL
  [Property mask-composite value 'subtract']
    expected: FAIL
  [Property mask-composite value 'intersect']
    expected: FAIL
  [Property mask-composite value 'exclude']
    expected: FAIL
