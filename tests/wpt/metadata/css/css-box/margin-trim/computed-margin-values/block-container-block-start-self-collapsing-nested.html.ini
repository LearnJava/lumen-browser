# Known engine gap, tracked as BUG-360: inline event-handler attributes
# (`onload=`/`onclick=`/...) are dead -- the live dispatcher only checks
# addEventListener-registered listeners. Every file here drives its
# assertions from `<body onload="checkLayout(...)">`, so `checkLayout` never
# runs, no `test()` ever registers, and the harness times out with zero
# subtests. Observed end to end by wptrunner (WPT-RUN-3 sl.5, css/css-box
# mass run). Re-run and flip to PASS once BUG-360 lands.

[block-container-block-start-self-collapsing-nested.html]
  expected: TIMEOUT
