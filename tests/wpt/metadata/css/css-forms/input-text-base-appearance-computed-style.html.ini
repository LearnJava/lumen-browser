# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented. Upstream test has its own typo (`const intial = ...`,
# missing the second `i`) that leaves the correctly-spelled `initial`
# unresolvable except through named access on an `<div id="initial">` --
# real browsers paper over the typo via that mechanism, Lumen has none.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.16, css/css-forms mass
# run). Re-run and flip to PASS once BUG-384 lands.

[input-text-base-appearance-computed-style.html]
  [UA styles of base appearance <input type=text>.]
    expected: FAIL
