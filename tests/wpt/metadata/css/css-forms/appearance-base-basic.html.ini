# Known engine gap, tracked as BUG-493: getComputedStyle() resolves against
# whatever style/layout snapshot was last externally pushed, which is empty
# during the page's own synchronous top-level <script> (this test's whole
# body runs inline, no task boundary). All ten subtests here run their
# assertions in this window; five inherited-property comparisons pass by
# accident (both sides read "" and compare equal to each other), only the
# box-sizing-vs-literal comparison actually surfaces as FAIL.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.16, css/css-forms mass
# run). Re-run and flip to PASS once BUG-493 lands.

[appearance-base-basic.html]
  [Basic appearance stylesheet properties for input-text]
    expected: FAIL
  [Basic appearance stylesheet properties for input-search]
    expected: FAIL
  [Basic appearance stylesheet properties for input-number]
    expected: FAIL
  [Basic appearance stylesheet properties for input-button]
    expected: FAIL
  [Basic appearance stylesheet properties for input-reset]
    expected: FAIL
  [Basic appearance stylesheet properties for button]
    expected: FAIL
  [Basic appearance stylesheet properties for select]
    expected: FAIL
  [Basic appearance stylesheet properties for textarea]
    expected: FAIL
  [Basic appearance stylesheet properties for meter]
    expected: FAIL
  [Basic appearance stylesheet properties for progress]
    expected: FAIL
