# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- the bare identifier `checkbox` (from `<input id="checkbox">`)
# is not resolvable at all (ReferenceError), not merely undefined.
#
# Observed via the structured wptreport (WPT-RUN-3 sl.16, css/css-forms mass
# run). Re-run and flip to PASS once BUG-384 lands.

[checkbox-checkmark-animation.html]
  [::checkmark pseudo-element animates]
    expected: FAIL
