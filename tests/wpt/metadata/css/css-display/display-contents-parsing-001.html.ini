# Known engine gap, tracked as BUG-489: getComputedStyle() returns nothing for
# the display:contents element itself -- flatten_contents() removes its box
# from the tree entirely (spec-correct for rendering) but the collect_*
# style/geometry maps are pure box-tree walkers, so the element has no entry
# to report from.
# Observed end to end by wptrunner (WPT-RUN-3 sl.7, css/css-display mass run).
# Re-run and flip to PASS once BUG-489 lands.

[display-contents-parsing-001.html]
  [Tests that the 'contents' value for the 'display' property is correctly parsed]
    expected: FAIL
