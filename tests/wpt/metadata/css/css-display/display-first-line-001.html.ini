# Known engine gap, tracked as BUG-490: getComputedStyle(el, pseudoElt) ignores
# the pseudo-element argument entirely (documented in dom.rs as not yet
# supported) -- a query for ::before/::after/::first-line/::first-letter's
# computed style silently returns the base element's own style instead.
# Observed end to end by wptrunner (WPT-RUN-3 sl.7, css/css-display mass run).
# Re-run and flip to PASS once BUG-490 lands.

[display-first-line-001.html]
  [display of first-line]
    expected: FAIL
