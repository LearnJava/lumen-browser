# Known engine gap, tracked as BUG-470: getComputedStyle() does not expose
# float/clear -- both getPropertyValue(float) and the in-operator check
# return nothing for a property missing from the fixed computed-style map.
# Observed end to end by wptrunner (WPT-RUN-3 sl.7, css/css-display mass run).
# Re-run and flip to PASS once BUG-470 lands.

[display-with-float-dynamic.html]
  [computed style for float]
    expected: FAIL
