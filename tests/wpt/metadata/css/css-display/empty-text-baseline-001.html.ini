# Known engine gap, tracked as BUG-467: document.fonts.ready is absent,
# throwing before the test ever calls done() -- external TIMEOUT.
# Observed end to end by wptrunner (WPT-RUN-3 sl.7, css/css-display mass run).
# Re-run and flip to PASS once BUG-467 lands.

[empty-text-baseline-001.html]
  expected: TIMEOUT
