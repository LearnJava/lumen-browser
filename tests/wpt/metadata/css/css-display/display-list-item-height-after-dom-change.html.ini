# Known engine gap, tracked as BUG-384: named access on Window is
# incomplete/incorrect -- a bare identifier referencing an element by its
# `id` attribute throws ReferenceError instead of resolving through the
# Window named-access mechanism. Observed end to end by wptrunner
# (WPT-RUN-3 sl.7, css/css-display mass run).
# Re-run and flip to PASS once BUG-384 lands.

[display-list-item-height-after-dom-change.html]
  [List item height after DOM change.]
    expected: FAIL
