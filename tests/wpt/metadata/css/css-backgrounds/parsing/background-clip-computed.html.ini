# Known engine gap, tracked as BUG-472: `getComputedStyle()` resolved-value
# coverage gaps -- `computed_style_to_map` (selector_query.rs) is a
# hand-written ~64-entry map; any property outside it (background-*
# longhands, border-*-color/-style/-width/-radius longhands, the
# border-width shorthand) silently returns "" instead of the resolved
# value, even though the property is implemented for real rendering.
# Observed via the structured wptreport (WPT-RUN-3 sl.9, css/css-backgrounds
# mass run). Re-run and flip to PASS once BUG-472 lands.

[background-clip-computed.html]
  [Property background-clip value 'border-box']
    expected: FAIL
  [Property background-clip value 'padding-box']
    expected: FAIL
  [Property background-clip value 'content-box']
    expected: FAIL
  [Property background-clip value 'border-area']
    expected: FAIL
  [Property background-clip value 'border-box, padding-box, content-box, border-area']
    expected: FAIL
  [Property background-clip value 'text']
    expected: FAIL
  [Property background-clip value 'border-area text']
    expected: FAIL
  [Property background-clip value 'text border-area']
    expected: FAIL
  [Property background-clip value 'border-box' multiple base values]
    expected: FAIL
  [Property background-clip value 'border-box, padding-box' multiple base values]
    expected: FAIL
  [Property background-clip value 'border-box, padding-box, content-box' multiple base values]
    expected: FAIL
