# Known engine gap, tracked as BUG-494: element.style = "css text" (whole-
# string assignment) is a silent no-op -- style is a getter-only accessor
# on the JS shim's element wrapper (crates/js/src/dom.rs:5613), missing the
# WebIDL [PutForwards=cssText] forwarding to style.cssText = value.
# Every test in this file uses exactly that pattern, so no background
# property is ever actually applied before being read back.
# Observed via the structured wptreport (WPT-RUN-3 sl.9, css/css-backgrounds
# mass run). Re-run and flip to PASS once BUG-494 lands.

[background-shorthand-serialization.html]
  [single value]
    expected: FAIL
  [multiple values]
    expected: FAIL
  [multiple backgrounds]
    expected: FAIL
  [background-size with non-initial background-position]
    expected: FAIL
  [multiple backgrounds with varying values]
    expected: FAIL
  [all initial values]
    expected: FAIL
  [border-area border-box]
    expected: FAIL
  [border-area padding-box]
    expected: FAIL
  [border-area text sets background-clip]
    expected: FAIL
  [text border-area serializes as border-area text]
    expected: FAIL
  [border-area text sets background-origin to border-box]
    expected: FAIL
