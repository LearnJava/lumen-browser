# Known engine gap, tracked as BUG-496: `HTMLElement.dataset`
# (DOMStringMap) is entirely unimplemented -- zero matches for "dataset"
# anywhere in crates/js/src/dom.rs. The test script crashes on
# `div.dataset.expected` before registering any test(), so the harness
# times out with zero subtests.
# Observed via the structured wptreport (WPT-RUN-3 sl.9, css/css-backgrounds
# mass run). Re-run and flip to PASS once BUG-496 lands.

[border-width-cssom.html]
  expected: TIMEOUT
