# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- id="container" gives neither window.container nor a
# bare `container` identifier. The test script crashes on the first bare
# reference before registering any test(), so the harness times out with
# zero subtests.
# Observed via the structured wptreport (WPT-RUN-3 sl.9, css/css-backgrounds
# mass run). Re-run and flip to PASS once BUG-384 lands.

[inheritance.sub.html]
  expected: TIMEOUT
