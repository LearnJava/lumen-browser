# Known engine gap, tracked as BUG-497: CSSStyleDeclaration.cssText
# serialization omits the trailing semicolon after a declaration
# ("border-radius: 10px" instead of spec-required "border-radius: 10px;").
# Observed via the structured wptreport (WPT-RUN-3 sl.9, css/css-backgrounds
# mass run). Re-run and flip to PASS once BUG-497 lands.

[border-radius-css-text.html]
  [Setting border-radius does not expand to longhand properties in cssText.]
    expected: FAIL
