# Known test-infra gap: /common/worklet-reftest.js is not vendored (whole
# tests/wpt/common/ directory absent) -- "importWorklet is not defined"
# throws before setup({explicit_done:true})'s done() is ever reached, which
# blocks harness completion entirely (0 registered subtests) instead of
# surfacing as a harness ERROR. Confirmed by re-running this exact file over
# plain HTTP (bypassing the unrelated HTTPS-port gap): same TIMEOUT/0-subtests
# shape. Not an engine defect in CSS Paint API parsing itself.
#
# On top of that, the preceding test (idlharness.html) also TIMEOUTs, and
# BUG-380 (browsing-context reuse, executor doesn't verify navigation
# outcome) surfaces idlharness.html's stale results here instead --
# `AssertionError: Got results from /css/css-paint-api/idlharness.html,
# expected .../paint-function-valid.https.html`. Same pattern as
# css-font-loading/idlharness.https.html.ini.
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-paint-api.
# Re-run and flip to PASS once /common/worklet-reftest.js is vendored (and
# BUG-380 lands).

[paint-function-valid.https.html]
  expected: ERROR
