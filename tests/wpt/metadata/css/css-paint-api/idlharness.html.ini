# Known test-infra gap: resources/idlharness.js is not vendored (HTTP 404) --
# script load failure blocks the whole harness (0 registered subtests),
# not an engine defect. Same pattern as css-typed-om (WPT-RUN-3 slice 37).
# WPT-RUN-3 slice 38 mass run (structured wptreport JSON), css/css-paint-api.
# Re-run and flip to PASS once resources/idlharness.js is vendored.

[idlharness.html]
  expected: TIMEOUT
