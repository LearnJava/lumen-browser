# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[style-query-registered-custom-em-change.html]
  [Initially, 1em (16px) + 10px evaluates to 26px. Target does not match.]
    expected: FAIL
  [Changing the container's font-size to 10px makes 1em (10px) + 10px evaluate to 20px. Target matches.]
    expected: FAIL
