# BUG-501: CSS.supports(conditionText) single-arg form doesn't fall back to wrapping a bare declaration in parens
# WPT-RUN-3 slice 30 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[scroll-state-snapped-snap-changing.html]
  expected: ERROR
