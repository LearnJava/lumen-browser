# + residual subtests this slice, not individually clustered.
# WPT-RUN-3 slice 31 mass run (structured wptreport JSON).
# Re-run and flip to PASS once the cited bug(s) land.

[text-spacing-trim-fit-width.html]
  [[consistent\] Check that text-spacing-trim: trim-start scales larger than none because of trimmed start margin]
    expected: FAIL
  [[per-line-all\] Check that text-spacing-trim: trim-start scales larger than none because of trimmed start margin]
    expected: FAIL
