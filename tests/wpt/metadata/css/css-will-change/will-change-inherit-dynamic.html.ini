# Known engine gap, tracked as BUG-384: named access on Window (bare-id) is
# not implemented -- the test references a <div id=outer> as a bare
# identifier, which throws ReferenceError instead of resolving.
# Observed via WPT-RUN-3 slice 24 (css-will-change mass run).

[will-change-inherit-dynamic.html]
  [will-change change is properly propagated to children if needed]
    expected: FAIL
