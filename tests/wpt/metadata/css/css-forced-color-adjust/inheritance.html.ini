# Known engine gap, tracked as BUG-512: `forced-color-adjust` (Forced
# Colors Mode L1, shipped in every evergreen browser) is not implemented
# at all -- absent from ComputedStyle.
# Observed via the structured wptreport (WPT-RUN-3 sl.21, css/css-link-
# params + css-forced-color-adjust + css-size-adjust + css-device-adapt +
# css-env + css-overscroll-behavior mass run). Re-run and flip to PASS
# once the bug lands.


[inheritance.html]
  [Property forced-color-adjust has initial value auto]
    expected: FAIL
  [Property forced-color-adjust inherits]
    expected: FAIL
