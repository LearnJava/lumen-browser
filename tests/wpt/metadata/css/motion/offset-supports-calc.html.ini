# Known engine gap, tracked as BUG-384: named access on Window is not
# implemented -- an element with `id="x"` gives neither `window.x` nor a
# bare global `x`, so any inline <script> that refers to an id-named
# element as a bare identifier throws ReferenceError. Observed via the
# structured wptreport (WPT-RUN-3 sl.28, css/motion mass run). Re-run and
# flip to PASS once BUG-384 lands.

[offset-supports-calc.html]
  [offset-position supports calc]
    expected: FAIL
  [offset-path supports calc]
    expected: FAIL
  [offset-distance supports calc]
    expected: FAIL
  [offset-rotate supports calc]
    expected: FAIL
  [offset-anchor supports calc]
    expected: FAIL
