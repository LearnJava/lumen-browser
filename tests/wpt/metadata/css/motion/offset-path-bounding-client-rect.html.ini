# Known engine gap, tracked as BUG-540: getBoundingClientRect() ignores the
# offset-path motion-path transform -- confirmed via --dump-layout that the
# box's layout rect stays untransformed, while CSS-SPECS.md documents the
# path repositioning as applied purely at paint time. Observed via the
# structured wptreport (WPT-RUN-3 sl.28, css/motion mass run). Re-run and
# flip to PASS once BUG-540 lands.

[offset-path-bounding-client-rect.html]
  [Bounding client rect for #blue]
    expected: FAIL
  [Bounding client rect for #purple]
    expected: FAIL
