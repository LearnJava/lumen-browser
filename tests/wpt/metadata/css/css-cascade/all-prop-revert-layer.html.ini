# Known engine gap, tracked as BUG-483 (second symptom, see bug file):
# window.getComputedStyle() returns a Proxy over a bare `{}` with no
# Symbol.iterator and hardcoded length=0/item()='' -- `for (const p of
# getComputedStyle(el))` throws synchronously before any test() registers.
# Observed end to end by wptrunner (WPT-RUN-3 sl.6, css/css-cascade mass
# run). Re-run and flip to PASS once BUG-483 lands.


[all-prop-revert-layer.html]
  expected: TIMEOUT
