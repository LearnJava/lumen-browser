# Known engine gap, tracked as BUG-384: named access on Window (bare-id)
# is not implemented -- a bare identifier referencing an element by its
# id throws ReferenceError instead of resolving.
# Observed via WPT-RUN-3 slice 24 (css-content mass run).

[element-replacement-dynamic-002.html]
  [getComputedStyle().height after ancestor becomes replaced]
    expected: FAIL
