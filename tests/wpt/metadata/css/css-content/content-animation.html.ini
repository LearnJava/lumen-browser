# Known engine gap, tracked as BUG-463: Web Animations animate() is not
# installed on the prototype (or doesn't support this property).
# Observed via WPT-RUN-3 slice 24 (css-content mass run).

[content-animation.html]
  [The content property can be animated with a discrete animation type.]
    expected: FAIL
