# Known engine gap, tracked as BUG-539: window.getComputedStyle() returns a
# Proxy with no has-trap -- the `in` operator against getComputedStyle(el)
# is always false, even for implemented properties, which
# computed-testcommon.js-style helpers probe before checking the actual
# value.
# Observed via the structured wptreport (WPT-RUN-3 sl.29).
# Re-run and flip to PASS once BUG-539 lands.

[animation-delay-computed.html]
  [Property animation-delay value '-500ms']
    expected: FAIL
  [Property animation-delay value 'calc(2 * 3s)']
    expected: FAIL
  [Property animation-delay value '20s, 10s']
    expected: FAIL
  [Property animation-delay value 'calc(10s + (sign(2cqw - 10px) * 5s))']
    expected: FAIL
