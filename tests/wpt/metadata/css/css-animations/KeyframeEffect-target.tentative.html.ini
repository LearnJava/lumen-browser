# Known engine gap, tracked as BUG-485: document.head is undefined -- no
# getter/native binding exists (unlike document.body). The shared
# parsing-testcommon.js test_valid_selector/test_invalid_selector helper
# (or a direct document.head.appendChild call) throws on
# 'Cannot read properties of undefined (reading append/appendChild)'
# before the test's own assertion ever runs.
# Observed via the structured wptreport (WPT-RUN-3 sl.29).
# Re-run and flip to PASS once BUG-485 lands.

# Known engine gap, tracked as BUG-536: CSS Transitions/Animations produce
# no JS-observable Animation/CSSAnimation object -- getAnimations() never
# reports a running CSS-triggered animation, cascading into
# 'Cannot read properties of undefined (reading effect/ready/pending/...)'.
# Observed via the structured wptreport (WPT-RUN-3 sl.29).
# Re-run and flip to PASS once BUG-536 lands.

[KeyframeEffect-target.tentative.html]
  [Returned CSS animations have the correct effect target]
    expected: FAIL
  [effect.target should return the same CSSPseudoElement object each time]
    expected: FAIL
  [effect.target from the script-generated animation should return the same CSSPseudoElement object as that from the CSS generated animation]
    expected: FAIL
