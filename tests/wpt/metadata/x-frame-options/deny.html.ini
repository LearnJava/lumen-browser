[deny.html]
  expected: TIMEOUT
  [`DENY` blocks same-origin framing]
    expected: TIMEOUT

  [`DENY` blocks cross-origin framing]
    expected: TIMEOUT

  [`denY` blocks same-origin framing]
    expected: TIMEOUT

  [`denY` blocks cross-origin framing]
    expected: TIMEOUT

  [`  DENY ` blocks same-origin framing]
    expected: TIMEOUT

  [`  DENY ` blocks cross-origin framing]
    expected: TIMEOUT

  [`DENY` blocks same-origin framing with CSP default-src 'self']
    expected: TIMEOUT

  [`DENY` blocks cross-origin framing with CSP default-src 'self']
    expected: TIMEOUT

  [`DENY` allows same-origin framing with CSP frame-ancestors 'self']
    expected: TIMEOUT

  [`DENY` blocks cross-origin framing with CSP frame-ancestors 'self']
    expected: TIMEOUT
