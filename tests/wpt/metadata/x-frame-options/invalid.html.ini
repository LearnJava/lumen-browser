[invalid.html]
  expected: TIMEOUT
  [`INVALID` allows same-origin framing]
    expected: TIMEOUT

  [`INVALID` allows cross-origin framing]
    expected: TIMEOUT

  [`ALLOW-FROM https://example.com/` allows same-origin framing]
    expected: TIMEOUT

  [`ALLOW-FROM https://example.com/` allows cross-origin framing]
    expected: TIMEOUT

  [`ALLOW-FROM=https://example.com/` allows same-origin framing]
    expected: TIMEOUT

  [`ALLOW-FROM=https://example.com/` allows cross-origin framing]
    expected: TIMEOUT

  [`ALLOWALL` allows same-origin framing]
    expected: TIMEOUT

  [`ALLOWALL` allows cross-origin framing]
    expected: TIMEOUT

  [`"DENY"` allows same-origin framing]
    expected: TIMEOUT

  [`"DENY"` allows cross-origin framing]
    expected: TIMEOUT

  [`DE NY` allows same-origin framing]
    expected: TIMEOUT

  [`DE NY` allows cross-origin framing]
    expected: TIMEOUT

  [`"SAMEORIGIN"` allows same-origin framing]
    expected: TIMEOUT

  [`"SAMEORIGIN"` allows cross-origin framing]
    expected: TIMEOUT

  [`"SAMEORIGIN,DENY"` allows same-origin framing]
    expected: TIMEOUT

  [`"SAMEORIGIN,DENY"` allows cross-origin framing]
    expected: TIMEOUT

  [`(the empty string)` allows same-origin framing]
    expected: TIMEOUT

  [`(the empty string)` allows cross-origin framing]
    expected: TIMEOUT
