[sameorigin.sub.html]
  expected: TIMEOUT
  [`SAMEORIGIN` allows same-origin framing]
    expected: TIMEOUT

  [`SAMEORIGIN` blocks cross-origin framing]
    expected: TIMEOUT

  [`sameOriGin` allows same-origin framing]
    expected: TIMEOUT

  [`sameOriGin` blocks cross-origin framing]
    expected: TIMEOUT

  [`  SAMEORIGIN ` allows same-origin framing]
    expected: TIMEOUT

  [`  SAMEORIGIN ` blocks cross-origin framing]
    expected: TIMEOUT

  [SAMEORIGIN allows same-origin nested in same-origin framing]
    expected: TIMEOUT

  [SAMEORIGIN blocks cross-origin nested in same-origin framing]
    expected: TIMEOUT

  [SAMEORIGIN blocks same-origin nested in cross-origin framing]
    expected: TIMEOUT

  [SAMEORIGIN blocks cross-origin nested in cross-origin framing]
    expected: TIMEOUT
