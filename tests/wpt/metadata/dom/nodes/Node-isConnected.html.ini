# Node.isConnected implemented in WEB_API_SHIM (BUG-311, FIXED 2026-07-19):
# ancestor-chain reachability to documentElement. "Test with ordinary child
# nodes" now PASSes. "Test with iframes" still FAILs — it relies on separate
# iframe sub-documents via `contentDocument`, which the shim does not model as
# distinct connected trees (independent engine gap, not weakened here).
[Node-isConnected.html]
  [Test with iframes]
    expected: FAIL
