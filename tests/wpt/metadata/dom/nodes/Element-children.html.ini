# BUG-310 landed `children` as a live HTMLCollection with `item`/`namedItem`,
# but these two subtests stay FAIL by design: they assert the full HTMLCollection
# named-getter *enumeration* semantics (exact `Object.getOwnPropertyNames` order,
# non-enumerable named props) AND depend on `createElementNS("", ...)` producing a
# non-HTML-namespace element — Lumen currently folds every non-SVG namespace to
# HTML (`_lumen_create_element_ns`), so the `name`-exposure rule can't be honoured.
# Both are out of BUG-310's ElementTraversal scope; tracked for a follow-up.
[Element-children.html]
  [Element-children]
    expected: FAIL
  [Element-children 1]
    expected: FAIL
