# BUG-310 landed `children` as a live HTMLCollection with `item`/`namedItem`.
# Subtest names corrected 2026-07-20: this file has no `test(fn, name)` calls, so
# testharness.js names each unnamed test from `document.title` ("HTMLCollection
# edge cases" / "HTMLCollection edge cases 1"), NOT from the filename — the
# previous `.ini` pinned the wrong names (`Element-children`/`Element-children 1`),
# so neither ever matched a real result and the gate showed unexpected FAIL
# instead of a recognized, tracked expectation.
#
# Both subtests were `expected: FAIL` (BUG-322: no native element wrapper had a
# `[[Prototype]]` set, so every `instanceof Element` check failed; BUG-323: the
# HTMLCollection Proxy had no `ownKeys`/`getOwnPropertyDescriptor` traps). Both
# bugs are now fixed — flipped to `expected: PASS` 2026-07-21. If a fresh
# `run_report.py --all` run shows either one still red, re-open the relevant bug
# instead of silently reverting this file to FAIL.
#
# 2026-08-02 (WPT-RUN-2 gate check): "HTMLCollection edge cases 1" (the
# `ownKeys`/enumeration-order subtest) is red again — BUG-328 (still OPEN,
# filed 2026-07-22, same symptom: a `qux` named-property leak from a
# `createElementNS("", ...)` element plus wrong enumeration order). Flipped
# back to `expected: FAIL`, re-flip only when BUG-328 is closed. The other
# subtest ("HTMLCollection edge cases", `.item("foo")` index coercion) passes
# now — left at `expected: PASS`; BUG-328 also described it as failing on
# 2026-07-22, so something else fixed that half in the interim.
#
# 2026-08-05: BUG-328 fixed (Namespace::None + single tree-order id-then-name
# pass in `_lumen_html_collection_named`/`_lumen_html_collection_own_names`,
# `crates/js/src/dom.rs`). Both subtests pass now — flipped to `expected: PASS`.
[Element-children.html]
  [HTMLCollection edge cases]
    expected: PASS
  [HTMLCollection edge cases 1]
    expected: PASS
