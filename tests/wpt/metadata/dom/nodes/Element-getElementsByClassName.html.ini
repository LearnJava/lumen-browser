# getElementsByClassName landed (BUG-302, FIXED) — the "disconnected subtrees"
# subtest now passes. The other two stay FAIL by design: BUG-302's fix returns
# a static array (same simplified semantics as the neighboring
# getElementsByTagName), not a live HTMLCollection, so `instanceof
# HTMLCollection` and live-collection-after-mutation checks still fail.
[Element-getElementsByClassName.html]
  [getElementsByClassName should work on disconnected subtrees.]
    expected: PASS
  [Interface should be correct.]
    expected: FAIL
  [getElementsByClassName() should be a live collection]
    expected: FAIL
