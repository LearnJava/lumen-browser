# Async DOM test (P2-wpt S6 curated async subset). The MutationObserver callback
# fires asynchronously (microtask delivery works — the harness reaches `OK`, not
# `TIMEOUT`), proving the async completion + polling pipeline end to end. The one
# subtest fails only because the `MutationRecord` interface constructor is not
# exposed as a global, tracked as BUG-317 (BUG-314 family) — not a weakened test.
# Flip the FAIL to PASS once BUG-317 lands.
[MutationObserver-callback-arguments.html]
  [Callback is invoked with |this| value of MutationObserver and two arguments]
    expected: FAIL
