# Genuine engine gap (Element.hasAttributes() missing), tracked as BUG-312. Observed
# end to end by wptrunner (P2-wpt S5 curated subset) — not a weakened
# test. Flip the FAIL(s) to PASS once BUG-312 lands.
[Element-hasAttributes.html]
  [element.hasAttributes() must return false when the element does not have attribute.]
    expected: FAIL
  [element.hasAttributes() must return true when the element has attribute.]
    expected: FAIL
