# Async DOM test (P2-wpt S6 curated async subset). This test exercises
# wptrunner's async-test timeout driving against Lumen: subtree childList
# mutations are never delivered to the observer, so both async subtests run to
# the testharness timeout and the harness reports `TIMEOUT` — recorded here as
# the genuine expectation (wptrunner drives + reports the timeout correctly).
# Tracked as BUG-318 (MutationObserver subtree delivery) — not a weakened test.
# Flip the TIMEOUTs to PASS once BUG-318 lands.
[MutationObserver-disconnect.html]
  expected: TIMEOUT
  [subtree mutations]
    expected: TIMEOUT
  [disconnect discarded some mutations]
    expected: TIMEOUT
