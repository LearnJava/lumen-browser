# Async DOM test (P2-wpt S6 curated async subset). MutationObserver subtree
# delivery landed (BUG-318, FIXED) — the harness now completes `OK` instead of
# timing out. Validated end to end by wptrunner.
[MutationObserver-disconnect.html]
  [subtree mutations]
    expected: PASS
  [disconnect discarded some mutations]
    expected: PASS
