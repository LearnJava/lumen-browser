[Element-hasAttribute.html]
  # Lumen's JS engine has no `Element.prototype.setAttributeNS` yet — the first
  # subtest constructs an attribute via `setAttributeNS` and so throws
  # `TypeError: el.setAttributeNS is not a function`. Tracked as BUG-309. The
  # second subtest (case-insensitive `hasAttribute`) passes. This expectation
  # records the *genuine* engine result the harness observes end to end — it is
  # not a weakened test; flip it to PASS when BUG-309 lands.
  [hasAttribute should check for attribute presence, irrespective of namespace]
    expected: FAIL
