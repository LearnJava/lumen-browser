# createProcessingInstruction + XML Name validation landed (BUG-313), so all 8
# INVALID_CHARACTER_ERR subtests now PASS and are no longer listed. The 3
# remaining "Should get a ProcessingInstruction" subtests still FAIL because
# they assert `pi instanceof ProcessingInstruction` / `pi instanceof Node`,
# which needs the DOM interfaces exposed as globals — tracked as BUG-314.
# Flip these to PASS once BUG-314 lands.
[Document-createProcessingInstruction.html]
  [Should get a ProcessingInstruction for target "xml:fail" and data "x".]
    expected: FAIL
  [Should get a ProcessingInstruction for target "A·A" and data "x".]
    expected: FAIL
  [Should get a ProcessingInstruction for target "a0" and data "x".]
    expected: FAIL
