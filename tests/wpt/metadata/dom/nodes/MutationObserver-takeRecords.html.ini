# Async DOM test (P2-wpt S6 curated async subset). The observer callback is
# delivered asynchronously (harness reaches `OK`), but MutationObserver record
# bookkeeping is incorrect under the V8 shim: extra/duplicate records are
# reported and `takeRecords()` does not clear the pending queue. Tracked as
# BUG-316 — not a weakened test. Flip the FAIL(s) to PASS once BUG-316 lands.
[MutationObserver-takeRecords.html]
  [unreachabled test]
    expected: FAIL
  [All records present]
    expected: FAIL
  [No more records present]
    expected: FAIL
