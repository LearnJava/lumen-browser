# BUGS.md — Баг-трекер Lumen Browser

Живой список известных багов движка. История прогонов — в `graphic_tests/results/*.json` (коммитируются).

**Как добавить баг:**
1. Создай файл `bugs/BUG-NNN-OPEN.md` (следующий номер по счёту, сейчас BUG-321)
2. Добавь строку в таблицу ниже со ссылкой на файл

**При изменении статуса:** переименуй файл (`BUG-NNN-OPEN.md` → `BUG-NNN-FIXED.md`) и обнови ссылку в таблице.

**Статусы:** `OPEN` · `IN PROGRESS` · `FIXED <date>` · `WONTFIX (Phase N+)`

---

## Список багов

| ID | Статус | Компонент | Описание |
|---|---|---|---|
| [BUG-001](bugs/BUG-001-FIXED.md) | FIXED 2026-05-15 | layout | display:none on inline elements not working |
| [BUG-002](bugs/BUG-002-FIXED.md) | FIXED 2026-05-20 | layout/paint | inline padding/border/margin stacks vertically instead of flowing |
| [BUG-003](bugs/BUG-003-FIXED.md) | FIXED 2026-05-15 | layout | style="" attribute not processed by cascade |
| [BUG-004](bugs/BUG-004-FIXED.md) | FIXED 2026-05-24 | layout | height on inline elements (display:inline-block applies; display:inline ignores per CSS 2.1 §10.6.1) |
| [BUG-005](bugs/BUG-005-FIXED.md) | FIXED 2026-05-21 | layout+paint | `<img>` inside `<span>` not rendered |
| [BUG-006](bugs/BUG-006-FIXED.md) | FIXED 2026-05-21 | layout | table layout not implemented (td/th render as blocks) |
| [BUG-007](bugs/BUG-007-FIXED.md) | FIXED 2026-05-20 | layout | `<sub>`/`<sup>`/`<small>` missing UA styles |
| [BUG-008](bugs/BUG-008-FIXED.md) | FIXED 2026-05-20 | layout | `<del>`/`<ins>`/`<u>`/`<s>` text-decoration missing UA styles |
| [BUG-009](bugs/BUG-009-FIXED.md) | FIXED 2026-05-20 | layout | `<a>` missing UA styles (no blue color, no underline) |
| [BUG-010](bugs/BUG-010-FIXED.md) | FIXED 2026-05-20 | layout | `<hr>` renders nothing |
| [BUG-011](bugs/BUG-011-FIXED.md) | FIXED 2026-05-22 | layout/paint | list markers (bullet, numbers) not rendered |
| [BUG-012](bugs/BUG-012-FIXED.md) | FIXED 2026-05-20 | layout | `<del>`/`<ins>` break inline flow (each on new line) |
| [BUG-013](bugs/BUG-013-FIXED.md) | FIXED 2026-05-22 | layout | adjacent `<span style="...">` stack vertically without separator |
| [BUG-014](bugs/BUG-014-FIXED.md) | FIXED 2026-05-21 | image | JPEG not decoded (PNG only) |
| [BUG-015](bugs/BUG-015-FIXED.md) | FIXED 2026-05-25 | paint | broken `<img>` src shows no alt text |
| [BUG-016](bugs/BUG-016-FIXED.md) | FIXED 2026-05-20 | css-parser/paint | border-style: dashed/double now work; dotted still square (→ BUG-029) |
| [BUG-017](bugs/BUG-017-FIXED.md) | FIXED 2026-05-22 | layout/paint | text-decoration-style ignored (all render as solid) |
| [BUG-018](bugs/BUG-018-FIXED.md) | FIXED 2026-05-22 | layout | text-decoration-color ignored (always inherits text color) |
| [BUG-019](bugs/BUG-019-FIXED.md) | FIXED 2026-05-20 | css-parser/paint | outline not rendered at all |
| [BUG-020](bugs/BUG-020-FIXED.md) | FIXED 2026-05-26 | layout | overflow axis coercion: visible+hidden combo не клипало ось; CSS Overflow L3 §2.1 visible→auto в compute_style; TEST-14: 1.70%→0.03% PASS |
| [BUG-021](bugs/BUG-021-FIXED.md) | FIXED 2026-05-22 | html-parser | HTML bgcolor attribute ignored |
| [BUG-022](bugs/BUG-022-FIXED.md) | FIXED 2026-05-22 | css-parser | Quirks-mode hashless hex colors not parsed |
| [BUG-023](bugs/BUG-023-FIXED.md) | FIXED 2026-05-26 | layout+paint | opacity deviation — P1: strut fix 2026-05-26; P5 paint: premultiplied alpha double-mult at edge-AA pixels in composite shader → TEST-13 0.24% |
| [BUG-024](bugs/BUG-024-FIXED.md) | FIXED 2026-05-21 | layout | box-sizing: content-box — border not added to outer size; height% resolved against width |
| [BUG-025](bugs/BUG-025-FIXED.md) | FIXED 2026-05-22 | layout | max-height does not clamp block height; InlineSpace not included in shrink-to-fit width |
| [BUG-026](bugs/BUG-026-FIXED.md) | FIXED 2026-05-22 | layout/paint | `<img>` CSS/HTML width+height ignored — renders at natural size (remaining TEST-18 ~10%: BUG-032) |
| [BUG-027](bugs/BUG-027-FIXED.md) | FIXED 2026-05-20 | layout | block element ignores explicit width — body stretches to viewport |
| [BUG-028](bugs/BUG-028-FIXED.md) | FIXED 2026-05-26 | shell | relayout-on-resize + maximized window triggers BUG-027 |
| [BUG-029](bugs/BUG-029-FIXED.md) | FIXED 2026-05-21 | paint | border-style: dotted renders square dots instead of circles |
| [BUG-030](bugs/BUG-030-FIXED.md) | FIXED 2026-05-20 | layout | IFC: no whitespace gap between inline-block siblings (CSS §4.1.2) |
| [BUG-031](bugs/BUG-031-FIXED.md) | FIXED 2026-05-20 | layout | IFC: missing strut descent causes rows to be ~4px too short |
| [BUG-032](bugs/BUG-032-FIXED.md) | FIXED 2026-05-22 | paint/image | object-fit image quality ~16%: area averaging заменяет bilinear при downscale |
| [BUG-033](bugs/BUG-033-FIXED.md) | FIXED 2026-05-22 | paint | box-shadow: нет Gaussian blur — рендерится solid прямоугольник вместо размытой тени |
| [BUG-034](bugs/BUG-034-FIXED.md) | FIXED 2026-05-22 | layout | transform-origin 50% 50% default not resolved against box size — pivot at (0,0) instead of center |
| [BUG-035](bugs/BUG-035-FIXED.md) | FIXED 2026-05-22 | layout | ::before/::after pseudo-elements не генерируются в box_tree (реализация частичная) |
| [BUG-036](bugs/BUG-036-FIXED.md) | FIXED 2026-05-26 | layout | border-radius: % значения (50%, etc.) не резолвятся → radius=0; только px работает |
| [BUG-037](bugs/BUG-037-FIXED.md) | FIXED 2026-05-26 | paint | CSS filter effects не применяются визуально (grayscale/sepia/blur/etc.) — shared filter_uniform перезаписывался; fix: per-pass буфер через mapped_at_creation |
| [BUG-038](bugs/BUG-038-FIXED.md) | FIXED 2026-05-26 | layout | list-style-position: inside — маркер занимал отдельную строку; li высотой 2× от нормы; fix: не продвигать child_y, сдвигать InlineRun вправо на marker_w |
| [BUG-039](bugs/BUG-039-FIXED.md) | FIXED 2026-05-26 | paint | dashed/dotted border mismatch vs Chrome/Edge: dash ratio 3:1→Skia algo, corner squares→circle quads for dotted, 1px linear SDF AA |
| [BUG-040](bugs/BUG-040-FIXED.md) | FIXED 2026-05-27 | layout | table layout unit tests assume direct `<tr>` children of `<table>`; html-full-tree-builder now injects implicit `<tbody>` breaking them |
| [BUG-041](bugs/BUG-041-FIXED.md) | FIXED 2026-05-27 | css-parser | style::tests::line_clamp_integer_value / _standard_property / _not_inherited fail: CSS rule `div { -webkit-line-clamp: 3 }` produces None — test accesses doc.root().children[0] which is `<html>` after full HTML5 parsing, so rule doesn't match `<div>` |
| [BUG-042](bugs/BUG-042-FIXED.md) | FIXED 2026-05-29 | js | QuickJsRuntime missing JsRuntime::resume() impl — all lumen-js tests fail to compile |
| [BUG-043](bugs/BUG-043-FIXED.md) | FIXED 2026-05-29 | paint | lumen-paint test suite красный (19 падений): устаревшие golden + overflow coercion + half-leading |
| [BUG-044](bugs/BUG-044-FIXED.md) | FIXED 2026-05-29 | shell | lumen-shell не компилируется: non-exhaustive match по DisplayCommand — новые варианты PushMaskLayer/PopMaskLayer/DrawSvgPath/BoxModelOverlay |
| [BUG-045](bugs/BUG-045-FIXED.md) | FIXED 2026-05-29 | layout | backdrop-filter не создавал stacking context |
| [BUG-046](bugs/BUG-046-FIXED.md) | FIXED 2026-05-30 | layout | 3 устаревших теста lumen-layout --lib: webp теперь декодируется → picture-тесты обновлены |
| [BUG-047](bugs/BUG-047-FIXED.md) | FIXED 2026-05-30 | layout | НЕ баг (мисдиагноз): line-clamp реально усекает контент — тест переписан на ground-truth |
| [BUG-048](bugs/BUG-048-FIXED.md) | FIXED 2026-05-30 | shell | lumen-shell не компилируется: non-exhaustive match — новый вариант DrawScrollbar |
| [BUG-049](bugs/BUG-049-FIXED.md) | FIXED 2026-05-30 | shell | lumen-shell не компилируется: non-exhaustive match — новый вариант PageBreak |
| [BUG-050](bugs/BUG-050-FIXED.md) | FIXED 2026-05-31 | network | doctest mock.rs:16 не компилировался — `use NetworkTransport` не импортирован |
| [BUG-051](bugs/BUG-051-FIXED.md) | FIXED 2026-05-31 | layout | abs-pos с top+bottom+height:auto (inset:0) схлопывался в height 0 |
| [BUG-052](bugs/BUG-052-FIXED.md) | FIXED 2026-05-31 | paint/cpu_raster | DrawBorder anti_alias:true → паника в debug-профиле для sub-pixel рамок |
| [BUG-053](bugs/BUG-053-FIXED.md) | FIXED 2026-06-02 | shell | `cargo build -p lumen-shell --features quickjs` не компилировался: trait PersistentJs потерял декларации |
| [BUG-054](bugs/BUG-054-FIXED.md) | FIXED 2026-06-04 | network | stale_pooled_connection_triggers_retry падает на Windows (WSAECONNRESET) |
| [BUG-055](bugs/BUG-055-FIXED.md) | FIXED 2026-06-04 | layout | tests::collect_picture_unsupported_type_falls_back: AVIF теперь поддерживается |
| [BUG-056](bugs/BUG-056-FIXED.md) | FIXED 2026-06-03 | shell | font_registry used after move in parse_and_layout: clippy E0382 |
| [BUG-057](bugs/BUG-057-FIXED.md) | FIXED 2026-06-03 | paint | wgpu Vulkan crash on first render — fix: DX12 backend по умолчанию на Windows |
| [BUG-058](bugs/BUG-058-FIXED.md) | FIXED 2026-06-04 | layout | display:contents не сглажен перед lay_out: паника при открытии cnn.com |
| [BUG-059](bugs/BUG-059-FIXED.md) | FIXED 2026-06-04 | font | WOFF2-декодер отклоняет шрифты с контурами из 0 точек |
| [BUG-060](bugs/BUG-060-FIXED.md) | FIXED 2026-06-04 | font | WOFF2-декодер обрывается с «unexpected end of font data» — routing потоков |
| [BUG-061](bugs/BUG-061-FIXED.md) | FIXED 2026-06-04 | driver | test_32_list_markers падал: ожидания не обновлены после добавления секций |
| [BUG-062](bugs/BUG-062-FIXED.md) | FIXED 2026-06-04 | network | clippy «very complex type» в doh.rs → type alias DnsCacheMap |
| [BUG-063](bugs/BUG-063-FIXED.md) | FIXED 2026-06-04 | layout | clippy: manual_clamp, dead_code, collapsible_if в mathml.rs |
| [BUG-064](bugs/BUG-064-FIXED.md) | FIXED 2026-06-08 | driver | test_33_multi_column падал: ожидания не обновлены после изменения высот |
| [BUG-065](bugs/BUG-065-FIXED.md) | FIXED 2026-06-04 | shell | Клик по `<a href>` не срабатывал: hit-test не вычитал TAB_BAR_HEIGHT |
| [BUG-066](bugs/BUG-066-FIXED.md) | FIXED 2026-06-07 | paint | render_tile() без cfg(cpu-render) вызывает cpu_raster → clippy падает |
| [BUG-067](bugs/BUG-067-FIXED.md) | FIXED 2026-06-08 | js | EventTarget не определён глобально → `class X extends EventTarget` бросает ReferenceError |
| [BUG-068](bugs/BUG-068-FIXED.md) | FIXED 2026-06-08 | shell | clippy: collapsible_if в reader_view.rs |
| [BUG-069](bugs/BUG-069-FIXED.md) | FIXED 2026-06-08 | image | collect_picture_unsupported_type_falls_back: stub-форматы jxl/heic/heif убраны из supported_mime_types |
| [BUG-070](bugs/BUG-070-FIXED.md) | FIXED 2026-06-08 | js | Дубликат BUG-067 (тот же корень: EventTarget) |
| [BUG-071](bugs/BUG-071-FIXED.md) | FIXED 2026-06-08 | mcp | MockSession не реализует set_clock/set_rng_seed/freeze_fingerprint |
| [BUG-072](bugs/BUG-072-FIXED.md) | FIXED 2026-06-08 | js | Form Constraint Validation API: ReferenceError «HTMLInputElement is not defined» |
| [BUG-073](bugs/BUG-073-FIXED.md) | FIXED 2026-06-08 | js | chrome_runtime_absent ломается: window.chrome.runtime ставился безусловно |
| [BUG-074](bugs/BUG-074-FIXED.md) | FIXED 2026-06-08 | layout | height:100% на flex-item не резолвится → TEST-67 bar рендерится h=0 |
| [BUG-075](bugs/BUG-075-FIXED.md) | FIXED 2026-06-08 | layout | display:table без явной ширины растягивается до контейнера вместо shrink-to-fit |
| [BUG-076](bugs/BUG-076-FIXED.md) | FIXED 2026-06-11 | paint | box-shadow blur spread ~1% deviation — TEST-15: 1.06% |
| [BUG-077](bugs/BUG-077-FIXED.md) | FIXED 2026-06-09 | image/paint | femtovg-бэкенд алиасинг при downscale — area avg ресемплинг |
| [BUG-078](bugs/BUG-078-FIXED.md) | FIXED 2026-06-11 | layout/paint | object-fit contain/cover image quality ~13% deviation — TEST-19: 12.68% |
| [BUG-079](bugs/BUG-079-FIXED.md) | FIXED 2026-06-14 | layout | quirks-bgcolor: hashless-hex quirk применялся к шортхенду `background:` |
| [BUG-080](bugs/BUG-080-FIXED.md) | FIXED 2026-06-11 | paint | border-style: residual dotted/dashed 3% deviation — TEST-21: 3.02% |
| [BUG-081](bugs/BUG-081-FIXED.md) | FIXED 2026-06-11 | layout | vertical-align: sub-pixel 0.99% deviation |
| [BUG-082](bugs/BUG-082-FIXED.md) | FIXED 2026-06-11 | paint | css-filter 33% deviation — TEST-30: 33.07% |
| [BUG-083](bugs/BUG-083-FIXED.md) | FIXED 2026-06-11 | layout/paint | list-markers residual 3.4% deviation |
| [BUG-084](bugs/BUG-084-FIXED.md) | FIXED 2026-06-12 | paint | border-radius residual 1.5% deviation — TEST-36: 1.50% |
| [BUG-085](bugs/BUG-085-FIXED.md) | FIXED 2026-07-04 | paint | linear/radial gradient — TEST-39: 12.05%→1.62%→1.18%→**0.00%**. D2-1 (2026-06-23): repeating linear/radial + все radial рисуются per-pixel в device-разрешении (обход 256-тексельного LUT femtovg) → smooth-radial совпадают пиксель-в-пиксель. Тест `'39'` удалён из KNOWN_DEBTORS (цель 0.5% достигнута: 0.48% normal / 0.05% --ipc; run.py:278); файл бага висел OPEN из-за дрейфа трекера. Ревизия P3 2026-07-04: свежий gdigrab (после калибровки TEST-00) = **TEST-39 0.00%**, остаток 1.18% не воспроизводится → закрыт |
| [BUG-086](bugs/BUG-086-FIXED.md) | FIXED 2026-06-09 | paint | conic-gradient: triangle-fan не обрезался по box + игнорировал repeating |
| [BUG-087](bugs/BUG-087-FIXED.md) | FIXED 2026-06-09 | paint | gradient layers ignored background-size/position/repeat — TEST-45: 17.29% |
| [BUG-088](bugs/BUG-088-FIXED.md) | FIXED 2026-06-12 | css-parser/layout | individual CSS transform properties (translate/rotate/scale) — TEST-46: 4.63% |
| [BUG-089](bugs/BUG-089-FIXED.md) | FIXED 2026-06-09 | paint | SVG basic shapes not rendered (rect/circle/ellipse/line) — TEST-47: 21.71% |
| [BUG-090](bugs/BUG-090-FIXED.md) | FIXED 2026-06-12 | layout | -webkit-line-clamp multi-line truncation — TEST-48: PASS 0.26% |
| [BUG-091](bugs/BUG-091-FIXED.md) | FIXED 2026-06-08 | paint | background-blend-mode: bottom layer wrapped in PushBlendMode — TEST-49: 30.62% |
| [BUG-092](bugs/BUG-092-FIXED.md) | FIXED 2026-06-12 | css-parser/layout | CSS variables var() in cascade — TEST-50: PASS 0.0001% |
| [BUG-093](bugs/BUG-093-FIXED.md) | FIXED 2026-06-11 | paint | scrollbar rendering TEST-51: threshold calibration замаскировала реальный дефект BUG-123 |
| [BUG-094](bugs/BUG-094-FIXED.md) | FIXED 2026-06-11 | paint | text-shadow with blur ~7% deviation — TEST-52: 6.82% |
| [BUG-095](bugs/BUG-095-FIXED.md) | FIXED 2026-06-09 | layout/paint | background-origin/background-clip positioning ~32% deviation — TEST-53: 31.78% |
| [BUG-096](bugs/BUG-096-FIXED.md) | FIXED 2026-06-09 | paint/layout | SVG `<path>` stroke tessellation not rendered — TEST-54: 9.50% |
| [BUG-097](bugs/BUG-097-FIXED.md) | FIXED 2026-06-09 | layout/paint | `<video>` placeholder: posterless video painted grey instead of transparent |
| [BUG-098](bugs/BUG-098-FIXED.md) | FIXED 2026-06-11 | paint | mix-blend-mode: ~14% deviation — PA-3: offscreen CPU mix_blend_rgba |
| [BUG-099](bugs/BUG-099-OPEN.md) | OPEN (DEBTOR) | js/paint | `<canvas>` 2D context реализован; TEST-57 ратчет 4.14→2.96% (прогон 2026-06-23). Остаток = font-parity + canvas-AA → KNOWN_DEBTOR |
| [BUG-100](bugs/BUG-100-OPEN.md) | OPEN (DEBTOR) | layout | ::first-letter / ::first-line РЕАЛИЗОВАНЫ (дрейф трекера, ревизия 2026-06-23): drop-cap флоатится (`extract_first_letter_float` + 7 тестов), first-line зелёная/жирная. Diff-картинка: фича работает, остаток TEST-58 4.92% = font-parity тела абзаца (Inter vs Edge) + edge-AA 48px drop-cap (rule 3) → KNOWN_DEBTOR |
| [BUG-101](bugs/BUG-101-OPEN.md) | OPEN (DEBTOR) | layout/paint | image-set()/cross-fade() РЕАЛИЗОВАНЫ. Грамматика cross-fade() приведена к CSS Images L4 §4 (`parse_cross_fade` в layout/src/style.rs, 2026-06-23): unprefixed `cross-fade(url,url,30%)` (устаревшая 3-арг webkit-форма с висячим bare percentage) теперь невалидна как в Edge → ячейки cf-30/cf-70 пустые, совпадают с Edge; `-webkit-cross-fade()` и валидная L4 `cross-fade(<image> <pct>?, <image>)` рисуются. TEST-59 24.18%→17.15%. Остаток = ресэмплинг фото-картинок (класс BUG-219) + font-parity меток (rule 3) → KNOWN_DEBTOR. TEST-59 |
| [BUG-102](bugs/BUG-102-FIXED.md) | FIXED 2026-06-17 | paint | SVG stroke-width/dasharray молча терялись в standards-mode (unitless user units) + join-шипы; TEST-60 11.51%→1.41%, TEST-54 5.58%→2.30% |
| [BUG-103](bugs/BUG-103-OPEN.md) | OPEN (DEBTOR) | js/shell | View Transitions L1 рендерится (F2-4 ревизия 2026-06-22): startViewTransition + root cross-fade работают. «99.53%» был blank-gdigrab артефактом; KNOWN_DEBTOR ратчет 99.53→10.99% (прогон 2026-06-23: gdigrab дал настоящий кадр). Остаток = тайминг захвата Edge (async callback, как BUG-199/126) + font-parity. Опц. полный L1 (named groups + ::view-transition pseudo) = XL. TEST-61 |
| [BUG-104](bugs/BUG-104-FIXED.md) | FIXED 2026-06-19 | layout | TEST-62 63.70%→2.32%: реальная причина — column flex-grow не распределял free space. `lay_out_flex` хардкодил `container_main=0`/`free_space=0` для column → `.right-col` дети `flex:1` схлопывались в h≈0. Фикс: `explicit_main` для column (явная height или растяжение родителем re-layout). Геометрия пиксель-точна (diff: все заливки идентичны). Остаток 2.32% = font-parity (BUG-128) метки секций + border-radius edge-AA (BUG-176) → KNOWN_DEBTORS. box_tree.rs:5097/7191 |
| [BUG-105](bugs/BUG-105-FIXED.md) | FIXED 2026-06-22 | layout | TEST-63 48%→2.02%: `display:masonry` невалиден в Edge → fallback на `column-count:3` multicol. Реальная причина — multicol balance заполнял первую колонку до высоты контейнера (5/4/0) вместо равномерного разбиения. Фикс: `balanced_column_height` (бин-поиск минимальной высоты колонки) → 9 карт = 3×3 как Edge. Геометрия пиксель-точна; остаток = border-radius edge-AA (BUG-176) + текст. box_tree.rs:6933 |
| [BUG-106](bugs/BUG-106-FIXED.md) | FIXED 2026-06-09 | layout | TEST-64 table: missing UA heading defaults → h3 без размера и margin |
| [BUG-107](bugs/BUG-107-FIXED.md) | FIXED 2026-06-09 | layout | flex align-content: normal/stretch не распределял свободное пространство |
| [BUG-108](bugs/BUG-108-FIXED.md) | FIXED 2026-06-17 | layout | TEST-66 5.24%→1.08%: реальная причина — parent↔last-child bottom margin не коллапсил (CSS 2.1 §8.3.1), свотчи уезжали вниз +30px/секция. Остаток 1.08% — текст (font-parity, rule 3) + border-radius AA |
| [BUG-109](bugs/BUG-109-FIXED.md) | FIXED 2026-06-23 | paint/font | font-variation-settings: wght/wdth/slnt не применялись в femtovg-окне — варьируемый run теперь рендерится через lumen-font outlines (vector fill). GPU-путь применял оси и раньше; TEST-68 переписан на пустые боксы, исходные 3.21% более не воспроизводятся |
| [BUG-110](bugs/BUG-110-FIXED.md) | FIXED 2026-06-14 | layout/paint | object-fit: SVG viewBox scaling ~8% deviation — TEST-70: 8.03% |
| [BUG-111](bugs/BUG-111-FIXED.md) | FIXED 2026-06-08 | paint/shell | lumen-paint/shell не компилировались после мержа A-2 CSS Custom Highlight API |
| [BUG-112](bugs/BUG-112-FIXED.md) | FIXED 2026-06-08 | driver | test_32_list_markers регрессия: P4 добавил 2 @counter-style списка |
| [BUG-113](bugs/BUG-113-FIXED.md) | FIXED 2026-06-09 | layout | TEST-53 row-2 drift ~24px: trailing cross_gap утекал в single-line flex |
| [BUG-114](bugs/BUG-114-FIXED.md) | FIXED 2026-06-24 (DEBTOR) | css-parser/layout | `font` shorthand drops font-size/line-height — TEST-53 9.62%→1.71%. Не было арма `"font" =>` в `apply_declaration` (style.rs), shorthand падал в `_ => {}`; font-size игнорировался pre-pass-ом. Фикс: `parse_font_shorthand` (CSS Fonts L4 §6.10) + резолв `<font-size>` в `apply_font_size` + арм `"font" =>` (style/variant/weight/stretch/line-height/family, reset-to-initial по §3.1) + вынесен `apply_line_height_value`. 7 юнит-тестов. Остаток 1.71% = font-parity (Inter vs Edge sans/mono, rule 3) → KNOWN_DEBTORS '53' (BUG-128) |
| [BUG-115](bugs/BUG-115-FIXED.md) | FIXED 2026-06-23 | css-parser | percent `background-size` not supported — TEST-45 residual |
| [BUG-116](bugs/BUG-116-FIXED.md) | FIXED 2026-06-09 | layout | auto table column widths: content-based sizing (CSS 2.1 §17.5.2) |
| [BUG-117](bugs/BUG-117-FIXED.md) | FIXED 2026-06-09 | layout | multi-column greedy assignment two bugs — TEST-33 16.14% |
| [BUG-118](bugs/BUG-118-FIXED.md) | FIXED 2026-06-09 | test/snapshot | snapshot_cpu reference PNGs outdated for 12 pages |
| [BUG-119](bugs/BUG-119-FIXED.md) | FIXED 2026-06-10 | test/html | raw U+0001 byte в `<head>` 17 тест-страниц → content shifted 20px |
| [BUG-120](bugs/BUG-120-FIXED.md) | FIXED 2026-06-10 | layout/text | C0 control chars render as 1-line text box instead of invisible |
| [BUG-121](bugs/BUG-121-FIXED.md) | FIXED 2026-06-10 | test/driver | snapshot_vs_edge gate red: wgpu vs femtovg бэкенды дают разный результат |
| [BUG-122](bugs/BUG-122-FIXED.md) | FIXED 2026-06-15 | test/paint | flaky compositor tests: wall-clock deadline зависел от планировщика ОС |
| [BUG-123](bugs/BUG-123-FIXED.md) | FIXED 2026-06-11 | paint | scroll container's own bg+border clipped by its own overflow scissor |
| [BUG-124](bugs/BUG-124-OPEN.md) | OPEN (DEBTOR) | layout/paint | TEST-51 residual 1.09%: бокс у Edge стоит на 1px ВЫШЕ Lumen (бордер y=195 vs 196, контент 197 vs 198) — расхождение позиции в layout (накопление line-height 19.2px по потоку), не AA-кайма. Paint-снэппинг ЭМПИРИЧЕСКИ исключён (2026-06-25, step37 3 раунда: снэп fill/border/clip в femtovg → 1.09→1.17→1.13, не помогло). Корень = PS-1 (единая политика pixel-snapping в layout, домен P1), не точечный фикс. KNOWN_DEBTOR 1.09% (TEST-51) |
| [BUG-125](bugs/BUG-125-FIXED.md) | FIXED 2026-06-22 | layout/paint | CSS Motion Path L1 — боксы садились top-left-на-путь вместо anchor-на-путь (off by пол-бокса). Добавлен `T(-anchor)` в `forward_box_transform` + `walk` (offset-anchor `auto`→transform-origin=центр), поворот вокруг anchor. TEST-76 3.18%→0.54% (боксы пиксель-в-пиксель; остаток = диагональный градиент-трек `calc()`-стопы → BUG-230, KNOWN_DEBTOR) |
| [BUG-126](bugs/BUG-126-OPEN.md) | OPEN (DEBTOR) | layout | CSS Anchor Positioning L1 — placement фикснут (53.45% → 12.94%): position-area definite-size элементы прижимаются к якорю вместо растягивания на band (anchor.rs place_axis/align_in_band). 3×3 сетка совпадает с Edge(position-area) пиксель-в-пиксель. Остаток-должник: тест использует устаревшее `inset-area` (Edge игнорирует, поддерживает только `position-area`) + span-ряд (Lumen спек-корректнее Edge). KNOWN_DEBTORS 12.94% |
| [BUG-127](bugs/BUG-127-OPEN.md) | OPEN (DEBTOR) | shell/layout | CSS Scroll-Driven Animations L1 — feature wired (F2-2 2026-06-22): `animation-timeline: scroll()/view()/<named>` теперь драйвит прогресс анимации от позиции скролла/вьюпорта, а не от часов (`shell/animation_scheduler.rs`). TEST-78 12.02%→10.07%→9.54%: scroll()/named боксы садятся в from-state как у Edge, view()-бокс едет по view-прогрессу; с D2-2 (BUG-231 FIXED) фон view-бокса композится (интерполированный оранжевый). Перепроверено 2026-07-04 на свежей сборке main: 4.62% (стабильно с full-run 2026-07-01 = 4.64%), diff-декомпозиция = font-parity текста (заголовок, метки карточек, .info-моноблок; Inter vs Edge, rule 3, класс BUG-128) + субпиксельные сдвиги анимируемых боксов; from-state рамки совпадают пиксель-в-пиксель. Точечного P3-дефекта нет, закроется только с FP-1 → KNOWN_DEBTOR ратчет 5.76→4.64 |
| [BUG-128](bugs/BUG-128-OPEN.md) | OPEN (DEBTOR) | font | font-parity (serif vs sans, Inter vs Edge) — общий должник многих тестов. TEST-79 6.76% внесён в KNOWN_DEBTORS (прогон 2026-06-23) |
| [BUG-129](bugs/BUG-129-FIXED.md) | FIXED 2026-06-14 | layout | CSS Tables border-collapse: collapse — TEST-80 16.81% |
| [BUG-130](bugs/BUG-130-FIXED.md) | FIXED 2026-06-13 | paint | view-transition-name: TEST-81 32.47% — ложная причина, реальная = BUG-141 |
| [BUG-131](bugs/BUG-131-FIXED.md) | FIXED 2026-06-13 | paint | INTERACTION TEST-100 (transform×overflow) 9.57%: overflow-клип закрывался до дочернего SC |
| [BUG-132](bugs/BUG-132-FIXED.md) | FIXED 2026-06-12 | paint | INTERACTION TEST-101 (border-radius×overflow) 4.04%: PushClipRoundedRect добавлена |
| [BUG-133](bugs/BUG-133-FIXED.md) | FIXED 2026-06-12 | paint | INTERACTION TEST-102 (opacity×z-index) 17.04%: per-draw alpha вместо offscreen |
| [BUG-134](bugs/BUG-134-FIXED.md) | FIXED 2026-06-15 | paint | INTERACTION TEST-103 (filter×transform): ложная регрессия — устаревший бинарь |
| [BUG-135](bugs/BUG-135-FIXED.md) | FIXED 2026-06-17 | paint | INTERACTION TEST-104 (mask×gradient×radius) 51.97% → 0.44% PASS (фикс BUG-183) |
| [BUG-136](bugs/BUG-136-FIXED.md) | FIXED 2026-06-13 | layout | INTERACTION TEST-105 (float/clear×margin) 4.84%: три дефекта float-раскладки |
| [BUG-137](bugs/BUG-137-FIXED.md) | FIXED 2026-06-12 | paint | INTERACTION TEST-106 (transform×z-index) 4.02%→PASS 0.02% |
| [BUG-138](bugs/BUG-138-FIXED.md) | FIXED 2026-06-13 | paint | INTERACTION TEST-107 (shadow×radius×overflow): box-shadow на скруглённом боксе — квадратный FillRect |
| [BUG-139](bugs/BUG-139-FIXED.md) | FIXED 2026-06-12 | paint | INTERACTION TEST-108 (вложенные transform) 4.62%: PopTransform эмитировался до дочерних SC |
| [BUG-140](bugs/BUG-140-FIXED.md) | FIXED 2026-06-13 | paint | INTERACTION TEST-109 (clip-path×transform×radius) 14.10%→4.80% |
| [BUG-141](bugs/BUG-141-FIXED.md) | FIXED 2026-06-13 | layout | TEST-71 17.83%: flex align-items:center в non-wrap контейнере игнорировал cross size |
| [BUG-142](bugs/BUG-142-FIXED.md) | FIXED 2026-06-17 | paint/shadow-dom | :host / ::slotted rendering diverges — TEST-72: 11.24% → 0.00% |
| [BUG-143](bugs/BUG-143-FIXED.md) | FIXED 2026-06-22 | layout | TEST-75 16.97%→0.25%: `grid-template-rows:masonry` невалиден в Edge → fallback на обычный grid. Три фикса: (1) grid placement теперь сортирует items по `order` (CSS Grid §6); (2) grid `align:stretch` больше не растягивает items с явной `height` (CSS Grid §11.2); (3) flex column больше не двойного-считал border (BUG-232) — `.label`-полосы садятся в правильную высоту. Геометрия = Edge пиксель-в-пиксель. box_tree.rs:7853 |
| [BUG-144](bugs/BUG-144-OPEN.md) | OPEN (DEBTOR) | paint | CSS filter/backdrop-filter — TEST-30: row-flip 16.42%→10.48% (FLIP_Y) + gradient hard-stop row 2 (BUG-085) 10.48%→7.56% + backdrop colour-matrix/combo больше не тёмные (CPU backdrop-пайплайн `apply_backdrop_filters`: blur через `box_blur_rgba`, без мёртвого filter_image-readback) 7.56%→4.36% + blur-качество (edge-bleed clamp к border-box + честный 3-pass Gaussian вместо single-box) 4.36%→4.27%. Ревизия P3 2026-07-04 (свежая сборка main): TEST-30 = 4.27% стабильно, diff-декомпозиция = filter/edge AA (тонкие 1px vertical AA-полосы по кромкам всех filter-карт rows 1-3, класс BUG-176/247) + blur-kernel AA (row 4) + font-parity текста меток карт (rule 3, класс BUG-128); filter-математика (`apply_filter_rgba`) спек-корректна и покрыта юнит-тестами, геометрия совпадает. Точечного P3-дефекта нет → KNOWN_DEBTOR ратчет 4.36→4.27 |
| [BUG-145](bugs/BUG-145-FIXED.md) | FIXED 2026-06-12 | paint | РЕГРЕССИЯ: offscreen filter layer сайзился по bounds → viewport stretch |
| [BUG-146](bugs/BUG-146-FIXED.md) | FIXED 2026-06-12 | paint | TEST-15 box-shadow регрессия 1.06%→6.58%: blur-FBO без FLIP_Y |
| [BUG-147](bugs/BUG-147-FIXED.md) | FIXED 2026-06-12 | shell | clippy -D warnings fails: redundant use, dead code, unnecessary cast |
| [BUG-148](bugs/BUG-148-FIXED.md) | FIXED 2026-06-12 | shell | test hit_page_range_field fails: W-2b добавил строку Scale, row сдвинулся |
| [BUG-149](bugs/BUG-149-FIXED.md) | FIXED 2026-06-13 | test/snapshot | snapshot_cpu красный: эталоны устарели после PA-5 dashed/dotted бордеров |
| [BUG-151](bugs/BUG-151-FIXED.md) | FIXED 2026-06-13 | layout | Parent-first-child margin collapse не применяется (CSS 2.1 §8.3.1) |
| [BUG-152](bugs/BUG-152-FIXED.md) | FIXED 2026-06-13 | layout | anon_style клонирует float_side/clear/position родителя → анонимный бокс флоатится |
| [BUG-153](bugs/BUG-153-FIXED.md) | FIXED 2026-06-14 | test | CPU-эталоны протухли: регрессия от BUG-151 + 1024-byte сдвиг |
| [BUG-154](bugs/BUG-154-FIXED.md) | FIXED 2026-06-15 | layout | mix_polar путает индекс hue для LCH/Oklch (hue на индексе 2, не 0) |
| [BUG-155](bugs/BUG-155-FIXED.md) | FIXED 2026-06-15 | js | perf_observer_lcp_entry: index out of bounds — element_nid=42 за пределами тест-дока |
| [BUG-156](bugs/BUG-156-FIXED.md) | FIXED 2026-06-15 | paint/layout | ЛОЖНАЯ РЕГРЕССИЯ TEST-27: устаревший lumen.exe в прогоне 06-15 |
| [BUG-157](bugs/BUG-157-FIXED.md) | FIXED 2026-06-15 | paint | ЛОЖНАЯ РЕГРЕССИЯ TEST-40: та же причина — устаревший lumen.exe |
| [BUG-158](bugs/BUG-158-FIXED.md) | FIXED 2026-06-15 | layout | карточки новостей lenta.ru налезают друг на друга: `flex:1` (flex-basis:0) item в column-flex схлопывался в height=0 — нет automatic minimum size (§4.5) |
| [BUG-159](bugs/BUG-159-FIXED.md) | FIXED 2026-06-15 | paint | z-indexed потомок плоского overflow:auto scroll-контейнера сбегал из scroll-слоя (рисовался после PopScrollLayer) → fill_buckets переустанавливает scroll-слой для дочерних SC (кроме fixed/sticky) |
| [BUG-160](bugs/BUG-160-FIXED.md) | FIXED 2026-06-15 | font | WOFF2-шрифты не декодируются («unexpected end of font data»), спасает только woff-fallback — затрагивает большинство сайтов |
| [BUG-161](bugs/BUG-161-FIXED.md) | FIXED 2026-06-15 | network | HTTP/2 HPACK «dynamic table size update exceeds negotiated max» → ya.ru не грузится |
| [BUG-162](bugs/BUG-162-FIXED.md) | FIXED 2026-06-15 | encoding | детектор кодировки выдаёт ibm866 на чистом ASCII (example.com) вместо UTF-8 |
| [BUG-163](bugs/BUG-163-FIXED.md) | FIXED 2026-06-15 | shell/paint | lazy `<img>` на lenta.ru не рисовались: `LazyImageSlot` всегда красил серый placeholder даже после загрузки картинки + above-the-fold lazy-картинки не дозагружались на initial paint (proximity-check был только в relayout) |
| [BUG-164](bugs/BUG-164-FIXED.md) | FIXED 2026-06-15 | shell/js | внешние `<script src>` не скачиваются и не исполняются (collect_inline_scripts берёт только инлайны) → JS бандлы (lenta.ru owlBundle.js и т.д.) не работают, первопричина BUG-163 |
| [BUG-165](bugs/BUG-165-FIXED.md) | FIXED 2026-06-15 | layout | flex `align-content` сдвигал строку, не двигая поддерево item-ов: вложенный контент оставался на месте → items вылезали из контейнеров (TEST-65: 16.40%) |
| BUG-166 | FIXED 2026-06-16 | lumen-js | `video_bindings::tests::native_video_load_registers_pending` flaky on parallel run — two tests raced on the process-global `video_gif_store` singleton; serialized them with a `STORE_GUARD` mutex | crates/js/src/video_bindings.rs |
| [BUG-167](bugs/BUG-167-FIXED.md) | FIXED 2026-06-19 | shell | вход/выход Fullscreen API не пересчитывал вьюпорт: окно растягивалось на весь десктоп, но страница оставалась в исходном вьюпорте (~1024×720). Корень: `set_fullscreen` применяет размер асинхронно, `inner_size()` сразу после вызова ещё старый. Фикс: `fullscreen_resize_pending` + `arm_fullscreen_resize`/`poll_fullscreen_resize` (из `about_to_wait`) ждут смены `inner_size()` и прогоняют тот же resize+relayout, что `WindowEvent::Resized`; чистое решение `decide_fullscreen_poll` под 6 юнит-тестов | crates/shell/src/main.rs |
| [BUG-169](bugs/BUG-169-FIXED.md) | FIXED 2026-06-16 | network+shell | Linux: pre-existing clippy/test-сбои в `#[cfg(linux/macos)]` platform-коде (не ловились на Windows-dev). network/ctap2: private `descriptor_is_fido` в тестах (E0603), unnecessary `unsafe` (1128), collapsible `if` (1192). shell: unused imports `screen_capture.rs:16`, dead `entry_from_path` `file_dialog.rs:116`. Починены как unblock гейта PH1-2a. | crates/network/src/ctap2.rs:1128 |
| [BUG-170](bugs/BUG-170-FIXED.md) | FIXED 2026-06-16 | shell+font | `@font-face` web-шрифты блокируют первый paint: `load_font_faces` качает все woff2 до layout (FOUT не реализован). Надо `font-display: swap` — рисовать фолбэком сразу, подменять web-шрифт в фоне с relayout. `font-display` уже парсится (css-parser:2199), не используется. | crates/shell/src/main.rs:3067 |
| [BUG-171](bugs/BUG-171-FIXED.md) | FIXED 2026-06-19 | shell | **Этап 1 ✅** (префетч-кэш `prefetch::PREFETCH_CACHE`): внешние скрипты + linked CSS прогреваются в фоне во время streaming, сеть снята с UI-потока. **Этап 2 ✅**: весь финальный pipeline вынесен с UI-потока — в `LoadEvent::LoadDone` `render_bytes` (fetch скриптов → QuickJS → fetch+декод картинок/CSS/шрифтов → layout) исполняется на `std::thread::spawn`, готовый результат прилетает назад как `LoadEvent::RenderDone(Box<RenderOutcome>, gen)` и применяется на UI через `apply_loaded_page`. Окно остаётся отзывчивым всю CPU-фазу. Разблокировано B-1/ADR-014 (`QuickJsRuntime` — `Send`-хэндл): JS-контекст создаётся на рендер-потоке и пересылается на UI. `PersistentJs: Send`, `hyp_provider: Arc<…>`, generation-guard отбрасывает устаревшую навигацию. | crates/shell/src/main.rs:7322 |
| [BUG-172](bugs/BUG-172-FIXED.md) | FIXED 2026-06-19 | shell | Картинки качались дважды на streaming-страницах: PH1-2c `spawn_stream_image_loads` грузил прогрессивно, финальный `fetch_and_decode_images` качал их же заново. Закрыто общим per-load кэшем `image_cache::IMAGE_CACHE` (`DecodedImageCache`): оба пути декодируют через `get_or_decode`/`get_or_decode_current` с единой fetch+decode-логикой (`decode_image`), generation-scoped + in-flight дедуп (как `prefetch::PREFETCH_CACHE`). Cache-hit отдаёт уже декодированные пиксели — финальный проход не трогает сеть/декодер; reset на старте навигации (`start_streaming_load`) и per-render (`render_source_to_png`). | crates/shell/src/image_cache.rs |
| [BUG-173](bugs/BUG-173-FIXED.md) | FIXED 2026-07-04 | paint | Остаток SVG `<path>` vs Edge после BUG-102 (triangle-soup AA-швы, stroke-edge AA, self-intersecting fill, dash-on-curve). Устранён двумя срезами: **fill** нативный с 2026-06-30 (`DrawSvgFill` → femtovg `fill_path`/tiny_skia `Winding`, self-intersecting по winding), **stroke** нативный с 2026-07-04 (`DrawSvgStroke` → femtovg `stroke_path`, срез d87dae63) — AA только на истинной границе, без внутренних швов на кривых/пунктире; CPU/wgpu ре-тесселлируют бит-идентично. Свежий full-build gdigrab: TEST-54 2.30%→**0.26%**, TEST-60 1.41%→**0.40%**, TEST-119 0.81%→**0.38%** (все ≤0.5%) → записи 54/60/119 удалены из KNOWN_DEBTORS. TEST-119 также зависела от BUG-262 (FIXED, `svg_paint_matrix`). Инхерентный AA повёрнутых rect/circle/dash остаётся у BUG-247. Регресс-тесты `nonzero_path_fill_emits_drawsvgfill_not_triangle_soup`, `svg_stroke_cpu_matches_tessellated_path` | crates/engine/paint/src/svg_path.rs |
| [BUG-174](bugs/BUG-174-FIXED.md) | FIXED 2026-06-17 | layout | In-flow (inline-block) SVG `<path>` рисовался в raw user-координатах `d` без смещения на origin своего SVG-вьюпорта — все пути из разных SVG-ячеек схлопывались в верхний левый угол страницы (видны только те, что попали в свой clip). TEST-119 56.35% → 0.81%. | crates/engine/layout/src/box_tree.rs:1198 |
| [BUG-175](bugs/BUG-175-FIXED.md) | FIXED 2026-06-17 | paint | `border-radius` + `border`: рамка рисовалась 4 axis-aligned прямоугольниками без учёта радиуса → квадратные углы вокруг скруглённого фона (видно на пилюлях/кругах/эллипсах с бордером в TEST-36). Теперь однородная solid-рамка рисуется even-odd кольцом между внешним и внутренним скруглёнными rect. TEST-36 1.50% → 1.11%. | crates/engine/paint/src/backends/femtovg_backend.rs:1682 |
| [BUG-176](bugs/BUG-176-FIXED.md) | FIXED (DEBTOR) 2026-07-04 | paint | Расследование P3: свежий full-build gdigrab TEST-36 = 0.96% (был baseline 1.11%). Гипотеза «эллиптическая kappa» (row 6, `border-radius: H/V`) ОПРОВЕРГНУТА — CPU-путь рисует ту же геометрию с 0.208% (эллипсы геометрически верны, bézier аффинно-инвариантен, покрыто `draw_fill_rounded_rect_elliptical_different_radii`); diff — однопиксельная кайма по всему периметру (прямые кромки включительно, магнитуда медиана 124), т.е. инхерентное расхождение AA femtovg vs device-snapping Edge (класс BUG-124/247), не дефект border-radius. Квадратные рамки закрыты BUG-175. KNOWN_DEBTORS ратчет 1.11% → 0.96%. | crates/engine/paint/src/backends/femtovg_backend.rs:1342 |
| [BUG-177](bugs/BUG-177-FIXED.md) | FIXED 2026-06-17 | layout | `height` на table-cell трактовался как фиксированный, а не минимальный (CSS 2.1 §17.5.3): ячейка с `height:64px` + content выше (52×32 блок + margin 16px → 64px content > 56px content-box) зажималась в 64px, content переполнял её в border-spacing-зазор, pitch строки был короче на величину переполнения и ошибка накапливалась вниз по таблице. Теперь used-height = max(specified, content). TEST-115 13.45% → 0.00%. | crates/engine/layout/src/box_tree.rs:5471 |
| [BUG-178](bugs/BUG-178-FIXED.md) | FIXED 2026-06-17 | layout | shrink-to-fit auto-width контейнера с несколькими `float`-детьми считал ширину как max ребёнка, а не сумму (CSS 2.1 §9.5.1 — флоаты стоят бок о бок). Float-обёртка с двумя `float:left` детьми по 200px сжималась до 200px → второй флоат переносился под первый вместо ряда. `preferred_inline_block_width` + `max_content_outer_width`: суммируем margin-box ширины float-детей, max берём только среди in-flow. TEST-51 9.91% → 1.09% (остаток = BUG-124, дробные Y-координаты). | crates/engine/layout/src/box_tree.rs:3750 |
| [BUG-179](bugs/BUG-179-FIXED.md) | FIXED 2026-06-17 | layout | flex-item с `flex-basis:auto` и без явной `width` использовал ширину из предварительного прохода (`item.rect.width` = ширина контейнера, т.к. блоки растягиваются). Элемент с `min-width:200px` в контейнере 600px получал base=600px → total_hyp=700px > 600px → ошибочный shrink → элемент 514px вместо 200px (второй столбец TEST-46 уезжал ~160px вправо). Фикс: `flex_auto_base_main_width` вычисляет max-content-width и ограничивает `min-width`/`max-width` (CSS Flexbox §9.2/§9.7). | crates/engine/layout/src/box_tree.rs:3932 |
| [BUG-180](bugs/BUG-180-FIXED.md) | FIXED 2026-06-17 | layout | TEST-18 21.21%→2.11%: блок-обёртка `<img>` не учитывала descent line-box baseline-выровненной inline-картинки («image bottom gap», CSS 2.1 §10.8) — каждый ряд картинок уезжал вверх на ~descent px, ошибка копилась вниз. `child_y += descent_px` после baseline replaced-ребёнка (box_tree.rs:5527). Остаток 2.11% = image-resampling AA (BUG-219) → KNOWN_DEBTORS |
| [BUG-181](bugs/BUG-181-FIXED.md) | FIXED 2026-06-20 | layout/paint | `object-fit` basic — расследовано: геометрия всех 5 режимов (fill/contain/cover/none/scale-down) + object-position верна (средние RGB ячеек совпадают с Edge ±0.1, лучший сдвиг 0,0, letterbox 19.5px корректен). TEST-19 9.05% = image-resampling AA на высокочастотном контенте (perceptron-диаграмма + agi rusty-текстура) → BUG-219, TEST-19 в KNOWN_DEBTORS. Регресс-тесты `bug181_*` (display_list.rs) фиксируют геометрию |
| [BUG-182](bugs/BUG-182-FIXED.md) | FIXED 2026-06-24 | layout/paint | `vertical-align: middle` on inline-block centred the box on the line-box instead of the baseline. With a tall `vertical-align: top` box (100px) in the same row the baseline sits off-centre (CSS 2.1 §10.8.1), so the 60px middle cell was 20px too low. Now the row's baseline position is computed (strut ascent + middle/baseline extents, then top/bottom boxes extend the line) and middle aligns its centre to `baseline − x-height/2`. TEST-24 row1 pixel-perfect: 0.93% → 0.50% (residual = row2 SUP/MID/SUB font-parity, rule 3 / BUG-128). box_tree.rs:6065 |
| [BUG-183](bugs/BUG-183-FIXED.md) | FIXED 2026-06-17 | paint | `mask-image` gradient mask not implemented — TEST-26: 17.74% → 5.02% (остаток BUG-218 mask-mode:luminance) |
| [BUG-184](bugs/BUG-184-FIXED.md) | FIXED 2026-06-24 (DEBTOR) | paint | `clip-path` deviation — TEST-31: 0.59% KNOWN_DEBTOR. P3-ревизия 2026-07-04: свежий full-build gdigrab = 0.59% (bad=4318; inset/rect=0 через scissor, femtovg shape-fill AA-fringe ~1px на curved/diagonal: circle 1244+ellipse 856+polygon/path 2108). CPU-путь (tiny_skia `--screenshot`) той же геометрии = 0.06% (bad=459) → **опровергает** дефект геометрии/движка, остаток чисто femtovg edge-AA (класс BUG-176/247/173). Совпасть = inward-AA clip-маски (femtovg не умеет) или CPU-coverage-маска (крупный переписыв, вне scope P3) |
| [BUG-185](bugs/BUG-185-FIXED.md) | FIXED 2026-06-21 (DEBTOR) | layout/paint | list `::marker` geometry — TEST-32 3.75% (KNOWN_DEBTORS BUG-128). Две геометрии: (1) `::marker { content: "→ " }` на `list-style-type:disc` рисовал диск вместо строки (painter приоритетил bullet-форму над непустым text); теперь непустой text всегда побеждает форму. (2) Широкий маркер (длинный @counter-style prefix/suffix «#1: » шире `em*1.5` бокса) переполнялся в первое слово контента; теперь бокс растёт влево, строка right-align'ится у контент-края. Остаток = font-parity (Edge serif vs Inter sans) → класс BUG-128 |
| [BUG-186](bugs/BUG-186-FIXED.md) | FIXED 2026-06-18 | layout | `multi-column` column fragmentation — TEST-33: 14.89% → 0.12% PASS |
| [BUG-187](bugs/BUG-187-FIXED.md) | FIXED 2026-06-20 | layout/paint | form controls static rendering — TEST-34: 4.78% → 3.02% (DEBTOR, KNOWN_DEBTORS BUG-128). Закрыты все form-control-специфичные дефекты: inline-block-поток, radio-круг, `<option>` display:none, color-swatch, value-текст инпутов, submit-лейбл, placeholder серым у пустых полей, checkbox белая галочка (DrawSvgPath) + radio белая точка-в-центре. Остаток = чисто font-parity лейблов/value (Inter vs Edge UI-шрифт) + line-height сдвиг → класс BUG-128 |
| [BUG-188](bugs/BUG-188-FIXED.md) | FIXED 2026-06-21 (DEBTOR) | layout/paint | individual `translate`/`rotate`/`scale` transforms — TEST-46: 4.63%→1.96% (KNOWN_DEBTORS BUG-128). Транспозиция спек-корректна: все юнит-боксы (translate/rotate/scale/scale-xy/combined) совпадают с Edge пиксель-в-пиксель; остаток — font-parity 8 monospace-меток + косвенный сдвиг teal-бокса от ширины метки в flex-ряду |
| [BUG-189](bugs/BUG-189-FIXED.md) | FIXED 2026-06-21 (DEBTOR) | paint | SVG basic shapes — TEST-47: 3.71% → 2.27% (KNOWN_DEBTORS BUG-226). Диагональный `<line>` рисовался как `FillRect` по всему bbox (сплошной оранжевый прямоугольник вместо тонкого штриха, 45% в ячейке) — теперь штрихуется толстым сегментом (`DrawSvgPath` через `push_thick_segment`) по диагонали bbox, направление по знакам user-координат концов. Остаток = SVG-штрих не центрирован на кромке → BUG-226 + stroke/rounded-corner AA |
| [BUG-190](bugs/BUG-190-FIXED.md) | FIXED 2026-06-23 | paint | `background-blend-mode` TEST-49 2.39%→0.14% (CPU). Корень не blend-математика (она канонна), а интерполяция градиентных стопов в straight вместо premultiplied (CSS Images L4 §3.1): затухание в `transparent` тянуло цвет к чёрному → мутная середина, усиленная color-dodge. Фикс: `gradient_math::premultiplied_subdivide_stops` субдивизирует alpha-варьирующие сегменты premul-интерполяцией; подключено в `femtovg_stops`+`skia_gradient_stops`. Непрозрачные градиенты байт-идентичны. Попутно TEST-45 8.95%→4.74%. |
| [BUG-226](bugs/BUG-226-FIXED.md) | FIXED 2026-06-21 (DEBTOR) | paint | SVG stroke на rect/circle/ellipse центрирован на кромке (½ наружу + ½ внутрь, SVG 2 §13.7) — TEST-47: 2.27% → 1.20% (KNOWN_DEBTORS BUG-176). Раньше stroke рисовался целиком ВНУТРИ бокса (`DrawBorder { rect: b.rect }`, border-box). Теперь `emit_svg_shape` надувает bbox на stroke-width/2 по всем сторонам, внешние радиусы = r+w/2, even-odd-кольцо (BUG-175) даёт inner = r−w/2 → центрлайн на исходной кромке. Square-углы (без радиуса) остаются square. Регрессий SVG нет (54/60/119 — только `<path>`, не затронуты; 70/82 на baseline). Остаток 1.20% = эллиптическая kappa-AA (BUG-176) |
| [BUG-191](bugs/BUG-191-FIXED.md) | FIXED 2026-06-20 | paint | TEST-52 5.83%→4.25% DEBTOR: blur-пайплайн корректен — sigma=radius/2, GPU GaussianBlur на full-RT слое (halo не клипуется), multi-shadow и цветные glow совпадают с Edge по extent/intensity (glow-only и 20px кейсы проверены пиксельно). Остаток = font-parity (Edge serif vs Inter sans, rule 3) → KNOWN_DEBTORS BUG-128. Регресс-тест `text_shadow_blur_sigma_is_half_radius_for_test52_progression` |
| [BUG-192](bugs/BUG-192-OPEN.md) | OPEN (DEBTOR) | paint | `<video>` placeholder — расследовано: фича корректна. Edge рисует `<video src="nonexistent.mp4">` без серого placeholder (прозрачный бокс, виден только фон); Lumen рендерит так же (BUG-097: пустое `<video>` без poster/кадра ничего не рисует). Бордер-бокс (200×120, 3px solid #4299e1) совпадает с Edge по размеру/цвету. Декомпозиция diff (0.54%): 90% = font-parity 6 меток `.label` (11px sans, Inter vs Edge, rule 3); остаток 0.05% = бокс на 1px ниже (y=212 vs 211) из-за line-height «normal» метки в ряду 1 (Inter ≈1.2 vs Edge → row1_height 171 vs 170), тоже font-parity. Реального дефекта нет → KNOWN_DEBTOR 0.54% (класс BUG-128). TEST-55 |
| [BUG-193](bugs/BUG-193-FIXED.md) | FIXED 2026-06-17 | layout | TEST-64 13.89%→8.99%: `display:table`-обёртка не схлопывала margin с соседним блоком (CSS 2.1 §8.3.1) — bottom-margin таблицы + top-margin `<h3>` складывались (38.72px вместо 20px), нижняя таблица уезжала на ~19px. `is_block` теперь включает `Table` (box_tree.rs:5462). Остаток 8.99% = font-parity (BUG-128) → KNOWN_DEBTORS |
| [BUG-194](bugs/BUG-194-FIXED.md) | FIXED 2026-06-24 (DEBTOR) | layout | `align-content` геометрия всех 7 кейсов пиксель-в-пиксель с Edge; реальный дефект — текст-потомок flex-item (`.item{display:flex}`) терялся (`NodeData::Text`→`BoxKind::Skip` отбрасывался), цифры 1–6 не рисовались. Фикс: `build_anon_text_item` оборачивает текст в анонимный flex/grid-item (CSS Flexbox §4 / Grid §6) + кросс-выравнивание `align-items:center` сдвигает всё поддерево item'а (`shift_y_box`), а не только `rect.y` (InlineRun цифр прилипал к верху бокса). TEST-65 1.33%→1.40%: остаток = font-parity цифр+подписей (Arial vs Inter, rule 3) + edge-AA → KNOWN_DEBTORS BUG-128. box_tree.rs:2736/7798 |
| [BUG-195](bugs/BUG-195-FIXED.md) | FIXED 2026-06-24 (DEBTOR) | paint | `::selection` — расследовано: правила парсятся, но тест не выделяет текст, поэтому фича не видна без user-selection. Свотчи (#0078D4/#e74c3c/#1a6ead) совпадают с Edge по цвету и X-позиции (41–220). Декомпозиция diff: 93% пикселей — текст (62% метки-лейблы, 31% центрированный белый текст внутри свотчей), Inter sans vs Edge sans (rule 3). Остаток = ~1–2px накопленный вертикальный line-height-дрейф верх/низ свотчей. Реального дефекта движка нет → KNOWN_DEBTOR (BUG-128) 1.07%. TEST-66 |
| [BUG-196](bugs/BUG-196-FIXED.md) | FIXED 2026-06-18 | css-parser/layout | `::before`/`::after` с `content:attr()` не генерировались на flex/grid-контейнерах — TEST-67: 16.41% → 1.36% (KNOWN_DEBTORS, остаток font-parity) |
| [BUG-197](bugs/BUG-197-FIXED.md) | FIXED 2026-06-21 | layout | TEST-69 3.61%→0.00%: причина не border-spacing (геометрия корректна), а отсутствие UA-дефолта `td,th { padding:1px }` (HTML Rendering §15.3.8). `apply_ua_table_cell_padding` + cellpadding-атрибут override (style.rs) |
| [BUG-198](bugs/BUG-198-FIXED.md) | FIXED 2026-06-20 | layout/paint | inline `<svg>` ошибочно применял CSS `object-fit`/`object-position` к viewBox — Edge их игнорирует и фитит viewBox через `preserveAspectRatio` (SVG §7.8). Замена `compute_object_fit_transform` → `compute_preserve_aspect_ratio_transform` (box_tree.rs). Доп.: femtovg `draw_fill_rounded_rect` + `CornerRadii::clamped_to_box` схлопывали эллиптические углы `min(w/2,h/2)` → SVG `<ellipse>` рисовался «стадионом»; §5.5-клампинг сохраняет rx≠ry. TEST-70 7.82% → 1.63% → KNOWN_DEBTORS (BUG-176, kappa-AA эллиптических дуг) |
| [BUG-199](bugs/BUG-199-OPEN.md) | OPEN (DEBTOR) | layout | `@starting-style` — Lumen рендерит спек-корректное settled-состояние (обе коробки 200×200, opacity 1 после 0.4s entry-перехода); display-list геометрия/цвета идеальны. Edge headless --screenshot (без virtual-time) ловит entry-transition в полёте: transform у START-значения (box-a scale(0.5)→107px, box-b translateX(-80px)), opacity у END-значения (1) — взаимно несогласованный кадр = артефакт тайминга захвата Edge, не дефект движка. Совпасть требует проводки @starting-style в каскад (P4) + engine entry-переходов + воспроизведения невозможного кадра. KNOWN_DEBTORS 7.03%, тот же класс что BUG-126/TEST-77 |
| [BUG-200](bugs/BUG-200-FIXED.md) | FIXED 2026-06-19 | paint | TEST-80 collapse varied-width borders: thin cell's bg erased thick neighbour's shared border (ordered path emits cell bg+border interleaved in DOM order). Fix: redraw cell borders after all cell backgrounds in collapse mode (`display_list.rs` fill_buckets). Residual 9.91% = font-parity vertical drift (BUG-128) → KNOWN_DEBTORS |
| [BUG-201](bugs/BUG-201-FIXED.md) | FIXED 2026-06-20 | layout | SVG `<use>` TEST-82 5.00%→2.38% DEBTOR: три дефекта — (1) `<polygon>`/`<polyline>` без ветки рендера (symbol-звёзды не рисовались); (2) HTML5 не самозакрывает `<use/>` → соседние `<use>` вкладывались как DOM-дети, рендерился лишь первый клон (теперь use/polygon сканируют mis-nested siblings); (3) element-transform применялся к doc-координатам — `scale` масштабировал origin вьюпорта, клоны ряда 3 уезжали по Y (теперь transform в user-space, потом маппинг user→doc). Остаток = font-parity (.label text) → KNOWN_DEBTORS BUG-128 |
| [BUG-202](bugs/BUG-202-FIXED.md) | FIXED 2026-06-17 | layout | TEST-83 14.02%→7.88%: реальная причина не scroll-behavior, а text-only inline-block без shrink-to-fit. `preferred_inline_block_width` мерил только дочерние боксы и игнорировал текст `InlineRun` (он в `segments`, не в `children`) → None → бокс растягивался на всю строку. Добавлена ветка измерения текста сегментов (box_tree.rs:3732). Pills `.pill` теперь обтягивают текст и текут в ряд. Остаток 7.88% = font-parity (BUG-128) → KNOWN_DEBTORS |
| [BUG-203](bugs/BUG-203-FIXED.md) | FIXED 2026-06-20 | paint | `text-decoration-skip-ink` gap geometry — TEST-84: 8.20% → 6.02% (DEBTOR BUG-128, font-parity) |
| [BUG-204](bugs/BUG-204-FIXED.md) | FIXED 2026-06-24 | layout | `anchor-name` basic «stub» deviation — root cause: missing UA `body { margin: 8px }`; tests 85–89 lack a CSS reset, so Edge shifts the frame 8px while Lumen drew it at the edge. Fixed via `apply_ua_body_margin` (style.rs). TEST-85→0.00% |
| [BUG-205](bugs/BUG-205-FIXED.md) | FIXED 2026-06-24 | layout | `position-anchor` fallback «stub» deviation — same root cause as BUG-204 (UA body margin). TEST-86→0.13% |
| [BUG-206](bugs/BUG-206-FIXED.md) | FIXED 2026-06-24 | layout | `inset-area: none` «stub» deviation — same root cause as BUG-204 (UA body margin). TEST-87→0.00% |
| [BUG-207](bugs/BUG-207-FIXED.md) | FIXED 2026-06-24 | layout | `anchor-name` nested «stub» deviation — same root cause as BUG-204 (UA body margin). TEST-88→0.00% |
| [BUG-208](bugs/BUG-208-FIXED.md) | FIXED 2026-06-24 | layout | multiple `anchor-name` «stub» deviation — same root cause as BUG-204 (UA body margin). TEST-89→0.00% |
| [BUG-209](bugs/BUG-209-OPEN.md) | OPEN (DEBTOR) | layout | TEST-90 (AVIF `<picture>`/`<img>`): доминирующая причина девиации — вложенный column-flex item схлопывался по контенту вместо растяжения на cross-size ряда — исправлена в [BUG-241](bugs/BUG-241-FIXED.md) (гейт `explicit_cross.is_some()` + регресс-тест `flex_nested_stretch_after_indefinite_pass_fills_row`). TEST-90 2.27%→1.71%: рамки/фоны ячеек совпадают с Edge пиксель-в-пиксель (diff чёрный). AVIF-данные в тесте обрезаны — ни Lumen, ни Edge их не декодируют; Edge рисует placeholder сломанной картинки (иконка + alt-текст), Lumen — нет. Остаток = эта chrome-иконка (пиксельно с Edge не совпадёт) + вертикальный сдвиг подписи + font-parity меток (rule 3) → KNOWN_DEBTOR 1.71% |
| [BUG-210](bugs/BUG-210-FIXED.md) | FIXED 2026-06-18 | layout | CSS system color keywords resolved to wrong values — `system_color()` light-scheme значения приведены к Edge (Highlight #0078d7, LinkText/VisitedText/ActiveText #0066cc, ButtonBorder #000, GrayText #6d6d6d, AccentColor #0075ff, HighlightText white) + deprecated keywords (ThreeD*/Scrollbar) → standard per CSS Color 4 §6.3. TEST-92 15.59% → 0.90% (остаток = BUG-124, gdigrab суб-пиксель на границах). crates/engine/layout/src/style.rs:17903 |
| [BUG-211](bugs/BUG-211-FIXED.md) | FIXED 2026-06-21 | layout | `field-sizing: content` — layout уже работал; контент-боксы были невидимы, т.к. `appearance: none` стрипал авторский border/background ПОСЛЕ каскада (`apply_ua_appearance` клобберил `border:2px`/`background`). Фикс: стрип UA-appearance перенесён ПЕРЕД авторским каскадом (pre-scan winning `appearance`), авторские значения теперь побеждают. TEST-93 4.11%→3.54% → KNOWN_DEBTORS (BUG-225) |
| [BUG-212](bugs/BUG-212-FIXED.md) | FIXED 2026-06-21 | layout | TEST-95 3.39%→2.86%: font-size-adjust масштабирование было корректно; реальный дефект — absolute `line-height:100px` (ratio-encoded) схлопывался при пост-каскадной смене used-font-size → `line_height_is_relative` флаг замораживает absolute line-box (style.rs + box_tree apply_font_size_adjust). Остаток = font-parity → KNOWN_DEBTORS BUG-128 |
| [BUG-213](bugs/BUG-213-FIXED.md) | FIXED 2026-06-23 (DEBTOR) | css-parser/layout | `counter-set` order — расследовано: порядок reset→increment→set уже спек-корректен (`counters.rs` apply_reset/apply_increment/apply_set, CSS Lists L3 §4). Все 5 значений счётчика совпадают с Edge пиксель-в-пиксель (5/6/0/1/42); цветные боксы и границы строк идентичны. Регресс-тест `counter_set_test97_sibling_rows` фиксирует всю последовательность. Остаток TEST-97 2.78% = чистый font-parity ::before-меток + текста строк (Inter sans vs Edge sans, rule 3) → KNOWN_DEBTORS BUG-128 |
| [BUG-214](bugs/BUG-214-OPEN.md) | OPEN (DEBTOR) | paint | `accent-color` РЕАЛИЗОВАН (дрейф трекера, ревизия 2026-06-23): `emit_form_control_accents` тинтит checkbox/radio/range/progress + юнит-тесты. Diff-картинка: цвета-акценты верны, остаток TEST-110 1.70% = расхождение нативной отрисовки form-виджетов (трек/thumb слайдера, progress-бар) vs UA-виджеты Edge → KNOWN_DEBTOR |
| [BUG-215](bugs/BUG-215-OPEN.md) | OPEN (DEBTOR) | layout | `shape-outside: path()` РЕАЛИЗОВАН (дрейф трекера, ревизия 2026-06-23): `parse_shape_path_px` флэттит path()→полигон, текст обтекает (`float_context_path_left_float`). Diff-картинка: фича работает, остаток TEST-113 1.41% = AA диагональной кромки + font-parity обтекающего текста → KNOWN_DEBTOR |
| [BUG-216](bugs/BUG-216-OPEN.md) | OPEN (DEBTOR) | layout | CSS `quotes` + `open-quote`/`close-quote` — реальный дефект исправлен: `::before`/`::after` generated content и текст `<q>` склеивались через лишний inter-word пробел (`“ auto quotes ”`); теперь примыкают вплотную как в Edge (`“auto quotes”`). Корень не в `quotes` (он работал) — `wrap_inline_run`/`one_line_fallback` вставляли пробел на ЛЮБОЙ границе inline-сегментов, тогда как пробел нужен только когда границу разделял collapsible whitespace (CSS Text L3 §4.1.1). Фикс заодно чинит `<span>a</span><span>b</span>`→«ab» и `<em>x</em>!`→«x!». TEST-117 2.28%→2.23%; остаток = чистый font-parity (Edge serif vs Lumen Inter sans, rule 3, класс BUG-128) → KNOWN_DEBTOR. Регресс-тесты `bug216_*` в box_tree.rs. crates/engine/layout/src/box_tree.rs |
| [BUG-217](bugs/BUG-217-OPEN.md) | OPEN (DEBTOR) | css-parser | `prefers-contrast`/`prefers-reduced-data` РЕАЛИЗОВАНЫ и спек-корректны (ревизия 2026-06-23): парсинг+матчинг в parser.rs (MediaFeature::PrefersContrast/PrefersReducedData + unit-тесты). Diff: swatch `.a` (prefers-contrast: no-preference) зелёный в обоих; единственное расхождение = swatch `.b` (prefers-reduced-data: no-preference) — Lumen матчит (зелёный, как требует тест), Edge не поддерживает prefers-reduced-data → красный. Lumen корректнее reference-браузера (класс BUG-126/TEST-77, BUG-199/TEST-71, BUG-237/TEST-122). TEST-120 KNOWN_DEBTOR 3.26% |
| [BUG-218](bugs/BUG-218-FIXED.md) | FIXED 2026-06-19 | css-parser/paint | `mask-mode: luminance` not parsed/applied — TEST-26 luma-cell остаток 5.02% (P4); `emit_push_mask` bakes `luminance(rgb)·alpha` в stop alpha |
| [BUG-219](bugs/BUG-219-FIXED.md) | FIXED 2026-07-04 (DEBTOR) | image/paint | image downscale resampling pixel-parity vs Edge — TEST-18 остаток 2.11%, TEST-19 9.05% (тонкий AA по всем фото после фикса BUG-180; структурный >60 diff всего 0.43% → геометрия верна). P3-расследование: гипотеза «более близкий kernel» проверена — Lanczos-3 и Mitchell-bicubic реализованы и прогнаны, НИ ОДИН не выигрывает равномерно (Mitchell улучшает TEST-18, регрессирует TEST-19; Lanczos хуже box на обоих). Остаток inherent (resampling+AA-парность vs проприетарный content-adaptive Skia-downscale Edge, класс rule 2/3 BUG-247/176/184). Прод оставлен на area-avg, baseline не тронуты → KNOWN_DEBTORS |
| [BUG-220](bugs/BUG-220-FIXED.md) | FIXED 2026-06-24 | paint | scroll-контейнер в ordered (stacking-context) пути терял scrollbar: `box_layer_ops` эмитил `PushScrollLayer`/`PopScrollLayer`, но не `DrawScrollbar` (был только в legacy `walk`). Логика scrollbar вынесена в общий хелпер `emit_scrollbars(b, padding_box, is_scroll_x, is_scroll_y, out)` и вызывается из обеих веток — `walk` (после PopScrollLayer) и `box_layer_ops` (push в `overflow_post` после PopScrollLayer). Хелпер мерит content-extent относительно padding-box origin с полом padding-box size (не border-box) → фантомный горизонтальный scrollbar при наличии border больше не появляется. TEST-83: 0 DrawScrollbar → 3 (по числу .scroll-box). Регресс-тест `ordered_scroll_container_emits_scrollbar`. display_list.rs |
| [BUG-221](bugs/BUG-221-FIXED.md) | FIXED 2026-06-20 | paint | CPU-бэкенд снимка (`render_to_image_cpu`, cpu_raster.rs) доведён до паритета с femtovg: border-radius (TEST-36 60.60%→0.47% — клампинг радиусов FillRoundedRect), радиальный градиент-круг вместо эллипса (TEST-39 10.68%→1.40%, TEST-26 5.02%→0.00%), реальная отрисовка `<img>` с object-fit + area-averaged downscale (TEST-18 52.22%→2.15%). Разблокирует полную замену gdigrab на `run.py --ipc` (TAB-7) |
| [BUG-222](bugs/BUG-222-FIXED.md) | FIXED 2026-06-19 | js/shell | WASM-реестр (`wasm::REGISTRY`) не очищается при разрушении JS-контекста: «утёкший» `Persistent` функции-импорта роняет QuickJS на `list_empty(&rt->gc_obj_list)` при teardown. Закрыт B-1/ADR-014: `js_thread_main` зовёт `wasm::clear_registry()` в teardown JS-потока до дропа `Runtime`. |
| [BUG-223](bugs/BUG-223-FIXED.md) | FIXED 2026-06-20 | network/ipc | `lumen-network-service` не компилируется (E0004): `match` в `network_service.rs:51` не покрывал таб-варианты `IpcRequest::{CreateTab,CloseTab,NavigateTab,Screenshot}` (TAB-4/5). Добавлены явные arm-ы → `IpcResponse::TabError` (сетевой процесс таб-ами не управляет), match исчерпывающий. `--workspace --all-targets` снова зелёный. |
| [BUG-224](bugs/BUG-224-FIXED.md) | FIXED 2026-06-20 | layout | НЕ регрессия движка: `test_33_multi_column` хранил устаревший ground-truth (660x88, atomic-раскладка до BUG-186). После BUG-186 (фрагментация колонок, TEST-33 14.89%→0.12%) Edge-корректная высота mc[4] = 660x64 (две 36px col-sm фрагментируются на 24px по 3 колонкам + span 16px). Подтверждено Edge `getBoundingClientRect → 64` и пиксельным паритетом TEST-33 ≈0.1%. p3-bug198 (diff чисто SVG) к multicol не прикасался. Юнит-тест ре-базлайнен 88→64. |
| [BUG-225](bugs/BUG-225-FIXED.md) | FIXED 2026-06-23 | paint | `appearance: none` подавлял value/placeholder-текст у text-input'ов: `emit_form_control_indicator` делал ранний `return` при `Appearance::None`. Фикс: ранний return заменён на флаг `suppress_primitive`, гейтящий только нативные примитивы (color-swatch, галочка/точка checkbox/radio, select-стрелка, range/progress/meter); value/placeholder text-инпутов и label кнопок рисуются всегда. `<select appearance:none>` сохраняет выбранный label, но теряет ▼. +5 регресс-тестов |
| [BUG-227](bugs/BUG-227-FIXED.md) | FIXED 2026-06-21 | paint | `color_management::tests::detects_p3_from_description` и `detects_rec2020_from_description` красные на main (paint/color_management.rs:62/84): `detect_color_space_from_icc` больше не детектит P3/Rec2020 по text-описанию профиля после рефактора ICC в lumen-core (коммиты X-1 `68927090` + H-2 Phase 3 `23014125`). Не связано с BUG-226 (paint/display_list). Домен ICC (P1). Замечено P3 при merge p3-bug-226. **FIX (ICC-2):** ре-базлайн тестов под новое поведение — сниффинг по строке намеренно убран, профиль без реальных примариев → Srgb (`description_text_is_not_sniffed_*`); тот же дубликат закрыт в lumen-image |
| BUG-228 | FIXED 2026-06-25 | layout/paint | Маркеры НЕ пропадали — это был ложный недосчёт в тесте. Кастомные `@counter-style` маркеры (`(a) `, `#1: `) легитимно шире дефолтных 24px (поведение BUG-185, `marker_w = default_w.max(text_w)`), а ассерт `test_32_list_markers` считал маркеры строго по `border_box.width == 24.0`, отбрасывая 6 широких → 27 вместо 33. Движок корректен. Фикс: фильтр в тесте сменён на `tag_name == "li" && height ≈ 22.4 && width < 60` (ground-truth гистограмма снапшота: маркеры w∈{24,25,26,29}, контент-раны w≥166, li-блоки h=26); `#[ignore]` снят. Полный suite `lumen-driver --test all` = 113 passed. |
| [BUG-229](bugs/BUG-229-FIXED.md) | FIXED 2026-06-21 | image | PNG `iCCP` распаковывался сырым `DeflateDecoder` вместо `ZlibDecoder` — zlib-обёртка профиля (RFC 1950) ломала инфляцию → профиль тихо терялся → ни один PNG с ICC не управлялся по цвету (P3/AdobeRGB/Rec2020 пересыщены). `png/mod.rs:40`. Найден при ICC-6. Регресс-тест `icc_color_management.rs` |
| [BUG-230](bugs/BUG-230-FIXED.md) | FIXED 2026-06-24 | layout/paint | `calc()` в позиции color-stop градиента схлопывал градиент. Корень: `resolve_stop_positions` (paint/src/gradient_math.rs) не имел ветки `Length::Calc` — позиция-`Calc` падала в `_ => 0.0` и обнуляла стоп. Фикс: `Calc` резолвится через `Length::resolve` с `percent_basis = line_len`, затем нормируется на `line_len` (CSS Images L3 §3.3). Регресс-тест `resolve_calc_stop_resolves_against_line_len`. Делегировано Laguna M.1, интегрировано/проверено локально |
| [BUG-231](bugs/BUG-231-FIXED.md) | FIXED 2026-06-23 | paint/shell | Анимированные `background-color`/`color` теперь композятся без relayout (D2-2). В `CompositorOverride` добавлены `color`/`background_color`; `to_compositor_frame` включает узлы с цветовым override; `emit_box_self` запоминает диапазон своих команд и через `apply_color_override` патчит background-`FillRect`/`FillRoundedRect` (по совпадению clip-rect — тени с другим rect не задеваются) + перерезолвит border/outline currentColor; `walk_with_anim` Block-ветка делает то же inline. TEST-78 10.07%→9.54% (view-бокс едет по прогрессу с интерполированным оранжевым фоном вместо teal). Остаток TEST-78 = font-parity текста (класс B, BUG-128) → остаётся KNOWN_DEBTOR. Найдено при F2-2/BUG-127 |
| [BUG-232](bugs/BUG-232-FIXED.md) | FIXED 2026-06-22 | layout | Column flex двойного-считал border у item с явной `height`: `lay_out_flex` присваивал `style.height = inner_main` (border-box значение) на content-box item без принудительного `box-sizing:border-box`, поэтому border добавлялся повторно (height:28 + border:2 → 36 вместо 32). Фикс: форсировать border-box перед re-layout (зеркало cross-axis stretch-пути). Найдено при F2-3 (сдвигало `.label`-полосы в TEST-75). box_tree.rs:7601 |
| [BUG-233](bugs/BUG-233-FIXED.md) | FIXED 2026-06-22 | js (shell) | Глобал `self` не был определён → webpack-бандлы падали `ReferenceError: self is not defined` (lenta.ru, ×4 + каскад). В конце `WEB_API_SHIM` добавлен блок алиасинга `self`/`window`/`globalThis` на один объект (+ `window.window/top/parent/frames/length`), как в браузере (WindowOrWorkerGlobalScope). Свойства на `self` теперь видны через `window` (один объект-ссылка). +2 регресс-теста. crates/js/src/dom.rs (конец shim) |
| [BUG-238](bugs/BUG-238-FIXED.md) | FIXED 2026-06-29 | shell/storage | Подсистемы хранили данные в OS-каталогах (`%APPDATA%\lumen\…` / `~/.config/lumen/…`) вместо портативного `<exe_dir>/data/` — нарушение политики портативного хранения (CLAUDE.md, решение 2026-06-16). Точки: `lumen_idb_dir` (IndexedDB SQLite), `extensions_dir` (extensions/mod.rs), `config_path` (config.rs, `fingerprint.toml`). Эталон — `browser_data_dir()` в adblock.rs. Все три переведены на `browser_data_dir().join(...)` (IDB был сделан ранее; extensions + fingerprint config — этой правкой, по решению пользователя мигрировать и конфиг). Опциональная миграция старых данных не делалась. +регресс-тесты `extensions_dir_is_portable`/`config_path_is_some` |
| [BUG-235](bugs/BUG-235-FIXED.md) | FIXED 2026-06-23 | paint (build) | `bg_tile_geometry` (общий хелпер из p1-cpu-bg-image) под wgpu-only сборкой (`lumen-driver` дефолт) была dead-code → `clippy -D warnings` падал, блокируя workspace-гейт. Пред-существовало на main. Фикс: `#[cfg(any(feature="backend-femtovg", feature="cpu-render"))]` — компилируется только для бэкендов-потребителей (wgpu тайлит на GPU). display_list.rs:920 |
| [BUG-234](bugs/BUG-234-FIXED.md) | FIXED 2026-06-23 | shell/network | HTTP-кэш подключён к загрузке подресурсов. Новый process-global `HTTP_CACHE` (config.rs) шарит один `DiskHttpCache` (`<exe_dir>/data/cache/http_cache.db`, переживает рестарт) между всеми клиентами через `Arc`; приватный/Tor-режим (`no_persistent_state`) → in-memory `HttpCache` (на диск ничего). Проводка `.with_http_cache(...)` в `apply_http` (choke point всех клиентов — page/subresource/`fetch()`). `fetch_subresource` уже умеет RFC 7234 (fresh-hit / 304-revalidate / store). Повторные заходы на сайт теперь отдают подресурсы из кэша вместо полной сетевой загрузки. config.rs (HTTP_CACHE/build_http_cache/apply_http) |
| [BUG-237](bugs/BUG-237-OPEN.md) | OPEN (DEBTOR) | layout | `line-height-step` (CSS Rhythmic Sizing L1 §2): Lumen спек-корректно округляет line-box вверх до кратного шагу (48/60/40px фоны), **Edge не поддерживает свойство** (удалён из Chromium ~2018) → эталон рисует un-stepped 24px fallback. Тот же класс, что BUG-126/TEST-77 (`inset-area`) и BUG-199/TEST-71 (`@starting-style`): Lumen корректнее reference-браузера. Привести к 0.5% можно лишь отключив рабочую реализацию — запрещено. TEST-122 KNOWN_DEBTOR 11.19% (+ font-parity переноса natural-колонки, rule 3) |
| [BUG-236](bugs/BUG-236-FIXED.md) | FIXED 2026-06-23 | layout | Регрессия от BUG-125: `offset-path: ray()` сдвигал боксы на пол-бокса. Унифицированная motion-матрица `T(rect+path)·R·m·T(-(rect+anchor))` корректна для `path()` (point_at_distance — абсолютная точка пути), но `resolve_ray` возвращал *относительное* смещение (anchor-агностичное), и вычитание anchor добавляло лишний −20px. Фикс: `resolve_motion_transform` принимает `anchor`, `ray()` возвращает абсолютную точку `anchor + displacement` (как `path()`) → вычитание anchor в матрице сворачивается в чистое смещение луча. TEST-99 3.27%→0.02% (CPU --screenshot vs Edge). +регресс-тест `ray_path_point_is_anchor_plus_displacement`. motion_path.rs, property_trees.rs |
| [BUG-239](bugs/BUG-239-FIXED.md) | FIXED 2026-06-23 | layout/paint | `radial-gradient(ellipse …)` рисовался кругом: парсер отбрасывал форму/размер, `DrawRadialGradient` нёс только центр, все бэкенды рисовали изотропный круг. Найдено при D2-1/BUG-085 — `.rg-ellipse` давал 69% диффа TEST-39. Сквозной фикс: `RadialShape`/`RadialSize` + `radial_gradient_radii` (style.rs), `radius_x`/`radius_y` в `DrawRadialGradient` (билдер вычисляет), femtovg+cpu_raster рисуют эллипс по `(rx,ry)`. Круги byte-identical (BUG-221 сохранён). TEST-39 `.rg-ellipse` 31%→0.76%, тест в целом 1.62%→1.18%. +4 unit-теста. style.rs, display_list.rs, femtovg_backend.rs, cpu_raster.rs |
| [BUG-241](bugs/BUG-241-FIXED.md) | FIXED 2026-06-23 | layout | Вложенный column-flex item схлопывался по контенту вместо растяжения на cross-size ячейки (TEST-90: ячейки `.cell-item` короткие полоски вместо заполнения рядов). Причина: в `lay_out_flex` re-layout column-flex после предварительного indefinite-прохода не гейтился на *definite* cross-size — при `explicit_cross=None` эффективный cross падал на `line_cross` (no-op stretch), но re-layout всё равно записывал резолвнутую px `style.height` обратно в item, затирая `height:auto`; последующий проход с definite cross видел `is.height.is_some()` и пропускал настоящий stretch. Фикс: добавлен гейт `&& explicit_cross.is_some()`. +регресс-тест `flex_nested_stretch_after_indefinite_pass_fills_row`. box_tree.rs:7706 |
| [BUG-242](bugs/BUG-242-FIXED.md) | FIXED 2026-06-23 | layout/paint/shell | Фон канвы (CSS Backgrounds §3.11.1) не покрывал вьюпорт: фон корня (`<body>`→`<html>` через `propagate_canvas_background`) красился только на rect html-бокса (= размер контента). В окне больше страницы (фикс. 1024×720 страница в развёрнутом окне) фон body заливал лишь полосу размером с бокс, остальная канва оставалась белой (UA-дефолт). Видно при maximize TEST-90: мажента справа во всю ширину, но только на высоту 720, ниже — белое. Не ловится graphic_tests (гоняются ровно 1024×720, где html-бокс = канва). Фикс: `lumen_layout::canvas_background_color(root)` достаёт непрозрачный фон корня; shell передаёт его в новый `RenderBackend::set_canvas_background`; femtovg-бэкенд чистит **весь** кадр этим цветом вместо белого. box_tree.rs, backend.rs, femtovg_backend.rs, shell/main.rs |
| [BUG-240](bugs/BUG-240-OPEN.md) | OPEN (DEBTOR) | paint/test-harness | TEST-45: «4.73%» = артефакт устаревшего gdigrab-снимка (run.py без `--build`). Свежая сборка 2026-06-25 → **1.02%**; отрисовка слоёв корректна (свежий рендер совпадает с Edge пиксель-в-пиксель: `.no-repeat`/`.repeated`/`background-size`-тайлы, проверено сэмплами цветов). «Реальное расхождение radial-слоёв» из исходного отчёта не подтвердилось — был тот же устаревший снимок. Остаток = font-parity 9 monospace-меток (rule 3, BUG-128) + ~0.14% ряд y=0 (canvas-фон не закрашивает строку 0: html-бокс с propagated body-фоном сдвинут на y=1 схлопыванием margin:1px сквозь body без BFC). KNOWN_DEBTOR 1.02% (TEST-45) |
| [BUG-243](bugs/BUG-243-FIXED.md) | FIXED 2026-06-25 | js | `document.createElementNS` не определён на настоящем `document` (`dom.rs:5529`) + `install_svg_bindings` целиком падает (`svg.rs:246` `class SVGElement extends Element`, а глобального `Element`/`Node` нет). Любая страница, строящая SVG на клиенте (динамические графики/иконки/деревья, в т.ч. `docs/roadmap-*.html`), рендерится пустой: HTML/CSS-каркас рисуется верно, SVG-дерево отсутствует. SVG layout+paint работают (статический inline-SVG корректен); layout детектит SVG по имени тега (`box_tree.rs:521`), поэтому фикс = `createElementNS` должен создавать НАТИВНЫЙ узел (с `__nid__`), а не оторванный JS-объект. Подтверждено end-to-end (см. файл бага). Полный бриф для починки в `bugs/BUG-243-FIXED.md` |
| BUG-244 | FIXED 2026-06-29 | layout/paint (svg) | SVG presentation `transform` теряло компоненты вращения/сдвига. Корень был НЕ в применении только scale+translate, а в схлопывании в AABB: `lay_out_svg_element_position` клало результат `composed`-трансформа в осе-ориентированный `b.rect` (`apply_transform_to_bbox` ре-баундит повёрнутые углы), а paint рисовал из `b.rect` — поворот терялся. Доп. дефект: `rotate(a cx cy)` композился в порядке `R·T·T⁻¹ = R`, теряя центр. **Фикс (как CTM в браузере, по референсу Blink `SVGTransformList::Concatenate`):** (1) исправлен порядок композиции `rotate(a cx cy)` = `T(cx,cy)·R·T(-cx,-cy)`; (2) layout сохраняет полную doc-space матрицу `M_doc = viewport ∘ composed` в `BoxKind::SvgShape.svg_transform` (поле было неиспользуемо после layout); `b.rect` остаётся AABB для clip/hit-test; (3) `emit_svg_shape` при наличии rotate/skew (off-diagonal b/c≠0) оборачивает геометрию в `PushTransform`/`PopTransform` и эмитит её в user-координатах — pure translate/scale сохраняет точный `b.rect` fast-path. Регресс-тесты `svg_rotated_rect_paints_under_ctm_in_user_space` + `svg_untransformed_rect_has_no_ctm` (display_list.rs). gdigrab-ратчет TEST-130/132/135/137 — на следующем полном прогоне run.py (нужен ffmpeg; страницы JS-построены). `box_tree.rs:771/1320`, `display_list.rs:6210` |
| BUG-245 | FIXED 2026-06-30 | layout/paint (svg) | `fill-rule: evenodd` не соблюдался на самопересекающихся `<path>`/`<polygon>`: центр пентаграммы и кольца-через-subpath заливались как при nonzero (дырок нет). **Корень:** `display_list.rs` тесселлировал заливку SVG-path одинаково для NonZero и EvenOdd (`tessellate_fill` — per-contour ear-clip, заливает всё). **Фикс:** новый `svg_path::tessellate_fill_even_odd` — горизонтальный sweep: критические сканлайны на каждой Y-вершине **и** на каждом Y пересечения рёбер (чтобы внутри полосы порядок активных рёбер стабилен), на каждой полосе рёбра сортируются по X и парятся even-odd → заливаемые трапеции (2 треугольника). Выход — непересекающаяся замостка ровно even-odd области, поэтому nonzero-заливка треугольного супа в обоих бэкендах (femtovg+cpu_raster) даёт even-odd форму без изменений в бэкендах. `display_list.rs:6354` роутит EvenOdd на новую функцию, NonZero остаётся на ear-clip (нулевой риск регрессии зелёных тестов). Регресс-тесты: `even_odd_pentagram_has_hollow_centre`, `even_odd_concentric_squares_punch_ring`, `even_odd_simple_triangle_is_filled` (svg_path.rs). `svg_path.rs`, `display_list.rs` |
| BUG-246 | FIXED 2026-06-30 | layout/paint (svg) | `<use width=.. height=..>`, ссылающийся на `<symbol>` с `viewBox`, не масштабировал инстанс: все инстансы рисовались интринсик-размером viewBox независимо от width/height. **Корень:** арм `"use"` в `collect_svg_shapes_impl` (box_tree.rs) применял только `transform` + translate(x,y) `<use>`, игнорируя `width`/`height` инстанса и `viewBox` цели — viewBox символа нигде не применялся (`collect_svg_shapes_impl` рекурсит в детей символа напрямую). **Фикс (SVG 2 §5.7 — `<symbol>` через `<use>` ведёт себя как вложенный `<svg>` viewport):** при target=`symbol`/`svg` c viewBox вычисляю масштаб viewBox→viewport через `compute_preserve_aspect_ratio_transform` (viewport = width/height `<use>` → иначе symbol → иначе viewBox-дименсии = identity; preserveAspectRatio символа) и компоную `[sx,0,0,sy,tx,ty]` в `combined` ПОСЛЕ translate(x,y), т.е. в локальной системе символа. Регресс-тест `svg_use_symbol_viewbox_scales_to_use_size` (инстансы width=40/80 дают rect 40px/80px). gdigrab-ратчет TEST-132 — на следующем полном прогоне run.py. `box_tree.rs` арм `"use"` |
| BUG-247 | OPEN (DEBTOR) | paint (svg rasterizer) | Растеризация кривых/диагоналей/пунктира (SVG `<circle>/<ellipse>`, диагональные `<path>/<polygon>`-края, `stroke-dasharray`) расходится с Edge на ~1px по краю под gdigrab-захватом: для плотных кривых/пунктирных страниц это превышает строгие 0.5% даже без логического дефекта (геометрия/цвет верны, отличается только сглаживание края + субпиксельная фаза). Высокочастотный dash-узор особенно чувствителен (каждый штрих рассинхронен сдвигом). Замерено: TEST-136 (кривые) 1.98%, TEST-134 (dash) 3.69%. Класс «rule 3» (AA), но для не-текста. Найдено при создании TEST-134/136. **Срез 2026-06-30:** компонент «nonzero `<path>`/`<polygon>` fill triangle-soup AA-швы» устранён нативной заливкой по контурам (`DrawSvgFill`, см. BUG-173) — TEST-138 1.93%→1.86%, вклад в TEST-54 2.30%→1.14%. Доминирующий остаток BUG-247 = edge-AA повёрнутых `<rect>` (FillRect под CTM), circle/ellipse-stroke-кольца (DrawBorder) и dash-штрихов — TEST-137 4.74%→4.73% без изменений (лопасти = повёрнутые rect, не fill), что подтверждает инхерентную femtovg-vs-Edge природу остатка. **Срез 2026-07-04 (штрихи):** SVG-stroke переведён с пре-тесселлированного triangle-soup (`DrawSvgPath`) на новую команду `DrawSvgStroke` (сырые контуры + StrokeParams): femtovg штрихует нативно через `stroke_path` (AA только на истинной границе штриха, без внутренних швов на кривых/пунктире; дэш-нарезка — тем же `apply_dash_pattern`, что и fallback), CPU/wgpu ре-тесселлируют через `tessellate_stroke_ex` — бит-идентично старому выводу (регресс-тест `svg_stroke_cpu_matches_tessellated_path`). Целевые компоненты — TEST-134 (dash 3.69%) и TEST-136 (кривые 1.98%) — замер на следующем полном gdigrab-прогоне run.py. **Замер P3 2026-07-04 (свежая dev-release-сборка main, чистый gdigrab):** TEST-134 (dash) 3.69%→**3.35%** (ратчет, подтверждён двумя чистыми прогонами), TEST-138 (dashboard polyline-обводки) 2.18%→**1.79%** (ратчет) — нативный `DrawSvgStroke` реально сгладил пунктир/полилинии; TEST-136 (circle/ellipse кривые) 1.98% без изменений (остаток = кольца `DrawBorder`, не штрих). Baseline'ы 134/138 в KNOWN_DEBTORS снижены. **Итог:** все тесты BUG-247 (133–138, 101, 30/31/36 по классу) уже в KNOWN_DEBTORS; остаток инхерентен (femtovg-vs-Edge edge-AA повёрнутых `<rect>` FillRect под CTM + circle/ellipse-кольца DrawBorder + dash-фаза), точечного логического дефекта нет → переклассифицирован в **DEBTOR** (класс BUG-124/176). Остаток BUG-247 = повёрнутые `<rect>` (FillRect под CTM) + circle/ellipse-кольца (DrawBorder) |
| BUG-248 | FIXED 2026-06-29 | paint (rasterizer) | TEST-21 `border-style` dashed/dotted/double. Прежний baseline 3.13% (сборка 1108a9bb) оказался артефактом устаревшего gdigrab-снимка (класс BUG-240): полный full-build прогон 2026-06-29 дал **0.49%** (≤0.5%, цель достигнута). Запись «21» убрана из KNOWN_DEBTORS. Геометрия бордеров верна; остаток <0.5% = AA-фаза штрих/точка-сегментов (класс BUG-247). |
| BUG-249 | FIXED 2026-06-29 | layout/paint | TEST-101 (интеракция `border-radius` × `overflow:hidden`): скруглённый клип детей расходился с Edge (углы/круг/pill/вложенность). **Корень:** оба бэкенда игнорировали `radii` у `PushClipRoundedRect` и обрезали прямоугольным scissor (femtovg `canvas.scissor`; cpu_raster `clip_stack` Rect) — углы детей оставались острыми. **Фикс (как clip-path/BUG-140):** subtree рендерится в offscreen-слой, `PopClip` композитит его одним `fill_path` по скруглённому контуру (`rounded_rect_clip_path` femtovg / `rasterize_rounded_rect_coverage` cpu) — углы следуют border-radius. Радиусы клампятся к половине меньшей стороны (круг/pill корректны). gdigrab TEST-101 **3.90% → 0.71%**; остаток = sub-pixel AA скруглённых краёв под gdigrab (класс BUG-247), KNOWN_DEBTOR перенаправлён на BUG-247@0.71%. Регресс-тесты: `clip_radii_clamped_to_half_min_side`, `rounded_rect_clip_path_builds`, `rounded_clip_cuts_child_corners`. `femtovg_backend.rs`, `cpu_raster.rs` |
| [BUG-250](bugs/BUG-250-FIXED.md) | FIXED 2026-06-27 | paint (font-metrics) | TEST-02/04 (`color-named`/`color-alpha`) и TEST-56 (`mix-blend-mode`) пересекли порог 0.5% после коммита 51dbf9fd (FontMeasurer: хардкод ascent/descent 0.8/0.2 → пропорции OS/2 sTypoAscender/Descender). Сдвиг вертикальной базовой линии текста на доли пикселя → текстовые строки чуть разошлись с Edge (02/04: 0.68%, регион — строка текста y124–304; 56: 1.83%, рассеяно по подписям сетки блендов). Зафиксированные baseline'ы 02/04 (0.0%, промоут 25.06 21:22) устарели — сделаны ДО смены FontMeasurer. Класс «rule 3» (font-parity, как BUG-128). Найдено при перепроверке вчерашних коммитов 2026-06-26. Фикс: возвращено старое вычисление descent через `units_per_em` (494/2048 σ font_size), ascent оставлен пропорциональным. |
| BUG-251 | FIXED 2026-06-27 | paint | Workspace clippy red: `unused variable: region` в тесте `extend_region_replicated_fills_top_band` (`crates/engine/paint/src/backends/femtovg_backend.rs:4149`) — `let region = extend_region_replicated(...)` биндится, но ассерты проверяют `px`, не `region`. `cargo clippy --workspace --all-targets -- -D warnings` падает (lumen-paint lib test). Предсуществует на main с коммита 51dbf9fd (2026-06-25). Тривиальный фикс: `let _region` / `_ =`. Блокирует общий clippy-гейт всех ролей. Найдено P1 при /lumen-task-finish 2026-06-26 |
| BUG-252 | FIXED 2026-06-27 | core | `cargo clippy -p lumen-core --all-targets -- -D warnings` КРАСНЫЙ после ph3-color-management: три неиспользуемые константы `SRGB_TO_XYZ_D65`/`DISPLAYP3_TO_XYZ_D65`/`REC2020_TO_XYZ_D65` (`crates/core/src/icc.rs:~1034,1042,1050`, добавлены как forward-матрицы D→XYZ, но инверсного пути нет) + `unused variable: t` в тесте `build_rgb_transform_to_rec2020_accepts_rec2020_profile` (`icc.rs:~1755`, `let t = ...` биндится, `t.apply()` не вызывается). Второй красный clippy-гейт помимо BUG-251 (paint). Фикс: удалить мёртвые константы или `#[allow(dead_code)]` + `let _t`. Найдено при ревью коммитов 2026-06-27 (c486539c..510b0e6d) |
| BUG-253 | FIXED 2026-06-27 | core (color) | Знаковая ошибка в Rec.2020 OETF: `rec2020_encode` (`crates/core/src/icc.rs:1092`) считает `(ALPHA - 1.0) + ALPHA * c.powf(0.45)` вместо BT.2020 `ALPHA * c.powf(0.45) - (ALPHA - 1.0)`. На белом (c=1) даёт 1.198 вместо 1.0, по всему тон-диапазону +яркость. Затрагивает ICC-путь (matrix-shaper) для Rec.2020-дисплеев. Дубликаты `rec2020_gamma_encode` в `image/src/lib.rs:394` и `paint/src/renderer.rs:3728` написаны ВЕРНО — расходится только icc.rs. Тест не ловит (не вызывает apply). Найдено при ревью 2026-06-27 |
| BUG-254 | FIXED 2026-06-27 | js | Регрессия теста: `cargo test -p lumen-js` КРАСНЫЙ — `dom::tests::window_remove_event_listener_stops_popstate` падает после navapi Phase 1d (a66d36b1). `history.back()` теперь только ставит дельту в очередь (`take_history_traversals`), `popstate` доставляется шеллом — в юнит-тесте шелла нет, `count` остаётся 0 вместо 1. Соседние тесты той же группы обновлены симуляцией `_lumen_deliver_popstate`, этот — нет. Фикс: добавить `_lumen_deliver_popstate(...)` перед проверкой count, как в `history_go_single_step_popstate`. Найдено при ревью 2026-06-27 |
| BUG-255 | FIXED 2026-06-27 | shell/js (Navigation API) | Shell-backed Navigation API state нефункционален: `commit_nav_state` (`crates/shell/src/main.rs:~12683`) делал `eval_js(format!("_lumen_navigation_set_state({json})"))` — `json` без кавычек интерпретировался JS как объектный литерал, нативный binding ждал `String` → coercion `"[object Object]"` → `JSON.parse` падал → `navigation.entries()`/`currentEntry` всегда пустые. Доп.: поле `state` в JSON двойной экранировки (строка `"null"` вместо `null`). Фикс: передавать JSON строковым литералом с экранированием через `serde_json::to_string`, `state` парсить через `serde_json::from_str`. Найдено при ревью 2026-06-27 (d97097e6) |
| BUG-256 | FIXED 2026-06-27 | shell (Navigation API) | Коллизия nav-ключей: `commit_nav_state`/`navigate_to` (`crates/shell/src/main.rs:~12761`) присваивал `format!("nav-{nav_key_counter}")` И новой записи в `nav_back`, И «текущей» записи — одинаковый ключ на каждой навигации → `navigation.entries()` отдавал неуникальные `key`, `traverseTo(key)` не мог однозначно разрешить цель. Фикс: добавлено `current_nav_key: String` в `Lumen`; текущей записи ключ не меняется при перемещении между стеками, новая страница получает свежий ключ после инкремента счётчика. Найдено при ревью 2026-06-27 (d97097e6) |
| BUG-257 | FIXED 2026-06-27 | shell (Windows GDI) | Подозрение: запрос ICC-профиля дисплея всегда возвращал `Srgb` на Windows. `query_gdi_icc_profile` (`crates/shell/src/platform/display_color_profile.rs:54`) первым вызовом `GetICMProfileA(hdc, NULL, &buf_size)` проверял `== 0` для раннего выхода, но по MSDN вызов с NULL-буфером возвращает FALSE (устанавливая только размер) → ранний `return None` → широко-гаммная ветка ph3 никогда не активировалась. Также `PlatformDisplayColorProfile.cached` объявлен как `Option`, но заполнялся только через `&self` (невозможно). Фикс: убрано условие `== 0` (оставлен только `buf_size == 0`), `cached` переведён на `std::sync::OnceLock`, GDI запрос выполняется ровно один раз. Найдено при ревью 2026-06-27 (c486539c) |
| BUG-258 | FIXED 2026-06-29 | image (clippy) | `cargo clippy -p lumen-image --all-targets -- -D warnings` был КРАСНЫЙ: `clippy::excessive_precision` на двух литералах в `rec2020_gamma_encode` (`crates/engine/image/src/lib.rs:394`) — `1.099_296_826` и `0.099_296_826` несли больше значащих цифр, чем представимо в `f32`. Блокировал dependency-clippy всех крейтов, тянущих lumen-image. Фикс: усечены до round-trip-точных `1.099_296_8` / `0.099_296_82` (значения из clippy-подсказки; `1.099_296_8` уже использовался в парном `rec2020_gamma_decode`). Gate-unblock в ветке BUG-244 (одобрено пользователем 2026-06-29). Найдено P1 при RP-1 /lumen-task-finish 2026-06-29 |
| BUG-259 | FIXED 2026-06-29 | paint (cpu_raster test) | `cargo test -p lumen-paint` НЕ КОМПИЛИРУЕТСЯ: `error[E0063] missing field color_space in initializer of lumen_layout::GradientStop` в `crates/engine/paint/src/cpu_raster.rs:2936,2937,3200,3201` (и др., 11 ошибок) — поле `GradientStop.color_space: ColorSpace` добавлено ICC/color-management-работой (`layout/src/style.rs:5127`), но тест-инициализаторы в cpu_raster.rs не обновлены. Предсуществует на чистом main, не связано с RP-3 (RP-3 не трогает paint). Блокирует scoped-test/workspace test-гейт всех ролей (lumen-paint тянется в замыкание driver/shell). Тривиальный фикс: добавить `color_space: ColorSpace::Srgb` (или дефолт) в каждый затронутый литерал GradientStop в тестах cpu_raster.rs. Найдено P1 при RP-3 /lumen-task-finish 2026-06-29 |
| BUG-260 | FIXED 2026-06-29 | shell (сборка) | `lumen-shell` (lib+bin) НЕ КОМПИЛИРОВАЛСЯ на чистом main: `error[E0063] missing field text_orientation` в 90 конструкторах `DisplayCommand::DrawText` по 32 файлам шелла (chrome/UI-текст: `strip.rs`, `address_bar.rs`, `find.rs`, все `panels/*`, и др.). Поле `DrawText.text_orientation: Option<TextOrientation>` добавлено вертикальным inline-потоком (коммит `235f0ca2` P3-vertical Phase 2): paint/layout-path обновлены, но 90 собственных конструкторов шелла — нет. Блокировал ВСЮ работу в шелле (включая `cargo run -p lumen-shell`) у всех ролей, не только finish-гейт. Фикс: `text_orientation: None` (= горизонтальный дефолт, корректен для всего chrome-текста) во все 90 сайтов. Найдено и исправлено P1 при RP-2 2026-06-29 |
| BUG-261 | FIXED 2026-06-29 | paint (svg) | РЕГРЕССИЯ TEST-82 (`<use>` SVG), та же первопричина, что BUG-262. 5.66% замерен прогоном 2026-06-29 17:39 на СТАЛОМ бинаре от 15:45 (BUG-244 влит 15:19, BUG-262 — лишь 20:24): run.py без --build не пересобирает lumen.exe. BUG-244 переиспользовал `BoxKind::SvgShape.svg_transform` под document-space paint-матрицу, перезаписывая её в `lay_out_svg_element_position`. Flex-ряды TEST-82 раскладываются дважды (intrinsic + финал); на втором проходе перезаписанная матрица читалась как «собственный transform клона» → `<use>`-клоны (особенно ряды `<g>`/`<symbol>`/nested) уезжали вне clip-rect и пропадали. BUG-262 (влит 20:24) починил это, разведя paint-матрицу в отдельное поле `svg_paint_matrix`; `svg_transform` снова immutable layout-input. На свежем HEAD display-list полон (все клоны), CPU-снимок совпадает с Edge, gdigrab TEST-82 = 2.31% ≤ baseline 2.38%. Регресс-тест `svg_use_clones_in_flex_keep_position_across_relayout`. box_tree.rs:1339 |
| BUG-262 | FIXED 2026-06-29 | layout (svg) | РЕГРЕССИЯ TEST-119 (`paint-order`) 0.81%→16.52% — корень НЕ в paint-order и не AA-остаток stroke, а в layout: третья инлайн-блочная `<svg>` (первая на перенесённой строке) теряла весь path-cell. ПРИЧИНА: BUG-244 переиспользовал `BoxKind::SvgShape.svg_transform` под document-space paint-матрицу, перезаписывая её в `lay_out_svg_element_position`. Инлайн-блок при переносе строки раскладывается дважды (box_tree.rs:6308 + повторно 6335); на втором проходе перезаписанная матрица (с трансляцией строки-1 989,29) читалась как «собственный transform элемента» → path смещался в (1018,394), вне своего clip-rect (29,365,460,320) → невидим. ФИКС: добавлено отдельное выходное поле `svg_paint_matrix`; `svg_transform` теперь immutable layout-input, повторная раскладка рекомпозит из чистого элемент-трансформа. Регресс-тест `svg_path_inline_block_wrap_keeps_child_in_root`. box_tree.rs:1339 |
| BUG-263 | FIXED 2026-06-29 | layout (clippy) | `cargo clippy -p lumen-layout --all-targets -- -D warnings` (и любой scoped clippy, тянущий lumen-layout в замыкание, напр. `-p lumen-paint`) был КРАСНЫЙ: `clippy::too_many_arguments` на трёх функциях — `apply_declaration` (`crates/engine/layout/src/style.rs:11213`, 8/7), `lay_out_vertical_inline_run` (`crates/engine/layout/src/vertical.rs:490`, 9/7), `wrap_inline_run_vertical` (`crates/engine/layout/src/vertical.rs:548`, 11/7). Предсуществовал на чистом main (vertical.rs-функции от P3-vertical Phase 2), блокировал finish-clippy-гейт всех ролей (тот же класс, что BUG-251/252/258/260). Фикс: `#[allow(clippy::too_many_arguments)]` на каждую (идиоматичный нефункциональный фикс линта; рефакторинг сигнатур в структуру отложен). Gate-unblock в ветке BUG-259. ПРИМ.: это не зеленит `-p lumen-layout --all-targets` полностью — там остаётся `items_after_test_module` в vertical.rs (→ BUG-264). Найдено P3 при /lumen-task-finish BUG-259 2026-06-29 |
| BUG-264 | FIXED 2026-07-01 | code-health (workspace clippy) | `cargo clippy --workspace --all-targets -- -D warnings` был КРАСНЫЙ (EXIT 101) — линты в feature-gated таргетах, не ловимые ни дефолтной сборкой, ни scoped `-p <crate>`. **P5-территория.** Остававшаяся часть (1) `lumen-paint (lib)` wgpu-рендер `renderer.rs`: 16× `redundant_semicolons` через `};;` в макросе `flush_batch!` (`renderer.rs:4196`) — убран лишний `;`; 1× `dead_code` `rec2020_gamma_decode` (`renderer.rs:3722`) — функция не имела вызывающих (пара `rec2020_gamma_encode` используется), удалена; 16× `excessive_precision` на rec2020/P3-матрицах (`renderer.rs:3724/3727/3730`) — усечены `cargo clippy --fix`. Пункт (2) из формулировки BUG-264 (`E0063 missing field text_orientation` в `cpu_raster.rs`) при перепроверке 2026-07-01 уже не воспроизводится — устранён по пути более ранним фиксом (класс BUG-260). Регресс: `cargo test -p lumen-paint` и `--features backend-wgpu` зелёные. Найдено P3 при /lumen-task-finish BUG-259 2026-06-29, закрыто P5 при health-свипе 2026-07-01 |
| BUG-265 | FIXED 2026-07-01 | code-health (workspace clippy) | Второй красный gate-пункт `cargo clippy --workspace --all-targets`, вскрывшийся сразу после починки BUG-264: `clippy::len_zero` в `crates/bidi-server/src/protocol.rs:2006` (`assert!(...unwrap().len() > 0)` в тесте `capture_screenshot_with_live_window_returns_base64_data`) — не ловится scoped `-p lumen-bidi-server` clippy (не воспроизводит без `--workspace` unification). Фикс: `!...is_empty()`. Найдено и исправлено P5 при health-свипе 2026-07-01 |
| BUG-266 | FIXED 2026-07-01 | code-health (workspace clippy) | Третий красный gate-пункт: `clippy::too_many_arguments` (10/7) на `run_window_mode` (`crates/shell/src/main.rs:546`) — тот же класс, что FIXED BUG-263. Фикс: `#[allow(clippy::too_many_arguments)]` (идиоматичный нефункциональный фикс, как BUG-263; рефакторинг сигнатуры в структуру отложен). `cargo clippy --workspace --all-targets -- -D warnings` теперь чист. Найдено и исправлено P5 при health-свипе 2026-07-01 |
| [BUG-267](bugs/BUG-267-FIXED.md) | FIXED 2026-07-03 | paint (CPU-растеризатор) | CPU-путь рендера (`--screenshot`/PDF/IPC-снимок/cpu_vs_edge-гейт) был катастрофически медленным на больших полотнах со слоями/фильтрами: `lenta.ru` `--screenshot` = **141.7 с** при fetch+JS+layout всего 5.3 с (полотно 1024×7324) ⇒ ~136 с чистой растеризации. Причина: `cpu_raster.rs` гонял полнополотные аллокации/clone/blur/blend-проходы на каждый Push/Pop-слой, `PushFilter` игнорировал `bounds`. **Срез A (2026-07-03, P3):** dirty-bbox на слоях (`CpuLayer`), `PopFilter`/`PushBackdropFilter` кропают blur до bbox ink ⊕ 3r — бенч 60 теней: 169.5 с → 1.5 с (×113), PNG байт-в-байт. **Срезы B–D (2026-07-03, P3):** clip- (rounded-rect + clip-path), mask-, opacity- и blend-композиты клонируют/умножают/блендят только dirty-окно слоя ⊕ 2px AA (`crop_dirty_window`) — вне окна источник прозрачен, композит бит-идентичен. Бенч 60 рядов clip+mask+opacity+blend (1024×6000): **40.1 с → 0.34 с (×117), PNG байт-в-байт**; все 145 страниц graphic_tests через `--screenshot` байт-в-байт против main; 7 регресс-юнитов «кроп == полное полотно», включая все 17 blend-режимов. Нюансы: AA-покрытия clip/масок растеризуются по-прежнему полнополотно (перенос пути на смещение кропа не гарантирует бит-идентичное f32-округление рёбер; дёшево — одна аллокация без blend-проходов); transform-композит не кропается (bilinear-ресемплинг через скомпонованную аффину не бит-идентичен), пустые группы скипаются. Живого окна (femtovg/GPU) не касается. Найдено аудитом реальных сайтов 2026-07-02 |
| [BUG-268](bugs/BUG-268-FIXED.md) | FIXED 2026-07-04 | shell (загрузчик стилей) + html-parser (preload) | Экранный рендер применял `<link rel=stylesheet media="print">`: на `www.w3.org` после каждой ссылки печатался её URL в угловых скобках (print-правило `a::after{content attr(href)}`). Каскад отсекает `@media print` корректно, но `collect_link_hrefs()` собирал href всех `<link rel=stylesheet>` **без проверки атрибута `media`** — print-only лист целиком вливался в каскад (его правила не обёрнуты в `@media`, каскад их не фильтрует). **Фикс:** media-гейт у `<link>` через штатный `parse_media_query().matches()` (второго матчера нет). `collect_link_hrefs`/`load_linked_stylesheets` принимают `MediaContext` параметром; экранный контекст строит `screen_media_context(viewport, dark_mode)` — те же media_type/размеры/prefers-color-scheme, что каскадный `media_context_from_viewport`. Preload-путь: `PreloadHint::Stylesheet` получил поле `media` (preload_scanner.rs), `feed_preload_and_emit` не греет кэш и не шлёт `CssLoaded` для не матчащих листов (progressive-кадры не красятся print-стилями). Тесты: `collect_link_hrefs_media_gate` (шелл), `link_stylesheet_media_attr_preserved` (html-parser). **Отложено → BUG-270:** print-пайплайн (`do_print_to_pdf`) всё ещё хардкодит `media_type="screen"` (`style.rs`), поэтому PDF не применяет `@media print`; гейт уже параметризован под передачу `media_type="print"`. Найдено аудитом 2026-07-02, исправлено P3 2026-07-04 |
| [BUG-269](bugs/BUG-269-FIXED.md) | FIXED 2026-07-04 | layout/shell (замещаемые элементы, aspect-ratio) | `<img width="240">` без явной высоты рендерился с нулевой высотой (невидим). Корень в shell: гейт `wants_intrinsic = !has_w && !has_h` пропускал `apply_intrinsic_size` при ЛЮБОМ заданном атрибуте, а для `width`-only DOM оставался без height и без intrinsic → used height = 0. Фикс: гейт → `!(has_w && has_h)` (применять intrinsic, если хоть одна сторона не задана), а `apply_intrinsic_size` теперь aspect-ratio-aware — недостающую сторону считает из intrinsic ratio (CSS 2.1 §10.6.2), `width="240"` intrinsic 120×80 → 240×160 (не 240×80 и не 0). Presentational hint (specificity 0), percentage-размеры не перетираются. +5 юнит-тестов `bug269_*`. main.rs |
| BUG-270 | FIXED 2026-07-04 | layout (print media) + shell (print-пайплайн) | Генерация PDF (`do_print_to_pdf`/`do_print_to_pdf_with_opts`) хардкодила `MediaContext.media_type="screen"`, поэтому печать **не применяла** `@media print`-правила и `<link rel=stylesheet media=print>`. **Фикс:** каскад — `media_context_from_viewport` теперь выбирает `"print"`/`"screen"` по sticky thread-local (`set_print_media`/`print_media_active`, паттерн `FORCED_COLORS`), read-точка та же, что у `@media`-фильтра; `parse_and_layout` получил параметр `media_print` — оборачивает layout-проход в `set_print_media(true)`+reset и строит print-`MediaContext` (`print_media_context`) для media-гейта `<link>` (BUG-268). Оба print-пайплайна передают `media_print=true`, все экранные/headless/dump-пути — `false`. Юнит-тест `bug270_print_media_flag_switches_cascade_media_type`. Найдено при фиксе BUG-268 2026-07-04 |
| [BUG-271](bugs/BUG-271-FIXED.md) | FIXED 2026-07-07 | shell (event loop) + js (таймер-шим) | После загрузки JS-тяжёлой страницы (lenta.ru) процесс постоянно жёг ~1.6 ядра CPU при нуле отрисованных кадров. **Корень:** протухший `ControlFlow::WaitUntil` — после того как последний JS-таймер отработал (`take_timer_wakeup()`→`None`), в winit оставался дедлайн в прошлом = Poll-подобный spin ~22 800 итераций/с, каждая с несколькими блокирующими eval-round-trip'ами в поток `lumen-js`. **Фикс:** (1) `about_to_wait` всегда выставляет control flow явно — `WaitUntil(min(timer, rAF))` либо `Wait`; (2) rAF отвязан от полной перерисовки: вместо безусловного `has_raf_pending→request_redraw` (60 fps full paint для пустого rAF-цикла, 0.95 ядра) — rAF-насос в `about_to_wait` по WaitUntil-таймеру, repaint только при `take_dom_dirty`; (3) таймер-шим клампит вложенные (>5) timeout/interval к ≥4 мс (HTML LS §8.6). lenta.ru в простое: 1.6 ядра → 0.002; чистый rAF-цикл 0.95 → 0.04 ядра; rAF-анимации сохраняют 60 fps. Найден и исправлен P3 2026-07-07 |
| [BUG-272](bugs/BUG-272-OPEN.md) | OPEN (срез 1 влит 2026-07-07, срез 2 влит 2026-07-08, срез 3 влит 2026-07-15, срез 4 влит 2026-07-15, срез 5 влит 2026-07-16, срез 6 влит 2026-07-18, срез 7 влит 2026-07-18, срез 8 влит 2026-07-18, срез 9 влит 2026-07-18) | paint (femtovg, offscreen-слои) + font (bytes_store) | lenta.ru держала 1371 МБ WS при одной картинке. **Корень:** каждый Push{ClipRoundedRect,ClipPath,Opacity,Filter,Mask,Backdrop,BlendMode} создавал offscreen-текстуру размером со весь framebuffer и освобождал её только после flush в конце кадра — ~150 слоёв × 5 МБ жили одновременно, драйвер удерживал пик (1168 МБ GPU-памяти процесса). **Срез 1:** пул переиспользуемых слоёв (`layer_pool`, release на Pop → reuse на Push; пик = глубина вложенности): синтетика 120 клипов 1025→227 МБ GPU; lenta.ru 1371→713 МБ WS. **Срез 2:** blend-mode (PA-3) offscreen src-слой переведён на тот же `layer_pool` (он никогда не сэмплится GPU, только рендер-таргет + screenshot-readback → FLIP_Y-флаг пула не влияет): синтетика 120 mix-blend-mode:multiply 1230→637 МБ WS на первом кадре. lenta.ru mix-blend-mode не использует — эффект не проявляется на конкретно этом сайте. **Срез 3:** one-off CPU-композитный `result_id` (create_image на каждый Pop, GPU-сэмплится → нужен отдельный не-FLIP_Y пул) переведён на новый `blend_result_pool` с переиспользованием слота через `update_image` вместо пересоздания; безопасность reuse гарантирует `canvas.flush()` в начале каждого `composite_blend_layer`, исполняющий отложенный `fill_path` предыдущего слоя до перезаписи. Убирает один из подозреваемых источников роста WS в простое на blend-heavy страницах (см. «Новое наблюдение» в файле бага). **Срез 4:** два оставшихся one-off того же класса — `composite_filter_layer`'s colour-matrix re-upload и `apply_backdrop_filters`'s filtered-backdrop re-upload — переведены на тот же пул, обобщённый в `cpu_upload_pool`/`acquire_cpu_upload_image`/`release_cpu_upload_image` (был `blend_result_pool`); безопасность разделяемого reuse — каждый потребитель безусловно флашит canvas перед своим re-upload шагом. **Срез 5:** первый шаг пункта «bounding-box-слои вместо full-frame» — `apply_backdrop_filters`'s filtered-backdrop теперь обрезается (`crop_region_rgba`) до bbox элемента (device px) перед аплоадом вместо полного framebuffer, через новый выделенный `backdrop_bbox_pool` (не общий `cpu_upload_pool` — переменный размер по элементам вытеснял бы full-framebuffer-потребителей общего пула). `elem_image_id` и остальные Push-типы (Opacity/Filter/ClipRoundedRect/ClipPath/Mask*/BlendMode) пока не тронуты — full-framebuffer, план следующих срезов в файле бага. Визуальная приёмка 2026-07-16: A/B gdigrab branch-vs-main (штатная TEST-00-калибровка была сломана десктопной цветовой трансформацией night-light/HDR-класса) — TEST-30/103 побайтово идентичны main, срез пиксельно нейтрален. **Срез 6 (font):** @font-face-шрифт хранился дважды — в `FontRegistry::bytes_store` и в `LoadedFace::bytes` рендера (дефолтный wgpu/CPU-путь глифов). `read_face_bytes` переведён с `Option<Vec<u8>>` на `Option<Arc<[u8]>>`; т.к. байты в реестре уже sfnt (`maybe_decode_font`→`Ok(None)`), рендер складывает тот же Arc — обе стороны разделяют одну аллокацию вместо двух копий (экономия — одна полная копия каждого кастомного шрифта). **Срез 7:** `PushOpacity` несёт `bounds` (document-space bbox); off-viewport opacity-группы куллятся целиком (`group_bounds`+`matching_close`, тот же механизм, что BUG-273 срез 1 для blend). **Срез 8:** тот же viewport-cull распространён на чистые clip-опенеры `PushClipRoundedRect` (`rect`) / `PushClipPath` (`shape.bounding_rect()`) — clip самый безопасный класс (ограничивает детей своей областью). **Срез 9:** viewport-cull распространён на все четыре mask-опенера `PushMask{Image,LinearGradient,RadialGradient,ConicGradient}` по их `rect` — mask, как clip, ограничивает видимые пиксели своим `rect` (image — scissor; градиент — `DestinationIn` только по `rect` обнуляет alpha вне него). `PushMaskLayer` (SVG-`<mask>` content) отложен. Остаток: glyph atlas (~285 МБ над baseline), bbox-сайзинг **видимых** слоёв, многокопийные image-кэши — план в файле бага |
| [BUG-273](bugs/BUG-273-OPEN.md) | OPEN (срез 1 влит 2026-07-16) | paint (femtovg, blend/filter-композитинг) | Прокрутка страниц с mix-blend-mode/filter/backdrop-filter — слайдшоу: 1000000-final.html ~660 мс/кадр (1.5 FPS), baseline без них 4.6 мс. **Корень (профиль LUMEN_FRAME_LOG=2):** каждый blend/filter-слой на каждом кадре = flush → glReadPixels всего framebuffer (синхронный GPU-стоп) → попиксельный CPU-бленд → upload обратно (~78 мс/слой); межкадрового кэша нет. **Срез 1:** `run_content_pass` теперь пропускает целиком Push…Pop-группу (`PushFilter`/`PushBackdropFilter`/`PushBlendMode`), если её bbox (новое поле `PushBlendMode.bounds`) не пересекает viewport (переиспользует ADR-016 M0.2 `is_command_culled`, ранее применявшийся только к листьям) — ни offscreen-layer, ни CPU-readback composite для невидимого контента. Остаток: межкадровый кэш слоя (срез 2); полный фикс = GPU-бленд в wgpu/vello (Phase 3). Вместе с багом влиты инструменты: LUMEN_FRAME_LOG=1/2 + scripts/scroll_perf.py |
| [BUG-274](bugs/BUG-274-OPEN.md) | OPEN | paint (WgpuBackend) / shell (event loop, wgpu-путь) | Сравнение `LUMEN_BACKEND=femtovg` vs `=wgpu` на 1000000-final.html: в простое (10с, без ввода) wgpu жжёт ~1422 мс CPU против 375 мс у femtovg (~4×) — похоже на не закрытый для wgpu-пути аналог BUG-271 (protuhший WaitUntil/безусловный rAF-redraw), но не подтверждено профилем. Отдельно: разовый скачок памяти между t=5с и t=10с после старта (WorkingSet 763.6→1034.9 МБ, PrivateBytes 741.7→1015.7 МБ), ДО первого взаимодействия — 8 PageDown + 8 PageUp после скачка память не меняют (не утечка, разовая стоимость инициализации). На скролле wgpu, наоборот, экономичнее CPU (62.5 мс vs 203 мс у femtovg). Корень не диагностирован — только симптом и гипотезы, план в файле бага |
| [BUG-275](bugs/BUG-275-OPEN.md) | OPEN | paint (`Renderer::new_async`, выбор wgpu-бэкенда) | `WGPU_BACKEND=vulkan` на Intel Iris Plus (Windows 10) — окно презентует целиком белым, хотя рендер по всем наблюдаемым признакам исправен (реальный адаптер, кадры кодируются/презентуются без ошибок валидации, конфигурация поверхности идентична рабочему DX12-пути). Обходится тем, что `Renderer::new_async` пробует DX12 первым; Vulkan — fallback за explicit opt-in. Корень (драйвер/WSI Intel Iris Plus либо wgpu 26 Vulkan-swapchain) не подтверждён — нужна проверка на другой машине/GPU |
| [BUG-276](bugs/BUG-276-FIXED.md) | FIXED 2026-07-13 | paint (`WgpuBackend`/`renderer.rs`) | `LUMEN_BACKEND=wgpu` не проходил даже калибровочный TEST-00 (4.85% против эталона Edge, диф в полосе `y:684-718`) — набор графических тестов никогда не гонялся против wgpu. Root-cause: `PushClipRect`/`PushClipRoundedRect`/`PushClipPath` клали clip rect в `clip_stack`, не применяя накопленный `transform_stack` (shell сдвигает контент на `TAB_BAR_HEIGHT=36` через `PushTransform`) → scissor обрезал нижние 35px контента. Фикс — `apply_transform_to_clip()` перед пересечением с `clip_stack.last()`. TEST-00 → PASS (0.00%); полный прогон установил wgpu-базлайн 65/141 PASS (список расхождений → BUG-277) |
| [BUG-277](bugs/BUG-277-OPEN.md) | OPEN — теперь активный default-backend долг (не только Phase-3 гейт) | paint (`WgpuBackend`/`renderer.rs`) — wgpu-рендеринг расходится с femtovg | Установленный wgpu-базлайн: первый полный прогон `LUMEN_BACKEND=wgpu graphic_tests/run.py --continue-on-fail` после фикса BUG-276 — 65 PASS · 38 FAIL · 38 DEBTOR (существующие femtovg-долги) · 0 ERR. Список 38 FAIL-тестов и их diff% зафиксирован в файле бага. `P1-wgpu-flip` (2026-07-13) перевёл wgpu в дефолтный бэкенд без переноса этого базлайна в `KNOWN_DEBTORS` → полный прогон без `LUMEN_BACKEND=` читался как «43 FAIL регрессия» (заведено как [BUG-287](bugs/BUG-287-FIXED.md), закрыто P3 2026-07-16 как объяснённое этим багом). `KNOWN_DEBTORS` теперь содержит 35 из 37 живых wgpu-чисел (2 достигли ≤0.5%/ратчетнуты независимо) — таблица в файле бага остаётся историческим снимком 2026-07-13, актуальные числа = `graphic_tests/run.py`. Остаётся открытым: довести каждый до паритета с Edge либо обосновать постоянный `KNOWN_DEBTOR` (крупные ≥10% провалы 49/59/76/101/104 — кандидаты на реальный wgpu-рендер-дефект, не просто AA-дрейф; TEST-76 разобран точечно в BUG-287 «Корень найден») |
| [BUG-278](bugs/BUG-278-FIXED.md) | FIXED 2026-07-13 | network (HTTP client) | HTTP-клиент отвергал close-delimited ответы (ни `Content-Length`, ни `chunked`, ни явный `Connection: close`) как протокольную ошибку — ломало все фетчи против `wptserve` (Python `http.server`, эталонный сервер WPT), который так и делает. RFC 7230 §3.3.3 п.7: read-to-EOF для ответов применяется безусловно, не только при явном `Connection: close`. Фикс — `read_response`/`read_response_streamed` теперь всегда читают до EOF как последний fallback. Найдено P2-wpt S4 |
| [BUG-279](bugs/BUG-279-FIXED.md) | FIXED 2026-07-13 (document-level) | js (live DOM bindings) | `document.getElementsByTagName` отсутствовал целиком в `var document = {...}` (`crates/js/src/dom.rs`) — ронял `testharness.js`'s собственный module-level setup (`test_timeout()`/`get_script_url()`) с `TypeError: not a function`. Фикс делегирует в уже существующий `_lumen_query_selector_all` (тот же паттерн, что и `querySelectorAll`). `Element.prototype.getElementsByTagName` и `document.getElementsByClassName` всё ещё отсутствуют (не в этом фиксе). Найдено P2-wpt S4 |
| [BUG-280](bugs/BUG-280-FIXED.md) | FIXED 2026-07-16 | js (JS environment bootstrap, `WEB_API_SHIM`) | `window` был плоским JS-объектом-литералом, не настоящим global object движка — свойства, назначенные через `window.x = ...`/`self.x = ...` (в т.ч. `testharness.js`'s `expose()`, ~50 функций: `test`, `assert_*`, `add_completion_callback`, …), были недостижимы как голые (неквалифицированные) идентификаторы. Фикс: `WEB_API_SHIM` копирует все свойства `window` на `globalThis` (значения — присваиванием, аксессоры — `defineProperty`), затем `window = globalThis` — с этого момента `window`/`self`/`globalThis` один и тот же объект (общий шим, оба движка). Проверено юнит-тестами (rquickjs) и живыми BiDi-пробами на дефолтной V8-сборке. Смок-тест `tests/wpt/run_smoke.py` по-прежнему не проходит целиком — по независимой причине, см. [BUG-291](bugs/BUG-291-FIXED.md). Найдено P2-wpt S4 |
| [BUG-281](bugs/BUG-281-FIXED.md) | FIXED 2026-07-14 | js (`WEB_API_SHIM`, `document`/`Element` shape) | React 18 (UMD prod build) падает на `ReactDOM.createRoot(...).render(...)` с `Cannot read properties of undefined (reading '_reactListening<rnd>')`. Причина — `document.nodeType`/`documentElement.tagName`/`ownerDocument`-идентичность/`namespaceURI` были неверны (см. файл бага). Фикс: `Document::document_element()` (root → `<html>`, отдельно от `root()`), новые нативы `_lumen_get_html_element`/`_lumen_get_namespace_uri` в обоих движках (QuickJS + V8), правки JS-шима (`document.nodeType=9`, `documentElement` возвращает реальный `<html>`, `element.namespaceURI`, `element.ownerDocument` — non-enumerable во избежание цикла document↔element в `from_rq`-сериализации `eval()`). Найдено Ph3-v8-migration S12 |
| [BUG-282](bugs/BUG-282-OPEN.md) | OPEN (DEBTOR 10.16%) | layout / css-parser (`@color-profile`, `color()`) | TEST-142: Lumen рендерит спек-корректные swatch-цвета для `color(--name c1 c2 c3)` (каналы трактуются как sRGB, реальная ICC-трансформация отложена); Edge не поддерживает `@color-profile`/custom-ident в `color()` вовсе — весь `color()` невалиден, фон элементов остаётся прозрачным. Тот же класс, что BUG-199/TEST-71 и BUG-126/TEST-77. Найдено P4 2026-07-15 |
| [BUG-283](bugs/BUG-283-FIXED.md) | FIXED 2026-07-15 | font (`Rasterizer::rasterize`) | Краш процесса (`memory allocation of 906238016744 bytes failed`) на https://lenta.ru через ~7с после старта, сразу после подгрузки кастомных `@font-face`. Причина: `rasterize()` считал `width = (x_max - x_min) as u32` без проверки `x_max >= x_min` — при инвертированном/испорченном bbox глифа отрицательная разность `i32` заворачивалась в число, близкое к `u32::MAX`, и `vec![0u8; width*height]` пытался выделить сотни ГБ. Фикс: диапазоны считаются как `i32`, `None` при `<= 0` (инвертированный/испорченный bbox) или `> 8192` (MAX_GLYPH_DIM — защита от экстремального, но валидного bbox/pixel_size) до каста в `u32`. 3 юнит-теста. Найдено при ручном прогоне релизной сборки 2026-07-15 |
| [BUG-284](bugs/BUG-284-FIXED.md) | FIXED 2026-07-15 | layout (`style.rs` cascade, `rule_index.rs`) | Браузер практически непригоден на реальных сайтах (https://ria.ru): каждый relayout стоил ~2.4с и вызывался повторно (6× подряд, ~13.6с полного зависания UI-потока) сразу после загрузки страницы, ещё до скролла. Причина — `RuleIndex` индексировал только `sheet.rules` верхнего уровня; правила внутри `@layer`/`@media`/`@supports` (и в `compute_pseudo_element_style`, который не индексировался вовсе) проверялись перебором для каждого DOM-узла. У ria.ru большая часть из 3093 правил лежит в `@media`-блоках. Фикс: `RuleIndex::build_from_rules` обобщён на произвольный срез правил; новый кэш `CascadeIndex` строит по одному индексу на каждый `@layer`/`@media`/`@supports` блок + переиспользуется в `compute_pseudo_element_style`. Relayout: ~2.4с → ~1.15с; повторные штормы relayout пропали (первая пачка DOM-мутаций укладывается в один проход). `cargo test -p lumen-layout` 3189/3189 зелёных; `graphic_tests/run.py --continue-on-fail` показал «Изменений нет» vs эталонный прогон на main (визуальных регрессий нет). Найдено при ручном тестировании 2026-07-15 |
| [BUG-286](bugs/BUG-286-OPEN.md) | OPEN (частичная митигация влита) | shell (`main.rs`, `maybe_expand_cv_relevant`) | Первый скролл колесом на тяжёлой реальной странице (https://ria.ru) подвисает на ~1с (`[engine] relayout 1041.99ms` посреди скролла) — `content-visibility:auto` relevance-триггер звал синхронный `self.relayout()` напрямую, а не `relayout_raf_dirty()`, которым пользуются остальные scroll/relayout-триггеры. Фикс маршрутизации влит (под `LUMEN_ENGINE_THREAD=1` теперь уходит на движковый поток), но на дефолтной сборке поведение не меняется — корневая стоимость (полный CSS-каскад на ~1141 узел) не устранена, см. остаток в BUG-284/`security-performance.md` §14.2. Найдено при расследовании BUG-284 2026-07-15 |
| [BUG-287](bugs/BUG-287-FIXED.md) | FIXED (не движковая регрессия) 2026-07-16 | paint (test-harness baseline, не движок) | Полный `graphic_tests/run.py --continue-on-fail` на `main` давал 43 FAIL — оказалось НЕ движковой регрессией. `git bisect` на представителе (TEST-76) сошёлся на `c76cbeae` (`P1-wgpu-flip`, 2026-07-13): wgpu стал дефолтным бэкендом, но `KNOWN_DEBTORS` в `graphic_tests/run.py` остался настроен под femtovg-baseline. Все 37 нетриаженных номеров (кроме TEST-14→BUG-288 и TEST-71 независимого ратчета) день-в-день совпадают с уже задокументированным wgpu-vs-Edge базлайном [BUG-277](bugs/BUG-277-OPEN.md) (измерен 2026-07-13, ДО флипа, как гейт для Phase 3). Фикс: `KNOWN_DEBTORS` обновлён — 35 записей подняты до живого wgpu-числа со ссылкой на BUG-277 (femtovg-родословная сохранена в комментарии), TEST-132/TEST-71 ратчетнуты вниз, TEST-138 удалён (≤0.5%). Полный прогон снова зелёный (0 FAIL). BUG-277 остаётся OPEN как основной трекер wgpu-парности |
| [BUG-288](bugs/BUG-288-OPEN.md) | OPEN (DEBTOR) | paint (`emit_scrollbars`) / test-triage | TEST-14 (overflow, часть BUG-287): overflow-axis coercion (BUG-020, CSS Overflow L3 §2.1 — один явный не-`visible` axis коэрсит второй `visible` в `auto`) корректно клипует контент, но `auto` — scrollable-значение, и `emit_scrollbars` (BUG-220, 2026-06-24) теперь рисует на нём статический scrollbar; Edge использует overlay-scrollbar, невидимый в headless-скриншоте (тот же класс, что TEST-83). Тест без текста — весь diff 1.63% это два scrollbar-виджета. TEST-14 → `KNOWN_DEBTORS` baseline 1.63%, порог 0.5% не менялся. Найдено P3 2026-07-16 при триаже BUG-287 |
| [BUG-289](bugs/BUG-289-FIXED.md) | FIXED 2026-07-16 | paint (`display_list.rs::emit_inline_run`) | Вертикальный `writing-mode` текст на реальных страницах никогда не рисовался корректно: layout (`vertical.rs`) верно вычислял позиции колонок/фрагментов, но `emit_inline_run` (единственный конвертер `InlineFrag → DrawText` для реального DOM) не проверял `writing_mode` вообще и трактовал `frag.x`/`frag.width`/`line_idx·line_h` как горизонтальную геометрию — вертикальные боксы либо оставались пустыми, либо текст накладывался горизонтально поверх соседних колонок. Найдено P3-vertical Срезом 5 (первый end-to-end graphic-тест на реальном DOM, `graphic_tests/145-writing-mode.html`) — все предыдущие unit-тесты (Срезы 1–3) проверяли либо layout-геометрию напрямую, либо paint с вручную сконструированными `DrawText`, минуя этот путь. Фикс: новая `emit_inline_run_vertical` читает те же поля с их реальным (вертикальным) физическим смыслом. Остаток (font-parity + upright per-glyph advance) → `KNOWN_DEBTOR` [BUG-290](bugs/BUG-290-OPEN.md) |
| [BUG-290](bugs/BUG-290-OPEN.md) | OPEN (DEBTOR) | paint/layout (font metrics; `upright` per-glyph advance) | TEST-145 (writing-mode) residual 3.68% после BUG-289: (1) font-parity Inter vs Edge sans (rule 3, тот же класс что BUG-100/TEST-58); (2) `text-orientation: upright` использует пословный (не per-glyph) вертикальный аванс — Edge раскладывает каждый символ индивидуально. Оба — уже задокументированные, сознательно отложенные пробелы (`docs/tasks/ph3-writing-mode-vertical.md`), не layout/paint-геометрия. `mixed`/`upright`/`sideways` визуально различимы (DoD задачи выполнен). `KNOWN_DEBTORS` baseline. Найдено P1 2026-07-16 |
| [BUG-291](bugs/BUG-291-FIXED.md) | FIXED 2026-07-17 | js (DOM child-node bindings, `crates/js/src/dom.rs`) | Корень — JS-обёртки DOM-узлов не были интернированы: `_lumen_make_element(nid)` строил новый JS-объект на каждый вызов, поэтому `.lastChild`/`.firstChild`/`.parentElement`/`.children` возвращали разные объекты для одного и того же узла (`tbody.lastChild === tr` → `false`), из-за чего `testharness.js`'s `Output.show_results` падал на `tbody.lastChild.lastChild.appendChild(...)` с `TypeError`, обрывая `notify_complete()` до вызова коллбэка, который опрашивает смок-тест. Фикс — `_lumen_node_wrappers` (per-nid кэш, тот же паттерн что `_validity_msg`/`_canvas2d_ctxs`) в общем шиме (`WEB_API_SHIM`, оба движка); безопасно, т.к. arena node-id append-only (без переиспользования слотов) и JS-контекст пересоздаётся с нуля на каждой навигации. Регрессия закрыта юнит-тестом `repeated_node_access_returns_identical_wrapper`. `tests/wpt/run_smoke.py` всё ещё не проходит целиком — по независимой, ранее не задокументированной причине (гонка дефолтной домашней страницы с BiDi `navigate`, см. «Остаток» в файле бага). Смежная находка (querySelector не scoped к вызывающему узлу на detached-поддереве) заведена отдельно как [BUG-298](bugs/BUG-298-FIXED.md) |
| [BUG-292](bugs/BUG-292-OPEN.md) | OPEN | network (`filter/easylist.rs` + filter context в `lib.rs`) | Адблок блокирует топ-левел навигацию на обычные сайты: easylist-правило `/^https?:\/\/[0-9a-z]{5,}\.com\/.*/$script,third-party,xhr,domain=<стриминги>` в ABP-семантике узкое, но в Lumen все три ограничения снимаются разом — нет типа ресурса «document» (у навигации `resource_type: None`, unknown удовлетворяет ограничения), `third_party: None` — то же, `domain=` parsed-and-ignored. Итог: любой голый домен `<слово≥5>.com` (example.com, github.com…) не открывается («blocked: easylist»). Нужен `ResourceType::Document`/`is_top_level` в `RequestContext` + не применять типизированные правила к документу. Найдено 2026-07-16 |
| [BUG-293](bugs/BUG-293-OPEN.md) | OPEN | shell (`main.rs` — дренаж `window.open()` popup-запросов) | Вкладка, открытая `window.open('file:///…')`, не загружается: дренаж popup-запросов заворачивает URL как есть в `PageSource::Url` → сетевой путь → `unsupported scheme: file`. Разбор `file://`→путь уже существует (CLI `PageSource::from_arg`; automation-хелпер `main.rs:517` для BiDi/MCP), но в этом пути не переиспользован. Проверить заодно другие JS-навигации (`location.href`). Учесть security: web→file переход блокировать осознанно, file→file — разрешить. Найдено 2026-07-16 |
| [BUG-294](bugs/BUG-294-OPEN.md) | OPEN | layout (`box_tree.rs`, `lay_out_flex`) | Flex-item с `margin-left` в row-контейнере позиционируется со сдвигом в 2× margin вместо 1×: `lay_out_flex`'s row-ветка передаёт `content_x + main_cursor + m_l` в `lay_out()`, но `lay_out_inner` сама добавляет `margin_left` к переданному `start_x` (как и все остальные вызовы `lay_out` в файле — они передают margin-box start без предварительного добавления margin). Похожая двойная-добавка вероятна и для `margin-top` row-item'ов, и для `margin-left` column-item'ов (тот же паттерн, непроверено — нет существующих flex-тестов с margin на item, все используют `gap`). Найдено P2-DEVX-2 2026-07-16 при написании golden-теста на flex-геометрию |
| [BUG-295](bugs/BUG-295-OPEN.md) | OPEN | bidi-server (`protocol.rs`) + shell live-window wiring | Шесть BiDi-команд (`network.setOfflineStatus`/`addIntercept`/`continueRequest`/`failRequest`, `browser.setTimezoneOverride`, `emulation.setUserAgentOverride`) корректно отвечают по протоколу и реально пишут в `BidiState`, но ни одно поле (`offline`/`intercepts`/`timezone_override`/`*_ua_override`) не читается ничем, что управляет живым окном — `state.live` эти команды не трогают вовсе, в отличие от `browsingContext.navigate`/`script.evaluate`. Live-страница не видит offline-режим, перехват/провал запроса, смену часового пояса или UA. Найдено DEVX-6 2026-07-17 при написании `tests/wpt/verify_devx6_bidi_scenarios.py` |
| [BUG-297](bugs/BUG-297-OPEN.md) | OPEN | test/snapshot (`snapshot_cpu.rs`, `graphic_tests/snapshots/cpu/`) | `cpu_snapshots_match_references` красный на 31 странице, эталоны устарели с 2026-06-30 (801d7640) — все P4-мержи с тех пор (writing-mode, @function, @color-profile, backface-visibility, revert-layer, counter-set, font-size-adjust, contain-intrinsic-size, interpolate-size…) сдвинули CPU-рендер несвязанных страниц без регенерации эталонов. Третий рецидив (BUG-118, BUG-149). Найдено P1-imagebitmap 2026-07-17 в scoped-test гейте перед мержем |
| [BUG-298](bugs/BUG-298-FIXED.md) | FIXED 2026-07-17 | js (`dom.rs` shim, `v8_runtime.rs`, `selector_query.rs`) | `Element`/`DocumentFragment`/`ShadowRoot`.querySelector(All) делегировали в тот же нативный биндинг, что и `document.querySelector` — искали от корня документа, а не от вызывающего узла, поэтому запрос внутри detached-поддерева (типичный паттерн `testharness.js`'s `render()`) всегда возвращал `null`/пусто. Фикс — `query_all_within` + `_lumen_query_selector(_all)_scoped`. Ранее ошибочно принималось за «гонку установки JS-контекста» (см. правку `CLAUDE.md`). Найдено P2-wpt S4 |
| [BUG-299](bugs/BUG-299-FIXED.md) | FIXED 2026-07-17 | js (`dom.rs` shim) | `Element.prototype.insertAdjacentText` отсутствовал вовсе — `testharness.js`'s `Output.show_results` вызывает его безусловно для тестов без записанных assert'ов, падение молча обрывало `completion`-коллбэк-цепочку. Реализован через существующие `before`/`prepend`/`append`/`after`. Найдено P2-wpt S4 |
| [BUG-300](bugs/BUG-300-FIXED.md) | FIXED 2026-07-17 | shell (`main.rs`, `check_wait_condition`) | Фоллбэк-ветка `DocumentReady`-ожидания (`_ => self.layout_box.is_some()`) не проверяла `nav_start`, поэтому могла подтвердить готовность по **устаревшему** `layout_box` предыдущей/начальной вкладки ещё до начала реальной навигации — `browsingContext.navigate` ACK'ал за ~10мс вместо реального ожидания. Фикс — тот же гейт `nav_start.is_none()`, что и в основной ветке. Остаток (специфичный для wptrunner+wptserve) → [BUG-301](bugs/BUG-301-OPEN.md). Найдено P2-wpt S4 |
| [BUG-301](bugs/BUG-301-FIXED.md) | FIXED 2026-07-18 | wptrunner-интеграция (`browsers/lumen.py` `env_options`) | `run_smoke.py` таймаутился, потому что `wptrunner` статик-роутом перекрывает `/resources/testharnessreport.js` своим отчётом (`__wptrunner_message_queue`), а нашего вендоренного (ставит `__lumen_wpt_results`) `wptserve` при этом НЕ отдаёт — `LumenTestharnessExecutor` опрашивает глобаль, которую страница не устанавливает. На простом HTTP-сервере наш файл отдаётся → «вручную работает». Фикс: `env_options` задаёт `testharnessreport` = наш файл. Теперь харнесс реально гоняет тест (subtest FAIL по [BUG-309](bugs/BUG-309-OPEN.md) `setAttributeNS`). Найдено P2-wpt S4 2026-07-17, пофикшено 2026-07-18 |
| [BUG-302](bugs/BUG-302-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | `Element.getElementsByClassName` отсутствует в основном DOM-шиме (есть только в dom_parser.rs) — скрипты news.ycombinator.com падают целиком (`el.getElementsByClassName is not a function`), маскируя дальнейшие несовместимости. Та же семья, что BUG-299. Найден первым прогоном scripts/perf_audit.py (/lumen-perf-audit) |
| [BUG-303](bugs/BUG-303-OPEN.md) | OPEN | js (V8 / event loop) | github.com: JS-исполнение не завершается ≥240с и на V8 (сеть готова за 0.7с) — гипотеза «медленный QuickJS» из аудита 2026-07-02 опровергнута; похоже на незавершающийся цикл из-за отсутствующего API (`Automatic publicPath is not supported`); сайт не должен уметь вешать пайплайн навсегда. Найден /lumen-perf-audit |
| [BUG-304](bugs/BUG-304-OPEN.md) | OPEN | network (резолвер) | DNS os error 11004 (WSANO_DATA) на живых доменах (mc.yandex.ru, top-fwz1.mail.ru, ssp.rambler.ru, static-mon.yandex.net, cdn.skcrtxr.com) — имя резолвится системно, но Lumen просит не тот тип записи (нет фолбэка AAAA→A?); сайты теряют подресурсы. Замечен аудитом 2026-07-02, заведён /lumen-perf-audit 2026-07-17 |
| [BUG-305](bugs/BUG-305-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | Конструктор `Image`/`HTMLImageElement` отсутствует в шиме — `new Image()` (предзагрузка, трекинг-пиксели, canvas) валит скрипты ria.ru целиком (`Image is not defined`). Реализация = алиас `document.createElement('img')`. Найден /lumen-perf-audit |
| [BUG-306](bugs/BUG-306-OPEN.md) | OPEN | js/dom (атрибуция не сделана) | github.com раздувает процесс на ~2.3 ГБ одной вкладкой (610→2904 МБ в живом прогоне; воспроизводимо), после чего сессия умирает (BUG-307). Профиль иной, чем lenta-кэши BUG-272: JS-тяжёлый сайт, вероятно V8-хип/удержание DOM шимом. Найден живым /lumen-perf-audit |
| [BUG-307](bugs/BUG-307-OPEN.md) | OPEN | shell (main thread) | UI-поток «не отвечает» (IsHungAppWindow): обратимо 39–48 с на ria/rbc, НЕОБРАТИМО после вкладок-гигантов github+stackoverflow — сессия мертва, даже navigate по таймауту; для пользователя «браузер завис, убивать процесс». Несмотря на ADR-016 M0–M4 часть путей блокирует event loop. Найден живым /lumen-perf-audit + наблюдение пользователя |
| [BUG-308](bugs/BUG-308-OPEN.md) | OPEN | shell (навигация) | Страница с HTTP-ошибкой (403 антибот, w3.org) держит document_ready 129–205 с в живом окне, хотя headless отдаёт тот же 403 за 0.75 с — вкладка «вечно грузится» вместо мгновенной страницы ошибки. Найден живым /lumen-perf-audit |
| [BUG-309](bugs/BUG-309-OPEN.md) | OPEN | js (`dom.rs` `WEB_API_SHIM`) | `Element.prototype.setAttributeNS`/`getAttributeNS`/`removeAttributeNS` и прочие `*NS`-аксессоры атрибутов отсутствуют — `el.setAttributeNS(...)` кидает `TypeError`. Первый реальный движковый пробел, всплывший после починки [BUG-301](bugs/BUG-301-FIXED.md): WPT `dom/nodes/Element-hasAttribute.html` гоняется до конца, subtest 1 честно FAIL. Записан `expected: FAIL` в metadata. Найдено P2-wpt 2026-07-18 |
| [BUG-310](bugs/BUG-310-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | ElementTraversal (`childElementCount`/`firstElementChild`/`lastElementChild`/`nextElementSibling`/`previousElementSibling`) и `ParentNode.children` отсутствуют в шиме — возвращают `undefined`, `.children` — голый массив без `.item()`. 10 WPT `dom/nodes` тестов, `expected: FAIL`. Найдено P2-wpt S5 2026-07-18 |
| [BUG-311](bugs/BUG-311-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | `Node.prototype.isConnected` отсутствует — возвращает `undefined` вместо булева. WPT `dom/nodes/Node-isConnected.html`, `expected: FAIL`. Найдено P2-wpt S5 2026-07-18 |
| [BUG-312](bugs/BUG-312-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | `Element.prototype.hasAttributes()` отсутствует — `el.hasAttributes is not a function`. WPT `dom/nodes/Element-hasAttributes.html`, `expected: FAIL`. Найдено P2-wpt S5 2026-07-18 |
| [BUG-313](bugs/BUG-313-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | `document.createProcessingInstruction` отсутствует + нет валидации Name/`?>` (`InvalidCharacterError`). WPT `dom/nodes/Document-createProcessingInstruction.html` 11/12 `expected: FAIL`. Найдено P2-wpt S5 2026-07-18 |
| [BUG-314](bugs/BUG-314-OPEN.md) | OPEN | js (WEB_API_SHIM, dom.rs) | DOM-конструкторы (`Comment`/`Text`/`DocumentFragment`/`DocumentType`/`Document`/`ProcessingInstruction`/`HTML*Element`) не выставлены как глобальные интерфейсы — `X is not defined`/`window[ctor] is not a constructor`. Семья [BUG-305](bugs/BUG-305-OPEN.md). WPT `DocumentFragment-constructor`/`Document-doctype`, `expected: FAIL`. Найдено P2-wpt S5 2026-07-18 |
| [BUG-315](bugs/BUG-315-FIXED.md) | FIXED 2026-07-17 (renamed from BUG-295, then BUG-310 — second number collision resolved during a follow-up merge 2026-07-18) | `tools/wptrunner/wptrunner/browsers/lumen.py` + `crates/shell/src/main.rs` | Две независимые причины на один симптом (`run_smoke.py` TIMEOUT): (1) `TestEnvironment.get_routes` в вендоренном `environment.py` всегда подменяет `/resources/testharnessreport.js` дженериковым файлом wptrunner, если продукт-плагин не передаёт `env_options()["testharnessreport"]` — Lumen не передавал, страница молча гоняла чужой репортер, никогда не трогающий `window.__lumen_wpt_results`. Фикс — `env_options()` теперь передаёт абсолютный путь к нашему `testharnessreport.js`. (2) BiDi/MCP-сессии шарили persistent on-disk HTTP-кэш с обычным браузингом — на фиксированных портах `wptserve` (8000/8001) с `Cache-Control: max-age=3600` это давало вечное переигрывание устаревшего тела ответа между прогонами независимо от (1). Фикс — `no_persistent_state=true` для `--bidi-port`/`--mcp*` сессий, решается ДО `config::init_global` (raw-скан `std::env::args()`, профиль — set-once `OnceLock`). `run_smoke.py` теперь доходит до `TEST_END: Test OK. Subtests passed 1/2`. Пересекается с [BUG-301](bugs/BUG-301-FIXED.md) (та же route-часть фикса, найдена независимо), но здесь дополнительно исправлена вторая, самостоятельная причина — poisoning persistent-кэша |
| [BUG-316](bugs/BUG-316-OPEN.md) | OPEN (renamed from BUG-296, then BUG-311 — second number collision resolved during a follow-up merge 2026-07-18) | driver (`crates/driver/tests/cases/snapshot_cpu.rs`, feature `cpu-render`) | `cases::snapshot_cpu::cpu_snapshots_match_references` падает (~30 страниц, большие расхождения в байтах) при совместном тестировании `lumen-driver`+`lumen-shell` (только так активируется `cpu-render` — `cargo test -p lumen-driver` соло эту фичу не включает и модуль вообще не компилирует). Подтверждено предсуществующим: идентично падает на `main` тем же комбинированным прогоном, не связано ни с одной веткой. Обнаружено `scripts/scoped-test.sh` гейтом при мердже BUG-291 (первый раз кто-то прогнал driver+shell вместе). Не диагностировано: дрейф эталонов `graphic_tests/snapshots/cpu/` vs реальная регрессия растеризатора — вероятно тот же дрейф, что и в [BUG-297](bugs/BUG-297-OPEN.md) (более полная диагностика, найдена независимо около того же времени — возможный дубликат, не подтверждено). Найдено P2-bug291 2026-07-17 |
| [BUG-317](bugs/BUG-317-OPEN.md) | OPEN (renamed from BUG-315 — third-time number collision with `origin/main`'s real BUG-315 above, resolved during the S6/S7 merge-back 2026-07-18) | js (WEB_API_SHIM, dom.rs) | `MutationRecord` не выставлен как глобальный интерфейс — `MutationRecord is not defined` (колбэк MutationObserver срабатывает асинхронно, но записи нельзя проверить через `instanceof`). Семья [BUG-314](bugs/BUG-314-OPEN.md). WPT `dom/nodes/MutationObserver-callback-arguments.html`, `expected: FAIL`. Найдено P2-wpt S6 2026-07-18 |
| [BUG-318](bugs/BUG-318-OPEN.md) | OPEN (renamed from BUG-316 — collided with `origin/main`'s BUG-316 above, resolved during the S6/S7 merge-back 2026-07-18) | js (WEB_API_SHIM, dom.rs) | MutationObserver: некорректный учёт записей (дубли/лишние, `takeRecords()` не очищает очередь) + subtree-мутации не наблюдаются (async-тест уходит в TIMEOUT). WPT `dom/nodes/MutationObserver-takeRecords.html` (`expected: FAIL`), `MutationObserver-disconnect.html` (`expected: TIMEOUT`). Найдено P2-wpt S6 2026-07-18 |
| [BUG-319](bugs/BUG-319-OPEN.md) | OPEN (renamed from BUG-317, freed up by the two renumberings above, 2026-07-18) | bidi-server (protocol.rs) | `script.evaluate` игнорирует `awaitPromise`: при `awaitPromise:true` возвращает сам объект промиса (`{}`), а не разрешённое значение. Пайплайн WPT не затронут (executor использует `awaitPromise=false`+polling). Проверено `tests/wpt/verify_s6_await_promise.py`. Найдено P2-wpt S6 2026-07-18 |
| [BUG-320](bugs/BUG-320-OPEN.md) | OPEN | paint (femtovg, `acquire_layer`/`layer_pool`) | `mix-blend-mode` (все режимы кроме `normal`/`plus-lighter`) не рендерится вовсе на `main`: `acquire_layer()` сайзит offscreen-слой по `self.width`/`self.height` (физический размер окна), а не по размеру АКТИВНОГО render-таргета — во время scroll-blit band-рендера (ADR-016 M3.2.1b) это меньше band-FBO, из-за чего `src_rgba.len() != backdrop_rgba.len()` в `composite_blend_layer` и CPU-блендинг молча пропускается. Воспроизведено на чистом `merge-base` (не регрессия BUG-272 срез 14 — срез 14's bbox-путь для `PushBlendMode` случайно обходит баг, не чинит его намеренно). Найдено визуальной A/B-приёмкой BUG-272 срез 14 2026-07-19 |

---

## Ограничения Phase 0 (не баги — запланировано позже)

Тесты, перешедшие в PASS: TEST-27 (direction-rtl), TEST-29 (@container), TEST-35 (grid), TEST-37 (float).

| Фича | Фаза | TEST (последний прогон) |
|---|---|---|
| `position:absolute/fixed/relative` | Phase 1 | — |
| CSS-анимации / transitions | Phase 2 | — |
| HiDPI / DPR-масштабирование | Phase 1 | — |
| `column-count` / `column-width` (multi-column) | Phase 1 | TEST-33: 16.14% → BUG-083 |
| `mask-image` | Phase 1 | TEST-26: 20.26% → BUG (Phase 1) |
| `contain:` CSS containment | Phase 1 | TEST-28: 1.82% → BUG (Phase 1) |
| Form controls UA styles | Phase 1 | TEST-34: 4.78% → BUG (Phase 1) |
| `clip-path: circle/ellipse/polygon` — точная форма | Phase 1 | TEST-31: 8.85% (bbox работает) |
| SVG rendering | Phase 1 | TEST-47: FIXED 2026-06-09 → BUG-089 |
| SVG `<path>` stroke | Phase 1 | TEST-54: FIXED 2026-06-09 → BUG-096 |
| SVG stroke advanced | Phase 1 | TEST-60: FIXED 2026-06-17 → BUG-102 (остаток → BUG-173, KNOWN_DEBTORS) |
| `<canvas>` 2D context | Phase 2 | TEST-57: 28.66% → BUG-099 |
| View Transitions API | Phase 2 | TEST-61: рендерится (F2-4); --ipc 10.71% = Edge-тайминг+текст → BUG-103 KNOWN_DEBTOR |
| CSS Scroll Snap | Phase 1 | TEST-62: FIXED 2026-06-19 → BUG-104 (column flex-grow; остаток 2.32% → BUG-128, KNOWN_DEBTORS) |
| CSS Masonry | Phase 2 | TEST-63: 26.13% → BUG-105 |
